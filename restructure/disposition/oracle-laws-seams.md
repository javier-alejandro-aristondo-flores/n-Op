# Disposition — oracle laws and seams

Scope: `journal/pages/03-laws-and-residuals/{3.1-generic-dynamics, 3.2-residual-definitions, 3.3-coupling-structure}.md`
and `journal/pages/07-consumers-and-seams/{7.1-pino-bridge, 7.2-residual-machinery, 7.3-cross-cutting-rules}.md`
Read at: 2af93d2

Checker calibration performed (scratch copy at `/tmp/.../scratchpad/cal1`, baseline green there):

| probe | planted | result |
|---|---|---|
| bracketed ref to nonexistent page | `[nonexistent-page-xyz]` | **fires** |
| backticked ref to nonexistent page | `` `nonexistent-page-xyz` `` | **silent** |
| bare id + § to nonexistent page | `nonexistent-page-xyz §2` | **silent** |
| § coordinate that does not resolve on a real page | `` `residual-definitions §99` `` | **fires** |
| duplicate `canonical-for` topic across two pages | — | **fires** |
| `canonical-for` topic identical to the page id | — | **silent** (no vacuity check exists) |
| verbatim duplicated prose block across two pages | — | **silent** |
| corrupted value in scope (diamond metastability +25 → +250 meV/atom) | — | **silent** — `check_data_agreement.py` sweeps `journal/pages/` but no rule in it reads this value |
| corrupted value in scope (diamond AHC −345 → −545 meV) | — | **silent**, same reason |
| category count on its *owning* page, "nineteen" → "twenty-one" | — | **silent** (see row 24) |

So: every citation-resolution claim below is checker-backed; every **value** in this scope
is **unchecked** and was verified by hand against the registry CSV and `9.1-accuracy-ledger`
where a source exists, or marked unverifiable.

---

## Disposition rows

| # | Fact or block | Current location | Disposition | Target | Evidence |
|---|---|---|---|---|---|
| 1 | Frontmatter `authority: canon`, `content-hash`, `tag` — all six pages | `3.1-generic-dynamics.md:4-5`, `3.2:4-5`, `3.3:4-5`, `7.1:4-5`, `7.2:4-5`, `7.3:4-5` | delete | — | D1/§4: the two-tier `authority` system dies with ch. 11; `content-hash` is a stamp git already answers. Six pages × 2 keys. |
| 2 | `referenced-by` blocks (all six pages) | same files, frontmatter | delete | — | §4: `referenced-by` stays generated, never hand-written. Regenerated into `corpus.json`. |
| 3 | GENERIC equation `dx/dt = L·δE/δx + M·δS/δx`, the four symbols, and the two degeneracy conditions | `3.1-generic-dynamics.md:38-51` | keep | `oracle/laws/generic-dynamics#generic-form` | sole statement; 16 pages list `generic-dynamics` in `depends-on` |
| 4 | Every regime is an **extraction** of the one equation; static = equilibrium readout, time-evolving = trajectory readout; the EOM-violation residual is the structural ground of the others; `/physics` emits granular, aggregation lives in `/informed-operator` | `3.1-generic-dynamics.md:53-61` | keep | `oracle/laws/generic-dynamics#extraction-principle` | sole statement of the extraction principle. The trailing granularity sentence duplicates `residual-definitions` intro and `pino-bridge §1` — see row 5. |
| 5 | "`/physics` emits the full granular residual vector; aggregation into a scalar training objective lives in `/informed-operator`" | `3.1-generic-dynamics.md:60-61` | delete | (cite `oracle/laws/residual-definitions#granularity`) | third verbatim-in-substance statement of one fact. Also at `3.2:55-58`, `7.1:58-60`, `7.2:220-222`, `1.2-product.md:150-152`. Duplication is invisible to both checkers (calibration row 7). |
| 6 | The `E[x]` decomposition (5 terms) and `S[x]` decomposition (3 terms), including the `E_EM` transverse-only annotation | `3.1-generic-dynamics.md#canonical-functionals-and-operators:65-82` | keep | `oracle/laws/generic-dynamics#functionals` | sole statement |
| 7 | The `L` decomposition (6 blocks) and `M` decomposition (2 blocks), with MVP channel sets | `3.1-generic-dynamics.md:84-102` | keep | `oracle/laws/generic-dynamics#operators` | sole statement |
| 8 | The E/S/L/M pieces are assembled across the four BO levels; each level contributes terms acting on its irreducible state | `3.1-generic-dynamics.md:104-105` | keep | `oracle/laws/generic-dynamics#operators` | one-sentence bridge to `born-oppenheimer-levels`; no other statement |
| 9 | The nine-regime extraction table | `3.1-generic-dynamics.md#the-nine-regimes-as-extractions:109-119` | keep | `oracle/laws/generic-dynamics#nine-regimes` | sole statement; page is `canonical-for` "nine regimes as extractions" |
| 10 | Pointer: per-regime derivations are "in the canon chapters (especially `typed-compositions`)" | `3.1-generic-dynamics.md:121-122` | delete | (cite `oracle/registry/typed-compositions`) | "the canon chapters" is a pre-book chapter-numbering phrase with no referent in a flat-journal tree. Verified **not** find/replace damage: `2f1d22f^` has the same words with `impl-06-compositions` in the parenthetical. The substantive pointer (`typed-compositions`) survives as a citation. |
| 11 | Pointer: derivations "grounded in `physics/research/group-{A,B,C}-*.md`" | `3.1-generic-dynamics.md:123` | delete | — | **dangling path.** `physics/research/` contains only `diamond-defect-corpus/` and `diamond-stretch-and-skew-sweep/`; no `group-*` file exists anywhere in the repo (`find` over the whole tree). Neither checker looks at filesystem paths. Same class as the plan's defect 2. |
| 12 | Generator structure is **per-tier**: the two-generator form and its degeneracy conditions hold per tier / per BO level, not as one global bracket | `3.1-generic-dynamics.md#generator-structure-is-per-tier:127-132` | keep | `oracle/laws/generic-dynamics#per-tier-generators` | sole statement; this is the reconciliation that makes the written functionals consistent with the degeneracy conditions |
| 13 | The `γ̂`-block of `L` is Lie–Poisson, `{A,B}(γ̂) = Tr(γ̂·[δA/δγ̂, δB/δγ̂])`, written `[·, γ̂]` not `[Ĥ_KS, ·]`; Jacobi and degeneracy hold by construction | `3.1-generic-dynamics.md:134-139` | keep | `oracle/laws/generic-dynamics#per-tier-generators` | sole statement. Note the L-block list at `:90` still writes the bare `(1/iℏ)[Ĥ_KS, ·]` form this bullet corrects — **contradiction row 3**. |
| 14 | L2 is single-generator (Hamiltonian) at fixed entropy; `S_vib(R,h)` is slow/parametric; the apparent `L·δS_vib/δR ≠ 0` is not a degeneracy violation | `3.1-generic-dynamics.md:140-146` | keep | `oracle/laws/generic-dynamics#per-tier-generators` | sole statement |
| 15 | Jacobi status per `L`-block: canonical blocks exact; generated `AntisymmForm` cross-blocks not automatic; V1 restricts to semidirect-product/Lie–Poisson class or flags | `3.1-generic-dynamics.md:148-154` | keep | `oracle/laws/generic-dynamics#jacobi-status` | sole statement; `build-sequence` Phase-8 depends on it |
| 16 | `Degeneracy` is cert-only, identically zero by construction, a generator-construction-bug tripwire — not a PINO loss term | `3.1-generic-dynamics.md:156-159` | move | `oracle/laws/residual-definitions#categories` (category 8) | the fact is about a residual *category*, and `residual-definitions §1` item 8 already states it. Keep one copy, on the page that owns the category vocabulary; `generic-dynamics` cites it. |
| 17 | The clause "(removed from the `residual-definitions §4.1` training gate)" | `3.1-generic-dynamics.md:159` | delete | — | scaffolding: narrates an edit. The present-tense fact (cert-only, never a training residual) survives in row 16. |
| 18 | `E`-functional activation is level-conditional: L1 uses `E_KS[γ̂; R₀,h₀]` parametric in frozen geometry; L2 replaces it with `E_BO = min_γ̂ E_KS`; the e-ph channel contributes only the beyond-reference part (no double-count) | `3.1-generic-dynamics.md:161-167` | keep | `oracle/laws/generic-dynamics#level-conditional-activation` | sole statement; the double-count guard is load-bearing |
| 19 | **Normative** gauge fixing and electrostatic partition: Weyl gauge `A₀≡0` + transversality `∇·A=0`; `E_EM` counts the transverse sector only; the longitudinal/electrostatic sector is owned by the matter functionals; consequences for the `EOM/A` residual and the minimal-coupling channel | `3.1-generic-dynamics.md#gauge-fixing:169-182` | keep | `oracle/laws/generic-dynamics#gauge-partition` | sole normative statement. `unified-state:42-44` explicitly defers here, so this is the real owner of the gauge convention — **relevant to R1's dangling promise, row 62.** |
| 20 | The trailing provenance stamp "(2026-07 gap-audit A2.)" | `3.1-generic-dynamics.md:182` | delete | — | D2: pages carry no history. See **Log-worthy** row 2. |
| 21 | Residuals are the physics-informed loss terms `/informed-operator` trains against; realized as `OutputRole = ResidualLeaf(key)`; emission discipline is granular and `/physics` never preaggregates | `3.2-residual-definitions.md:52-58` | keep | `oracle/laws/residual-definitions#granularity` | canonical statement of the granularity discipline; the target of rows 5, 44, 63 |
| 22 | The taxonomy is **nineteen** categories = 9 EOM + 3 GENERIC-structural + 5 algebraic + 2 constraint; a facet on each contribution, not a granularity floor or a weighting unit | `3.2-residual-definitions.md#1-the-nineteen-categories:59-67` | keep | `oracle/laws/residual-definitions#categories` | page is `canonical-for` "the nineteen residual categories"; cited by 7 pages |
| 23 | "the pre-multiscale count was seventeen — that is where the older 'seventeen' figure comes from, and it is not a second, smaller taxonomy" | `3.2-residual-definitions.md:64-67` | delete | — | scaffolding: a guard against a stale number. `grep -rn seventeen` over `journal/` + `informed-operator/` returns **this line only** — the figure it defends against no longer exists in the corpus, so the guard defends nothing. Removing it is safe only because that grep is empty; **Phase 2 must re-run it before deleting.** |
| 24 | The `CategoryTag` enum is the closed set of **19** symbols | `3.2-residual-definitions.md:180-183` | keep | `oracle/laws/residual-definitions#categorytag` | **checker hole:** `check_book_structure.py:333` matches only `(\d+)\s+residual categories`. This page states its count as the word "nineteen" (`:9`, `:59`, `:61`) and as "**19** symbols" (`:180`) — none matches. Planting "twenty-one" on the owning page passes clean, while the same corruption on any *citing* page (`1.5-rationale.md:77`, `4.1-physics-graph.md:178`, `8.7-build-verification.md:36`) fires. The one page that defines the count is the one page unchecked. Phase 2: state it in the checked phrasing. |
| 25 | The 9 EOM-violation categories (7 micro DOF + `EOM/DefectPopulation` + `EOM/Continuum`), their aggregate form, and the axis-structure rule (union of diagonal-operator axes and active `CouplingChannel` `InvariantTerm` axes; no per-coupling residual category) | `3.2-residual-definitions.md:69-99` | keep | `oracle/laws/residual-definitions#eom-categories` | sole statement; the axis-structure rule is the join to `coupling-structure` |
| 26 | The 3 GENERIC-structural categories: `Degeneracy` (cert-only), `Conservation` (incl. static γ̂-trace admissibility `‖Tr γ̂ − N_e‖²`), `Positivity` (incl. `T_e ≥ T_L` row 72, breakdown integral row 75, γ̂ N-representability, applicability-gated `ω²≥0` and `‖γ̂²−γ̂‖²`) | `3.2-residual-definitions.md:101-129` | keep | `oracle/laws/residual-definitions#structural-categories` | sole statement. Registry rows 72 (`hot-carrier-temperature-balance`) and 75 (`avalanche-multiplication`) verified present in `registry-manifest.csv`. |
| 27 | The admissibility argument — a candidate γ̂ outside the bounds can zero every EOM residual while being unphysical; these gates are what make the oracle sound as a verifier of the state itself | `3.2-residual-definitions.md:126-129` | keep | `oracle/laws/residual-definitions#structural-categories` | sole statement; load-bearing for the verifier claim |
| 28 | Trailing stamp "(2026-07 gap-audit A1)" | `3.2-residual-definitions.md:128` | delete | — | D2. See **Log-worthy** row 1. |
| 29 | Citation `gamma-hat §5` for "extreme eigenvalues per block are cheap" | `3.2-residual-definitions.md:123-125` | move | re-cite `oracle/state/gamma-hat#encoding-vocabulary`, or state the cost claim here | **dangling promise (new).** `gamma-hat §5` is "Diamond MVP commitment" — it gives the `(Reciprocal, BlockDiag)` encoding and a ~18 MB sizing and says **nothing about eigenvalue-extraction cost.** The checker passes because §5 exists. Second confirmed instance of the plan's defect class, alongside R1→`unified-state` (row 62). |
| 30 | The 5 algebraic-identity categories (Kramers–Kronig, SumRules incl. rotational sum rule row 126, BalanceLaws incl. Wegscheider row 125, Symmetries, MethodEquivalence) | `3.2-residual-definitions.md:131-144` | keep | `oracle/laws/residual-definitions#algebraic-categories` | sole statement. Rows 125/126 verified in the CSV. |
| 31 | The **equivalence-pair vs consistency-pair** distinction: `τ_equiv` trips on any disagreement, `τ_method` only on excess beyond a declared model gap; κ rows 121–122 bind to row 25 as a consistency pair | `3.2-residual-definitions.md:145-156` | keep | `oracle/laws/residual-definitions#pair-kinds` | **this is the canonical owner**, and it is the target for rows 68, 76 and for `capability-slices:84` / `named-formulas:163`, which currently cite `cross-cutting-rules` for it |
| 32 | The 2 constraint-violation categories, disjoint by input-domain type; `Static/Thermodynamic` incl. the T,P-aware metastability band (diamond +25 meV/atom @300 K, 1 atm, Berman–Simon point, σ 5 meV/atom, row 124) and the three slow-tier consistency identities | `3.2-residual-definitions.md:158-178` | keep | `oracle/laws/residual-definitions#constraint-categories` | sole statement. **Values unchecked** — calibration confirms `check_data_agreement.py` does not read the +25 meV/atom figure. Row 124 (`tp-aware-hull`) resolves. |
| 33 | Categories 16/17 stay disjoint because they consume type-distinct inputs, **and the PINO curriculum schedules them differently for that reason** | `3.2-residual-definitions.md:176-178` | keep | `oracle/laws/residual-definitions#constraint-categories` | the justification is load-bearing for §4.1's Polish phase; keep the two clauses together |
| 34 | `ResidualKey = (producer, axes)`; `Producer = Formula \| Method`; `ContributionFacets = (category, bundle, dressing)` as a non-identity sidecar; PINO holds `Map<ResidualKey, Weight>`; weights persist across recompiles | `3.2-residual-definitions.md#2-the-atomic-unit:186-207` | keep | `oracle/laws/residual-definitions#residualkey` | canonical; `7.2 §2` restates it verbatim — see row 47 |
| 35 | Substrate-cluster placement of `ResidualKey` (C5 `ContentAddress`), `CategoryTag`/`BundleId`/`AxisLabel` (C1 `Universe[T]`), `ContributionFacets` (C3 sidecar fiber) | `3.2-residual-definitions.md:209-213` | keep | `oracle/laws/residual-definitions#residualkey` | sole statement of this mapping |
| 36 | The five worked examples of a separately-weightable contribution, and the three negative examples | `3.2-residual-definitions.md#3-examples:215-228` | keep | `oracle/laws/residual-definitions#granularity-examples` | sole statement; the negatives are what make the discipline operational |
| 37 | `evaluate : (State, Environment) → (residuals, gradient, …)`; aggregation (per-category sums, GradNorm, RAD, per-bundle schedules, curriculum gating) lives in `/informed-operator` | `3.2-residual-definitions.md#4-output-type:230-243` | keep | `oracle/laws/residual-definitions#output-type` | sole statement of the kernel's output type. `Environment` appears here as an unowned type — see **Open questions** `environment-record-owner`. |
| 38 | **The curriculum gating schedule** — `[0,0.10)` Warmup: Conservation+Positivity; `[0.10,0.60)` Refine: +EOM/\* and Algebraic/\* except MethodEquivalence; `[0.60,0.90)` Polish: +MethodEquivalence, Static/Snapshot, Static/Thermodynamic; `[0.90,1.00]` Cooldown: weights frozen — plus the four-clause rationale | `3.2-residual-definitions.md#41-curriculum-gating-defaults:245-266` | keep | `oracle/laws/residual-definitions#curriculum-gate` | **stays oracle-side.** The gate is keyed on `CategoryTag`, the oracle's own closed vocabulary, and answers "when is this residual *meaningful*" — a property of the residual surface, not of the operator's data diet. `open-decisions` records it closed at `0.10/0.60/0.90`. |
| 39 | The schedule is a normative **default**, not a contract; `/informed-operator` declares its own `Map<CategoryTag, GateSchedule>` to override | `3.2-residual-definitions.md:268-270` | keep | `oracle/laws/residual-definitions#curriculum-gate` | the override clause is what keeps the fact oracle-side without over-reaching into the operator |
| 40 | The denominator phrase "fraction ∈ [0, 1] of **total training budget**" | `3.2-residual-definitions.md:253` | move | `oracle/laws/residual-definitions#curriculum-gate`, **restated as a fraction of the informed stage** | the stage ordering (row 41) says the oracle attaches for one stage only, so these fractions cannot be fractions of total training. `training-stages.md:17` reads them as within-stage. **Contradiction row 1** — flagged, not adjudicated; the builder must not copy this phrase forward unresolved. |
| 41 | **The training stage ordering** — (1) supervised VASP epochs narrow the space, (2) one informed epoch where the oracle scores and returns keyed residuals + gradients, (3) inference with no oracle call; plus *why* (a residual is a local signal, useless over a large space; the oracle refines, it does not search) and the four consequences (oracle is training-time machinery, nothing in the inference path calls `Validate`, any diagram showing a permanent oracle loop is wrong, physics residuals' place among the four supervisory sources) | `informed-operator/design/training-stages.md:10-41` **and** `journal/live/presentations/2026-07-22-prep.md:286-296` — **no page in `journal/pages/` states it** | mine | `operator/training/stage-ordering` | **the confirmed homeless fact, located.** Owner must be operator-side: it is about the operator's epochs and its deployed form, and it constrains the oracle only by saying when the oracle is absent. The oracle journal owns the *gate inside* stage 2 (row 38); the operator journal owns *the stages*. The boundary is exactly: **`CategoryTag` participation = oracle; epoch sequence and the data source driving each epoch = operator.** Note the source file carries retired ids `arch-11-residuals §11.4.1` and `arch-18-open-decisions` (`:5,:17`) — mine the content, not the citations. |
| 42 | Granularity composes with hash-consing: Stage-3 hash-consing already shares upstream DAG; granularity adds only individually-addressable leaves; full `Map<ResidualKey, Scalar>` in one forward pass at no extra cost; reverse-mode gives per-key gradients by structural projection | `3.2-residual-definitions.md#5:272-283` | keep | `oracle/laws/residual-definitions#hash-consing` | sole statement; this is the cost argument that makes granularity affordable |
| 43 | Closure: contributions are unbounded, *generators* are countable (one per `(formula, applicability cell)` + cert-only and ground-truth-bridge subtypes); the closed-vocabulary discipline holds at the generator level | `3.2-residual-definitions.md#6:285-291` | keep | `oracle/laws/residual-definitions#closure` | sole statement; reconciles granularity with the closed-vocabulary claim |
| 44 | Per-residual error composition: every generator carries `characteristic-scale : σ`, a **declared scale not a fitted weight**, composed by `Quantity.combineTol` into a per-`ResidualKey` budget summing input σ + model-form error + Stage-4 truncation + dressing staleness + coefficient-provenance σ | `3.2-residual-definitions.md#7:293-306` | keep | `oracle/laws/residual-definitions#error-budget` | canonical; the target of `7.2 §1`'s `characteristic-scale` comment and `7.2 §4.1`/`§7`'s flow-into statements |
| 45 | MVP headline design-grade targets (gap ±0.15 eV post-G₀W₀, C_ij ±5%, κ(300 K) ±20%, E_form ±0.2 eV, μ factor-2) restated here | `3.2-residual-definitions.md:308-311` | delete | (cite `oracle/accuracy/accuracy-ledger#mvp-targets`) | the sentence itself says the ledger is canonical. Restating five of them here is the same pattern that the next sentence documents as having already caused drift. Values **unchecked** by the data checker either way. |
| 46 | "Every numeric tolerance named across `/physics` is valued once in the tolerance ledger (`cert-obligations §1.2`), which is canonical for the list. **Restating the enumeration here is what let it drift — it named ten symbols against a ledger of sixteen.**" | `3.2-residual-definitions.md:311-314` | delete | — | scaffolding (narrates a past defect). But the *rule* it protects — one home per tolerance — is the restructure's whole thesis; it belongs in `practice/agent-contract` as a rule, not here as a war story. See **Log-worthy** row 3. |
| 47 | `ResidualKey` / `Producer` definition and the `Map<ResidualKey, Weight>` ↔ `Map<ResidualKey, Scalar>` split, restated | `7.2-residual-machinery.md#2-granularity:98-116` | delete | (cite `oracle/laws/residual-definitions#residualkey`) | the heading itself says "canonical reference: `residual-definitions`" and then restates the block verbatim. Pure duplication; invisible to both checkers. **Exception:** the last three lines — the `dressing` facet is a provenance label and bare/dressed residuals live as *distinct* `FormulaApply`/`MethodInvoke` chains, not weighted siblings — are stated only here. See row 48. |
| 48 | Bare and dressed residuals on the same observable live as **distinct graph chains**, not as weighted siblings | `7.2-residual-machinery.md:112-116` | move | `oracle/compilation/physics-graph#dressing-chains` | sole statement, and it is a fact about graph construction, not about the factory |
| 49 | The `CouplingChannel` object: a symmetry-respecting function from a tensor product of state pieces into `Scalar \| AntisymmForm \| PSDSymmForm`, landing in `E_coupling` / off-diagonal `L` / off-diagonal `M`; every cross-regime term in `generic-dynamics` is one instance | `3.3-coupling-structure.md#1-the-object:52-73` | keep | `oracle/laws/coupling-structure#target-shapes` | sole statement; page is `canonical-for` "coupling target shapes" |
| 50 | `record CouplingChannel` and `record StatePiece`; the `(component, sub-dof)` alphabet; `order`/`derivative` as compose-time truncation choices; the four coverage-policy fields and their roles | `3.3-coupling-structure.md#2-the-parameter-axes:75-118` | keep | `oracle/laws/coupling-structure#channel-record` | canonical; `7.3 §4` restates the first half — see row 77 |
| 51 | "The allowed `(component, sub-dof)` pairs are tabulated alongside the state-component definition" | `3.3-coupling-structure.md:100-102` | move | `oracle/state/unified-state#sub-dof-table` — **and the table must be written** | **dangling promise (third instance).** `unified-state` has no such table: it lists the 7 slots and the `StateComponent` universe and stops. The pointer is prose, so no checker sees it. Same class as R1 (row 62) and `gamma-hat §5` (row 29). |
| 52 | `generate-invariants : CrystalSymmetryGroup × CouplingChannel → GeneratorOutput`; `record InvariantTerm`; `record GeneratorOutput`; the echoed `polynomial_sufficient` flag; constructive-vs-decompositional framing against Stage 2 | `3.3-coupling-structure.md#3-the-invariant-generator:120-170` | keep | `oracle/laws/coupling-structure#invariant-generator` | sole statement; page is `canonical-for` "invariant-generator routine" |
| 53 | The generator **contract**: three well-formedness guards, the O(1) spinor-parity pre-prune, then the trivial-irrep projector | `3.3-coupling-structure.md:172-186` | keep | `oracle/laws/coupling-structure#generator-contract` | sole statement |
| 54 | Emptiness and complexity: character inner product `⟨χ_T, χ_trivial⟩_G`; MVP worst case `\|G\| ≤ 192`, `dim(T) ≤ ~250`; pre-prune ≤ ~200 ops, Reynolds projection ≤ ~12M ops; cached on `Address[CrystalSymmetryGroup] × Address[CouplingChannel]`; cache key excludes theory context | `3.3-coupling-structure.md:188-198` | keep | `oracle/laws/coupling-structure#generator-cost` | sole statement. Figures **unchecked** by the data checker; `\|G\| ≤ 192` agrees with `open-decisions`' GAP requirement line. |
| 55 | "Emptiness ≠ correctness" — a non-empty `poly` may still be only the short-range part | `3.3-coupling-structure.md:200-203` | keep | `oracle/laws/coupling-structure#generator-cost` | live hazard guarding a real confusion; candidate for cross-listing in `practice/traps` |
| 56 | Worked example: diamond electron-phonon channel declaration and its four compose-time steps | `3.3-coupling-structure.md#4-worked-example:205-231` | keep | `oracle/laws/coupling-structure#worked-example` | sole worked example of the whole mechanism |
| 57 | **Symmetry generates the form; provenance supplies the values.** The symmetry group fixes which invariants exist and their index structure; deformation potentials / Fröhlich / anharmonic parameters come from the `ProvenanceLedger` | `3.3-coupling-structure.md:233-241` | keep | `oracle/laws/coupling-structure#form-vs-values` | the load-bearing separation; §8's provenance contract depends on it |
| 58 | Stage 2.5 — invariant synthesis: inputs, action, outputs, and its position between Stage 2's block-diagonalization and Stage 3 | `3.3-coupling-structure.md#5-stage-25:243-259` | move | `oracle/compilation/compose-time-pipeline#stage-2-5` | a pipeline stage defined on a laws page. `compose-time-pipeline` is the pipeline's owner and already carries stages 1–4; the sub-stage should live with its siblings and `coupling-structure` should cite it. **Nomenclature defect (§4 of the plan): "Stage 2.5" inside a "4+1 stage" pipeline — propose a rename to Javier, do not act.** |
| 59 | Composition rules: direct sum within a target; E/L/M assembly across targets; monotone order truncation; **no channel-correlation primitive in V1** (correlated mechanisms are one channel with more pieces); kernel extensions add as one more summand, so a long-range mechanism splits into two channels (Verdi–Giustino SR/LR split) | `3.3-coupling-structure.md#6-composition:261-286` | keep | `oracle/laws/coupling-structure#composition` | sole statement; the V1 additivity restriction is a real design commitment |
| 60 | Cert hooks: obligations 1, 2, 5 collapse to projection-residual checks; `kernel_extension` is not exempt (little-group equivariance `‖K(Rq,ω) − D(R)K(q,ω)D(R)†‖ < δ_sym`); target-tag-determined projection rule; PSD guard on the assembled per-mechanism super-block, not per off-diagonal kernel | `3.3-coupling-structure.md#7-cert-hooks:288-320` | keep | `oracle/laws/coupling-structure#cert-hooks` | sole statement of *how* the generator discharges the obligations; `cert-obligations` owns the obligation list and the tolerances |
| 61 | "The cert-obligation indices are now fixed in `cert-obligations §1.1`: equivariance = 1, antisymmetry of `L` = 5, PSD of `M` = 2" | `3.3-coupling-structure.md:318-320` | keep, drop "now" | `oracle/laws/coupling-structure#cert-hooks` | the index mapping is a present-tense fact; "are now fixed" is the scaffolding wrapper (D1) |
| 62 | `make-coupling-channel` registration; `applicability` validated as first-order decidable on typeclass tags; identity is `Address[CouplingChannel]` under the canonical-serialization rule; identical channels collapse to one address | `3.3-coupling-structure.md#8-registration-discipline:322-338` | keep | `oracle/laws/coupling-structure#registration` | sole statement |
| 63 | **Coefficient-provenance contract:** every provenanced coefficient carries `(value, σ, source, cost-class)` with `cost-class ∈ {curated, per-material-DFPT, fit}`; a cert obligation refuses any composition whose active channels carry unprovenanced coefficients; MVP diamond coefficients are `curated` | `3.3-coupling-structure.md:340-348` | keep | `oracle/laws/coupling-structure#provenance-contract` | sole statement; feeds the `coefficient-provenance σ` term of `residual-definitions §7` |
| 64 | **`slope-kind` double-count guard** for `dE_g/dT` (AHC): `slope-kind ∈ {isochoric, total}` on the coefficient; a cert obligation refuses a `total` slope co-active with row 63's thermal-expansion path; curated ZPR amplitudes (GaN −189, AlN −399, diamond −345 meV) are `isochoric`; the lattice-expansion part (GaN −49, AlN −85 meV) is row 63's | `3.3-coupling-structure.md:350-364` | keep | `oracle/laws/coupling-structure#slope-kind-guard` | rows 120 and 63 verified in the CSV. **Values unchecked by the data checker** (calibration probe B). Hand-verified against `9.1-accuracy-ledger:71` (row 15): "diamond −345 meV indirect, GaN −189, AlN −399" — **agrees.** |
| 65 | Trailing "(Wave-1 III-N audit: `[timeline] §2026-06-10 (Wave 1)`; the prior `total` tag on isochoric Engel magnitudes was corrected there.)" | `3.3-coupling-structure.md:363-364` | delete | — | D2. The citation resolves (dated anchors + parenthetical are checked). See **Log-worthy** row 4. |
| 66 | **Polarization-reference pairing guard:** ΔP's ±5% rests on an accidental cancellation (Dreyer PRX 6 021038 §V.D–E), holding only under a self-consistent pairing; `polarization-reference ∈ {ZB-proper, H-improper}` on rows 113/114/117; a cert obligation refuses mismatched tags; `is-AlGaN-GaN` validity scope, cert-refused for high-In InGaN/GaN | `3.3-coupling-structure.md:366-380` | keep | `oracle/laws/coupling-structure#polarization-pairing-guard` | sole statement; the "improper `e₃₁` ≈ 3.4× proper" ratio is the reason the guard is machine-checkable rather than advisory |
| 67 | **Learned-correction training contract:** a PINO-learned coefficient is (1) fit only against external anchors and frozen w.r.t. the PINO loss — a correction trained on the residual it modifies can co-adapt to zero it; (2) `Δα` ships as identity until anchors exist, and the high-E×high-T corner stays cert-refused | `3.3-coupling-structure.md:382-392` | move | `operator/loss/learned-corrections` — **with the refusal half kept at** `oracle/certification/cert-obligations#obligation-9` | this is a **training** constraint stated on an oracle laws page. Reason (1) is a loss-design rule (the operator's business); reason (2) is a refusal the oracle enforces. The block currently fuses them. Hand-verified against `9.1-accuracy-ledger:105` (row 49): agrees verbatim on "fit only to external anchors and frozen w.r.t. the PINO loss" and "ships as identity". |
| 68 | `record CouplingSpec { channels, theory_context }`; theory frame is part of identity by the record-address rule | `3.3-coupling-structure.md:394-402` | keep | `oracle/laws/coupling-structure#couplingspec` | sole statement; page is `canonical-for` "CouplingSpec record" |
| 69 | "`CouplingSpec` was previously a bare `SparseSet[CouplingRegistry]`; it is now a two-field record. … The `CouplingSpec` schema version is **bumped** so old bare-set addresses cannot collide" | `3.3-coupling-structure.md:404-410` | mine | `oracle/laws/coupling-structure#couplingspec` (the version-distinctness rule only) | the "previously/now" framing is scaffolding (D1). The *live* fact — the schema version distinguishes the two encodings so addresses cannot collide — survives, but restated without the history it is nearly vacuous. Marked `mine` rather than `delete` per the brief's tie-break; **I am not confident it survives the removal of its own motivation.** |
| 70 | The diamond MVP `CouplingSpec` (e-ph short-range + minimal coupling + ion-ion electrostatic + phonon-phonon in `M`, under the MVP default theory context) | `3.3-coupling-structure.md:410-414` | keep | `oracle/laws/coupling-structure#couplingspec` | sole statement; agrees with the MVP sets in `generic-dynamics:74-77, 99-101` |
| 71 | `theory_context` is **definitional input**, set at Stage 1, required before Stage 2 builds the (possibly double-cover) group; `make-theory-context` smart constructor must normalize before any `Address` is taken (hybrid double-representation normalization + relativistic PP/run consistency) | `3.3-coupling-structure.md:416-427` | keep | `oracle/laws/coupling-structure#theory-context-placement` | sole statement; page is `canonical-for` "TheoryContext placement" |
| 72 | Coverage policy is not an enumerated registry: a channel is a tuple in parameter space; the runtime rule is "active = applicability holds ∧ invariant basis non-empty"; `record CoverageBound`; MVP global cap `(4, Gradient(1))` driven solely by 4-phonon anharmonicity; per-mechanism inner table prunes before the character test | `3.3-coupling-structure.md#9-coverage-policy:429-460` | keep | `oracle/laws/coupling-structure#coverage-policy` | canonical; `open-decisions` Closed decisions restates the cap — **contradiction-free but duplicated**, and the register copy should become a citation |
| 73 | The piezoelectric-acoustic channel (`LongRangeStatic(1)`, `1/q` pole), `is-noncentrosymmetric`-gated, inert for diamond | `3.3-coupling-structure.md:462-466` | keep | `oracle/laws/coupling-structure#mechanism-range-table` | belongs with the table it extends (row 75), not in the coverage-policy prose |
| 74 | "This **closed the former open-decisions coupling-channel coverage-policy item** (now recorded under `[open-decisions]` Closed decisions)" | `3.3-coupling-structure.md:466` | delete | — | textbook D1/§5: a closed item's *resolution* is the page body (rows 72–73); the story of it having been open is scaffolding |
| 75 | Mechanism range and polynomial sufficiency: why some couplings are non-polynomial; `record MechanismRange`; the total O(1) derived `polynomial_sufficient` projection; the `make-coupling-channel` well-formedness invariant; `mechanism_range` ⟂ `applicability`; the 15-row template table | `3.3-coupling-structure.md#10:468-533` | keep | `oracle/laws/coupling-structure#mechanism-range-table` | canonical; page is `canonical-for` "polynomial-sufficiency flag" and "coupling coverage policy" |
| 76 | Screening channels are not in the diamond MVP but are the forcing function for the schema; ALDA `f_xc` is the degenerate corner — **tag by general mechanism, not by the cheapest approximation** | `3.3-coupling-structure.md:535-540` | keep | `oracle/laws/coupling-structure#mechanism-range-table` | the tagging rule is a live discipline, not an aside |
| 77 | `record KernelExt` and the four-variant backbone (one fiber-bundle section, differing in rank / value field / parametric-vs-tabulated) | `3.3-coupling-structure.md#11:542-561` | keep | `oracle/laws/coupling-structure#kernel-ext` | sole statement; page is `canonical-for` "KernelExt extension family" |
| 78 | Kernel sizing: `Parametric` < 1 KB; `Tabulated` full-frequency dielectric matrix for diamond ≈ **440 GB** worst case, ≈ 0.5 GB after plasmon-pole + IBZ reduction; grid is a `CacheEligible` sidecar folded in by address, never by value; **no MVP channel is tabulated**; 440 GB is the number the persistent-storage tier must be designed against | `3.3-coupling-structure.md:562-575` | keep | `oracle/laws/coupling-structure#kernel-ext` | sole statement. Arithmetic spot-check: 12³ × 64 × 500² complex128 ≈ 442 GB — **self-consistent.** Data checker does not read it. |
| 79 | `GaugeRule` is `None` for every MVP channel and recorded only when a genuine P3 gauge-fixing rule attaches | `3.3-coupling-structure.md:577-580` | keep | `oracle/laws/coupling-structure#kernel-ext` | sole statement |
| 80 | `record TheoryContext` (4 fields, ten closed C1 vocabularies in `canonical-vocabularies §7`); it enters neither the generator cache key nor the runtime kernel; it is solely cert+provenance metadata | `3.3-coupling-structure.md:582-605` | keep | `oracle/laws/coupling-structure#theory-context-placement` | sole statement; the "does not enter" clauses are what make the caching sound |
| 81 | The MVP default theory context: `GGA(PBE)` / PseudoDojo v0.4.1 NC (Ga `3d` semicore promoted) / `KohnSham` / `ScalarRelativistic`; PBE's UWBG gap underestimate is handled by theory-conditioning the reference-battery obligation, not by upgrading the default; `Hybrid(HSE06)` is the documented upgrade | `3.3-coupling-structure.md:607-613` | keep | `oracle/laws/coupling-structure#mvp-theory-context` | sole statement; restated (compatibly) in `open-decisions` Closed decisions, which should become a citation |
| 82 | PSD closure: why invariant-subspace membership does not imply PSD; the three documented assumptions `[PSD-e-ph]`, `[PSD-ph-ph]`, `[PSD-rad]` with origins, references, and tight/loose closure; closure is **tight at the operator level** (Reynolds image of a PSD seed is PSD, so no SDP feasibility search) and **loose at the coefficient level**, so the runtime guard `λ_min ≥ −δ_PSD` stays | `3.3-coupling-structure.md#12:615-658` | keep | `oracle/laws/coupling-structure#psd-closure` | sole statement; the tight/loose split is what turns an obligation into a theorem plus a cheap guard |
| 83 | Dormant SDP fallback (V2): interior-point `O(dim^{3.5})` at registration only, block-diagonalizable per Gatermann–Parrilo 2004; infeasibility rejects the channel; specified for forward-compatibility only | `3.3-coupling-structure.md:660-666` | keep | `oracle/laws/coupling-structure#psd-closure` | forward-looking, not history — not scaffolding |
| 84 | "`pino-bridge` is the only surface `/informed-operator` (and other downstream consumers) sees. Two exports." | `7.1-pino-bridge.md:33-34` | keep | `oracle/seams/pino-bridge#surface` | canonical owner of the "only surface" claim. `learnable-structure-requirements.md:13-15` restates it — see row 96. |
| 85 | The `Validate` signature (4 params → 4 outputs) and the semantics of `request` (subgraph selection) and `gradient` (`Skip` runs forward-only) | `7.1-pino-bridge.md#1-validate:36-57` | keep | `oracle/seams/pino-bridge#validate` | page is `canonical-for` "Validate signature". **Duplicated at `1.2-product.md:137-146`** with a *different* type vocabulary (`Map<SlotKey, Float>` vs `Map<ResidualKey, Scalar>`, `kernel : content hash` vs `cert : CertEvidence`) — **contradiction row 4.** |
| 86 | Output is granular by construction; the PINO chooses aggregation; `/physics` does not pre-sum | `7.1-pino-bridge.md:58-60` | delete | (cite `oracle/laws/residual-definitions#granularity`) | fourth statement of the same fact; see row 5 |
| 87 | The `Import` signature and per-target ingestion: each call wraps one external datum as a `GroundTruthBridgeGenerator`; Stage 1 inserts a pinned `Input` node carrying `(value, sigma)` plus a cert-only `ResidualLeaf`; `Import` is **not** differentiated through; its outputs serve obligation 4 and the operator's target-vs-prediction comparison | `7.1-pino-bridge.md#2-import:62-82` | keep | `oracle/seams/pino-bridge#import` | page is `canonical-for` "Import signature"; sole statement of the not-differentiated-through rule |
| 88 | `CoverageMask = RoaringCoverageMask`: flat-index construction over the generator's `axes`; sparse-from-start rationale; why Roaring (O(1) membership, set ops for the cert evaluator, ubiquitous bindings); persisted into the `coverage_mask` column of `SqliteReferenceCache.entries` | `7.1-pino-bridge.md#21:84-107` | keep | `oracle/seams/pino-bridge#coverage-mask` | sole statement; `open-decisions` records it closed and should cite rather than restate |
| 89 | What is not exported: `Predict`, `Certify`, `EnumerateObservables` remain internal `/physics` API for non-PINO consumers; not part of the pino-bridge contract | `7.1-pino-bridge.md#3:109-114` | keep | `oracle/seams/pino-bridge#not-exported` | sole statement; `1.2-product.md:219` cites it correctly |
| 90 | The factory's three responsibilities and its placement in Stage 1 under the always-cheap reframe | `7.2-residual-machinery.md:43-49` | keep | `oracle/seams/residual-machinery#factory` | sole statement |
| 91 | `record ResidualGenerator` — all 20 fields | `7.2-residual-machinery.md#1:51-96` | keep | `oracle/seams/residual-machinery#generator-record` | page is `canonical-for` "ResidualGenerator record"; 17 pages reference it |
| 92 | The `bundle` field comment: "A SET, not a scalar: 40 of the 134 registry rows carry two bundles … and this field read `BundleId` singular, which silently narrowed every one of them on the way in." | `7.2-residual-machinery.md:57-65` | mine | `oracle/seams/residual-machinery#generator-record` — keep "a set, not a scalar; 40 of 134 rows carry two bundles", drop the rest | the narrowing story is D1 scaffolding inside a code comment, where neither the changelog stripper nor any grep for history markers will find it. The count (40 of 134) is a live check. See **Log-worthy** row 5. |
| 93 | `diff-tag : D0 \| DN \| D1 \| D2 \| D3 \| D4` | `7.2-residual-machinery.md:70` | keep | `oracle/seams/residual-machinery#generator-record` | **nomenclature defect (§4):** `DN` is not between `D0` and `D1`. Propose the rename to Javier; do not act. |
| 94 | `layer : 0..6 — the 7-layer compute DAG of residual-generator-catalog §2` | `7.2-residual-machinery.md:67-68` | move | re-cite `oracle/laws/residual-definitions#generator-layers` or wherever the 7-layer DAG lands from ch. 11 | **dangling reference in canon (new, defect-2 class).** `residual-generator-catalog` is **not a page id** — the page is `deriv-generator-catalog`. Checker calibration: `check_book_structure.py:466` skips any `pid` not in the page set, so a bare id + `§` to a nonexistent page is silently ignored. This is the *only* occurrence of the string in the repo. Since ch. 11 is being mined, the 7-layer DAG needs a new home and this citation needs to point at it. |
| 95 | `make-residual-generator(...)` factory entry point; called once per formula at load time; inserted into Stage 1 when `applicability` holds | `7.2-residual-machinery.md#3:117-131` | keep | `oracle/seams/residual-machinery#factory-entry` | page is `canonical-for` "residual factory"; `coupling-structure §8` cites it as the pattern channels reuse |
| 96 | The three generator subtypes (standard / ground-truth-bridge / cert-only) and their loss treatment | `7.2-residual-machinery.md#4:133-143` | keep | `oracle/seams/residual-machinery#subtypes` | sole statement |
| 97 | Fidelity generators (the ε obligation): **normative** — a lowering that introduces representation error must register a paired cert-only fidelity generator carrying an a-posteriori estimator; the three triggering lowerings with estimators and costs | `7.2-residual-machinery.md#41:145-167` | keep | `oracle/seams/residual-machinery#fidelity-generators` | sole statement; `open-decisions` verifier-gap 4 and `traps` §58's sibling both point here |
| 98 | **An a-priori target is not a substitute for an a-posteriori estimate** — the target is what the plan intended, the estimate is what it achieved, only the second is evidence | `7.2-residual-machinery.md:162-167` | keep | `oracle/seams/residual-machinery#fidelity-generators` | live discipline; already cross-listed as `traps` §58's principle. Keep one full statement here, a one-line hazard in `practice/traps`. |
| 99 | The fidelity generator's output flows into `Quantity.combineTol` and into the cert, and **never** into the training loss — a network must not be able to reduce loss by making the oracle's self-assessment optimistic | `7.2-residual-machinery.md:169-172` | keep | `oracle/seams/residual-machinery#fidelity-generators` | sole statement; the anti-gaming argument is load-bearing and parallels row 67 |
| 100 | **Registration-time adjoint gate:** `D2` and `D3` run vJp-vs-JvP on `N ≈ 64` sampled points; max relative error > `τ_adj` (default `1e-4`) fails the build loud; `D3` runs a second check on the reciprocal condition number of the fixed-point Jacobian against `τ_cond`; the escape hatch is `D4`, not `D3` | `7.2-residual-machinery.md#5:174-188` | keep | `oracle/seams/residual-machinery#registration-gate` | page is `canonical-for` "registration-time adjoint gate"; `open-decisions`' `τ_cond` closure points here |
| 101 | "(`D3` was a fallback tier in the retired catalog legend; that is where the misreading comes from — see `[named-formulas]`.)" | `7.2-residual-machinery.md:186-188` | delete | — | D1: explicitly a retired-legend story. The live rule ("`D3` is not a weaker `D2`") is row 100; the hazard is already `traps` §53. |
| 102 | A passing gate is not evidence of a correct tag: vJp and JvP agree trivially where the true gradient is zero; a piecewise-constant output passes spuriously; rows 46 and 50 did; check that sampled points straddle a varying region | `7.2-residual-machinery.md:190-197` | keep | `oracle/seams/residual-machinery#registration-gate` | **near-verbatim duplicate of `traps` §52**, which itself carries "— enforced, `[residual-machinery]` §5". Keep the operative rule here; reduce `practice/traps` to the hazard line + citation. Same arrangement applies to `traps` §60 vs the `τ_cond` sentence in row 100. |
| 103 | **A dormant row cannot be gated, and must not read as though it were.** Row 122 (`iterative-lbte-kappa`) is `D3` but its `source` cell declares it dormant/anchored in V1: no fixed point, so no Jacobian, so both gates pass trivially. Its gate is **deferred, not passed**, and must be recorded as such in the cert. Any row whose `source` declares it dormant or anchored inherits this rule. | `7.2-residual-machinery.md:198-208` | keep | `oracle/seams/residual-machinery#registration-gate` | sole statement, **and it has no trap entry** unlike its siblings 52/53/60. Row 122 verified in the CSV. Candidate for a new `practice/traps` entry — flagged for Phase 2, not created here. |
| 104 | Most D2 generators with a fixed-point solve are wired to the implicit-diff adjoint synthesized at Stage 4; the gate verifies that synthesized adjoint, not a hand-written backward | `7.2-residual-machinery.md:210-213` | keep | `oracle/seams/residual-machinery#registration-gate` | sole statement — **and it is the sentence that makes the open verifier gap legible.** See **Open questions** `adjoint-drift-monitoring`. |
| 105 | Training-loop consumption: enumerate active residuals for the current curriculum phase (**Warmup → Refine → Polish**), sample per policy, evaluate forward + projection, run same-pass fixed-point iteration at the DAG layer barrier; aggregation lives entirely in `/informed-operator` | `7.2-residual-machinery.md#6:215-222` | move | `operator/training/loop` (the loop steps) + delete the aggregation sentence as a re-statement of row 5 | this describes the *operator's* loop from an oracle page. The phase list omits Cooldown and so matches neither `residual-definitions §4.1` (Warmup/Refine/Polish/Cooldown) nor `11.8` / `residual-loss-methodology` (Warmup/Refine/Calibrate/Polish) — **contradiction row 2.** |
| 106 | `record OneShotCert` (Layer 1.25) and its scheme enum, hashes, parameters, granular `closure-residual`, `reference-state`, `staleness-coeff`, cost-tier | `7.2-residual-machinery.md#7:224-247` | keep | `oracle/seams/residual-machinery#dressing-certs` | sole statement |
| 107 | The **validity radius**: a Layer-1.25 dressing drops its state-dependence; to first order the dropped term is `‖x − reference-state‖ · staleness-coeff`; their product is the dressing-staleness entry `combineTol` already listed but had no number for; nothing is refused for leaving the radius — the term simply grows | `7.2-residual-machinery.md:249-261` | keep | `oracle/seams/residual-machinery#dressing-certs` | sole statement. "…already lists but until now had no number for" is D1 phrasing wrapped around a live fact — rewrite present-tense, do not delete. `open-decisions` verifier-gap 3 records this closure and should cite rather than restate. |
| 108 | `record IterationSnapshot` and `record IterativeResult` (Layer 1.75, V2-deferred); V1 ships Layer 1.25 wired and Layer 1.75 as type/cert scaffolding with `not-implemented-in-V1` stubs that fail loud | `7.2-residual-machinery.md:263-286` | keep | `oracle/seams/residual-machinery#dressing-certs` | sole statement. **Nomenclature defect (§4): "Layer-1.75."** Propose; do not rename. Note "scaffolding" here means *stub code*, not corpus scaffolding — do **not** delete on a keyword sweep. |
| 109 | Cadence policy: T0 every SGD step, T1 RAD-subsampled, T2 per-epoch cached, T3 on-demand/calibration-only with a cheap proxy; only T0/T1 on the per-sample hot path; this is what makes the always-cheap claim honest about runtime cadence | `7.2-residual-machinery.md#8:288-304` | move | `operator/training/cadence` (the training-loop cadence) — leaving the **cost-tier definitions** at `oracle/registry/named-formulas#cost-tiers` | the left column (≤10 µs / ≤10 ms / ≤10 s / ≤10 min) is an oracle property of a formula; the right column ("every SGD step", "per-batch stochastic importance") is a training-loop policy. Same split as rows 38/41. `residual-loss-methodology.md:164-168` states an overlapping T0–T3 cadence with a **different T2 rule** — see contradiction row 5. |
| 110 | The example row assignments: rows 25 and 92 tagged T2; row 80 is T3 "and belongs on the line below"; row 87 is T3 | `7.2-residual-machinery.md:298-299` | mine | `oracle/registry/named-formulas#cost-tiers` (as plain assignments) | rows 25/80/87/92 and their names all verified against `registry-manifest.csv`. "both of which the CSV actually tags T2" and "belongs on the line below" are correction artifacts (D1) — keep the assignments, drop the narration. |
| 111 | "These rules cut across formulas, methods, residuals, cert, and the pino-bridge surface. They are not architectural decisions — **those live in `arch-*`**." | `7.3-cross-cutting-rules.md:26-30` | delete | — | **retired-id namespace live in canon** — `arch-*` pages were renamed on 2026-07-22 (`physics/library/formulas/retired-ids.csv`). A *third* site beyond the plan's two, and the first inside `journal/pages/`. No checker reads `retired-ids.csv` (plan defect 7), and a glob is not an id, so nothing could have caught it. The sentence's referent no longer exists; the framing dies with the page (row 116). |
| 112 | Rule 1 — method equivalence as a residual, not a path-selector: all applicable formulas instantiated as `FormulaApply` nodes with hash-consing; equivalence enforced via `Algebraic/MethodEquivalence`, one leaf per pair; obligation 6 consumes it and trips with both values as witnesses | `7.3-cross-cutting-rules.md#1:31-47` | delete | (cite `oracle/laws/residual-definitions#algebraic-categories` and `oracle/certification/cert-obligations#obligation-6`) | every clause is owned elsewhere: the category by `residual-definitions §1`, the obligation by `cert-obligations`, hash-consing by `compose-time-pipeline §3`. Pure re-statement. |
| 113 | Rule 1's equivalence-pair vs consistency-pair paragraph (τ kinds; Callaway-vs-BTE κ is a consistency pair) | `7.3-cross-cutting-rules.md:48-54` | delete | (cite `oracle/laws/residual-definitions#pair-kinds`) | verbatim-in-substance duplicate of `3.2-residual-definitions.md:145-156`. **This is the fact `capability-slices:84` and `named-formulas:163` currently cite `[cross-cutting-rules]` for** — both citations should repoint to `residual-definitions`. |
| 114 | Rule 1's last bullet — the `Observable` output role designates the *exposed* value; **selection is by `ContributionFacets` precedence: declared dressing tier, then registration order**; unselected formulas still contribute their leaves | `7.3-cross-cutting-rules.md:55-59` | move | `oracle/compilation/physics-graph#observable-role` | **the only non-duplicated fact on this page.** `physics-graph` defines `Observable(bundle)` at `:129` and `:134` but states **no selection precedence anywhere**. If `cross-cutting-rules` is dissolved without moving this, the rule is lost. |
| 115 | Rule 1's closing claim — the architecture never averages observables and never silently selects between formulas at runtime; both are exposed and the disagreement is a typed residual | `7.3-cross-cutting-rules.md:61-63` | move | `oracle/compilation/physics-graph#observable-role` | travels with row 114; it is the *why* for that precedence rule |
| 116 | Rule 2 — same-type ⇒ one interface many handlers; type-change between stages ⇒ keep the stages; with both example lists | `7.3-cross-cutting-rules.md#2:65-81` | move | `program/purpose/architectural-principles#same-type-vs-stage` | a genuine cross-cutting design heuristic, and the only block on the page that is not a restatement of a specific mechanism. It is an *architectural principle*, which is precisely what line 27 denies this page contains. |
| 117 | Rule 3 — provenance tags are not weighting axes: facets are queryable provenance, never part of `ResidualKey` identity, never a loss-weight basis; weighting lives in the operator's curriculum keyed by `CategoryTag` gates only | `7.3-cross-cutting-rules.md#3:83-92` | move | `oracle/laws/residual-definitions#facets` | the rule is *about* `ContributionFacets`, which `residual-definitions §2` owns and already states as "sidecar; not part of identity". Merging removes the duplication and puts the prohibition next to the definition. `computational-overview:487` cites `cross-cutting-rules §3` and must repoint. |
| 118 | Rule 4 — couplings are declared channels, terms are generated; adding a regime is a channel declaration, not a code edit; channels register through the same factory pattern | `7.3-cross-cutting-rules.md#4:94-107` | delete | (cite `oracle/laws/coupling-structure#coverage-policy` and `#registration`) | every clause restates `coupling-structure` §1–§5, §8, §9. The one memorable sentence ("adding a new physical regime is a channel declaration, not a code edit") is a rephrasing of §9's coverage-policy rule; if Javier wants it kept for readability it belongs at `coupling-structure#coverage-policy`. `computational-overview:549` cites `cross-cutting-rules §4` and must repoint. |
| 119 | The page `cross-cutting-rules` itself, and its `canonical-for: [cross-cutting rules]` | `7.3-cross-cutting-rules.md` (whole page) | delete | — | **It is a bag of rules, not a topic.** Disposition per rule above: 2 delete-as-duplicate (112/113, 118), 3 move (114/115, 116, 117), 1 delete-as-remnant (111). Nothing remains. Its ownership claim is **de-facto vacuous** — topic `cross-cutting rules` vs id `cross-cutting-rules`, differing by one hyphen, so it escapes both the duplicate-topic invariant (calibration: no vacuity check exists) **and the plan's 18-page vacuity census**, which counts only exact matches. Graph load is low: 3 `referenced-by`, 4 prose citations, all repointable above. **This page is listed as surviving in the plan's target tree (§3, `oracle/seams/`); I am recommending it does not.** |

---

## Open questions

| id | question | owning page | why it is open |
|---|---|---|---|
| `adjoint-drift-monitoring` | The vJp-vs-JvP gate runs once at registration against the **formula's** adjoint. Stage 4 synthesizes the **composition's** adjoint later, per composition, over a graph Stage 3 has already rewritten. Nothing re-validates the second. | `oracle/seams/residual-machinery#registration-gate` | `10.2-open-decisions.md:99-107` names it still open and explicitly notes the ε rule does not close it: the rewrite-admission rule makes a rewrite's *value* discrepancy visible but says nothing about whether the downstream-synthesized adjoint is still the one that was gated. `7.2-residual-machinery.md:210-213` states the wiring that creates the gap but does not flag it. **Owner is the gate's page, not Stage 4's** — the gap is a hole in the gate's coverage, and D7 puts a gap next to the fact it is a gap in. `oracle/compilation/compose-time-pipeline#stage-4` should carry a pointer, not a second copy; the emitted register would otherwise show one gap as two. |
| `environment-record-owner` | The `Environment` record appears in signatures across this scope — `Validate(state, env, …)`, `evaluate : (State, Environment) → …`, `applicability : (Crystal, Environment) → Bool` (three times), `Static/Thermodynamic` inputs — and **no page defines its fields.** | `oracle/state/crystal-inputs#environment` (proposed) | confirms the plan's second homeless fact from inside this scope. `grep -rn "record Environment"` over `journal/pages/` returns nothing; the only field-level statement anywhere is `11.8-deriv-generator-catalog.md:580` ("the `Environment` record needs a `temperature_gradient` field") — inside the chapter being deleted. No `canonical-for` claims it. `learnable-structure-requirements.md` R4 requires the operator accept it as typed conditioning channels and cites `crystal-inputs`, which does not define it. |
| `state-array-layout` | R1 requires emission in "per-slot array shapes and layouts, units, and the gauge conventions" of `unified-state`; `unified-state` states mathematical types only. Layouts and units are stated nowhere; the gauge convention is normative in `generic-dynamics`, not `unified-state`. Also: R1 demands finite IEEE-754 float64 at the seam, and no oracle page states a state dtype. | `oracle/state/unified-state#slot-layouts` (proposed; table must be written) | the plan's confirmed dangling promise, verified. `2.2-unified-state.md:29-44` is the whole slot definition. Two further instances of the same class found in this scope: `residual-definitions:123` → `gamma-hat §5` (row 29) and `coupling-structure:100` → "tabulated alongside the state-component definition" (row 51). All three resolve to a page; none lands on the claim. |
| `sub-dof-pair-table` | Which `(StateComponent, SubDofTag)` pairs are legal? `coupling-structure §2` says they "are tabulated alongside the state-component definition"; no such table exists. | `oracle/state/unified-state#sub-dof-table` | see row 51. `make-coupling-channel` validation cannot be implemented without it. |
| `curriculum-denominator` | Are `residual-definitions §4.1`'s fractions fractions of *total* training or of the *informed stage*? The two sources disagree, and the answer changes what the schedule means. | `oracle/laws/residual-definitions#curriculum-gate` | contradiction row 1. Registered here as an open question rather than resolved, because either reading is coherent and the choice is auditor 2's. |
| `curriculum-phase-names` | Four phases or three? `Cooldown` or `Calibrate`? Four sources give three different answers. | `oracle/laws/residual-definitions#curriculum-gate` | contradiction row 2. Blocks the `11.8` `curriculum-phase` enum from being mined verbatim. |
| `dormant-row-cert-encoding` | A dormant row's gate is "deferred, not passed, and must be recorded as such in the cert" — but the cert has no *deferred* value. `adjoint-cert : Passed \| Failed(witness) \| NotApplicable \| Relaxed(rationale)` has four cases and none of them is deferred. | `oracle/seams/residual-machinery#registration-gate` | `7.2:93` (the enum) against `7.2:198-208` (the rule). **Found in this survey; not previously registered anywhere.** The rule as written cannot be encoded in the record the same page defines. Structural, not physical — but I am registering it as an open question rather than a contradiction because the fix is obviously an added enum case and the *question* is whether `Relaxed(rationale)` was meant to carry it. |

---

## Log-worthy advancements

| date | finding or decision | evidence | attribution | superseded |
|---|---|---|---|---|
| 2026-07 | **γ̂ admissibility gates added to `Positivity`** — ensemble N-representability (`γ̂†=γ̂`, `0 ⪯ γ̂ ⪯ 1`) as per-k-block spectral bounds, plus applicability-gated T=0 idempotency. Rationale: a candidate γ̂ outside these bounds can zero every EOM residual while being unphysical, so without them the oracle is not sound as a verifier of the *state*, only of its dynamics. | `3.2-residual-definitions.md:120-129` ("2026-07 gap-audit A1") | 2026-07 gap-audit, item A1 | extends the pre-audit `Positivity` category |
| 2026-07 | **Gauge fixing and the electrostatic partition made normative.** `A` is carried in the Weyl gauge with transversality; `E_EM` counts the transverse sector only; the longitudinal/electrostatic sector is owned by the matter functionals. Consequence: the `EOM/A` residual becomes gauge-unambiguous and no electrostatic energy is double-counted between field and matter terms. | `3.1-generic-dynamics.md:169-182` ("2026-07 gap-audit A2") | 2026-07 gap-audit, item A2 | prior `E_EM` statement, which did not partition |
| 2026-07-2x | **A restated enumeration drifted against its canonical ledger** — the tolerance list restated in `residual-definitions §7` named ten symbols against a ledger of sixteen. Recorded because it is the empirical basis for the one-home rule this restructure is built on, and it is the only *measured* instance of the drift in the corpus. | `3.2-residual-definitions.md:311-314`; ledger at `cert-obligations §1.2` | corpus self-audit (date not stated on the page — **needs pinning from git before this row is written to the log**) | the ten-symbol restatement |
| 2026-06-10 | **AHC ZPR amplitudes re-tagged `isochoric`** after being carried as `total`, and the `slope-kind ∈ {isochoric, total}` tag introduced as a machine-checkable double-count guard against row 63's thermal-expansion path. A `total` slope already folds in the lattice-expansion shift (~30–40%) that row 63 carries separately. | `3.3-coupling-structure.md:350-364`; `[timeline] §2026-06-10 (Wave 1)`; ledger row 15 at `9.1-accuracy-ledger.md:71` | Wave-1 III-N audit | the prior `total` tag on Engel/Miglio isochoric magnitudes |
| 2026-07-2x | **`ResidualGenerator.bundle` corrected from scalar to set.** The field read `BundleId` singular while 40 of the 134 registry rows carry two bundles (`B1/B4`, `B3/B9`, …), so every dual-bundle row was silently narrowed at registration. | `7.2-residual-machinery.md:57-65` | corpus self-audit (date not stated — **pin from git**) | the singular `BundleId` field |
| 2026-07-21 | **`CouplingSpec` promoted from a bare `SparseSet[CouplingRegistry]` to a `{channels, theory_context}` record, with a schema-version bump** so old bare-set addresses cannot collide with new record addresses. Makes the theory frame part of content-addressed identity automatically. | `3.3-coupling-structure.md:404-410` | coupling coverage-policy wave | the bare-set `CouplingSpec` |
| 2026-07-2x | **Piezoelectric-acoustic e-ph channel added** (`LongRangeStatic(1)`, `1/q` pole), the second long-range e-ph mechanism the wurtzite III-N members carry, `is-noncentrosymmetric`-gated. Closed the coupling-channel coverage-policy open item. | `3.3-coupling-structure.md:462-466`; `open-decisions` Closed decisions | coupling coverage-policy wave | the 14-row template table |
| 2026-07-21 | **The frozen-Layer-1.25 validity radius became computable.** `staleness-coeff = ‖∂(dressing)/∂x‖` measured once at the reference state at compile time, times a runtime state norm, is the dressing-staleness term `combineTol` listed but had no number for. | `7.2-residual-machinery.md:249-261`; `open-decisions` verifier-gap 3 | 2026-07-21 ε-rule wave | the unquantified dressing-staleness term |
| 2026-07-2x | **Rows 46 and 50 shipped adjoint certificates for gradients that do not exist** — piecewise-constant outputs pass the vJp-vs-JvP gate spuriously. Established that a passing gate is not evidence of a correct `D2` tag. | `7.2-residual-machinery.md:190-197`; `traps` §52 | corpus self-audit | the assumption that the registration gate is sufficient |
| 2026-07-2x | **Row 122's `D3` gate is deferred, not passed.** A dormant/anchored row has no fixed point, so there is no Jacobian to condition and both gates pass trivially — the spurious-pass failure one level worse, since the gradient is structurally absent rather than locally zero. Generalized: any row whose `source` declares it dormant or anchored inherits the rule. | `7.2-residual-machinery.md:198-208`; registry row 122 `iterative-lbte-kappa` | corpus self-audit | the unqualified `D3` reading of row 122 |
| 2026-07-2x | **`Δα` learned-correction freezing rule.** A correction trained on the same residual it modifies can co-adapt to zero that residual and destroy the obligation-9 domain it exists to protect. Therefore: fit only against external anchors, frozen w.r.t. the PINO loss; and with no V1 anchors, ship as identity and keep the high-E×high-T corner cert-refused. | `3.3-coupling-structure.md:382-392`; `9.1-accuracy-ledger.md:105` (row 49) | high-field wave | the unconstrained learned-coefficient path |
| — | **Survey finding (this pass): the residual-category count is unchecked on its own owning page.** The count checker matches only `\d+ residual categories`; `residual-definitions` states its count as "nineteen" and as "**19** symbols". Corrupting it on the owning page passes clean; corrupting it on any citing page fires. | `check_book_structure.py:333`; calibration probe, this document's header | this survey | the belief that the 19-category count is checked |
| — | **Survey finding (this pass): `residual-generator-catalog` is not a page id.** A bare id + `§` citation to a nonexistent page is silently skipped by `check_citations` (`:466-467`), so the corpus carries a dangling reference in canon that no checker can see. | `7.2-residual-machinery.md:68`; `check_book_structure.py:466` | this survey | the belief that §-coordinate citations are fully validated |
| — | **Survey finding (this pass): a third live retired-id site, inside canon.** `cross-cutting-rules:28` directs readers to `arch-*` for architectural decisions. The plan located two retired ids, both in an untracked file; this one is in `journal/pages/`. | `7.3-cross-cutting-rules.md:28`; `physics/library/formulas/retired-ids.csv` | this survey | the plan's count of two live retired-id sites |
| — | **Survey finding (this pass): `physics/research/group-{A,B,C}-*.md` does not exist.** `generic-dynamics` grounds its nine-regime extraction table in files absent from the repo. Path citations are checked by nothing. | `3.1-generic-dynamics.md:123`; `find . -name "group-*"` returns empty | this survey | — |

---

## Contradictions — COLLECTED, NOT RESOLVED

| claim | source A | source B | nature of the conflict |
|---|---|---|---|
| What the curriculum fractions are fractions **of** | `3.2-residual-definitions.md:253` — "fraction ∈ [0, 1] of **total training budget**" | `informed-operator/design/training-stages.md:8,17` — the oracle attaches for one stage only; "curriculum gating **within this stage** follows [§4.1]" | If the oracle is absent from stages 1 and 3, a schedule indexed on *total* training would place Warmup and Cooldown outside the window in which any residual is evaluated. Either the denominator is the informed stage, or the stage ordering is wrong. Load-bearing: it determines when each category actually turns on. |
| The curriculum **phase names and count** | `3.2-residual-definitions.md:245-259` — four phases: Warmup / Refine / Polish / **Cooldown** | (i) `7.2-residual-machinery.md:218` — three: "Warmup → Refine → Polish"; (ii) `11.8-deriv-generator-catalog.md:332` — `curriculum-phase : Warmup \| Refine \| **Calibrate** \| Polish`; (iii) `informed-operator/design/residual-loss-methodology.md:199-205` — Warm-up / Refine / **Calibrate** / Polish | Three distinct vocabularies across four sources, two of them inside canon. The `11.8` enum is a *typed* disagreement, not prose, and `11.8` is being mined — mining it verbatim would import the wrong enum. |
| The `γ̂`-block of `L` | `3.1-generic-dynamics.md:90` — "Liouville–von Neumann on γ̂ `(1/iℏ)[Ĥ_KS, ·]`" | `3.1-generic-dynamics.md:134-139` — "written `[·, γ̂]` **not** the bare `[Ĥ_KS, ·]`" | Self-contradiction within one page: the operator table states the form the later section explicitly corrects. The correcting section gives the reason (Jacobi and degeneracy hold by construction only in Lie–Poisson form), so the table appears simply stale — but I am not ruling on it. |
| The `Validate` output types | `7.1-pino-bridge.md:39-48` — `residuals : Map<ResidualKey, Scalar>`, `values : Map<ObservableRef, Value>`, `cograds : Map<ResidualKey, Cotangent>`, `cert : CertEvidence` | `1.2-product.md:140-146` — `residuals : Map<SlotKey, Float>`, `values : Map<Name, Value>`, `cograds : optional gradient map`, **`kernel : content hash`** | Same signature, two type vocabularies, and a **different fourth output** (`cert : CertEvidence` vs `kernel : content hash`). `pino-bridge` is `canonical-for` "Validate signature", so `product` should be the derived copy — but the fourth field is not a renaming, it is a different value. |
| T2 residual cadence | `7.2-residual-machinery.md:298` — T2 is "**per-epoch cached** (offline reference cache per composition + `(T,P,q)` query)" | `informed-operator/design/residual-loss-methodology.md:167,353` — T2 is "**curriculum-scheduled**: first 30% of training without T2; introduce gradually" and "evaluated on a fraction of the batch chosen by largest-residual-first" | Two different policies for the same tier, and the second introduces a *third* schedule axis (a 30% cost-tier gate) alongside the category gate (0.10/0.60/0.90) and the source-weight schedule. Whether these compose or conflict is not stated anywhere. |
| Where the seam contract's requirements are canonical | `informed-operator/design/learnable-structure-requirements.md:3-6` — "Not part of the lint-enforced atomic tree; the atomic tree … is canonical for every shape, convention, and count cited here" | `7.1-pino-bridge.md:33-34` + `2.2-unified-state.md` — the canon it defers to does not contain the shapes, layouts, or units R1 defers to it for | The deference is circular in one direction: R1 says canon is canonical for the layouts; canon does not state them. Neither document is wrong on its own terms; together they leave the fact unowned. (Registered also as open question `state-array-layout`.) |

---

## Notes for Phase 2

**The stage-ordering / curriculum boundary, stated once so the builder does not have to re-derive it.**
Three facts, three owners:

- **Epoch sequence** — supervised VASP epochs → one informed epoch → oracle-free inference, plus the
  *why* (a residual is a local signal; the oracle refines, it does not search) and the four
  consequences. → `operator/training/stage-ordering`. Mined from
  `informed-operator/design/training-stages.md` (whole file) and `2026-07-22-prep.md:286-296`.
  These are the **only** two statements of it and neither is in canon.
- **Category participation gate** — which `CategoryTag`s are live at which fraction. →
  `oracle/laws/residual-definitions#curriculum-gate`. Stays where it is. It is keyed on the
  oracle's own closed vocabulary and states a property of the residual surface.
- **Source-weight schedule** — `w_cheap / w_vasp / w_exp / λ_residual` over training. →
  `operator/loss/`. Mined from `residual-loss-methodology.md:171-205`.

The test that separates them: *if the operator switched to a different data source, which fact
changes?* The gate does not; the stage ordering and the weight schedule both do.

**Two of these three currently collide on the same numbers.** The category gate and the
source-weight schedule both use `0.10 / 0.60 / 0.90` and both name their middle phases
"Refine". They are different schedules on different axes. Whoever writes the two pages should
make the distinction explicit at both ends, or a later reader will merge them.

**Ordering hazard — `cross-cutting-rules` must not be deleted before row 114 is moved.**
The `Observable`-role selection precedence ("declared dressing tier, then registration order")
exists in exactly one sentence in the corpus, on the page I am recommending be dissolved.
`physics-graph` defines the role and states no precedence. Delete-then-move loses it.

**Ordering hazard — `residual-machinery §2` before `residual-definitions §2`.**
Row 47 deletes a duplicated block but three lines at its tail are unique (row 48). Deleting the
section wholesale loses the bare-vs-dressed-chains rule.

**Ordering hazard — ch. 11 and the `layer : 0..6` field.** Row 94's citation is already broken;
the 7-layer compute DAG it means to cite lives in `11.8-deriv-generator-catalog §2`, which is
being mined. If ch. 11 is dissolved before the DAG gets a home, the field's semantics go with it.
`11.8` also carries the **retired differentiability legend** the plan warns about — the same page,
so mining it needs the warning applied and the `curriculum-phase` enum treated as suspect
(contradiction row 2).

**On `pino-bridge` versus `learnable-structure-requirements` — the duplication is real but
smaller than it looks, and the split should survive.** I checked all ten requirements. R1–R8 and
O1–O2 are *demands on the operator*; `pino-bridge` states *what the oracle exports*. Those are the
two halves of one contract and they belong on opposite sides of the module boundary:
`oracle/seams/pino-bridge` and `operator/seam/learnable-structure`. What is genuinely duplicated
is the *oracle-side setup* each requirement restates before making its demand:

| | duplicated statement | canonical owner |
|---|---|---|
| intro | "the only surface a downstream consumer sees is the pino-bridge" | `pino-bridge#surface` (row 84) |
| intro | the two-loop symmetry (training sinks cotangents into weights, design into the candidate) | stated **three** times: `1.5-rationale.md:129`, `1.2-product.md:197-199`, LSR:20-23. One owner; two citations. |
| R1 | admissibility is scored, never presupposed | `residual-definitions#structural-categories` |
| R2 | symmetry-quotiented axis grids fixed at compose time | `compose-time-pipeline#stage-2` |
| R3 | the oracle returns per-key cotangents | `pino-bridge#validate` |
| R5 | float normalization (canonical quiet-NaN, `−0.0 → +0.0`) | `representation-substrate#addressing` |
| R8 | loop logic lives in `/interface` | `program/purpose/purpose-and-scope` |
| O1 | the Stage-2 sidecar carries per-instance symmetry structure | `compose-time-pipeline#stage-2` |
| O2 | tiers evolve at heterogeneous rates | `oracle/state/multiscale-state` |

Each should become a citation, leaving the requirement itself. **None of them currently
*disagrees* with its canonical source** — I checked each — so this is redundancy, not drift. The
one exception is R1, which cites `unified-state` for content that page does not contain
(open question `state-array-layout`), and R1's finite-float64 demand, which has no oracle-side
counterpart at all.

**LSR carries the find/replace damage (plan defect 8) at lines 4–5** — ``the atomic tree
(`the canon chapters*`, `the canon chapters*`)`` — so its own provenance sentence is unreadable
and the two documents it names cannot be recovered from the text. `git log -S` on that file is
the only route. Mining it does not require them, but the log entry attributing the contract does.
I checked whether the same damage hit my scope: `generic-dynamics:122` also reads "the canon
chapters", but `git show 2f1d22f^` proves it was already there before the rename (only
`impl-06-compositions` → `typed-compositions` changed), so it is pre-book chapter-numbering prose,
**not** find/replace damage. Disposition unchanged (row 10) but the classification differs.

**Nomenclature defects present in this scope, per §4 — propose to Javier, do not act.**
`DN` outside `D0–D4` (`7.2:70`) · `Layer-1.75` (`7.2:227,272,285`) · `Stage 2.5` inside a
"4+1 stage" pipeline (`3.3:224,243`; `7.3:76,99`). All are load-bearing tokens appearing in typed
enums, not just prose, so renaming them is a code-shaped change even though no code exists yet.

**Values in this scope are unverified and unverifiable by the current tooling.** Calibration
(header table) shows `check_data_agreement.py` reads none of them despite sweeping
`journal/pages/`. I hand-checked the three that have a canonical source and they agree: AHC ZPR
amplitudes against ledger row 15, the `Δα` freezing rule against ledger row 49, and registry rows
25/63/72/75/80/87/92/104/120/121/122/124/125/126 against `registry-manifest.csv` (all resolve,
all names match). The rest — the diamond metastability band (+25 meV/atom, σ 5 meV/atom), the
440 GB dielectric-matrix figure, `|G| ≤ 192`, `dim(T) ≤ ~250`, ~12M ops, `τ_adj = 1e-4`,
`N ≈ 64` — have **no canonical source in the corpus to check against.** I did verify the 440 GB
figure is arithmetically self-consistent with its own stated dimensions (12³ × 64 × 500² ×
complex128 ≈ 442 GB). Everything else in that list is stated once and checked by nothing. That is
a statement about coverage, not a claim that any of them is wrong — auditor 2's call.

**Blocks I could not disposition confidently.** Row 69 (`CouplingSpec` schema-version bump): I
marked it `mine` per the tie-break rule, but the live fact is thin once its "previously/now"
motivation is stripped, and I could not determine whether the version bump is still *doing*
anything in a corpus with no legacy addresses to collide with. Row 92 (the `bundle`-field comment)
has the same shape. Both are cases where the scaffolding *is* the explanation, and removing it
leaves a rule with no visible reason. Flagging rather than guessing.

**What I swept and dismissed.** All §-coordinate citations in the six files were machine-checked
against every page's numbered and dated headings: two do not resolve or are not checkable —
`residual-generator-catalog §2` (row 94) and `accuracy-ledger §1/§15/§49`. The latter is a
**notation collision, not a broken pointer**: those are ledger *row* numbers written in
section-coordinate syntax, and `accuracy-ledger` has zero numbered headings, so
`check_citations` skips them by the `pid not in coords or not coords[pid]` guard. Content-wise
all three land (verified against ledger rows 1, 15, 49). Under the new `[id#anchor]` scheme they
must be re-expressed as row references. I also verified `timeline §2026-06-10 (Wave 1)` and
`traps §58` *are* checked — by the dated-anchor path and the dedicated trap-number handler
respectively — so neither is a finding despite both targets having no numbered headings.
