# Merged — open

Fragments: 11/11

### from `oracle-state`

| id | question | owning page | why it is open |
|---|---|---|---|
| `state-wire-schema` | Per-slot wire schema for the 7-tuple: dtype, unit, index order, memory layout, and `γ̂`'s array shape as a function of its `CompressionPlan` | `oracle/state/unified-state` | R1 (`learnable-structure-requirements.md:27-31`) obliges the operator to emit "per-slot array shapes and layouts, units, and the gauge conventions recorded there" — none of it is recorded there. The seam's mandatory structural check has no specification to check against. (= salvage `I1`) |
| `environment-schema` | The closed `Environment` field set, each field's type and unit, and the **structural / swept** partition | `oracle/state/crystal-inputs` | `Environment-structural` keys the kernel cache (`compose-time-pipeline:278`, `computational-overview:590`) and no page says which fields are structural. A structural field misfiled as swept silently reuses a kernel outside its envelope. (= salvage `I0`) |
| `crystal-type` | Is `Crystal` the pair `(PeriodicityStructure, SiteDecoration)` or the full triple with `Environment`? | `oracle/state/crystal-inputs` | `(Crystal, Environment) → Bool` is the applicability signature on every registry row, `CouplingChannel`, `ResidualGenerator`, and property template. `Crystal` is defined nowhere (grep: zero definitions, ≥7 signature uses). The pairing with `Environment` implies the pair, but nothing states it |
| `mesh-adjoint-scheme` | Discrete- vs continuous-adjoint of the finite-volume operator, for differentiating `EOM/Continuum` | `oracle/state/multiscale-state` | stated twice on the page (`2.4:276-280`, `2.4:473-475`); inherits the Stage-4→Stage-5 AD seam. Live residue of `open-decisions` item 2 |
| `hole-transport-anchors` | `μ_p, α_p, v_sat,p` anchors per host — the `p` schema is committed, the coefficients are not | `oracle/state/multiscale-state` | `2.4:478-479`. A per-composition data gap; every bipolar macro composition is unanchored until it closes |
| `msq-niel-coefficients` | `η_recomb(T_L)` and the NIEL displacement cross-section `σ_d(host, particle, energy)` | `oracle/state/multiscale-state` | `2.4:208-211`, `2.4:483-487`: **no closed form exists in the corpus** — only the coupling structure and a curated `ProvenanceLedger` slot. F-H2 cannot be evaluated without them. A data-acquisition task, explicitly not to be invented |
| `regime-threshold-windows` | Per-material regime-switch field windows are order-of-magnitude only | `oracle/state/multiscale-state` | `2.4:485-487`; the §8.1 windows (`10⁴/10⁵/few×10⁵ V/cm`) gate `applicability-classifiers`' per-sample mask, so their width is load-bearing |
| `g0w0-cost-scope-tag` | G₀W₀ cost is not scope-tagged by cell size: T2 (≤10 s) at MVP scale, hours for V1 defect supercells | `oracle/state/born-oppenheimer-levels` | `open-decisions:122-125`. The tier table is violated silently rather than refused. A cost claim, so the ε rule does not reach it. **Cross-scope:** the text lives on `10.2-open-decisions`, the topic belongs here |
| `supercell-memory-budget` | The γ̂ memory budget when leaving the primitive cell | `oracle/state/gamma-budget` | `2.6:31-32` says it "is the first thing to revisit"; nothing tracks it, so it is an obligation with no owner |

---

### from `oracle-laws-seams`

| id | question | owning page | why it is open |
|---|---|---|---|
| `adjoint-drift-monitoring` | The vJp-vs-JvP gate runs once at registration against the **formula's** adjoint. Stage 4 synthesizes the **composition's** adjoint later, per composition, over a graph Stage 3 has already rewritten. Nothing re-validates the second. | `oracle/seams/residual-machinery#registration-gate` | `10.2-open-decisions.md:99-107` names it still open and explicitly notes the ε rule does not close it: the rewrite-admission rule makes a rewrite's *value* discrepancy visible but says nothing about whether the downstream-synthesized adjoint is still the one that was gated. `7.2-residual-machinery.md:210-213` states the wiring that creates the gap but does not flag it. **Owner is the gate's page, not Stage 4's** — the gap is a hole in the gate's coverage, and D7 puts a gap next to the fact it is a gap in. `oracle/compilation/compose-time-pipeline#stage-4` should carry a pointer, not a second copy; the emitted register would otherwise show one gap as two. |
| `environment-record-owner` | The `Environment` record appears in signatures across this scope — `Validate(state, env, …)`, `evaluate : (State, Environment) → …`, `applicability : (Crystal, Environment) → Bool` (three times), `Static/Thermodynamic` inputs — and **no page defines its fields.** | `oracle/state/crystal-inputs#environment` (proposed) | confirms the plan's second homeless fact from inside this scope. `grep -rn "record Environment"` over `journal/pages/` returns nothing; the only field-level statement anywhere is `11.8-deriv-generator-catalog.md:580` ("the `Environment` record needs a `temperature_gradient` field") — inside the chapter being deleted. No `canonical-for` claims it. `learnable-structure-requirements.md` R4 requires the operator accept it as typed conditioning channels and cites `crystal-inputs`, which does not define it. |
| `state-array-layout` | R1 requires emission in "per-slot array shapes and layouts, units, and the gauge conventions" of `unified-state`; `unified-state` states mathematical types only. Layouts and units are stated nowhere; the gauge convention is normative in `generic-dynamics`, not `unified-state`. Also: R1 demands finite IEEE-754 float64 at the seam, and no oracle page states a state dtype. | `oracle/state/unified-state#slot-layouts` (proposed; table must be written) | the plan's confirmed dangling promise, verified. `2.2-unified-state.md:29-44` is the whole slot definition. Two further instances of the same class found in this scope: `residual-definitions:123` → `gamma-hat §5` (row 29) and `coupling-structure:100` → "tabulated alongside the state-component definition" (row 51). All three resolve to a page; none lands on the claim. |
| `sub-dof-pair-table` | Which `(StateComponent, SubDofTag)` pairs are legal? `coupling-structure §2` says they "are tabulated alongside the state-component definition"; no such table exists. | `oracle/state/unified-state#sub-dof-table` | see row 51. `make-coupling-channel` validation cannot be implemented without it. |
| `curriculum-denominator` | Are `residual-definitions §4.1`'s fractions fractions of *total* training or of the *informed stage*? The two sources disagree, and the answer changes what the schedule means. | `oracle/laws/residual-definitions#curriculum-gate` | contradiction row 1. Registered here as an open question rather than resolved, because either reading is coherent and the choice is auditor 2's. |
| `curriculum-phase-names` | Four phases or three? `Cooldown` or `Calibrate`? Four sources give three different answers. | `oracle/laws/residual-definitions#curriculum-gate` | contradiction row 2. Blocks the `11.8` `curriculum-phase` enum from being mined verbatim. |
| `dormant-row-cert-encoding` | A dormant row's gate is "deferred, not passed, and must be recorded as such in the cert" — but the cert has no *deferred* value. `adjoint-cert : Passed \| Failed(witness) \| NotApplicable \| Relaxed(rationale)` has four cases and none of them is deferred. | `oracle/seams/residual-machinery#registration-gate` | `7.2:93` (the enum) against `7.2:198-208` (the rule). **Found in this survey; not previously registered anywhere.** The rule as written cannot be encoded in the record the same page defines. Structural, not physical — but I am registering it as an open question rather than a contradiction because the fix is obviously an added enum case and the *question* is whether `Relaxed(rationale)` was meant to carry it. |

---

---

### from `oracle-compilation`

| id | question | owning page | why it is open |
|---|---|---|---|
| `adjoint-drift-monitoring` | Stage 4 synthesizes the composition's adjoint over a graph Stage 3 already rewrote; the registration gate validated only the formula's adjoint. What revalidates the second? | `oracle/compilation/compose-time-pipeline#stage-4` | Explicitly open at `10.2:99-105` ("Still open — and the ε rule does not close it"). Stated on no pipeline page today |
| `environment-record` | What are the fields of `Environment`, and which are **structural** (recompile triggers) vs **scalar** (runtime inputs)? | `oracle/state/crystal-inputs#environment` | The kernel-cache fingerprint at `4.2:277-281` keys on "`Environment`-structural"; `applicability-classifiers:124` tags kernels with "the `Environment` box". Neither the record nor the split is defined anywhere. `EnvField` (`4.1:98`) is likewise undefined. Homeless |
| `steppable-form-manifest` | What is the schema of the steppable-form manifest and the per-tier tangent map? | `oracle/seams/pino-bridge` *(or a new `interface/` page)* | Cited as a closed decision by `gamma-hat:97`, `4.4:242`, `4.4:628`, `traps`, `timeline` — but no page's `canonical-for` claims it. Only a full schema exists, in `journal/live/specs/2026-07-16-evolver-duality-research.md:353`, which is slated for deletion. **Homeless resolution of a closed question** |
| `pde-mesh-adjoint-scheme` | Which adjoint scheme for the PDE mesh? (the mesh *format* is committed in `multiscale-state`) | `oracle/state/multiscale-state` | `4.4:624-626`; deferred just-in-time |
| `surrogate-net-build-vs-adopt` | Build or adopt the surrogate net for `D4` formulas? | `oracle/registry/named-formulas` | `4.4:624`. **Note the D4 retag collision** — Contradictions C6 |
| `layer-175-onramp` | The GW / DMFT onramp for iterative fixed-point dressings | `oracle/state/born-oppenheimer-levels#dressing-tiers` | `4.4:626`; V2-deferred, V1 ships loud `not-implemented` stubs |
| `implementation-language` | Which languages fill the four settled roles? | `program/build/forced-decisions` | `4.4:61-66`, `4.2:204-206`. The four-role split is settled; the fill is not |
| `observable-catalog` | The 52-observable (+16 FoM) catalog is researched but not machine-readable | `oracle/registry/observable-bundles` | `4.4:605-606`; enumeration task |
| `b9-bundle` | The rest of the `B9` bundle is not formalized | `oracle/registry/observable-bundles` | `4.4:606` |
| `csp-residual-catalog` | The crystal-structure-validity residual catalog is not machine-readable | `oracle/laws/residual-definitions` | `4.4:605-606` |
| `stage3-performance` | Equality saturation at Stage 3 is "the hardest pass to build" and its performance is open-ended | `oracle/compilation/compose-time-pipeline#stage-3` | `4.4:363-364`. Not listed in `10.2-open-decisions` — an open item with no register entry today |

---

---

### from `oracle-cert-accuracy`

| id | question | owning page | why it is open |
|---|---|---|---|
| `obligation-9-scope` | Obligation 9 is stated as "surrogate-net validity (for `D4` surrogate formulas)", but `D4` now means *relaxed* (argmin/hull/sort/discrete), and its checker body — declared input domain, surrogate uncertainty, refresh currency, dev set — describes a learned net. All six `D4` rows are relaxations; the one learned surrogate (`quasi-particle-shift-G0W0-surrogate`, row 6) is `D2` and falls outside. Does obligation 9 rebind to the true surrogate rows, split into surrogate-validity + relaxation-validity, or rescope some other way? | `oracle/certification/cert-obligations#the-ten-obligations` | **Logged, not resolved** (auditor 2 / the code spec own the answer). Homeless today: the question is written out at `journal/live/specs/2026-07-21-oracle-code-spec-research-brief.md:108` (R4.2) — a container D13 deletes — and nowhere in canon. `10.2-open-decisions.md:52` item 1 names the same phantom set ("the D4 surrogate formulas") without noticing. Three canon sites carry the stale scope: `5.1:65`, `5.4:30`, `4.4:519`; a fourth, `5.1:147` (`δ_surrogate` = "`D4` relaxation validity"), was already updated to the new meaning, so **the page contradicts itself** |
| `semiconductor-interface-predicate` | Row 116 (`interface-trap-density`) needs a semiconductor-interface applicability predicate; `has-metal-semiconductor-interface` is too narrow because traps sit at semiconductor/dielectric interfaces too. Until one is defined, row 116 carries an always-true stub | `oracle/certification/applicability-classifiers#row-116-and-119` | Live and correctly reasoned — naming the gap rather than inventing a classifier is `[traps] §61`. Currently stated twice (`5.2:81-88` and `10.2-open-decisions.md:71-81`); under D7 the classifier page owns it and the register is emitted |
| `environment-record-unowned` | What is the `Environment` record — its fields, units, and which are runtime-swept vs compose-time-frozen? | `oracle/state/…` (needs an owner; **not** a certification page) | Confirms the brief's homeless fact from this side. No page's `canonical-for` names it (checked all 58). My scope uses it load-bearingly at four sites: `5.1:63` (battery lookup key), `5.2:30` (predicate signature), `5.2:116-126` (the swept-scalar **`Environment` box** — scalar ranges a kernel is valid on), `9.2:92` (CSV column). All five reference CSVs carry an `Environment` column. `9.2 §Schema discipline` is the de-facto definition and it is a one-line column gloss |
| `algan-bowing-b-unseeded` | The AlGaN polarization bowing coefficient `b_P` (and the e_ij bowings) are not seeded; mid-x AlGaN ΔP claims carry a widened σ until they are | `oracle/accuracy/accuracy-ledger#polarization-bowing` | Gated on a paywalled pin-read (FBA APL 80 1204 (2002) / Ambacher JPCM 14 3399 (2002)) — a Javier acquisition task. The sign guard between the `−b_P` canon form and the CSV's `+b` form is live until then |
| `mesh-sigma-floor-undeclared` | No numeric per-observable mesh-σ floor has ever been declared; ledger row 2's "the declared mesh-σ floor" was a forward reference that has dangled since 2026-06-10 | `oracle/accuracy/accuracy-ledger#observable-regimes` (row 2) | The page names it honestly and defers to `[open-decisions]`; under D7 it becomes a frontmatter entry on the ledger itself |
| `soft-applicability-classifiers` | Soft `[0,1]` applicability classifiers, and classifier composition under perturbation | `oracle/certification/applicability-classifiers#v1-commitment` | Deferred at `5.2:112-114` in a trailing parenthetical, tracked nowhere else |
| `csv-to-sqlite-path` | How do the reference CSVs become `SqliteReferenceCache` rows? Neither page states an ingest step, a build target, or which artifact obligation 8 actually reads at runtime | `oracle/certification/cert-obligations#reference-cache` | Surfaced by the contradiction below. Not a research gap — a specification gap in the corpus's own machine-readable path |

---

### from `oracle-registry`

| id | question | owning page | why it is open |
|---|---|---|---|
| `unregistered-composition-formulas` | Eighteen formula names invoked by `typed-compositions` are not registry rows, so its closed-vocabulary claim does not hold in full. Nine are definitional (transcription + tag assignment); nine need literature (four hardness models, `harmonic-transition-rate-normalization`, `jump-diffusivity`, `htst-rate`, `turnover-frequency`, `formation-energy-from-references`). Which register, and in what order? | `oracle/registry/typed-compositions#declared-gap` | Declared and machine-enforced but unresolved. Currently double-homed: the page's frontmatter *and* `10.2-open-decisions.md:137-154` narrate it, and `10.2` dissolves. Verified: all 18 absent from the CSV; all 18 invoked at `formula =` sites in the page body |
| `undeclared-non-formula-slots` | Two names are invoked in non-`formula =` slots and are neither registry rows nor declared: `exchange-coupling-formula` (`kernel =`, `6.8:113`) and `harmonic-rate-prefactor` (`ν₀ =`, `6.8:168`). Are they in scope for the declared gap, or a separate vocabulary (`ResponseKernel` members) that no page owns? | `oracle/registry/typed-compositions#declared-gap` | The `unregistered-formulas` mechanism only sweeps `formula =` arguments (`check_data_agreement.py:99-107`), so the closed-vocabulary claim has a second hole the declared gap does not cover. `harmonic-rate-prefactor` also names the same ν₀ the same page computes via `harmonic-transition-rate-normalization` |
| `dn-not-between-d0-and-d1` | The `D0 | DN | D1 | D2 | D3 | D4` vocabulary is misordered — `DN` is not between `D0` and `D1`, and canon carries a written warning about it in two places. Propose a replacement ordering. **Propose only; do not rename** (plan §4). | `oracle/registry/named-formulas#diff-tags` | The vocabulary is stated in **11 places** and explained in exactly one. Full inventory below |
| `impl-language-undecided` | The typeclass alphabet is presented as language-neutral pseudocode because the implementation language is undecided. | `oracle/registry/typeclass-alphabet` | Inherited from `10.2-open-decisions`, which dissolves; `6.2` is the only chapter-6 page that depends on it (`6.2:8-9`) |
| `cij-averaging-scheme` | Which averaging scheme (Voigt / Reuss / Hill) `BulkModulus = AlgebraicOf({ElasticConstants}, formula = bulk-modulus)` uses is an open pick. | `oracle/registry/typed-compositions#mechanical` | Stated inline as "an open pick" at `6.8:87-88` and nowhere registered as an open item |
| `argument-type-alphabet-homeless` | The argument types used throughout the method and template signatures — `Extractor`, `Aggregator`, `ResponseKernel`, `PathMethod`, `Optimizer`, `EigenSolver`, `ConvexSolver`, `KineticMethod`, `Sampler`, `ProjKind`, `Classifier`, `ComparisonMetric`, `TensorNorm`, `HessianMethod`, `NonlinearSolver`, `BiSlabSolver`, `ChargeNeutralitySolver`, `ConvergenceCriterion` — are **defined by no page and carry no glossary entry**. Which page owns them? | unassigned — candidate `oracle/registry/computational-methods#argument-types` | Homeless-fact class. Swept `journal/pages/` + `glossary.md`: zero glossary entries, and each appears in only 1–3 files, all of them *uses*. The 12 method signatures and 20 template signatures cannot be typed without them |
| `environment-record-homeless` | `Environment` appears in seven signatures in this chapter alone (`6.5:51,79,84,96,100`; `6.6:54,176`) and 23 files corpus-wide; **no page's `canonical-for` claims it** and it has no glossary entry. | unassigned — the brief names it already | Confirms the brief's second homeless fact from inside this chapter. `Crystal`, its partner in `applicability : (Crystal, Environment) → Bool`, does have a glossary entry; `Environment` does not |
| `does-formula-registry-survive` | `formula-registry` (6.9) and `properties` (6.10) both hold vacuous `canonical-for` and both lose almost all inbound edges when ch. 11 is deleted. Do they survive as pages, or dissolve into `named-formulas` + `typed-compositions` + the emitted index? | `oracle/registry/` (structural) | `6.9`'s inbound edges are `deriv-csp`, `deriv-generator-catalog`, `deriv-high-field` (all ch. 11) and `properties`. `6.10`'s sole inbound edge is `deriv-high-field` (ch. 11). Post-deletion the pair is a disconnected island. My rows dissolve 6.9 and merge 6.10; **Javier should confirm** |
| `cost-tier-legend-unguarded` | The `T0–T3` legend is written out in full in at least four places and no checker holds any two together (probe B). Where does it live, and how is the single copy enforced? | `oracle/registry/named-formulas#cost-tiers` | `check_data_agreement.py:460-462` hardcodes `{'T0','T1','T2','T3'}` and `{'D0'…'DN'}` as a *fifth* copy, directly under a comment saying vocabularies are "HARVESTED, never restated here" (`:436-437`) |

### Every place the `D0 | DN | D1 | D2 | D3 | D4` vocabulary is stated or explained

Requested inventory. **Explained once. Stated eleven times.**

| Location | Form |
|---|---|
| `journal/pages/06-vocabularies-and-registry/6.6-named-formulas.md:63-121` | **the sole explanation** — per-tag definitions, the mixed-output rule, the `D0`-misuse test, the `DN`-vs-`DX` spelling rule |
| `journal/pages/06-vocabularies-and-registry/6.6-named-formulas.md:49` | `record FormulaRecord` field, bare list |
| `journal/pages/06-vocabularies-and-registry/6.1-canonical-vocabularies.md:145` | bare list **+ the "`DN` is not inside `D0..D4`" warning** |
| `journal/pages/06-vocabularies-and-registry/6.9-formula-registry.md:105` | bare list, with an explicit link-don't-repeat note |
| `journal/pages/07-consumers-and-seams/7.2-residual-machinery.md:70` | `record ResidualGenerator` field, bare list, annotated "named-formulas is canonical" |
| `journal/pages/10-process-and-governance/10.3-audit-prompt.md:116` | bare list **+ the "`DN` is not inside `D0–D4`" warning** (occurrence 2 of 2) |
| `journal/pages/11-appendix-derivations/11.6-deriv-csp.md:27` | quoted inside a **collision note** — that survey's own `D+`/`D0`/`D-` legend, where `D0` means the opposite |
| `journal/pages/11-appendix-derivations/11.8-deriv-generator-catalog.md:27` | quoted inside the **retired-legend banner** |
| `journal/live/specs/2026-07-21-oracle-code-spec-research-brief.md:61` | bare list + the targetable⇒`D1`/`D2`/`D3` rule |
| `journal/live/specs/2026-07-21-oracle-code-spec-research-brief.md:123` | bare list, inside the `D4`-retag scope note |
| `informed-operator/design/residual-loss-methodology.md:119` | bare list, inside a **superseded-legend banner** |
| `informed-operator/design/residual-loss-methodology.md:421` | written `D0–D4` — **omits `DN` entirely**, which is the defect biting |
| `journal/tools/check_data_agreement.py:461` | hardcoded Python set `{'D0','D1','D2','D3','D4','DN'}` — a twelfth copy, in code |

Related live collisions the vocabulary must not reintroduce, per `6.6:137-143`: `DX` (the DX center in AlGaN/AlN, `deriv-defects` Part B) and `D1`–`D5` (wurtzite deformation potentials in `accuracy-ledger`).

### `T0–T3` and the `D`-tags: is each owned by exactly one page?

Asked specifically; answered specifically.

**`T0–T3` — one owner, four unguarded copies.** `glossary.md:41` names `named-formulas` as canonical. The legend is written out in full at `6.6:59-61` (owner), `6.9:100-102` (verbatim duplicate), `4.4-computational-overview.md:435` (duplicate + the 76/40/11/5 distribution, which I verified against the CSV and which reconciles), and `informed-operator/design/residual-loss-methodology.md:328`. `7.2-residual-machinery.md:296-297` restates `T0`/`T1` but attaches sampling cadence — that is a *different* fact and should keep its own home. `11.6-deriv-csp.md:27` runs a **different `T0`–`T4` scale one tier deeper**, flagged in place. Probe B: no checker holds any two together.

**`D`-tags — one owner, eleven restatements.** Table above.

---

---

### from `program`

| id | question | owning page | why it is open |
|---|---|---|---|
| `implementation-language-picks` | Which language fills each of the four polyglot roles (compiler host, runtime host, group-theory engine, proof assistant)? | `program/build/forced-decisions#implementation-language` | The four-role shape is settled; the picks are not. `deriv-language-study` records Haskell / Julia / GAP / Lean 4 as candidates and, more usefully, which requirement each was chosen to satisfy. Nothing in the build order depends on the pick (`8.6:29-30`), which is why it can stay open. Currently stated as open item 6 on `10.2-open-decisions`, which dissolves per §3 |
| `layer-125-l1-exposure` | What exactly must L1 expose at Layer 1.25 — the unoccupied-band count and wavefunction format for G₀W₀, and the volume-dependent Grüneisen phonon outputs for QHA? | `oracle/state/born-oppenheimer-levels` | `8.4:36` defers it in prose: "specify them when building `state/level-1`". The requirement is named; the specification does not exist |
| `validate-request-selectors` | Should `Validate`'s `request` parameter accept the static schema's closed-enum tags as selectors, so subsetting never requires enumerating keys? | `oracle/seams/pino-bridge` | Recorded at `1.2:173-176` as a "future edit, deliberately not made yet". Selector sugar over existing enums; no new machinery |
| `beta-ga2o3-wave-status` | Is the β-Ga₂O₃ wave still "frozen pending its adversarial audit"? | `oracle/accuracy/reference-battery#wave-program` | `1.5:217-220` states frozen-pending-audit as of 2026-07-16 and cites a `live/specs/` file being deleted. The status is a dated snapshot on a page that disclaims being updated; the successor page needs the current state, which I cannot establish from the corpus |
| `oracle-file-abi` | How is the loading convention / ABI for a non-native program calling an oracle-file resolved — native module, flat-array C-style ABI, or both? | `oracle/seams/pino-bridge` | `1.2:252-255`. The abstract contract (one self-describing file, flat arrays at the boundary) is fixed; the container is not |
| `oracle-file-wire-formats` | What are the wire formats for state files, residual/value maps, and the schema table on the CLI surface? | `oracle/seams/pino-bridge` | `1.2:256-257`. Constrained by canonical-serialization discipline; otherwise open |
| `compile-cache-policy` | What is the compile-cache management policy (eviction, sharing, provenance listing) that enumerate-and-compile design searches rely on? | `oracle/compilation/representation-substrate` | `1.2:258-260`. Content addressing makes correctness free; only policy is open |
| `environment-schema` | Which page owns the `Environment` record type? | `oracle/state/crystal-inputs` (proposed) | Named in signatures at `1.2:132-133`, `8.6:37`, and `compose-time-pipeline §1`; claimed by no `canonical-for`. Confirms the brief's homeless-fact finding from a second direction |
| `diamond-dataset-owner` | Does the diamond strain-hypersurface dataset (1,179 rows / 1,131 distinct) get a canonical owner, and is it reference data or training feedstock? | `oracle/accuracy/reference-battery` (proposed) | Stated only at `1.5:197-204`, on a page that disclaims being current, while `8.2:48-50` already depends on it for Cap-1's data-backed sensitivity test. A load-bearing dataset with no owner |

---

---

### from `practice`

| id | question | owning page | why it is open |
|---|---|---|---|
| `surrogate-net-build-vs-adopt` | Surrogate net for relaxation rows: build or adopt? | `oracle/registry/named-formulas` | **The item as written names a set that does not exist.** `10.2-open-decisions.md:52` says "the D4 surrogate formulas"; the 2026-07-21 retag redefined `D4` as *relaxed*, not *surrogate*. Needs Javier's judgment on what the question now ranges over before it can be restated |
| `pde-mesh-adjoint-scheme` | PDE-mesh adjoint scheme — discrete- vs continuous-adjoint of the finite-volume operator for the macro continuum tier | `oracle/state/multiscale-state#macro-tier` | mesh *format* is closed (`DeviceMesh` finite-volume `Universe`, fields as fibers); only the adjoint binding remains, reusing the Stage-4→Stage-5 AD seam |
| `layer-175-minimum-spec` | Layer-1.75 minimum spec sufficient for a V2 contributor to implement self-consistent GW / DMFT | `oracle/state/born-oppenheimer-levels#dressing-tiers` | V2-deferred; `IterativeResult` is specified for forward compatibility but the spec behind it is not |
| `semiconductor-interface-predicate` | A semiconductor-interface applicability predicate broad enough for semiconductor/dielectric interfaces | `oracle/certification/applicability-classifiers` | `has-metal-semiconductor-interface` is too narrow. Registry row 116 (`interface-trap-density`) carries an always-true stub until the predicate exists. Naming the gap rather than inventing a classifier is trap 61 |
| `implementation-language-picks` | Which language fills each of the four polyglot roles | `program/build/forced-decisions#language-roles` | the four-role *shape* is closed; the picks are not, and nothing in the build order depends on them. Each slot must be re-argued against its alternatives before code is written |
| `adjoint-drift-monitoring` | Post-registration adjoint-drift monitoring | `oracle/seams/residual-machinery#registration-gate` | the vJp-vs-JvP gate runs once at registration against the **formula's** adjoint; Stage 4 synthesizes the **composition's** adjoint later over a graph Stage 3 has rewritten. Different objects; nothing revalidates the second. The rewrite-admission rule makes a rewrite's *value* discrepancy visible and says nothing about the adjoint |
| `g0w0-cost-scope-tag` | G₀W₀ cost is not scope-tagged by cell size | `oracle/registry/named-formulas` (cost tiers) | at MVP scale a T2 budget (≤10 s) is plausible; for V1 defect supercells it is hours, so the tier table is violated silently rather than refused. A *cost* claim, so the identity/ε rule does not reach it |
| `unregistered-composition-formulas` | Eighteen composition formula names invoked by `typed-compositions` are not registry rows | `oracle/registry/typed-compositions` | the closed-vocabulary claim does not yet hold in full. Two tranches: ~half definitional (transcription + tag assignment), the rest modelling choices needing literature (four hardness models, HTST and jump-diffusivity prefactor conventions, turnover frequency, formation-energy-from-references). Deliberately not registered on thin provenance |
| `evolver-lowering-spec` | The evolver's full lowering spec — manifest record, refusal enum, the scorer–evolver exactness obligation | the row-76 target (see Notes) | deferred to its own named wave, slow tier first. **Constrains the product**: no time-evolution verb may be claimed until it lands |
| `wave1-data-acquisition-gaps` | Nine named Wave-1 data-acquisition gaps: AlN electron Caughey–Thomas quartet (paywalled); AlN hole mobility; AlN *measured* avalanche α_ii; AlN hole α_ii; κ_BR as normalized `(1/E)dE/dT` for GaN/AlN; `Z*_⊥`; the `e₁₅` sign (literature-split ±); AlGaN d-constants / `v_sat(x)` / holes; AlN `B′` and per-axis sound velocities | `oracle/accuracy/accuracy-ledger` and `oracle/accuracy/reference-battery` | live cert-refusal boundaries currently stated only inside a chronology entry (`10.5-timeline.md:116-122`). Each is a value that is refused rather than guessed |
| `mesh-sigma-floor-undeclared` | No numeric per-observable mesh-σ floor has ever been declared | `oracle/accuracy/accuracy-ledger#row-2` | **`9.1-accuracy-ledger.md:58` says this gap "is named as an open item in `[open-decisions]`". It is not** — `10.2-open-decisions.md` contains no mesh-σ item. A dangling promise created by the repair of the dangling pointer that trap 61 is about |
| `log-attribution-schema` | Who or what produced each log entry, and what each superseded | `log/timeline.md` | the schema requires attribution and supersession; **no entry in the current 12 supplies either**. Javier must decide the attribution granularity the contract requires — see Notes |
| `rename-forwarding-mechanism` | Does the corpus keep a rename-forwarding mechanism at all? | `practice/traps` (trap 57) / `practice/agent-contract` | §9 deletes `retired-names.csv`, which is trap 57's entire mechanism. "Fix names before they land" (trap 47) is the replacement, but it is a discipline, not a check — and a rename that lands only in `data/registry-manifest.csv` still orphans every prose mention |
| `appendix-comparison-replacement` | What replaces "compare the appendix against what landed in canon"? | `practice/agent-contract#reporting` | `10.3-audit-prompt.md:87,126` directs an auditor to diff the research stratum against canon. D3 deletes the second stratum, so after cutover an auditor has one source and nothing to diff it against except git history |

---

### from `appendix-a`

| id | question | owning page | why it is open |
|---|---|---|---|
| OQ-A1 | Is the long-wavelength identity `Σ_βδ C_αβγδ q̂_β q̂_δ = ρv²_αγ(q̂)` a declared `Algebraic/MethodEquivalence` **equivalence pair** binding registry rows 60/62 (elastic) to rows 9/11 (phonon), scored at `τ_equiv`? | `oracle/laws/residual-definitions` | It is theorem-backed, cross-bundle (B7↔B2), cheap, and stated three times in `11.1` (§2.6, §3.6, §S.4). Canon has the machinery (`3.2:147-150`, `5.1:141`) and has never declared this pair. Nothing in the corpus says whether the omission is deliberate. |
| OQ-A2 | Where does **Cauchy momentum balance** `ρ∂²u/∂t² = ∂_jσ_ij + f_i` live, and is it a residual? | `oracle/laws/residual-definitions` | Proposed as a mechanical residual at `11.1:695-696` (§S.4). Zero hits for "Cauchy" in `journal/pages/` outside ch. 11. `multiscale-state`'s macro tier is `(T_L, φ, n, p, j)` — **no mechanical field** — so there is no tier for it to be an `EOM/Continuum` instance of. Ledger row 41 `resonant_modes` is nonetheless flagged "device-scale (continuum elastic)". |
| OQ-A3 | Which ledger-tracked observables have **no registry row**? Confirmed in my scope: `⟨u²⟩` / Debye–Waller (`11.1:504-505`), `C_V = −T∂²F/∂T²` (`11.3:340`), degree of rate control (`11.3:501`). | `oracle/accuracy/accuracy-ledger` | The ledger tracks 59 observables (`3.2:310-311`); the registry has 132 substantive rows. Nothing joins the two, so a ledger entry with no implementing row is invisible. `9.1-accuracy-ledger.md:94` (row 38) is the shape of the problem — see C2 below. |
| OQ-A4 | If `Φ(R)` is Fourier-interpolated with the **acoustic sum rule restored by symmetrization** (`11.1:544-545`), does registry row 7's residual detect anything? | `oracle/laws/residual-definitions` | Symmetrization makes the ASR hold by construction, so scoring it on post-symmetrized `Φ` is identically zero regardless of the physics. Row 7 does not state which `Φ` it consumes; rows 7 and 8 both annotate their input as "from the DFPT reference solve (**unregistered input**)". |
| OQ-B1 | What is the relation between `χ` (density–density), `χ^PP` (polarization–polarization), and `χ^JJ` (current–current)? | `oracle/registry/typed-compositions` | `11.2:15` **declares this its own job and does not do it**: "`χ^JJ` was uncounted here and is never related back to `χ` in the body, which is exactly the job this banner exists to do." Registry rows 93/94 and the optical observables table (`11.2:377-381`) use all three interchangeably. A self-declared dangling promise. |
| OQ-B2 | Which registry row implements Kramers–Kronig? | `oracle/laws/residual-definitions` | cat 11 is a whole residual category (`3.2:134-135`); no row in the 134-row manifest is a KK residual, and `3.2:218-219` gives "One Kramers–Kronig identity on one component of `ε(ω)` at one frequency band" as the *example* of an atomic contribution. A category with no producer. |
| OQ-C1 | Should **degree of rate control** `X_RC,i = (k_i/r)(∂r/∂k_i)` be a registry row? | `oracle/registry/formula-registry` | `11.3:501`, `:522`. It is the sensitivity of the observable to each rate constant — i.e. the answer to "which barrier must the oracle get right?". It is a derivative of a composed quantity, so it may belong to the AD layer rather than the registry. Not adjudicated. |
| OQ-C2 | Does the **slow tier** admit two state schemas (`{[D]^q}` defect populations *and* `θ ∈ Δ^S` coverages / `{x_i}` compositions), or must they unify? | `oracle/state/multiscale-state` | `multiscale-state:413` gives the slow tier a single schema `{[D]^q}` indexed `(species, site)`. `unified-state:57-58` names composition vectors as first-class slow-tier state. `11.3:269-282` and `:446-461` give two further schemas. Four names, one tier, no reconciliation. |
| OQ-C3 | Is the **fluctuation–dissipation relation** `⟨ξξ⟩ = 2k_BT L` a constraint the oracle checks on `M`? | `oracle/laws/generic-dynamics` | Stated twice (`11.3:311-312`, `:472`) and nowhere in canon. It is what ties `M` to `T`; without it `M ⪰ 0` is a stipulation rather than a consequence. `generic-dynamics:96-102` defines `M` structurally with no temperature dependence stated. |

---

---

### from `appendix-b`

| id | question | owning page | why it is open |
|---|---|---|---|
| `fermi-level-charge-balance-closure` | Where does `E_F(T)` come from? The self-consistent charge-balance closure `∑_D∑_q q[D]^q + p − n + ∑_d q_d[Dopant^q_d] = 0` is an input to registry row 30 and to residual R-T2, and it is solved by no registered formula and stated on no surviving page. | `oracle/state/multiscale-state#charge-balance-closure` | Row 30 `defect-formation-energy` takes `E_F` as a given; `2.4-multiscale-state.md:450-452` scores `dE_form/dE_F = q` against an `E_F(T)` it never defines. The fixed-point solve exists only at `11.4-deriv-defects.md:66-71` and `:621`. Homeless-fact class. |
| `dopant-ionization-registry-rows` | Do dopant ionization/solubility get registry rows, or stay as the residuals `R_SolubilityLimit` / `R_ActivationEnergy`? The same four physical quantities are typed twice — as signatures (`11.4` B.3) and as residuals (`11.6` D.1) — with no row either way. | `oracle/registry/formula-registry` | `dopant_activation`, `solubility_limit`, `self_compensation`, `DX_transition` have no registry rows; `R_DopantSitePref`, `R_SelfCompensation`, `R_ActivationEnergy` have none either. Two descriptions, zero implementations. |
| `hydrogen-three-way-coupling-owner` | Who owns the three-way H coupling (surface termination χ ↔ long-time bulk `[H]` transport ↔ dopant activation)? | `oracle/state/multiscale-state#surface-coupling` | `11.4-deriv-defects.md:652` raises it as needing a single owner and never assigns one. Canon splits the pieces across F-G2 (row 106), F-47 (row 110) and nothing for the dopant-activation leg. |
| `environment-schema` | What is the `Environment` record's full slot set, and who owns it? `11.5` names four slots (`applied_electric_field`, `applied_stress`, `temperature`, and a needed `temperature_gradient`); `11.4` E.2 needs `μ_H(T,p)`/`μ_O(T,p)`; `11.6` C.2/D.1 need `μ_elements`. | `oracle/state/crystal-inputs#environment` | The plan's second confirmed homeless fact, corroborated here from three directions. `11.5-deriv-high-field.md:592` states an unmet *requirement* on a record no page owns, so the requirement has nowhere to be recorded either. |
| `response-causality-slot` | Does the `response` interface need a `causal? : Boolean` parameter, or a separate relaxed variant, to host real-valued nonlinear responses (`α(E)`, `J_FN(E)`, `v(E)`)? | `oracle/registry/observable-bundles` | Raised twice in `11.5` (`:502` and `:593`) and resolved nowhere. Registry rows 49, 74, 123 all carry `B9` today without the question being settled. |
| `csp-descriptor-derivation` | Is `structure-uniqueness-CSP` (row 85) derived anywhere? Its descriptor, its distance metric, its uniqueness threshold, and its declared relaxation are the spec of an MVP Cap-1 row and I could not find a derivation on any page. | `oracle/registry/formula-registry` **or** a new `oracle/registry/named-formulas` anchor | See Notes §C — this is a homeless fact and it contradicts my brief's premise. |
| `diamond-metal-tbr-scope` | Is the TBR (thermal boundary resistance) at the diamond/refractory-metal contact in scope for the MVP? It is the thermal bottleneck of the device concept, has no registry row, no CSV anchor, and no canon statement. | `oracle/accuracy/accuracy-ledger#diamond-metal-contacts` | `11.5` D.3 is the sole statement (row 119). `9.1-accuracy-ledger.md` tracks κ but nothing at an interface. |
| `cheap-proxy-label-accuracy` | What cheap-proxy accuracy is sufficient for label generation? `11.6` B.3 asserts ~85% sign accuracy on dynamical stability "is enough" without stating the criterion that makes it enough. | `operator/training/curriculum#label-generation` | The only quantified cheap-label acceptance threshold in the corpus, and it is an unjustified assertion in a page being deleted. |

---

---

### from `appendix-c`

| id | question | owning page | why it is open |
|---|---|---|---|
| `migs-worked-example-inputs` | `MIGS-corrected-barrier` (registry row 48): the §6 worked example's inputs (`S≈0.85`, `φ_CNL≈4.0`) evaluate to ~3.5–3.7 eV under every termination-tagged χ, not the ~4.5 eV the example states. What pins them? | `oracle/registry/formula-registry` (row 48) | Flagged UNPINNED in **both** `11.8:493` and the `registry-manifest.csv` row-48 `Source` cell. The value must not be quoted until pinned. The CSV's pointer to `deriv-generator-catalog §6` dies at cutover. |
| `environment-schema` | What are the fields of the `Environment` record? | `oracle/state/unified-state` | The brief's confirmed homeless fact, and my scope holds both the only gesture at its contents (`11.8:269` `env-slots : subset of Environment record`) and an unadopted amendment (`11.8:580`, add `temperature_gradient`). Used in 12+ canon signatures, defined in none. |
| `response-causality-slot` | Does the response interface need a `causal? : Boolean` slot for non-causal nonlinear responses? | `oracle/registry/property-templates` | Proposed at `11.8:582` as "adopt directly"; never adopted. No trace outside ch. 11. |
| `observable-catalog-owner` | Which page owns the 52 catalog observables and their math? | *(none — target-tree gap)* | The target structure in the plan has no such page. The ledger owns their tolerances (a different fact), the registry owns 132 formulas (a different axis). See Notes. |
| `fom-count-16-or-15` | Are the 16 FoMs 16, or 15? | `oracle/accuracy/accuracy-ledger` | The count is asserted in three canon pages; the only enumeration (`11.7` Part B) has 15 rows. See C-1. |
| `out-of-scope-limits-coverage` | Do `11.7` Part F's 12 out-of-scope limits all appear in `out-of-scope`? | `oracle/certification/out-of-scope` | Only strong correlation confirmed present (`5.3:26`). The other 11 were not individually swept — **not checked**. |
| `sampling-policy-enum-owner` | `residual-machinery`'s `sampling-policy` enum is `UniformBatch \| RAD(τ) \| Importance \| ValidationOnly`; `11.8` §3.1 has `UniformBatch \| RADAdaptive \| PerEpoch \| OnDemand`. Which is current, and where did `PerEpoch`/`OnDemand` go? | `oracle/seams/residual-machinery` | The tier→policy map (row 47) is stated in the appendix's enum. Translating it needs this answered. |

---

---

### from `strata`

| id | question | owning page | why it is open |
|---|---|---|---|
| `environment-schema` | What is the closed field schema of the `Environment` record, and which fields are structural vs swept? | `oracle/state/crystal-inputs` | **Homeless fact, second confirmed instance.** No page claims `Environment` in `canonical-for`, yet it appears in every registry row's `applicability`, in `ResidualGenerator`, in several property templates, in `Validate`, and in seam R4. A structural field misfiled as swept silently reuses a kernel outside its envelope |
| `state-wire-schema` | Per slot: dtype, unit, index order, gauge/sign convention — and `γ̂`'s shape as a function of the `CompressionPlan` | `oracle/state/unified-state` | **The dangling promise.** Seam R1 cites this page for "per-slot array shapes and layouts, units, and gauge conventions"; the table was never written. The citation resolves, so no checker can see it |
| `query-domain-per-slot` | What is a query coordinate for each slot under R2, and what is the scope of the discretization-invariance claim? | `oracle/seams/pino-bridge` | **Genuinely open.** If slot 6's emission width is set by the compiling kernel's cutoff, one trained operator cannot serve two oracle-files at different cutoffs. Three named ways out; the unit must pick one. A bounded deferral is acceptable here; an unscoped transfer claim is not |
| `operator-input-side` | What does `Learnable_Structure` *consume*? Crystal identity appears in no seam requirement | `operator/seam` | Deferred unit I3. The contract specifies emission and conditioning and is silent on input |
| `time-semantics-at-seam` | What does R4's "query time" mean when `/physics` holds no trajectory? | `operator/seam` | Deferred unit I4. R4 names a query time; the oracle is scorer-only and nothing accumulates |
| `supervision-coverage-per-slot` | Which slots does the supervised stage actually constrain? | `operator/training` | Deferred unit I5. Partially answered by the H₂ coverage analysis (row 142): 3 present, 1 partial, 3 absent — but for one run, not for a training corpus |
| `toolchain-selection` | Which language/toolchain configuration, tested against bring-your-own reverse-mode AD, an e-graph library, a typed IR, and a codegen path? | `program/build` | R0.1. Unblocks nearly everything; canon marks it closed and it is not |
| `obligation-9-scope` | Does cert obligation 9 rebind to the true surrogate rows, split into surrogate-validity + relaxation-validity, or get rescoped? | `oracle/certification/cert-obligations` | Obligation 9 says "surrogate-net validity (for `D4` surrogate formulas)" but `D4` now means *relaxed*; its checker body describes a learned net, which a soft-hull relaxation is not. The one actual learned surrogate is `D2` and falls outside the obligation |
| `surrogate-net-scope` | Which registry rows does `open-decisions` item 1 govern, now that `D4` no longer means surrogate? | `oracle/registry/named-formulas` | Must be re-established **before** the build-vs-adopt decision can be made. This is plan §2 defect 6 |
| `pde-mesh-adjoint` | Discrete or continuous adjoint of the finite-volume operator for the macro continuum tier? | `oracle/seams/residual-machinery` | R5.3/R7.2, open |
| `layer-175-minimum-spec` | What is the minimum sufficient for a later contributor to implement self-consistent GW / DMFT? | `oracle/seams/residual-machinery` | R7.4. V1 ships type/cert scaffolding + loud not-implemented stubs |
| `adjoint-drift-monitoring` | Stage 4 synthesizes the composition's adjoint over a rewritten graph; nothing revalidates it | `oracle/seams/residual-machinery` | The one remaining open verifier-soundness gap (`README.md:107-113`). The plan's own §5 worked example |
| `frohlich-kernelext-assembly` | Assembling the multi-mode Fröhlich parameter set into the `KernelExt.Parametric` slot | `oracle/registry/computational-methods` | Source pinned (Mengle & Kioupakis Table I); assembly remains research-side work |
| `b3-polarization-bowing` | The exact AlGaN bowing `b` — +0.019 vs +0.021 C/m² both circulate, and secondary quotes are demonstrably corrupted | `oracle/accuracy/accuracy-ledger` | Gated on a primary read of FBA APL 80 1204 (2002) / Ambacher JPCM 14 3399 (2002). **User-action item** — no open copies exist; Javier acquiring via Texas State |
| `caughey-thomas-quartet` | The β-Ga₂O₃ C–T electron quartet (μ_max/μ_min/N_ref/α) | `oracle/accuracy/accuracy-ledger` | No consensus published fit; audit confirmed the gap genuine. Derive from published μ(N_D) or acquire |
| `debye-temperature-spread` | β-Ga₂O₃ Θ_D — literature spread 420–870 K | `oracle/accuracy/accuracy-ledger` | Recorded as a spread, central 738 K pinned; the lower endpoint is unpinned |
| `evolver-wave-scope` | Does the evolver sibling ship, and in what order (slow tier → macro → micro-restricted)? | `oracle/compilation/compose-time-pipeline` | The memo's §3.5 is explicitly a V-ordering, "not a commitment"; the verdict survives-with-restrictions was never converted into a build decision |

---

