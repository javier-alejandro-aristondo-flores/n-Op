# Merged — log

Fragments: 11/11

### from `oracle-state`

| date | finding or decision | evidence | attribution | superseded |
|---|---|---|---|---|
| 2026-07 (reconciliation) | **The refined emergence axiom.** A quantity is emergent from a tier iff recoverable by coarse-graining *on the same timescale and the same scale*. This admitted the slow and macro tiers as first-class state without reintroducing the constraint-manifold pathology, and made aging and device-scale operation representable at all | `multiscale-state §1` (`2.4:66-88`); landed at `unified-state:46-62` | corpus reconciliation | the unqualified "distributions and defect populations are emergent" rule of `unified-state` |
| 2026-07 | **`unified-state` ⊥ `born-oppenheimer-levels` resolved:** L4's "own irreducible state" is made concrete as the macro continuum-field tier, with the full distribution kept emergent by moment closure | `born-oppenheimer-levels:62-71`; `multiscale-state:101-105` | corpus reconciliation | the stated contradiction between the two pages |
| 2026-07-21 | **`/physics` is scorer-only for `γ̂`.** Resolved a standing contradiction: `library-landscape` and the README denied trajectories while `gamma-hat §2` listed time-stepping as a `γ̂` write path. Canon won; the write path is construction and self-consistency only. Consequence: drift is **exported** to the integrating consumer via the steppable-form manifest, not dissolved | `gamma-hat:67-73`; `10.5-timeline.md:257-260` | 2026-07-21 oracle-file decision | `gamma-hat §2`'s time-stepping write path |
| 2026-07-21 | **The four γ̂ data-structure questions were one problem, and it closed.** Exact content-addressed identity meeting approximate numerics, seen from four sides. Resolution: **identity stays exact; ε is estimated beside it.** `≈_ε` is not transitive ⇒ no quotient, no canonical representative, nothing to hash — so identity cannot be bisimulation-up-to-ε. Dispositions: ε-equality → the rewrite-admission rule; materialization → a Stage-4 adjoint-tape schedule (a cost question with no error term); drift → exported to the consumer; rank-applicability → a Stage-4 compile-time predicate | `gamma-hat §4` (`2.3:123-177`); `representation-substrate §4.1`/`§20.4.2`; `open-decisions:58-70` | 2026-07-21 | `open-decisions` item 3; the "genuinely open CS problems" framing in `computational-overview` |
| 2026-07 | **H redistribution range corrected 170×.** Diamond interstitial H at 500 °C: `√(Dt) ≈ 6 µm` in 1000 h, not the earlier ~1 mm. Changes whether H redistribution is a device-scale or a near-surface phenomenon | `multiscale-state:177-180` (F-G2, `E_diff = 1.7 eV`, `D(500 °C) ≈ 1e−13 cm²/s`) | corpus reconciliation | the ~1 mm estimate |
| 2026-07 | **An embedded copy of registry rows drifted from the CSV and was removed**, establishing `physics/library/formulas/registry-manifest.csv` as the sole source for row content | `multiscale-state:458-461` | 2026-07 reconciliation | the embedded row table in `multiscale-state §14` |
| 2026-07 | **Frenkel-pair yield was dimensionally invalid** without the macroscopic displacement cross-section `Σ_d = N_atom·σ_d` (cm⁻¹): the bare `N_d·(1−η)·Φ_dose` is a fluence (cm⁻²), not a concentration | `multiscale-state:201-211`; `traps §7` | corpus reconciliation | the pre-`Σ_d` F-H2 expression |
| 2026-07 | **Scharfetter–Gummel is mandatory for the macro carrier flux.** At the UWBG operating point (1 MV/cm, ~10 nm cells) the cell Péclet number is ≈40, where central differencing makes the *residual operator itself* wrong — the operator would be scored against a discretization artifact | `multiscale-state:380-392`; `traps §31` | corpus reconciliation | naive/central finite-volume differencing for `j_f` |
| 2026-07-21 | **The dressing-staleness bound exists:** a frozen Layer-1.25 dressing owes an estimator like any other approximation, and the estimator *is* the validity radius — `‖Δx‖·‖∂(dressing)/∂x‖_ref`, sensitivity measured once at the reference state | `open-decisions:113-121` | 2026-07-21 | verifier-soundness gap 3 ("no declared validity radius"), **still stated as open at `born-oppenheimer-levels:104-109`** |
| 2026-07-21 | **`tau-energy-pop` and `tau-energy-acoustic` merged into `tau-energy-POP-acoustic` (row 73)** — one row carrying both the polar-optical and acoustic channels | `retired-names.csv:56-57`; `multiscale-state:309-311` | 2026-07-21 retag | the two separate proposed formulas |

---

### from `oracle-laws-seams`

| date | finding or decision | evidence | attribution | superseded |
|---|---|---|---|---|
| 2026-07 | **γ̂ admissibility gates added to `Positivity`** — ensemble N-representability (`γ̂†=γ̂`, `0 ⪯ γ̂ ⪯ 1`) as per-k-block spectral bounds, plus applicability-gated T=0 idempotency. Rationale: a candidate γ̂ outside these bounds can zero every EOM residual while being unphysical, so without them the oracle is not sound as a verifier of the *state*, only of its dynamics. | `3.2-residual-definitions.md:120-129` ("2026-07 gap-audit A1") | 2026-07 gap-audit, item A1 | extends the pre-audit `Positivity` category |
| 2026-07 | **Gauge fixing and the electrostatic partition made normative.** `A` is carried in the Weyl gauge with transversality; `E_EM` counts the transverse sector only; the longitudinal/electrostatic sector is owned by the matter functionals. Consequence: the `EOM/A` residual becomes gauge-unambiguous and no electrostatic energy is double-counted between field and matter terms. | `3.1-generic-dynamics.md:169-182` ("2026-07 gap-audit A2") | 2026-07 gap-audit, item A2 | prior `E_EM` statement, which did not partition |
| 2026-07-2x | **A restated enumeration drifted against its canonical ledger** — the tolerance list restated in `residual-definitions §7` named ten symbols against a ledger of sixteen. Recorded because it is the empirical basis for the one-home rule this restructure is built on, and it is the only *measured* instance of the drift in the corpus. | `3.2-residual-definitions.md:311-314`; ledger at `cert-obligations §1.2` | corpus self-audit (date not stated on the page — **needs pinning from git before this row is written to the log**) | the ten-symbol restatement |
| 2026-06-10 | **AHC ZPR amplitudes re-tagged `isochoric`** after being carried as `total`, and the `slope-kind ∈ {isochoric, total}` tag introduced as a machine-checkable double-count guard against row 63's thermal-expansion path. A `total` slope already folds in the lattice-expansion shift (~30–40%) that row 63 carries separately. | `3.3-coupling-structure.md:350-364`; `[timeline] §2026-06-10 (Wave 1)`; ledger row 15 at `9.1-accuracy-ledger.md:71` | Wave-1 III-N audit | the prior `total` tag on Engel/Miglio isochoric magnitudes |
| 2026-07-2x | **`ResidualGenerator.bundle` corrected from scalar to set.** The field read `BundleId` singular while 40 of the 134 registry rows carry two bundles (`B1/B4`, `B3/B9`, …), so every dual-bundle row was silently narrowed at registration. | `7.2-residual-machinery.md:57-65` | corpus self-audit (date not stated — **pin from git**) | the singular `BundleId` field |
| 2026-07-21 | **`CouplingSpec` promoted from a bare `SparseSet[CouplingRegistry]` to a `{channels, theory_context}` record, with a schema-version bump** so old bare-set addresses cannot collide with new record addresses. Makes the theory frame part of content-addressed identity automatically. | `3.3-coupling-structure.md:404-410` | coupling coverage-policy wave | the bare-set `CouplingSpec` |
| 2026-07-2x | **Piezoelectric-acoustic e-ph channel added** (`LongRangeStatic(1)`, `1/q` pole), the second long-range e-ph mechanism the wurtzite III-N members carry, `is-noncentrosymmetric`-gated. Closed the coupling-channel coverage-policy open item. | `3.3-coupling-structure.md:462-466`; `open-decisions` Closed decisions | coupling coverage-policy wave | the 14-row template table |
| 2026-07-21 | **The frozen-Layer-1.25 validity radius became computable.** `staleness-coeff = ‖∂(dressing)/∂x‖` measured once at the reference state at compile time, times a runtime state norm, is the dressing-staleness term `combineTol` listed but had no number for. | `7.2-residual-machinery.md:249-261`; `open-decisions` verifier-gap 3 | 2026-07-21 ε-rule wave | the unquantified dressing-staleness term |
| 2026-07-2x | **Rows 46 and 50 shipped adjoint certificates for gradients that do not exist** — piecewise-constant outputs pass the vJp-vs-JvP gate spuriously. Established that a passing gate is not evidence of a correct `D2` tag. | `7.2-residual-machinery.md:190-197`; `traps` §52 | corpus self-audit | the assumption that the registration gate is sufficient |
| 2026-07-2x | **Row 122's `D3` gate is deferred, not passed.** A dormant/anchored row has no fixed point, so there is no Jacobian to condition and both gates pass trivially — the spurious-pass failure one level worse, since the gradient is structurally absent rather than locally zero. Generalized: any row whose `source` declares it dormant or anchored inherits the rule. | `7.2-residual-machinery.md:198-208`; registry row 122 `iterative-lbte-kappa` | corpus self-audit | the unqualified `D3` reading of row 122 |
| 2026-07-2x | **`Δα` learned-correction freezing rule.** A correction trained on the same residual it modifies can co-adapt to zero that residual and destroy the obligation-9 domain it exists to protect. Therefore: fit only against external anchors, frozen w.r.t. the PINO loss; and with no V1 anchors, ship as identity and keep the high-E×high-T corner cert-refused. | `3.3-coupling-structure.md:382-392`; `9.1-accuracy-ledger.md:105` (row 49) | high-field wave | the unconstrained learned-coefficient path |
| — | **Survey finding (this pass): the residual-category count is unchecked on its own owning page.** The count checker matches only `\d+ residual categories`; `residual-definitions` states its count as "nineteen" and as "**19** symbols". Corrupting it on the owning page passes clean; corrupting it on any citing page fires. | `check_book_structure.py:333`; calibration probe, this document's header | this survey | the belief that the 19-category count is checked |
| — | **Survey finding (this pass): `residual-generator-catalog` is not a page id.** A bare id + `§` citation to a nonexistent page is silently skipped by `check_citations` (`:466-467`), so the corpus carries a dangling reference in canon that no checker can see. | `7.2-residual-machinery.md:68`; `check_book_structure.py:466` | this survey | the belief that §-coordinate citations are fully validated |
| — | **Survey finding (this pass): a third live retired-id site, inside canon.** `cross-cutting-rules:28` directs readers to `arch-*` for architectural decisions. The plan located two retired ids, both in an untracked file; this one is in `journal/pages/`. | `7.3-cross-cutting-rules.md:28`; `physics/library/formulas/retired-ids.csv` | this survey | the plan's count of two live retired-id sites |
| — | **Survey finding (this pass): `physics/research/group-{A,B,C}-*.md` does not exist.** `generic-dynamics` grounds its nine-regime extraction table in files absent from the repo. Path citations are checked by nothing. | `3.1-generic-dynamics.md:123`; `find . -name "group-*"` returns empty | this survey | — |

---

---

### from `oracle-compilation`

| date | finding or decision | evidence | attribution | superseded |
|---|---|---|---|---|
| 2026-07-21 | **Identity stays exact; ε rides alongside.** `≈_ε` is not transitive, so it induces a covering by maximal cliques rather than a quotient — no canonical representative, nothing to hash. Content addressing therefore cannot absorb tolerance | `4.3:186-219` → `oracle/compilation/representation-substrate#identity-exact` | corpus (keystone resolution of the γ̂ cluster) | the framing of ε-equality as an open CS problem |
| 2026-07-21 | **Rewrite-admission rule.** A Stage-3 rewrite is admissible iff exact over ℝ, its float side conditions are discharged by an e-class interval/not-equals analysis, and it registers a fidelity generator | `4.2:145-158` → `oracle/compilation/compose-time-pipeline#rewrite-admission` | corpus, adopting Zhang et al. (PLDI 2023, `egglog`) | "exactness-only" as the admission gate |
| 2026-07-22 | **Correction to the egglog evidence.** The sound rule set won on accuracy in 104 of 289 benchmarks *against 135 where the unsound set still won*. The corpus had quoted 104 without 135, turning an even trade into a win | `4.2:160-173` | corpus self-correction | the one-sided 104-only citation |
| 2026-07-21 | **Materialization policy reclassified** from an accuracy problem to a Stage-4 scheduling problem: forcing vs deferring changes cost, not value, so no error term exists and no fidelity generator is owed | `4.2:214-233`, `gamma-hat:143-150` | corpus | "materialization policy … no principled default" as an open γ̂ data-structure problem |
| 2026-07-21 | **Attribution correction:** DAG-rematerialization NP-completeness is Naumann (*J. Discrete Algorithms* 7(4) 402–410, 2009), a separate result from `revolve` (Griewank & Walther, *ACM TOMS* 26(1) 19–45, 2000). Previously folded into the `revolve` parenthetical as though one source gave both | `4.2:220-222`, `gamma-hat:148-149` | corpus self-correction | the merged citation |
| 2026-07-21 | **Rejected addressing alternatives recorded:** quantized addressing (buys ε-dedup at the cost of injectivity, plus a grid artifact at every cell boundary) and ball/interval addressing (relocates non-transitivity into ball overlap, doubles every payload) | `4.3:221-230` | corpus | — (recorded to prevent re-proposal) |
| 2026-07-16 | **Integrator interface closed:** `/physics` emits a per-tier tangent map and a steppable-form manifest — a pure function, not an integrator. Long-trajectory drift is *exported* to the integrating consumer, not dissolved | `4.4:627-629`, `gamma-hat:96-101` | corpus | long-trajectory drift as an internal γ̂ open problem |
| 2026-07-21 | **Runtime cost is three-class, not one:** per-sample core (µs–ms), on-request spectral (0.1–10 s, per-epoch cached), per-composition reference (seconds–minutes, calibration-only) | `4.2:283-294` | corpus | the single "µs–ms" runtime figure |
| 2026-07-21 | **Rank-dependent applicability of the low-rank γ̂ slot is a compile-time predicate** on `(PeriodicityStructure, SiteDecoration)` decided at Stage 4 — the runtime cost that was the original objection no longer exists | `gamma-hat:170-177` | corpus | the runtime-check framing |
| 2026-07-22 | **Compression plans carry per-plan error targets;** ranks are chosen to meet the target, not by structure alone, and the target enters the per-residual budget via `Quantity.combineTol` | `4.2:200-204` | corpus | rank selection by structure alone |
| *(undated)* | **Adjoint-tape materialization owes no fidelity generator** — recomputing and reading a stored value give the same value. Marks the boundary of the ε rule: it is about value, not cost | `4.2:223-229` | corpus | — |

*Included with a note per the brief's under-logging guidance:* the last row is
undated in the corpus; the timeline surveyor may be able to date it.

---

---

### from `oracle-cert-accuracy`

| date | finding or decision | evidence | attribution | superseded |
|---|---|---|---|---|
| 2026-07-22 | Registry rows 116 (`interface-trap-density`) and 119 (`subthreshold-swing-Dit`) removed from the `is-noncentrosymmetric`-gated polarization band 113–119. Neither depends on inversion-symmetry breaking, so the gate had been refusing trap density and subthreshold swing for **every centrosymmetric host, including diamond — the MVP system** — and was circular, since row 115 consumes `D_it`. Row 119 rebound to `is-dielectric-layer`; row 116 left on an always-true stub pending a named predicate | `5.2-applicability-classifiers.md:65-88`; `10.2-open-decisions.md:71-81` | corpus gap-audit, 2026-07-22 | the whole-band `113–119` gating |
| 2026-07 (Wave-2 audit, 2026-07-16) | The polar/noncentrosymmetric conflation was separated into two independent predicates. They coincide on diamond (both false) and wurtzite III-N (both true) — which is why the conflation was invisible — and split on β-Ga₂O₃ (C2/m: centrosymmetric, yet its dominant mobility limiter is a multi-mode Fröhlich interaction) | `5.2-applicability-classifiers.md:43-63` | 2026-07 gap-audit / Wave-2 | single "polar" predicate |
| 2026-07 | The MVP status legend was split: "path-met" means the closed-form path exists **and** its diamond anchors are seeded so cert 4/8 can check the target — explicitly **not** that a cert run has passed. The former bare "met" read as target-met | `9.1-accuracy-ledger.md:115-119` | 2026-07 gap-audit D0 | bare "met" status |
| 2026-07-21 (approx.) | The tolerance-symbol check was rebuilt to **harvest** its symbol list from the ledger table it checks, replacing nine hard-coded symbols against a ledger of seventeen — a check that had reported clean on 53% coverage with the shortfall recorded in a comment and acted on by nothing | `check_data_agreement.py:172-183`; the stale description survives at `5.1-cert-obligations.md:156-158` | corpus tooling work | the hard-coded nine-symbol list |
| undated | Exhaustiveness of the tolerance ledger was **demonstrated unmachine-checkable** and the failed attempt recorded rather than retried: `τ` is not a reserved tolerance prefix (48 correct uses across six pages are physical times), and narrowing by value shape does not separate a `1e-12` s relaxation time from a `1e-12` tolerance. Verdict: a review rule, labelled as one | `5.1-cert-obligations.md:119-129`; `check_data_agreement.py:203-220`; `[traps] §69` | corpus tooling work | the claim that "`δ`/`τ` denotes a tolerance throughout" |
| 2026-07-16 (Wave 2) | A **fourth** β-Ga₂O₃ axis frame was identified in the reference data — the RUS+LDI elastic frame `x∥a y∥b z∥c*`, whose first axis is the real `a`, 13.83° from `a*` — so its `C₁₁` is not along the crystal-physical `e₁`. Four frames, one material | `9.1-accuracy-ledger.md:174-181`; `10.4-traps.md:141-149` | Wave-2 adversarial audit | the three-frame guard |
| 2026-07-16 (Wave 2) | Pyroelectric coefficients seeded with a sign correction: `p` is **positive** in the seeded ZB-reference frame (AlN +3.0e−6, GaN +4.5e−6 C/m²K, σ = ×2), because ZB-ref `P_sp` is negative and its magnitude falls with T. Raw literature quotes `p` negative under the opposite convention. The drift is ~20–30% of `n_s·q` over ΔT ≈ 750 K — larger than the ±5% ΔP budget at the operating point | `9.1-accuracy-ledger.md:239-247` | Wave-2 adversarial audit | unsigned/unflipped literature values |
| 2026-07-16 | Lan–Ebert PRB 113 155302 (2026), off-axis holography, refutes the ZB-reference bowing curvature for InGaN and validates the LH frame; the ±5% AlGaN/GaN ΔP anchor is untouched. Recorded because it *narrows* where the Dreyer accidental cancellation may be relied on | `9.1-accuracy-ledger.md:91` (#35) | Wave-2 gate-G1 reading | — |
| 2026-06-10 (Wave 1) → 2026-07-16 | The `slope-kind ∈ {isochoric, total}` tag was introduced on AHC gap slopes after the Engel/Miglio AHC values (clamped-lattice e-ph only, i.e. isochoric) had been carried under a `total` tag — "the worst of both", since a `total` tag makes the cert refuse co-activation with row 63's thermal-expansion path | `9.1-accuracy-ledger.md:148-163`; `5.1-cert-obligations.md:174-178` | Pass C / Wave-1 audit | `total` tag on isochoric magnitudes |
| undated | The Dreyer ±5%-on-ΔP result was established to hold by an **accidental cancellation** (PRX 6 021038 (2016) §V.D–E) between the spurious ZB-reference term and the proper/improper-e₃₁ error — large, opposite-sign, nearly cancelling — and **not** by generic reference-cancellation. Consequences: the pairing cert, and the refusal for high-In InGaN/GaN | `9.1-accuracy-ledger.md:91`; `5.1-cert-obligations.md:183-191` | corpus physics audit | "generic reference cancellation" reading |

---

### from `oracle-registry`

| date | finding or decision | evidence | attribution | superseded |
|---|---|---|---|---|
| 2026-07-21 | **`DN` split out of `D0`.** The old `D0` conflated "a pure read whose adjoint is the identity" with "an integer output with no derivative" — opposite answers to the question the tag exists to answer. Eleven smooth analytic rows sat at `D0` and therefore registered with **no adjoint at all**, because `build-verification` exempts `D0`; among them the alloy-lattice interpolation that `product` promises is directly optimizable through baked gradients. Those eleven are now `D1`. | `6.6-named-formulas.md:123-130`; `10.2-open-decisions.md`; CSV now shows exactly one `D0` row | corpus retag pass | the single-`D0` legend |
| 2026-07-21 | **The twelfth `D0` row.** `chemical-potential-ref-table` survived the first retag because "ref-cache lookup" reads like a pure read. It takes `T` and `P`, so tagging it `D0` zeroed `∂μ_i/∂T` and left row 69's Maxwell cross-derivative residual **vacuously satisfied**. Generalized rule: an implementation detail (a cache) is not a mathematical one (an identity adjoint). | `6.6-named-formulas.md:130-135` | second-pass audit | the first retag pass's coverage claim |
| undated (before 2026-07-22) | **Mixed-output rule adopted.** A row returning a real quantity *and* a discrete label is tagged by its continuous component; a discrete label never drags a row to `DN`. Without it the same construction was tagged three ways — `radius-ratio-coordination-class`, `elastic-stability-criteria` and `structure-uniqueness-CSP` read `D1`, `D1` and `DX`. | `6.6-named-formulas.md:111-121` | corpus retag pass | the `D1`/`D1`/`DX` tagging of those three rows |
| 2026-07-21 | **`D3` established as a refinement of `D2`, not an alternative**, and the `τ_cond` conditioning gate added. Every `D3` row runs the `D2` registration gate **plus** a fixed-point-Jacobian conditioning check. Without it the tag would be strictly weaker than `D2`, which is backwards: the ill-conditioned fixed point is the one failure mode canon names for it, and the gradient it produces is *large and wrong* rather than absent. Bites `fermi-level-charge-neutral`, `self-consistent-charge-balance` (∂F/∂E_F flattest exactly in a wide-gap intrinsic semiconductor) and `SCPH-self-consistent-phonons` (soft modes). | `6.6-named-formulas.md:92-104`; `10.2-open-decisions.md:133-135` | gap-closure pass | the unbounded-conditioning open item |
| undated (before 2026-07-22) | **The `path` field un-retired as an anchor class.** It was declared retired on the grounds that the always-cheap reframe collapsed the two runtime paths. It did — but the column's live meaning is *what a row's value is anchored against*, and that is the axis obligation-6 consistency pairs run along. Retiring a field several pages depend on is how a load-bearing distinction becomes folklore. | `6.6-named-formulas.md:152-173`; CSV `Path` = 117 cheap / 15 faithful | corpus audit | the retirement of the `path` field |
| undated (before 2026-07-22) | **`DX` renamed `DN`.** `DX` collided with the DX center — the deep-donor configuration this corpus discusses in AlGaN and AlN — so searching for the tag returned the physics and searching for the physics returned the tag. Diff tags are written in backticks in prose for the same reason; the wurtzite deformation potentials `D1`–`D5` are the other live collision. | `6.6-named-formulas.md:137-143` | nomenclature audit | the `DX` spelling |
| undated (before 2026-07-22) | **Four inline-math invocations replaced by declared names**, closing a violation of the no-inline-math rule inside the page validating the closed vocabulary. The expressions are recorded alongside the names — `(2ω/c)·Im(√ε)`, `Re(√ε)`, `σ/(n·e)`, `E_BO + F_vib + F_el` — which is what makes registering those four rows transcription rather than research. | `6.8-typed-compositions.md:214-226` | corpus audit | inline mathematics in `formula =` slots |
| undated (before 2026-07-22) | **Five corrected physical forms made canonical**: optical absorption `(2ω/c)·Im(√ε)`; the operator-spectrum-area sum rule carries the `2/π` prefactor; the acoustic sum rule sums over all lattice translations (`Σ_J Σ_R Φ_{IαJβ}(R) = 0`); the magnetic relaxation term is the orientation-preserving `S × (S × H_eff)`; the harmonic transition-rate normalization consumes products-over-modes, not spectra. | `6.6-named-formulas.md:145-150`; `6.8-typed-compositions.md:144-145` | physics reconciliation | the pre-correction forms of all five |
| undated (before 2026-07-22) | **`B1..B11` bundle labels re-canonicalized.** `properties` carried a pre-canon scheme in which `B11` meant "topology atlas"; canon now has `B11` = degradation. | `6.10-properties.md:51-53` | canon consolidation | the pre-canon `B*` scheme in `properties` |
| undated (before 2026-07-22) | **`mesh-interpolation` registered as a sub-method, preserving the closed 12-method alphabet.** The compile-time band/e-ph interpolator — Fourier for gauge-free band energies and velocities, Wannier–EPW for gauge-sensitive e-ph matrix elements, with mandatory dipole/quadrupole polar corrections; runtime reads the interpolated grid only, C1-clean. Interpolation is a sub-method, not a new top-level method. | `6.1-canonical-vocabularies.md:72-77`; `6.4-computational-methods.md:83-87` | UWBG scope extension | a proposed thirteenth method |
| 2026-07 | **The `Source`-legend lag closed.** Rows 128–134 (the 2026-07 gap-audit package) carry `Source = topology atlas`; the Counts prose always described them correctly and only the column legend lagged. | `6.9-formula-registry.md:95` | gap-audit pass | the pre-fix `Source` legend |
| undated (before 2026-07-22) | **Five incompatible differentiability legends collapsed to one**, and `formula-registry` switched from restating the vocabulary to linking it. The restatement is named as the cause of the five-legend divergence. | `6.9-formula-registry.md:104-106` | legend reconciliation | four superseded diff legends |
| 2026-07-22 | **Bundle vocabulary harvested from the field schema, not the nearby enumeration.** A first version of the `Bundle` column check harvested only the `6.1 §4` table, reported rows 91–94 (`Z*`, `ε∞`, `χ∞`, `α_M`, the `L1` primitives) as defects, and the "fix" retagged four correct rows to `B1` before either canon page was read. Both pages say `L1` is deliberate. | `check_data_agreement.py:436-457`; `10.4-traps.md` §70; commit `287d2eb` | checker calibration | the table-only harvest and the four incorrect `B1` retags |

---

---

### from `program`

| date | finding or decision | evidence | attribution | superseded |
|---|---|---|---|---|
| 2026-07-16 | Reconciliation campaign: the full specification base passed a staged sweep finding and fixing ~88 verified defects, followed by adversarial multi-agent certification. Auditors were calibration-gated (5/5 planted contradictions detected before their reports were trusted), ran two independent rounds (first by document family, then re-sliced by invariant class), and produced evidence transcripts rather than verdicts. | `1.5-rationale.md:179-184`; `10.5-timeline.md §2026-07-16 (reconciliation pass)` | multi-agent audit campaign | — |
| 2026-07-16 | The scorer↔stepper duality research resolved **"survives with restrictions"**: the evolver is a flag-gated Stage-4 sibling artifact ("evolver-file") sharing the scorer's content-addressed RHS forests, with the integrator staying consumer-side. Time-evolution product verbs remain unclaimed until that lowering is specified and built as its own named wave (slow tier first); when trajectories exist, the scorer audits them under the same slot keys. | `1.2-product.md:242-251`; `10.2-open-decisions.md:213-216,258-259` | evolver-duality deep-dive | supersedes the "commissioning an independent deep-dive" status still stated at `1.5-rationale.md:211-216` — see Contradiction C2 |
| 2026-06-25 | The single waiver carried by the reconciliation campaign — a stale formula count in the CS-audience deck — was discharged by retiring the deck. No waiver stands. | `1.5-rationale.md:184-187`; `10.5-timeline.md §2026-06-25 (CS-audience deck)` | reconciliation campaign follow-up | — |
| 2026-07-21 | An adversarial pass re-audited the reconciliation campaign's own bulk edits and corrected what those edits had broken. Recording this matters beyond the fix: it is the pass whose D4 retag (*surrogate* → *relaxed*) left two live references to a "D4 surrogate" set that no longer exists (`10.2-open-decisions` item 1, and `8.3-mvp-scope.md:43`). | `1.5-rationale.md:187-188`; `8.3-mvp-scope.md:43-44` | adversarial re-audit | supersedes the *surrogate* reading of the D4 tag |
| 2026-07-16 | Diamond strain-hypersurface feedstock characterised: 1,179 rows at hybrid level (HSE06, gap-tuned exact exchange), **1,131 distinct strain shapes after de-duplication**, six lattice-distortion families to ±10%, with stress tensors, mapped onto the oracle's import interface. Methodological guard established: **de-duplication must precede any fit.** | `1.5-rationale.md:197-204` | dataset salvage + health audit | supersedes the ~877 figure, which was a byte-salvage count and never a row count |
| 2026-07-08 | Diamond reference battery seeded with a machine-readable anchor for every declared accuracy target, including the high-T 4-phonon anchor κ(773 K) ≈ 620 W/m·K and κ(1100 K), landing as registry rows 121–122 (closed-form 4-phonon correction + dormant iterative-LBTE consistency sibling). | `8.4-forced-decisions.md:37-50`; `physics/library/cert/reference-data/transport-coefficients.csv` | battery seeding pass | — |
| 2026-07-21 | The identity/ε rule closed three of the four named verifier-soundness gaps. Recorded here because `8.7`'s registration gate depends on which gaps remain live. | `10.2-open-decisions.md:94-97` | 2026-07-21 pass | supersedes three of the four originally-named gaps |

---

---

### from `practice`

Rows L1–L12 convert the twelve existing timeline entries. **Every one of the twelve
fails the attribution field** — the page names "four skeptic passes", "two adversarial
auditors", "six auditors", "66 skeptics", and never a person, model or tool version.
Entries failing a second field are marked ⚠.

| date | finding or decision | evidence | attribution | superseded |
|---|---|---|---|---|
| L1 | 2026-06-09 — Founding physics audit. Three structural gaps found and closed: the degradation pipeline, the micro↔device bridge, and polarization. Produced the composed error budget (named the highest-leverage single fix), the coefficient-provenance contract and its refusal, the GENERIC per-tier generator structure, and the rule that doc-canon discipline should be mechanized as a check | `oracle/state/multiscale-state`, `oracle/accuracy/accuracy-ledger`, `oracle/laws/coupling-structure`, `oracle/certification/cert-obligations`, `oracle/laws/generic-dynamics`, `practice/conventions` | **MISSING** — "deep adversarial audit", actor unnamed | not stated ⚠ |
| L2 | 2026-06-10 — Re-audit addendum. All priority-zero items and all three structural gaps confirmed resolved; ten further findings landed: the Frenkel-pair dimensional defect, mandatory Scharfetter–Gummel, the machine-checkable slope-kind tag, the learned-correction training contract, the degenerate-Einstein and plasmon–phonon gates, the image-force reconciliation | `oracle/state/multiscale-state`, `oracle/laws/coupling-structure`, `oracle/certification/cert-obligations`, `oracle/certification/out-of-scope`, `oracle/accuracy/accuracy-ledger`; naming-is-addressing promoted to `practice/conventions` | **MISSING** | supersedes parts of L1's remediation |
| L3 | 2026-06-10 (Wave 1) — III-N seeding and its adversarial audit. Produced the polarization-convention pairing rule (trap 12) and the accidental-cancellation finding (trap 13); sixteen numeric and citation corrections A1–A16, one of which caught a **fabricated citation** in previously-cleared material, which became the clearance-is-not-evidence rule (trap 45) | `practice/traps#frames` 12–13, `practice/traps#practice` 45; `oracle/certification/cert-obligations`, `oracle/laws/coupling-structure`, `oracle/accuracy/accuracy-ledger`, seeded reference CSVs; the A1–A16 table is cited by 13 live sites | **MISSING** — "two inventory passes, six primary-literature deep dives, two adversarial auditors" | A16 explicitly supersedes L2's clearance of A5/A7/A8 |
| L4 | 2026-06-10 (Pass C) — per-material accuracy design. The AHC gap renormalization, the 4-phonon high-T correction, the breakdown-field temperature slope (correcting a **sign error**, trap 3), the T,P-aware hull with its metastability band, the Wannierization quality gate | `oracle/accuracy/accuracy-ledger` and registry rows — **but the page says "the full primary-source citations are in git history"** ⚠ | **MISSING** | corrects the prior "E_b drops ~20%" claim |
| L5 | 2026-06-25 — CS-audience deck retired. Carried a stale formula count under a standing waiver and published a boolean validity predicate that canon forbids; the waiver is discharged with it | `program/purpose/product` (evidence-never-verdicts) | **MISSING** | supersedes the deck; the boolean-validity-predicate prohibition supersedes its published claim |
| L6 | 2026-07-07 — Gap audit. Verifier-soundness holes closed: γ̂ admissibility must be *scored* (trap 24), the gauge/electrostatic partition (trap 25), the two-predicate polar split (trap 26). Seven registry rows added. The forward wave programme (Waves 3–5, the reduced metals schema, the Curie-point-inside-the-operating-window decision) promoted to `reference-battery` | `practice/traps#verifier-soundness` 24–26; `oracle/accuracy/reference-battery`; the A–E section index is cited by **8 data-file rows** (7 in `registry-manifest.csv`, 1 in `transport-coefficients.csv`) | **MISSING** — "three parallel inventory passes" | not stated ⚠ |
| L7 | 2026-07-16 — Reconciliation pass and certification. 21 commits, 88 verified findings fixed, a calibration gate, two certifier rounds. Produced the **authority order**, the **frozen-dirs rule**, and the certification protocol (calibration gate, evidence transcripts, dismissed-near-findings log) — all three existed *only* in that report | `practice/conventions`, and (post-restructure) `restructure/auditor-brief.md#ground-rules` | **MISSING** — "two certifier rounds" | **the authority order and frozen-dirs rule are themselves superseded by this restructure (D3, D13)** — record that here |
| L8 | 2026-07-16 (Wave 2) — β-Ga₂O₃ seeding cleared its adversarial audit and seeded, with the pyroelectric rider; the bowing rider remains gated pending a pin-read. Separately the scorer↔stepper duality returned "survives with restrictions", closing the integrator-interface decision | `practice/traps#frames` 16, 39; `oracle/accuracy/accuracy-ledger`; the integrator-interface contract (disposition row 76) | **MISSING** | the evolver decision supersedes the prior open integrator item |
| L9 | 2026-07-21 — Corpus reconciliation into a book. 29 contradictions found, six high-severity; four live defects fixed where canon contradicted the seeded data, including two sign errors that would have inverted physics for anyone seeding from the ledger. 58 pages across 11 chapters, addressed by page `id`, generated apparatus replacing ~10,500 lines of monolith duplication | the corpus itself; superseded in turn by this restructure | **MISSING** — "three systematic passes" | supersedes the pre-book monolith tree |
| L10 | 2026-07-21/22 — Naming: 38 serial page ids (`arch-NN-`, `impl-NN-`) renamed to descriptive phrases; the diff tag `DX` renamed `DN` (collided with the DX center); ~25 registry formulas renamed, including `vibronic-coupling-factor` → `lattice-relaxation-phonon-quanta` and `field-activated-ionization-rate` → `impact-ionization-coefficient`; `GAP` found to mean three things | `practice/traps#practice` 56, 59; `practice/conventions#naming`; `retired-ids.csv`/`retired-names.csv` **which this restructure deletes** | **MISSING** | supersedes the three-numbers-per-page scheme; superseded in turn by D6 (filename = id) |
| L11 | 2026-07-21 — **Exact identity, measured error.** One rule resolves six open faces: *identity is exact, always; ε never enters the address; any operation that can make the computed object differ from its exact counterpart emits a computable a-posteriori estimate of that difference, as cert evidence.* `/physics` confirmed **scorer-only** — γ̂ never evolves in the oracle — resolving a standing contradiction between `library-landscape`/README and `gamma-hat §2`. Drift is *exported* via the steppable-form manifest's encoding validity domain. No new residual category: cert-only generators, so the 19-tag enum is untouched and the training loss unpolluted | `oracle/compilation/representation-substrate#identity`, `oracle/state/gamma-hat`, `oracle/compilation/compose-time-pipeline §3–§4`, `practice/traps#practice` 63–65. Literature: Lubich–Oseledets 2014; Kieri–Lubich–Walach 2016; Ceruti–Kusch–Lubich, *BIT* 62 (2022); Zhang et al., PLDI 2023; Blondel et al., NeurIPS 2022 | **MISSING** | supersedes the four γ̂ open questions and three of four verifier-soundness gaps |
| L12 | 2026-07-21/22 — **Calibration is the artifact whose incompleteness nothing downstream can detect.** Canon claimed `check_the_checkers.py` "plants one defect per check" and "29/29 fired" was cited corpus-wide as warrant for a green run; four data-checker classes and nine structure-checker classes had **no probe at all**. All thirteen worked — the defect was in what the calibration *established*. Repair: coverage is **derived from the checker's source** and asserted at the end of every run. Also: a cost-tier distribution wrong in three of four entries that still summed to 132 (trap 67); a reference-data row whose unquoted comma shifted `Added` to `experimental` and passed both checkers (trap 67/arity); frontmatter reduced by 613 of 1,517 lines; `lint.py`'s unique checks folded into `check_book_structure.py` | `practice/traps#practice` 58, 66, 67; `check_the_checkers.py` uncovered-check gate; `practice/agent-contract#what-green-means` | **MISSING** | supersedes the "one defect per check" claim and the 29/29 warrant |
| L13 | 2026-07-21 — Hardening the book against its own restructure: six adversarial skeptics, one per bulk transformation, briefed to find damage rather than confirm success. ~40 defects, all in the seam between what a transformation intended and what it did. Ten registry rows wrongly tagged or named; **rows 46 and 50 would have shipped a certificate for a gradient that does not exist**; `D3` redefined from an *alternative* to `D2` into a *refinement* of it, plus a `τ_cond` conditioning gate the adjoint gate structurally cannot see. Both checkers were part of the defect. The `depends-on` edge criterion was documented as derivable and derived by nobody — deriving it found 33 missing edges (325 → 358) | `practice/traps#practice` 52, 53, 60; `oracle/registry/named-formulas`, `oracle/seams/residual-machinery §5`; `practice/agent-contract#depends-on` | **MISSING** — "six adversarial skeptics… 66 skeptics tried to refute" | supersedes the `D2`/`D3` vocabulary ordering |
| L14 | 2026-07-21 — **The implementation-language decision was reopened.** Canon recorded it closed and prescribed four languages; it was not closed. Split at its real seam: the four-role polyglot *shape* stays closed, the *picks* are open, and every language mention becomes a candidate carrying the requirement it was chosen to satisfy | `program/build/forced-decisions#language-roles`; `implementation-language-picks` | **MISSING** | supersedes the prior "closed, four languages prescribed" record |
| L15 | 2026-07-21 — Calibrated certification result recorded honestly: six auditors over disjoint invariant classes, each calibrated against a planted defect both checkers passed clean. **Four of six found their exact plant; two found a *different real defect* in the same class and reported it as the plant. Recorded as a 4/6 gate, not 6/6.** Both "wrong" finds were genuine — a glossary claim that a lint check existed when none did, and a trap marked `enforced` at a page that did not state its rule. 54 findings raised, 35 survived refutation, 19 killed | `10.5-timeline.md:309-327`; `practice/agent-contract#reporting` | **MISSING** | — |

---

### from `appendix-a`

| date | finding or decision | evidence | attribution | superseded |
|---|---|---|---|---|
| 2026-07-07 | **L1 — Gap audit B7 opened the group-A/B/C derivations to physics review and found four independent errors.** The pass is the origin of L2–L7. | `11.1:742-747`, `11.2:527-533`, `11.3:711-714`; all three cite `[timeline] §2026-07-07 (gap audit)` B7 | gap-audit B7 | — |
| 2026-07-07 | **L2 — Thermal-expansion formula corrected.** Was `α_IJ = (S⁻¹)·Σγ·c_v` — a compliance/stiffness inversion, with the tensorial Grüneisen undefined and `1/V` missing. Now `α_IJ = (1/V)S_IJKL Σ γ^{(KL)}_{qs} c_v`, `S = C⁻¹` the compliance, `γ^{(KL)}` the strain-Grüneisen tensor whose volumetric trace is the scalar `γ_qs`. | `11.1:502` (corrected form), `11.1:743-746` (record) | gap-audit B7 | the inverted `(S⁻¹)` form |
| 2026-07-07 | **L3 — e-ph vertex mass convention made multi-species-correct.** The bare `1/√(2Mω)` is single-species shorthand; multi-species cells carry per-atom `M_κ` through the mass-weighted eigenvector normalization `Σ_κ M_κ\|e_κ\|² = 1`. | `11.1:514-522`, `11.2:148-152`, `11.1:746-747` | gap-audit B7 | the unannotated single-mass vertex |
| 2026-07-07 | **L4 — Non-collinear spin-DFT re-attributed.** The construction was labelled "Vignale–Rasolt"; that is *current*-DFT. Correct attribution is von Barth–Hedin / Kübler. Vignale–Rasolt is retained, correctly, for the orbital-current functional in the relativistic synthesis. | `11.2:188` (the corrected distinction), `11.2:527-530` | gap-audit B7 | the "Vignale–Rasolt" label on non-collinear spin-DFT |
| 2026-07-07 | **L5 — Maxwell source term's `4π` restored** (Gaussian units) in the SBE/Maxwell coupling `(∂_t² + c²∇×∇×)E = −4π∂_t²P`. | `11.2:330-332`, `11.2:531-532`; trap 11 `10.4-traps.md:109-110` | gap-audit B7 | the `4π`-less source term |
| 2026-07-07 | **L6 — Spin–orbit double-count removed.** The relativistic Hamiltonian carried an explicit SOC term *on top of* the Dirac operator; SOC emerges in the non-relativistic reduction and must not be added by hand. | `11.2:466-467` (the corrected parenthetical), `11.2:530-531` | gap-audit B7 | the Dirac-plus-explicit-SOC Hamiltonian |
| 2026-07-07 | **L7 — H-theorem direction corrected.** The Lyapunov table said `−S[f]` is "non-decreasing"; the H-theorem gives `dS/dt ≥ 0` ⇒ `−S` **non-increasing**. The error contradicted the same file's §1.4. | `11.3:546` (corrected row), `11.3:711-714` | gap-audit B7 | the "non-decreasing" direction |
| 2026-07-16 | **L8 — Reconciliation pass converted the three status banners into changelogs and replaced 11.3 §4.4's legacy global "regime N" numbers with regime names** (they matched neither the file's local numbering nor the `generic-dynamics` table). | `11.1:740-741`, `11.2:526`, `11.3:708-710` | reconciliation pass, `live/audits/` record | the global regime-number coordinates |
| (undated — pre-2026-07) | **L9 — The nineteen-category residual taxonomy descends from `11.3:665-679` (§4.5).** That section proposes exactly four clusters — EOM, conservation, positivity/inequality, algebraic identities — which is the structure `residual-definitions` §1 now implements as 9 + 3 + 5 + 2. | `11.3:665-684` vs `3.2:59-183`; `multiscale-state:417` cites §4.5 for the shared residual shape | group-C derivation → `residual-definitions` | the four-cluster informal grouping |
| (undated — pre-2026-07) | **L10 — The three-tier stratified state contract descends from `11.3:601-630` (§4.3).** `multiscale-state:406` states the lineage explicitly: "Resolving [deriv-transport] §4.3 ('three distinct state schemas + a *common* residual contract') into concrete types." | `multiscale-state:404-425` | group-C derivation → `multiscale-state` | the informal kinematic-geometry argument |
| 2026-07-30 | **L11 — `generic-dynamics:123` grounds the nine-regime extractions in `physics/research/group-{A,B,C}-*.md`, a path that does not exist.** `physics/research/` holds only the diamond dataset (7 files). The content is `11.1`/`11.2`/`11.3`. `formula-registry:70` makes the same claim for rows 1–87. Neither is caught by the checker (probe 1). | `3.1-generic-dynamics.md:123`; `6.9-formula-registry.md:70`; `find physics/research -type f` → 7 files, no `group-*` | this survey | — |
| 2026-07-30 | **L12 — Content-hash does not cover frontmatter.** Mutating `title:` into invalid YAML on a valid page left both the hash and every check green (probe 6). Frontmatter is unguarded. | scratch probe 6; and `11.1:3` fails `yaml.safe_load` today while `check_book_structure.py --check` reports OK | this survey | — |

---

---

### from `appendix-b`

| date | finding or decision | evidence | attribution | superseded |
|---|---|---|---|---|
| L1 · 2026-07-22 | The defect migration-barrier table's onset criterion was wrong while its values were right: the header read `τ = 1 yr`, which reproduces **none** of its seven onset temperatures. All seven invert to `ln(ν₀τ) ≈ 30.6` — a hop time of order a second. Under the stated `τ = 1 yr`, 773 K would sit well above `V_C⁰` onset and the "on the verge of mobility at 500 °C" reading would be false. | `11.4-deriv-defects.md:91-98` | 2026-07-22 adversarial re-audit | the `τ = 1 yr` header criterion |
| L2 · 2026-07-07 | Gap-audit B7 corrections to the defects derivation: the charge-balance exact form and the P-diamond activation example were ~100× and ~3× wrong; misfit convention normalized to `(a_film − a_sub)/a_sub`; diamond CTE anchor corrected to ≈3×10⁻⁶/K at 773 K; Freysoldt symbol renamed `E_corr^FNV` to stop colliding with the Fermi level. | `11.4-deriv-defects.md:672-677`; trap 44 at `10.4-traps.md:303-307` | 2026-07-07 gap audit | the pre-B7 values |
| L3 · 2026-07-22 | Two files stated two different closed forms for **one** registry row's `v_sat`. The bare `√(2ℏω_LO/m*)` evaluates to 4.0×10⁷ cm/s for GaN against the ~2×10⁷ quoted beside it — a factor-of-two disagreement between a formula and its own worked value. The Shockley form `√(8ℏω_LO/(3πm*))` gives ~2.6×10⁷ and is the surviving one. | `11.5-deriv-high-field.md:77` | 2026-07-22 adversarial re-audit | the bare `√(2ℏω_LO/m*)` form |
| L4 · 2026-07-22 | The diamond Caughey–Thomas worked example evaluated the `β = 2` form **one line after declaring `β = 1`** — the retag to `β = 1` reached the prose and not the arithmetic under it. `transport-coefficients.csv` names this page as the superseded `β ≈ 2` source. | `11.5-deriv-high-field.md:87-94`; `transport-coefficients.csv` `caughey-thomas-beta` | 2026-07-22 adversarial re-audit | `β ≈ 2` for diamond |
| L5 · 2026-07-22 | **Two errors partly cancelled into a plausible answer.** In the GaN HEMT hot-carrier example a 100× Joule-density fix was applied to the producing line and not the consuming one, and a *sheet* carrier density was used as if volumetric. Net effect: a believable 386 K rise against an actual ≈1930 K. The qualitative conclusion — carriers hot enough to ionize below the breakdown field — was strengthened, not weakened. | `11.5-deriv-high-field.md:320-332` | 2026-07-22 adversarial re-audit | the 386 K figure |
| L6 · undated (pre-`multiscale-state`) | The coupled electrical–thermal PDE residual was placed **inside** `eom-violation/` rather than as a sixth top-level category, on the reasoning that `eom-violation` is over the unified-state evolution while the coupled check is over the *emergent field tuple* `(φ, n, j, T)`. This is the reasoning that produced the two cross-tier EOM siblings in the nineteen-category taxonomy. | `11.5-deriv-high-field.md:265-281`; outcome at `3.2-residual-definitions.md:59-68` | the non-equilibrium derivation stream | the `residuals/coupled-pde/` top-level proposal |
| L7 · 2026-07-22 | **A project premise was refuted by its own numbers.** The claim that UWBG radiation tolerance is "uniformly 1–2 orders of magnitude" better than Si is contradicted three paragraphs above it by the same page's `E_d` table: against Si's ≈21 eV, diamond (37–50 eV) is ~2×, AlN (~35) and Ga₂O₃ (~25) are better, and **GaN (~20 eV) is at or below Si.** Both halves — the ratio and the word "uniformly" — were wrong. | `11.5-deriv-high-field.md:417` | 2026-07-22 adversarial re-audit | "not uniformly better… 1–2 orders of magnitude" |
| L8 · 2026-07-21 | A claim that a current-density-from-distribution formula was "already in registry" was false — no such row existed or had existed. The emergent-moment conclusion survived; only its justification was fabricated. | `11.5-deriv-high-field.md:556,603-605` | 2026-07-21 corpus reconciliation | the "already in registry" justification |
| L9 · 2026-07-16 | Wave-1 III-N anchors rewritten in place with the audited values: GaN `v_sat` 2.5×10⁷ → 1.4×10⁷ (the old number was `v_peak`); AlN `ω_LO` 100 → 110–114 meV; Fröhlich `α_F` GaN 0.49 → 0.40, AlN 0.65 → 0.58; AlN `μ_e` ~300 (doped) → 871⊥/619∥ intrinsic FP, 426 best experimental; diamond image-force lowering 0.18 → 0.16 eV at 10⁶ V/cm — **and a separate in-repo 0.06 eV was traced to a √10 field-scaling error.** Impact-ionization anchors reconciled to Cao-2018 / Bulutay / Ghosh–Singisetti in a second pass. | `11.5-deriv-high-field.md:607-627`; `9.1-accuracy-ledger.md:185-191` | 2026-06-10 Wave-1 audit, landed 2026-07-16 | the pre-Wave-1 III-N anchor set |
| L10 · 2026-07-16 | Two independent tables of diamond/metal contact stability had **drifted systematically because one restated the other rather than citing it.** Resolved by declaring `deriv-defects` §F.4 the owner. (Recording this because the mechanism — restatement instead of citation — is the general failure the restructure is built to prevent, and because **the declared deference was never actually applied**; see Contradiction C3.) | `11.6-deriv-csp.md:143-146` | 2026-07-16 strata rewrite | the independent `T_stable` column in `deriv-csp` C.3 |
| L11 · 2026-07-07 | Gap-audit B8 corrections to the CSP derivation: misfit denominators normalized to `/a_sub` (diamond-on-Si read **+52%** under the old `/a_film` denominator, now −34%); Pt/H-diamond `φ_B` and W/Mo carbide onsets harmonized to ranges; Σ3 twin-boundary `γ_TB` corrected from "≈0" to "tens of mJ/m², effectively 0". | `11.6-deriv-csp.md:359-364` | 2026-07-07 gap audit | the `/a_film` misfit denominator; `γ_TB ≈ 0` |
| L12 · 2026-07-30 (this sweep) | **Verified by planting, not by trusting green.** On a scratch copy at `2af93d2`: a numeric value in a chapter-11 table contradicting `reference-data/*.csv` (diamond κ(300) → 9999) produces **`data agreement clean`**; a prose citation to a section that does not exist (`[deriv-defects] Part ZZ`) produces **no finding**; a backticked reference to a nonexistent id produces **no finding**; the same reference in brackets **does** fire. The data checker is *alive* on chapter 11 — a planted retired formula name fired two finding classes from `11.5:630`. It simply does not compare page numerics to the CSVs. | this fragment, Notes §D | Phase-1 survey (appendix B) | the implicit assumption that a clean `check_data_agreement.py` run covers appendix values |

---

---

### from `appendix-c`

| date | finding or decision | evidence | attribution | superseded |
|---|---|---|---|---|
| L-1 · 2026-06 | **Implementation-language decision closed on the *shape*: a four-role polyglot of domain-specific DSLs joined at the Stage-4→Stage-5 codegen seam** (compiler host owning Stages 1–4 + substrate, emitting source for a separate runtime host; offline group-theory engine; offline proof assistant). Leading candidates recorded as Haskell / Julia / GAP / Lean 4. | `11.9#recommendation`; `10.2-open-decisions` Closed decisions; commit "ec52314-era" per `11.9:248` | `deriv-language-study`: 4 web-verified Round-1 axes (compute/hardware, substrate type-system fit, compiler/staging fit, in-house dependency + build cost), 1 adversarial audit, 1 Round-2 pass. **Per-axis dossiers were under `/tmp/impl-lang-research/` and no longer exist** — the study's internal provenance is unrecoverable. | The prior framing "which numerical ecosystem" (Julia vs Python+JAX vs custom-MLIR) |
| L-2 · 2026-07-21 | **The language *picks* were reopened; only the four-role shape stayed closed.** Every mention of a language in the corpus is a candidate to compare against, never a mandate; the requirement each candidate was chosen to satisfy is the durable part. | `11.9:244-247`; `10.2-open-decisions` item 6 (`10.2:83-91`) | Javier | the 2026-06 pick set as a commitment |
| L-3 · 2026-07-07 | **Gap audit: the Diamond–W worked example carried five compounding physics errors in one artifact** — Fröhlich/POP-limited `v_sat` fired on non-polar diamond in violation of its own `is-polar-material` classifier; the ~4.5 eV *n*-type barrier fed into leakage for a *p*-type contact (~3.5 eV wrong); image-force lowering 0.06 eV (a √10 field error; correct 0.16 eV at 10⁶ V/cm); κ_L(773 K) ≈ 800 against a battery value of 620; χ untagged by termination. | `11.8:596-601`; corrections in place at `11.8#6.2`; generalized as `[traps] §44` | 2026-07-07 gap audit | the pre-audit §6 example |
| L-4 · 2026-07-21 | **A live physics sign error was found in a file named as a source for downstream formula research**: `breakdown_field E_b` was recorded as dropping ~20% from 300 K to 800 K; **E_b RISES with T** (`κ_BR>0`, +5×10⁻⁴/K diamond, +7×10⁻⁴/K 4H-SiC). UWBG breakdown *hardens* with T. | `11.7:295-301`; `accuracy-ledger` #20; registry row 123 `breakdown-field-temperature-slope`; Hiraiwa–Kawarada JAP 114 034506 (2013) | 2026-07-21 corpus reconciliation | the conflation of breakdown with the mobility/velocity collapse |
| L-5 · 2026-07-21 | **κ(T) accuracy regime re-anchored**: the observable catalog read "±10% diamond, ±20% III-N" with a 2000 W/mK 300 K anchor while `accuracy-ledger` cited *that file* as its source — so the disagreement pointed the wrong way. Ledger per-temperature bands are canonical. | `11.7:289-294`; `accuracy-ledger` #13 | 2026-07-21 P1 repair | ±10%/±20% and "drops from 2000 to ~600 W/mK at 800 K" |
| L-6 · 2026-06 | **Cross-stream reconciliation of the residual catalog**: cost tiers fixed at T0–T3 (S2's T0–T4 rejected — MLIP-ness belongs in the `source` tag, not the cost tier); observable bundles fixed at 11 (B1–B11), with B11 `degradation` newly introduced for time-integrated lifetime residuals; `ReferenceCache` unified from two proposals into one type with two namespaces; ~13% dedup compression from ~100 raw proposals to 87 entries. | `11.8` §4.1, §4.3, §4.4; landed in `canonical-vocabularies §4`, `named-formulas:48` | S6 reconciliation pass | S2's T0–T4; the S2/S3 bundle proliferation; separate ref-phase-cache and defect-reference-battery |
| L-7 · **2026-07-30** | **The appendix generator catalog's `Diff` column disagrees with the current registry on 18 of its 87 carried-over rows, and the appendix's own §1.1 translation legend mispredicts 9 of them** — including all three fixed-point rows (`fermi-level-charge-neutral`, `SCPH-self-consistent-phonons`, `self-consistent-charge-balance`), which the legend maps to `D2` but which are canonically `D3`. **Separately, appendix rows `#88`/`#89` (the two architectural rejections) are registry rows 103–104; registry rows 88/89 are unrelated substantive formulas.** Rows `#1`–`#87` *do* align, which is what makes the two exceptions dangerous. | This survey; mechanical diff of all 89 rows against `physics/library/formulas/registry-manifest.csv` @ 2af93d2. Detail in R-1/R-2 above. | Phase-1 structure survey (appendix C) | the scope banner's blanket claim that all row numbers are snapshot-local |
| L-8 · **2026-07-30** | **A canon field's definition is reachable only through a dead id.** `residual-machinery` declares `layer : 0..6` and defines it solely by citing "the 7-layer compute DAG of `residual-generator-catalog §2`" — **no page has that id** (it is `deriv-generator-catalog`). The reference is unbracketed, so `REF_RE` never sees it; verified by planting `totally-nonexistent-page-xyz` in that exact position and getting a clean `--check`. | `7.2-residual-machinery.md:67-68`; probe described under Notes | Phase-1 structure survey (appendix C) | — |

---

---

### from `strata`

| date | finding or decision | evidence | attribution | superseded |
|---|---|---|---|---|
| 2026-07-16 | **The scorer↔stepper strong duality is refuted; a shared-kernel factorization survives.** No S-algebra `α_step` exists such that `fold(α_step)` steps every graph `fold(α_score)` scores — causality assignment is a global matching property, not a per-node denotation. The stepper is a Stage-4 sibling lowering consuming a system-layer analysis | `journal/live/specs/2026-07-16-evolver-duality-research.md §1, §2.2` | Independent deep-research commission (`[timeline] §2026-07-16`, Wave 2 / evolver) | The "one IR, two interpreters" strong reading |
| 2026-07-16 | **The integrator interface closed**: `dynamics` is a per-tier tangent kernel plus a steppable-form manifest; the integrator stays consumer-side. Three alternatives rejected with reasons (opaque RHS closure discards the structure tags; shipping an integrator imports step-size judgment; deferring buys no information) | `…evolver-duality-research.md §5`; `README.md:115-116` | Same commission, adopted | `open-decisions` item 5, previously open |
| 2026-07-16 | **The scorer is the evolver's auditor.** The 10 non-EOM residual categories become the integrator's preservation obligations under the *same* `ResidualKey`s; scoring an evolver-produced trajectory measures exactly the drift the integrator failed to prevent, with no new machinery | `…evolver-duality-research.md §2.8` | Same commission | — |
| 2026-07-16 | **Wave-2 β-Ga₂O₃ package cleared and seeded** by two independent adversarial auditors (conventions: CLEAR-WITH-PINS; numbers: BLOCK→fixed). Nine value/provenance corrections applied, incl. the E_c mis-attribution (the anisotropic triple 10.2/4.8/7.6 MV/cm replaces a scalar "≈8" that appears nowhere in the cited paper) and the c\* direction labels | `journal/live/audits/2026-07-16-wave2-beta-ga2o3-audit.md §2, §5` | Two web-verifying auditors on the Wave-1 refute mandate, adjudicated | The 2026-07-08 drafted values |
| 2026-07-16 | **The `is-polar-material` conflation was split into two predicates** — `is-polar-material` (Born charges/LO–TO; gates Fröhlich/POP) vs `is-noncentrosymmetric` (piezo classes; gates rows 113–119/128) — normative in `applicability-classifiers`. β-Ga₂O₃ is centrosymmetric yet strongly polar, which is what forced the split | `…-seeding.md §1.1`, `§5` item 3 | Wave-2 seeding spec | The single-predicate assumption |
| 2026-07-16 | **G1/G2 gate texts read.** The FB erratum corrects only CuPt-ordered In-alloy *piezoelectric* polarization — the P_sp bowing is untouched. "Ebert et al." resolved to Lan et al. PRB 113 155302 (CC-BY, InGaN-only), which **experimentally refutes the ZB-reference bowing curvature for InGaN and validates the LH frame**, upgrading the ledger row-35 cert-refusal from theoretical to experimental | `…-audit.md §4` | User-acquired erratum (Texas State) + open-access re-check | The audit's own §1 premise that curvature is reference-invariant |
| 2026-07-16 | **Two "paywalls" were bot-blocks, not paywalls** — both the Klimm tensor paper and the Lan/Ebert holography paper are open access; the auditors' 403s were bot-blocking. Closed G2 the same day and produced the derived κ[010] high-T anchors | `…-audit.md §4` G2, `§4` G1 item 2 | Post-audit re-check | A GAP entry and a gate that were both spurious |
| 2026-07-16 | **The diamond dataset is whole; family 4 was never lost.** Two truncated transfers had concluded a family existed and was lost; the archive order is 1,3,2,6,5,**4**, so every truncated copy stopped before reaching it. Direct single-file download (10,698,913,488 bytes) passes `gzip -t` end-to-end. The salvage inference was right; only the conclusion was wrong | `physics/research/diamond-stretch-and-skew-sweep/read-me-first.md#provenance-and-recovery-history` | Dataset recovery, 2026-07-16 | The 877-point byte-salvage and its "family 4 lost" conclusion |
| 2026-07-21 | **The differentiability tag `DX` was renamed `DN`** (it collided with the DX center) and eight rows retagged; `structure-uniqueness-CSP` (row 85) moved `DX → D4`, so it is no longer validation-only and its relaxation is now part of the spec; the `D4` set grew from three rows to six. Rows 74 and 41 renamed | `journal/live/specs/2026-07-21-…-brief.md#changelog`; `physics/library/formulas/retired-names.csv` | P1 registry repair | The `DX` vocabulary; `D4` as "surrogate" |
| 2026-07-21 | **The four γ̂ "open CS problems" were one problem seen four ways, and closed.** Identity stays exact and ε is estimated beside it; dispositions: ε-equality → the rewrite-admission rule, materialization → the Stage-4 adjoint-tape schedule, drift → exported to the integrating consumer, rank applicability → a Stage-4 compile-time predicate | `…-brief.md §G7 R7.3`; `gamma-hat §4` | Registry/substrate repair pass | `open-decisions` item 3; also corrects a claim that canon had called these "the only open CS problems" when `computational-overview §13` disclaimed exactly that |
| 2026-07-21 | **The rewrite-admission rule is settled, not open**: a rewrite is admitted iff it is exact over ℝ with its float side conditions discharged by an e-class interval / not-equals analysis and a fidelity generator registered. Equality saturation stays offline for a stated reason rather than as a hedge | `…-brief.md §G2 R2.4` | Same pass | The "genuinely open problem" framing, which the brief notes was **twice mis-called open** |
| 2026-07-22 | **A checker-surface hole cost real findings**: `check_data_agreement.py` exempted all of `journal/live/` on a comment reading "frozen work product", lifting the `live/audits/` rationale one directory up and contradicting `conventions`, which says specs are explicitly *not* frozen. Extending the surface to `journal/live/specs/` cost two findings across the three specs then present | `restructure/salvage/2026-07-22-oracle-interface-research-request.md §8`; `restructure/salvage/README.md` | Oracle-interface research request (recovered from an unmerged worktree) | The blanket `journal/live/` exemption |
| (undated, pre-2026-07-22) | **A parser bug made the data checker report clean while pages invoked unregistered formulas.** It scanned only backtick-quoted formula names, so `formula = <name>` arguments were invisible; widening the scan surfaced **17 findings immediately** | `…cs-framing-outline.md` Beat 31 | Self-reported | The narrower scan |
| 2026-07-22 | **A synthesis worth adopting: score-not-solve is a complexity-theoretic requirement.** Verification is only cheap given a complete witness; a partial witness forces the checker to solve for the rest, returning it to the hard class. This is why the oracle demands the full state including the electronic degrees of freedom — and therefore why the operator must carry them | `…cs-framing-outline.md` Beat 9, and `#found-vs-brought` | Talk preparation; flagged by its own author as **brought in, not found in canon**, and as a candidate canon addition | Nothing — canon asserted score-not-solve without deriving it |
| 2026-07-30 | **Citation enforcement provably stops at `journal/pages/`.** One dangling `[id]` planted in `journal/pages/`, `informed-operator/design/` and `journal/live/specs/` in a single run fired only in `journal/pages/`. Separately, one retired formula name planted in `informed-operator/design/` and `journal/live/specs/` fired only in the former | This fragment, "Verification performed"; `check_book_structure.py:50,135`; `check_data_agreement.py:69-82` | Phase 1 structure survey (this agent) | The assumption that a green checker covers the non-book strata |

---

