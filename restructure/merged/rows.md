# Merged — rows

Fragments: 11/11

### from `oracle-state`

| # | Fact or block | Current location | Disposition | Target | Evidence |
|---|---|---|---|---|---|
| 1 | `authority: canon` + `content-hash:` keys | all six pages, frontmatter (`2.1:4-5`, `2.2:4-5`, `2.3:4-5`, `2.4:4-5`, `2.5:4-5`, `2.6:4-5`) | delete | — | D1/§4: `authority` tier dies with ch. 11; `content-hash` is a restamped identifier git already answers |
| 2 | `referenced-by` lists | all six, frontmatter | delete | — | §4: stays generated, never hand-written; regenerated into `corpus.json` |
| 3 | "Three physically orthogonal inputs fully specify *what crystal, in what conditions*" | `2.1-crystal-inputs.md:18-19` | keep | `oracle/state/crystal-inputs#top-level-inputs` | sole statement of the input triple; cited by `compose-time-pipeline §1`, `product`, `build-order`, `build-sequence` |
| 4 | `PeriodicityStructure` — `d ∈ {0,1,2,3}`, lattice vectors `{a_i}`, periodicity flags, Bravais/space group, cell vectors `h` | `2.1:21-23` | keep | `oracle/state/crystal-inputs#periodicity-structure` | only definition in the corpus |
| 5 | `SiteDecoration` — species at Wyckoff positions, orbital basis, optional spin/charge/occupancy, tag `host\|defect\|adsorbate\|substrate\|impurity` | `2.1:24-28` | keep | `oracle/state/crystal-inputs#site-decoration` | only definition; `multiscale-state §3.2` depends on `SiteDecoration.occupancy` by name |
| 6 | "Defects, surfaces, adsorbates, magnetic configurations, charged systems and alloys are special cases of `SiteDecoration`, not new top-level types" | `2.1:26-28` | keep | `oracle/state/crystal-inputs#site-decoration` | load-bearing closure rule — it is what keeps the input alphabet at three |
| 7 | `Environment` — untyped prose list: temperature, pressure/volume, chemical potentials, applied E/B fields, applied stress, temperature gradient, carrier-injection | `2.1:29-31` | keep | `oracle/state/crystal-inputs#environment` | **homeless fact, confirmed.** No `canonical-for` anywhere names `Environment` (swept all 58 pages' `canonical-for`; only `crystal-inputs: top-level inputs` is even adjacent). See row 8 and Notes §A |
| 8 | The five harsh-environment `Environment` fields, **typed**: `radiation_flux`/`ParticleFlux`, `radiation_dose Φ_dose`/`Fluence`, `displacement_threshold E_d`/`Energy`, `vibration_spectrum`/`PSD`, `p_O2`/`Pressure` | `2.4-multiscale-state.md:427-442` (§12, "Required `Environment` field additions") | move | `oracle/state/crystal-inputs#environment` | a delta table ("*Required additions*") is scaffolding by shape — a diff against another page. Its five rows are the only *typed* `Environment` rows in the corpus; they merge into row 7's table |
| 9 | Same five fields restated untyped inside `crystal-inputs` prose with a cross-cite to `multiscale-state §12` | `2.1:31-34` | delete | — | duplication: collapses into rows 7+8 once the typed table lands on the owning page. The cross-cite disappears with it |
| 10 | "Presence of these fields fires the §4 applicability predicates (first-order decidable on field presence)" | `2.4:440-442` | move | `oracle/state/crystal-inputs#environment` | present-tense rule about `Environment`, not about the slow tier. Makes *absent ≠ zero* load-bearing — see Notes §A |
| 11 | `Reference` = bag of `(Crystal, Environment, weight)` baselines; composes from the three inputs; belongs to the cert layer | `2.1:36-38` | keep | `oracle/state/crystal-inputs#not-inputs` | only statement; `Crystal` is used here as a type name and defined nowhere — Notes §A |
| 12 | `Property` is an output request, a parameter of the oracle-file's `Validate` call | `2.1:38-39` | keep | `oracle/state/crystal-inputs#not-inputs` | agrees with `pino-bridge §1`'s `request` parameter |
| 13 | "(`predict` / `residual` were the pre-oracle-file verb names and no longer exist.)" | `2.1:40` | delete | — | scaffolding: retired names, "no longer exist". D1 verbatim. Not log-worthy — a rename, not an advancement |
| 14 | The 7-tuple `x(t) = (h, R_I, P_I, Π_h, Z_I, γ̂, A)` with per-slot mathematical types | `2.2-unified-state.md:29-40` | keep | `oracle/state/unified-state#slot-set` | the corpus's most-cited single block; 12 pages list `unified-state` in `depends-on` |
| 15 | `A` carried in Weyl gauge `A₀ ≡ 0`, transverse `∇·A = 0`; electrostatic sector in the matter functionals | `2.2:42-44` | keep | `oracle/state/unified-state#slot-set` | but the page **defers** the normative statement to `generic-dynamics` (`3.1-generic-dynamics.md:169-182`), where it actually lives. Retarget the pointer; do not duplicate the paragraph. `traps §25` names both pages as enforcers |
| 16 | Per-slot **array shapes and layouts, units, index order, dtype**, and `γ̂`'s shape as a function of its `CompressionPlan` | **absent** — promised at `2.2#slot-set` by `informed-operator/design/learnable-structure-requirements.md:27-31` (R1) | — (gap) | `oracle/state/unified-state#wire-schema` | **Dangling promise, confirmed by probe.** `2.2:29-40` gives mathematical types (`ℝ^{3N}`, `GL⁺(3,ℝ)`), never array layout; **zero units appear anywhere on the page**; `Z_I` is only "discrete"; `γ̂` has no array shape at all. Probe D3 (Notes §D) shows no checker can see this. Open question `I1` |
| 17 | The 7-tuple are the irreducible DOFs of the micro tier; quantities recoverable by coarse-graining **on the micro timescale and scale** are emergent and stay out — adding one would create a constraint manifold | `2.2:46-53` | keep | `oracle/state/unified-state#emergence` | already the *refined* axiom; the resolution has landed on this page |
| 18 | Quantities not recoverable on the micro timescale/scale are first-class state in their own tier: slow defect populations + composition vectors (hours–years), homogenized device-scale fields; they couple only parametrically, so introduce no constraint manifold | `2.2:55-62` | keep | `oracle/state/unified-state#emergence` | the micro-side half of the three-tier stratification; `multiscale-state` owns the tiers themselves |
| 19 | "(This is also the reconciliation of the **earlier** 'distributions are emergent' wording with `born-oppenheimer-levels`: L4 introduces its own irreducible state … the full distribution itself stays emergent by moment closure.)" | `2.2:64-67` | delete | — | scaffolding: "the earlier wording" is history of the page correcting itself. The substantive clause is already stated present-tense at `born-oppenheimer-levels:62-71`, which owns L4 — deleting loses nothing. Log-worthy row 2 |
| 20 | `x(t)` is a **type** the PINO instantiates per time step; `/physics` holds no values of `x(t)` — it defines what `x(t)` is and how to test a candidate | `2.2:69-71` | keep | `oracle/state/unified-state#type-not-value` | score-not-solve, stated at the state level. Load-bearing for the seam |
| 21 | The seven slot labels are the closed C1 vocabulary `StateComponent`, a `Universe[StateComponent]`; downstream addresses slots by dense ordinal handle, not raw symbol | `2.2:73-76` | keep | `oracle/state/unified-state#slot-set` | R1 depends on this exact rule (`learnable-structure-requirements.md:30-31`) |
| 22 | Title + framing: "γ̂ … the most demanding object in the state vector: one logical entity, multiple inequivalent encodings, different operations cheap on different encodings" | `2.3-gamma-hat.md:26-34` | keep | `oracle/state/gamma-hat` (preamble) | the page's reason to exist |
| 23 | "This file documents … **how the four questions once listed as open data-structure problems resolved** (§4)" | `2.3:38-40` | delete | — | scaffolding: the page advertising its own history. Rows 32-36 keep the resolutions |
| 24 | `Basis ∈ {Real, Reciprocal, Wannier, NaturalOrbital, SymmetryAdapted}` × `Form ∈ {Dense, Sparse, BlockDiag, LowRank}` | `2.3:41-48` (§1) | keep | `oracle/state/gamma-hat#encoding-vocabulary` | sole statement; `canonical-for: γ̂ encoding vocabulary` |
| 25 | The five first-class V1 `(Basis, Form)` pairs and what each is for | `2.3:50-58` | keep | `oracle/state/gamma-hat#encoding-vocabulary` | sole statement |
| 26 | Stage 4 selects one slot per density-matrix node from `(PeriodicityStructure, SiteDecoration)`; transcoders convert on demand | `2.3:60-63` | keep | `oracle/state/gamma-hat#encoding-vocabulary` | this is the `CompressionPlan` topic the page owns |
| 27 | **`/physics` is scorer-only: `γ̂` never evolves inside the oracle.** States arrive complete, are scored, discarded; no trajectory, no `γ̂` with a history | `2.3:67-70` (§2) | keep | `oracle/state/gamma-hat#scorer-only` | cited as the decision by `compose-time-pipeline:183`, `computational-overview:222,226`, `open-decisions:228`. Highest-traffic anchor on the page |
| 28 | "This is not new — `[library-landscape]` and the README already say … **This section used to list time-stepping as a `γ̂` write path, which contradicted them; canon wins** and the write path is construction and self-consistency only." | `2.3:70-73` | delete | — | scaffolding: "used to list … canon wins" is the story of a contradiction closing. The surviving fact ("the write path is construction and self-consistency only") is already in row 30's diagram. Log-worthy row 3 |
| 29 | An EOM-violation residual over `γ̂` scores a supplied `∂_t γ̂` against `L·δE/δx + M·δS/δx` — scoring a proposed rate is not taking a step | `2.3:75-78` | keep | `oracle/state/gamma-hat#scorer-only` | the non-obvious corollary; without it §2 reads as forbidding the `EOM/γ̂` residual |
| 30 | Read path (dominant: apply `Ĥ`, extract density, trace, eigendecomposition) vs write path (construction + self-consistent step) and the two pipelines | `2.3:80-94` | keep | `oracle/state/gamma-hat#read-write-paths` | but see Contradictions/Notes §C — `computational-overview:222-227` restates it with cost detail this page lacks; one page must own it |
| 31 | "**Where trajectory ownership went.**" `/physics` emits a per-tier tangent map + steppable-form manifest (a pure function, not an integrator); the consumer that integrates owns `γ̂` representation health; drift is **exported, not dissolved** | `2.3:96-101` | keep | `oracle/seams/pino-bridge#steppable-form-manifest` (the export) + a one-line consequence at `oracle/state/gamma-hat#scorer-only` | the fact is live and load-bearing; the heading "Where … went" is scaffolding — rename. The manifest is a seam export, not a state fact |
| 32 | Self-consistency is *structured* by the coalgebraic fixed-point form but *solved* by the Stage-4 implicit-diff adjoint; convergence iteration happens above the substrate | `2.3:103-106` | keep | `oracle/state/gamma-hat#read-write-paths` | γ̂-specific; the general Stage-4 rule is `compose-time-pipeline §4` |
| 33 | §3 table — five strategies (codata interface, typed term algebra, e-graph, pullback bundle, tensor network) realized as Stage-4 internals, not architectural peers | `2.3:108-121` | keep | `oracle/state/gamma-hat#stage4-internals` | sole statement; "pullback bundle = multi-slot V2" is the only place the V1/V2 slot cardinality is fixed |
| 34 | §4 heading "The four questions, and where each went" + "This section listed four 'genuinely open CS problems'. They were one problem … seen from four sides … **None is still open here.**" | `2.3:123-131` | delete | — | pure scaffolding — the frame around the resolutions, per exception 1 of the brief. Rows 35-38 carry the resolutions forward. Log-worthy row 4. **Retarget hazard:** 8 external citations point at `gamma-hat §4` — Notes §B |
| 35 | Node identity is **exact**, never bisimulation-up-to-ε: `≈_ε` is not transitive, so it yields no quotient, no canonical representative, nothing to hash. A rewrite is admitted when exact over ℝ *and* its float side conditions are discharged by an e-class analysis carrying interval and not-equals facts. E-graphs stay offline | `2.3:133-141` | keep | `oracle/state/gamma-hat#identity-is-exact` | present-tense guard (brief exception 1). The *general* rule is canonical at `representation-substrate §4.1/§20.4.2` and the rewrite-admission rule at `compose-time-pipeline §3` — cite, do not restate |
| 36 | Materialization policy is a **cost** question with no error term: the rematerialization-vs-storage trade-off; `revolve` (Griewank & Walther, *ACM TOMS* 26(1) 19–45, 2000) optimal for a chain, **NP-complete** on a general DAG (Naumann, *J. Discrete Algorithms* 7(4) 402–410, 2009) | `2.3:143-150` | move | `oracle/compilation/compose-time-pipeline#stage4-adjoint-tape` | the page says so itself: "It belongs to Stage-4 codegen as a schedule over the adjoint tape … **not to this page**". The two literature results are the substance and must not be lost in the move |
| 37 | Long-trajectory drift is not `/physics`'s problem (nothing accumulates in a scorer); the robust DLRA integrators — projector-splitting (Lubich & Oseledets, *BIT* 54, 2014), BUG (Ceruti & Lubich, *BIT* 62(1) 23–44, 2022) — have error bounds **independent of small singular values** (Kieri/Lubich/Walach, *SIAM J. Numer. Anal.* 54, 2016); rank-adaptive BUG (Ceruti/Kusch/Lubich, *BIT* 62, 2022) preserves norm, energy, and monotone gradient decrease — mapping term-for-term onto trace, `L`, and `M` of `generic-dynamics` | `2.3:152-168` | move | `oracle/seams/pino-bridge#steppable-form-manifest` | this is the hand-off content for the consumer that integrates — it belongs with the manifest that declares the hand-off, not on the state page. The `(norm, energy, gradient-decrease) ↔ (trace, L, M)` mapping is the load-bearing claim |
| 38 | Rank-dependent applicability of `(NaturalOrbital, LowRank)` is a **compile-time predicate** decided at Stage 4 from `(PeriodicityStructure, SiteDecoration)`, not a runtime check; four-index objects (BSE, BTE collision matrices) go to TT compression | `2.3:170-177` | keep | `oracle/state/gamma-hat#encoding-vocabulary` | it is a slot-selection rule — same subject as §1, and belongs beside the other slot choices |
| 39 | §5 Diamond MVP: `(Reciprocal, BlockDiag)`, k-blocks as orbitals, ~18 MB, densifying ~460 MB "which is exactly why the encoding forbids it" | `2.3:179-184` | delete | — | verbatim duplication of `gamma-budget:18-25`, which carries the derivation this does not. Replace with a citation to `oracle/state/gamma-budget`. **Unsure-flag:** if Phase 2 folds `gamma-budget` into `gamma-hat` (Notes §E) this becomes the merge site instead |
| 40 | §1 "The emergence-axiom **correction**": "`unified-state` lists defect populations and distributions among quantities that are *emergent — coarse-grainings of `x(t)`* … That classification is **too strong**: it **contradicts** `[born-oppenheimer-levels]` (L4) … and it forbids exactly the physics the project exists to predict" | `2.4-multiscale-state.md:57-64` | delete | — | scaffolding **and factually stale**: the quoted string "emergent — coarse-grainings of `x(t)`" appears nowhere in `unified-state` today (grep: its only occurrence corpus-wide is this quotation of it). Both cited pages already carry the resolution — rows 17-18, 47. See Contradictions 1 |
| 41 | **Refined emergence axiom.** `y` is emergent from a tier **iff** recoverable from that tier's state by coarse-graining on the **same timescale and same scale**. `n_{q,s}`, `f_n(k,r)`, electron/lattice temperatures are emergent at the micro timescale | `2.4:66-70` | keep | `oracle/state/multiscale-state#emergence-axiom` | `canonical-for` topic; the definition the whole stratification rests on. Must survive row 40's deletion intact |
| 42 | Two non-recoverable classes: **slow/history-dependent** (different timescale — defect concentrations, charge-state distributions, H content, oxidation/carbide fronts; frozen at micro timescale, Arrhenius barriers 2–7 eV, carry integrated thermal/irradiation history) and **homogenized/device-scale** (different scale — `T_L, φ, n, p, j` on a device mesh, not derivable from one unit cell) | `2.4:71-81` | keep | `oracle/state/multiscale-state#emergence-axiom` | sole statement of the two-axis split |
| 43 | Because the tiers are independent by timescale **or** by scale they create **no algebraic constraint** with the micro 7-tuple; they couple only parametrically. The full distribution is never promoted | `2.4:83-87` | keep | `oracle/state/multiscale-state#emergence-axiom` | this is the argument that the added tiers do not reintroduce the pathology `unified-state` guards against |
| 44 | "The micro axiom holds verbatim; this document adds the two tiers and **reconciles** `unified-state` ⊥ `born-oppenheimer-levels`." | `2.4:87-88` | delete | — | scaffolding: a document describing its own editorial act. Nothing in it is a fact about the physics or the types |
| 45 | §2 three-tier table — Micro / Slow / Macro × (members, equilibration timescale & scale, index geometry, dynamics) | `2.4:90-97` | keep | `oracle/state/multiscale-state#three-tiers` | `canonical-for: three-tier state stratification`; the page's spine |
| 46 | Slow and macro are **adiabatic parameters** of micro: micro fast-equilibrates at fixed slow/macro under `Environment`; slow/macro drift under time-averaged `⟨micro⟩_τ` or homogenized coefficients | `2.4:98-101` | keep | `oracle/state/multiscale-state#three-tiers` | the coupling contract in one sentence |
| 47 | The slow tier is a configurational layer above L4; the macro tier is **L4's spatial fluid-limit reduction** lifted from one cell to a device mesh — the "irreducible state" `born-oppenheimer-levels` attributes to L4 | `2.4:101-105` | keep | `oracle/state/multiscale-state#three-tiers` | present-tense identification, not a reconciliation narrative. `born-oppenheimer-levels:62-71` states the same thing from the other side — acceptable as a two-way structural fact, but see Notes §C |
| 48 | `/physics` scores each tier's law-violation, the PINO supplies each tier's trajectory (score-not-solve preserved at every scale); **no new computational method** — slow reuses `kinetic-evolution`, macro reuses the device-PDE residual pattern (row 71) | `2.4:105-108` | keep | `oracle/state/multiscale-state#three-tiers` | the closed-alphabet invariant, asserted at the tier level |
| 49 | §3.1 `DefectSpecies` closed C1 `Universe[T]` (`carrier_kind = Closed`, `ordinal_policy = DenseU32`) + the five-host member table with charge states | `2.4:112-127` | keep | `oracle/state/multiscale-state#defect-species` | sole statement; cited as `multiscale-state §3.1` from 2 sites |
| 50 | `DefectSpecies` element record `{name, site : LatticeSite, charge_states, spin}`; adding a member is a versioned `schema_version` bump | `2.4:128-131` | keep | `oracle/state/multiscale-state#defect-species` | the extension rule; parallels `canonical-vocabularies §7` |
| 51 | §3.2 slow-state fiber as cluster **C3** `PersistentMap[TypedKey, V]` (HAMT-32, stage-visible), **not** part of `ResidualKey` identity + the six-field schema table (`conc[D,q]`, `charge_dist[D]`, `H_content`, `oxide_front`, `carbide_thickness`, `dislocation_density`) with types, units, indices | `2.4:133-147` | keep | `oracle/state/multiscale-state#slow-state-schema` | `canonical-for: slow / configurational state schema`. **This is the only fully typed-and-united state schema in my scope** — the shape `unified-state` is missing (row 16) |
| 52 | The slow fiber is a new C3 fiber that is the **dynamic promotion of `SiteDecoration.occupancy`**, not a mutation of `Z_I`; `Z_I` stays immutable; static `occupancy` becomes the initial condition `s(t=0)`; tier hygiene ties no constraint manifold. Drives B11 with defect-resolved sub-outputs in B4 | `2.4:150-158` | keep | `oracle/state/multiscale-state#slow-state-schema` | a *decision*, stated present-tense. Ties directly to row 5's `SiteDecoration` |
| 53 | §4 nine slow-kinetic formulas F-G1…F-H2 with governing equations, barrier values, T/D tags, instantiation forms and bundle assignments | `2.4:161-211` | keep | `oracle/state/multiscale-state#slow-kinetics` | cited as `multiscale-state §4`. Values must be re-seeded from `registry-manifest.csv` rows 105–112, never from the `deriv-*` sources cited inline (brief trap) |
| 54 | The `[deriv-defects]` / `[deriv-high-field]` / `[deriv-observable-catalog]` Part-and-section citations threaded through §4 (≈30 of them) | `2.4:161-211` passim | delete | — | ch. 11 ceases to exist (D3); these citations cannot survive it. The *content* they point at is already inlined here — this page is where those derivations land |
| 55 | "Eight of the nine processes are **new** `FormulaRecord`s (rows 105–112); **F-F5 is the existing row 81, re-tagged** so its output `x_carbide` is a slow-state field" | `2.4:161-162`, `2.4:187-188` | delete framing / keep fact | `oracle/state/multiscale-state#slow-kinetics` | "new"/"re-tagged"/"not re-added" is authoring history. The surviving fact: *`x_carbide` (row 81) is a slow-state field.* Registry row numbers stay |
| 56 | "an earlier '~1 mm' was 170× high" (F-G2 H-diffusion range, correct value √(Dt) ≈ 6 µm in 1000 h) | `2.4:179` | delete framing / keep value | `oracle/state/multiscale-state#slow-kinetics` | the parenthetical is the story of a correction; **6 µm** is the fact. Log-worthy row 5 |
| 57 | Two research-flags inside §4: #46 "OUTSIDE registry unless reaction-rate template present" — satisfied by `kinetic-evolution`; and F-H2's `η_recomb(T_L)`/`σ_d` have **no closed form in the corpus** | `2.4:192-194`, `2.4:208-211` | keep | `oracle/state/multiscale-state#slow-kinetics` + `open-questions:` frontmatter | the second is a live gap, not a remnant → open question `msq-niel-coefficients`. The first is a *resolved* flag → keep one clause, drop "Research-flagged" |
| 58 | §5 `EOM/DefectPopulation` residual: the formula, its derivation as the slow-tier specialization of `‖dx_i/dt − (LδE/δx + MδS/δx)‖²`, axes `(DefectSpecies, ChargeState, SiteClass)`, one `ResidualLeaf` per `(species, charge, site)`, no preaggregation, `ResidualKey`/facets, curriculum band **Refine** `[0.10, 0.60)` | `2.4:215-235` | keep | `oracle/state/multiscale-state#eom-defect-population` | `canonical-for`; most-cited section of the page (4 external `§5` citations) |
| 59 | "This is `[deriv-csp]` Part E.1 `R_ThermalCycleStability`'s population-drift residual **promoted to first-class**." | `2.4:234-235` | delete | — | provenance of a promotion, and it cites a dying container. The residual's definition (row 58) is complete without it |
| 60 | §6 adiabatic driving contract: the five-row `⟨micro⟩_τ` table, the `d s/dt = Φ_kinetic(…)` signature, and the reverse (slow→micro) parametric dependence via `SelfConsistentChargeBalanceOf` + SRH `τ_n = 1/(σ_n v_th N_T)` | `2.4:237-256` | keep | `oracle/state/multiscale-state#adiabatic-driving` | `canonical-for: adiabatic micro→slow driving contract`; the only statement of the bidirectional contract |
| 61 | §7.1 `DeviceMesh : Universe[MeshCell]` C7 closed universe (`DenseU32`, `Roaring\|Bitset`), `MeshCell` carrying `(centroid, volume, face-list)`, fields as C3 fibers, `MerkleDAG diff = O(changed frontier)` | `2.4:262-269` | keep | `oracle/state/multiscale-state#device-mesh` | `canonical-for: device-mesh Universe` |
| 62 | Finite-volume discretization in integral conservation form; the mesh is **conservative** so the `Conservation` residual holds discretely | `2.4:271-274` | keep | `oracle/state/multiscale-state#device-mesh` | the guarantee that makes cat. 9 checkable on the macro tier |
| 63 | "**Relation to the `[open-decisions]` PDE-mesh item (open item 2).** The macro tier **subsumes and narrows** the deferred item: the mesh *format* is now committed … what remains open is only the *mesh-adjoint scheme*" | `2.4:276-280` | delete framing / move gap | `open-questions:` on `oracle/state/multiscale-state` | D7: the register is emitted from the page. The committed part is rows 61-62; the open part is open question `mesh-adjoint-scheme` (duplicated at §15.1 — row 76) |
| 64 | §7.2 `MacroState = (T_L, φ, n, p, j)` with types **and units** (`[K]`, `[V]`, `[m⁻³]`, `[A·m⁻²]`) on `DeviceMesh` | `2.4:282-287` | keep | `oracle/state/multiscale-state#macro-state-schema` | `canonical-for: macro continuum-field state schema`; cited as `§7.2` from `unified-state:60`. The second united schema in scope (cf. row 16) |
| 65 | Per-field justifications: `T_L` spatial coarse-graining of micro `S_vib`; `φ` Poisson-constrained so the constraint is *scored* not free; `n,p` are **0th moments** of `f_n` (densities, not the distribution); `j` via closed-form 1st-moment closure so current-continuity is a scorable balance | `2.4:289-296` | keep | `oracle/state/multiscale-state#macro-state-schema` | each is a promotion decision with a stated reason; deleting the reasons re-opens the question |
| 66 | **Kept emergent (never promoted):** `f_n(k,r)` (promotion double-counts its moments → DAE), `T_e(r)`, `E(r) = −∇φ`, all transport coefficients. "`(T_L,φ,n,p,j)` are a new **scale**, not a new **distribution**" | `2.4:298-301` | keep | `oracle/state/multiscale-state#macro-state-schema` | the exclusion list is as load-bearing as the inclusion list — it is what keeps the DAE out |
| 67 | §8.1 two-temperature energy balance (steady + transient), `τ_E` per-composition, `T_e` **never state**, the four field-regime windows (Ohmic ≲10⁴ V/cm → saturated ≳ few×10⁵), positivity bound `T_e ≥ T_L` scored as a `Positivity` residual | `2.4:305-314` | keep | `oracle/state/multiscale-state#moment-closures` | `canonical-for: moment-closure (emergent T_e, j)`; the regime windows are cited by `applicability-classifiers:115` |
| 68 | "`tau-energy-POP-acoustic` (row 73) carries both channels in one row … **they were proposed as separate `tau-energy-pop` / `tau-energy-acoustic` formulas**" | `2.4:309-311` | delete | — | scaffolding carrying **two retired names**. `retired-names.csv:56-57` retires both to `tau-energy-POP-acoustic` on 2026-07-21; this sentence is their only live use in my scope. Verification §3 requires zero |
| 69 | §8.2 drift-diffusion momentum closure: `j_n`/`j_p` with "**only the diffusion term changes sign** between carriers, never the drift term", Einstein `D = μk_BT/q`, Caughey–Thomas `μ(E)`, saturated collapse `j ≈ qnv_sat`, coefficients micro-supplied, faithful tier verifies vs BTE-`j(E)` | `2.4:317-322` | keep | `oracle/state/multiscale-state#moment-closures` | the sign rule is a trap-grade statement; keep it emphatic |
| 70 | **Degenerate-statistics caveat (declared model-form error):** p⁺ B-doped diamond at 10²⁰–10²¹ cm⁻³ needs `D/μ = (k_BT/q)·F_{1/2}(η)/F_{−1/2}(η)`; V1 carries the nondegenerate form with the discrepancy entered as a declared model-form-error term in `combineTol` on any composition crossing `n_degenerate(host)`; same gate carries the plasmon–phonon/LST exclusion | `2.4:324-332` | keep | `oracle/state/multiscale-state#moment-closures` | `traps §42` enforces it against this page. A live declared-error budget, not a remnant |
| 71 | §9 the three macro balance PDEs (P)/(DD)/(H) + the eight-row homogenization map HM-1…HM-8 (micro output → relation → macro coefficient → equation) | `2.4:334-355` | keep | `oracle/state/multiscale-state#homogenization-map` | `canonical-for: the micro→macro homogenization map`; the actual micro/macro bridge |
| 72 | **Supply contract** — per-composition, error-tagged (cheap closed-form + faithful BTE tied by `Algebraic/MethodEquivalence`, the `dressing` facet), cached (content-addressed, `O(log₃₂ n)`, never a re-solve), compile/runtime split (Stage 1–4 fix the form, Stage 5 evaluates) | `2.4:357-362` | keep | `oracle/state/multiscale-state#homogenization-map` | the "no solver-call hot paths" invariant applied to coefficients |
| 73 | §10 `EOM/Continuum` residual: the general form, the five-row per-field `RHS_field` table, axes `(MeshCell, MacroField)`, `RoaringCoverageMask`, "macro instance of the EOM-violation family, not a new top-level category", score-not-solve restated for the mesh | `2.4:364-378`, `2.4:394-400` | keep | `oracle/state/multiscale-state#eom-continuum` | `canonical-for`; 3 external `§10` citations |
| 74 | **Scharfetter–Gummel is required, not optional:** the exponentially-fitted face flux with `B(t)=t/(e^t−1)`; at UWBG operating point cell Péclet `Pe ≈ 40`, where central differencing makes **the residual operator itself wrong**; removable singularity guarded by `B(t)≈1−t/2`; only the convection-dominated carrier flux needs it | `2.4:380-392` | keep | `oracle/state/multiscale-state#eom-continuum` | `traps §31` enforces it against this page. Highest-consequence single paragraph in my scope |
| 75 | §11 unified three-tier residual contract: the tier × (`x`, index, EOM category, `L`, `M`) table; one residual shape instantiated three ways; macro `L` quasi-static; one `ResidualKey` space across tiers; `CategoryTag` **closed set grows 17 → 19**; `/physics` never pre-sums across tiers | `2.4:404-425` | keep | `oracle/state/multiscale-state#three-tier-residual-contract` | `canonical-for`. The "17 → 19" phrasing is a delta — restate as "the closed set has 19 members, including `EOM/DefectPopulation` and `EOM/Continuum`" |
| 76 | "Resolving `[deriv-transport]` §4.3 ('three distinct state schemas + a *common* residual contract') **into concrete types**" | `2.4:404-405` | delete | — | provenance sentence citing a dying container. The concrete types (row 75) stand alone |
| 77 | §12 heading "**Required** `Environment` field additions" + the driving-tier framing sentence | `2.4:427-430` | delete | — | the delta framing; the five typed rows move to `crystal-inputs` (row 8) |
| 78 | "`p_O2` is a specialization of the existing partial-pressure slot; `μ_env` chemical potentials already present" | `2.4:440-441` | move | `oracle/state/crystal-inputs#environment` | a fact about `Environment`'s own structure — that `p_O2` is not an independent field but a specialization. Must not be lost in the merge |
| 79 | §13 three `Static/Thermodynamic` residuals R-T1 (Gibbs adsorption `‖dγ/dμ + Γ‖²`), R-T2 (charge–Fermi Maxwell `‖dE_form/dE_F − q‖²`), R-T3 (Clausius–Clapeyron analog), with T/D tags; curriculum **Polish** `[0.60, 0.90)` | `2.4:444-455` | move | `oracle/laws/residual-definitions#static-thermodynamic` | these are residual definitions, not state. `residual-definitions §1` item 17 owns the category; a state page defining residuals is the duplication the restructure exists to remove. **Unsure-flag:** they tie `charge_dist[D]`/`[H]`/`x_ox` (slow state) to registry rows 30/44 — if Phase 2 keeps residual definitions beside the state they constrain, this stays. Flagging rather than guessing |
| 80 | §14 "**New** registry rows 105–112" — the eight ids, plus "Rows 103–104 are the two architectural markers; F-F5 = `carbide-growth-parabolic` is the existing row 81, **not re-added**" | `2.4:458-466` | delete | — | duplication + scaffolding. `registry-manifest.csv` is the sole source (the page says so); "new"/"not re-added" is authoring history. Row 53 already binds each formula to its row number |
| 81 | "(the canonical, sole source for row content — **an embedded copy here drifted and was removed by the 2026-07 reconciliation**)" | `2.4:459-461` | delete | — | scaffolding: the story of a defect being fixed. Log-worthy row 6 — the *lesson* (an embedded copy of a CSV drifts) belongs in the log |
| 82 | §15 five open sub-decisions: (1) mesh-adjoint scheme, (2) mesh generation/refinement — structured-tensor for V1, adaptive deferred to V2, (3) hole-transport coefficient anchors, (4) bidirectional slow↔macro coupling, (5) `η_recomb(T_L)`, `σ_d`, regime thresholds — curated `ProvenanceLedger` coefficients, flagged as data-acquisition not invention | `2.4:468-487` | move | `open-questions:` frontmatter on `oracle/state/multiscale-state` (items 1, 3, 5) + body (items 2, 4) | D7. **Items 2 and 4 are not open** — 2 is a committed V1 decision with a V2 deferral, 4 is a stated contract; they are body facts miscategorized as gaps. Items 1/3/5 are genuine open questions |
| 83 | "The corpus was genuinely insufficient on five bounded points; each is a tracked sub-decision, **not a silent gap**" | `2.4:468-470` | delete | — | self-defense about the page's own honesty. The emitted register makes the claim structurally |
| 84 | §16 "Landing edits to existing docs" — the entire section, listing edits already applied to `unified-state`, `generic-dynamics`, `born-oppenheimer-levels`, `residual-definitions`, `crystal-inputs`, `canonical-vocabularies`, `open-decisions`, the registry | `2.4:489-496` | delete | — | pure scaffolding: a completed to-do list of edits to other pages, including "(contradiction removed)". Every item is either already applied (verified: rows 17-18, 47) or superseded by this disposition table |
| 85 | The 7-tuple partitions into four levels with dependencies flowing strictly upward (L4→3→2→1); the hierarchy partitions the **state-component space**, complementary to `PhysicsGraph` which partitions the *computation* | `2.5-born-oppenheimer-levels.md:34-38` | keep | `oracle/state/born-oppenheimer-levels#hierarchy` | `canonical-for: 4-level BO hierarchy`; the complementarity claim prevents a standing confusion |
| 86 | L1–L4 definitions: operands, regimes, and mathematical apparatus per level | `2.5:40-55` | keep | `oracle/state/born-oppenheimer-levels#hierarchy` | sole statement; `capability-slices` and `cross-cutting-rules` depend on it |
| 87 | Each level uses lower levels as inputs but introduces its own irreducible state; a **regime** is a navigational *view* across contributing levels | `2.5:57-60` | keep | `oracle/state/born-oppenheimer-levels#hierarchy` | defines "regime", used corpus-wide |
| 88 | **L4's own irreducible state is the macro continuum tier** — `T_L, φ, n, p, j` on a device mesh, full distribution kept emergent by moment closure; slow defect populations form a first-class slow tier; micro 7-tuple is the L1/L2 tier | `2.5:62-69` | keep | `oracle/state/born-oppenheimer-levels#l4-irreducible-state` | present-tense and correct; the resolution **has** landed here |
| 89 | "…**this resolves the apparent tension** with `unified-state`'s emergence wording (see `unified-state` and `multiscale-state §1`)" | `2.5:69-71` | delete | — | scaffolding: names a tension that no longer exists on either page, and its `multiscale-state §1` pointer lands on the section row 40 deletes. Row 88 survives without it |
| 90 | BO level is **derivable** from a node's transitive inputs — not a stored field on `Node`; Stage 1 ordering follows the level discipline | `2.5:73-77` | keep | `oracle/state/born-oppenheimer-levels#hierarchy` | the anti-denormalization rule; `physics-graph §5` agrees |
| 91 | §1 dressing tiers are **V1-vs-V2 implementation scope, not a runtime hierarchy**; dressing is a Stage-4 codegen choice; the `dressing` tag is a provenance label, **not a loss-weighting axis** | `2.5:79-86` | keep | `oracle/state/born-oppenheimer-levels#dressing-tiers` | `canonical-for: dressing tiers`; the "not a loss-weighting axis" clause is load-bearing for `/informed-operator` |
| 92 | The Layer 1 / 1.25 / 1.75 / 2 / 3 table with per-layer members and cert types (`OneShotCert`, `IterativeResult`) | `2.5:87-119` | keep | `oracle/state/born-oppenheimer-levels#dressing-tiers` | sole statement. **`Layer-1.75` is one of the four nomenclature defects (plan §4)** — propose a rename to Javier, do not rename unilaterally |
| 93 | **Frozen at reference (normative):** a Layer-1.25 dressing is computed once per composition at the reference state and is thereafter constant; contributes no gradient; gap-vs-strain enters only through row 63's deformation potential; cost is a **dressing-staleness** term in the error model | `2.5:96-104` | keep | `oracle/state/born-oppenheimer-levels#dressing-tiers` | normative and present-tense; the deliberate trade is stated with its price |
| 94 | "What is **NOT yet declared** is the **validity radius** … Until it is, the staleness term has no bound, and a composition cannot be refused for leaving the radius because no radius is stated. Tracked in `[open-decisions]`." | `2.5:104-109` | delete | — | **stale gap: `open-decisions:113-121` closed it on 2026-07-21** — the estimator `‖Δx‖·‖∂(dressing)/∂x‖_ref` *is* the radius, measured once at the reference state, entering `combineTol`. See Contradictions 2. Replace with the resolution (row 95), not with nothing |
| 95 | The resolution to fold in: dressing-staleness bound = `‖Δx‖ · ‖∂(dressing)/∂x‖_ref`, sensitivity coefficient measured once at compile time, `OneShotCert` gains the coefficient field, product enters `combineTol` | `journal/pages/10-process-and-governance/10.2-open-decisions.md:113-121` (outside my scope) | move | `oracle/state/born-oppenheimer-levels#dressing-tiers` | brief exception 1: a closed item's resolution is a present-tense fact and belongs on the page that owns the topic. Flagged for the `open-decisions` surveyor as a cross-scope handoff |
| 96 | Still-open residue of that item: **G₀W₀ cost is not scope-tagged by cell size** — T2 (≤10 s) plausible at MVP scale, hours for V1 defect supercells, so the tier table is violated silently rather than refused | `10.2-open-decisions.md:122-125` (outside my scope) | move | `open-questions:` on `oracle/state/born-oppenheimer-levels` | genuinely open, and it is about Layer-1.25's cost tier — this page owns the tier. Open question `g0w0-cost-scope-tag` |
| 97 | Diamond MVP runs entirely against Layer 1.25, preserving the closed-form discipline; needs one dressing wired | `2.5:121-123` | keep | `oracle/state/born-oppenheimer-levels#dressing-tiers` | MVP scope commitment |
| 98 | **G₀W₀:** PBE underestimates the diamond indirect gap by ~23% (~4.2 eV vs measured 5.47 eV); G₀W₀ corrects to ~5.5 eV | `2.5:124-126` | keep | `oracle/state/born-oppenheimer-levels#dressing-tiers` | **value-checked:** agrees with `physics/library/cert/reference-data/material-constants.csv:28` (`bandgap-indirect, diamond, 300 K, 5.47 eV ± 0.01`, whose own note reads "PBE −23% ⇒ G₀W₀/hybrid path (registry row 6)"). Re-seed from the CSV, not from this page |
| 99 | **First-order SCP is judged and not wired:** marginal at 773 K, growing above 1500 K; MVP covers vibrational T-dependence with QHA (registry row 12, "QHA suffices ≤800 °C"); full SCPH (row 13) defers with Layer 1.75 | `2.5:126-130` | keep | `oracle/state/born-oppenheimer-levels#dressing-tiers` | a live scope decision with its reason; `capability-slices` carries the QHA window |
| 100 | Dense `γ̂` is `O(N_r²)` and "**was flagged as** a feasibility risk"; at MVP scale a non-issue because γ̂ is never densified | `2.6-gamma-budget.md:15-16` | keep fact / delete framing | `oracle/state/gamma-budget#budget` | "was flagged as" is history; "γ̂ is never densified" is the live rule and the page's thesis |
| 101 | Encoding: `(Reciprocal, BlockDiag)`, one block per k-point, each stored as orbitals `N_PW × N_b`, not dense `N_PW × N_PW` | `2.6:18-20` | keep | `oracle/state/gamma-budget#budget` | consistent with `gamma-hat §1` row 24; this page carries the sizing consequence |
| 102 | Sizing derivation: PW cutoff ~400 eV ⇒ `N_PW ≈ 1000`; `N_b ≈ 40`; 8×8×8 MP ⇒ ~29 irreducible k-points; `N_PW × N_b × 16 B × N_k` ≈ **~18 MB**; densified `N_PW² × 16 × N_k` ≈ **460 MB** | `2.6:21-25` | keep | `oracle/state/gamma-budget#budget` | `canonical-for: γ̂ MVP budget`; the only place the arithmetic is shown. `gamma-hat §5` (row 39) quotes the two numbers without it |
| 103 | Warm-start initializer: tight-binding 3NN sp³d⁵ for carbon ⇒ ~18×18 Hamiltonian per k, kilobytes; seeds the SCF inner loop; **not a separate residual path** | `2.6:26-28` | keep | `oracle/state/gamma-budget#budget` | the "not a separate residual path" clause prevents a wrong reading; `forced-decisions` is the dependency |
| 104 | Beyond MVP: defect/interface supercells grow `N_PW` linearly, orbital storage stays ≈ linear in `N_atoms × N_b`; the dense-γ̂ concern returns only if a large supercell is densified — which the encoding forbids. **"A supercell memory budget is the first thing to revisit when leaving the primitive cell."** | `2.6:29-32` | keep | `oracle/state/gamma-budget#beyond-mvp` | the final sentence is an unowned forward obligation — candidate open question `supercell-memory-budget`, see Notes §E |

---

### from `oracle-laws-seams`

| # | Fact or block | Current location | Disposition | Target | Evidence |
|---|---|---|---|---|---|
| 1 | Frontmatter `authority: canon`, `content-hash`, `tag` — all six pages | `3.1-generic-dynamics.md:4-5`, `3.2:4-5`, `3.3:4-5`, `7.1:4-5`, `7.2:4-5`, `7.3:4-5` | delete | — | D1/§4: the two-tier `authority` system dies with ch. 11; `content-hash` is a stamp git already answers. Six pages × 2 keys. |
| 2 | `referenced-by` blocks (all six pages) | same files, frontmatter | delete | — | §4: `referenced-by` stays generated, never hand-written. Regenerated into `corpus.json`. |
| 3 | GENERIC equation `dx/dt = L·δE/δx + M·δS/δx`, the four symbols, and the two degeneracy conditions | `3.1-generic-dynamics.md:38-51` | keep | `oracle/laws/generic-dynamics#generic-form` | sole statement; 16 pages list `generic-dynamics` in `depends-on` |
| 4 | Every regime is an **extraction** of the one equation; static = equilibrium readout, time-evolving = trajectory readout; the EOM-violation residual is the structural ground of the others; `/physics` emits granular, aggregation lives in `/informed-operator` | `3.1-generic-dynamics.md:53-61` | keep | `oracle/laws/generic-dynamics#extraction-principle` | sole statement of the extraction principle. The trailing granularity sentence duplicates `residual-definitions` intro and `pino-bridge §1` — see row 5. |
| 5 | "`/physics` emits the full granular residual vector; aggregation into a scalar training objective lives in `/informed-operator`" | `3.1-generic-dynamics.md:60-61` | delete | (cite `oracle/laws/residual-definitions#granularity`) | third verbatim-in-substance statement of one fact. Also at `3.2:55-58`, `7.1:58-60`, `7.2:220-222`, `1.2-product.md:150-152`. Duplication is invisible to both checkers (calibration row 7). |
| 6 | The `E[x]` decomposition (5 terms) and `S[x]` decomposition (3 terms), including the `E_EM` transverse-only annotation | `3.1-generic-dynamics.md#canonical-functionals-and-operators:65-82` | keep | `oracle/laws/generic-dynamics#functionals` | sole statement |
| 7 | The `L` decomposition (6 blocks) and `M` decomposition (2 blocks), with MVP channel sets | `3.1-generic-dynamics.md:84-102` | keep | `oracle/laws/generic-dynamics#operators` | sole statement |
| 8 | The E/S/L/M pieces are assembled across the four BO levels; each level contributes terms acting on its irreducible state | `3.1-generic-dynamics.md:104-105` | keep | `oracle/laws/generic-dynamics#operators` | one-sentence bridge to `born-oppenheimer-levels`; no other statement |
| 9 | The nine-regime extraction table | `3.1-generic-dynamics.md#the-nine-regimes-as-extractions:109-119` | keep | `oracle/laws/generic-dynamics#nine-regimes` | sole statement; page is `canonical-for` "nine regimes as extractions" |
| 10 | Pointer: per-regime derivations are "in the canon chapters (especially `typed-compositions`)" | `3.1-generic-dynamics.md:121-122` | delete | (cite `oracle/registry/typed-compositions`) | "the canon chapters" is a pre-book chapter-numbering phrase with no referent in a flat-journal tree. Verified **not** find/replace damage: `2f1d22f^` has the same words with `impl-06-compositions` in the parenthetical. The substantive pointer (`typed-compositions`) survives as a citation. |
| 11 | Pointer: derivations "grounded in `physics/research/group-{A,B,C}-*.md`" | `3.1-generic-dynamics.md:123` | delete | — | **dangling path.** `physics/research/` contains only `diamond-defect-corpus/` and `diamond-stretch-and-skew-sweep/`; no `group-*` file exists anywhere in the repo (`find` over the whole tree). Neither checker looks at filesystem paths. Same class as the plan's defect 2. |
| 12 | Generator structure is **per-tier**: the two-generator form and its degeneracy conditions hold per tier / per BO level, not as one global bracket | `3.1-generic-dynamics.md#generator-structure-is-per-tier:127-132` | keep | `oracle/laws/generic-dynamics#per-tier-generators` | sole statement; this is the reconciliation that makes the written functionals consistent with the degeneracy conditions |
| 13 | The `γ̂`-block of `L` is Lie–Poisson, `{A,B}(γ̂) = Tr(γ̂·[δA/δγ̂, δB/δγ̂])`, written `[·, γ̂]` not `[Ĥ_KS, ·]`; Jacobi and degeneracy hold by construction | `3.1-generic-dynamics.md:134-139` | keep | `oracle/laws/generic-dynamics#per-tier-generators` | sole statement. Note the L-block list at `:90` still writes the bare `(1/iℏ)[Ĥ_KS, ·]` form this bullet corrects — **contradiction row 3**. |
| 14 | L2 is single-generator (Hamiltonian) at fixed entropy; `S_vib(R,h)` is slow/parametric; the apparent `L·δS_vib/δR ≠ 0` is not a degeneracy violation | `3.1-generic-dynamics.md:140-146` | keep | `oracle/laws/generic-dynamics#per-tier-generators` | sole statement |
| 15 | Jacobi status per `L`-block: canonical blocks exact; generated `AntisymmForm` cross-blocks not automatic; V1 restricts to semidirect-product/Lie–Poisson class or flags | `3.1-generic-dynamics.md:148-154` | keep | `oracle/laws/generic-dynamics#jacobi-status` | sole statement; `build-sequence` Phase-8 depends on it |
| 16 | `Degeneracy` is cert-only, identically zero by construction, a generator-construction-bug tripwire — not a PINO loss term | `3.1-generic-dynamics.md:156-159` | move | `oracle/laws/residual-definitions#categories` (category 8) | the fact is about a residual *category*, and `residual-definitions §1` item 8 already states it. Keep one copy, on the page that owns the category vocabulary; `generic-dynamics` cites it. |
| 17 | The clause "(removed from the `residual-definitions §4.1` training gate)" | `3.1-generic-dynamics.md:159` | delete | — | scaffolding: narrates an edit. The present-tense fact (cert-only, never a training residual) survives in row 16. |
| 18 | `E`-functional activation is level-conditional: L1 uses `E_KS[γ̂; R₀,h₀]` parametric in frozen geometry; L2 replaces it with `E_BO = min_γ̂ E_KS`; the e-ph channel contributes only the beyond-reference part (no double-count) | `3.1-generic-dynamics.md:161-167` | keep | `oracle/laws/generic-dynamics#level-conditional-activation` | sole statement; the double-count guard is load-bearing |
| 19 | **Normative** gauge fixing and electrostatic partition: Weyl gauge `A₀≡0` + transversality `∇·A=0`; `E_EM` counts the transverse sector only; the longitudinal/electrostatic sector is owned by the matter functionals; consequences for the `EOM/A` residual and the minimal-coupling channel | `3.1-generic-dynamics.md#gauge-fixing:169-182` | keep | `oracle/laws/generic-dynamics#gauge-partition` | sole normative statement. `unified-state:42-44` explicitly defers here, so this is the real owner of the gauge convention — **relevant to R1's dangling promise, row 62.** |
| 20 | The trailing provenance stamp "(2026-07 gap-audit A2.)" | `3.1-generic-dynamics.md:182` | delete | — | D2: pages carry no history. See **Log-worthy** row 2. |
| 21 | Residuals are the physics-informed loss terms `/informed-operator` trains against; realized as `OutputRole = ResidualLeaf(key)`; emission discipline is granular and `/physics` never preaggregates | `3.2-residual-definitions.md:52-58` | keep | `oracle/laws/residual-definitions#granularity` | canonical statement of the granularity discipline; the target of rows 5, 44, 63 |
| 22 | The taxonomy is **nineteen** categories = 9 EOM + 3 GENERIC-structural + 5 algebraic + 2 constraint; a facet on each contribution, not a granularity floor or a weighting unit | `3.2-residual-definitions.md#1-the-nineteen-categories:59-67` | keep | `oracle/laws/residual-definitions#categories` | page is `canonical-for` "the nineteen residual categories"; cited by 7 pages |
| 23 | "the pre-multiscale count was seventeen — that is where the older 'seventeen' figure comes from, and it is not a second, smaller taxonomy" | `3.2-residual-definitions.md:64-67` | delete | — | scaffolding: a guard against a stale number. `grep -rn seventeen` over `journal/` + `informed-operator/` returns **this line only** — the figure it defends against no longer exists in the corpus, so the guard defends nothing. Removing it is safe only because that grep is empty; **Phase 2 must re-run it before deleting.** |
| 24 | The `CategoryTag` enum is the closed set of **19** symbols | `3.2-residual-definitions.md:180-183` | keep | `oracle/laws/residual-definitions#categorytag` | **checker hole:** `check_book_structure.py:333` matches only `(\d+)\s+residual categories`. This page states its count as the word "nineteen" (`:9`, `:59`, `:61`) and as "**19** symbols" (`:180`) — none matches. Planting "twenty-one" on the owning page passes clean, while the same corruption on any *citing* page (`1.5-rationale.md:77`, `4.1-physics-graph.md:178`, `8.7-build-verification.md:36`) fires. The one page that defines the count is the one page unchecked. Phase 2: state it in the checked phrasing. |
| 25 | The 9 EOM-violation categories (7 micro DOF + `EOM/DefectPopulation` + `EOM/Continuum`), their aggregate form, and the axis-structure rule (union of diagonal-operator axes and active `CouplingChannel` `InvariantTerm` axes; no per-coupling residual category) | `3.2-residual-definitions.md:69-99` | keep | `oracle/laws/residual-definitions#eom-categories` | sole statement; the axis-structure rule is the join to `coupling-structure` |
| 26 | The 3 GENERIC-structural categories: `Degeneracy` (cert-only), `Conservation` (incl. static γ̂-trace admissibility `‖Tr γ̂ − N_e‖²`), `Positivity` (incl. `T_e ≥ T_L` row 72, breakdown integral row 75, γ̂ N-representability, applicability-gated `ω²≥0` and `‖γ̂²−γ̂‖²`) | `3.2-residual-definitions.md:101-129` | keep | `oracle/laws/residual-definitions#structural-categories` | sole statement. Registry rows 72 (`hot-carrier-temperature-balance`) and 75 (`avalanche-multiplication`) verified present in `registry-manifest.csv`. |
| 27 | The admissibility argument — a candidate γ̂ outside the bounds can zero every EOM residual while being unphysical; these gates are what make the oracle sound as a verifier of the state itself | `3.2-residual-definitions.md:126-129` | keep | `oracle/laws/residual-definitions#structural-categories` | sole statement; load-bearing for the verifier claim |
| 28 | Trailing stamp "(2026-07 gap-audit A1)" | `3.2-residual-definitions.md:128` | delete | — | D2. See **Log-worthy** row 1. |
| 29 | Citation `gamma-hat §5` for "extreme eigenvalues per block are cheap" | `3.2-residual-definitions.md:123-125` | move | re-cite `oracle/state/gamma-hat#encoding-vocabulary`, or state the cost claim here | **dangling promise (new).** `gamma-hat §5` is "Diamond MVP commitment" — it gives the `(Reciprocal, BlockDiag)` encoding and a ~18 MB sizing and says **nothing about eigenvalue-extraction cost.** The checker passes because §5 exists. Second confirmed instance of the plan's defect class, alongside R1→`unified-state` (row 62). |
| 30 | The 5 algebraic-identity categories (Kramers–Kronig, SumRules incl. rotational sum rule row 126, BalanceLaws incl. Wegscheider row 125, Symmetries, MethodEquivalence) | `3.2-residual-definitions.md:131-144` | keep | `oracle/laws/residual-definitions#algebraic-categories` | sole statement. Rows 125/126 verified in the CSV. |
| 31 | The **equivalence-pair vs consistency-pair** distinction: `τ_equiv` trips on any disagreement, `τ_method` only on excess beyond a declared model gap; κ rows 121–122 bind to row 25 as a consistency pair | `3.2-residual-definitions.md:145-156` | keep | `oracle/laws/residual-definitions#pair-kinds` | **this is the canonical owner**, and it is the target for rows 68, 76 and for `capability-slices:84` / `named-formulas:163`, which currently cite `cross-cutting-rules` for it |
| 32 | The 2 constraint-violation categories, disjoint by input-domain type; `Static/Thermodynamic` incl. the T,P-aware metastability band (diamond +25 meV/atom @300 K, 1 atm, Berman–Simon point, σ 5 meV/atom, row 124) and the three slow-tier consistency identities | `3.2-residual-definitions.md:158-178` | keep | `oracle/laws/residual-definitions#constraint-categories` | sole statement. **Values unchecked** — calibration confirms `check_data_agreement.py` does not read the +25 meV/atom figure. Row 124 (`tp-aware-hull`) resolves. |
| 33 | Categories 16/17 stay disjoint because they consume type-distinct inputs, **and the PINO curriculum schedules them differently for that reason** | `3.2-residual-definitions.md:176-178` | keep | `oracle/laws/residual-definitions#constraint-categories` | the justification is load-bearing for §4.1's Polish phase; keep the two clauses together |
| 34 | `ResidualKey = (producer, axes)`; `Producer = Formula \| Method`; `ContributionFacets = (category, bundle, dressing)` as a non-identity sidecar; PINO holds `Map<ResidualKey, Weight>`; weights persist across recompiles | `3.2-residual-definitions.md#2-the-atomic-unit:186-207` | keep | `oracle/laws/residual-definitions#residualkey` | canonical; `7.2 §2` restates it verbatim — see row 47 |
| 35 | Substrate-cluster placement of `ResidualKey` (C5 `ContentAddress`), `CategoryTag`/`BundleId`/`AxisLabel` (C1 `Universe[T]`), `ContributionFacets` (C3 sidecar fiber) | `3.2-residual-definitions.md:209-213` | keep | `oracle/laws/residual-definitions#residualkey` | sole statement of this mapping |
| 36 | The five worked examples of a separately-weightable contribution, and the three negative examples | `3.2-residual-definitions.md#3-examples:215-228` | keep | `oracle/laws/residual-definitions#granularity-examples` | sole statement; the negatives are what make the discipline operational |
| 37 | `evaluate : (State, Environment) → (residuals, gradient, …)`; aggregation (per-category sums, GradNorm, RAD, per-bundle schedules, curriculum gating) lives in `/informed-operator` | `3.2-residual-definitions.md#4-output-type:230-243` | keep | `oracle/laws/residual-definitions#output-type` | sole statement of the kernel's output type. `Environment` appears here as an unowned type — see **Open questions** `environment-record-owner`. |
| 38 | **The curriculum gating schedule** — `[0,0.10)` Warmup: Conservation+Positivity; `[0.10,0.60)` Refine: +EOM/\* and Algebraic/\* except MethodEquivalence; `[0.60,0.90)` Polish: +MethodEquivalence, Static/Snapshot, Static/Thermodynamic; `[0.90,1.00]` Cooldown: weights frozen — plus the four-clause rationale | `3.2-residual-definitions.md#41-curriculum-gating-defaults:245-266` | keep | `oracle/laws/residual-definitions#curriculum-gate` | **stays oracle-side.** The gate is keyed on `CategoryTag`, the oracle's own closed vocabulary, and answers "when is this residual *meaningful*" — a property of the residual surface, not of the operator's data diet. `open-decisions` records it closed at `0.10/0.60/0.90`. |
| 39 | The schedule is a normative **default**, not a contract; `/informed-operator` declares its own `Map<CategoryTag, GateSchedule>` to override | `3.2-residual-definitions.md:268-270` | keep | `oracle/laws/residual-definitions#curriculum-gate` | the override clause is what keeps the fact oracle-side without over-reaching into the operator |
| 40 | The denominator phrase "fraction ∈ [0, 1] of **total training budget**" | `3.2-residual-definitions.md:253` | move | `oracle/laws/residual-definitions#curriculum-gate`, **restated as a fraction of the informed stage** | the stage ordering (row 41) says the oracle attaches for one stage only, so these fractions cannot be fractions of total training. `training-stages.md:17` reads them as within-stage. **Contradiction row 1** — flagged, not adjudicated; the builder must not copy this phrase forward unresolved. |
| 41 | **The training stage ordering** — (1) supervised VASP epochs narrow the space, (2) one informed epoch where the oracle scores and returns keyed residuals + gradients, (3) inference with no oracle call; plus *why* (a residual is a local signal, useless over a large space; the oracle refines, it does not search) and the four consequences (oracle is training-time machinery, nothing in the inference path calls `Validate`, any diagram showing a permanent oracle loop is wrong, physics residuals' place among the four supervisory sources) | `informed-operator/design/training-stages.md:10-41` **and** `journal/live/presentations/2026-07-22-prep.md:286-296` — **no page in `journal/pages/` states it** | mine | `operator/training/stage-ordering` | **the confirmed homeless fact, located.** Owner must be operator-side: it is about the operator's epochs and its deployed form, and it constrains the oracle only by saying when the oracle is absent. The oracle journal owns the *gate inside* stage 2 (row 38); the operator journal owns *the stages*. The boundary is exactly: **`CategoryTag` participation = oracle; epoch sequence and the data source driving each epoch = operator.** Note the source file carries retired ids `arch-11-residuals §11.4.1` and `arch-18-open-decisions` (`:5,:17`) — mine the content, not the citations. |
| 42 | Granularity composes with hash-consing: Stage-3 hash-consing already shares upstream DAG; granularity adds only individually-addressable leaves; full `Map<ResidualKey, Scalar>` in one forward pass at no extra cost; reverse-mode gives per-key gradients by structural projection | `3.2-residual-definitions.md#5:272-283` | keep | `oracle/laws/residual-definitions#hash-consing` | sole statement; this is the cost argument that makes granularity affordable |
| 43 | Closure: contributions are unbounded, *generators* are countable (one per `(formula, applicability cell)` + cert-only and ground-truth-bridge subtypes); the closed-vocabulary discipline holds at the generator level | `3.2-residual-definitions.md#6:285-291` | keep | `oracle/laws/residual-definitions#closure` | sole statement; reconciles granularity with the closed-vocabulary claim |
| 44 | Per-residual error composition: every generator carries `characteristic-scale : σ`, a **declared scale not a fitted weight**, composed by `Quantity.combineTol` into a per-`ResidualKey` budget summing input σ + model-form error + Stage-4 truncation + dressing staleness + coefficient-provenance σ | `3.2-residual-definitions.md#7:293-306` | keep | `oracle/laws/residual-definitions#error-budget` | canonical; the target of `7.2 §1`'s `characteristic-scale` comment and `7.2 §4.1`/`§7`'s flow-into statements |
| 45 | MVP headline design-grade targets (gap ±0.15 eV post-G₀W₀, C_ij ±5%, κ(300 K) ±20%, E_form ±0.2 eV, μ factor-2) restated here | `3.2-residual-definitions.md:308-311` | delete | (cite `oracle/accuracy/accuracy-ledger#mvp-targets`) | the sentence itself says the ledger is canonical. Restating five of them here is the same pattern that the next sentence documents as having already caused drift. Values **unchecked** by the data checker either way. |
| 46 | "Every numeric tolerance named across `/physics` is valued once in the tolerance ledger (`cert-obligations §1.2`), which is canonical for the list. **Restating the enumeration here is what let it drift — it named ten symbols against a ledger of sixteen.**" | `3.2-residual-definitions.md:311-314` | delete | — | scaffolding (narrates a past defect). But the *rule* it protects — one home per tolerance — is the restructure's whole thesis; it belongs in `practice/agent-contract` as a rule, not here as a war story. See **Log-worthy** row 3. |
| 47 | `ResidualKey` / `Producer` definition and the `Map<ResidualKey, Weight>` ↔ `Map<ResidualKey, Scalar>` split, restated | `7.2-residual-machinery.md#2-granularity:98-116` | delete | (cite `oracle/laws/residual-definitions#residualkey`) | the heading itself says "canonical reference: `residual-definitions`" and then restates the block verbatim. Pure duplication; invisible to both checkers. **Exception:** the last three lines — the `dressing` facet is a provenance label and bare/dressed residuals live as *distinct* `FormulaApply`/`MethodInvoke` chains, not weighted siblings — are stated only here. See row 48. |
| 48 | Bare and dressed residuals on the same observable live as **distinct graph chains**, not as weighted siblings | `7.2-residual-machinery.md:112-116` | move | `oracle/compilation/physics-graph#dressing-chains` | sole statement, and it is a fact about graph construction, not about the factory |
| 49 | The `CouplingChannel` object: a symmetry-respecting function from a tensor product of state pieces into `Scalar \| AntisymmForm \| PSDSymmForm`, landing in `E_coupling` / off-diagonal `L` / off-diagonal `M`; every cross-regime term in `generic-dynamics` is one instance | `3.3-coupling-structure.md#1-the-object:52-73` | keep | `oracle/laws/coupling-structure#target-shapes` | sole statement; page is `canonical-for` "coupling target shapes" |
| 50 | `record CouplingChannel` and `record StatePiece`; the `(component, sub-dof)` alphabet; `order`/`derivative` as compose-time truncation choices; the four coverage-policy fields and their roles | `3.3-coupling-structure.md#2-the-parameter-axes:75-118` | keep | `oracle/laws/coupling-structure#channel-record` | canonical; `7.3 §4` restates the first half — see row 77 |
| 51 | "The allowed `(component, sub-dof)` pairs are tabulated alongside the state-component definition" | `3.3-coupling-structure.md:100-102` | move | `oracle/state/unified-state#sub-dof-table` — **and the table must be written** | **dangling promise (third instance).** `unified-state` has no such table: it lists the 7 slots and the `StateComponent` universe and stops. The pointer is prose, so no checker sees it. Same class as R1 (row 62) and `gamma-hat §5` (row 29). |
| 52 | `generate-invariants : CrystalSymmetryGroup × CouplingChannel → GeneratorOutput`; `record InvariantTerm`; `record GeneratorOutput`; the echoed `polynomial_sufficient` flag; constructive-vs-decompositional framing against Stage 2 | `3.3-coupling-structure.md#3-the-invariant-generator:120-170` | keep | `oracle/laws/coupling-structure#invariant-generator` | sole statement; page is `canonical-for` "invariant-generator routine" |
| 53 | The generator **contract**: three well-formedness guards, the O(1) spinor-parity pre-prune, then the trivial-irrep projector | `3.3-coupling-structure.md:172-186` | keep | `oracle/laws/coupling-structure#generator-contract` | sole statement |
| 54 | Emptiness and complexity: character inner product `⟨χ_T, χ_trivial⟩_G`; MVP worst case `\|G\| ≤ 192`, `dim(T) ≤ ~250`; pre-prune ≤ ~200 ops, Reynolds projection ≤ ~12M ops; cached on `Address[CrystalSymmetryGroup] × Address[CouplingChannel]`; cache key excludes theory context | `3.3-coupling-structure.md:188-198` | keep | `oracle/laws/coupling-structure#generator-cost` | sole statement. Figures **unchecked** by the data checker; `\|G\| ≤ 192` agrees with `open-decisions`' GAP requirement line. |
| 55 | "Emptiness ≠ correctness" — a non-empty `poly` may still be only the short-range part | `3.3-coupling-structure.md:200-203` | keep | `oracle/laws/coupling-structure#generator-cost` | live hazard guarding a real confusion; candidate for cross-listing in `practice/traps` |
| 56 | Worked example: diamond electron-phonon channel declaration and its four compose-time steps | `3.3-coupling-structure.md#4-worked-example:205-231` | keep | `oracle/laws/coupling-structure#worked-example` | sole worked example of the whole mechanism |
| 57 | **Symmetry generates the form; provenance supplies the values.** The symmetry group fixes which invariants exist and their index structure; deformation potentials / Fröhlich / anharmonic parameters come from the `ProvenanceLedger` | `3.3-coupling-structure.md:233-241` | keep | `oracle/laws/coupling-structure#form-vs-values` | the load-bearing separation; §8's provenance contract depends on it |
| 58 | Stage 2.5 — invariant synthesis: inputs, action, outputs, and its position between Stage 2's block-diagonalization and Stage 3 | `3.3-coupling-structure.md#5-stage-25:243-259` | move | `oracle/compilation/compose-time-pipeline#stage-2-5` | a pipeline stage defined on a laws page. `compose-time-pipeline` is the pipeline's owner and already carries stages 1–4; the sub-stage should live with its siblings and `coupling-structure` should cite it. **Nomenclature defect (§4 of the plan): "Stage 2.5" inside a "4+1 stage" pipeline — propose a rename to Javier, do not act.** |
| 59 | Composition rules: direct sum within a target; E/L/M assembly across targets; monotone order truncation; **no channel-correlation primitive in V1** (correlated mechanisms are one channel with more pieces); kernel extensions add as one more summand, so a long-range mechanism splits into two channels (Verdi–Giustino SR/LR split) | `3.3-coupling-structure.md#6-composition:261-286` | keep | `oracle/laws/coupling-structure#composition` | sole statement; the V1 additivity restriction is a real design commitment |
| 60 | Cert hooks: obligations 1, 2, 5 collapse to projection-residual checks; `kernel_extension` is not exempt (little-group equivariance `‖K(Rq,ω) − D(R)K(q,ω)D(R)†‖ < δ_sym`); target-tag-determined projection rule; PSD guard on the assembled per-mechanism super-block, not per off-diagonal kernel | `3.3-coupling-structure.md#7-cert-hooks:288-320` | keep | `oracle/laws/coupling-structure#cert-hooks` | sole statement of *how* the generator discharges the obligations; `cert-obligations` owns the obligation list and the tolerances |
| 61 | "The cert-obligation indices are now fixed in `cert-obligations §1.1`: equivariance = 1, antisymmetry of `L` = 5, PSD of `M` = 2" | `3.3-coupling-structure.md:318-320` | keep, drop "now" | `oracle/laws/coupling-structure#cert-hooks` | the index mapping is a present-tense fact; "are now fixed" is the scaffolding wrapper (D1) |
| 62 | `make-coupling-channel` registration; `applicability` validated as first-order decidable on typeclass tags; identity is `Address[CouplingChannel]` under the canonical-serialization rule; identical channels collapse to one address | `3.3-coupling-structure.md#8-registration-discipline:322-338` | keep | `oracle/laws/coupling-structure#registration` | sole statement |
| 63 | **Coefficient-provenance contract:** every provenanced coefficient carries `(value, σ, source, cost-class)` with `cost-class ∈ {curated, per-material-DFPT, fit}`; a cert obligation refuses any composition whose active channels carry unprovenanced coefficients; MVP diamond coefficients are `curated` | `3.3-coupling-structure.md:340-348` | keep | `oracle/laws/coupling-structure#provenance-contract` | sole statement; feeds the `coefficient-provenance σ` term of `residual-definitions §7` |
| 64 | **`slope-kind` double-count guard** for `dE_g/dT` (AHC): `slope-kind ∈ {isochoric, total}` on the coefficient; a cert obligation refuses a `total` slope co-active with row 63's thermal-expansion path; curated ZPR amplitudes (GaN −189, AlN −399, diamond −345 meV) are `isochoric`; the lattice-expansion part (GaN −49, AlN −85 meV) is row 63's | `3.3-coupling-structure.md:350-364` | keep | `oracle/laws/coupling-structure#slope-kind-guard` | rows 120 and 63 verified in the CSV. **Values unchecked by the data checker** (calibration probe B). Hand-verified against `9.1-accuracy-ledger:71` (row 15): "diamond −345 meV indirect, GaN −189, AlN −399" — **agrees.** |
| 65 | Trailing "(Wave-1 III-N audit: `[timeline] §2026-06-10 (Wave 1)`; the prior `total` tag on isochoric Engel magnitudes was corrected there.)" | `3.3-coupling-structure.md:363-364` | delete | — | D2. The citation resolves (dated anchors + parenthetical are checked). See **Log-worthy** row 4. |
| 66 | **Polarization-reference pairing guard:** ΔP's ±5% rests on an accidental cancellation (Dreyer PRX 6 021038 §V.D–E), holding only under a self-consistent pairing; `polarization-reference ∈ {ZB-proper, H-improper}` on rows 113/114/117; a cert obligation refuses mismatched tags; `is-AlGaN-GaN` validity scope, cert-refused for high-In InGaN/GaN | `3.3-coupling-structure.md:366-380` | keep | `oracle/laws/coupling-structure#polarization-pairing-guard` | sole statement; the "improper `e₃₁` ≈ 3.4× proper" ratio is the reason the guard is machine-checkable rather than advisory |
| 67 | **Learned-correction training contract:** a PINO-learned coefficient is (1) fit only against external anchors and frozen w.r.t. the PINO loss — a correction trained on the residual it modifies can co-adapt to zero it; (2) `Δα` ships as identity until anchors exist, and the high-E×high-T corner stays cert-refused | `3.3-coupling-structure.md:382-392` | move | `operator/loss/learned-corrections` — **with the refusal half kept at** `oracle/certification/cert-obligations#obligation-9` | this is a **training** constraint stated on an oracle laws page. Reason (1) is a loss-design rule (the operator's business); reason (2) is a refusal the oracle enforces. The block currently fuses them. Hand-verified against `9.1-accuracy-ledger:105` (row 49): agrees verbatim on "fit only to external anchors and frozen w.r.t. the PINO loss" and "ships as identity". |
| 68 | `record CouplingSpec { channels, theory_context }`; theory frame is part of identity by the record-address rule | `3.3-coupling-structure.md:394-402` | keep | `oracle/laws/coupling-structure#couplingspec` | sole statement; page is `canonical-for` "CouplingSpec record" |
| 69 | "`CouplingSpec` was previously a bare `SparseSet[CouplingRegistry]`; it is now a two-field record. … The `CouplingSpec` schema version is **bumped** so old bare-set addresses cannot collide" | `3.3-coupling-structure.md:404-410` | mine | `oracle/laws/coupling-structure#couplingspec` (the version-distinctness rule only) | the "previously/now" framing is scaffolding (D1). The *live* fact — the schema version distinguishes the two encodings so addresses cannot collide — survives, but restated without the history it is nearly vacuous. Marked `mine` rather than `delete` per the brief's tie-break; **I am not confident it survives the removal of its own motivation.** |
| 70 | The diamond MVP `CouplingSpec` (e-ph short-range + minimal coupling + ion-ion electrostatic + phonon-phonon in `M`, under the MVP default theory context) | `3.3-coupling-structure.md:410-414` | keep | `oracle/laws/coupling-structure#couplingspec` | sole statement; agrees with the MVP sets in `generic-dynamics:74-77, 99-101` |
| 71 | `theory_context` is **definitional input**, set at Stage 1, required before Stage 2 builds the (possibly double-cover) group; `make-theory-context` smart constructor must normalize before any `Address` is taken (hybrid double-representation normalization + relativistic PP/run consistency) | `3.3-coupling-structure.md:416-427` | keep | `oracle/laws/coupling-structure#theory-context-placement` | sole statement; page is `canonical-for` "TheoryContext placement" |
| 72 | Coverage policy is not an enumerated registry: a channel is a tuple in parameter space; the runtime rule is "active = applicability holds ∧ invariant basis non-empty"; `record CoverageBound`; MVP global cap `(4, Gradient(1))` driven solely by 4-phonon anharmonicity; per-mechanism inner table prunes before the character test | `3.3-coupling-structure.md#9-coverage-policy:429-460` | keep | `oracle/laws/coupling-structure#coverage-policy` | canonical; `open-decisions` Closed decisions restates the cap — **contradiction-free but duplicated**, and the register copy should become a citation |
| 73 | The piezoelectric-acoustic channel (`LongRangeStatic(1)`, `1/q` pole), `is-noncentrosymmetric`-gated, inert for diamond | `3.3-coupling-structure.md:462-466` | keep | `oracle/laws/coupling-structure#mechanism-range-table` | belongs with the table it extends (row 75), not in the coverage-policy prose |
| 74 | "This **closed the former open-decisions coupling-channel coverage-policy item** (now recorded under `[open-decisions]` Closed decisions)" | `3.3-coupling-structure.md:466` | delete | — | textbook D1/§5: a closed item's *resolution* is the page body (rows 72–73); the story of it having been open is scaffolding |
| 75 | Mechanism range and polynomial sufficiency: why some couplings are non-polynomial; `record MechanismRange`; the total O(1) derived `polynomial_sufficient` projection; the `make-coupling-channel` well-formedness invariant; `mechanism_range` ⟂ `applicability`; the 15-row template table | `3.3-coupling-structure.md#10:468-533` | keep | `oracle/laws/coupling-structure#mechanism-range-table` | canonical; page is `canonical-for` "polynomial-sufficiency flag" and "coupling coverage policy" |
| 76 | Screening channels are not in the diamond MVP but are the forcing function for the schema; ALDA `f_xc` is the degenerate corner — **tag by general mechanism, not by the cheapest approximation** | `3.3-coupling-structure.md:535-540` | keep | `oracle/laws/coupling-structure#mechanism-range-table` | the tagging rule is a live discipline, not an aside |
| 77 | `record KernelExt` and the four-variant backbone (one fiber-bundle section, differing in rank / value field / parametric-vs-tabulated) | `3.3-coupling-structure.md#11:542-561` | keep | `oracle/laws/coupling-structure#kernel-ext` | sole statement; page is `canonical-for` "KernelExt extension family" |
| 78 | Kernel sizing: `Parametric` < 1 KB; `Tabulated` full-frequency dielectric matrix for diamond ≈ **440 GB** worst case, ≈ 0.5 GB after plasmon-pole + IBZ reduction; grid is a `CacheEligible` sidecar folded in by address, never by value; **no MVP channel is tabulated**; 440 GB is the number the persistent-storage tier must be designed against | `3.3-coupling-structure.md:562-575` | keep | `oracle/laws/coupling-structure#kernel-ext` | sole statement. Arithmetic spot-check: 12³ × 64 × 500² complex128 ≈ 442 GB — **self-consistent.** Data checker does not read it. |
| 79 | `GaugeRule` is `None` for every MVP channel and recorded only when a genuine P3 gauge-fixing rule attaches | `3.3-coupling-structure.md:577-580` | keep | `oracle/laws/coupling-structure#kernel-ext` | sole statement |
| 80 | `record TheoryContext` (4 fields, ten closed C1 vocabularies in `canonical-vocabularies §7`); it enters neither the generator cache key nor the runtime kernel; it is solely cert+provenance metadata | `3.3-coupling-structure.md:582-605` | keep | `oracle/laws/coupling-structure#theory-context-placement` | sole statement; the "does not enter" clauses are what make the caching sound |
| 81 | The MVP default theory context: `GGA(PBE)` / PseudoDojo v0.4.1 NC (Ga `3d` semicore promoted) / `KohnSham` / `ScalarRelativistic`; PBE's UWBG gap underestimate is handled by theory-conditioning the reference-battery obligation, not by upgrading the default; `Hybrid(HSE06)` is the documented upgrade | `3.3-coupling-structure.md:607-613` | keep | `oracle/laws/coupling-structure#mvp-theory-context` | sole statement; restated (compatibly) in `open-decisions` Closed decisions, which should become a citation |
| 82 | PSD closure: why invariant-subspace membership does not imply PSD; the three documented assumptions `[PSD-e-ph]`, `[PSD-ph-ph]`, `[PSD-rad]` with origins, references, and tight/loose closure; closure is **tight at the operator level** (Reynolds image of a PSD seed is PSD, so no SDP feasibility search) and **loose at the coefficient level**, so the runtime guard `λ_min ≥ −δ_PSD` stays | `3.3-coupling-structure.md#12:615-658` | keep | `oracle/laws/coupling-structure#psd-closure` | sole statement; the tight/loose split is what turns an obligation into a theorem plus a cheap guard |
| 83 | Dormant SDP fallback (V2): interior-point `O(dim^{3.5})` at registration only, block-diagonalizable per Gatermann–Parrilo 2004; infeasibility rejects the channel; specified for forward-compatibility only | `3.3-coupling-structure.md:660-666` | keep | `oracle/laws/coupling-structure#psd-closure` | forward-looking, not history — not scaffolding |
| 84 | "`pino-bridge` is the only surface `/informed-operator` (and other downstream consumers) sees. Two exports." | `7.1-pino-bridge.md:33-34` | keep | `oracle/seams/pino-bridge#surface` | canonical owner of the "only surface" claim. `learnable-structure-requirements.md:13-15` restates it — see row 96. |
| 85 | The `Validate` signature (4 params → 4 outputs) and the semantics of `request` (subgraph selection) and `gradient` (`Skip` runs forward-only) | `7.1-pino-bridge.md#1-validate:36-57` | keep | `oracle/seams/pino-bridge#validate` | page is `canonical-for` "Validate signature". **Duplicated at `1.2-product.md:137-146`** with a *different* type vocabulary (`Map<SlotKey, Float>` vs `Map<ResidualKey, Scalar>`, `kernel : content hash` vs `cert : CertEvidence`) — **contradiction row 4.** |
| 86 | Output is granular by construction; the PINO chooses aggregation; `/physics` does not pre-sum | `7.1-pino-bridge.md:58-60` | delete | (cite `oracle/laws/residual-definitions#granularity`) | fourth statement of the same fact; see row 5 |
| 87 | The `Import` signature and per-target ingestion: each call wraps one external datum as a `GroundTruthBridgeGenerator`; Stage 1 inserts a pinned `Input` node carrying `(value, sigma)` plus a cert-only `ResidualLeaf`; `Import` is **not** differentiated through; its outputs serve obligation 4 and the operator's target-vs-prediction comparison | `7.1-pino-bridge.md#2-import:62-82` | keep | `oracle/seams/pino-bridge#import` | page is `canonical-for` "Import signature"; sole statement of the not-differentiated-through rule |
| 88 | `CoverageMask = RoaringCoverageMask`: flat-index construction over the generator's `axes`; sparse-from-start rationale; why Roaring (O(1) membership, set ops for the cert evaluator, ubiquitous bindings); persisted into the `coverage_mask` column of `SqliteReferenceCache.entries` | `7.1-pino-bridge.md#21:84-107` | keep | `oracle/seams/pino-bridge#coverage-mask` | sole statement; `open-decisions` records it closed and should cite rather than restate |
| 89 | What is not exported: `Predict`, `Certify`, `EnumerateObservables` remain internal `/physics` API for non-PINO consumers; not part of the pino-bridge contract | `7.1-pino-bridge.md#3:109-114` | keep | `oracle/seams/pino-bridge#not-exported` | sole statement; `1.2-product.md:219` cites it correctly |
| 90 | The factory's three responsibilities and its placement in Stage 1 under the always-cheap reframe | `7.2-residual-machinery.md:43-49` | keep | `oracle/seams/residual-machinery#factory` | sole statement |
| 91 | `record ResidualGenerator` — all 20 fields | `7.2-residual-machinery.md#1:51-96` | keep | `oracle/seams/residual-machinery#generator-record` | page is `canonical-for` "ResidualGenerator record"; 17 pages reference it |
| 92 | The `bundle` field comment: "A SET, not a scalar: 40 of the 134 registry rows carry two bundles … and this field read `BundleId` singular, which silently narrowed every one of them on the way in." | `7.2-residual-machinery.md:57-65` | mine | `oracle/seams/residual-machinery#generator-record` — keep "a set, not a scalar; 40 of 134 rows carry two bundles", drop the rest | the narrowing story is D1 scaffolding inside a code comment, where neither the changelog stripper nor any grep for history markers will find it. The count (40 of 134) is a live check. See **Log-worthy** row 5. |
| 93 | `diff-tag : D0 \| DN \| D1 \| D2 \| D3 \| D4` | `7.2-residual-machinery.md:70` | keep | `oracle/seams/residual-machinery#generator-record` | **nomenclature defect (§4):** `DN` is not between `D0` and `D1`. Propose the rename to Javier; do not act. |
| 94 | `layer : 0..6 — the 7-layer compute DAG of residual-generator-catalog §2` | `7.2-residual-machinery.md:67-68` | move | re-cite `oracle/laws/residual-definitions#generator-layers` or wherever the 7-layer DAG lands from ch. 11 | **dangling reference in canon (new, defect-2 class).** `residual-generator-catalog` is **not a page id** — the page is `deriv-generator-catalog`. Checker calibration: `check_book_structure.py:466` skips any `pid` not in the page set, so a bare id + `§` to a nonexistent page is silently ignored. This is the *only* occurrence of the string in the repo. Since ch. 11 is being mined, the 7-layer DAG needs a new home and this citation needs to point at it. |
| 95 | `make-residual-generator(...)` factory entry point; called once per formula at load time; inserted into Stage 1 when `applicability` holds | `7.2-residual-machinery.md#3:117-131` | keep | `oracle/seams/residual-machinery#factory-entry` | page is `canonical-for` "residual factory"; `coupling-structure §8` cites it as the pattern channels reuse |
| 96 | The three generator subtypes (standard / ground-truth-bridge / cert-only) and their loss treatment | `7.2-residual-machinery.md#4:133-143` | keep | `oracle/seams/residual-machinery#subtypes` | sole statement |
| 97 | Fidelity generators (the ε obligation): **normative** — a lowering that introduces representation error must register a paired cert-only fidelity generator carrying an a-posteriori estimator; the three triggering lowerings with estimators and costs | `7.2-residual-machinery.md#41:145-167` | keep | `oracle/seams/residual-machinery#fidelity-generators` | sole statement; `open-decisions` verifier-gap 4 and `traps` §58's sibling both point here |
| 98 | **An a-priori target is not a substitute for an a-posteriori estimate** — the target is what the plan intended, the estimate is what it achieved, only the second is evidence | `7.2-residual-machinery.md:162-167` | keep | `oracle/seams/residual-machinery#fidelity-generators` | live discipline; already cross-listed as `traps` §58's principle. Keep one full statement here, a one-line hazard in `practice/traps`. |
| 99 | The fidelity generator's output flows into `Quantity.combineTol` and into the cert, and **never** into the training loss — a network must not be able to reduce loss by making the oracle's self-assessment optimistic | `7.2-residual-machinery.md:169-172` | keep | `oracle/seams/residual-machinery#fidelity-generators` | sole statement; the anti-gaming argument is load-bearing and parallels row 67 |
| 100 | **Registration-time adjoint gate:** `D2` and `D3` run vJp-vs-JvP on `N ≈ 64` sampled points; max relative error > `τ_adj` (default `1e-4`) fails the build loud; `D3` runs a second check on the reciprocal condition number of the fixed-point Jacobian against `τ_cond`; the escape hatch is `D4`, not `D3` | `7.2-residual-machinery.md#5:174-188` | keep | `oracle/seams/residual-machinery#registration-gate` | page is `canonical-for` "registration-time adjoint gate"; `open-decisions`' `τ_cond` closure points here |
| 101 | "(`D3` was a fallback tier in the retired catalog legend; that is where the misreading comes from — see `[named-formulas]`.)" | `7.2-residual-machinery.md:186-188` | delete | — | D1: explicitly a retired-legend story. The live rule ("`D3` is not a weaker `D2`") is row 100; the hazard is already `traps` §53. |
| 102 | A passing gate is not evidence of a correct tag: vJp and JvP agree trivially where the true gradient is zero; a piecewise-constant output passes spuriously; rows 46 and 50 did; check that sampled points straddle a varying region | `7.2-residual-machinery.md:190-197` | keep | `oracle/seams/residual-machinery#registration-gate` | **near-verbatim duplicate of `traps` §52**, which itself carries "— enforced, `[residual-machinery]` §5". Keep the operative rule here; reduce `practice/traps` to the hazard line + citation. Same arrangement applies to `traps` §60 vs the `τ_cond` sentence in row 100. |
| 103 | **A dormant row cannot be gated, and must not read as though it were.** Row 122 (`iterative-lbte-kappa`) is `D3` but its `source` cell declares it dormant/anchored in V1: no fixed point, so no Jacobian, so both gates pass trivially. Its gate is **deferred, not passed**, and must be recorded as such in the cert. Any row whose `source` declares it dormant or anchored inherits this rule. | `7.2-residual-machinery.md:198-208` | keep | `oracle/seams/residual-machinery#registration-gate` | sole statement, **and it has no trap entry** unlike its siblings 52/53/60. Row 122 verified in the CSV. Candidate for a new `practice/traps` entry — flagged for Phase 2, not created here. |
| 104 | Most D2 generators with a fixed-point solve are wired to the implicit-diff adjoint synthesized at Stage 4; the gate verifies that synthesized adjoint, not a hand-written backward | `7.2-residual-machinery.md:210-213` | keep | `oracle/seams/residual-machinery#registration-gate` | sole statement — **and it is the sentence that makes the open verifier gap legible.** See **Open questions** `adjoint-drift-monitoring`. |
| 105 | Training-loop consumption: enumerate active residuals for the current curriculum phase (**Warmup → Refine → Polish**), sample per policy, evaluate forward + projection, run same-pass fixed-point iteration at the DAG layer barrier; aggregation lives entirely in `/informed-operator` | `7.2-residual-machinery.md#6:215-222` | move | `operator/training/loop` (the loop steps) + delete the aggregation sentence as a re-statement of row 5 | this describes the *operator's* loop from an oracle page. The phase list omits Cooldown and so matches neither `residual-definitions §4.1` (Warmup/Refine/Polish/Cooldown) nor `11.8` / `residual-loss-methodology` (Warmup/Refine/Calibrate/Polish) — **contradiction row 2.** |
| 106 | `record OneShotCert` (Layer 1.25) and its scheme enum, hashes, parameters, granular `closure-residual`, `reference-state`, `staleness-coeff`, cost-tier | `7.2-residual-machinery.md#7:224-247` | keep | `oracle/seams/residual-machinery#dressing-certs` | sole statement |
| 107 | The **validity radius**: a Layer-1.25 dressing drops its state-dependence; to first order the dropped term is `‖x − reference-state‖ · staleness-coeff`; their product is the dressing-staleness entry `combineTol` already listed but had no number for; nothing is refused for leaving the radius — the term simply grows | `7.2-residual-machinery.md:249-261` | keep | `oracle/seams/residual-machinery#dressing-certs` | sole statement. "…already lists but until now had no number for" is D1 phrasing wrapped around a live fact — rewrite present-tense, do not delete. `open-decisions` verifier-gap 3 records this closure and should cite rather than restate. |
| 108 | `record IterationSnapshot` and `record IterativeResult` (Layer 1.75, V2-deferred); V1 ships Layer 1.25 wired and Layer 1.75 as type/cert scaffolding with `not-implemented-in-V1` stubs that fail loud | `7.2-residual-machinery.md:263-286` | keep | `oracle/seams/residual-machinery#dressing-certs` | sole statement. **Nomenclature defect (§4): "Layer-1.75."** Propose; do not rename. Note "scaffolding" here means *stub code*, not corpus scaffolding — do **not** delete on a keyword sweep. |
| 109 | Cadence policy: T0 every SGD step, T1 RAD-subsampled, T2 per-epoch cached, T3 on-demand/calibration-only with a cheap proxy; only T0/T1 on the per-sample hot path; this is what makes the always-cheap claim honest about runtime cadence | `7.2-residual-machinery.md#8:288-304` | move | `operator/training/cadence` (the training-loop cadence) — leaving the **cost-tier definitions** at `oracle/registry/named-formulas#cost-tiers` | the left column (≤10 µs / ≤10 ms / ≤10 s / ≤10 min) is an oracle property of a formula; the right column ("every SGD step", "per-batch stochastic importance") is a training-loop policy. Same split as rows 38/41. `residual-loss-methodology.md:164-168` states an overlapping T0–T3 cadence with a **different T2 rule** — see contradiction row 5. |
| 110 | The example row assignments: rows 25 and 92 tagged T2; row 80 is T3 "and belongs on the line below"; row 87 is T3 | `7.2-residual-machinery.md:298-299` | mine | `oracle/registry/named-formulas#cost-tiers` (as plain assignments) | rows 25/80/87/92 and their names all verified against `registry-manifest.csv`. "both of which the CSV actually tags T2" and "belongs on the line below" are correction artifacts (D1) — keep the assignments, drop the narration. |
| 111 | "These rules cut across formulas, methods, residuals, cert, and the pino-bridge surface. They are not architectural decisions — **those live in `arch-*`**." | `7.3-cross-cutting-rules.md:26-30` | delete | — | **retired-id namespace live in canon** — `arch-*` pages were renamed on 2026-07-22 (`physics/library/formulas/retired-ids.csv`). A *third* site beyond the plan's two, and the first inside `journal/pages/`. No checker reads `retired-ids.csv` (plan defect 7), and a glob is not an id, so nothing could have caught it. The sentence's referent no longer exists; the framing dies with the page (row 116). |
| 112 | Rule 1 — method equivalence as a residual, not a path-selector: all applicable formulas instantiated as `FormulaApply` nodes with hash-consing; equivalence enforced via `Algebraic/MethodEquivalence`, one leaf per pair; obligation 6 consumes it and trips with both values as witnesses | `7.3-cross-cutting-rules.md#1:31-47` | delete | (cite `oracle/laws/residual-definitions#algebraic-categories` and `oracle/certification/cert-obligations#obligation-6`) | every clause is owned elsewhere: the category by `residual-definitions §1`, the obligation by `cert-obligations`, hash-consing by `compose-time-pipeline §3`. Pure re-statement. |
| 113 | Rule 1's equivalence-pair vs consistency-pair paragraph (τ kinds; Callaway-vs-BTE κ is a consistency pair) | `7.3-cross-cutting-rules.md:48-54` | delete | (cite `oracle/laws/residual-definitions#pair-kinds`) | verbatim-in-substance duplicate of `3.2-residual-definitions.md:145-156`. **This is the fact `capability-slices:84` and `named-formulas:163` currently cite `[cross-cutting-rules]` for** — both citations should repoint to `residual-definitions`. |
| 114 | Rule 1's last bullet — the `Observable` output role designates the *exposed* value; **selection is by `ContributionFacets` precedence: declared dressing tier, then registration order**; unselected formulas still contribute their leaves | `7.3-cross-cutting-rules.md:55-59` | move | `oracle/compilation/physics-graph#observable-role` | **the only non-duplicated fact on this page.** `physics-graph` defines `Observable(bundle)` at `:129` and `:134` but states **no selection precedence anywhere**. If `cross-cutting-rules` is dissolved without moving this, the rule is lost. |
| 115 | Rule 1's closing claim — the architecture never averages observables and never silently selects between formulas at runtime; both are exposed and the disagreement is a typed residual | `7.3-cross-cutting-rules.md:61-63` | move | `oracle/compilation/physics-graph#observable-role` | travels with row 114; it is the *why* for that precedence rule |
| 116 | Rule 2 — same-type ⇒ one interface many handlers; type-change between stages ⇒ keep the stages; with both example lists | `7.3-cross-cutting-rules.md#2:65-81` | move | `program/purpose/architectural-principles#same-type-vs-stage` | a genuine cross-cutting design heuristic, and the only block on the page that is not a restatement of a specific mechanism. It is an *architectural principle*, which is precisely what line 27 denies this page contains. |
| 117 | Rule 3 — provenance tags are not weighting axes: facets are queryable provenance, never part of `ResidualKey` identity, never a loss-weight basis; weighting lives in the operator's curriculum keyed by `CategoryTag` gates only | `7.3-cross-cutting-rules.md#3:83-92` | move | `oracle/laws/residual-definitions#facets` | the rule is *about* `ContributionFacets`, which `residual-definitions §2` owns and already states as "sidecar; not part of identity". Merging removes the duplication and puts the prohibition next to the definition. `computational-overview:487` cites `cross-cutting-rules §3` and must repoint. |
| 118 | Rule 4 — couplings are declared channels, terms are generated; adding a regime is a channel declaration, not a code edit; channels register through the same factory pattern | `7.3-cross-cutting-rules.md#4:94-107` | delete | (cite `oracle/laws/coupling-structure#coverage-policy` and `#registration`) | every clause restates `coupling-structure` §1–§5, §8, §9. The one memorable sentence ("adding a new physical regime is a channel declaration, not a code edit") is a rephrasing of §9's coverage-policy rule; if Javier wants it kept for readability it belongs at `coupling-structure#coverage-policy`. `computational-overview:549` cites `cross-cutting-rules §4` and must repoint. |
| 119 | The page `cross-cutting-rules` itself, and its `canonical-for: [cross-cutting rules]` | `7.3-cross-cutting-rules.md` (whole page) | delete | — | **It is a bag of rules, not a topic.** Disposition per rule above: 2 delete-as-duplicate (112/113, 118), 3 move (114/115, 116, 117), 1 delete-as-remnant (111). Nothing remains. Its ownership claim is **de-facto vacuous** — topic `cross-cutting rules` vs id `cross-cutting-rules`, differing by one hyphen, so it escapes both the duplicate-topic invariant (calibration: no vacuity check exists) **and the plan's 18-page vacuity census**, which counts only exact matches. Graph load is low: 3 `referenced-by`, 4 prose citations, all repointable above. **This page is listed as surviving in the plan's target tree (§3, `oracle/seams/`); I am recommending it does not.** |

---

---

### from `oracle-compilation`

Targets use `journal/section/page#anchor`. Anchor slugs are proposals.

**Coverage.** All 54 heading-level blocks across the four pages appear in exactly
one row (4.1: 7, 4.2: 8, 4.3: 12, 4.4: 27). Split rows are added wherever a
block contains a fact whose target differs from the block's own; those cite a
line range rather than an anchor. Source anchors here are abbreviated
GitHub-style slugs of the *current* headings — they are locators, not proposals.

### 4.1-physics-graph.md → `oracle/compilation/physics-graph`

| # | Fact or block | Current location | Disposition | Target | Evidence |
|---|---|---|---|---|---|
| 1 | `authority: canon`, `content-hash: f2084f41fb88` | `4.1-physics-graph.md:4-5` | delete | — | D3/§4 delete the `authority` tier and `content-hash`. Calibration proved the hash actively *masks* other defects (Notes → Calibration) |
| 2 | `canonical-for: PhysicsGraph schema / NodeKind, OutputRole, ResidualKey / per-stage sidecars` | `4.1-physics-graph.md:6-9` | keep | `oracle/compilation/physics-graph` frontmatter `owns:` | three topics, none identical to the id — this page is **not** vacuous; one of the good examples to pattern the other 18 on |
| 3 | The `PhysicsGraph` is the single data structure everything in `/physics` is a view of | `4.1-physics-graph.md#the-physicsgraph` (43-57) | keep | `oracle/compilation/physics-graph#the-graph` | sole statement; 14 pages list it in `referenced-by` |
| 4 | `Node` = 4 fields (`id`, `type`, `kind`, `role`); decorations live in sidecars, not on the node | `4.1-physics-graph.md#1-anatomy-of-a-node` | keep | `oracle/compilation/physics-graph#node` | sole normative statement of node anatomy |
| 5 | `NodeKind` is the substrate's primary closed-polymorphism mechanism; graph identity = closure of output `Address[GraphNode]` multiset | `4.1-physics-graph.md:81-88` | keep | `oracle/compilation/physics-graph#node` | restated at `4.3:143` and `4.3:311-316` — **duplicate pair**, keep here (topic owner), delete the 4.3 copy (row 47) |
| 6 | The three node kinds; `InputKind = StateSlot \| EnvScalar(EnvField)` | `4.1-physics-graph.md#2-the-three-node-kinds` | keep | `oracle/compilation/physics-graph#node-kinds` | sole statement |
| 7 | **`EnvField` is used and never defined** | `4.1-physics-graph.md:98` | move | `oracle/state/crystal-inputs#environment` | homeless — see Open questions `environment-record`. No page's `canonical-for` claims it; only other mentions are `11.8:269`, `11.8:580` (a page being deleted) |
| 8 | Symmetry projection / fixed-point solves / observables+residuals are **not** additional node kinds | `4.1-physics-graph.md:110-123` | keep | `oracle/compilation/physics-graph#node-kinds` | negative-space statement; sole source. High value — prevents re-proposal |
| 9 | `OutputRole = Internal \| Observable(BundleId) \| ResidualLeaf(ResidualKey)` and what each exposes | `4.1-physics-graph.md#3-output-role` | keep | `oracle/compilation/physics-graph#output-role` | sole statement |
| 10 | Per-stage sidecar list + `CompressionPlan` family; sidecars are not hash-consed and do not survive their last consumer | `4.1-physics-graph.md#4-per-stage-sidecars` | keep | `oracle/compilation/physics-graph#sidecars` | sole *definition*; restated verbatim at `4.4:296-307` (row 78) |
| 11 | Stage-visibility poset `1 < 2 < 2.5 < 3 < 4 < 5` | `4.1-physics-graph.md:145-147` | delete | — | duplication: stated three times (`4.1:146`, `4.3:304`, `4.4:298`). Keep the `4.3` statement (row 46), which owns the substrate's attachment discipline |
| 12 | "The graph *is* every other vocabulary" — 13-row realization table | `4.1-physics-graph.md#5-the-graph-is-every-other-vocabulary` | keep | `oracle/compilation/physics-graph#vocabulary-realization` | sole cross-map from every closed vocabulary to its graph realization. Load-bearing for the closure argument |
| 13 | BO level is "derivable from a node's transitive inputs; **not stored**" | `4.1-physics-graph.md:179` | keep | `oracle/compilation/physics-graph#vocabulary-realization` | restated `4.4:252`; this is the owning page (row 71 deletes the 4.4 copy) |
| 14 | Why it is *the* data structure — closure / composition / correctness / performance / substrate-agnosticism | `4.1-physics-graph.md#6-why-it-is-the-data-structure` | keep | `oracle/compilation/physics-graph#why-one-structure` | rationale, present-tense, no history markers |
| 15 | Closing analogy: graph : `/physics` :: relational schema : database | `4.1-physics-graph.md:208-211` | keep | `oracle/compilation/physics-graph#why-one-structure` | the single best one-line framing in the chapter |
| 16 | 44 backticked cross-refs, **0 bracketed** | `4.1-physics-graph.md` (whole) | move | one citation syntax `[id#anchor]` per §4 | 100% of this page's references are in the syntax calibration proved unchecked |

### 4.2-compose-time-pipeline.md → `oracle/compilation/compose-time-pipeline`

| # | Fact or block | Current location | Disposition | Target | Evidence |
|---|---|---|---|---|---|
| 17 | `authority`, `content-hash: 909aad6a17d6` | `4.2:4-5` | delete | — | D3/§4 |
| 18 | `canonical-for: 4+1 stage compose-time pipeline / always-cheap discipline / stage boundaries` | `4.2:6-9` | move | `oracle/compilation/compose-time-pipeline` frontmatter `owns:` | topic 1 carries the nomenclature defect (Contradictions row C1); topics 2–3 are sound. **Do not rename** — proposal only, per §4 |
| 19 | One residual surface, one stack, one fidelity; cost bounded by compose-time specialization | `4.2#the-compose-time-pipeline` (50-54) | keep | `oracle/compilation/compose-time-pipeline#always-cheap` | the always-cheap discipline's own statement; cited by `architectural-principles` p1 |
| 20 | Everything before Stage 5 runs once per `(PeriodicityStructure, SiteDecoration, Environment)` tuple | `4.2:56-60` | keep | `oracle/compilation/compose-time-pipeline#always-cheap` | sole statement of the recompile unit |
| 21 | Stage 1 — symbolic lift: inputs, template instantiation, applicability pruning, sidecar discarded | `4.2#1-stage-1-symbolic-lift` | keep | `oracle/compilation/compose-time-pipeline#stage-1` | sole normative statement |
| 22 | Stage 2 — symmetry quotient: block-diagonalization + IBZ orbit collapse, up to 48× in cubic | `4.2#2-stage-2-symmetry-quotient` | keep | `oracle/compilation/compose-time-pipeline#stage-2` | sole normative statement |
| 23 | Stage 2.5 — invariant synthesis: per-channel generator run, `GeneratorOutput` sidecar | `4.2#25-stage-25-invariant-synthesis` | keep | `oracle/compilation/compose-time-pipeline#stage-2-5` | sole statement of 2.5 *as a pipeline stage*; the generator itself is owned by `coupling-structure §3` |
| 24 | `InvariantTerm` is the constructive dual of an irrep-block decomposition — same machinery as §2, used to build rather than decompose | `4.2:114-116` | keep | `oracle/compilation/compose-time-pipeline#stage-2-5` | sole statement; explains why 2.5 is a sub-stage of 2 rather than a peer. **This is the argument that justifies the "2.5" name** — load-bearing for C1 |
| 25 | Stage 3 — the three rewrites (hash-consing, cross-formula CSE, tearing/alias elimination) | `4.2#3-stage-3-algebraic-simplification` | keep | `oracle/compilation/compose-time-pipeline#stage-3` | sole normative statement |
| 26 | Why only three; exactness is not a property of the rewrite *system* | `4.2:139-143` | keep | `oracle/compilation/compose-time-pipeline#rewrite-admission` | sole statement; the premise of the admission rule |
| 27 | **Rewrite-admission rule (normative), 3 conditions** | `4.2:145-154` | keep | `oracle/compilation/compose-time-pipeline#rewrite-admission` | the live guard that closed γ̂ question 1. Present-tense fact — keep per brief exception 1 |
| 28 | Equality saturation stays an **offline** rewrite oracle | `4.2:156-158` | keep | `oracle/compilation/compose-time-pipeline#rewrite-admission` | restated `gamma-hat:139-141`, `4.4:235`; this is the owning page |
| 29 | Herbie/egglog evidence: unsound rules, 289 benchmarks, 104 wins vs **135** losses, soundness affordable not free | `4.2:160-173` | keep | `oracle/compilation/compose-time-pipeline#rewrite-admission` | the external evidence the rule rests on. Keep the numbers; they are the reason condition 2 is written as it is |
| 30 | "(This corpus quoted the 104 without the 135 until 2026-07-22, which turned an even trade into a win.)" | `4.2:173` | delete | — | history of a corrected error → Log-worthy L1. The *corrected* claim survives in row 29 |
| 31 | The e-graph community arrived at the same exact-identity/ε-alongside separation independently | `4.2:175-178` | move | `oracle/compilation/representation-substrate#identity-exact` | belongs beside the separation it corroborates. **Carries a retired coordinate `§20.4.1`** — re-anchor to `#identity-exact` (Contradictions C2) |
| 32 | `[traps] §50` — rewrite exact almost-everywhere is tolerable for a scorer, not for a flow | `4.2:180-184` | keep | `oracle/compilation/compose-time-pipeline#rewrite-admission` | a live hazard (brief exception 2); consumer-side under the scorer-only decision |
| 33 | Stage 3 preserves granularity — sharing upstream nodes does not collapse `ResidualLeaf` keys | `4.2:186-189` | keep | `oracle/compilation/compose-time-pipeline#stage-3` | sole statement of the interaction between CSE and granularity |
| 34 | Stage 4 — compression-plan selection; **each plan carries a per-plan error target**, rank chosen to meet it | `4.2:195-204` | keep | `oracle/compilation/compose-time-pipeline#stage-4` | the error-target refinement is original here and feeds `Quantity.combineTol` |
| 35 | Stage 4 — implicit-differentiation adjoint; one extra linear *system*, **not `O(1)` work**; Blondel et al. 2022 | `4.2:205-213` | keep | `oracle/compilation/compose-time-pipeline#stage-4` | the qualification is precise here and **dropped** in `4.4:370-373` (Contradictions C5) |
| 36 | Stage 4 — adjoint-tape materialization schedule; `revolve` optimal on a chain, **NP-complete on a DAG** (Naumann 2009) | `4.2:214-223` | keep | `oracle/compilation/compose-time-pipeline#stage-4` | resolution of γ̂ question 2, stated on the owning page |
| 37 | This lowering **owes no fidelity generator** — it marks the boundary of the ε rule (value, not cost) | `4.2:223-229` | keep | `oracle/compilation/compose-time-pipeline#stage-4` | negative-space guard; sole statement |
| 38 | "(This was previously carried as an open data-structure problem … in `gamma-hat §4`. It was misfiled …)" | `4.2:230-233` | delete | — | history of a reclassification → Log-worthy L3. The disposition itself survives in row 36 |
| 39 | Stage 4 — codegen: one entry, four typed exits | `4.2:234-238` | keep | `oracle/compilation/compose-time-pipeline#stage-4` | sole statement |
| 40 | Stage 4 sidecars are codegen inputs, erased after codegen | `4.2:240-242` | delete | — | duplication of `4.1:167-168` (row 10), which owns sidecar lifecycle |
| 41 | Stage 5 — runtime kernel application; `evaluate` signature with 4 outputs | `4.2#5-stage-5-runtime-kernel-application` | keep | `oracle/compilation/compose-time-pipeline#stage-5` | the raw kernel primitive; `pino-bridge §1` wraps it |
| 42 | The PINO sees the graph only through `ResidualKey` hashes; loss aggregation lives in `/informed-operator` | `4.2:262-264` | keep | `oracle/compilation/compose-time-pipeline#stage-5` | module-boundary statement; restated `4.4:498`, `cross-cutting-rules §3` |
| 43 | Compose-time/runtime boundary table (6 rows: stage · runs · cost · output) | `4.2#6-the-compose-time--runtime-boundary` | keep | `oracle/compilation/compose-time-pipeline#boundary` | **enumerates six stages under a "4+1" label** — the defect's clearest instance (C1) |
| 44 | Composition fingerprint keys a kernel cache; scalar env params are runtime inputs, not recompile triggers | `4.2:277-281` | keep | `oracle/compilation/compose-time-pipeline#boundary` | sole statement; restated `4.4:589-591` (row 88). **Depends on an undefined structural/scalar split of `Environment`** — Open questions `environment-record` |
| 45 | **Runtime cost is three-class, not one** — per-sample core / on-request spectral / per-composition reference | `4.2:283-294` | keep | `oracle/compilation/compose-time-pipeline#boundary` | corrects the single "µs–ms" figure in the row above it; sole statement |

### 4.3-representation-substrate.md → `oracle/compilation/representation-substrate`

| # | Fact or block | Current location | Disposition | Target | Evidence |
|---|---|---|---|---|---|
| 46 | `authority`, `content-hash: 2e06544bce5b` | `4.3:4-5` | delete | — | D3/§4 |
| 47 | `canonical-for` — 7 distinct topics | `4.3:6-13` | keep | `oracle/compilation/representation-substrate` frontmatter `owns:` | the richest non-vacuous ownership claim in the chapter; pattern to copy |
| 48 | Substrate is **a contract, not a single dynamic container** | `4.3#representation-substrate` (48-57) | keep | `oracle/compilation/representation-substrate#contract` | sole statement; prevents the "one container" misreading |
| 49 | Five substrate primitives with full type definitions | `4.3#1-substrate-primitives` | keep | `oracle/compilation/representation-substrate#primitives` | sole *definition*; `4.4:82-92` restates as a cost table (row 62) |
| 50 | `Address[D]` is domain-separated; SHA-256, truncation only in log output | `4.3:101-105` | keep | `oracle/compilation/representation-substrate#primitives` | sole statement |
| 51 | Four op-signatures (`PredicateOps`, `SymbolicTensorOps`, `EvidenceOps`, `GroupOps`) | `4.3#2-the-parametric-op-signature-family` | keep | `oracle/compilation/representation-substrate#op-signatures` | sole definition. Prose says "Four … (a fourth, `GroupOps`, is added below). Three cover …" — awkward but not a defect |
| 52 | Per-cluster representation table (C1–C7 + 4 named fibers) | `4.3#3-per-cluster-representation-table` | keep | `oracle/compilation/representation-substrate#clusters` | sole statement of cluster→backend mapping |
| 53 | The `InvariantTerm`/`FormulaApply` symbolic-form row is **a separate fiber, not C4** | `4.4:143` → target `4.3#clusters` | move | `oracle/compilation/representation-substrate#clusters` | 4.3's table lists this row **unlabelled**, between C4 and the next; only `4.4:143` says explicitly it is not C4. Original to 4.4 — the one place the ambiguity is resolved |
| 54 | Canonical serialization rule — 11 numbered rules | `4.3#4-canonical-serialization-rule` | keep | `oracle/compilation/representation-substrate#serialization` | sole definition; `4.4:99-110` restates all 11 (row 63) |
| 55 | **§4.1 Identity is exact; ε rides alongside** — normative, with the non-transitivity argument | `4.3#41-identity-is-exact-ε-rides-alongside` | keep | `oracle/compilation/representation-substrate#identity-exact` | the keystone. Cited by `gamma-hat:128`, `4.4:231`, `10.2:62`, `live/specs`. **Every one of those citations uses the retired `§20.4.2` form** (C2) |
| 56 | Four consequences if the quotient is lost (no representative / dedup dies / `O(1)` equality dies / union-find dies) | `4.3:205-215` | keep | `oracle/compilation/representation-substrate#identity-exact` | the proof body; sole statement |
| 57 | **Rejected alternatives** — quantized addressing, ball/interval addressing — "recorded so they are not re-proposed" | `4.3:221-230` | keep | `oracle/compilation/representation-substrate#identity-exact` | **Not scaffolding.** Rejected-alternative records are forward-looking guards, not history of the corpus. Also → Log-worthy L4 |
| 58 | **§4.2 Estimate, don't decide** — any operation that can differ from exact emits an a-posteriori estimate | `4.3#42-the-consequence-estimate-dont-decide` | keep | `oracle/compilation/representation-substrate#estimate-dont-decide` | the ε obligation; registration duty lives at `residual-machinery §4.1` |
| 59 | "The gate gets **weaker**, not stronger" — from "must be exact" to "must estimate its own error" | `4.3:253-256` | keep | `oracle/compilation/representation-substrate#estimate-dont-decide` | present-tense statement of the rule's scope. The phrase "The prior rule was…" is a comparison, not a changelog — keep, but rewrite to drop "prior" |
| 60 | Every estimator so far is a byproduct of work already done (truncated SVD gives `σ_{k+1}`; inner solve gives its residual) | `4.3:257-261` | keep | `oracle/compilation/representation-substrate#estimate-dont-decide` | the affordability argument; sole statement |
| 61 | Hot-path commitments + 13-row complexity table + `SparseSet` backend ladder | `4.3#5-hot-path-commitments` | keep | `oracle/compilation/representation-substrate#hot-paths` | sole definition; `4.4:158-167` restates the table (row 64) and `4.4:119-123` the ladder (row 65) |
| 62 | What the substrate replaces (4 items) | `4.3#6-what-the-substrate-replaces` | keep | `oracle/compilation/representation-substrate#replaces` | keep items 1–3; item 4 (`PhysicsGraph` identity, `4.3:311-316`) duplicates `4.1:85-88` → delete that bullet, cross-reference instead (row 5) |
| 63 | What the substrate does **not** replace (3 items) | `4.3#7-what-the-substrate-does-not-replace` | keep | `oracle/compilation/representation-substrate#not-replaced` | negative-space; sole statement. High value |
| 64 | §8 Relationship to existing files — 11 bullets restating what each other page owns | `4.3#8-relationship-to-existing-files` | delete | — | **duplication engine in miniature.** Every bullet restates the other page's own claim; the header sentence ("each retains its canonical authority over its own concept") admits it. Replaced by the emitted topic→page map in `index/corpus.json` (§6) |
| 65 | §8's one non-restating claim: `IrrepLabel` identity is `(Address[GroupAtlas-context], local-irrep-name)` | `4.3:347-348` | move | `oracle/registry/canonical-vocabularies#irrep-label` | this is a *definition*, not a pointer, and it is the only one in §8. Would be lost if §8 is deleted wholesale |
| 66 | §9 Versioning discipline — `schema_version` bump, old addresses stay valid, migration is an explicit morphism; ROBDD atom-order versioning | `4.3#9-versioning-discipline` | keep | `oracle/compilation/representation-substrate#versioning` | forward-looking discipline, not history. Sole statement |

### 4.4-computational-overview.md → dissolved

**Verdict: dissolve the page.** Of ~40 claim-blocks, 27 restate a page that
already owns the topic, 11 are original, and 2 are self-referential scaffolding.
The 11 originals have identifiable topic owners elsewhere. See Notes for Phase 2
→ *The 4.4 recommendation*, which conflicts with plan §3 and needs Javier's call.

| # | Fact or block | Current location | Disposition | Target | Evidence |
|---|---|---|---|---|---|
| 67 | `title: `/physics` — Computational Architecture Reference` — **unquoted backtick, invalid YAML** | `4.4:3` | delete | — | `yaml.safe_load` raises `ScannerError`; verified against all four scope pages, this is the only failure. §4 requires quoted titles |
| 68 | `authority`, `content-hash: 4823d9f76406` | `4.4:4-5` | delete | — | D3/§4 |
| 69 | `canonical-for: - computational-overview` — **vacuous** | `4.4:6-7` | delete | — | owns only its own id; the duplicate-topic invariant cannot fire on it. This page is a 6,475-word restatement engine sitting *outside* the anti-duplication machinery — the two facts are causally linked |
| 70 | Header blockquote: computational lens, physics meaning suppressed, depth target | `4.4:33-41` | mine | `practice/conventions#computational-lens` | the *lens* is a reusable reading convention worth keeping; the page-scoped framing is not |
| 71 | "This is a hand-written companion to the canon chapters (1–10)" | `4.4:38` | delete | — | **the duplication charter.** A page whose stated job is to restate ten chapters is a duplication engine by construction |
| 72 | "Every claim cites its canonical `arch-xx` / `impl-xx` source" | `4.4:39` | delete | — | retired id formats (plan §2 defect 7 class) **and** false of the page: zero of its 99 citations use either format. Same defect as `10.3-audit-prompt:96` |
| 73 | `/physics` is a compiler emitting a numerical kernel; compose-time vs runtime split | `4.4#1-what-kind-of-program-this-is` | delete | — | restates `4.2:50-60` + `architectural-principles` p1 |
| 74 | Compose-time is "branchy, allocation-heavy, pointer-chasing; latency- and correctness-bound, not throughput-bound" | `4.4:53-54` | move | `oracle/compilation/compose-time-pipeline#boundary` | **original** — no other page characterizes the compiler's own performance profile. One sentence, worth relocating |
| 75 | Four-role polyglot split settled; languages open; compiler emits runtime-host source, JIT once, flat arrays only | `4.4:61-66` | delete | — | restates `open-decisions` + `deriv-language-study`. The decision belongs in `program/build` per the language surveyor's scope |
| 76 | Central design lever: expensive structure discovery once at compile time | `4.4:68-72` | delete | — | restates `4.2:50-54` (the always-cheap discipline) |
| 77 | §2 heading + intro ("every load-bearing object is a *fiber* over five primitives; the storage and identity contract for the whole system") and the §2.1 primitives cost table | `4.4#2-the-memory-model` (75-92), `4.4#21-primitives` | delete | — | restates `4.3:48-53` and `4.3#1` |
| 78 | Cost annotations original to §2.1: "≈ two SIMD loads"; "ordinal doubles as a dense array index" | `4.4:84-85` | move | `oracle/compilation/representation-substrate#hot-paths` | **original** constant-factor detail absent from `4.3 §5` |
| 79 | §2.2 the 11 serialization rules, restated | `4.4#22-content-addressing-and-the-canonical-serializer` | delete | — | restates `4.3#4` rule for rule |
| 80 | "The serializer's *injectivity* is the single highest-consequence invariant in the system" | `4.4:96-98` | delete | — | also at `4.3:226-227` and `11.9:86`; `4.3` owns it |
| 81 | Numerical note: float normalization is what makes content-addressing safe for computed values; length-prefixing prevents concatenation ambiguity | `4.4:112-115` | move | `oracle/compilation/representation-substrate#serialization` | **original** — states *why* rules 10/11 exist. `4.3:170-181` gives the rules without the rationale |
| 82 | §2.3 `SparseSet` backend ladder | `4.4#23-backend-selection-decision-procedures` | delete | — | restates `4.3:286-294` |
| 83 | HAMT depth bound `≤ ⌈log₃₂ \|keys\|⌉` (≤ ~6 for ≤ 10⁹ keys); insert path-copies one root-to-leaf path | `4.4:125` | move | `oracle/compilation/representation-substrate#hot-paths` | **original** — `4.3` gives `O(log₃₂ n)` without the concrete depth bound |
| 84 | §2.4 op-signatures + per-cluster table | `4.4#24-op-signatures-and-the-per-cluster-storage-table` | delete | — | restates `4.3#2`–`#3`. Exception: the not-C4 row → row 53 |
| 85 | §2.5 hot-path complexity table | `4.4#25-hot-path-complexity-commitments` | delete | — | restates `4.3#5` |
| 86 | §3 three-tier state (micro/slow/macro), moment closure, homogenization | `4.4#3-the-data-operated-on--state-and-γ̂` (173-182) | delete | — | restates `multiscale-state` |
| 87 | **§3.1 the 7-tuple table — container, layout, byte counts per slot** | `4.4#31-the-7-tuple` | move | `oracle/state/unified-state#slot-layouts` | **This is the table the brief says was never written.** Seam requirement R1 cites `unified-state` for "per-slot array shapes and layouts"; `unified-state` has no such table and `4.4:186-192` is the only place one exists. Deleting 4.4 wholesale would realize the dangling promise permanently |
| 88 | Slots are a closed `StateComponent` universe; downstream code indexes by dense ordinal, not symbol | `4.4:194-195` | move | `oracle/state/unified-state#slot-layouts` | **original** — the "not by symbol" access rule is stated nowhere else |
| 89 | §3.2 γ̂ 5×4 encoding grid + slot-selection decision procedure | `4.4:199-209` | delete | — | restates `gamma-hat §1` (`2.3:41-63`), including the five first-class pairs |
| 90 | γ̂ MVP budget: `N_PW≈1000`, `N_b≈40`, `N_k≈29`, ≈18 MB vs ≈460 MB dense, TB warm start, supercell scaling | `4.4:211-220` | delete | — | restates `gamma-budget` (`2.6:16-33`) number for number |
| 91 | "k-blocks are mutually independent → embarrassingly parallel and independently addressable"; read costs set by `N_b`, not `N_PW²` | `4.4:216-217`, `4.4:225` | move | `oracle/state/gamma-budget#parallelism` | **original** — the parallelism consequence of the block encoding appears nowhere else |
| 92 | γ̂ read/write asymmetry | `4.4:222-227` | delete | — | restates `gamma-hat §2` (`2.3:65-106`) |
| 93 | §3.2 the four γ̂ questions, "resolved not open", with dispositions | `4.4:229-247` | delete | — | restates `gamma-hat §4` (`2.3:123-177`), which the passage itself names canonical. See Open questions note on resolution siting |
| 94 | §3.3 L1–L4 dependency order; level derived from transitive inputs, not stored | `4.4:249-254` | delete | — | restates `born-oppenheimer-levels` + `4.1:179` |
| 95 | §3.3 the "Operations (computational)" column — per-level algorithm classes | `4.4:256-260` | move | `oracle/state/born-oppenheimer-levels#computational-operations` | **original** — maps each BO level to its numerical-kernel class; no other page does |
| 96 | Order-of-operations: an L2 force evaluation contains a converged L1 inner solve; the adjoint must thread implicit-diff through both | `4.4:262-265` | move | `oracle/compilation/compose-time-pipeline#stage-4` | **original** and load-bearing for adjoint synthesis — the nesting constraint is stated nowhere else |
| 97 | §3.4 dressing tiers Layer 1 / 1.25 / 1.75; MVP runs entirely at 1.25; 1.75 V2-deferred | `4.4#34-dressing-tiers` | delete | — | restates `born-oppenheimer-levels §1`. Carries the `Layer-1.75` nomenclature defect (§4) — propose only |
| 98 | §4 `PhysicsGraph` node/kind/role definitions and sidecar list | `4.4#4-the-ir--the-physicsgraph` (282-304) | delete | — | restates `4.1#1`–`#4` |
| 99 | **Arena / index DAG** — nodes in a flat array, `NodeId` = integer handle not pointer; gives cache locality, trivial serialization, natural hash-cons table | `4.4:277-280` | move | `oracle/compilation/physics-graph#representation` | **original and sole statement.** `10.3-audit-prompt:106` asks whether "the arena/index DAG" is the right choice — an auditor question pointing at a structure only this page defines. Delete 4.4 without moving this and the audit prompt dangles |
| 100 | **`CompressionPlan` Stage-4 selection procedure** — the five choose-by rules (small/dense → Dense; inferred sparsity → Sparse; `r ≪ n` → LowRank; hierarchical off-diagonal → HODLR; high-dim tensor → TT) | `4.4:306-312` | move | `oracle/compilation/compose-time-pipeline#stage-4` | **original.** `4.2:195-204` names the plans and the error target but gives **no selection criteria**; `4.1:158-165` gives only the type. This is the actual decision procedure |
| 101 | §5 pipeline summary table (in→out, algorithm class, cost) | `4.4#5-the-core-algorithm--the-41-pipeline` (321-328) | delete | — | restates `4.2#6`. **Second instance of the "4+1" label over six enumerated stages** (C1) |
| 102 | The "Algorithm class" column — macro expansion+BDD / finite-group rep theory / Reynolds / e-graph / numerical-LA planning | `4.4:323-328` | move | `oracle/compilation/compose-time-pipeline#boundary` | **original** — names the mathematical class of each stage; `4.2#6` gives cost and output only |
| 103 | §5 Stage 1 / Stage 2 / Stage 2.5 / Stage 3 narrative restatements | `4.4:330-364` | delete | — | restates `4.2#1`–`#3` and `coupling-structure §3` (verified: pre-prune, emptiness test, `\|G\|≤192`, `dim(T)≤250`, ≤12M ops all at `3.3:172-194`) |
| 104 | Schur numerical note: cubic blocks are *small* (`d_λ ∈ {1,2,3}`, 4 under SOC) → favors cache-resident dense solve per block, **poor fit for wide-SIMD/GPU unless batched** | `4.4:340-342` | move | `oracle/compilation/compose-time-pipeline#stage-2` | **original** — the only hardware-mapping consequence stated anywhere in the corpus |
| 105 | §5 Stage 4 restatement: compression selection, implicit-diff adjoint, τ_adj gate, codegen | `4.4:366-381` | delete | — | restates `4.2#4` + `residual-machinery §5`. Drops `4.2`'s "not `O(1)` work" qualification (C5) |
| 106 | Conditioning of the adjoint solve is set by the fixed-point map's Jacobian; near-singular Jacobians (slow self-consistency) are **the failure mode** | `4.4:372-373` | move | `oracle/compilation/compose-time-pipeline#stage-4` | **original** — names the failure mode; `4.2` and `residual-machinery §5` give the `τ_cond` guard without saying what it guards against |
| 107 | §5 Stage 5 restatement | `4.4:383-385` | delete | — | restates `4.2#5` |
| 108 | **§6.1 the 12-method table — kernel, complexity, selection criteria, numerical stability** | `4.4#61-the-12-methods--numerical-kernels` | move | `oracle/registry/computational-methods#kernels` | **original, and the largest single block of unowned engineering content in my scope.** `computational-methods` is 309 words and owns only "method signatures"; it carries no complexity, no solver-selection rule, no stability note. Verified absent corpus-wide: `reorthogonalization`, `robust orientation predicates`, `Scharfetter–Gummel` (only in `multiscale-state`), broadening-η regularization, symplectic-for-MD, cell-lists vs `O(N²)`, near-degeneracy block solvers |
| 109 | §6.2 the 10 representative template signatures | `4.4#62-the-20-templates--graph-construction-macros` | delete | — | restates `property-templates` (verified: `6.5:16-40` carries typed signatures) |
| 110 | The "Composes" column (template → method) and the `HarmonicStiffnessHessianOf` annotation "+ symmetrization + acoustic-sum-rule enforcement" | `4.4:414-425` | move | `oracle/registry/property-templates#composition` | **original** — the template→method map and the ASR-stabilization note are not in `property-templates` |
| 111 | §6.3 formula distribution by cost-tier and diff-tag | `4.4#63-the-132-formulas--distribution` | delete | — | restates `canonical-vocabularies §3` / `named-formulas`. Carries the `D0\|DN\|D1..D4` ordering defect (§4) — propose only |
| 112 | §6.4 the dynamics computationally (`L` antisymmetric, `M` PSD, EOM-violation residual) | `4.4#64-the-dynamics-computationally` | delete | — | restates `generic-dynamics` + `residual-definitions` |
| 113 | §7 symmetry & topology structures table | `4.4#7-symmetry--topology-data-structures` | delete | — | restates `canonical-vocabularies §5` + `topology-atlas` |
| 114 | §7 cost column (Schreier–Sims storage `O(\|G\|·d)`; character table `O(\|G\|·#irreps)`; SNF `O(m·n·log max)`; coset-tree lookup `O(log #orbits)`) and "computed offline in GAP, baked in as content-addressed tables" | `4.4:453-462` | move | `oracle/registry/topology-atlas#costs` | **original** — `topology-atlas` states SNF is polynomial but gives no storage/lookup costs and no offline-bake statement |
| 115 | §8 `Validate` signature, `ResidualKey`, granularity, gradients, `Import`, `RoaringCoverageMask` | `4.4#8-outputs-and-the-consumer-boundary` | delete | — | restates `pino-bridge §1`–`§2.1` + `residual-definitions §4` |
| 116 | §9 ten cert obligations table | `4.4#9-verification--cert` | delete | — | restates `cert-obligations` |
| 117 | §9 the **per-obligation complexity column** | `4.4:509-520` | move | `oracle/certification/cert-obligations#costs` | **original** — `cert-obligations:110` gives one blanket "`O(1)`–`O(block)` per invariant" line; the per-obligation costs exist only here |
| 118 | §9 `SqliteReferenceCache` operational detail: write-once, deletes disallowed, WAL mode, `n ≈ 10–10⁴` | `4.4:522-527` | move | `oracle/certification/cert-obligations#reference-cache` | **original** operational spec beyond `cert-obligations §2` |
| 119 | §10 coupling-structure summary | `4.4#10-coupling-structure` | delete | — | restates `coupling-structure §1`, `§3`, `§10`, `§11` |
| 120 | §11 the math-to-location map (14 rows: branch of math → where it enters) | `4.4#11-the-math-to-location-map` | mine | `index/corpus.json` (emitted) — see Notes | **original**, and genuinely useful, but it is a *navigation index in prose* — exactly what §6 says must be emitted rather than hand-written. Hand-maintaining it is how it rots |
| 121 | §12 the `core ← shared ← …` import chain is **retired**; "must not be cited to architectural-principles" | `4.4:576-581` | delete | — | scaffolding about scaffolding. The same retirement is already stated at `architectural-principles:24` — which itself needs the history marker stripped (that page is another surveyor's scope) |
| 122 | What survives of the build discipline: freeze a layer's typed interfaces, then implement upper-layer leaves independently | `4.4:580-581` | move | `program/build/build-sequence#discipline` | the one present-tense fact inside row 121 |
| 123 | §12 polyglot seam + **differential golden test (emitted kernel vs tree-walking interpreter of the same IR)** | `4.4:582-588` | move | `program/build/build-verification#golden-test` | the seam restates `open-decisions`, but the golden test is **original** and is a concrete verification obligation |
| 124 | §12 composition fingerprint / kernel cache | `4.4:589-591` | delete | — | restates `4.2:277-281` (row 44) |
| 125 | §13 "internally coherent (58 pages, both checkers clean)" and "was deep-audited" | `4.4:597-599` | delete | — | a page certifying the corpus by page-count and a green run. Calibration (Notes) shows the green run does not license the claim. Self-referential |
| 126 | §13 formalization gaps: 52-observable (+16 FoM) catalog, crystal-structure-validity residual catalog, rest of `B9` | `4.4:604-613` | move | Open questions → owning pages | genuinely open enumeration work; see Open questions `observable-catalog`, `b9-bundle`, `csp-residual-catalog` |
| 127 | §13 the landed/deferred parenthetical (rows 113–119, 120–127, sub-method, V2 deferrals, "remediated through P2") | `4.4:606-613` | delete | — | history of what landed when → Log-worthy L6. The V2 deferral *list* survives as open questions (row 126) |
| 128 | §13 "The data-structure layer — no longer open … Kept here rather than deleted because an entry that silently disappears reads as though it was never a problem" | `4.4:614-618` | delete | — | **the corpus explicitly arguing for retaining scaffolding.** D1/D2 answer it: the entry goes to `log/timeline.md`, which is exactly the "does not silently disappear" mechanism the passage is reaching for |
| 129 | §13 **no post-registration adjoint-drift monitoring** — registration validates the formula's adjoint; Stage 4 synthesizes the composition's adjoint over a rewritten graph; nothing revalidates | `4.4:619-623` | move | Open questions → `oracle/compilation/compose-time-pipeline#stage-4` | live open question; currently stated only here and at `10.2:99-105`, neither of which is the owning page. This is plan §5's worked example, confirmed real |
| 130 | §13 deferred engineering decisions: surrogate-net build-vs-adopt, PDE-mesh adjoint scheme, Layer-1.75 onramp, language pick | `4.4:624-629` | move | Open questions → owning pages | four live open items |
| 131 | "(The integrator-interface signature closed 2026-07-16 …)" | `4.4:627-629` | delete | — | closure framing → Log-worthy L5; the *resolution* (per-tier tangent + steppable-form manifest, integrator consumer-side) is a present-tense fact that currently has **no owning page** — Open questions `steppable-form-manifest` |
| 132 | §13 closing: treat §§1–12 as claims that *should be valid*, §13 as *intentionally open* | `4.4:631-632` | delete | — | a page declaring its own claims merely "should be" valid is the companion problem restated. Under D7 open items live on owning pages, so the distinction has no carrier |

---

---

### from `oracle-cert-accuracy`

| # | Fact or block | Current location | Disposition | Target | Evidence |
|---|---|---|---|---|---|
| 1 | Frontmatter `authority: canon` · `content-hash` · `tag` | all six pages, e.g. `5.1-cert-obligations.md:4-5` | delete | — | D1/§4: the two-tier `authority` system dies with ch. 11; git answers "did this change". Six identical instances |
| 2 | `canonical-for: [accuracy-ledger]` and `canonical-for: [reference-battery]` — topic identical to the page id | `9.1-accuracy-ledger.md:6-7`, `9.2-reference-battery.md:6-7` | delete | replaced by real `owns:` at `oracle/accuracy/accuracy-ledger` and `oracle/accuracy/reference-battery` | Vacuous ownership (defect 4). Both are on the plan's list of 18. Calibrated: planting a vacuous `canonical-for` on 5.1 left the checker green, topic count silently 98→97. Proposed real topics below in Notes |
| 3 | `title: \`reference-data/\` — Machine-Readable Cert Reference Battery` — unquoted backtick | `9.2-reference-battery.md:3` | delete | retitled, quoted, at `oracle/accuracy/reference-battery` | Confirmed: `yaml.safe_load` raises `ScannerError: found character '\`' that cannot start any token` at line 3. Both checkers stay green (defect 5) |
| 4 | Cert is a first-class deliverable: schema · deterministic text renderer · freeze fixture + tamper tripwire · high-precision oracle cross-check | `5.1-cert-obligations.md:45-48` | keep | `oracle/certification/cert-obligations#certificate-artifact` | Sole full statement. 5.4:33-36 restates it minus the renderer, plus "schema is the cross-workstream contract" and "(non-load-bearing)" — merge those two clauses in (see row 21) |
| 5 | The certificate is an inert s-expression carrying scalar verdicts plus numeric witnesses for failures | `5.1-cert-obligations.md:46-48` | keep | `oracle/certification/cert-obligations#certificate-artifact` | Stated twice, near-verbatim: also `5.4-cert-obligations-detail.md:33-34` |
| 6 | **The ten cert obligations** (numbered list) | `5.1-cert-obligations.md:50-67` | keep | `oracle/certification/cert-obligations#the-ten-obligations` | The canonical list. **Stated three times corpus-wide** with three different third columns — here, `5.4:20-31` (Layer-0 axis), `4.4-computational-overview.md:509-520` (computational check + complexity). All three must be rewritten as one table or any retag hits one and misses two. This is the mechanism that produced the obligation-9 defect |
| 7 | Obligation 9 — "Surrogate-net validity (for `D4` surrogate formulas)" | `5.1-cert-obligations.md:65` | move | `oracle/certification/cert-obligations#the-ten-obligations`, carrying open question `obligation-9-scope` | **Confirmed scope defect, logged not resolved.** `named-formulas.md:105` defines `D4` = *relaxed* (argmin/hull/sort/discrete). All six `D4` registry rows are relaxations — soft-hull log-sum-exp (45, 67), softmin (46), soft-cutoff (50), Gumbel-Softmax (84), min-over-set (85) — no learned net among them. The one learned surrogate, `quasi-particle-shift-G0W0-surrogate` (row 6), is `D2`. See Open questions |
| 8 | Obligation 9's checker body — declared input domain contains the query · surrogate uncertainty below tolerance · refresh up to date | `5.4-cert-obligations-detail.md:30` | move | `oracle/certification/cert-obligations#the-ten-obligations` | Same defect, other half. This body describes a learned net; it is the evidence that the obligation was written for surrogates, not relaxations |
| 9 | "Each obligation maps onto a Layer-0 axis (§10)" | `5.1-cert-obligations.md:69-70` | move | `oracle/certification/cert-obligations#axis-mapping` | **Dangling promise.** `§10` names no page and 5.1 has only §1.1–§1.3 and §2. `typeclass-alphabet` — the Layer-0 alphabet page — has exactly one heading and no numbered sections, so it cannot be the referent either. Calibrated: a *bare* `§9999` passes both checkers clean, while `\`residual-machinery §9999\`` fires. The content the promise wants is 5.4's table (row 21) |
| 10 | Cert evidence is one `MerkleDAG[EvidenceOps, EvidencePayload]` per composition; obligation leaves are `EvidenceOps.attestation` nodes; aggregation is the semilattice meet; root `Address` is the artifact `/informed-operator` consumes | `5.1-cert-obligations.md:72-81` | keep | `oracle/certification/cert-obligations#evidence-aggregation` | Sole statement of the aggregation semantics (`Failed`/`Pending`/`Passed`). `4.4:505-507` restates the meet and early-exit; that page is the complexity view |
| 11 | §1.1 Coupling-derived simplifications: obligations 1, 2, 5 collapse to projection-residual checks for invariant-generator formulas; obligation 2 uses the **congruence-action Reynolds operator** on the assembled per-mechanism dissipative super-block, not per off-diagonal kernel; `O(1)`–`O(block)`, runs at Stage 2.5 | `5.1-cert-obligations.md:83-111` | keep | `oracle/certification/cert-obligations#coupling-derived-checks` | Sole statement. `coupling-structure §12` holds the PSD *existence* theorem; this is its runtime guard — the split is stated and clean |
| 12 | §1.2 Tolerance ledger — the 17-row table of canonical tolerance names and defaults | `5.1-cert-obligations.md:134-152` | keep | `oracle/certification/cert-obligations#tolerance-ledger` | Sole canonical source; `check_data_agreement.py:179-183` harvests `TOL_SYMBOLS` from this exact table by regex on the `## [\d.]+ Tolerance ledger` heading. **Ordering hazard: renaming that heading silently empties the symbol list** — the tool has a guard (line 184) but the guard is the only thing between a rename and 17 unchecked tolerances |
| 13 | `ε` is reserved for permittivity; **`τ` is not a reserved tolerance prefix** — `τ_n`, `τ_p`, `τ_PO`, `τ_E`, `τ_hop`, `τ_iv`, `τ_alloy` are physical times, 48 uses across six pages; a `τ_x` is a tolerance only if it is in the table | `5.1-cert-obligations.md:115-129` | move | `oracle/practice/conventions#symbol-namespaces` + a pointer from `oracle/certification/cert-obligations#tolerance-ledger` | Live naming rule, not history. **But it is currently written as history** — "this page used to claim it was", "the overclaim mattered" — so the *rule* keeps and the *story of the retracted claim* deletes. `[traps] §69` already carries the hazard |
| 14 | "`check_data_agreement.py` check (c) compares a **hard-coded list** of tolerance symbols" | `5.1-cert-obligations.md:156-158` | delete | — | **Stale description of the tool.** The tool was fixed: `check_data_agreement.py:173-183` reads "The symbol list is HARVESTED from the page that owns it, not restated here. It used to be nine symbols hard-coded in this file against seventeen in `cert-obligations §1.2`". The page describes the pre-fix mechanism. The *conclusion* it draws ("a tolerance introduced without a row here is invisible to it") is still true and should keep — see row 15 |
| 15 | Exhaustiveness of the tolerance ledger is a **review rule, not a machine result**; when you add a tolerance, add it here and to the checker's list | `5.1-cert-obligations.md:154-161` | keep | `oracle/certification/cert-obligations#tolerance-ledger` | Live rule with a named reason. Independently confirmed by the checker's own 20-line comment at `check_data_agreement.py:203-220` explaining why the check cannot exist. Drop the second clause ("add it to that list") — harvesting made it obsolete |
| 16 | §1.3 The four composition-validity refusals: unprovenanced-coefficient · AHC `slope-kind` double-count · learned-correction-without-anchor · polarization-convention pairing. Each a `Failed` verdict with a named witness | `5.1-cert-obligations.md:163-191` | keep | `oracle/certification/cert-obligations#composition-refusals` | Sole statement of all four. Individually echoed from `coupling-structure §8`, `accuracy-ledger` #35, `out-of-scope`, `reference-battery:50` — those are pointers, this is the definition |
| 17 | Improper `e₃₁ ≈ 3.4× proper`; Dreyer PRX 6 021038 (2016) accidental cancellation; ±5%-ΔP target scoped `is-AlGaN-GaN` | `5.1-cert-obligations.md:186-191` | move | `oracle/accuracy/accuracy-ledger#polarization-coefficients` (values) + pointer retained at `#composition-refusals` | Value-bearing prose on a cert page. `9.1:91` (#35) and `9.1:219-220` state the same physics with the numbers and the sources; `polarization-piezoelectric.csv` is canonical. Cite, don't restate |
| 18 | §2 `SqliteReferenceCache` — process-local SQLite at `physics/library/cert/reference-data/cache.sqlite`, WAL mode, the `entries` table DDL, key construction, schema versioning, write-once discipline, why-SQLite rationale | `5.1-cert-obligations.md:193-227` | keep | `oracle/certification/cert-obligations#reference-cache` | Sole statement, and the page's second real owned topic. **Contradicts 9.2** on what lives in that directory — see Contradictions |
| 19 | The cache and the EvidenceDAG are the only persistent components of `/physics`; everything else is recomputed from the graph | `5.1-cert-obligations.md:229-230` | keep | `oracle/certification/cert-obligations#reference-cache` | Sole statement of a load-bearing architectural fact |
| 20 | 5.4 opening: "The ten obligations are listed in `cert-obligations`. Each maps onto a Layer-0 axis, so its checker is a generic function over a typeclass" | `5.4-cert-obligations-detail.md:17-18` | delete | — | Pure connective tissue for a page that should not exist. The claim itself is row 9 |
| 21 | **The obligation → Layer-0 axis mapping table** (10 rows: `Sampleable`×group action, `Quantity` ordering, `HasAnalyticStructure`, content-side CSV read, `Integrable`, `Sampleable`+`approxEq`+degeneracy tripwire, `DiscreteStructure` morphism, versioning discipline, D4 domain check, `residual-machinery §5` gate) | `5.4-cert-obligations-detail.md:20-31` | move | `oracle/certification/cert-obligations#axis-mapping` | **The split is not real — merge and delete the page.** Detail: see Notes for Phase 2. This table is 5.4's only unique content and it is exactly what 5.1's dangling `(§10)` promises |
| 22 | 5.4 closing paragraph — certificate is an s-expression; schema is the cross-workstream contract; freeze fixture, tamper tripwire, high-precision oracle cross-check (non-load-bearing) | `5.4-cert-obligations-detail.md:33-36` | move | `oracle/certification/cert-obligations#certificate-artifact` | Duplicates row 4/5 with two clauses 5.1 lacks ("cross-workstream contract", "non-load-bearing") and missing one 5.1 has ("deterministic text renderer"). Classic duplication drift: neither copy is complete |
| 23 | Every property, observable and residual carries `applicability : (Crystal, Environment) → Bool`; the PINO loss masks non-applicable properties per-sample; this is what makes the architecture compositional across crystal types | `5.2-applicability-classifiers.md:30-41` | keep | `oracle/certification/applicability-classifiers#the-predicate-contract` | Sole statement. Note the load-bearing use of the unowned `Environment` type — see Open questions |
| 24 | **The two-predicate polar split** — `is-polar-material` (Born charges / LO–TO, gates Fröhlich) vs `is-noncentrosymmetric` (point group, gates the polarization package); they coincide on diamond and wurtzite III-N and **split on β-Ga₂O₃** | `5.2-applicability-classifiers.md:43-63` | keep | `oracle/certification/applicability-classifiers#polar-predicate-split` | Sole normative statement. `[traps]` and `coupling-structure` cite it. Present-tense throughout except the "(normative; 2026-07 gap-audit / Wave-2)" parenthetical, which deletes |
| 25 | Rows 116 and 119 are not in the polarization package; row 119 gates on `is-dielectric-layer`; row 116 needs a semiconductor-interface predicate that does not exist and carries an always-true stub | `5.2-applicability-classifiers.md:65-88` | keep | `oracle/certification/applicability-classifiers#row-116-and-119`, carrying open question `semiconductor-interface-predicate` | The *gates* are present-tense fact and keep. The framing — "(corrected 2026-07-22)", "The band was written `113–119` and gated whole", "It was also circular" — is scaffolding and deletes; the correction goes to the log (see Log-worthy). **This page is the owner of the open question**, per row 33 |
| 26 | §1 Applicability examples table — 12 rows of property → predicate → note | `5.2-applicability-classifiers.md:90-109` | keep | `oracle/certification/applicability-classifiers#example-predicates` | Sole collected statement of the standard predicate vocabulary. Delete the "Migrated from the research stratum, reconciled with…" provenance line (91-93) |
| 27 | V1 commitment: every registry entry gets an explicit `applicability` field; always-true stubs acceptable for V1.0. Deferred: soft `[0,1]` classifiers, classifier composition under perturbation | `5.2-applicability-classifiers.md:111-114` | keep | `oracle/certification/applicability-classifiers#v1-commitment` + two `open-questions` entries | The commitment is fact; the two deferrals are open questions and belong in frontmatter per D7, not in a trailing parenthetical |
| 28 | **Swept-`Environment` validity windows** — a validity window depending on a runtime-swept `Environment` scalar is re-evaluated per training sample in the PINO loss mask; each emitted kernel is tagged with the `Environment` box on which its Stage-2.5 structure is valid | `5.2-applicability-classifiers.md:116-128` | keep | `oracle/certification/applicability-classifiers#swept-environment-windows` | Sole statement, and the strongest single consumer of the unowned `Environment` record — it demands an `Environment` *box* (scalar ranges), which nothing defines. Delete the closing sentence "This is the resolution of the former 'current-vs-initial-state evaluation' question" (127-128); the resolution keeps, the story of the question deletes |
| 29 | `CouplingChannel.applicability` uses the same predicate contract; a channel returning false is skipped at Stage 2.5 and contributes no invariants | `5.2-applicability-classifiers.md:130-133` | keep | `oracle/certification/applicability-classifiers#the-predicate-contract` | Sole statement of the channel-side binding |
| 30 | Storage shape: predicate = `MerkleDAG[PredicateOps, C1Atom]` ROBDD over C1 typeclass-tag atoms; atom order is part of the predicate-vocabulary version. **Cert obligation checkers are NOT ROBDDs** — they are typed registered morphisms through the C2 generator registry | `5.2-applicability-classifiers.md:135-147` | keep | `oracle/certification/applicability-classifiers#predicate-storage` | Sole statement, and the "the split is preserved" clause is a live anti-conflation guard, not history |
| 31 | The out-of-scope register — 20 bullets, from strongly-correlated systems through inverse design | `5.3-out-of-scope.md:24-120` | keep | `oracle/certification/out-of-scope#exclusions` | Sole collected register. Individual entries are echoed from `multiscale-state`, `[traps]`, `accuracy-ledger`; this is the roll-up and the enforcement site (`[traps] §22`, `§40` both name it) |
| 32 | Within row 31 — the AlN gap list (no measured impact-ionization, no bulk hole mobility, no >500 K single-crystal κ), the β-Ga₂O₃ hole-transport refusal, and the EDF-tail anchor absence | `5.3-out-of-scope.md:48-52, 69-84` | move | statement stays at `oracle/certification/out-of-scope#exclusions`; the **values and σ** cite `oracle/accuracy/accuracy-ledger#residue` | **Same gap list stated at three sites**: here, `9.1:269-275` ("Residue"), `9.2:81-83` ("Also pending"). Each carries a different subset. The out-of-scope page should carry the *stance* (cert-refused) and cite the ledger for what is missing |
| 33 | "V1 has no design-grade path, only per-material σ-widening in the ledger (`accuracy-ledger §14`)" | `5.3-out-of-scope.md:62-63` | move | `oracle/certification/out-of-scope#exclusions`, citation rewritten to `[accuracy-ledger#observable-regimes]` | **Ambiguous ordinal.** `accuracy-ledger` has six headings and no §14; `§14` means *table row 14* (thermal_expansion). `§N` is overloaded in this corpus — heading number on most pages, numbered-list item in `[traps] §16/§40/§51/§61/§69`, table row here. Same class at `9.1:70` (`[traps] §40`), `9.1:174` (`[traps] §16`), `9.2:78` (`[traps] §51`) |
| 34 | "`predict` raises `out-of-scope` with a witness for any of these; cert obligation-3 flags suspect cases" | `5.3-out-of-scope.md:122-123` | keep | `oracle/certification/out-of-scope#refusal-mechanism` | Sole statement of the runtime binding. Note it names obligation **3** (analytic limits) — worth a second look, but that is a contents question, not mine |
| 35 | Ledger intro: the per-observable accuracy regime is the **error-model seed**; each value is a declared characteristic scale that `Quantity.combineTol` composes; "it is a *reference table*, not the registry" | `9.1-accuracy-ledger.md:34-41` | keep | `oracle/accuracy/accuracy-ledger#what-this-is` | Sole statement of the ledger's role, and the sentence that keeps it from being mistaken for the registry. Delete "Restored from `[deriv-observable-catalog]` (the catalog's 'Accuracy regime' column, dropped in the original CSV transcription)" — provenance of a repair, and the referent dies with ch. 11 |
| 36 | "Design-grade" = **device-design-adequate at the UWBG operating point (>500 °C, high field)**, not ab-initio-converged | `9.1-accuracy-ledger.md:43-45` | keep | `oracle/accuracy/accuracy-ledger#design-grade` | Sole definition of a term used across `8.x`, `3.2`, `5.3`. Delete "(and tracked in the audit's P2 list)" — dangling: the audit is collapsed into the timeline |
| 37 | Two counts, two terms: **catalog observables** (52, `deriv-observable-catalog` Part C) vs **ledger-tracked observables** (59) | `9.1-accuracy-ledger.md:49-53` | keep | `oracle/accuracy/accuracy-ledger#observable-counts` | Live disambiguation; `glossary.md:76-77` carries both terms and points both at `accuracy-ledger`. **Hazard:** the glossary's "Catalog observables" entry defines itself by reference to `[deriv-observable-catalog]`, which ch. 11 deletion removes. The count 52 must be restated without its container |
| 38 | **The 52-row observable accuracy table** (#1–52: regime + cheap-vs-faithful per observable) | `9.1-accuracy-ledger.md:55-108` | keep | `oracle/accuracy/accuracy-ledger#observable-regimes` | Canonical, and machine-checked: `check_book_structure.py:366-376` verifies the headline count equals the table's max row id and that no row id is missing. **Preserve the `\| N \|` row-start format** — the check is a regex on `^\| (\d+) \|` |
| 39 | The 16 FoMs are algebraic compositions of the above and inherit their composed `combineTol` budget | `9.1-accuracy-ledger.md:110-111` | keep | `oracle/accuracy/accuracy-ledger#observable-counts` | Sole statement |
| 40 | MVP design-grade targets table (7 rows) + the **path-met status legend** ("the closed-form path exists **and** its diamond anchors are seeded, so cert 4/8 can check the target — **not** a claim that a cert run has passed") | `9.1-accuracy-ledger.md:113-134` | keep | `oracle/accuracy/accuracy-ledger#mvp-targets` | Canonical. The legend is a live anti-overclaim guard and keeps; "(The former bare 'met' read as target-met; 2026-07 gap-audit D0.)" is history and deletes. `3.2:308-312` restates the seven targets but explicitly cites `[accuracy-ledger]` as canonical and warns that restating is what let it drift — that is the pattern to copy, not to remove |
| 41 | Per-material curated coefficients preamble: each value is a `ProvenanceLedger` entry `(value, σ, source, cost-class)`; diamond is `curated`, other materials `per-material-DFPT`; **per-value citations are the `source` column of the reference CSVs, which is canonical for them** | `9.1-accuracy-ledger.md:136-146` | keep | `oracle/accuracy/accuracy-ledger#curated-coefficients` | The single most important routing sentence in my scope: it names the CSVs, not this page, as canonical for citations. Keep the clause "`[timeline] §2026-06-10 (Pass C)` records what the pass decided, not the citations" as a rule; delete its "— it says so itself, and pointing at it for sources sent readers to a page that disclaims having them" tail |
| 42 | AHC ZPR / gap T-slope table (6 materials × isochoric/lat/total ZPR, dE_g/dT, slope-kind, source) + the `isochoric`-vs-`total` composition rule | `9.1-accuracy-ledger.md:148-163` | keep | `oracle/accuracy/accuracy-ledger#ahc-zpr` | Canonical seed. **Duplicated at `coupling-structure.md:360`** ("GaN −189, AlN −399, diamond −345 meV" with the same Engel/Miglio sources) — that site should cite, not restate. Delete "the prior `total` tag on isochoric magnitudes was the worst of both" (161-163 tail); keep the rule "the tag must describe what the value contains" |
| 43 | κ(T) battery table (4 materials × 300/773/1100 K + source) | `9.1-accuracy-ledger.md:165-172` | keep | `oracle/accuracy/accuracy-ledger#kappa-battery` | Canonical. **Restated at three sites outside the ledger**: `8.1-mvp-system.md:25` ("~2200 W·m⁻¹K⁻¹ (300 K; battery anchor, exp 2000–2500)"), `8.4-forced-decisions.md:40` ("κ(773 K) ≈ 620 W/m·K"), `11.8:505` ("620 … the curated battery anchor"). Calibrated: changing 2200→9999 here leaves **both checkers green** — nothing compares page values to the CSVs |
| 44 | **Monoclinic frame guard** — four distinct β-Ga₂O₃ axis systems; Klimm's plane-normal `λ₍₁₀₀₎`/`λ₍₀₀₁₎` lie along `a*`/`c*` and must never be relabelled; the elastic frame's first axis is the real `a`, 13.83° off `a*` | `9.1-accuracy-ledger.md:174-181` | keep | `oracle/accuracy/accuracy-ledger#monoclinic-frames`, with `practice/traps` reduced to a pointer | Near-verbatim duplicate of `10.4-traps.md:141-149` (trap 16), which itself ends "— enforced, [accuracy-ledger]". The trap survives as a hazard (brief exception 2) but should not carry the full text twice. **Note the sentence is broken**: "The other three / and must never be interchanged" — a clause was lost between lines 174 and 175 |
| 45 | Chynoweth + Caughey–Thomas + `κ_BR` table (5 materials) | `9.1-accuracy-ledger.md:183-191` | keep | `oracle/accuracy/accuracy-ledger#high-field-coefficients` | Canonical. **Duplicated in `11.5-deriv-high-field.md:49-54, 122-123`** — an appendix, where the plan warns values may be superseded. Seed from here and the CSVs, never from 11.5 |
| 46 | Wave-1 III-N seed preamble — "the seeding spec and the adversarial audit that refuted it were two separate artifacts; both collapsed into `[timeline] §2026-06-10 (Wave 1)`… the full tables are in git history" | `9.1-accuracy-ledger.md:193-197` | delete | — | Container history: which artifacts were merged into which log entry. The one live clause — per-value sources live in the CSVs' `source` column — is already row 41 |
| 47 | Electronic baseline + deformation potentials table (GaN/AlN, 7 quantities) | `9.1-accuracy-ledger.md:199-209` | keep | `oracle/accuracy/accuracy-ledger#iii-n-electronic` | Canonical seed. `11.5:122-123` restates ε_0/ε_∞/m* — appendix, mined and deleted |
| 48 | Polarization / piezo / Born table (ZB-reference `P_sp` + **PROPER** e₃₁), including the "e₃₁ improper — do NOT pair with ZB P_sp" reference row | `9.1-accuracy-ledger.md:211-222` | keep | `oracle/accuracy/accuracy-ledger#polarization-coefficients` | Canonical. The do-not-pair row is a live guard, not a remnant; `cert-obligations §1.3` is its enforcement |
| 49 | **AlGaN polarization bowing sign guard** — `P_sp(x) = xP_AlN + (1−x)P_GaN − b_P·x(1−x)` with `b_P` NEGATIVE, while the reference CSV states `+b·x(1−x)` with b positive; lifting the CSV's `+b` into this form flips the bowing | `9.1-accuracy-ledger.md:226-238` | keep | `oracle/accuracy/accuracy-ledger#polarization-bowing`, carrying open question `algan-bowing-b-unseeded` | A live convention trap between canon and its own data file. Keep the guard; the gate-G1 reading history ("gate G1's two texts read 2026-07-16: the FB Erratum leaves the P_sp bowing untouched") is log material, and the unseeded state is an open question |
| 50 | **Pyroelectric sign guard** — `p` is POSITIVE in the seeded ZB-reference frame because ZB-ref `P_sp` is negative and `\|P_sp\|` falls with T; raw literature quotes it negative under the opposite convention. Seeded values AlN +3.0e−6, GaN +4.5e−6 C/m²K, σ = ×2 | `9.1-accuracy-ledger.md:239-247` | keep | `oracle/accuracy/accuracy-ledger#polarization-coefficients` | Live guard. Note the page names `polarization-piezoelectric.csv` as canonical for the two values while stating them — correct pattern, keep it explicit |
| 51 | Elastic constants table (GaN/AlN/AlGaN), including "AlGaN is linear Vegard **except C₁₃, which is superlinear**" and the C₁₃-weak-link note | `9.1-accuracy-ledger.md:249-255` | keep | `oracle/accuracy/accuracy-ledger#elastic-coefficients` | Canonical. Delete the parenthetical "(the canon summary had dropped it, leaving canon describing plain Vegard while the CSV it summarizes said otherwise)" — history of a repair; the fact keeps |
| 52 | Carrier transport table (Caughey–Thomas quartets, Fröhlich α_F/ω_LO, Θ_D, alloy ΔU, AlGaN κ dip) | `9.1-accuracy-ledger.md:257-267` | keep | `oracle/accuracy/accuracy-ledger#transport-coefficients` | Canonical. `11.5:122-123` restates α_F/ω_LO with `[Wave-1 A12]` tags — appendix |
| 53 | **Residue** — the cert-refused / frontier list (diamond α_n/α_p unseparated; AlN α_ii and μ_p; β-Ga₂O₃ hole α_ii; AlN C-T quartet paywalled; `κ_BR` normalized form; `Z*_⊥` and e₁₅ sign; NIEL `σ_d`; `η_recomb(T_L)`; EDF-tail anchors) | `9.1-accuracy-ledger.md:269-275` | keep | `oracle/accuracy/accuracy-ledger#residue` | Canonical and the most complete of the three copies (rows 32, 57). Several entries are open questions and should also appear in frontmatter per D7 |
| 54 | 2026-07 gap-audit additions table — accuracy regimes for observables #53–59 (registry rows 128–134) | `9.1-accuracy-ledger.md:277-287` | move | merged into `oracle/accuracy/accuracy-ledger#observable-regimes` as rows 53–59 | The heading is a **dated container**, not a topic: these are ledger rows like any other, split out only because of when they arrived. Merging is required anyway — `check_book_structure.py:367` reads `^\| (\d+) \|` across the whole body and already treats 1–59 as one sequence |
| 55 | Composition: each `ResidualGenerator` carries `characteristic-scale : σ` seeded from this ledger; `combineTol` composes along the DAG; Stage-4 `CompressionPlan` truncation adds a per-plan error target | `9.1-accuracy-ledger.md:289-296` | keep | `oracle/accuracy/accuracy-ledger#composition` | Sole statement of how ledger values reach the residual budget. `3.2 §7` and `4.2 §4` are the mechanism pages; this is the seed side |
| 56 | 9.2 Purpose — this directory holds the machine-readable reference data obligation-8 reads; canonical source-of-truth for "what does the literature say about this quantity?" | `9.2-reference-battery.md:22-24` | keep | `oracle/accuracy/reference-battery#purpose` | Sole statement of the CSVs' authority |
| 57 | 9.2 Contents — the directory tree listing 8 CSVs + `README.md (this file)`, plus four dated population-status paragraphs (2026-06-10 Wave 1, 2026-07-08 Phase 3, Wave 2 2026-07-16, the Wave 3/4/5 programme) and "Also pending" | `9.2-reference-battery.md:26-83` | move | present-tense contents table → `oracle/accuracy/reference-battery#contents`; the wave programme → `oracle/accuracy/reference-battery#wave-programme` + `log/timeline.md`; dated status prose → delete | **Three defects in one block.** (a) `README.md (this file)` is false — 9.2 is a book page, not `physics/library/cert/reference-data/README.md`, and no README.md exists there; this is an unmigrated remnant of the page's pre-book life. (b) The tree lists 8 CSVs; 5 exist. (c) Four dated "Population status" paragraphs are pure history of seeding order. What survives: which CSVs exist, what each holds, what is still unseeded, and the Wave 3/4/5 plan with its two Wave-4 conditions (Ni Curie point at 627 K inside the operating window; B8 contact provenance pinned first) |
| 58 | 9.2 citations to `live/audits/2026-07-16-wave2-beta-ga2o3-audit.md` and `live/specs/2026-07-08-wave2-beta-ga2o3-seeding.md` ("carries the audited values") | `9.2-reference-battery.md:60-62` | delete | — | Both paths resolve today; both containers are deleted by D13 (`journal/live/audits/` outright, `live/specs/` superseded). A canon page pointing at a deleted container for its audited values is a dangling promise the moment cutover lands. The values themselves are in the CSVs and `9.1` |
| 59 | **Schema discipline** — every reference row carries Property · Material · **Environment** · Value · Uncertainty (σ) · Source · Source class · Version · Added · Modified, with the three σ encodings (absolute, multiplicative `×N`, `unbounded`) and the `—`-means-unassigned rule | `9.2-reference-battery.md:85-104` | keep | `oracle/accuracy/reference-battery#row-schema` | Sole definition of the reference-row schema, and it matches all five CSV headers exactly. **This is the closest thing in the corpus to a definition of `Environment`** and it is only a column gloss ("temperature, pressure, applied field as applicable") — see Open questions. The three-σ-encoding dispatch rule is load-bearing and stated nowhere else |
| 60 | **How cert obligation-8 uses this** — 4-step procedure: look up by (Property, Material, Environment) → compute `\|predicted − reference\|/σ` → trip at >3σ → emit evidence with the row's provenance | `9.2-reference-battery.md:106-113` | delete | fold into `oracle/certification/cert-obligations#the-ten-obligations` (obligation 8) | **Third statement of one rule.** `5.1:61-64` states the same four steps; `5.1:148` states the threshold as `τ_battery = 3σ`; `5.4:29` states it again. The tolerance is named in the tolerance ledger, so the battery page should cite `τ_battery`, not restate `3σ`. See Contradictions for which obligation owns the trip |
| 61 | Why CSV (V1) — lowest-common-denominator format surviving language-decision uncertainty; may re-serialize to Arrow/Parquet later, but the canonical archive stays CSV for human-auditability | `9.2-reference-battery.md:115-117` | keep | `oracle/accuracy/reference-battery#why-csv` | Sole statement of a live format decision tied to the open impl-language question |
| 62 | Initial population sources — four bullets naming the seed tables; "Population is incremental and reviewable — every row should be defensible against a literature citation before commit" | `9.2-reference-battery.md:119-128` | move | the review rule → `oracle/accuracy/reference-battery#contents`; the four bullets → delete | The four "will be assembled from" bullets are a plan that has already executed — the CSVs exist and 9.1 holds their contents. Future-tense scaffolding. The one-row-one-citation rule is live discipline and keeps |
| 63 | What this is NOT — not a training dataset, not a simulation-result cache, not authoritative beyond its sources | `9.2-reference-battery.md:130-134` | keep | `oracle/accuracy/reference-battery#boundaries` | Live scope fence. "training data lives elsewhere, possibly in `informed-operator/data/`" — the hedge and the path are both stale; restate as the `operator/` boundary |

---

### from `oracle-registry`

Target notation: `oracle/registry/<page>#<anchor>`. Anchors are proposed slugs.

### 6.1-canonical-vocabularies

| # | Fact or block | Current location | Disposition | Target | Evidence |
|---|---|---|---|---|---|
| 1 | `authority: canon`, `content-hash: 4edb…`, `referenced-by:` list | `6.1-canonical-vocabularies.md:1-41` | delete | — | D1/D6/§4: `authority` tier dies with ch. 11; `content-hash` replaced by git; `referenced-by` is regenerated |
| 2 | "Every other document references these numbers rather than restating them." | `6.1-canonical-vocabularies.md:44` | delete | — | **false as written** — see Contradiction 1. The *intent* survives as the `corpus.json` emission rule (plan §6); the sentence is an unenforced aspiration |
| 3 | Count table, 11 of 14 rows: top-level inputs 3 · state 7-tuple · BO levels 4 · dressing layers · methods 12(+3) · templates 20 · formulas 132(+2) · bundles 11 · residual categories 19 · cert obligations 10 · Layer-0 typeclasses 4 | `6.1-canonical-vocabularies.md:46-58` | delete | — | every one restates a count whose topic another page owns: `crystal-inputs` (`index.md:98`), `unified-state` (`:87`), `born-oppenheimer-levels` (`:43`), `computational-methods` (`:54`), `property-templates` (`:88`), the CSV, `observable-bundles` (`:18`), `residual-definitions` (`:92`), `cert-obligations` (`:89`), `typeclass-alphabet` (`:51`). Under plan §6 the count table is an **emitted** view over `corpus.json`, not a hand-maintained page |
| 4 | Count-table row `State sub-DOF tags` (`orbital, spin, sublattice, valley, strain, gauge, charge, none`) | `6.1-canonical-vocabularies.md:60` | delete | — | owned by `coupling-structure` — `glossary.md:62` names it as the canonical page and `3.3-coupling-structure.md:98` states the enumeration |
| 5 | Count-table row `Crystal symmetry group | first-class (space group × time-reversal × U(1) × SU(2))` | `6.1-canonical-vocabularies.md:59` | move | `oracle/compilation/representation-substrate#crystal-symmetry-group` | it is a one-line restatement of §5 on the same page; travels with row 12 |
| 6 | Count-table row `Theory-context vocabularies | 10 (…)` | `6.1-canonical-vocabularies.md:61` | keep | `oracle/registry/canonical-vocabularies#theory-context` | this is the one count the page genuinely owns (`index.md:95`); merge into §7's own table |
| 7 | §1 — the twelve method names as a closed list | `6.1-canonical-vocabularies.md:63-70` | move | `oracle/registry/computational-methods#the-alphabet` | `computational-methods` is canonical-for *method signatures* (`index.md:54`); the name list and the signatures are one fact split across two pages |
| 8 | §1 — the three registered sub-methods and the `mesh-interpolation` description (Fourier for gauge-free band energies/velocities, Wannier–EPW for gauge-sensitive e-ph matrix elements, mandatory dipole/quadrupole polar corrections, runtime reads the interpolated grid only, C1-clean) | `6.1-canonical-vocabularies.md:72-77` | move | `oracle/registry/computational-methods#sub-methods` | the interpolator's *content* exists only here; `6.4:83-87` carries only the † footnote naming the three. Sole statement of the Fourier/Wannier–EPW split |
| 9 | §1 — "the closed 12-method alphabet is preserved; interpolation is a sub-method, not a new top-level method" | `6.1-canonical-vocabularies.md:76-77` | move | `oracle/registry/computational-methods#sub-methods` | live closure rule, not history |
| 10 | §2 — the three template tables (general 12 · renormalization/configurational/symmetry 3 · domain-interface/defect/thermo 5) with the `Produces` column and per-template notes | `6.1-canonical-vocabularies.md:79-119` | move | `oracle/registry/property-templates#what-each-produces` | `property-templates` is canonical-for *template signatures*; the `Produces` mapping (what observables each template instantiates) exists **only** here and `6.5` explicitly defers to it (`6.5:18-19`). Splitting a template's signature from what it produces is the split that forces the reader to open two pages |
| 11 | §2 — the collapse discipline: "collapse *N observables with the same shape* into *1 template × N argument tuples*" | `6.1-canonical-vocabularies.md:81-83` | move | `oracle/registry/property-templates#discipline` | live design rule; sole statement |
| 12 | §2 — "Bulk-boundary correspondence is **not** a template; handled at the cert layer (obligation-7…)" | `6.1-canonical-vocabularies.md:120-122` | delete | — | verbatim duplicate of `6.5:106-107`, which states it as part of the overlap-resolution block. Keep the `6.5` copy |
| 13 | §3 — the row-band provenance narrative (rows 1–87 research-grounded, 88–102 linear-response/topology, 105–112 slow-tier, 113–118 polarization/2DEG, 120–127 accuracy package, 128–134 gap-audit) | `6.1-canonical-vocabularies.md:128-143` | delete | — | duplicated at greater length and detail by `6.9:42-80`, which names the individual rows. Two copies of one band map; keep the fuller one (row 62 below) |
| 14 | §3 — "Each formula carries a typed signature, a cost tier `T0..T3`, a differentiability tag from `D0 | DN | D1 | D2 | D3 | D4` … and an applicability classifier" | `6.1-canonical-vocabularies.md:143-147` | delete | — | third copy of the tag vocabulary; `named-formulas` owns it (`6.6:41-56`, `glossary.md:41`) and `6.9:104-106` already models the correct behaviour (link, do not repeat) |
| 15 | §3 — the parenthetical warning "(`DN` is not inside `D0..D4` — see [named-formulas])" | `6.1-canonical-vocabularies.md:145-146` | delete | — | **the nomenclature defect, occurrence 1 of 2.** The warning exists only because the vocabulary is misordered. Renaming (plan §4) removes the need for it; until then the warning belongs once, on the owning page. See Open question `dn-not-between-d0-and-d1` |
| 16 | §3 — "See `formula-registry.md` for the narrative index." | `6.1-canonical-vocabularies.md:147` | delete | — | `.md`-filename citation, matched by neither checker; and `formula-registry` dissolves (rows 59-66) |
| 17 | §4 — the `B1..B11` bundle table (ID · Bundle · Primary level) | `6.1-canonical-vocabularies.md:149-166` | move | `oracle/registry/observable-bundles#the-eleven` | **MACHINE-LOAD-BEARING.** `check_data_agreement.py:450-452` harvests the `\| Bn \|` codes from this exact file to validate the CSV `Bundle` column, and hard-errors if the table is not found (`:464-467`). Moving it requires updating `_VOCAB_PAGE`. Merging it into `observable-bundles`, which holds the per-bundle contents, gives one page one job |
| 18 | §4 — the L1-primitive parenthetical (rows 91–94: Z*, ε∞, χ∞, α_M carry `L1`, not a B-tag) | `6.1-canonical-vocabularies.md:167-170` | move | `oracle/registry/observable-bundles#l1-primitives` | live and load-bearing: `traps §70` records that harvesting the table without this note retagged four correct rows. The *schema* source is `6.6:44-47` (harvested at `check_data_agreement.py:456-457`); this is its prose statement |
| 19 | §4 — "(A file tree may additionally group observable *modules* by output data-shape … but the canonical, residual-driving grouping is the eleven physics-domain bundles above.)" | `6.1-canonical-vocabularies.md:170-172` | delete | — | near-verbatim duplicate of `6.7:49-50`. Keep the `6.7` copy |
| 20 | §5 — `CrystalSymmetryGroup = SpaceGroup ⋊ TimeReversal ⋊ U(1)Gauge? ⋊ SU(2)Spin?` and its assembly at Stage 1+2 from `PeriodicityStructure × SiteDecoration` | `6.1-canonical-vocabularies.md:174-184` | move | `oracle/compilation/representation-substrate#crystal-symmetry-group` | sole definition; its *identity* is an `Address[GroupAtlas]` and its derived outputs are substrate fibers, both substrate concerns. Alternative target flagged in Notes |
| 21 | §5 — the group's identity (`Address[GroupAtlas]` over the canonical serialization of finite presentation, factor descriptors, action homomorphisms) and derived-output storage (character tables, irreps, projectors, BZ stalks, Fourier caches as Stage-2/2.5 sidecars) | `6.1-canonical-vocabularies.md:188-193` | move | `oracle/compilation/representation-substrate#crystal-symmetry-group` | same fact-cluster; the citations already point into `representation-substrate §3, §4` |
| 22 | §5 — `IrrepLabel = (group : Address[GroupAtlas-context], local-name : Symbol)`, local-name unique only inside its group context, and its role as Stage-2 output / Stage-2.5 input discriminator | `6.1-canonical-vocabularies.md:194-205` | move | `oracle/compilation/representation-substrate#irrep-label` | sole definition; `4.3-representation-substrate.md:133` already lists `IrrepLabel` as a C1 vocabulary it governs |
| 23 | §6 — the allowed `(StateComponent, SubDofTag)` pairs table (`γ̂`, `A`, `R_I`, `P_I`, `h`, `Π_h`, `Z_I`) | `6.1-canonical-vocabularies.md:207-222` | move | `oracle/laws/coupling-structure#sub-dof-pairs` | `SubDofTag` is owned by `coupling-structure` (`glossary.md:62`), the constructors that enforce the table live there (`3.3:92-98`), and the page itself says so |
| 24 | §6 — "`StatePiece` constructors reject pairs not listed here at registration time" | `6.1-canonical-vocabularies.md:223-224` | move | `oracle/laws/coupling-structure#sub-dof-pairs` | a live registration guard; travels with the table it guards |
| 25 | §7 — the ten theory-context vocabularies table (`XCFunctionalTag` … `SOCScheme`) with member lists and notes | `6.1-canonical-vocabularies.md:240-251` | keep | `oracle/registry/canonical-vocabularies#theory-context` | sole statement; `3.3-coupling-structure.md:596` explicitly defers here ("The vocabularies backing these four fields (ten closed C1 vocabularies) are…"). The *record* stays in `coupling-structure §11`, the *vocabularies* here — a clean split |
| 26 | §7 — the newness argument (no existing vocabulary covers them; the `{SCP, SSCHA, TDEP, GW, BSE-iterated, polaron}` selector is a per-observable dressing method, a different axis from the composition-global theory frame) | `6.1-canonical-vocabularies.md:229-235` | keep | `oracle/registry/canonical-vocabularies#theory-context` | a live non-duplication rationale, not history: it is what stops the two axes being merged again |
| 27 | §7 — each is a `Universe[T]` with `carrier_kind = Closed` and dense `u32` ordinals; adding a member is a versioned `schema_version` bump, not an open-registry append, "because it changes the meaning of every recorded coefficient" | `6.1-canonical-vocabularies.md:235-238` | keep | `oracle/registry/canonical-vocabularies#theory-context` | live versioning rule with its reason attached |
| 28 | §7 — `AtomicSpecies` V1 membership `{C, B, N, Al, Ga, O, H}` and the justification (β-Ga₂O₃ host; O-bearing defects `O_N`, `V_Al–O`, `V_Ga–O_N`, `V_O–H`; slow-tier rows 106, 110 read H and row 109 reads O via `p_O2`) | `6.1-canonical-vocabularies.md:253-260` | keep | `oracle/registry/canonical-vocabularies#atomic-species` | sole statement; the "required by committed content, not future scope" framing is present-tense justification, not history |
| 29 | §7 — "Si and the contact-metal species enter with their waves (schema_version bump)" | `6.1-canonical-vocabularies.md:260-261` | keep | `oracle/registry/canonical-vocabularies#atomic-species` | forward-looking scope statement, not a remnant |
| 30 | §7 — the `HybridAsManyBody` normalization rule: a hybrid is always `XCFunctionalTag.Hybrid` + `ManyBodyLevel.KohnSham`, normalized by `make-theory-context` so the `Address[TheoryContext]` is canonical | `6.1-canonical-vocabularies.md:261-264` | keep | `oracle/registry/canonical-vocabularies#theory-context` | live canonicalization rule; **but** delete the "`* HybridAsManyBody` is reserved/deprecated for V1" framing (§9 scaffolding vocabulary) and the `*` marker in the table, restating it as "not a `ManyBodyLevel` member" |
| 31 | §7 — these vocabularies condition *interpretation and verification*, never *enumeration* of the symmetry-invariant basis; they touch exactly four cert obligations (reference-battery, named-formula-consistency, reference-versioning, surrogate-validity) "and none of the others" | `6.1-canonical-vocabularies.md:266-270` | keep | `oracle/registry/canonical-vocabularies#theory-context` | a live scoping guard with a closed answer; sole statement |

### 6.2-typeclass-alphabet

| # | Fact or block | Current location | Disposition | Target | Evidence |
|---|---|---|---|---|---|
| 32 | `authority`, `content-hash`, `referenced-by` | `6.2-typeclass-alphabet.md:1-16` | delete | — | D6/§4 |
| 33 | The framing: every observable output is typed by three orthogonal axes plus a discrete bucket, as four typeclasses | `6.2-typeclass-alphabet.md:19-20` | keep | `oracle/registry/typeclass-alphabet#axes` | sole statement; `4.1-physics-graph.md:180` cites it for the `type` field on every node |
| 34 | "Presented here as language-neutral typed pseudocode; the implementation language is undecided — see [open-decisions]" | `6.2-typeclass-alphabet.md:20-22` | move | page `open-questions:` frontmatter of `oracle/registry/typeclass-alphabet` | D7/§5: `10.2-open-decisions` dissolves into per-page frontmatter. `depends-on: open-decisions` (`:8-9`) must be retargeted with it — it is this page's only declared dependency |
| 35 | `Quantity` (Value axis): `unitsOf`, `approxEq(tol)`, `rescale`, `combineTol`; combineTol associative/commutative/monotone with per-instance max-absolute or root-sum-square | `6.2-typeclass-alphabet.md:23-28` | keep | `oracle/registry/typeclass-alphabet#quantity` | sole statement; cited by `3.2-residual-definitions.md:299` and `5.1-cert-obligations.md:131` for the σ error model |
| 36 | `Sampleable` (Shape axis) and its à-la-carte capabilities `Integrable` / `Differentiable` / `Restrictable`, with the `exceptionSet` (phase transitions, band crossings, charge-transition levels) and the `chart` tag | `6.2-typeclass-alphabet.md:29-37` | keep | `oracle/registry/typeclass-alphabet#sampleable` | sole statement; the exception-set list is cited from `10.3-audit-prompt.md:116` |
| 37 | `HasAnalyticStructure` (Constraint axis): witnesses `(Local | Global)`-tagged, `certifyAnalytic` returns witnesses or a typed failure | `6.2-typeclass-alphabet.md:38-42` | keep | `oracle/registry/typeclass-alphabet#analytic-structure` | sole statement |
| 38 | `DiscreteStructure` (Combinatorial axis): `identity`, `compose`, `isoEq`; not `Quantity`, not `Sampleable`; topology-atlas outputs live here | `6.2-typeclass-alphabet.md:43-46` | keep | `oracle/registry/typeclass-alphabet#discrete-structure` | sole statement; `6.3:40` and `5.4-cert-obligations-detail.md:28` both depend on it |
| 39 | The alias definitions — `Response = Sampleable + Integrable + Differentiable + HasAnalyticStructure(KramersKronig)` over a frequency domain, etc. | `6.2-typeclass-alphabet.md:48-51` | keep | `oracle/registry/typeclass-alphabet#aliases` | **live, not history**: `Response`, `FieldOnGrid`, `Tensor` and `Scalar` appear as return types throughout `6.4` and `6.5` signatures (`6.4:49-54`, `6.5:29-36`). Without the aliases those signatures do not type |
| 40 | The framing "The **old names** … **survive only as** aliases" | `6.2-typeclass-alphabet.md:48-49` | delete | — | §9 history marker; restate present-tense as "Four aliases name common parameterizations" |
| 41 | "Cert obligations (§12) map onto these axes mechanically." | `6.2-typeclass-alphabet.md:51` | move | `oracle/registry/typeclass-alphabet#axes`, retargeted to `[cert-obligations-detail]` | the claim is live, the coordinate is rotted: bare `§12` names no page, and the axis-mapping table it promises is `5.4-cert-obligations-detail` (whose title is literally "detail and **axis mapping**"). Dangling-promise class |

### 6.3-topology-atlas

| # | Fact or block | Current location | Disposition | Target | Evidence |
|---|---|---|---|---|---|
| 42 | `authority`, `content-hash`, `referenced-by` | `6.3-topology-atlas.md:1-12` | delete | — | D6/§4 |
| 43 | `canonical-for: topology atlas` against `id: topology-atlas` | `6.3-topology-atlas.md:6-7` | move | new owned topics on `oracle/registry/topology-atlas` | **near-vacuous ownership**: the topic differs from the id by one hyphen, so the duplicate-topic invariant is effectively inert here even though the plan's count of 18 does not include it. Needs ≥1 genuinely distinct topic (suggest `symmetry-indicator group X_BS`, `compose-time topology classification`) |
| 44 | `depends-on: []` | `6.3-topology-atlas.md:8` | move | declared deps on `oracle/registry/typeclass-alphabet`, `oracle/certification/cert-obligations` | the body depends on both (`:40`) and declares neither — a missing graph edge that survives only because the citations are bare ordinals rather than ids |
| 45 | "The architecture treats a *material* as a composition `(Lattice + SiteDecoration + Laws) → Material` whose properties are derived, never hardcoded" | `6.3-topology-atlas.md:15-17` | move | `oracle/compilation/compose-time-pipeline` (or `program/purpose/architectural-principles`) | this is the composition axiom, not a topology fact; it is restated here as preamble. Flagged as low-confidence in Notes |
| 46 | `TopologyAtlasEntry = (space-group 1..230 (+magnetic), AZ-class, X_BS, EBRs, compatibility)` | `6.3-topology-atlas.md:20-27` | keep | `oracle/registry/topology-atlas#entry` | sole record definition; `glossary.md:36` points here |
| 47 | `X_BS` computed in polynomial time via Smith Normal Form on the integer matrix of orbit-induced representations | `6.3-topology-atlas.md:29-30` | keep | `oracle/registry/topology-atlas#x-bs` | `4.4-computational-overview.md:458` restates the SNF/complexity claim; that copy is another surveyor's, but this is the owning statement |
| 48 | 117 of 230 space groups have non-trivial `X_BS` under time-reversal in the spin-doubled setting; max `|X_BS| = 72` | `6.3-topology-atlas.md:30-32` | keep | `oracle/registry/topology-atlas#x-bs` | sole statement of both numbers anywhere in the corpus |
| 49 | The always-on / opt-in split: cheap parts (`X_BS` class, orbit-representation decomposition, compatibility check, boundary-mode multiplicity via indicator lookup) always-on at compose-time; expensive global integrals (Wilson loops, Chern, Z₂ via Pfaffian) opt-in per observable | `6.3-topology-atlas.md:32-36` | keep | `oracle/registry/topology-atlas#cost-split` | sole statement; consumed by `4.2-compose-time-pipeline.md:82` |
| 50 | "The atlas gives the PINO a navigational signal: `X_BS` tells the model which compositions are topologically equivalent, so gradients in one inform the other. Topology is the map, not a feature." | `6.3-topology-atlas.md:37-39` | keep | `oracle/registry/topology-atlas#pino-signal` | sole statement of *why* the atlas exists; the only operator-facing claim on the page |
| 51 | "Atlas outputs are `DiscreteStructure` instances (§10), and cert obligation-7 is literally a morphism over them." | `6.3-topology-atlas.md:39-40` | move | `oracle/registry/topology-atlas#entry`, retargeted to `[typeclass-alphabet#discrete-structure]` and `[cert-obligations]` | claim is live and confirmed at `5.4-cert-obligations-detail.md:28`; the bare `§10` resolves to nothing. Dangling-promise class |

### 6.4-computational-methods

| # | Fact or block | Current location | Disposition | Target | Evidence |
|---|---|---|---|---|---|
| 52 | `authority`, `content-hash`, `referenced-by: []` | `6.4-computational-methods.md:1-11` | delete | — | D6/§4. Note the empty `referenced-by`: outside a heading in `4.4-computational-overview.md:389`, **nothing in the corpus cites this page** — the content it holds is reached via `6.1 §1` instead. Merging rows 7–9 here fixes that |
| 53 | "The closed primitive set. Each carries a typed signature and a sub-method dispatch." | `6.4-computational-methods.md:14-15` | keep | `oracle/registry/computational-methods#the-alphabet` | sole statement |
| 54 | The twelve typed signatures with their sub-method dispatch tables (`state-readout` … `symmetry-projection`) | `6.4-computational-methods.md:17-81` | keep | `oracle/registry/computational-methods#signatures` | sole statement; canonical-for *method signatures* (`index.md:54`) |
| 55 | "(ALWAYS invokes a named registry formula — no inline math)" on `algebraic-combination` | `6.4-computational-methods.md:25` | keep | `oracle/registry/computational-methods#signatures` | restates the rule `6.6:36-37` owns, but here it is a signature-site constraint; keep both, cross-link |
| 56 | † footnote: the three registered sub-methods added for UWBG scope; "sub-methods extend a method's dispatch table without changing its typed signature; each requires a sub-method test and a regression-freeze entry" | `6.4-computational-methods.md:83-87` | keep | `oracle/registry/computational-methods#sub-methods` | the extension rule and its two gates are stated only here |

### 6.5-property-templates

| # | Fact or block | Current location | Disposition | Target | Evidence |
|---|---|---|---|---|---|
| 57 | `authority`, `content-hash`, `referenced-by` | `6.5-property-templates.md:1-14` | delete | — | D6/§4 |
| 58 | "Templates are parameterized method chains; concrete observables are instantiations." | `6.5-property-templates.md:16-17` | keep | `oracle/registry/property-templates#what-a-template-is` | sole statement |
| 59 | "See `canonical-vocabularies §2` for the grouping and the 'produces' summary; signatures follow." | `6.5-property-templates.md:18-19` | delete | — | the split it points across is closed by rows 10–11 (the `Produces` tables land on this page). A pointer to content now on the same page |
| 60 | The twenty template signatures with their inline annotations — fixed-point structure shared across SCPH/SSCHA/GW/BSE/polaron emitting `IterativeResult`; CE-vs-Redlich–Kister distinctness; `SymmetryAdaptedHamiltonianOf` as constructive Stage-1; `SelfConsistentChargeBalanceOf` closing the L3↔non-equilibrium cycle; `HarmonicStiffnessHessianOf` as a specialization; `MassActionEquilibriumOf` as equilibrium-not-driven | `6.5-property-templates.md:21-103` | keep | `oracle/registry/property-templates#signatures` | sole statement; canonical-for *template signatures* (`index.md:88`) |
| 61 | "Overlap resolution": `ClusterExpansion` is a parameterization of `ConfigurationalFreeEnergyOf`, not a separate template · bulk-boundary correspondence is a cert obligation (§8 obligation-7), not a template · `HarmonicStiffnessHessianOf` specializes `SecondDerivativeOf` | `6.5-property-templates.md:105-109` | keep | `oracle/registry/property-templates#overlap-resolution` | live anti-duplication rules for the template vocabulary. Two edits: delete the "(recorded once)" framing (§9 marker), and retarget the bare `§8 obligation-7` to `[cert-obligations]` — it names no page and conflicts with three other bare ordinals for the same target (Contradiction 4) |

### 6.6-named-formulas

| # | Fact or block | Current location | Disposition | Target | Evidence |
|---|---|---|---|---|---|
| 62 | `authority`, `content-hash`, `referenced-by` | `6.6-named-formulas.md:1-28` | delete | — | D6/§4 |
| 63 | The CSV is canonical and machine-readable; 132 substantive + 2 architectural markers (force = −∇energy; equivariance) not residualized; `formula-registry.md` is the narrative index | `6.6-named-formulas.md:31-34` | keep | `oracle/registry/named-formulas#the-registry` | keep the CSV-is-canonical pointer and the two-marker rule; **delete the `formula-registry.md` pointer** (that page dissolves, rows 68-76) and let the counts come from the emitted index rather than prose |
| 64 | "Every algebraic combination invokes a named formula with typed inputs and an explicit output type; no inline math." | `6.6-named-formulas.md:35-37` | keep | `oracle/registry/named-formulas#no-inline-math` | the rule the whole registry exists to enforce; sole normative statement |
| 65 | `record FormulaRecord { name, signature, bundle, cost-tier, diff-tag, path, source, depends-on, applicability, adjoint-validated }` | `6.6-named-formulas.md:38-57` | keep | `oracle/registry/named-formulas#formula-record` | **MACHINE-LOAD-BEARING.** `check_data_agreement.py:456-457` harvests the extra `L1` bundle value from the phrase "`L1` primitive tag" in this file. Moving or rewording the `bundle` comment breaks the Bundle check silently (`traps §70`) |
| 66 | **Cost tiers** — `T0` closed-form (≤10 µs) · `T1` small LA / 1D quadrature (≤10 ms) · `T2` BZ/mesh integral (≤10 s) · `T3` self-consistent loop or PDE (≤10 min) | `6.6-named-formulas.md:59-61` | keep | `oracle/registry/named-formulas#cost-tiers` | **sole owner** per `glossary.md:41`. Four other copies exist and none is guarded: `6.9:100-102` (verbatim, → delete row 73), `4.4-computational-overview.md:435` (+distribution counts), `7.2-residual-machinery.md:296-297` (partial, with sampling cadence — a different fact), `informed-operator/design/residual-loss-methodology.md:328`. Probe B proves drift between any two is undetected |
| 67 | The differentiability tags' purpose statement — "The tag answers exactly one question: *how does a consumer obtain a gradient through this row?*" | `6.6-named-formulas.md:63-65` | keep | `oracle/registry/named-formulas#diff-tags` | the invariant that makes the vocabulary decidable; sole statement |
| 68 | `D0` — pure read, adjoint is the identity, registers without a synthesized adjoint; exactly one row qualifies (`reference-phase-energy-cache`, keyed on a phase id alone); a row taking continuous arguments is not a pure read | `6.6-named-formulas.md:66-72` | keep | `oracle/registry/named-formulas#diff-tags` | sole definition. Verified against the CSV: `Diff=D0` occurs exactly once |
| 69 | `DN` — no useful derivative; integer/categorical/boolean/set-valued; not relaxable in place; "**the strongest claim in this vocabulary**… so it is the tag most likely to be wrong"; the two pre-assignment checks | `6.6-named-formulas.md:73-78` | keep | `oracle/registry/named-formulas#diff-tags` | sole definition plus a live assignment discipline |
| 70 | `D1` smooth, no registration gate · `D2` adjoint required, vJp-vs-JvP agreement within `τ_adj`, the gate checks the *synthesized* adjoint not a hand-written backward | `6.6-named-formulas.md:79-84` | keep | `oracle/registry/named-formulas#diff-tags` | sole definition |
| 71 | `D3` — implicit-function adjoint; gradient is one linear solve against the transposed fixed-point Jacobian, independent of forward iteration count; **the iteration-count independence is the test** (if adjoint cost scales with forward steps the row is `D2`) | `6.6-named-formulas.md:85-90` | keep | `oracle/registry/named-formulas#diff-tags` | sole statement of the discriminating test |
| 72 | `D3` is a **refinement** of `D2`, not an alternative: every `D3` row runs the `D2` gate **and** a fixed-point-Jacobian conditioning check refusing registration below `τ_cond`; with the rows it bites named (`fermi-level-charge-neutral`, `self-consistent-charge-balance`, `SCPH-self-consistent-phonons`) and why (∂F/∂E_F flattest in a wide-gap intrinsic semiconductor; soft modes) | `6.6-named-formulas.md:92-104` | keep | `oracle/registry/named-formulas#diff-tags` | **This is a closed gap's resolution stated as present-tense fact — exactly brief exception 1.** `10.2-open-decisions.md:133-135` still narrates it as "closed on 2026-07-21"; that narration deletes, this survives. Restated in summary at `6.9:113-116` → delete that copy |
| 73 | `D4` — relaxed; genuinely non-smooth (argmin, hull, sort, discrete metric); ships a declared smooth relaxation whose bias is model-form error entering `combineTol`, approved with an obligation-9 validity domain; **the relaxation is named in the row's `source` cell** and a `D4` row without one fails the registry-build gate | `6.6-named-formulas.md:105-109` | keep | `oracle/registry/named-formulas#diff-tags` | sole statement; the `source`-cell rule is machine-checked (`check_data_agreement.py` class (g)) |
| 74 | **Mixed-output rule** — the diff-tag describes the continuous component; a discrete label never drags a row to `DN`; where the continuous→discrete map is itself load-bearing (threshold, min-over-set) that non-smoothness makes the row `D4` and the relaxation covers exactly that step | `6.6-named-formulas.md:111-121` | keep | `oracle/registry/named-formulas#mixed-outputs` | live tagging rule; sole statement |
| 75 | The three mixed-output rows named (`radius-ratio-coordination-class`, `elastic-stability-criteria`, `structure-uniqueness-CSP`) with their output shapes | `6.6-named-formulas.md:112-114` | keep | `oracle/registry/named-formulas#mixed-outputs` | verified present in the CSV |
| 76 | "…which is how these three rows **previously read** `D1`, `D1` and `DX`" | `6.6-named-formulas.md:117-119` | delete | — | §9 history marker. Log-worthy row 3 |
| 77 | "**`DN` splits the old `D0`**, which conflated…" — the retag narrative: eleven smooth analytic rows sat at `D0` and registered with no adjoint since `build-verification` exempts `D0`; among them the alloy-lattice interpolation `product` promises is directly optimizable; those eleven **are now** `D1` | `6.6-named-formulas.md:123-130` | delete | — | §9 history. **Two live facts must be extracted before it goes:** (i) `build-verification` exempts `D0` from adjoint synthesis — a live rule → move to `oracle/registry/named-formulas#diff-tags`; (ii) the alloy-lattice-interpolation row is on the differentiable path → already implied by its `D1` tag in the CSV. Log-worthy row 2 |
| 78 | The `chemical-potential-ref-table` story: survived the first pass because "ref-cache lookup" reads like a pure read; it takes `T` and `P`, so `D0` zeroed `∂μ_i/∂T` and left row 69's Maxwell cross-derivative residual vacuously satisfied | `6.6-named-formulas.md:130-135` | keep | `oracle/registry/named-formulas#diff-tags` | **the story is history but the test is live**: "an implementation detail (a cache) mistaken for a mathematical one (an identity adjoint)" is the operative `D0`-misuse rule that row 68 forward-references ("see the note on `D0` misuse below"). Rewrite present-tense as a rule; the "survived the first pass" framing deletes |
| 79 | "The tag is spelled `DN`, not `DX`" — `DX` collides with the DX center in AlGaN/AlN; diff tags are written in backticks in prose; the wurtzite deformation potentials `D1`–`D5` in `[accuracy-ledger]` are the other live collision | `6.6-named-formulas.md:137-143` | keep | `oracle/registry/named-formulas#diff-tags` **and** `practice/traps` | a live search hazard and a live typographic convention, not a remnant (brief exception 2). Both collisions still exist in the corpus today |
| 80 | "The corrected physics is canonical in the registry **and in §6 below**: optical absorption uses `(2ω/c)·Im(√ε)`; the operator-spectrum-area sum rule carries the `2/π` prefactor; the acoustic sum rule sums over all lattice translations (`Σ_J Σ_R Φ_{IαJβ}(R) = 0`); the magnetic relaxation term is `S × (S × H_eff)`; the harmonic transition-rate normalization consumes products-over-modes, not spectra" | `6.6-named-formulas.md:145-150` | keep | `oracle/registry/named-formulas#corrected-forms` | the five forms are live canonical statements. **But "§6 below" does not exist — this page has no §6 and no numbered sections at all.** Dangling promise; delete the pointer. Also delete "The corrected physics" framing → state the five forms directly |
| 81 | **`path` — the anchor class.** `cheap` (117 rows) vs `faithful` (15); **not** a runtime path selector; what a row's value is *anchored against* — own closed form vs a reference-grade DFT/DFPT/NEGF/MC evaluation or measured battery entry | `6.6-named-formulas.md:152-160` | keep | `oracle/registry/named-formulas#anchor-class` | sole owner. Counts verified against the CSV: `Path` = 117 cheap / 15 faithful / 2 `—` |
| 82 | A **consistency pair** is a `cheap` row and a `faithful` row computing the same observable with no agreement theorem, only a bounded model gap; QHA+Slack/Callaway κ vs iterative-LBTE κ is the canonical example; treating it as an equivalence pair scores the legitimate gap as a bug | `6.6-named-formulas.md:161-167` | keep | `oracle/registry/named-formulas#anchor-class` | the definition tying `path` to obligation-6. `7.3-cross-cutting-rules.md:51-54` defines consistency pairs but **never in terms of `cheap`/`faithful`** — see Notes |
| 83 | "The `Cheap vs faithful` column of `[accuracy-ledger]` is the per-observable statement of the same distinction." | `6.6-named-formulas.md:166-167` | keep | `oracle/registry/named-formulas#anchor-class` | citation verified to land on the claim: `9.1-accuracy-ledger.md:55` has that exact column header |
| 84 | "The field was **briefly declared retired**… It did — but the anchor-class meaning is what the corpus actually uses the column for, in four live places… Retiring a field that four pages depend on is how a load-bearing distinction becomes folklore." | `6.6-named-formulas.md:169-173` | delete | — | §9 history marker (`retired`). Log-worthy row 4. The generalizable lesson ("a field four pages depend on") belongs in `practice/traps`, not on this page. The "four live places" count is unverifiable — see Notes |
| 85 | **Applicability-decidability invariant** — every `applicability` predicate is first-order decidable in `(Crystal, Environment)`: finite case analysis on typeclass tags, not numeric thresholds or solver outputs; non-decidable classifiers forbidden at registration | `6.6-named-formulas.md:175-180` | keep | `oracle/registry/named-formulas#applicability-decidability` | sole statement of the invariant; the gate it names is `build-sequence` Phase 7 (`8.6:41`, verified) |

### 6.7-observable-bundles

| # | Fact or block | Current location | Disposition | Target | Evidence |
|---|---|---|---|---|---|
| 86 | `authority`, `content-hash`, `referenced-by: []` | `6.7-observable-bundles.md:1-11` | delete | — | D6/§4. **`referenced-by` is empty and it is accurate: no page anywhere in the corpus cites `observable-bundles`.** A true orphan, reachable only through `6.1 §4`. Row 17 merges the table here and gives it inbound edges |
| 87 | "The eleven physics-domain bundles `B1..B11` are listed in `canonical-vocabularies §4`. Representative contents:" | `6.7-observable-bundles.md:14-15` | delete | — | the pointer closes once row 17 lands the table on this page |
| 88 | The eleven bundles' representative contents (B1 electronic-structure … B11 degradation), including the registered-name aliases (`barrier-from-workfunction-affinity`, `radius-ratio-coordination-class` a.k.a. Pauling radius ratio, `elastic-stability-criteria` a.k.a. Born stability, `plastic-strain-fatigue-life` a.k.a. Coffin–Manson) and the row-band pointers (113–115/117–118 polarization-2DEG; 128–131 gate-dielectric; 105–112 slow-tier; 132 XRD) | `6.7-observable-bundles.md:17-47` | keep | `oracle/registry/observable-bundles#contents` | sole statement; canonical-for *bundle signatures* (`index.md:18`). All named rows verified present in the CSV. The behavior-name↔person-name aliases exist nowhere else and are the only defense against the person-attribution names re-entering |
| 89 | "(A file tree may additionally group observable *modules* by output data-shape; the residual-driving grouping is the eleven physics-domain bundles.)" | `6.7-observable-bundles.md:49-50` | keep | `oracle/registry/observable-bundles#contents` | keep this copy; delete the `6.1:170-172` duplicate (row 19) |

### 6.8-typed-compositions

| # | Fact or block | Current location | Disposition | Target | Evidence |
|---|---|---|---|---|---|
| 90 | `authority`, `content-hash`, `referenced-by` | `6.8-typed-compositions.md:1-12,32-37` | delete | — | D6/§4 |
| 91 | `unregistered-formulas:` frontmatter — the eighteen declared names | `6.8-typed-compositions.md:13-31` | keep | `oracle/registry/typed-compositions` frontmatter | **machine-read and probe-verified (probe C).** `check_data_agreement.py:110-147` fails an undeclared `formula =` argument and `:242` fails a stale declaration. This is the corpus's one working example of a gap declared in a way a checker can hold. Preserve the key under the new fixed frontmatter schema (plan §4 specifies a *fixed key set* — this key must be in it, or the mechanism dies at cutover) |
| 92 | "Every property in `properties.md` written as a typed composition — the validation that the closed vocabulary covers the target scope. Each invokes methods (§2), templates (§3), and named formulas (§4) — **except for the declared gap below**." | `6.8-typed-compositions.md:39-43` | move | `oracle/registry/typed-compositions#purpose` | the purpose statement is live; the three bare ordinals `(§2)(§3)(§4)` are pre-book coordinates naming no page and must become `[computational-methods]`, `[property-templates]`, `[named-formulas]`. The `properties.md` filename citation becomes `[properties]` — or dissolves if rows 106-110 merge the two pages |
| 93 | Structural compositions (LatticeParameters, BondLengths, CrystalStructure, Defects energy + characterization, Surfaces region + energy) | `6.8-typed-compositions.md:45-58` | keep | `oracle/registry/typed-compositions#structural` | sole statement |
| 94 | Electronic compositions (BandStructure, DOS, BandGap, ChargeDensity) | `6.8-typed-compositions.md:60-68` | keep | `oracle/registry/typed-compositions#electronic` | sole statement |
| 95 | Optical compositions (DielectricFunction, Absorption, RefractiveIndex, Photoluminescence) | `6.8-typed-compositions.md:70-78` | keep | `oracle/registry/typed-compositions#optical` | sole statement |
| 96 | Mechanical compositions (ElasticConstants, BulkModulus, StressStrain, Hardness) | `6.8-typed-compositions.md:80-92` | keep | `oracle/registry/typed-compositions#mechanical` | sole statement |
| 97 | The Voigt/Reuss/Hill averaging note — "an open pick; see the C_ij weak-link note in `[accuracy-ledger]`" | `6.8-typed-compositions.md:87-88` | move | `open-questions:` frontmatter of `oracle/registry/typed-compositions` | D7/§5: an open item stated inline. Citation verified to resolve to a page; whether it lands on a C_ij weak-link note is `accuracy` surveyor's call |
| 98 | Thermal compositions (PhononDispersion, HeatCapacity, ThermalConductivity, ThermalExpansion) | `6.8-typed-compositions.md:94-105` | keep | `oracle/registry/typed-compositions#thermal` | sole statement |
| 99 | Magnetic compositions (MagneticMoments, SpinDensity, ExchangeInteractions) | `6.8-typed-compositions.md:107-114` | keep | `oracle/registry/typed-compositions#magnetic` | sole statement. Flag: `kernel = exchange-coupling-formula` names nothing in the registry and is not in `unregistered-formulas` — see Open question `undeclared-non-formula-slots` |
| 100 | Transport compositions (ConductivityViaBTE, ConductivityViaKubo, Conductivity as a both-evaluated pair, CarrierMobility, IonicDiffusion, MigrationBarrier) | `6.8-typed-compositions.md:116-142` | keep | `oracle/registry/typed-compositions#transport` | sole statement; the `Conductivity` pair is the worked instance of obligation-6 method-equivalence |
| 101 | "(The harmonic transition-rate normalization consumes the **product** of normal-mode frequencies via the `product-of-modes` extractor, not the spectra.)" | `6.8-typed-compositions.md:144-145` | keep | `oracle/registry/typed-compositions#transport` | live correctness note; restates one of the five corrected forms in row 80 — cross-link rather than duplicate |
| 102 | Thermodynamic compositions (TotalEnergy, FormationEnergy, PhaseStability, FreeEnergy) | `6.8-typed-compositions.md:147-159` | keep | `oracle/registry/typed-compositions#thermodynamic` | sole statement |
| 103 | Chemical/surface compositions (AdsorptionEnergy, ReactionPathway, CatalyticActivity, SurfaceEnergy) | `6.8-typed-compositions.md:161-177` | keep | `oracle/registry/typed-compositions#chemical-surface` | sole statement. Flag: `ν₀ = harmonic-rate-prefactor` at `:168` names the same quantity the same page calls `harmonic-transition-rate-normalization` at `:135` — Contradiction 5 |
| 104 | "All target observables resolve to typed compositions over the closed vocabulary, **except the declared gap below**." | `6.8-typed-compositions.md:179-180` | keep | `oracle/registry/typed-compositions#purpose` | the honest form of the closed-vocabulary claim |
| 105 | **Declared gap** — eighteen invoked names are not registry rows, so the closed-vocabulary claim does not hold in full; the frontmatter is machine-read; a *declared* gap is reported separately from an *undeclared* one | `6.8-typed-compositions.md:182-188` | keep | `oracle/registry/typed-compositions#declared-gap` | verified by probe C. This block is the reason the page is trustworthy |
| 106 | The refusal rationale — "a registry row must be defensible against a literature citation, and registering these on thin provenance would put unsourced rows into the artifact whose entire discipline is that unsourced values are refused (`[traps]` §19)" | `6.8-typed-compositions.md:190-194` | keep | `oracle/registry/typed-compositions#declared-gap` | live policy; citation verified — `10.4-traps.md:162` is §19 "Unprovenanced coefficient ⇒ refuse the composition" |
| 107 | "The gap is tracked in `[open-decisions]`." | `6.8-typed-compositions.md:193-194` | move | `open-questions:` frontmatter of `oracle/registry/typed-compositions` | D7/§5. `10.2-open-decisions.md:137-154` restates this entire block; that copy deletes and this page becomes the single home. **This is the register/page drift class the plan's defect 6 describes, one page away from realizing** |
| 108 | Tranche 1 — **definition already fixed** (9 names): `refractive-index-from-dielectric`, `absorption-from-dielectric`, `mobility-from-conductivity`, `linear-elasticity-stress-strain` (Hooke), `slab-arithmetic`, `arrhenius`, `adsorption-energy-difference`, `helmholtz-free-energy-decomposition`, `QHA-expansion` (whose tensor form `[traps]` §9 pins, including the compliance-not-stiffness trap) | `6.8-typed-compositions.md:198-206` | keep | `oracle/registry/typed-compositions#declared-gap` | sole statement of the split; `10.4-traps.md:101` is §9 and does pin the compliance form |
| 109 | Tranche 2 — **needs sourcing** (9 names): the four hardness models, `harmonic-transition-rate-normalization`, `jump-diffusivity` (geometric prefactor convention is the trap), `htst-rate`, `turnover-frequency`, `formation-energy-from-references` | `6.8-typed-compositions.md:208-212` | keep | `oracle/registry/typed-compositions#declared-gap` | sole statement; this is the research-shaped half of the gap |
| 110 | "Four invocations above **used to carry** inline mathematics… They **now** carry the declared names instead" | `6.8-typed-compositions.md:214-217` | delete | — | §9 history marker. Log-worthy row 6 |
| 111 | The four declared-name→expression table: `absorption-from-dielectric` = `(2ω/c)·Im(√ε)` · `refractive-index-from-dielectric` = `Re(√ε)` · `mobility-from-conductivity` = `σ/(n·e)` · `helmholtz-free-energy-decomposition` = `E_BO + F_vib + F_el` | `6.8-typed-compositions.md:219-226` | keep | `oracle/registry/typed-compositions#declared-gap` | **load-bearing**: these four expressions are what makes registering those rows transcription rather than research. Deleting them converts four transcription tasks into four research tasks |

### 6.9-formula-registry

| # | Fact or block | Current location | Disposition | Target | Evidence |
|---|---|---|---|---|---|
| 112 | `authority`, `content-hash`, `referenced-by` | `6.9-formula-registry.md:1-21` | delete | — | D6/§4 |
| 113 | `canonical-for: [formula-registry]` — identical to `id: formula-registry` | `6.9-formula-registry.md:6-7` | delete | — | **vacuous ownership** (plan defect 4, one of the named 18). Probe D confirms nothing fires. The page owns no topic that another page could collide with, so it sits outside the anti-duplication machinery entirely — which is how it accumulated the duplication below |
| 114 | `[`../physics/library/formulas/registry-manifest.csv`](../physics/library/formulas/registry-manifest.csv)` | `6.9-formula-registry.md:26` | delete | — | **the link is broken.** From `journal/pages/06-vocabularies-and-registry/`, `../physics/...` resolves to `journal/pages/physics/...`, which does not exist (verified). Neither checker looks at relative markdown links |
| 115 | "What the registry is" — every algebraic combination invokes a named formula, no inline math, no string-encoded expressions; each formula is a typing rule for a `FormulaApply` node in the `PhysicsGraph`; the registry bounds Stage-1 graph construction; each is independently citable and verifiable; new formulas enter only through a controlled cert-validated process; "a contract, not a convenience" | `6.9-formula-registry.md:29-38` | delete | — | every clause is stated elsewhere: no-inline-math at `6.6:35-37`; `FormulaApply` typing rule at `4.1-physics-graph.md:174`; the Stage-1 bound at `4.1:174-176`; the controlled-entry gate at `8.6-build-sequence.md:41`. Nothing here is unique |
| 116 | "Counts" — the full row-band → physics-package map, naming the individual rows in bands 120–127 and 128–134 (`ahc-gap-renormalization`, `kappa-4phonon-high-t-correction`, `iterative-lbte-kappa`, `breakdown-field-temperature-slope`, `tp-aware-hull`, `detailed-balance-cycle-residual`, `rotational-sum-rule`, `alloy-disorder-scattering`, `pyroelectric-coefficient`, `poole-frenkel-current`, `tddb-thermochemical-e-model`, `dielectric-crystallization-jmak`, `xrd-structure-factor`, `raman-activity`, `radiative-recombination-rate`) with the gating predicates | `6.9-formula-registry.md:42-61` | move | `oracle/registry/named-formulas#row-bands` | the **only** full band map; `6.1:128-143` is a shorter copy of the same fact (row 13 deletes it). All 15 named rows verified present in the CSV. Keeping it on `named-formulas` puts the band map next to the record schema it describes |
| 117 | The `is-noncentrosymmetric` gating note — "the piezo-class predicate of the applicability-classifiers two-predicate split, not the Fröhlich `is-polar-material` gate" | `6.9-formula-registry.md:45-47` | move | `oracle/registry/named-formulas#row-bands` | a live disambiguation between two predicates that look interchangeable; sole statement in this chapter |
| 118 | "2 architectural markers (rows 103–104)… force = −∇energy (an autodiff identity) and equivariance (a structural constraint). They appear in the manifest so the decision is recorded, not so they generate loss." | `6.9-formula-registry.md:62-66` | move | `oracle/registry/named-formulas#the-registry` | the *reason* the two markers are in the CSV exists only here; `6.6:32-34` names them without saying why they are recorded |
| 119 | "Rows 1–87 grounded in `physics/research/`" and "Rows 88–102 are the linear-response and topology-atlas extensions" with their contents enumerated | `6.9-formula-registry.md:67-80` | move | `oracle/registry/named-formulas#row-bands` | same band map; travels with row 116. **Note the path `physics/research/` — `10.3-audit-prompt.md` was flagged in the plan for citing this directory for physics that moved. Verify the directory still exists before re-stating the provenance** |
| 120 | "Columns" — the eight-column CSV legend (`#`, `Name`, `Signature`, `Bundle`, `Tier`, `Diff`, `Path`, `Source`, `Depends on`) with per-column meanings | `6.9-formula-registry.md:82-96` | delete | — | restates the CSV's own header row plus semantics each of which is owned elsewhere: `Bundle`→`6.1 §4`/`6.6`; `Tier`→`6.6`; `Diff`→`6.6` (this row says so itself); `Path`→`6.6` (says so itself). Under plan §6 the column legend belongs to `data/` and the emitted `corpus.json`, not to a prose page |
| 121 | The `Source` column note — "'extension' / 'topology atlas' — the latter for rows 88–102 **and 128–134** (the 2026-07 gap-audit rows, which the Counts section below has always described correctly; only this legend lagged)" | `6.9-formula-registry.md:95` | delete | — | the surviving *fact* (rows 128–134 carry `Source = topology atlas`) belongs in the CSV, which already carries it. "the Counts section below has always described correctly; only this legend lagged" is §9 history about a fixed drift. Log-worthy row 7 |
| 122 | "Tag legend" **cost tiers** — `T0` ≤10 µs · `T1` ≤10 ms · `T2` ≤10 s · `T3` ≤10 min | `6.9-formula-registry.md:100-102` | delete | — | **verbatim second copy of `6.6:59-61`, unguarded — probe B proves drift between them is invisible to both checkers.** The page is duplicating in one paragraph exactly what it forbids in the next |
| 123 | "Tag legend" **differentiability** — "see `[named-formulas]`, which is canonical… Restating it here is what let five incompatible legends coexist; this page now links rather than repeats." | `6.9-formula-registry.md:104-106` | delete | — | a pure pointer plus §9 history about five dead legends. The *behaviour* it models is right and should become the corpus-wide default; the *sentence* is scaffolding. Log-worthy row 5 |
| 124 | "How a formula becomes a residual" — the factory reads each record and produces a `ResidualGenerator`; cost tier sets sampling cadence; diff tag sets gradient-bearing and triggers the `D2`/`D3` registration gate; applicability masks per training sample; each generator unfolds along `axes` to emit a content-addressed `Map<ResidualKey, Scalar>`; weighting and aggregation belong to `/informed-operator` | `6.9-formula-registry.md:108-119` | delete | — | a summary of `7.2-residual-machinery.md:40-50, 55-95` and `:296-297`, which own the factory, the record and the cadence table. The `D3`-refines-`D2` parenthetical duplicates row 72 |

### 6.10-properties

| # | Fact or block | Current location | Disposition | Target | Evidence |
|---|---|---|---|---|---|
| 125 | `authority`, `content-hash`, `referenced-by` | `6.10-properties.md:1-14` | delete | — | D6/§4. `referenced-by` names only `deriv-high-field` — **a chapter-11 page.** When ch. 11 dies this page's in-degree is zero |
| 126 | `canonical-for: [properties]` — identical to `id: properties` | `6.10-properties.md:6-7` | delete | — | **vacuous ownership** (plan defect 4, one of the named 18); probe D confirms it is unenforceable |
| 127 | "the nine categories define the **target scope**, not the implemented scope: the buckets any quantity a user might ask for should fall into" | `6.10-properties.md:17-21` | keep | `oracle/registry/properties#scope-vs-inventory` | the page's actual job, and it is not stated anywhere else |
| 128 | "**They are not a claim that the registry can compute all nine today.** … Read this page as scope, and the registry as inventory; **where they disagree, the registry wins.**" | `6.10-properties.md:22-29` | keep | `oracle/registry/properties#scope-vs-inventory` | a live precedence rule between two artifacts — exactly the kind of rule the restructure exists to make findable. Sole statement |
| 129 | Category 6 (Magnetic) has no dedicated bundle "because `γ̂` is mean-field and the UWBG scope did not force one" | `6.10-properties.md:25-27` | keep | `oracle/registry/properties#scope-vs-inventory` | a stated, reasoned coverage hole — the clearest instance of the rule in row 128. Candidate for `open-questions` instead; flagged in Notes |
| 130 | "Property → bundle map" preamble — the authoritative per-formula bundle assignment is the CSV `Bundle` column; the table is category→bundle overview only and does not re-define bundle semantics | `6.10-properties.md:31-37` | keep | `oracle/registry/properties#bundle-map` | an explicit, correct non-duplication disclaimer. Rare in this chapter and worth preserving as a pattern |
| 131 | The nine-row category→bundle table | `6.10-properties.md:39-49` | keep | `oracle/registry/properties#bundle-map` | sole statement of the category→bundle projection |
| 132 | "(The earlier `B1..B11` labels in this file were a stale pre-canon scheme — e.g. `B11`='topology atlas' — superseded by the `canonical-vocabularies §4` canon, where `B11`=degradation.)" | `6.10-properties.md:51-53` | delete | — | §9 history marker (`stale`, `pre-canon`, `superseded`). Log-worthy row 8 |
| 133 | The nine category sections — one paragraph of physical motivation each, plus the item list (Structural 5 · Electronic 4 · Optical 4 · Mechanical 4 · Thermal 4 · Magnetic 3 · Transport 4 · Thermodynamic 4 · Chemical/surface 4) | `6.10-properties.md:55-164` | keep | `oracle/registry/properties#categories` | **load-bearing input to `typed-compositions`**: `6.8:39` claims to write "every property in `properties.md`" as a composition, so this list is the checklist that claim is against. The motivation paragraphs exist nowhere else. See rows 141-143 for the merge proposal |
| 134 | The bare ordinal in "(defined in [canonical-vocabularies] §4\`)" — note the stray backtick | `6.10-properties.md:34` | move | `oracle/registry/properties#bundle-map` with an anchored citation | malformed citation: mixed bracketed-id and bare-ordinal forms plus an unbalanced backtick. Renders wrong today |

---

---

### from `program`

| # | Fact or block | Current location | Disposition | Target | Evidence |
|---|---|---|---|---|---|
| 1 | `authority: canon` + `content-hash` frontmatter keys | all 12 scope pages, frontmatter | delete | — | D6/§4 delete both corpus-wide; `authority` has no second tier once ch. 11 dissolves |
| 2 | PINO "predicts the **time evolution** of the state of a crystalline material" | `1.1-purpose-and-scope.md:19-20` | keep | `program/purpose/purpose-and-scope#what-n-op-is` | **Contradiction C1 — do not resolve.** Framing must be settled before the sentence is rewritten |
| 3 | Downstream target: durable UWBG chips for harsh environments (jet turbine, >500 °C, cycling, vibration, field, current density, radiation) | `1.1:20-25` | keep | `program/purpose/purpose-and-scope#downstream-target` | sole full statement in canon; `1.5:37-39` and `10.3-audit-prompt:23` restate it |
| 4 | `/physics` does not represent state *values*; it instantiates a physical system and defines the laws | `1.1:27-31` | keep | `program/purpose/purpose-and-scope#what-physics-is` | sole statement of the instantiate-vs-store distinction |
| 5 | Properties are derived from state, **never hard-coded**; some perturbations alter what properties do to the lattice | `1.1:31-33` | move | `oracle/registry/properties#derived-not-hardcoded` | a rule about properties, stated on the purpose page; `6.10-properties` owns properties and is silent on it |
| 6 | MVP models **diamond** | `1.1:34-35` | keep | `program/purpose/purpose-and-scope#mvp` | scope fact the purpose page legitimately owns |
| 7 | "…with three target capabilities: 1. crystal-structure prediction 2. electron-cloud diffusion 3. heat diffusion" | `1.1:35-40` | delete | cite `[capability-slices]` | duplication; `8.2-capability-slices` owns "MVP capabilities" and states all three in full |
| 8 | MVP discipline: "as much closed-form / computationally feasible expressions as possible" + purpose-built tools | `1.1:42-44` | keep | `program/purpose/purpose-and-scope#mvp-discipline` | sole statement; nothing else names the discipline |
| 9 | Broader material scope: c-BN, AlN, GaN, β-Ga₂O₃, AlGaN; W, Mo, Pt, Ti, Ni, Ta, TiN, WSi₂; SiC, Si, sapphire; Al₂O₃, HfO₂, AlN-as-dielectric | `1.1:44-47` | keep | `program/purpose/purpose-and-scope#material-scope` | **Contradiction C5** — `11.5-deriv-high-field.md:26` gives a different anchor set, neither a subset of the other |
| 10 | Comprehensive spec, diamond-first build — the distinction that "runs through everything" | `1.1:49-53` | keep | `program/purpose/purpose-and-scope#comprehensive-spec-diamond-build` | sole statement of the program's central scoping stance |
| 11 | "**`[mvp-scope]` owns MVP scope** … The paragraph above is a summary of it, not a second definition of it; where the two differ, that page wins." | `1.1:54-56` | delete | — | a tie-breaker that exists only because the fact is stated twice. Row 7 removes the duplication; the rule then has nothing to arbitrate. Scaffolding of the "we might disagree later" kind D1 forbids |
| 12 | `/physics` is a pure oracle: owns no training control flow, no sample selection beyond per-generator `sampling-policy`, no loss-consuming loop | `1.1:59-62` | keep | `program/purpose/purpose-and-scope#what-physics-is-not` | owner; `1.2:228-229` and `1.3:22-24` restate |
| 13 | Active-learning loops (residual-adaptive sampling, query-by-committee, importance reweighting) live in `/interface`, not `/physics` and not `/informed-operator`; both expose the signals an external policy consumes | `1.1:62-68` | move | `interface/boundary#what-interface-owns` | the plan gives `interface/` exactly one page stating the boundary. This paragraph is its only seed content anywhere in canon |
| 14 | `canonical-for: [product]` — identical to id | `1.2-product.md:6-7` | delete | replace with ≥1 distinct topic | vacuous ownership, defect 4; probe A shows no checker fires |
| 15 | Header blockquote: "companion to the atomic spec"; "every claim cites its canonical `arch-xx` / `impl-xx` source"; "Fixed by discussion 2026-07-16" | `1.2:25-31` | delete | — | `arch-xx`/`impl-xx` are retired id formats (defect 2 class); "the atomic spec/tree" is a pre-book container name; dated fixing is history |
| 16 | The amendment-direction rule: "amend by editing this file against the atomic tree, never the reverse" | `1.2:31` | move | `practice/conventions#companion-page-amendment` | a live editorial convention buried in a scaffolding blockquote |
| 17 | Identity: the product is **the oracle** — the compiled scoring function ships, not a service/framework/the PINO | `1.2:37-41` | keep | `program/purpose/product#identity` | owner |
| 18 | Mission: "verify whether a crystal is valid" = produce slot-by-slot evidence; the oracle never renders a verdict | `1.2:43-46` | keep | `program/purpose/product#evidence-not-verdict` | sharpest statement in canon; make it the owner |
| 19 | Consumers: two classes, one surface (`/informed-operator` hot loop; people screening/reconciling) | `1.2:48-53` | keep | `program/purpose/product#consumers` | sole statement |
| 20 | Principle **YAGNI** — no machinery without a present need | `1.2:56-57` | keep | `program/purpose/product#principles` | sole statement |
| 21 | Principle **Evidence, never verdicts** | `1.2:57-59` | delete | cite row 18 | restates §1 Mission four lines above it, on the same page |
| 22 | Principle **Refusal is first-class** | `1.2:59-61` | keep | `program/purpose/product#principles` | owner of the product-side phrasing; `1.4:34-40` owns the mechanics |
| 23 | Principle **No natural language** — every artifact is machine data | `1.2:61-63` | keep | `program/purpose/product#principles` | sole statement |
| 24 | Principle **Agnostic by purity** — pure function, flat arrays, no loop ownership | `1.2:63-66` | delete | cite `[architectural-principles#numerics-agnostic]` | third statement of one fact: `1.3:18-19`, `1.4:44-49`, here |
| 25 | Same bullet's parenthetical citing `informed-operator/design/learnable-structure-requirements.md` | `1.2:65-66` | mine | `operator/seam/learnable-structure` | container deleted per §9; the mirror-commitment claim survives |
| 26 | **Score, not solve.** "The caller supplies complete candidate states; the oracle never fills in missing pieces" | `1.2:67-68` | keep | `program/purpose/product#score-not-solve` | **owner — this is the oracle/operator boundary.** Its citation `(purpose-and-scope)` is a **dangling promise**: `1.1` states no such thing (full read, no match for "score", "solve", "complete candidate", "fills in") |
| 27 | Deployment shape: compiler (seconds–minutes, once per identity) + oracle-file (µs–ms, millions of calls) | `1.2:70-81` | keep | `program/purpose/product#deployment-shape` | the *persisted-artifact* framing is unique to this page; the compose/runtime split itself duplicates `compose-time-pipeline` and `1.5:59-70` |
| 28 | What an oracle-file contains — callable, static slot schema, identity/content hash, certificate reference | `1.2:83-97` | keep | `program/purpose/product#oracle-file-contents` | sole statement; "file hash = kernel hash" appears only here |
| 29 | Behavioral rules: one file per crystal identity · environment-box validity · oracle-files are immutable | `1.2:99-113` | keep | `program/purpose/product#behavioral-rules` | sole statement of all three |
| 30 | Inputs (identity): "CIF content plus an environment record"; a CIF alone is a compile request, not a scoreable object | `1.2:117-122` | delete | cite `[crystal-inputs]` | stated a third time at `1.5:99-103`; `2.1-crystal-inputs` owns "top-level inputs" |
| 31 | State is a deliberate **superset** of any interchange format; the missing pieces are what the operator learns to supply | `1.2:124-128` | keep | `program/purpose/product#state-input` | keep once here; `1.5:104-108` restates it verbatim-in-substance |
| 32 | *Structural* well-formedness (shapes, finite values) is the caller's obligation; *physical* admissibility is scored, never presupposed | `1.2:129-130` | keep | `program/purpose/product#state-input` | sole statement of the obligation split; load-bearing for the seam |
| 33 | `Environment` (per call): the operating-condition record, varying within the file's stamped box | `1.2:132-133` | keep | `program/purpose/product#environment-input` | **homeless-fact instance.** The `Environment` record type is named here, at `8.6:37` phase 3, and in `compose-time-pipeline §1` signatures; no page's `canonical-for` claims it. Matches the brief's confirmed homeless fact |
| 34 | The `Validate(state, env, request, gradient) → residuals, values, cograds, kernel` signature block | `1.2:137-145` | delete | cite `[pino-bridge#call-contract]` | the page cites `pino-bridge §1` as its own source; the signature is `pino-bridge`'s. Keep a prose sentence naming the four returns |
| 35 | Keyed floats only; the residual value is **raw** — the oracle never normalizes, weights, sums, or judges across slots | `1.2:147-149` | delete | cite row 18 + `[residual-definitions]` | third statement on one page (§1 Mission, §1 Principles, §4) |
| 36 | The static schema carries per key: producing registry row, axis coordinates, closed-enum tags, error scale σ; **consumers compute `z = value/σ` themselves — a join against the schema, not a product output** | `1.2:151-156` | keep | `program/purpose/product#static-schema` | the "z is a join, not an output" rule appears only here and is a real boundary |
| 37 | Refusal is absence: an uncertifiable check is not in the kernel, so its key is not in any map; the reason is enum + numeric witness in the cert record | `1.2:158-161` | keep | `program/purpose/product#refusal-is-absence` | owner of the product-facing phrasing |
| 38 | Selection: compose-time scoping + call-time subsetting, both already in the architecture | `1.2:165-172` | keep | `program/purpose/product#selection` | sole statement |
| 39 | "*Recorded refinement (future `pino-bridge` edit, deliberately not made yet):* `request` should additionally accept the schema's closed-enum tags as selectors" | `1.2:173-176` | move | `oracle/seams/pino-bridge` `open-questions:` | a deferred-edit marker — the "we might change it later" shape D1 names. Either make the edit or carry it as an open question under D7; not as prose |
| 40 | `Import` pins `(named-target, value, σ, provenance, coverage-mask)` as a first-class check; two readings — reconciliation vs design | `1.2:180-190` | delete | cite `[pino-bridge#import]` | stated three times: here, `1.5:120-127`, and `pino-bridge §2` which both cite |
| 41 | "In the artifact model, `Import` is a compiler input" — new pins → new oracle-file → every result attributable | `1.2:192-195` | keep | `program/purpose/product#import-is-a-compiler-input` | unique to the product/artifact view |
| 42 | The two loops are mirror images: training sinks gradients into weights, design into the candidate; one oracle, one surface, two sinks | `1.2:197-200` | keep | `program/purpose/product#two-loops` | keep **here**; delete the duplicate at `1.5:129-132` |
| 43 | The design-variable boundary: continuous (cell, positions, composition fraction) directly optimizable; discrete (species, decoration, symmetry family) = enumerate-and-compile or external search | `1.2:202-208` | keep | `program/purpose/product#design-variable-boundary` | keep **here**; delete the duplicate at `1.5:132-137` |
| 44 | Static instantaneous-property design works today; **lifetime** design additionally requires a time-evolution capability this spec does not claim | `1.2:208-210` | keep | `program/purpose/product#design-variable-boundary` | related to C1; the claim itself is stable under either framing |
| 45 | The CLI: `compile` · `inspect` · `validate`; in-program loading is primary; nothing interactive, stateful, or daemonized | `1.2:214-224` | keep | `program/purpose/product#cli` | owner; `1.3:29-31` states only the *boundary* fact (the CLI ships inside `/physics`), which is distinct |
| 46 | What the product is not — 5 bullets (no state/evolution/training/loop; no aggregation or judgment; no runtime external simulation; no open-ended expressions; no natural language) | `1.2:226-235` | delete | cite rows 12, 18, 23 + `[canonical-vocabularies]` | every bullet restates a fact with an owner elsewhere on this page or on `1.1`/`1.3`. Collapse to a cite list |
| 47 | Open decision 1: time-evolution verbs unclaimed; scorer↔stepper duality **resolved "survives with restrictions"** — evolver is a flag-gated Stage-4 sibling sharing the scorer's RHS forests, integrator stays consumer-side | `1.2:242-251` | mine | `oracle/laws/generic-dynamics#evolver-duality` | cites `live/specs/2026-07-16-evolver-duality-research.md`, a container deleted per D13/§9. The **resolution** is a present-tense fact (brief exception 1). **Contradiction C2** with `1.5:211-216` |
| 48 | "§8 is preserved verbatim: the hand-off is tendencies, never trajectories" | `1.2:248-249` | delete | keep only the tendencies-not-trajectories clause | `§8` is a bare ordinal into a document being deleted; unresolvable after cutover |
| 49 | Open decision 2 (loading convention / ABI) and 3 (wire formats) | `1.2:252-257` | move | `oracle/seams/pino-bridge` `open-questions:` | D7/§5 — the owning page declares its own gaps |
| 50 | Open decision 4 (compile-cache management policy) | `1.2:258-260` | move | `oracle/compilation/representation-substrate` `open-questions:` | D7/§5; content addressing makes correctness free, only policy is open |
| 51 | The three-library partition `/physics` · `/informed-operator` · `/interface` | `1.3-library-landscape.md:16` | keep | **becomes `journals/oracle` · `operator` · `interface`** — and the page states it in prose | the partition *is* the top-level tree; the page survives because rows 53–56 are boundary facts a directory layout cannot encode. See Notes |
| 52 | `/physics` numerics-agnostic at its seam while internally committed to the representation substrate | `1.3:18-20` | delete | cite `[architectural-principles#numerics-agnostic]` | see row 24; three statements of one fact |
| 53 | `/physics` does **not** hold time-varying state values, train networks, integrate trajectories, or wrap external DFT codes at runtime | `1.3:22-24` | keep | `program/purpose/library-landscape#physics-boundary` | the runtime-DFT exclusion appears only here and at `1.2:231-232` |
| 54 | "This book is primarily about `/physics`." | `1.3:24` | delete | — | container self-reference, and false under D4: the corpus covers all three modules |
| 55 | `/informed-operator` is the PINO; consumes `/physics` and **learns the time-evolution operator**; seam contract + loss methodology live under `informed-operator/design/` | `1.3:25-27` | mine | `operator/seam` + `operator/loss` | the design directory is deleted per §9. **Contradiction C1** — this page states the time-evolution framing |
| 56 | `/interface` owns every driving loop — training, design search, active learning; not yet designed | `1.3:28-29` | keep | `program/purpose/library-landscape#interface` + seeds `interface/boundary` | pairs with row 13 |
| 57 | The oracle's own CLI ships **inside** `/physics`; `/interface` is the loops, not the command line | `1.3:29-31` | keep | `program/purpose/library-landscape#cli-lives-in-physics` | sole statement of this boundary; without it the tree implies the CLI is interface work |
| 58 | Engineering aspects (defects, dopants, surfaces, interfaces, operating-condition effects) live **inside** `/physics`, not a separate library | `1.3:33-34` | keep | `program/purpose/library-landscape#engineering-is-inside-physics` | sole statement; load-bearing against a fourth module appearing |
| 59 | `canonical-for: [architectural principles]` — the page's own id with the hyphen spelled as a space | `1.4:6-7` | delete | replace with ≥1 distinct topic | **vacuous ownership the plan's count of 18 misses.** Checker key is `" ".join(topic.lower().split())` (`check_book_structure.py:197`) — case- and whitespace-normalised, **not** hyphen-normalised. See Notes for the full list of 5 |
| 60 | P1 One compile pipeline over one substrate (4+1 stage pipeline over the content-addressed substrate) | `1.4:21-23` | keep | `program/purpose/architectural-principles#one-pipeline` | owner. "4+1 stage" containing a "Stage 2.5" is on the plan §4 rename list |
| 61 | "The earlier `core ← shared ← …` import chain is **retired**: it described a module layout that no longer exists." | `1.4:24-26` | delete | — | explicit history marker (§9, 105-line pattern). **Contradiction C4**: `8.5:19-20` and `8.6:35-36` still name build phases `core` and `shared` |
| 62 | P2 Minimum primitives — the 12-method vocabulary + 3 registered sub-methods is the closed primitive set | `1.4:27-29` | keep | `program/purpose/architectural-principles#minimum-primitives` | keep the *principle*; the counts duplicate `canonical-vocabularies` — cite, do not restate |
| 63 | P3 No symbolic computation on the runtime path | `1.4:30-31` | keep | `program/purpose/architectural-principles#no-runtime-symbolics` | owner |
| 64 | P4 Typed everything — explicit typed signatures, no string-encoded formulas, no implicit parameters | `1.4:32-33` | keep | `program/purpose/architectural-principles#typed-everything` | owner; `8.7:25-26` restates as a checkable item |
| 65 | P5 Composition over duplication | `1.4:34-35` | keep | `program/purpose/architectural-principles#composition-over-duplication` | owner |
| 66 | P6 Loud at compose time, absent at runtime; never *raised* from the kernel; runtime failure is a `Failed` cert leaf with a witness, never an exception | `1.4:36-40` | keep | `program/purpose/architectural-principles#loud-then-absent` | **owner of the refusal mechanics**; `1.2:59-61` and `1.2:158-161` are the product-facing restatements |
| 67 | P7 Cert is first-class — schema + freeze fixture + tamper tripwire + high-precision oracle | `1.4:41-42` | keep | `program/purpose/architectural-principles#cert-first-class` | sole statement of the four-part weighting |
| 68 | P8 Numerics-agnostic at the seam, committed within; emits readouts and residuals; integrator/trainer/PINO downstream; time-evolution verbs unclaimed | `1.4:43-49` | keep | `program/purpose/architectural-principles#numerics-agnostic` | **owner** — rows 24 and 52 collapse into this |
| 69 | `canonical-for: [rationale]` vacuous; `referenced-by: []`; `depends-on: [timeline]` alone while the prose cites 12 pages | `1.5-rationale.md:6-10` | delete | replace | vacuous ownership + **orphan page** (nothing in canon cites it; `contents.md:19` lists it, `10.3-audit-prompt:57` names it in a chapter map). The 12 uncited edges are all backticked → defect 1 |
| 70 | Header blockquote: "**Historical snapshot** — claims, counts, and status as of 2026-07-16"; "Presentation artifacts are dated and are never updated"; "the canon chapters (1–10) are canonical"; "cites its canonical `arch-xx` / `impl-xx` / audit source"; "This is a **presentation companion**" | `1.5:14-19` | delete | — | five distinct scaffolding markers in six lines: dated snapshot, staleness disclaimer, chapter-number authority, retired id formats, deleted-container role |
| 71 | "Purpose of this report." paragraph | `1.5:21-24` | delete | — | document-about-the-document; the page's purpose is its frontmatter under §4 |
| 72 | §1 The one-paragraph version | `1.5:30-41` | delete | cite rows 3, 17, 18 | every clause has an owner on `1.1` or `1.2`. **Exception, split out as row 73** |
| 73 | End goal is **property-targeted crystal design**: "you state the properties you need; the system searches for crystals simultaneously physically real and fit for purpose — and it can show its work" | `1.5:36-41` | move | `program/purpose/purpose-and-scope#downstream-target` | the design-direction goal; `1.1` states the *chip* target but never the properties-in-structures-out goal. Note `journal/live/specs/2026-07-21-oracle-code-spec-research-brief.md:11` calls this "a *direction*, not a mechanism" and says no proposer component is specified anywhere — flag for auditor 3 |
| 74 | §2 **Verifying is cheaper than solving** — direct simulation is prohibitively expensive, ML surrogates are untrustworthy exactly at extrapolation; the answer is a grader so cheap and complete the operator can be disciplined by it at every step; the learned half's known weakness is exactly what the oracle exposes | `1.5:43-55` | keep | `program/purpose/purpose-and-scope#why-a-grader` | **the single most load-bearing block on this page and it is stated nowhere else in canon.** The program's central justification. Cross-scope: `journal/live/presentations/2026-07-22-cs-framing-outline.md:102` develops it into a complexity-class argument and notes "None of that is in the corpus" |
| 75 | §3 `/physics` is a compiler paired with the pure functions it emits — compose time vs runtime | `1.5:59-70` | delete | cite `[computational-overview]`, `[compose-time-pipeline]` | third statement; see row 27 |
| 76 | §3 The grammar is closed — 132 substantive formulas (+2 markers), 12 methods (+3 sub-methods), 20 templates, 11 bundles, 19 residual categories, 10 obligations; enumerable / traceable / decidable | `1.5:73-82` | delete | cite `[canonical-vocabularies]` | duplicate counts, **and unswept**: probe D shows "132 substantive **named** formulas" evades the count-drift regex. Keep the enumerable/traceable/decidable triad → move to `program/purpose/purpose-and-scope#why-a-grader` |
| 77 | §3 Identity is content-addressed; same inputs → bit-identical kernel; reproducibility is structural, not policy | `1.5:83-87` | delete | cite `[representation-substrate]` | duplicate of `1.2:92-95` and the substrate page |
| 78 | §3 The output is evidence, never a verdict | `1.5:88-95` | delete | cite row 18 | fourth statement of one fact across `1.2` §1/§4/§8 and here |
| 79 | §4 The interfaces (in: identity/CIF+env · in: state superset · out: four things) | `1.5:97-112` | delete | cite rows 30, 31, 34 | wholesale duplicate of `1.2` §3–§4 |
| 80 | §5 One framework, two loops — `Import`, measured datum ≡ desired property, mirror-image loops, continuous/discrete design variables | `1.5:114-139` | delete | cite rows 40, 42, 43 | wholesale duplicate of `1.2` §6 |
| 81 | §6 **Is a neural operator the right learner?** — periodic domain is the native habitat of spectral operators; the state really is function channels; discretization invariance is a requirement not a luxury; supervision is native (pointwise residuals + synthesized gradients) | `1.5:141-156` | move | `operator/seam#why-a-neural-operator` | **unique and load-bearing.** The only argued justification in canon for the choice of learner |
| 82 | §6 Caveats: not everything is a field (sites are a point set, species discrete) ⇒ hybrid architecture; localized features strain global spectral bases; long-horizon rollout drift is the known failure mode | `1.5:157-165` | move | `operator/seam#known-failure-modes` | the only statement of the operator's failure modes in canon; directly relevant to auditor 3 |
| 83 | §6 The compiler's symmetry analysis produces the exact block structure an equivariant network needs — the oracle can *hand the operator its equivariance* | `1.5:162-165` | move | `operator/seam#equivariance-handoff` | a concrete unexploited seam capability, stated once |
| 84 | §6 `Learnable_Structure` — a kernel for compiling arbitrary learnable structures; mirror-image of the oracle's compiler; one grades, one learns | `1.5:167-171` | mine | `operator/seam/learnable-structure` | the workstream named in `informed-operator/design/learnable-structure-requirements.md`, a container deleted per §9 |
| 85 | §7 A certified specification base — 58 pages, **99 canonical topics**, dependency graph, closed vocabularies, tolerance ledger, 10 obligations with four refusal modes | `1.5:175-179` | delete | — | corpus self-description; regenerated into `index/corpus.json` per §6. **Contradiction C3: 99 vs the measured 98** (checker prints `98 canonical topics`; my scan of all 58 pages agrees) |
| 86 | §7 The 2026-07-16 reconciliation campaign: ~88 defects, calibration-gated auditors (5/5 planted contradictions), two rounds, evidence transcripts | `1.5:179-184` | delete | — | history. **Log-worthy #1** |
| 87 | §7 "The one waiver that campaign carried — a stale formula count in the CS-audience deck — was **discharged** when the deck was retired; **no waiver stands today**" | `1.5:184-187` | delete | — | closed-item story. The resolution ("no waiver stands") is the absence of a thing, not a fact needing a home. **Log-worthy #2** |
| 88 | §7 "A 2026-07-21 pass then re-audited the campaign's own bulk edits adversarially and corrected what they had broken" | `1.5:187-188` | delete | — | history. **Log-worthy #3** — and materially relevant: it is the pass that produced the D4 retag behind defect 6 |
| 89 | §7 "held there by two standing checkers covering twelve structural invariants and sixteen mechanical sweeps, plus a third tool that calibrates them — **58 probes** … plus a gate that refuses green if any check has no probe at all" | `1.5:188-192` | delete | — | tooling self-description; §8 rebuilds `check_the_checkers.py` against the new rule set, so every count here goes stale at cutover |
| 90 | §7 Seeded, σ-disciplined reference data; genuine gaps marked `GAP` and **refused by the certification layer rather than filled by guesswork** | `1.5:193-196` | move | `oracle/accuracy/reference-battery#gap-discipline` | the *rule* is live and load-bearing; the 2026-07-08 seeding date is history. `GAP` is overloaded three ways (`[traps] §59`) — plan §4 rename list |
| 91 | §7 Training feedstock: recovered **1,179-row** HSE06 gap-tuned strain hypersurface of diamond, **1,131 distinct** strain shapes after de-duplication, six lattice-distortion families to ±10%, with stress tensors, mapped onto the import interface | `1.5:197-204` | move | `oracle/accuracy/reference-battery#diamond-strain-hypersurface` | **live and load-bearing** — this is the MVP's ground-truth feedstock for mechanics, energetics, and gap-vs-strain, and `8.2:48-50` already depends on it for Cap-1's data-backed sensitivity test. No page in canon owns the dataset |
| 92 | §7 "the ~877 figure quoted earlier was the byte-salvage count, not the row count, and de-duplication must precede any fit" | `1.5:199-201` | delete | keep the de-dup rule only | correction-of-a-past-number is history; **"de-duplication must precede any fit" is a live methodological guard** → carry it to row 91's target. **Log-worthy #4** |
| 93 | §7 A concrete first build — three capabilities, ~34 formulas, 10 of 12 methods, obligations 1–6 and 10, written build order | `1.5:205-208` | delete | cite `[mvp-scope]`, `[build-order]` | duplicate of `8.3:17-33` |
| 94 | §8 The evolver question — "a live research question **we are commissioning as an independent deep-dive**. Until it resolves, time-evolution product verbs stay unclaimed" | `1.5:210-216` | delete | cite row 47 | **Contradiction C2** — `1.2:242-251` says the same research **resolved** ("survives with restrictions"). One page says commissioned, the other says resolved |
| 95 | §8 The wave program — β-Ga₂O₃ next, seeding spec "drafted and **frozen pending its adversarial audit**"; then c-BN/4H-SiC, contact metals and substrates, gate dielectrics | `1.5:217-220` | mine | `oracle/accuracy/reference-battery#wave-program` | cites `live/specs/2026-07-08-wave2-beta-ga2o3-seeding.md`, a deleted container. The **wave ordering** is live; the "frozen pending audit" status is **stale** — see Open question `beta-ga2o3-wave-status` |
| 96 | §8 Known unknowns kept visible — measured impact ionization for AlN, hole transport, hot-carrier tail anchors recorded as cert-refused domains | `1.5:221-224` | move | `oracle/certification/out-of-scope#known-data-gaps` | live refusals; `5.3-out-of-scope` owns "scope exclusions" and does not currently list them |
| 97 | §9 The close | `1.5:226-234` | delete | — | rhetorical summation; every claim restated from §3 and §7 |
| 98 | Diamond primitive cell: Fd-3m (No. 227), 2 C at 8a Wyckoff, sp³, a = 3.567 Å, 8 valence electrons → **4 occupied bands** | `8.1-mvp-system.md:16-18` | keep | `program/build/mvp-system#the-cell` | sole statement; the 4-occupied-bands consequence is load-bearing for Cap 2 |
| 99 | Anchor table **value column** — 5.47 eV, ~165 meV, ~2200 K, ~2200 W·m⁻¹K⁻¹, C₁₁≈1079 / C₁₂≈124 / C₄₄≈578 GPa | `8.1:20-28` | delete | cite `physics/library/cert/reference-data/*.csv` | verified duplication: every value matches `material-constants.csv:27-28`, `elastic-tensors.csv:17,19`, `phonon-frequencies.csv:16-17`, `transport-coefficients.csv:33`. **Circular provenance** — those CSV rows record their own provenance as "curated MVP anchor (mvp-system)" while this page states the numbers. Per the brief, values seed from the ledger and the CSVs, never the other way |
| 100 | Anchor table **consequence column** — CBM sits on Δ at ≈0.76 Γ→X, *not* at X, and the six-fold Δ valley degeneracy is what the effective-mass and transport rows consume; PBE −23% ⇒ G₀W₀/hybrid required; phonon grid must resolve 165 meV; Θ_D ~2200 K ⇒ QHA valid through ~800 °C ⇒ SCPH (row 13) deferred; non-polar ⇒ Z\*=0 ⇒ registry rows 17, 21, 22 excluded by applicability | `8.1:22-27` | keep | `program/build/mvp-system#mvp-consequences` | **the page's unique content.** Each consequence is a design decision derived from an anchor and stated nowhere else |
| 101 | High-T failure: air-oxidation onset ~600–700 °C is the actual lifetime limiter; graphitization only above ~1500 °C in vacuum; the diamond–graphite boundary is the Cap-1 thermodynamic check; oxidation is the slow-tier degradation channel | `8.1:28` | keep | `program/build/mvp-system#high-t-failure` | **load-bearing for `[traps] §43`**, which names `[mvp-system]` as its enforcement point (`10.4-traps.md:299-301`) |
| 102 | Units: atomic units internally; report eV, Å, W·m⁻¹K⁻¹, cm²V⁻¹s⁻¹ | `8.1:30` | move | `practice/conventions#units` | a corpus-wide convention stated on an MVP page; `10.1-conventions` owns conventions |
| 103 | Each capability is a strict selection from the closed vocabularies, residual categories, and cert obligations; formula numbers reference the registry manifest | `8.2-capability-slices.md:19-22` | keep | `program/build/capability-slices#selection-discipline` | the discipline statement; owner |
| 104 | Cap 1 table — state used, BO levels, 6 methods, 5 templates, 12 formulas (57/60/61/62/85/30/44/52/54/67/124), bundles, residuals, cert 1/2/3/5, implementation | `8.2:24-39` | keep | `program/build/capability-slices#cap-1` | sole statement of Cap-1's vocabulary selection |
| 105 | Cap 1 acceptance test — null (ground-truth diamond returns ≈0 within σ), sensitivity (the specific keys that fire name the violated law), data-backed sensitivity against the strain hypersurface | `8.2:41-50` | move | `program/build/build-verification#first-end-to-end-gate` | labelled "**first end-to-end gate**" yet it sits on the capability page while `build-verification` owns "verification gates" and does not mention it. Either move it or make 8.7 cite it — do not leave the gate set split across two pages |
| 106 | Cap 2 table — state, BO levels L1+L4, 4 methods, 5 templates, 13 formulas, bundles B1/B3, residuals, cert 1/2/5/6, implementation | `8.2:52-67` | keep | `program/build/capability-slices#cap-2` | sole statement |
| 107 | Cap 2 Excluded (non-polar): rows 17, 21, 22 masked off by the `is-polar-material` classifier (false for diamond) | `8.2:63` | keep | `program/build/capability-slices#cap-2` | restates row 100's polarity consequence. One owner needed — recommend the *rule* stays on `mvp-system`, the *masked row list* stays here |
| 108 | Cap 3 table — state, BO levels L2+L3+L4, 3 methods, 4 templates, formulas 7–12/25/121/122/70, deferred 13/26/27, bundles, residuals, cert 2/3/5 | `8.2:69-84` | keep | `program/build/capability-slices#cap-3` | sole statement |
| 109 | The QHA + Slack/Callaway κ is a **consistency pair, not an equivalence pair** — no agreement theorem, only a bounded model gap; obligation-6 trips on excess beyond `τ_method`, not on disagreement; calling it a method-equivalence would score a legitimate model gap as a bug | `8.2:84` | move | `oracle/seams/cross-cutting-rules#consistency-vs-equivalence-pairs` | a cross-cutting rule buried in a table cell on an MVP page; the cell itself cites `[cross-cutting-rules]` and `[traps] §29` |
| 110 | "In the MVP": ~34 formulas of 132 · 10 of 12 methods · 10 of 20 templates · bundles B1/B2/B3/B7/B10 primary with all 11 touched · six residual families · obligations 1–6 **and 10** · Layers 1 + 1.25 | `8.3-mvp-scope.md:17-34` | move | `program/build/capability-slices#mvp-totals` | **merge recommendation.** Every total is a summation over the three Cap tables in `8.2`. Two pages that must be edited together to stay correct are one page |
| 111 | "~34 named formulas (**the rows above**, incl. the κ high-T siblings 121–122 and the hull pair 67/124)" | `8.3:18-19` | delete | — | "the rows above" points at `8.2`'s tables — a pre-book pointer that broke when the monolith was split. Unresolvable as written; the merge in row 110 repairs it by construction |
| 112 | Obligation 10 (registration adjoint gate) **stays** in the MVP because D2 gradients must be validated when the PINO first trains; only 7–9 defer | `8.3:31-33`, `8.3:41-42` | keep | `program/build/capability-slices#mvp-totals` | stated twice on one page (In-MVP and Deferred both argue it) — collapse to one |
| 113 | "Deferred (the other ~⅔)": remaining ~100 formulas, defect zoo beyond row 30, surface chemistry, interface/Schottky, high-field/hot-carrier/breakdown, degradation, most of the topology atlas (rows 96–102) | `8.3:35-40` | move | `oracle/certification/out-of-scope#mvp-deferrals` | `5.3-out-of-scope` owns "scope exclusions" and already carries an overlapping deferral list |
| 114 | Deferred: "**Layer 1.75** (iterative dressing), SCPH/SSCHA, **the D4 surrogate nets**, the non-diamond materials, heterostructures beyond the single c-BN lattice-match" | `8.3:43-44` | move | `oracle/certification/out-of-scope#mvp-deferrals` | **stale vocabulary, second instance of defect 6.** The 2026-07-21 retag changed `D4` from *surrogate* to *relaxed*; "the D4 surrogate nets" names a set that no longer exists — the same failure the plan documents at `10.2-open-decisions` item 1, realized again here and not in the plan's inventory. `Layer-1.75` is on the plan §4 rename list |
| 115 | "The buildable unit is roughly one-third of the full vocabulary." | `8.3:46` | keep | `program/build/capability-slices#mvp-totals` | the one sentence on this page that is a judgment rather than a sum |
| 116 | **Implementation language (H1) — resolved.** Concrete needs met by a polyglot of DSLs: a compiler host for Stages 1–4 + the substrate that emits source for a separate runtime host (owning optional GPU codegen), plus an offline group-theory engine and an offline proof assistant. **The four-role shape is settled; the languages filling the roles are not** | `8.4-forced-decisions.md:21-29` | keep | `program/build/forced-decisions#implementation-language` | **owner — and this fact is stated four times.** Here, `8.6:24-30`, `10.2-open-decisions:83-90` (open item 6), `10.2-open-decisions:169-179`. Recommend this page owns it; the open picks become an `open-questions:` entry here (`implementation-language-picks`) |
| 117 | **TB-3NN-sp³d⁵ for carbon as warm-start initializer** — a seed for the SCF inner loop only; not a separately-evaluated formula and not an independent residual | `8.4:30-32` | keep | `program/build/forced-decisions#tb-warm-start` | owner. Restated three times: `8.2:39` (Cap 1), `8.2:67` (Cap 2), `8.5:24` (step 6). The "not a residual" qualifier appears **only here** and is the part that prevents a modelling error |
| 118 | **Layer-1.25 substrate data (H7)** — G₀W₀ needs ~30–50 unoccupied bands + wavefunctions; QHA needs volume-dependent (Grüneisen) phonons; these are the L1 outputs the MVP requires | `8.4:33-36` | keep | `program/build/forced-decisions#layer-125-substrate-data` | sole statement of the L1 output requirement |
| 119 | "— specify them when building `state/level-1`." | `8.4:36` | move | `oracle/state/born-oppenheimer-levels` `open-questions:` | a build-time TODO in prose; under D7 an unspecified requirement is an open question on the owning page |
| 120 | **Reference-battery seed (H4)** — the full diamond battery enumerated (lattice a, gap, C₁₁/C₁₂/C₄₄ + bulk modulus + density, Θ_D, max phonon energy, κ(300 K), **κ(773 K) ≈ 620 W/m·K**, κ(1100 K), cohesive energy, diamond–graphite boundary point, ε_r, Isberg ToF mobilities, v_sat/β, Chynoweth pair) | `8.4:37-43` | move | `oracle/accuracy/reference-battery#diamond-battery` | a battery inventory on an MVP page; `9.2-reference-battery` owns it and already cites back (`9.2:52` cites "`forced-decisions §H4`" — the dependency runs the wrong way). "seeded 2026-07-08" is history |
| 121 | **Design-grade accuracy targets (H8)** — gap ±0.15 eV post-G₀W₀, C_ij ±5%, κ(300 K) ±20%, E_form ±0.2 eV, μ factor-2; cert obligation 4 checks them at the battery anchors | `8.4:44-48` | delete | cite `[accuracy-ledger]` | the page says so itself: "full per-observable ledger in [accuracy-ledger]". Restating five tolerances beside their ledger is the drift class D7 exists to prevent |
| 122 | "the high-T anchors κ(773 K)/κ(1100 K) are **landed** (registry rows 121–122 …)" | `8.4:48-50` | delete | — | closed-item framing; the anchors are facts on the ledger and the registry, and the story of them landing is history |
| 123 | The `H1` / `H4` / `H7` / `H8` labels themselves | `8.4:21,33,37,44` | delete | renumber or drop | **a fourth identifier namespace, defined nowhere.** Only H1/H4/H7/H8 exist — H2/H3/H5/H6 appear nowhere in the corpus — yet they are cited across pages (`8.5:27` "`forced-decisions` H4"; `9.2:52` "`forced-decisions §H4`"). Worse, `H1` collides with a second series: `2.4-multiscale-state.md:198,434,436` uses `F-H1` for a formula from `deriv-high-field` Part H. Same overload class as `GAP` (`[traps] §59`) |
| 124 | "A focused subset of the phases in `build-sequence`" + the 9 MVP build steps (`core`, `shared`, `inputs`, `state`, methods+formulas, canonicals, capability residuals, cert+battery, validate) | `8.5-build-order.md:17-29` | move | `program/build/build-sequence#mvp-column` | **merge recommendation.** This page is a projection of `8.6`'s phase table filtered by `8.3`'s scope. Express it as an in-MVP column on the one phase table; the projection then cannot drift from its source, and the omission in C6 becomes impossible |
| 125 | Steps citing `(§2)`, `of §3`, `(Cap 1/2/3 rows above)` | `8.5:22,23,25` | delete | rewrite as `[page#anchor]` | three bare cross-page pointers into `8.2`/`8.1` that broke when the monolith was split; plan §4 removes bare ordinals |
| 126 | "Completing this slice yields a diamond-only `/physics` that can emit a granular residual vector with cotangents, expose observable values, and certify them for all three capabilities — the concrete substrate `/informed-operator` then trains against" | `8.5:31-34` | keep | `program/build/build-sequence#mvp-exit-criterion` | the MVP's exit criterion, stated only here. **Contradiction C6** — it requires phase 13 (`API seal + pino-bridge`), which the build order omits |
| 127 | `canonical-for: [build sequence]` — the page's own id with the hyphen spelled as a space | `8.6-build-sequence.md:6-7` | delete | replace with ≥1 distinct topic | second in-scope instance of the hyphen-variant vacuous claim; see row 59 and Notes |
| 128 | Intro restating the four-role polyglot language shape | `8.6:24-29` | delete | cite row 116 | duplicate of `8.4:21-29`, down to the same two citations |
| 129 | "The phases below are language-neutral: none of them depends on the pick, which is why the pick can stay open this long." | `8.6:29-30` | keep | `program/build/build-sequence#language-neutrality` | the justification for leaving `implementation-language-picks` open; stated here and paraphrased at `10.2-open-decisions:89-90` |
| 130 | The 14-phase build table (Phase 0 scaffold → Phase 13 API seal + pino-bridge), each with scope and verifiable artifact | `8.6:32-47` | keep | `program/build/build-sequence#phases` | **owner** — the full build plan for `/physics`; nothing else states it |
| 131 | Phase 7's **applicability-decidability gate** — every classifier first-order decidable on typeclass tags; non-decidable entries rejected | `8.6:41` | keep | `program/build/build-sequence#phases` | a hard admission rule stated in a table cell; cites `named-formulas` |
| 132 | Phase 8's Stage-2.5 invariant synthesis instantiating active `CouplingSpec` and attaching generated `InvariantTerm`s | `8.6:42` | keep | `program/build/build-sequence#phases` | cites `coupling-structure`; "Stage-2.5" inside a "4+1 stage" pipeline is on the plan §4 rename list |
| 133 | Recommended start order (1–7, then 8–10, then 11–12, then 13) | `8.6:49-51` | keep | `program/build/build-sequence#phases` | fold into the table as an ordering column rather than a second statement of the same sequence |
| 134 | §1 Internal consistency, items 1–7 — every observable invokes only registered methods/templates/formulas; typed signatures with no string-encoded parameters; the directory tree contains every named concept; nine regime extractions realizable as typed compositions; every residual category grounded; every cert obligation maps to a Layer-0 axis; counts match | `8.7-build-verification.md:19-36` | keep | `program/build/build-verification#static-consistency` | owner of the static gate set. **Every `§`-ordinal in it (`§2`, `§3`, `§4`, `§6`, `§7`, `§10`) points into the pre-book monolith and must be rewritten to `[page#anchor]`** |
| 135 | Item 3's requirement that the tree contain every concept named "in this plan and in `architecture.md`" | `8.7:27-28` | delete | repoint at the real owners | **`architecture.md` does not exist anywhere in the repo** (`find . -name architecture.md` → nothing). A live dangling reference in canon, invisible to the checker because it is backticked — probe B/C confirm the asymmetry. Same class as defect 2. "this plan" is a pre-book self-reference |
| 136 | Item 7's count restatement — 12 methods, 20 templates, 132 formulas, 11 bundles, 19 residual categories, 10 cert obligations | `8.7:35-36` | delete | cite `[canonical-vocabularies]` | **unswept duplication.** Probe C: corrupting "132 formulas" here to "999" leaves the checker green, because the count sweep requires the adjacent phrase "substantive formulas". Restating six guarded numbers in an unguarded phrasing is a drift generator |
| 137 | "Once the Phase-0 skeleton exists, items 1–7 are checkable mechanically by walking the tree and the registry manifest." | `8.7:38-39` | keep | `program/build/build-verification#static-consistency` | the mechanization claim; the point of the whole section |
| 138 | Gate 1 Registration sanity — all 132 formulas instantiate; every `D2` **and** `D3` passes the registration-time adjoint gate; `D3` additionally passes the fixed-point-Jacobian conditioning check; `D4` carries an obligation-9 rationale naming its relaxation; `D0`/`DN` register without an adjoint; `D1` without a gate | `8.7:45-51` | keep | `program/build/build-verification#gate-1-registration` | owner of the per-D-tag registration rules. **Cross-scope trap:** this is the live legend; `11.8-deriv-generator-catalog` uses a retired one in which `D3` and `D0` mean something else (plan §2). Anyone mining 11.8 into this must not seed the tags from there |
| 139 | Gate 1 **Fidelity pairing** — every generator whose lowering introduces representation error registers its paired fidelity generator; a missing pairing **fails the build**, not a warning | `8.7:51-58` | keep | `program/build/build-verification#gate-1-registration` | the enforcement statement for `residual-machinery §4.1`; cites `[traps] §64` and `§58` (a build that passes without checking looks like a build that checked) |
| 140 | The adjoint-tape materialization schedule is the one lowering **exempt** by construction — it changes cost, not value | `8.7:58-61` | keep | `program/build/build-verification#gate-1-registration` | the sole stated exemption; without it the pairing rule is over-broad |
| 141 | Gate 2 End-to-end worked example — Diamond–W Schottky at 500 °C, `Environment(T = 773 K, field = 1 MV/cm)`; L3 ↔ non-equilibrium cycle closes by same-pass fixed point in ≤ 5 iterations; ~three dozen residuals accounted for; obligations 1, 2, 3, 5, 8 emit verdicts | `8.7:62-69` | keep | `program/build/build-verification#gate-2-worked-example` | owner. Partially restated at `4.4-computational-overview.md:529` — cross-scope with the compilation surveyor |
| 142 | Gate 3 Curriculum sanity — "a **three-phase** training run on Si bulk (~5 observables, ~1k samples) completes without GradNorm divergence, without a Layer-3 ↔ non-equilibrium fixed-point failure, and without an adjoint-cert reset mid-training" | `8.7:70-73` | keep | `program/build/build-verification#gate-3-curriculum` | **Contradiction C7** — `7.2-residual-machinery.md:218` says three phases (Warmup → Refine → Polish); `11.8-deriv-generator-catalog.md:332` says four (Warmup \| Refine \| Calibrate \| Polish) and `11.8:447` schedules work at a "Refine→Calibrate" boundary. Also operator-side content on a `/physics` build page — cross-link `operator/training` |
| 143 | Gate 4 Cross-regime cert obligations fire — obligation-6 (BTE-σ ≡ Kubo-σ), obligation-9 (out-of-domain D4 trips with a witness), obligation-10 (broken adjoint refused at registration), obligation-7 (diamond emits NA with rationale; a contrived Z₂ system emits the predicted edge-state count) | `8.7:74-79` | keep | `program/build/build-verification#gate-4-obligations` | sole statement of the obligation-firing tests |
| 144 | Gate 5 `/informed-operator` integration smoke test — `Validate` with `gradient = Skip` populates labels for ~10 Si observables; with `Compute` returns finite scalars and cotangents of the declared shape on a randomly-initialized state; `Import` accepts a synthetic VASP-formatted payload and returns `GroundTruthBridgeGenerator`s with coverage masks | `8.7:80-86` | keep | `program/build/build-verification#gate-5-seam-smoke-test` | sole statement of the seam acceptance test; the only place a VASP-formatted `Import` payload is named as a build requirement |

---

---

### from `practice`

| # | Fact or block | Current location | Disposition | Target | Evidence |
|---|---|---|---|---|---|
| **10.1-conventions** | | | | | |
| 1 | Frontmatter: `authority: canon`, `content-hash`, `referenced-by` | `10.1-conventions.md:1-19` | delete | — | D6/§4 delete all three identifiers corpus-wide; `referenced-by` is regenerated into `corpus.json` |
| 2 | `canonical-for: [conventions]` | `10.1-conventions.md:6-7` | delete | — | vacuous ownership (one of the 18); replaced by the `owns:` topics in rows 6–20 below |
| 3 | Intro: "Pages are the source of truth; the apparatus is generated from their frontmatter" | `10.1-conventions.md#(untitled intro)` | move | `practice/agent-contract#source-of-truth` | rule about how an agent treats the tree, not a corpus convention; D11 restates it |
| 4 | Intro: "The **authority order** … govern the whole base" | `10.1-conventions.md:25-26` | delete | — | authority order dies with the single-tier restructure (D3) |
| 5 | `## Authority order` — the 7-tier ranked list | `10.1-conventions.md#authority-order` | delete | — | D3: `authority: canon\|supporting` is deleted; with ch. 11 dissolved there is no second tier and no ranking to state |
| 6 | Tier 1 as a *rule* — "the page whose frontmatter claims the topic is the single source of truth for it; topics are globally unique and machine-checked" | `10.1-conventions.md#authority-order` item 1 | move | `practice/agent-contract#topic-ownership` | survives the tier collapse as the whole anti-duplication invariant. This is the one tier that is not a tier |
| 7 | Registry CSV is canonical for formula rows/names/signatures/cost/diff tags; reference-data CSVs canonical for seeded coefficient values | `10.1-conventions.md#authority-order` items 2–3 | move | `practice/agent-contract#data-outranks-prose` | still true and still load-bearing: data files survive the restructure and outrank the pages that quote them |
| 8 | **"Seed from the ledger and the reference CSVs, never from an appendix derivation"** | `10.1-conventions.md#authority-order` bullet 1 | move | `practice/traps#practice` (trap 46) | the appendix ceases to exist, so the prohibition's object dies; the positive half ("seed from the ledger and the CSVs") survives and is already trap 46. State once, in traps |
| 9 | "Generated outputs are never edited or audited" (`contents.md`/`index.md` emitted; `--check` gates freshness) | `10.1-conventions.md#authority-order` bullet 2 | move | `practice/agent-contract#generated-artifacts` | survives verbatim in intent; the artifact list changes to `corpus.json` + emitted `contents.md`/`index.md` (§6) |
| 10 | "`glossary.md` is **hand-maintained**, not generated — checked only for duplicate entries" | `10.1-conventions.md:53-55` | delete | — | see rows 82–84: the glossary becomes emitted. The sentence is a description of the thing being replaced |
| 11 | Frozen-material rule: `live/audits/` frozen, `live/specs/` not frozen, specs track current truth | `10.1-conventions.md#authority-order` bullet 3 | delete | — | both containers are demolished (D13: audits stratum deleted, specs stratum superseded). Nothing frozen remains to state a rule about. **See Contradictions row 3** — the rule is currently contradicted by the tool that cites it |
| 12 | "Strata are rewritten to current truth, with the superseded value preserved in a per-file `## Changelog`" | `10.1-conventions.md:63-65` | delete | — | D1: no changelogs. This sentence is the *charter* for the scaffolding the restructure removes |
| 13 | `## Naming is addressing` — registry names are hash-consed into content addresses; a post-landing rename is a substrate-wide rekey; **fix names before they land** | `10.1-conventions.md#naming-is-addressing` ¶1 | move | `practice/traps#practice` (trap 47) | duplicated verbatim as trap 47 today. One statement, in traps; `practice/conventions` cites it |
| 14 | The `retired-names.csv` forwarding mechanism and the same-paragraph resolution rule | `10.1-conventions.md#naming-is-addressing` ¶2 | delete | — | §9 deletes `retired-names.csv` (62 rows). The mechanism has no referent after cutover. **Ordering hazard — see Notes** |
| 15 | Eponym rule: rename when the name misdescribes, not because it is an eponym; test is "could a reader bind the name to the wrong object"; person goes in the `source` cell as `a.k.a.` | `10.1-conventions.md#naming-is-addressing` ¶3 | move | `practice/conventions#naming` | a live naming test, and the one that governs the §4 vocabulary renames Javier must approve |
| 16 | The two worked eponym renames (`vibronic-coupling-factor` →, `field-activated-ionization-rate` →) | `10.1-conventions.md:83-91` | delete | — | rename history; the *reason* each was renamed is the rule in row 15, which survives without the case studies. Log-worthy row L14 |
| 17 | `## File layout` — one canonical topic per page; chapter folders; `<chapter>.<n>` display tag; "the path is the record" | `10.1-conventions.md#file-layout` ¶1 | move | `practice/agent-contract#page-addressing` | rewritten: chapters and tags die (D6), but "one subject per page" and "the path is the record" become "filename is the id" |
| 18 | `pages/11-appendix-derivations/` holds derivations at `authority: supporting` | `10.1-conventions.md:104-106` | delete | — | D3: chapter 11 ceases to exist |
| 19 | `journal/live/` holds work products; on finishing, conclusions promote into canon and the artifact collapses into `[timeline]` | `10.1-conventions.md:107-109` | move | `practice/agent-contract#where-work-products-go` | the *container* dies but the **promote-then-collapse discipline** is the mechanism that keeps research from rotting in place — Javier's stated core pain. Restate against `log/timeline.md` |
| 20 | "`instructions.md` is the entry point an agent reads first, so it is the file whose staleness costs the most" | `10.1-conventions.md:111-114` | move | `practice/agent-contract#preamble` | true of `practice/agent-contract` in the new tree, and the justification for it being hard-gated (D8) |
| 21 | `## Frontmatter (mandatory)` — the YAML template | `10.1-conventions.md#frontmatter-mandatory` code block | delete | — | superseded wholesale by the plan's §4 schema (strict YAML, fixed key set, `owns`/`anchors`/`open-questions`) |
| 22 | "Five fields were removed on 2026-07-22 …613 of 1,517 lines said nothing the tools could not derive" | `10.1-conventions.md:135-142` | delete | — | pure history of a prior cleanup. Log-worthy row L11 |
| 23 | `id` is a permanent address, globally unique, cited as `[id]`; **ids are descriptive phrases, never serials** | `10.1-conventions.md#frontmatter-mandatory` field rule 1 | move | `practice/agent-contract#ids` | D6 keeps the substance and drops the indirection: filename *is* the id |
| 24 | The 38 `<tree>-NN-` renames and the `retired-ids.csv` map | `10.1-conventions.md:150-156` | delete | — | §9 deletes `retired-ids.csv`; the rename is history. Log-worthy row L10 |
| 25 | **"Prefer a two-word phrase to a bare common noun"** — `unified-state` not `state`, because a bracketed common noun is indistinguishable from a page reference | `10.1-conventions.md:157-162` | move | `practice/agent-contract#ids` | a live, non-obvious naming constraint that follows directly from the citation syntax. Keep; drop the parenthetical about it tripping its own check |
| 26 | `canonical-for` uniqueness — every topic appears in exactly one page's list, machine-enforced | `10.1-conventions.md` field rule 2 | move | `practice/agent-contract#owns` | becomes `owns:`, plus the new constraint that no topic may equal the page id (fixes defect 4) |
| 27 | `depends-on` is authored; empty lists written `[]` | `10.1-conventions.md` field rule 3 | move | `practice/agent-contract#depends-on` | survives as-is |
| 28 | `referenced-by` is generated, never authored; the 381-entry hand-kept mirror and its symmetry check | `10.1-conventions.md` field rule 4 | delete | — | the field is deleted (row 1); the *principle* it demonstrates is row 33 |
| 29 | **`depends-on` edge criterion** — `A depends-on B` iff A's body references B's id **or** A uses a term whose owner is B; first half machine-derived, second authorial | `10.1-conventions.md` field rule 5 | move | `practice/agent-contract#depends-on` | load-bearing and machine-implemented; the one-syntax change (§8) makes the derived half total |
| 30 | Rationale for the authorial half — "a topic string like 'conventions' is an ordinary English word… a checker that cries wolf gets ignored" | `10.1-conventions.md:178-181` | move | `practice/agent-contract#depends-on` | this is *why* the rule is half-authored; without it a Phase-2 builder will try to fully mechanize it and reintroduce the noise. Compare trap 69 |
| 31 | **Citable body** definition — prose minus fenced code, minus `## Changelog`, minus blockquote banners; `[timeline]`/`[traps]` exempt both directions | `10.1-conventions.md` field rule 6 | move | `practice/agent-contract#citable-body` | survives, **minus the changelog clause** (defect 3: that exemption is the unchecked region). The timeline/traps exemption survives — both remain corpus-wide indexes |
| 32 | **"The page graph is cyclic, and that is correct… nothing may compute a transitive closure over `depends-on` and read it as a build order"** | `10.1-conventions.md` field rule 7 | keep | `practice/agent-contract#depends-on` | sole statement of a rule that an agent given `corpus.json` will otherwise violate on sight. Restated in `instructions.md` §3.4 — collapse the two into this one |
| 33 | **"An invariant held by construction beats one held by a checker"** | `10.1-conventions.md:291-293`, restated `instructions.md:163` | move | `practice/agent-contract#enforcement-philosophy` | stated twice today; the design principle behind D6, D8 and the whole §8 hole list |
| 34 | Cross-references use the `id` inline or with a section coordinate; coordinates must resolve; dated anchors need a disambiguating parenthetical | `10.1-conventions.md#anchor-and-cross-reference-style` bullet 1 | move | `practice/agent-contract#citations` | rewritten to the single anchor-carrying syntax `[page#anchor]` (§4). The dated-anchor rule dies with `§`-ordinals but its *cause* (three entries share 2026-06-10) recurs in `log/timeline.md` — see Notes |
| 35 | Section numbers are bare ordinals under the id, deliberately not derived from the display tag | `10.1-conventions.md` bullet 2 | delete | — | §4: bare `§N` ordinals are gone, replaced by declared anchors |
| 36 | **Never link by file path** | `10.1-conventions.md` bullet 3 | keep | `practice/agent-contract#citations` | still the rule; the reason ("so file moves don't rot references") is stronger under a flat journal tree |
| 37 | Id-less targets (files outside `pages/`) are cited by path + stable heading anchor | `10.1-conventions.md` bullet 4 | move | `practice/agent-contract#citations` | survives with a shrunken target set: the data CSVs, `log/timeline.md`, `tools/`. `journal/live/` and `journal/tools/` drop out |
| 38 | **Never by line number** — line refs rot on every edit; frozen audits exempt | `10.1-conventions.md` bullet 5 | keep | `practice/agent-contract#citations` | machine-checked; the exemption clause dies with `live/audits/` |
| 39 | **Count phrasing is canonical** — the six whole-vocabulary phrasings, strictly checked; subsets must be spelled out or "10 of the 20" | `10.1-conventions.md` bullet 6 | keep | `practice/conventions#counts` | a live machine-checked writing rule that survives the restructure intact and prevents a recurring defect class (traps 67) |
| 40 | `## Style` — American English; one subject per page; keystone pages may own several topics ("that is cohesion, not drift") | `10.1-conventions.md#style` bullets 1–2 | keep | `practice/conventions#style` | the cohesion clause is the licence for a page to carry >1 `owns:` topic — required reading given defect 4's fix |
| 41 | Length: canon ~250 lines, keystones may exceed; appendix exempt; "the earlier flat rule is retired: the corpus violated it fifteen times" | `10.1-conventions.md:247-251` | move | `practice/conventions#style` | keep the target and the keystone exception; **delete the retired-rule sentence** (scaffolding) and the appendix exemption (no appendix) |
| 42 | ATX headings only; bare code fences for pseudo-syntax, tagged for real code; no decorated separator comments | `10.1-conventions.md#style` bullets 4–6 | keep | `practice/conventions#style` | mechanical, still true, cheap to check |
| 43 | `## The checkers` — the 12 numbered `check_book_structure.py` failure modes | `10.1-conventions.md#the-checkers` list | move | `practice/agent-contract#what-the-checkers-enforce` | rewritten against the new rule set (§8 closes six holes; items 11 and 12 die with `authority` and `content-hash`). Duplicated verbatim at `instructions.md:140-155` — **collapse to one** |
| 44 | "Three checks that used to be on this list are gone, along with what they policed" (chapter/tag/status/`referenced-by`) | `10.1-conventions.md:286-293` | delete | — | history of a checker cleanup. The surviving sentence is row 33 |
| 45 | "The hash is tool-maintained… a stale hash means a page was hand-edited without regenerating" | `10.1-conventions.md:295-299` | delete | — | `content-hash` is deleted (D6); git answers the question it answered |
| 46 | **"Green means the graph is sound. It does not mean the physics is right"** | `10.1-conventions.md:301-303` | move | `practice/agent-contract#what-green-means` | the scope statement for the whole checking apparatus. Its pointer to `[audit-prompt]` re-points to the external auditor brief (row 60) |
| 47 | **"Green does not mean a check ran"** + `check_the_checkers.py`, 58 probes, the three failure modes (missed / stale / uncovered) | `10.1-conventions.md:305-324` | move | `practice/agent-contract#what-green-means` | §8 keeps the three-mode design explicitly. Duplicated at `instructions.md:186-199` and at trap 58/66 — **four statements of one rule; collapse to traps + one contract pointer** |
| 48 | The 2026-07-22 "one defect per check was false of thirteen classes" incident narrative | `10.1-conventions.md:317-324` | delete | — | incident history; the durable rule is trap 66. Log-worthy row L12 |
| 49 | "Two of the 52 plant a *location* rather than a corpus defect" — the scratch-worktree refusal probes | `10.1-conventions.md:309-311` | move | `practice/agent-contract#what-green-means` | the **fact** (a checker must refuse to run from a scratch copy, because two identical copies of this book once sat on disk both green) is live and is `corpus_root.refuse_if_scratch_copy`. **The number 52 is stale — see Contradictions row 1** |
| 50 | `check_data_agreement.py`'s sweep list (row bands, backticked names, `formula =` args, duplicate literals, glossary divergence, retired names, case variants, `D4` source cells, declared-gap drift, glossary pointers, `name (row N)`, refdata uncertainty/source/arity/dates) | `10.1-conventions.md:326-344` | move | `practice/agent-contract#what-the-checkers-enforce` | rewritten against the rebuilt tool. The arity rationale ("an unquoted comma shifts every column right of it… one row shipped for a month with `Added` = `experimental`") is a live *design* argument — keep it, drop the date |
| 51 | "Its surface is wider than the book… `informed-operator/design/` consumes this vocabulary too" | `10.1-conventions.md:341-344` | move | `practice/agent-contract#sweep-surface` | still true and now more so: `operator/` is a first-class journal (D4). The clause about a stale D-tag note going stale is history — delete |
| 52 | **Short symbols are written in backticks** — ` `D1` ` is a diff tag, `D1` unbacked is a wurtzite deformation potential | `10.1-conventions.md:346-350` | move | `practice/traps#practice` (trap 56) | duplicated at trap 56; traps owns it |
| 53 | "`DX` was renamed to `DN` because it collided with the DX center" | `10.1-conventions.md:348-350` | delete | — | rename history; the live rule is "write short symbols in backticks". Log-worthy row L10 |
| 54 | "`lint.py` no longer exists. Its unique checks were folded into `check_book_structure.py`" | `10.1-conventions.md:352-355` | delete | — | tombstone for a file deleted months ago. Log-worthy row L13 |
| **10.2-open-decisions — dissolves entirely** | | | | | |
| 55 | Frontmatter + `canonical-for: [open decisions]` + 21 `depends-on` + 16 `referenced-by` | `10.2-open-decisions.md:1-47` | delete | — | D7: the register is emitted from per-page `open-questions`. **Note: "open decisions" differs from the id `open-decisions` only by a hyphen — a 19th vacuous ownership the plan's exact-match list of 18 misses** |
| 56 | "The architecture above is committed. These remain to be decided." | `10.2-open-decisions.md:50` | delete | — | framing sentence for a page that ceases to exist |
| 57 | Item 1 — surrogate-net build vs adopt, "for the D4 surrogate formulas" | `10.2-open-decisions.md:52` | move | open-question on `oracle/registry/named-formulas` | **confirmed defect 6**: the 2026-07-21 retag made `D4` mean *relaxed*, not *surrogate*, so the item governs a set that does not exist. Open question `surrogate-net-build-vs-adopt`; needs Javier |
| 58 | Item 2 — PDE-mesh adjoint scheme; mesh *format* closed as a `DeviceMesh` finite-volume `Universe` | `10.2-open-decisions.md:53-57` | move | open-question on `oracle/state/multiscale-state#macro-tier`; the closed *format* is already stated at `multiscale-state §7` | `pde-mesh-adjoint-scheme`. `2.4-multiscale-state.md:276,475` already discuss it and already carry the (correct) "open item 2" ordinal |
| 59 | Item 3 — the four struck-through γ̂ questions + "Closed 2026-07-21" + "Kept struck through rather than deleted so the list a reader sees matches the history" | `10.2-open-decisions.md:58-70` | delete | — | D1: strikethrough history. The four *resolutions* already live at `gamma-hat §4`, `representation-substrate §4.1`, `compose-time-pipeline §3` and `§4`. Log-worthy row L7. The final sentence is the corpus explicitly arguing for scaffolding — the exact practice D1 ends |
| 60 | Item 4 — Layer-1.75 minimum spec for a V2 contributor (self-consistent GW / DMFT) | `10.2-open-decisions.md:71-72` | move | open-question on `oracle/state/born-oppenheimer-levels#dressing-tiers` | `layer-175-minimum-spec`. That page owns "dressing tiers (V1 vs V2 scope)". `Layer-1.75` is one of the §4 names to propose renaming |
| 61 | Item 5 — semiconductor-interface applicability predicate; row 116 gated on `is-noncentrosymmetric` was refusing trap density for every centrosymmetric host including diamond; carries an always-true stub | `10.2-open-decisions.md:73-82` | move | open-question on `oracle/certification/applicability-classifiers` | `semiconductor-interface-predicate`. The *defect it records* (row 116's wrong gate, now stubbed) is a present-tense fact about a live registry row — it must land on the page, not only in the question |
| 62 | Item 6 — which language fills each of the four polyglot roles; the *shape* is closed, the picks are not; "every mention of a language elsewhere is a candidate, never a mandate" | `10.2-open-decisions.md:83-91` | move | open-question on `program/build/forced-decisions` | `implementation-language-picks`. **Cross-reference defect: four sites call this "open item 5"; it is item 6** — see Contradictions row 2 |
| 63 | Verifier-soundness gaps preamble — "named, not silently absent… an absent check reads exactly like a passing one, and a *silently* closed one reads like it was never there" | `10.2-open-decisions.md:93-97` | move | `practice/agent-contract#open-questions` | this is the *justification for D7* stated by the corpus itself, in one sentence. Keep the first half; the "silently closed" half is an argument for keeping closure stories and is superseded by D2 (the log holds them) |
| 64 | Gap 1 — **no post-registration adjoint-drift monitoring**; the gate runs once at registration against the formula's adjoint, Stage 4 synthesizes the composition's adjoint later over a rewritten graph, nothing revalidates it | `10.2-open-decisions.md:99-107` | move | open-question on `oracle/seams/residual-machinery#registration-gate` | `adjoint-drift-monitoring`. Still open. This is the plan's own §5 worked example |
| 65 | Gap 2 — struck "No Stage-3 rewrite-soundness contract" + Closed 2026-07-21 | `10.2-open-decisions.md:108-112` | delete | — | closure story. The **resolution** (rewrite-admission rule: exact over ℝ, float side conditions via e-class interval/not-equals analysis, registered fidelity generator) is already normative at `compose-time-pipeline §3`. Log-worthy L8 |
| 66 | Gap 3 — struck "No declared validity radius for frozen Layer-1.25 dressings" + closure | `10.2-open-decisions.md:113-121` | delete | — | closure story. Resolution facts (`‖Δx‖·‖∂(dressing)/∂x‖_ref`; `OneShotCert` gains the coefficient field; the product enters `combineTol` as the dressing-staleness term) are present-tense and belong on `oracle/seams/residual-machinery#oneshotcert` — verify they are actually stated there |
| 67 | Gap 3 residue — **G₀W₀ cost is not scope-tagged by cell size**; at MVP a T2 budget is plausible, at V1 defect supercells it is hours, so the tier table is violated silently rather than refused | `10.2-open-decisions.md:122-125` | move | open-question on `oracle/registry/named-formulas` (cost tiers) | `g0w0-cost-scope-tag`. Explicitly still open and separable; a *cost* claim the ε rule does not reach |
| 68 | Gap 4 — struck "Gradient error under truncated inner solves is unbudgeted" + closure | `10.2-open-decisions.md:126-131` | delete | — | closure story. `τ_cond` and `τ_trunc` are live guards; verify they are stated at `oracle/certification/cert-obligations#tolerance-ledger` and `oracle/seams/residual-machinery` |
| 69 | "The conditioning guard on implicit-function adjoints… closed on 2026-07-21: `τ_cond`, gated at registration for every `D3` row" | `10.2-open-decisions.md:133-135` | move | `oracle/certification/cert-obligations#tolerance-ledger` | the **guard is a present-tense fact** (D2 exception 1). Delete the "closed on" framing, keep "τ_cond is gated at registration for every `D3` row". Duplicated as trap 60 |
| 70 | **Unregistered composition formulas** — 18 names invoked by `typed-compositions` that are not registry rows; declared in that page's `unregistered-formulas` frontmatter and machine-checked; two tranches (≈half definitional, the rest needing literature); "deliberately not registered on thin provenance" | `10.2-open-decisions.md:137-152` | move | open-question on `oracle/registry/typed-compositions` | `unregistered-composition-formulas`. Genuinely open, with a live machine-checked declaration mechanism that must survive the frontmatter schema change. The refusal principle ("a row must be defensible against a citation") is a keep |
| 71 | The clause "Four invocations *used to* carry inline mathematics… they now carry their declared names" | `10.2-open-decisions.md:148-152` | delete | — | history marker. The live rule is `named-formulas` forbidding inline mathematics |
| 72 | `## Closed decisions` — multiscale state (three tiers, aging is core `/physics`, refined emergence axiom, moment closure, two EOM categories, no new method) | `10.2-open-decisions.md#closed-decisions` bullet 1 | delete | — | **duplication**: every clause restates `multiscale-state`, which owns all five topics (`index.md:83,93,97`). The resolution is already at its owner |
| 73 | Closed — implementation-language **shape**: four roles (compiler host owning Stages 1–4 + substrate emitting source; separate JIT runtime host; offline group-theory engine; offline proof assistant) | `10.2-open-decisions.md#closed-decisions` bullet 2 ¶1 | move | `program/build/forced-decisions#language-roles` | settled decision, present tense, and the only place the *four roles* are stated now that `deriv-language-study` is deleted. **The homeless-fact risk here is high** |
| 74 | The four candidate picks (Haskell/OCaml, Julia, GAP, Lean 4) **with the requirement each was chosen to satisfy** | `10.2-open-decisions.md:184-204` | move | `program/build/forced-decisions#language-roles` | the *requirements* are the durable part by the page's own argument; the picks are evidence for `implementation-language-picks`, not commitments. Preserve the requirement text, mark the names as candidates |
| 75 | Hardware: CPU-dominant compiler; GPU is a Stage-5-only per-composition option (symmetry quotient yields small irrep blocks — a GPU anti-pattern — so the diamond MVP often runs fastest on CPU); polyglot conditional on a Stage-4→Stage-5 differential golden test; Rust excluded by preference; Python+JAX disqualified (its tracer owns differentiation) | `10.2-open-decisions.md:206-212` | move | `program/build/forced-decisions#hardware-and-host` | present-tense engineering decisions with reasons. The JAX disqualification is the one that constrains implementation most and is stated nowhere else |
| 76 | Closed — **integrator interface (`dynamics`)**: per-tier evolver hand-off = causalized tangent kernel + steppable-form manifest, not an integrator; the ten manifest fields | `10.2-open-decisions.md#closed-decisions` bullet 3 ¶1 | move | `oracle/laws/generic-dynamics#evolver-handoff` **or** `operator/seam/` — see Notes | a large present-tense contract with no obvious owner in the target tree. **Flagged: this is the biggest un-homed block in my scope** |
| 77 | **Encoding validity domain** (added 2026-07-21) — the manifest declares the encoding each block was compiled against and when it stops being fair (`CompressionPlan` slot, rank, truncation target); "exporting the problem is legitimate; exporting it silently is not" | `10.2-open-decisions.md:226-233` | move | same target as row 76 | present-tense contract clause. Delete only the "(added 2026-07-21)" stamp |
| 78 | The `conserve \| bound \| monotone` obligation map matching the robust dynamical-low-rank literature; the rank-adaptive BUG integrator (Ceruti–Kusch–Lubich 2022) named as the family; error bounds independent of small singular values (Ceruti–Lubich 2022; Kieri–Lubich–Walach 2016); "naming it is a declaration, not an implementation obligation" | `10.2-open-decisions.md:235-254` | move | same target as row 76 | live, cited, physics-bearing, and the justification for score-not-solve surviving an evolver hand-off. **Do not lose the citations** |
| 79 | "Residue: the full lowering spec … lands as its own named wave — slow tier first — and no time-evolution product verb is claimed until it does" | `10.2-open-decisions.md:257-259` | move | open-question on the row-76 target | `evolver-lowering-spec`. A live scope constraint on `program/purpose/product`'s claims, not just a to-do |
| 80 | Closed — `ReferenceCache` backend = `SqliteReferenceCache`; coverage-mask = `RoaringCoverageMask`; curriculum schedule `0.10/0.60/0.90`; active-learning loop in `/interface`; applicability decidability first-order on typeclass tags | `10.2-open-decisions.md:260-269` | delete | — | **five-way duplication**: each restates a fact its owner already states (`cert-obligations §2`, `pino-bridge §2.1`, `residual-definitions §4.1`, `purpose-and-scope`, `named-formulas` + `build-sequence` Phase 7). The page's own citations prove it |
| 81 | Closed — coupling structure; coupling-channel coverage policy; representation substrate; PhysicsGraph identity; sidecar/evidence relationship; no solver-call hot paths | `10.2-open-decisions.md:270-311` | delete | — | same: six restatements of facts owned by `coupling-structure`, `representation-substrate`, `physics-graph`. **Auditor 2 should diff these summaries against their owners before deletion** — a summary that drifted is a contradiction, not a duplicate |
| **10.3-audit-prompt — leaves the corpus** | | | | | |
| 82 | Frontmatter (`canonical-for: [audit-prompt]`, vacuous) | `10.3-audit-prompt.md:1-14` | delete | — | the artifact stops being a page |
| 83 | Preamble — senior computational-physics auditor role; large token budget spent on verification not rediscovery; **"the commissioner is a computer scientist, not a physicist, and is deliberately deferring physics-correctness judgment to you. Do not defer back or hedge"** | `10.3-audit-prompt.md:17-19` | mine | `outside` → `restructure/auditor-brief.md#role` | the single most reusable paragraph in the file and the operating instruction for auditors 2 and 3 |
| 84 | §1 grounding — `/physics` is a pure oracle that scores rather than simulates; GENERIC form; 4+1 pipeline with the five stages named; representation substrate over five primitives; the 7-tuple; γ̂ is feasibility-critical | `10.3-audit-prompt.md#1-what-physics-is-grounding` | delete | — | a summary of six pages that each own their part (`generic-dynamics`, `compose-time-pipeline`, `representation-substrate`, `unified-state`, `gamma-hat`). Regenerate from `corpus.json` when the brief is next built |
| 85 | §2 — the two non-negotiable design commitments: **utmost accuracy without expensive numerics** ("accuracy is to be earned through structure, not brute force") and **largest possible canonical structure, then specialize down** | `10.3-audit-prompt.md#2-the-core-objective-and-the-design-philosophy` | mine | `program/purpose/architectural-principles#design-commitments` | **this is not audit scaffolding — it is the project's design charter, and it is stated here more sharply than anywhere else.** Check against `architectural-principles` for drift before merging |
| 86 | §2's central-tension paragraph — DFT/GW/BTE/phonons/SCF are normally expensive; where does the spec genuinely achieve closed-form accuracy and where does it quietly depend on an iterative solver | `10.3-audit-prompt.md:40` | mine | `outside` → `restructure/auditor-brief.md#part-1` + a pointer from `program/purpose/architectural-principles` | the sharpest statement of the project's central risk; belongs in both places, stated once and cited once |
| 87 | §3 directory map and recommended reading order | `10.3-audit-prompt.md#3-directory-map-research-file-guide` | delete | — | **substantially rotten and wholly derivable**: it maps chapters 1–11 that cease to exist, calls `physics/library/` an "EMPTY SCAFFOLD", and says `informed-operator/`, `interface/` are "mostly empty" — false under D4. Emit the replacement from `corpus.json` |
| 88 | §3's directive: "Read `pages/11-appendix-derivations/` as the physics ground truth and compare it against what landed in canon" | `10.3-audit-prompt.md:87` | delete | — | the comparison target is being deleted (D3). After Phase 3 there is no second stratum to diff against — **this is a real capability the restructure removes; see Notes** |
| 89 | §4 Part 1 — the physics-completeness question set (nine regimes, target observables, residual categories, coupling channels, regimes of validity, the GENERIC bet, the accuracy-vs-cost frontier, material generality) | `10.3-audit-prompt.md#part-1-the-physics` | mine | `outside` → `restructure/auditor-brief.md#part-1` | auditor 2/3 input. **Contains a nonexistent id — see Contradictions row 4** |
| 90 | §4 Part 2 — the structures question set (canonical-structure/specialize-down judgment, efficiency and the `O(log n)` claim, elegance, operad coverage, the two sui-generis exceptions) | `10.3-audit-prompt.md#part-2` | mine | `outside` → `restructure/auditor-brief.md#part-2` | same |
| 91 | §4 Part 3 — the eight seeded questions (end-to-end error model; the closed-form discipline's true boundary; differentiability completeness; residual sufficiency for learning; GENERIC structural guarantees; numerical stability of ε-vs-exact identity; the three formalization gaps; open-ended) | `10.3-audit-prompt.md#part-3` | mine | `outside` → `restructure/auditor-brief.md#part-3` | Q1 (composed error budget) is called "the linchpin of the whole design bet" — that judgment is worth carrying. **Q3 carries the `D0\|DN\|D1\|D2\|D3\|D4` warning that `DN` is not inside `D0–D4`** — §4 names this as a nomenclature defect to fix |
| 92 | §5 ground rule — "Verify against the documents; cite `arch-xx §y` / `impl-xx` / `physics/research/<file>` for every finding" | `10.3-audit-prompt.md:125` | delete | — | **all three citation formats are dead**: `arch-`/`impl-` are retired id prefixes; `physics/research/` still exists but now holds the diamond VASP strain-sweep dataset, not the physics research files. Replace with the one anchor-carrying syntax |
| 93 | §5 ground rule — "Compare the `physics/research/*` ground truth against what landed in the committed spec. Surfacing physics that was researched but lost, weakened, or contradicted in synthesis is high-value" | `10.3-audit-prompt.md:126` | mine | `outside` → `restructure/auditor-brief.md#ground-rules` | the *principle* is high-value and is exactly what Phase 1 is doing; the path is wrong. Re-point at `log/timeline.md` + git history |
| 94 | §5 — the three verdicts per finding (**defect** / **risk** / **intentionally open**) | `10.3-audit-prompt.md:127` | mine | `outside` → `restructure/auditor-brief.md#ground-rules` | a clean, reusable taxonomy; maps onto the disposition/open-questions/contradictions split |
| 95 | §5 — "Respect the philosophy… the bar is design-grade accuracy at acceptable cost, not matching a converged ab-initio calculation. Recommend; don't just condemn" | `10.3-audit-prompt.md:128` | mine | `outside` → `restructure/auditor-brief.md#ground-rules` | prevents an auditor from scoring every approximation as a defect |
| 96 | §5 — **"A prior audit's clearance is not evidence"** + the 2026-06-10 κ/fabricated-citation incident; re-verify *values*, never verdicts | `10.3-audit-prompt.md:130` | move | `practice/traps#practice` (trap 45) | already trap 45; traps owns the hazard. The auditor brief cites the trap rather than restating it |
| 97 | §5 — **"No free emptiness."** A clean verdict must be earned and shown: an evidence transcript of quote-pair tuples per invariant class swept, plus a log of near-findings considered and dismissed. "A bare 'no defects found' is not an audit result" | `10.3-audit-prompt.md:131` | mine | `outside` → `restructure/auditor-brief.md#ground-rules` **and** `practice/agent-contract#reporting` | **the highest-value rule in the file.** It is also the acceptance criterion the AGENT-BRIEF imposes on this very fragment. Belongs in the contract, not only in the auditor brief |
| 98 | §5 — **"Calibrate before certifying."** For a certification pass, first plant a known set of contradictions in a throwaway copy and confirm the prompt finds them; an uncalibrated clean verdict measures the prompt, not the corpus | `10.3-audit-prompt.md:132` | mine | `outside` → `restructure/auditor-brief.md#ground-rules` **and** `practice/agent-contract#reporting` | the human-auditor form of trap 58/66. §8 keeps `check_the_checkers.py`'s design for the machine side; this is the same rule for the reader side |
| 99 | §6 Deliverable — the six-section report shape | `10.3-audit-prompt.md#6-deliverable` | mine | `outside` → `restructure/auditor-brief.md#deliverable` | reusable as-is; drop §6.5's dependence on the §4 question numbering |
| **10.4-traps → `practice/traps`** | | | | | |
| 100 | Frontmatter: 21 `depends-on`, `canonical-for: [standing traps register]` | `10.4-traps.md:1-38` | move | `practice/traps` frontmatter | `owns:` must gain ≥1 topic distinct from the id; "standing traps register" is a title restatement. Propose `owns: [sign and frame conventions, cert-refusal boundaries, checker-soundness hazards]` |
| 101 | Intro — "Durable findings that must never be re-violated… the failure modes that survive ordinary review, because the wrong version looks exactly as plausible as the right one" + "Read this before changing anything involving a sign, a unit, a reference frame, or a validity boundary" | `10.4-traps.md:41-48` | keep | `practice/traps#intro` | states why the register exists; D2 exception 2 |
| 102 | The `enforced` / `advisory` status vocabulary | `10.4-traps.md:50-52` | keep | `practice/traps#intro` | live and useful: `advisory` marks a hazard guarded *only* by the register. **But every `enforced, [page]` pointer must be re-verified — one already fails, see row 111** |
| 103 | Group A, traps 1–6 (sign conventions: AlGaN P_sp bowing, pyroelectric sign, breakdown-field slope, misfit strain, e₁₅ literature-split, entropy-production direction) | `10.4-traps.md#a-sign-conventions` | keep | `practice/traps#sign-conventions` | live hazards; each cites `[accuracy-ledger]` which survives at `oracle/accuracy/accuracy-ledger` |
| 104 | Trap 3's clause "The earlier 'drops ~20%' conflated E_b with mobility collapse" | `10.4-traps.md:73-74` | keep | `practice/traps#sign-conventions` | **incident history that is load-bearing**: the wrong version is the one a reader will find in the literature, so naming it is the hazard, not the history. Same for traps 37, 38, 40 |
| 105 | Groups B–D, traps 7–23 (units/dimensions/σ encodings; reference frames and pairing; cert-refusal boundaries) | `10.4-traps.md#b-units-dimensions-uncertainty-encodings` … `#d-cert-refusal-boundaries` | keep | `practice/traps#units` · `#frames` · `#refusal-boundaries` | live hazards; no scaffolding found in this range beyond row 104's class |
| 106 | Group E, traps 24–33 (verifier-soundness: γ̂ admissibility scored not presupposed; gauge/electrostatic partition; two polar predicates; `ω²≥0` gating; T,P-aware hull; Callaway-vs-BTE; frozen learned corrections; Scharfetter–Gummel; PSD as a congruence-action Reynolds operator; swept-environment revalidation) | `10.4-traps.md#e-verifier-soundness-requirements` | keep | `practice/traps#verifier-soundness` | the traps that make the oracle sound *as a verifier* — the highest-consequence group |
| 107 | Group F, traps 34–44 (known-wrong-if-lifted-naively: κ error decoupling, 3-phonon high-T, α_ii σ ≥ ×3, image-force lowering, GaN peak-vs-saturation, β-Ga₂O₃ anisotropic triple, QHA per-material, alloy disorder, degenerate p⁺, diamond oxidation ceiling, worked examples) | `10.4-traps.md#f-known-wrong-if-lifted-naively` | keep | `practice/traps#lifted-values` | live |
| 108 | Trap 40's clause "the '~Θ_D/2' rule this register used to state was back-fitted from diamond alone and contradicted its own GaN example" | `10.4-traps.md:280-284` | keep | `practice/traps#lifted-values` | same class as row 104: a plausible-but-wrong rule a reader will re-derive. The *correction date* is scaffolding; the wrong rule is the hazard |
| 109 | Trap 44 — worked examples hide errors, with the five specific errors in `[deriv-defects]` B.3/B.4 and `[deriv-generator-catalog]` §6 | `10.4-traps.md:303-315` | keep (hazard) / delete (locators) | `practice/traps#lifted-values` | the hazard ("do not read 'the worked example' as a single artifact") survives; **both cited pages are deleted by D3, so the two locators become dangling**. Rewrite without them |
| 110 | Group G, traps 45–70 (practice traps) | `10.4-traps.md#g-practice-traps` | keep | `practice/traps#practice` | live; but six carry pointers into containers being demolished — rows 111–116 |
| 111 | Trap 29's enforcement pointer — "enforced, `[cert-obligations]` §1.2" | `10.4-traps.md:216-218` | move | `practice/traps#verifier-soundness`, re-pointed at `oracle/seams/cross-cutting-rules#method-pairs` | **confirmed dangling promise.** `5.1-cert-obligations.md` §1.2 contains no occurrence of "Callaway", "BTE", "consistency pair" or "excess"; it carries only the `τ_equiv`/`τ_method` constants (`5.1-cert-obligations.md:141-142`). The rule itself is stated at `7.3-cross-cutting-rules.md:51-53`, `8.2-capability-slices.md:84`, `6.6-named-formulas.md:164` — three statements, none cited by the trap |
| 112 | Traps 45 and 58's enforcement pointers — "enforced, `[audit-prompt]` §5" | `10.4-traps.md:322,416` | move | `practice/traps#practice`, re-pointed at `practice/agent-contract#reporting` | `audit-prompt` leaves the corpus (row 83); the rules it enforced move into the contract (rows 97–98) |
| 113 | Trap 46 — "Seed from the ledger and reference CSVs, never from an appendix derivation" | `10.4-traps.md:324-327` | move | `practice/traps#practice` | rewrite: the appendix ceases to exist, so state the positive rule ("seed from `oracle/accuracy/accuracy-ledger` and `data/reference-data/*.csv`") and drop the appendix clause. Absorbs row 8 |
| 114 | Trap 53's reference to "the retired S5 catalog legend" (where `D3` meant finite-difference fallback and `D0` meant closed-form analytical gradient) | `10.4-traps.md:363-371` | keep | `practice/traps#practice` | **keep despite naming a retired thing.** This is the legend `11.8-deriv-generator-catalog` still uses, and rows 12 and 106 were mis-registered by migrating out of it untranslated. Anyone mining ch. 11 in Phase 2 needs this trap. It is the single strongest argument for mining ch. 11 *before* deleting it |
| 115 | Trap 57 — "a rename must leave a forwarding address"; the entire mechanism is `retired-names.csv` + the same-paragraph resolution check | `10.4-traps.md:397-403` | delete | — | §9 deletes `retired-names.csv`. **Marked delete with reservation** — the *hazard* (a rename that lands only in the CSV orphans every prose mention) survives even without the CSV, and the plan's answer to it is "fix names before they land" (trap 47). If Phase 2 wants a forwarding mechanism at all, this trap is where the argument for one lives. **See Notes** |
| 116 | Trap 59's pointers — GAP sense (ii) cites `[open-decisions]`, sense (iii) cites `[deriv-csp] Part A` | `10.4-traps.md:419-429` | keep (hazard) / move (pointers) | `practice/traps#practice`; re-point (ii) at `program/build/forced-decisions#language-roles`, (iii) at wherever the CSP methodology survey lands | `open-decisions` dissolves and `deriv-csp` is deleted. The three-way overload is a live hazard and one of the §4 names to fix |
| 117 | Traps 55, 66, 67 — pointers into tool internals (`check_data_agreement.py` "check (g)"; `check_the_checkers.py` uncovered-check gate; `check_book_structure.py` per-tier cost distribution) | `10.4-traps.md:387,512,522` | keep (hazard) / move (pointers) | `practice/traps#practice`, re-pointed at the rebuilt tools | check (g) does exist today (`check_data_agreement.py:19`). All three pointers name tool-internal labels that the §8 rebuild will renumber — **re-verify after the rebuild, not before** |
| 118 | Trap 58's and 66's incident narratives (the dot-requiring section pattern, the ASCII-only `formula =` class, "for most of its life it did not", "29/29 fired was cited corpus-wide") | `10.4-traps.md:405-416,500-512` | keep (rule) / delete (narrative) | `practice/traps#practice` | the rules — "plant a defect of the class you claim is absent"; "coverage is derived from the checker's source, never from a list beside it" — are the whole point and survive. The dated narratives go to the log (L12) |
| 119 | Trap 61 — "a dangling pointer is safer than a plausible reconstruction"; the mesh-σ floor reconstruction that git archaeology proved was invention | `10.4-traps.md:443-455` | keep | `practice/traps#practice` | **the most relevant trap in the register to Phase 2.** A restructure that rewrites 44,000 words present-tense is exactly the pass this trap warns about. Keep the worked example — it is what makes the rule stick |
| 120 | Trap 62 — "not every gap the collapse exposed was caused by the collapse" | `10.4-traps.md:457-464` | keep | `practice/traps#practice` | same: a rule about repair passes, and Phase 2 is a repair pass. The three specific items are dated but the rule is not |
| 121 | Traps 63–65, 68–70 (never put ε in the address; a declared error target is not a measured error; "exact only" is untested not conservative; quote both arms of a comparison; a checker inherits the soundness of its invariant; harvest a vocabulary from the schema that owns the field) | `10.4-traps.md:466-575` | keep | `practice/traps#practice` | live methodological hazards, each with a worked case. Trap 70's rule — *find the page that defines the type, not the page that lists the values* — is directly applicable to the `owns:` redesign |
| **10.5-timeline → `log/timeline.md`** | | | | | |
| 122 | Frontmatter (12 `depends-on`, 16 `referenced-by`, `canonical-for: [development timeline]`) | `10.5-timeline.md:1-38` | delete | — | the log is not a page. **22 pages list `timeline` in `depends-on`; 27 prose citation lines across 18 files** (16 pages, `instructions.md`, one live spec) — every one becomes a path citation to `log/timeline.md` |
| 123 | Intro — "Full narrative for every entry is in git history; this page keeps only the outcome and where the durable result now lives" | `10.5-timeline.md:41-44` | move | `log/timeline.md#preamble` | rewritten: under D2 the log *is* the record, so "full narrative is in git" becomes a weaker claim than the schema requires. **See Notes — this sentence is in tension with the compliance obligation** |
| 124 | "The durable findings themselves are in [traps] — that register, not this chronology, is the value the audit practice produced" | `10.5-timeline.md:45-46` | keep | `log/timeline.md#preamble` | the correct division of labour between the log and the traps register, stated by the corpus |
| 125 | "Headings here are stable anchors: cite them as `timeline §<date-slug>`" | `10.5-timeline.md:48` | move | `log/timeline.md#preamble` | survives as the anchor convention for a path-cited file. Note **no page actually uses this form today** — the only occurrence of `timeline §` in the corpus is this sentence defining it |
| 126 | The 12 dated entries | `10.5-timeline.md#2026-06-09-founding-physics-audit` … `#2026-07-22-re-audit-the-calibration-itself` | move | `log/timeline.md` | converted to the five-field schema in **Log-worthy advancements** below. **All 12 fail the attribution field; 8 fail "what it superseded"; 1 fails the evidence-link field** — see Notes |
| 127 | The **Wave-1 A1–A16 correction table** | `10.5-timeline.md:97-114` | move | `log/timeline.md#wave-1-corrections` | **not history — a live resolver.** 13 sites cite `[Wave-1 A#]`; 2 of them (`9.1-accuracy-ledger`, `9.2-reference-battery`) are on surviving pages. The page says so itself: "the tags are live provenance markers, so the list they index is kept here" |
| 128 | The **gap-audit A–E section index** | `10.5-timeline.md:162-169` | move | `log/timeline.md#gap-audit-index` | **critical: 8 of these citations are in data files that survive** — 7 rows of `physics/library/formulas/registry-manifest.csv` and 1 of `transport-coefficients.csv` cite `gap-audit <letter><number>`. Deleting this table orphans provenance in the registry itself |
| 129 | Entry "2026-06-25 — CS-audience deck": "Superseded. Carried a stale formula count under a standing waiver, and published a boolean validity predicate that canon forbids… Retired with the collapse; the waiver is discharged" | `10.5-timeline.md:139-147` | move | `log/timeline.md` (L5) | the *decision* — evidence-never-verdicts, no boolean validity predicate — is a present-tense rule owned by `program/purpose/product`. Verify it is stated there; the deck's retirement is log-only |
| 130 | "Genuine gaps, flagged and never invented" — the nine Wave-1 acquisition gaps (AlN Caughey–Thomas quartet, AlN hole mobility, AlN measured α_ii, AlN hole α_ii, κ_BR for GaN/AlN, `Z*_⊥`, the e₁₅ sign, AlGaN d-constants/`v_sat(x)`/holes, AlN `B′` and per-axis sound velocities) | `10.5-timeline.md:116-122` | move | open-questions on `oracle/accuracy/accuracy-ledger` **and** `oracle/accuracy/reference-battery` | **these are live data gaps stated in a chronology.** They are cert-refusal boundaries today, not history. `wave1-data-acquisition-gaps` |
| **journal/instructions.md → `practice/agent-contract`** | | | | | |
| 131 | Header — "You are looking at a book… Read this first. Do not go spelunking through directories — the apparatus exists so you do not have to" | `instructions.md:1-9` | move | `practice/agent-contract#preamble` | the operating stance survives; "book" becomes "journals", and the apparatus becomes `corpus.json` |
| 132 | §1 The layout — the tree diagram | `instructions.md#1-the-layout` | delete | — | emitted from `corpus.json` (§6). Every path in it changes |
| 133 | §1's closing — "Everything else in the repo is data or code, not book" | `instructions.md:29-31` | move | `practice/agent-contract#what-is-in-scope` | **materially changed by D4**: `informed-operator/` and `interface/` become journals, so the sentence is now false. Rewrite as: journals are corpus; `data/`, `tools/`, `log/`, `index/` are not |
| 134 | §2 Three identifiers, three jobs — the `id`/`tag`/`content-hash` table | `instructions.md#2-three-identifiers-three-jobs` | delete | — | D6 collapses three names to one. The table exists to manage a problem being removed |
| 135 | §2 — "Cite the `id`, never the tag and never the file path… ids are the contract" | `instructions.md:41-43` | move | `practice/agent-contract#citations` | survives as "cite `[page#anchor]`". Merge with row 36 |
| 136 | §2 — section coordinates are bare ordinals; files that are not pages have no id and are cited by path | `instructions.md:45-49` | move | `practice/agent-contract#citations` | ordinals die (§4); the path-citation half survives for `log/`, `data/`, `tools/` |
| 137 | §2 — "Ids used to carry a serial (`arch-20-representations`, sections §20.x, at tag 4.3 — three numbers per page, none agreeing). All 38 were renamed on 2026-07-22" | `instructions.md:51-53` | delete | — | rename history + a pointer to `retired-ids.csv`, which §9 deletes. Log-worthy L10 |
| 138 | §3 Book → graph, steps 1–5 (read contents; read index to find the owner; open the page and follow `depends-on`; the graph is not a build order; do not read the whole corpus) | `instructions.md#3-book-graph-how-to-load-context` | move | `practice/agent-contract#finding-a-fact` | **this is the procedure `corpus.json` exists to make one hop instead of three** (§6). Rewrite against it; keep step 5's discipline verbatim |
| 139 | §3 step 1 — "chapters 2–6 are the contiguous `/physics` oracle block — if your task is oracle-internal, that range is usually sufficient" | `instructions.md:57-59` | move | `practice/agent-contract#finding-a-fact` | **this is the reason the `oracle` journal exists.** The fact survives; its expression becomes `journal: oracle`, which is strictly better — a range that must stay contiguous becomes a name |
| 140 | §4 Authority order — the summary chain, and "defined normatively in `10.1-conventions`, which is the only place to change it" | `instructions.md#4-authority-order-who-wins-when-two-files-disagree` | delete | — | dies with row 5. The "only one place to change it" discipline generalizes into the `owns:` invariant (row 26) |
| 141 | §4's three rules that bite (seed from the CSVs; appendix pages are supporting and `11.8` uses a retired legend; `live/audits/` frozen vs `live/specs/` not) | `instructions.md:84-93` | delete | — | rows 8, 11, 18 dispose of all three. The `11.8` warning survives as trap 53 (row 114) |
| 142 | §5 Graph → book, steps 1–6 — identify the topic, look it up, **never state a fact in two pages**, edit the body, add the `depends-on` edge, run the checkers | `instructions.md#5-graph-book-where-does-my-edit-go` | move | `practice/agent-contract#placing-a-new-fact` | **the single most important procedure in the file** and the direct answer to Javier's stated pain. Step 3's "duplicating it *silently* is what rots a corpus" is the thesis of the whole restructure |
| 143 | §5 step 2's fallback — "If the topic is not in the index, no page claims it — decide which page *should* own it and add the topic to that page's `canonical-for`" | `instructions.md:98-100` | keep | `practice/agent-contract#placing-a-new-fact` | the mechanism by which a homeless fact gets a home. It exists and was not used — the training-stage ordering went into two untracked files instead. D8 is the enforcement half |
| 144 | §5 steps 4–5's `content-hash` / `referenced-by` cautions | `instructions.md:105-108` | delete | — | both fields are deleted |
| 145 | §6 Renaming a registry formula — the three-step procedure through `retired-names.csv` | `instructions.md#6-renaming-a-registry-formula` | delete | — | the CSV is deleted (§9). "Fix names before they land, never after" survives as trap 47 |
| 146 | §7 Working copies — carry each block's source `id`; record the `content-hash`; a changed hash means the page moved underneath you; **"a working copy without per-block `id` provenance cannot be written back mechanically. Do not produce one."** | `instructions.md#7-working-copies` | move | `practice/agent-contract#working-copies` | the **id-provenance rule survives and matters more** under parallel agent sessions. The hash half is replaced by git. This is the rule that would have caught the `1e04770` two-copies failure |
| 147 | §8 What the checkers enforce — the 12 structure checks | `instructions.md:140-155` | delete | — | verbatim duplicate of row 43. One statement, in the contract |
| 148 | §8 — "`check_data_agreement.py` fails on sixteen classes" + the enumeration | `instructions.md:165-176` | move | `practice/agent-contract#what-the-checkers-enforce` | **the count is wrong and unchecked — see Contradictions row 5.** The tool declares fifteen lettered classes and the enumeration here omits two of them entirely |
| 149 | §8 — the arity rationale (the unquoted comma in `transport-coefficients.csv` that shifted `Added` to `experimental`) | `instructions.md:178-182` | move | `practice/agent-contract#what-the-checkers-enforce` | duplicated at `10.1-conventions:339-341` and in the timeline's 2026-07-22 entry. **Three statements; keep the design argument once, log the incident once** |
| 150 | §8 — "Green means the corpus is internally consistent. It does not mean the physics is right" + the plant-a-defect rule + `check_the_checkers.py` and its three failure modes | `instructions.md:184-199` | delete | — | duplicate of rows 46–47. The pointer to `10.3-audit-prompt` re-points to the external auditor brief |
| 151 | §9 Standing traps — "before changing anything involving signs, units, or reference frames, read `10.4-traps`", with the two worked examples (bowing coefficient flips the interface charge; pyroelectric coefficient inverts the temperature drift) | `instructions.md#9-standing-traps` | move | `practice/agent-contract#standing-traps` | keep the directive; the two examples are traps 1 and 2 — cite, do not restate |
| **journal/glossary.md → `practice/glossary`** | | | | | |
| 152 | Header — "Single-line definitions for the load-bearing terms. Each term lists the file that owns its full definition (`canonical-for`)" | `glossary.md:1-4` | delete | — | **the claim is false today: 73 of the 75 terms are not a `canonical-for` topic of any page** (all 98 topics enumerated; 2 terms match). The pointers resolve to a *page*, which is all `check_data_agreement.py` check (i) verifies |
| 153 | The 75 term rows, as **definitions** | `glossary.md:8-83` | delete | — | duplication by construction: each restates the owning page's definition in a cell no checker compares against the page. Several run 3–5 lines (`CouplingChannel`, `TheoryContext`, `KernelExt`, `EvidenceDAG`, `Ledger-tracked observables`) — these are second copies, not glosses |
| 154 | The 75 term rows, as a **term → owning page index** | `glossary.md:8-83` | move | `corpus.json` topic map; emitted into `practice/glossary` | the index function is exactly `corpus.json`'s topic → (page, anchor) map. Emit it, and the definition can never drift because there is only one |
| 155 | The 73 terms that are not any page's `canonical-for` topic — `PhysicsGraph`, `Node`, `NodeKind`, `CategoryTag`, `StateTier`, `ContentAddress`, `Universe`, `SparseSet`, `PersistentMap`, `MerkleDAG`, `CouplingChannel`, `KernelExt`, `TheoryContext`, `OneShotCert`, `IterativeResult`, `CompressionPlan`, `Stage 1`–`Stage 5`, `SqliteReferenceCache`, `RoaringCoverageMask`, … | `glossary.md:8-83` | move | `owns:` entries on the page each names | **the largest homeless-fact finding in my scope.** The corpus's *type vocabulary* — 73 named records, enums and stages — is owned by nothing under the machine-checked invariant. Feeding these into `owns:` fixes defect 4 on the same pass, since most of the 18 vacuous pages are exactly where these types live |
| 156 | The `GAP` row (three senses) | `glossary.md:42` | delete | — | duplicate of trap 59; its Canonical cell already points at `traps §59` rather than at an owner |
| 157 | The `Catalog observables` row — "the 52 observables of `[deriv-observable-catalog]` Part C — a frozen historical stratum count. '52 observables' in older documents means this" | `glossary.md:76` | delete | — | a decoder ring for superseded documents, pointing at a ch.-11 page. Pure scaffolding by D1/D3 |
| 158 | The `Ledger-tracked observables` row — the ledger headline count (52 catalog + #53–59) and the invariant that it equals the table's max row id with no missing ids | `glossary.md:77` | move | `oracle/accuracy/accuracy-ledger#counts` | the *invariant* is machine-checked and live; it is stated in the glossary rather than on the page that owns it |
| **journal/contents.md and index.md — emitted** | | | | | |
| 159 | `contents.md` — the 11 chapter groupings with per-page titles, ids and `canonical for` lists | `contents.md:8-156` | move | `corpus.json` → emitted `contents.md` | already generated; the content is entirely derivable from frontmatter. Chapter names become journal/section names |
| 160 | `contents.md` header — "The `tag` (e.g. `4.2`) is a display label: regenerated freely, never cited. Cite the `id`." | `contents.md:5` | delete | — | `tag` is deleted (D6); with filename = id there is nothing to warn about |
| 161 | `index.md` — the 98 topic → page rows | `index.md:9-106` | move | `corpus.json` topic map → emitted `index.md` | already generated; this is the map `corpus.json` §6 exists to serve in one read |
| 162 | `index.md` header rule — "Every canonical topic and the single page that owns it. If a topic is not here, no page claims it; if it appears once, the one-fact-one-home invariant holds" | `index.md:5` | move | `practice/agent-contract#owns` | **this is a rule, not generated text** — the clearest one-sentence statement of the corpus's central invariant, currently living in a file marked "do not edit" |
| 163 | `index.md` footer — "*98 canonical topics across 58 pages.*" | `index.md:108` | move | `corpus.json` → emitted | a derived count; it becomes ~54 pages and a larger topic count once rows 155 and 100 land |
| 164 | `contents.md:154` / `index.md:37` — `deriv-language-study` titled "Implementation-language decision (`open-decisions`, open item 5)" | `contents.md:154`, `index.md:37` | delete | — | the page is deleted with ch. 11, and the ordinal is wrong (Contradictions row 2). Recorded so the same title is not carried into `program/build/forced-decisions` |

---

### from `appendix-a`

### 11.1 `deriv-ion-dynamics` — Group A (structural, mechanical, thermal)

| # | Fact or block | Current location | Disposition | Target | Evidence |
|---|---|---|---|---|---|
| A1 | Frontmatter: `authority: supporting`, `content-hash`, `canonical-for: [deriv-ion-dynamics]`, `depends-on: [timeline]`, `referenced-by` | `11.1-deriv-ion-dynamics.md:1-12` | delete | — | Every key dies by decision: `authority` tier (D3), `content-hash` (D6), vacuous self-id ownership (defect 4), `referenced-by` is generated. `depends-on: [timeline]` is a scaffolding-only edge — the body cites `[timeline]` **only inside the changelog**. |
| A2 | `title: Group A — Ion Dynamics: Structural, Mechanical, Thermal` is **not valid YAML** | `11.1-deriv-ion-dynamics.md:3` | delete | — | Cause is an **unquoted colon**, *not* a backtick. `yaml.safe_load` → `mapping values are not allowed here`, line 2 col 30. See Notes — the plan's §2 defect 5 misattributes this one. |
| A3 | Phonon branch index: `s` (grp A) ≡ `ν` (grp B) ≡ `λ` (grp C) — one object, three symbols | `11.1-deriv-ion-dynamics.md:15` (Notation banner) | mine | `practice/traps#phonon-branch-index` | Live hazard, not a remnant. Zero hits for it in `10.4-traps.md` or `journal/glossary.md`. **Gets worse under the restructure**: the three groups' content lands on three different pages, so the collision survives while the banner that warns about it does not. |
| A4 | κ in this file is the lattice part only; `κ_total = κ_el + κ_ph` | `11.1-deriv-ion-dynamics.md:15` | delete | — | Duplication: stated at `6.2-typeclass-alphabet.md:27` (`κ = κ_el + κ_ph`, as the `combineTol` example). The banner cites that page for it. |
| A5 | Framing: "Deep mathematical research for the n-Op `/physics` library. Scope: …" | `11.1-deriv-ion-dynamics.md:17-21` | delete | — | Container scaffolding — describes the document, not the physics. |
| A6 | The three regimes are not three state spaces but three *functionals* over one configuration manifold | `11.1-deriv-ion-dynamics.md:22-25` | delete | — | Duplication: this is exactly `generic-dynamics#nine-regimes-as-extractions` ("Each traditional regime … is recovered as an **extraction** of this single equation"), `3.1-generic-dynamics.md:53-55`. |
| A7 | Symbol table: `R_I`, `h`, `Ω = det h`, reduced coords `s_I`, `ε` vs `η`, `q`, `s`, `ħ`, `k_B` | `11.1-deriv-ion-dynamics.md:27-37` | mine | `practice/glossary` | `h`, `Ω`, `R_I` are used unglossed across canon (`unified-state`, `generic-dynamics`, `typed-compositions`). `journal/glossary.md` has no entry for any of them. |
| A8 | `## Regime 1 — Structural` container + "fixes where atoms are" | `11.1:41-44` | delete | — | Section scaffolding; one sentence, no fact not in A6. |
| A9 | `E_BO` as constrained electronic minimum + Kohn–Sham reduction `E_KS[n; R,h]` | `11.1:46-63` (§1.1) | delete | — | Duplication: `born-oppenheimer-levels` L2 states `E_BO(R,h) = min_γ̂ E[γ̂; R,h]` (`2.5:45`); `generic-dynamics` E-functional block states both `E_BO` and `E_KS[γ̂]` (`3.1:68-71`). |
| A10 | `E_BO : (ℝ^{3N} × GL⁺(3)) → ℝ` is smooth a.e. **except at conical intersections, which the regime excludes** | `11.1:64-67` (§1.1) | mine | `oracle/certification/out-of-scope#conical-intersections` | The exclusion is a **scope boundary with no owner**. Zero hits for "conical intersection" in `journal/pages/` outside ch. 11. Every gradient-based residual in `residual-definitions` assumes this smoothness silently. |
| A11 | DOF accounting: `3N+9` raw; **`3N+6` physical** after quotienting global rotation and the `GL(3,ℤ)`, `|det M|=1` basis freedom; Parrinello–Rahman phase space `2(3N+6)` | `11.1:73-93` (§1.2) | mine | `oracle/state/unified-state#dof-count` | `unified-state` lists the seven slots and their types but states **no DOF count and no gauge quotient**. The `GL(3,ℤ)` lattice-basis equivalence is stated nowhere else in the corpus (grep: zero hits outside ch. 11). Load-bearing for any encoding/compression claim. |
| A12 | State vs derived split for the structural regime (state = `R,h,P,Π_h,Z`; derived = `n(r)`, `E_BO`, forces, stress, symmetry labels) | `11.1:95-100` (§1.2) | delete | — | Duplication: `unified-state:46-53` owns the irreducible/emergent split for the whole 7-tuple. |
| A13 | The structural regime has **no intrinsic dynamics**; "evolution" is a fictitious-time descent `dR/dτ = −∇E_BO`, `dh/dτ = −Ωσh^{−⊤}`; real-time evolution belongs to other regimes | `11.1:102-118` (§1.3) | mine | `oracle/laws/generic-dynamics#structural-extraction` | `generic-dynamics`'s table row says only "Critical points of `E` at `T=0`". *That there is no time evolution here at all, and that the optimizer time is non-physical*, is the operative fact for a PINO that would otherwise be handed a `dx/dt`. Not stated in canon. |
| A14 | Translational + rotational invariance ⇒ zero net force and zero torque | `11.1:120-125` (§1.4) | delete | — | Duplication: `residual-definitions` cat 9 `Conservation` covers "energy, particle-number / charge, momentum / crystal-momentum, spin" (`3.2:107`); registry row 103 `F-equals-minus-grad-E` is the architectural marker. |
| A15 | Space-group invariance of `E_BO` at a symmetric configuration; **Wyckoff site labels are conserved labels** | `11.1:126-128` (§1.4) | mine | `oracle/registry/topology-atlas#wyckoff-labels` | The equivariance marker is registry row 104 and `residual-definitions` cat 14 names "space-group equivariance of response tensors", but *Wyckoff labels as conserved discrete labels under the dynamics* appears nowhere else. |
| A16 | Cell positivity: `det h > 0` **and metric `g = hᵀh ≻ 0`** | `11.1:129-130` (§1.4) | mine | `oracle/state/unified-state#slot-constraints` | `unified-state:32` carries `h ∈ GL⁺(3,ℝ)` — which gives `det h > 0` but **not** `g ≻ 0` as an explicit admissibility test. This is a state-admissibility gate of exactly the kind `residual-definitions` cat 10 collects (cf. the γ̂ bounds); it is absent from that list. |
| A17 | Particle number `N` conserved | `11.1:131` (§1.4) | delete | — | Duplication: `residual-definitions` cat 9 (`3.2:107-112`), including the static γ̂-trace form. |
| A18 | Structural observables table — **the `Operation` column**: lattice params from `h`, `Ω = det h`, min-image bond length, bond/dihedral angles, forces (Hellmann–Feynman **+ Pulay**), space-group detection, defect descriptors by combinatorial matching, `γ_s = (E_slab − nE_bulk)/(2A)` | `11.1:133-148` (§1.5) | mine | `oracle/registry/typed-compositions#structural` | `typed-compositions:45-58` gives the *typed composition* (`StateReadoutOf(state.h, extractor = cell-metric-extraction)`) but never the **mathematics the extractor performs**. The two are complementary, not duplicates. The Pulay correction in particular is named only here and at §1.7. |
| A19 | Stress row: `σ_αβ = +(1/Ω) ∂E_BO/∂ε_αβ` (Nielsen–Martin), with the sign justified two ways | `11.1:144` (§1.5) | mine | `oracle/laws/generic-dynamics#stress-sign` | **The line is textually corrupted** — see Contradictions C1. The sign convention itself is load-bearing (it is the sign `dh/dτ = −Ωσh^{−⊤}` at A13 requires) and is stated nowhere else in canon. Mine the convention; the corrupted prose cannot be copied verbatim. |
| A20 | Structural couplings (electronic foundational; mechanical reference config; thermal expansion point; magnetic constraint; chemical adsorbate positions) | `11.1:149-163` (§1.6) | delete | — | Duplication: `coupling-structure` owns the `CouplingChannel` inventory (`3.3:519-527` channel table, `3.3:412` MVP `CouplingSpec`). Prose restatement is what drifts. |
| A21 | Structural numerics: plane-wave + cutoff, atom-centred/NAO (⇒ Pulay forces), PAW/pseudopotentials, BFGS/FIRE/CG, Niggli reduction / Spglib-style symmetry detection | `11.1:165-173` (§1.7) | mine | `oracle/registry/computational-methods#reference-implementations` | Partly covered: `computational-methods:35-38` lists BFGS/FIRE/CG as `variational-minimization` sub-methods. **Not** covered: plane-wave vs NAO basis choice, the Pulay-force consequence of atom-centred bases, PAW, Niggli reduction. Unsure whether the tool names survive — flagged in Notes. |
| A22 | `## Regime 2 — Mechanical` container | `11.1:177-183` | delete | — | Section scaffolding. |
| A23 | Deformation gradient `F = h h⁽⁰⁾⁻¹`; **Green–Lagrange strain** `η = ½(FᵀF − I) = ½(C − I)`; infinitesimal limit `ε = ½(∇u + ∇uᵀ)` | `11.1:185-198` (§2.1) | mine | `oracle/state/unified-state#strain-map` | **Zero hits for "Green-Lagrange"/"Green–Lagrange" in `journal/pages/` outside ch. 11.** `typed-compositions:84` uses `coord = symmetric-strain-η` — canon *names* `η` and never defines it. This is a homeless fact of the `Environment` class. |
| A24 | Strain-energy-density Taylor expansion; `C_ijkl` symmetries ⇒ ≤21 independent; Voigt index map `11→1 … 12→6` | `11.1:200-213` (§2.1) | mine | `oracle/registry/typed-compositions#mechanical` | Registry rows 57/60 consume `C_ij` in Voigt form (`(C_ij) → bool-vec + slack`, `(stress(strain)) → C_ij`); **the Voigt index map is defined nowhere in canon**. Any implementation must guess the ordering. |
| A25 | Atomistic bridge `Φ(η) = [E_BO(h(η),R(η)) − E_BO⁽⁰⁾]/Ω₀` with **internal `R` relaxed at each fixed cell** | `11.1:215-224` (§2.1) | mine | `oracle/registry/typed-compositions#mechanical` | The internal-relaxation requirement is the difference between a correct `C_ij` and a wrong one. `typed-compositions:83-85` says `SecondDerivativeOf(F = E_BO, x₀ = equilibrium-state, coord = symmetric-strain-η)` — silent on relaxation at each strained cell. Ledger row 36 (`elastic_tensor ±5%`, "DFT stress-strain / DFPT") assumes it. |
| A26 | Cauchy's first law `ρ ∂²u/∂t² = ∂_j σ_ij + f_i`; Lagrangian form with **first / second Piola–Kirchhoff** `P = FS`, `S_IJ = ∂Φ/∂η_IJ` | `11.1:226-243` (§2.1) | mine | `oracle/laws/generic-dynamics#mechanical-extraction` | **Zero hits for "Piola" and zero for "Cauchy" in `journal/pages/` outside ch. 11.** The continuum momentum balance is proposed as a residual at §S.4 and appears in no residual category. See Open question OQ-A2. |
| A27 | Mechanical state at two scales: atomistic (a *projection* of structural DOFs through the strain map, no new DOFs) vs continuum (`u(X,t)`, `u̇`; `6M` after meshing) | `11.1:245-267` (§2.2) | mine | `oracle/state/multiscale-state#mechanical-tier` | "No new DOFs — the regime is a projection" is the reason mechanical needs no state slot; `multiscale-state` builds a three-tier story (micro/slow/macro) in which the continuum-elastic tier **is not one of the three**. Ledger row 41 `resonant_modes ω_n` is flagged "**device-scale (continuum elastic)**" — so the tier is consumed and never declared. |
| A28 | Constitutive parameters (`C_ijkl`, hardness, yield) are **not state** — they are functionals of structural/electronic state and `T` | `11.1:260-263` (§2.2) | mine | `oracle/state/unified-state#derived-not-state` | Sharpens `unified-state`'s emergent/irreducible split for material parameters specifically. Prevents a PINO from being handed `C_ij` as an input channel. |
| A29 | Three nested mechanical dynamics: static (elliptic) · linear elastodynamics (hyperbolic, `v = √(C/ρ)`) · finite-strain/plastic (nonlinear, path-dependent `Ḟᵖ = γ̇ s⊗n`) — all Hamiltonian ± Rayleigh dissipation | `11.1:272-287` (§2.3) | mine | `oracle/laws/generic-dynamics#mechanical-extraction` | The PDE-type classification (elliptic/hyperbolic/parabolic) recurs at 11.3 §4.3 and is the basis of `multiscale-state`'s tier table. Canon's `generic-dynamics` row for Mechanical says only "2nd strain-derivatives of `F` at equilibrium" — it does not admit the dynamic cases at all. |
| A30 | Mechanical conservation: linear + angular momentum; elastic energy; **Cauchy symmetry `σ_ij = σ_ji` ≡ angular-momentum balance** | `11.1:289-294`, `305-306` (§2.4) | mine | `oracle/laws/residual-definitions#conservation` | Stress symmetry is an admissibility gate on any predicted `σ` and is not in cat 9 or cat 10. Cheap to check, currently unchecked. |
| A31 | Born stability: `C_IJ` positive-definite; cubic reduction `C₁₁ > |C₁₂|`, `C₁₁ + 2C₁₂ > 0`, `C₄₄ > 0` | `11.1:297-300` (§2.4) | delete | — | Duplication: registry row 57 `elastic-stability-criteria` (`(C_ij) → bool-vec + slack`, "a.k.a. Born"); `residual-definitions` cat 16 and `3.2:224` both name it. The page already cross-references the row inline. |
| A32 | Point-group symmetry sets exact zeros/equalities in `C_IJ` (cubic ⇒ 3 independent) | `11.1:301-304` (§2.4) | mine | `oracle/registry/typed-compositions#mechanical` | The independent-constant count per crystal class is the shape contract for row 60's output. Not stated in canon. |
| A33 | Mechanical observables table — `C_IJ = ∂²Φ/∂η_I∂η_J`; Cauchy stress pull-back `J⁻¹F(∂Φ/∂η)Fᵀ`; `K = (1/9)ΣC_IJ` (Voigt) / Reuss / VRH; `E = 9KG/(3K+G)`, `ν = (3K−2G)/(2(3K+G))`; **Christoffel** `Γ_ik(q̂) = C_ijkl q̂_j q̂_l`, `ρv² = eig Γ`; yield/hardness by constitutive integration | `11.1:308-318` (§2.5) | mine | `oracle/registry/typed-compositions#mechanical` | These are the operations behind registry rows 60, 61, 62 and ledger rows 36, 37, 38. `typed-compositions:86-91` names the formulas without giving them. See Contradictions C2 for the Christoffel/isotropic mismatch. |
| A34 | Mechanical↔thermal coupling: `C_IJ(T) = ∂²F(T,η)/∂η_I∂η_J` via QHA | `11.1:323-327` (§2.6) | mine | `oracle/registry/typed-compositions#mechanical` | The `T`-dependence route for `C_ij` is the QHA one; canon has QHA everywhere but never connects it to row 60. `capability-slices` claims "QHA suffices ≤800 °C" for the MVP — this is the identity that claim rests on. |
| A35 | **The long-wavelength identity**: `Σ_βδ C_αβγδ q̂_β q̂_δ = ρ v²_αγ(q̂)` — the `q→0` acoustic-phonon limit of the dynamical matrix **equals** the elastic tensor | `11.1:328-333` (§2.6), restated `11.1:511-513` (§3.6) and `11.1:696-697` (§S.4) | mine | `oracle/laws/residual-definitions#method-equivalence` | **Highest-value single row in my scope.** This is a *theorem-backed* cross-bundle equivalence pair (B7 mechanics ↔ B2 phonons) binding registry rows 60/62 to rows 9/11 — precisely the `Algebraic/MethodEquivalence` "equivalence pair" sub-kind defined at `3.2:147-150` with tolerance `τ_equiv = 1e-4` (`5.1-cert-obligations.md:141`). **Zero hits for it in `journal/pages/` outside ch. 11.** Canon built the machinery and never declared this pair. It dies with the chapter unless mined. See OQ-A1. |
| A36 | Piezoelectricity couples strain to polarization `P_el = e_{i,IJ} η_IJ`; magnetostriction; deformation potentials shift mobility under strain | `11.1:335-341` (§2.6) | delete | — | Duplication: registry rows 114 `piezoelectric-tensor`, 117 `piezoelectric-polarization-from-strain`, 63 `deformation-potential-gap-shift`, 64 `piezoresistance-coeff`; `coupling-structure:519-521` has the magneto-elastic and piezoelectric channels. |
| A37 | Mechanical numerics: finite-difference stress-strain, **DFPT for `∂²E/∂η_I∂η_J` directly**, FEM (P1/P2), spectral/Fourier on periodic cells, staggered-grid FD, Christoffel diagonalization | `11.1:343-351` (§2.7) | mine | `oracle/registry/computational-methods#reference-implementations` | DFPT is named in ledger row 36 as an alternative path to `C_ij`; the two-path structure (finite-difference vs DFPT) is the basis of a method-equivalence check that is not declared. |
| A38 | `## Regime 3 — Thermal` container | `11.1:355-359` | delete | — | Section scaffolding. |
| A39 | Harmonic expansion of `E_BO`: force-constant matrix `Φ_{IαJβ}` (Hessian) + third-order `Ψ` | `11.1:361-375` (§3.1) | mine | `oracle/registry/typed-compositions#thermal` | `typed-compositions:97` says `HarmonicStiffnessHessianOf(E_BO, R₀, u)` — names the operator, never defines `Φ`. Registry rows 7, 8, 126 all take `Φ` or `D(q)` as input; **`Φ` is defined only here.** Homeless. |
| A40 | Harmonic Hamiltonian; Bloch decomposition; **dynamical matrix** `D_{IαJβ}(q) = (M_IM_J)^{−½} Σ_R Φ(R) e^{−iq·R}`; diagonalization ⇒ `ω²_{qs}`, `ξ_{qs}`; second-quantized diagonal form | `11.1:377-404` (§3.1) | mine | `oracle/registry/typed-compositions#thermal` | Registry rows 7/8/9/126 consume `D(q)`; the mass-weighted Fourier definition that produces it is stated only here. Rows 7 and 8 both annotate their input as "D(q) from the DFPT reference solve (**unregistered input**)" — canon *admits* the gap and never fills it. |
| A41 | Quasi-harmonic Helmholtz free energy `F(T,η) = E_BO(η) + ½Σħω + k_BT Σ ln[1−e^{−ħω/k_BT}]`; **mode Grüneisen** `γ_qs = −∂lnω/∂lnΩ` | `11.1:406-417` (§3.1) | mine | `oracle/registry/typed-compositions#thermal` | Duplicated *within my own scope* at `11.3:246-253` (as `F_vib`) — see Notes, intra-scope duplication. Registry row 12 `grueneisen-mode` and `typed-compositions:104` `QHA-expansion` both consume it. Mine once, from 11.1 (the fuller statement). |
| A42 | Phonon BTE `∂n/∂t + v·∇_r n = C[n]`, `v_qs = ∇_q ω_qs`, three-phonon collision from `Ψ` by Fermi golden rule; RTA `C ≈ (n⁰−n)/τ` | `11.1:419-431` (§3.1) | mine | `oracle/registry/typed-compositions#thermal` | Registry rows 11 `phonon-group-velocity`, 25 `single-mode-rta-lattice-kappa`, 121/122 (4-phonon, iterative LBTE) all rest on this. Partially duplicated at `11.3:48-86` for *carriers*; the phonon instance is 11.1's. |
| A43 | Three thermal state representations: (a) classical MD `(R,P)`, `dim 6N`; (b) mode occupations `{n_qs}` on the Born–von Kármán grid, `3rN_c` modes, at equilibrium fully specified by the single scalar `T`; (c) semiclassical `n_qs(r,t)` | `11.1:433-454` (§3.2) | mine | `oracle/state/multiscale-state#thermal-tier` | The "(b) at equilibrium the whole distribution collapses to one scalar `T`" fact is the justification for phonon distributions being **emergent** in `unified-state:46-53`. `unified-state` asserts the conclusion; the reason is only here. |
| A44 | `D(q)` is itself a *parameter* derived from structural state + BO Hessian, not state | `11.1:456-459` (§3.2) | delete | — | Duplication: `unified-state:46-53` irreducible/emergent split; A12 already covers the general form. |
| A45 | Thermal EOM: classical MD (velocity-Verlet, **symplectic to preserve phase-space volume**); harmonic quantum `iħ ȧ = ħω a`; BTE; QHA "evolution in `T`" via `∂G/∂η = 0` at each `T` | `11.1:461-472` (§3.3) | mine | `oracle/registry/computational-methods#integrators` | The symplecticity requirement is a correctness constraint on any integrator the oracle scores against. `computational-methods` lists no time integrators at all (12 primitives, none is an integrator). |
| A46 | **Acoustic sum rule** `Σ_J Φ_{IαJβ} = 0` ⇔ three zero eigenvalues of `D(q→0)` ⇔ three acoustic branches | `11.1:480-483` (§3.4) | delete | — | Duplication: registry row 7 `acoustic-sum-rule`; `residual-definitions` cat 12 (`3.2:136-139`) states it in the same `Σ_J Σ_R Φ_{IαJβ}(R) = 0` form. |
| A47 | `D(q)` Hermitian; `ω² ≥ 0` in a mechanically stable phase; imaginary modes signal structural instability | `11.1:484-486` (§3.4) | delete | — | Duplication: registry row 8 `dynamical-matrix-hermiticity`; `residual-definitions` cat 10 has `ω² ≥ 0` **with the applicability gate** (`3.2:113-116`) — canon's form is strictly better than the appendix's. |
| A48 | Rotational sum rules constrain higher derivatives of `E_BO` | `11.1:487-488` (§3.4) | delete | — | Duplication: registry row 126 `rotational-sum-rule` (Born–Huang/Gazis–Wallis); `residual-definitions` cat 12. |
| A49 | Crystal momentum `ħq` conserved **modulo `G`** (Umklapp); three-phonon scattering conserves energy and crystal momentum mod `G`; Bose occupations ≥ 0 | `11.1:478-479`, `489-492` (§3.4) | mine | `oracle/laws/residual-definitions#conservation` | cat 9 says "momentum / crystal-momentum" — the **mod-`G` qualifier is absent**, and without it the residual is wrong for every Umklapp process. This is a defect-shaped omission; logging it, not fixing it. |
| A50 | Thermal observables table — `g(ω)` as a delta sum; `C_v(T)` Bose integral; `γ_qs = −dlnω/dlnΩ`; `κ_ij = Σ c v_i v_j τ`; `⟨u²⟩` with `coth(ħω/2k_BT)`; Debye–Waller `W = ½⟨(q·u)²⟩` | `11.1:494-506` (§3.5) | mine | `oracle/registry/typed-compositions#thermal` | Operations behind registry rows 10, 12, 25 and ledger rows for `C_v`, `κ`. `typed-compositions:99-104` gives the composition types only. Debye–Waller and `⟨u²⟩` have **no registry row at all** — see OQ-A3. |
| A51 | Thermal-expansion row: `α_IJ = (1/V) S_IJKL Σ_qs γ^{(KL)}_qs c_v(ω)`, with `S = C⁻¹` the **compliance** and `γ^{(KL)}` the strain-Grüneisen *tensor* (the scalar `γ_qs` is its volumetric trace) | `11.1:502` (§3.5) | mine | `oracle/registry/typed-compositions#thermal` | This is the **corrected** form (the 2026-07-07 gap audit fixed a compliance/stiffness inversion, an undefined tensorial Grüneisen, and a missing `1/V`). Mine the corrected statement; the correction itself is Log-worthy L2. |
| A52 | **e-ph vertex** `g^ν_{mn}(k,q) = ⟨ψ_{m,k+q}|∂_{qν}V|ψ_{n,k}⟩/√(2Mω_{qν})`, with the multi-species generalization carried by the mass-weighted eigenvector normalization `Σ_κ M_κ|e_κ|² = 1` | `11.1:514-522` (§3.6) | mine | `oracle/laws/coupling-structure#electron-phonon` | **Duplicated within my scope** at `11.2:148-152` (§E.6), which defers to "same convention note as group-A §3.6". Mine **once**, from 11.1. `coupling-structure` has the e-ph channel (`3.3:205-225`, `519-521`) but **never writes the vertex**. The single-species `√(2Mω)` shorthand is a live multi-species trap — candidate for `practice/traps`. |
| A53 | IR absorption couples photons to zone-centre LO/TO-split optical phonons via Born effective charges `Z*_{I,αβ} = Ω ∂P_α/∂u_{Iβ}` | `11.1:523-526` (§3.6) | delete | — | Duplication: registry row 92 `operator-position-derivative-tensor` / rows 91–94 linear-response primitives; `born-oppenheimer-levels:91-93` names "the linear-response sub-stage producing Z*, ε∞, χ∞; the LO/TO non-analytic correction". Restated at `11.2:389-391`. |
| A54 | Remaining thermal couplings (spin-lattice `J(R)`, magnon-phonon hybridization, phonon drag, QHA as finite-`T` phase-stability workhorse, ZPE + thermal corrections to adsorption/barriers) | `11.1:527-535` (§3.6) | delete | — | Duplication: `coupling-structure` channel table; QHA↔thermodynamic is A34/A41; ZPE-in-barriers is `11.3:511` (§3.6). |
| A55 | Thermal numerics: frozen-phonon/finite-displacement in a supercell **consistent with the chosen q-mesh**; DFPT at arbitrary `q`; Fourier interpolation of `Φ(R)` with **ASR restored numerically by symmetrization**; BTE solvers (ShengBTE/AlmaBTE/Phono3py/ALAMODE), direct-iterative vs RTA; Verlet/Nosé-Hoover/Langevin thermostats | `11.1:537-549` (§3.7) | mine | `oracle/registry/computational-methods#reference-implementations` | "ASR restored numerically by symmetrization" matters: it means row 7's residual is **zero by construction** in any pipeline that symmetrizes, so scoring it detects nothing unless the un-symmetrized `Φ` is scored. That is a soundness note for the oracle, not a tooling note. See OQ-A4. |
| A56 | `## Cross-regime synthesis` container | `11.1:553` | delete | — | Section scaffolding. |
| A57 | All three regimes are variational over one configuration manifold `Q = {(R,h)} ⊂ ℝ^{3N} × GL⁺(3)`, enlarged to `T*Q` with momenta | `11.1:555-566` (§S.1) | delete | — | Duplication of A6 / `generic-dynamics#nine-regimes-as-extractions`. |
| A58 | **Derivative-order table**: Structural = orders 0,1 · Mechanical = order 2 in `η` · Thermal = order 2 in `u` (+3 anharmonic, +1 in `T`) | `11.1:568-572` (§S.1) | mine | `oracle/laws/generic-dynamics#nine-regimes` | Adds the column `generic-dynamics`'s nine-regime table lacks: *what derivative order of `E_BO` each extraction needs*. Directly determines the AD order (`D`-tag) each bundle requires — a compile-time fact with a home. |
| A59 | The Parrinello–Rahman extended master Hamiltonian `H(R,P,h,Π_h;T)`; each regime is a projection of it | `11.1:574-586` (§S.1) | delete | — | Duplication: `generic-dynamics:68-69` states `E_kin(ions) = Σ|P_I|²/2M_I + tr(Π_hᵀΠ_h)/2W` — the same object in the `E`-functional decomposition. |
| A60 | **"One symmetry, three faces"**: translational + rotational invariance of `E_BO` produces *all three* regime sum rules — zero net force/torque (structural), elastic-tensor zero modes (mechanical), acoustic sum rule (thermal) | `11.1:588-598` (§S.1) | mine | `oracle/laws/residual-definitions#sum-rules` | Registry rows 7 and 126 and cat 9 all exist independently; **the fact that they are one symmetry** is stated only here. It is the reason a PINO cannot satisfy one and violate another, and it is the natural rationale paragraph for cat 12. Also the only statement of the **elastic-tensor zero modes** (rigid motions are not strain). |
| A61 | `E_BO` + the partition function `Z(T,h)` and `F = −k_BT ln Z` are the deepest common canonical; structural/mechanical/thermal observables as their derivatives; `C_IJ(T) = (1/Ω)∂²F/∂η_I∂η_J` | `11.1:600-622` (§S.2) | delete | — | Duplication: `born-oppenheimer-levels` L2/L3 (`2.5:44-51`) states exactly this layering (BO surface, then equilibrium statistics over L1/L2 spectra: partition functions, free energies, QHA, convex hull). `C_IJ(T)` is A34. |
| A62 | The ASCII derivation hierarchy `E_BO → {min; Hessian in R → D(q) → F_vib → F → Hessian in h}` | `11.1:624-644` (§S.2) | delete | — | Duplication: same content as A61 in diagram form; `born-oppenheimer-levels` L1→L4 owns the hierarchy. Diagram adds no fact. |
| A63 | Three irreducible differences: (1) time/statistics category — variational geometry vs hyperbolic PDE vs kinetic theory; (2) DOF dimension — finite vs field vs phase-space distribution; (3) privileged dependent variable — configuration vs strain vs spectrum | `11.1:646-671` (§S.3) | mine | `oracle/state/multiscale-state#kinematic-geometry` | Same shape as `11.3:601-630` (§4.3), which `multiscale-state:406` **already cites and resolves** — for group C. The group-A instance (configuration / strain / spectrum) is not resolved anywhere. Mine it alongside; it is the same argument on different variables. |
| A64 | The group-A state vector `s(t) = (h, R_I, P_I, Π_h, Z_I)`, `dim = 9+6N+9+N` | `11.1:673-690` (§S.4) | delete | — | Duplication: `unified-state:29-40` owns the 7-tuple, of which this is the first five slots. The remaining two (`γ̂`, `A`) come from 11.2 §B.6. Mine nothing; cite `unified-state`. |
| A65 | Continuum fields `u(X)` and reciprocal-space distributions `n_qs` are **emergent representations obtained by coarse-graining or Bloch-transforming `s`** — not irreducible state | `11.1:686-690` (§S.4) | delete | — | **Superseded.** `unified-state:54-67` and `multiscale-state` refine this: non-micro-recoverable quantities are *first-class state in their own tier*, not emergent. `unified-state:63-67` explicitly names this reconciliation. The flat appendix claim is the pre-refinement wording. Delete; do not carry forward. |
| A66 | Residual/constraint catalogue for PINO use: structural `∇_R E_BO = 0` and `∂E_BO/∂h = −Ωσ` **matches measured stress**; mechanical `C_IJ ≻ 0`, Cauchy momentum balance, sound velocity = `q→0` phonon limit; thermal ASR, `D(q)` Hermiticity/non-negativity, MD energy/momentum conservation, BTE detailed balance | `11.1:692-700` (§S.4) | mine | `oracle/laws/residual-definitions#category-provenance` | Mostly covered (cat 9, 10, 12, 13, 16 + rows 7, 8, 57). **Two members are not**: Cauchy momentum balance (A26/A30) and the `q→0` identity (A35). The "matches measured stress" clause is a **ground-truth-bridge** residual — `3.2:288-290` says those subtypes exist; none is enumerated. |
| A67 | Closing thesis: all three regimes reduce to **identities on derivatives of `E_BO`** | `11.1:702-704` (§S.4) | delete | — | Duplication of A6/A57/A61. |
| A68 | Appendix cheat sheet — STATE vs DERIVED for 12 quantities | `11.1:706-721` | delete | — | Duplication: `unified-state:46-67` owns the split; every row restates A12/A28/A44. |
| A69 | Sources verified during research (Wikipedia, Materials Project, VASP wiki, Phonopy, QE FAQ, Durham CMT note, arXiv 1504.05992 AHC) | `11.1:725-736` | delete | — | Provenance-of-the-document, not of a claim. Named-formula records carry `source` per formula (`6.6-named-formulas.md`); a page-level bibliography of Wikipedia articles is not that. The one specific citation (AHC 1504.05992) is already carried by registry row 120 `ahc-gap-renormalization`. |
| A70 | `## Changelog` (2026-07-16 reconciliation; 2026-07-07 gap-audit B7 with the α_IJ correction and the e-ph mass convention) | `11.1:738-747` | delete | — | Scaffolding by D1/D2. Content preserved: the α_IJ correction → Log-worthy **L2**; the e-ph convention → Log-worthy **L3**; the corrected statements themselves → A51, A52. |

### 11.2 `deriv-electronic` — Group B (electronic, magnetic, optical)

| # | Fact or block | Current location | Disposition | Target | Evidence |
|---|---|---|---|---|---|
| B1 | Frontmatter | `11.2-deriv-electronic.md:1-12` | delete | — | Same as A1. Frontmatter here **is** valid YAML (verified). |
| B2 | Units are **Gaussian** unless marked (the `4π` in f-sum, dielectric, Maxwell source); `Quantity.unitsOf` is canonical for implementation | `11.2:15` (banner) | delete | — | Duplication: trap 11 `10.4-traps.md:109-110` ("Gaussian units and the 4π in the Maxwell source term … — advisory"); `6.2-typeclass-alphabet.md:26` owns `unitsOf`. |
| B3 | **Three response objects collide**: density–density `χ`, polarization–polarization `χ^PP`, current–current `χ^JJ` — related, not identical; and `χ^JJ` "is never related back to `χ` in the body" | `11.2:15` (banner) | mine | `practice/traps#three-response-objects` | Live hazard. **Not a trap today** — zero hits for `χ^PP`/`χ^JJ`/"current-current" in `10.4-traps.md`. The banner **admits the derivation it promises is missing** — a self-declared dangling promise inside the page. See OQ-B1. |
| B4 | Framing + "the three regimes are instances of one effective one-body theory on `γ̂(r,r';t)` with a state-dependent `Ĥ[γ̂]` and a closure functional" | `11.2:17-26` | mine | `oracle/state/gamma-hat#one-body-substrate` | The framing sentence is scaffolding; the **`Ĥ[γ̂]` + closure** thesis is B15's seed and is the organizing claim for L1. `gamma-hat` is about *encoding*, not about what `γ̂` is. |
| B5 | Notation: `γ̂` (1RDM, promoted to 2×2 Pauli-spinor), `ρ = tr_σ γ̂(r,r)`, `m = tr_σ[σ γ̂(r,r)]`, `ψ_{nk}`/`f_{nk}`, `A/E/B`, `χ`, `L̂` | `11.2:28-40` | mine | `practice/glossary` + `oracle/state/gamma-hat#definition` | **Homeless fact of the `Environment` class.** `unified-state:37` names the slot "one-body density matrix, 2×2 Pauli-spinor operator"; `gamma-hat` covers encodings. Neither states `ρ = tr_σ γ̂(r,r)` or `m = tr_σ[σγ̂]`. Yet `residual-definitions` cat 10 (`3.2:120-129`) imposes `γ̂† = γ̂`, `0 ⪯ γ̂ ⪯ 1`, `‖γ̂²−γ̂‖²` on it. Constraints on an undefined object. |
| B6 | `## Regime 1 — Electronic` container | `11.2:44` | delete | — | Section scaffolding (heading only). |
| B7 | Hohenberg–Kohn + Kohn–Sham: `E_KS[ρ; R,h]` with explicit Hartree double integral; `Ĥ_KS = −(ħ²/2m)∇² + v_eff`, `v_eff = v_ext + v_H + v_xc`; SCF loop `Ĥ_KS[ρ]ψ = εψ`, `ρ = Σ f|ψ|²` | `11.2:46-67` (§E.1) | delete | — | Duplication: `born-oppenheimer-levels` L1 (`2.5:38-43`) names Kohn–Sham/TDKS/TDCSDFT, Hohenberg–Kohn, Runge–Gross, Liouville–von Neumann; `generic-dynamics:70` states `E_KS[γ̂]` = kinetic + Hartree + xc. The equations add detail but no fact canon lacks a name for. |
| B8 | **Runge–Gross ⇒ TDKS**; Liouville–von Neumann `iħ ∂_t γ̂ = [Ĥ_KS, γ̂]`; **the equation is nonlinear in `γ̂`** because `Ĥ_KS` depends on `ρ(t)` | `11.2:69-84` (§E.1) | mine | `oracle/laws/generic-dynamics#gamma-block` | The **nonlinearity** is the operative fact and is not in canon. `generic-dynamics:134-139` gives the `γ̂`-block as Lie–Poisson `[·, γ̂]` with `Ĥ_KS = δE/δγ̂` — that form *presupposes* the state-dependence without saying it makes the EOM nonlinear. Consequential for any adjoint/AD claim over the `EOM/γ̂` residual. |
| B9 | van Leeuwen's Keldysh-contour action is the variational principle under TDKS; it resolves the TDDFT "causality paradox" | `11.2:85-87` (§E.1) | mine | `oracle/laws/generic-dynamics#gamma-block` | Zero hits for "van Leeuwen"/"Keldysh"/"causality paradox" in `journal/pages/` outside ch. 11. It is the well-posedness warrant for the `EOM/γ̂` residual existing at all. Small, load-bearing, homeless. |
| B10 | Electronic state: occupied KS orbitals `{ψ_nk}` + occupations `{f_nk}`, equivalently `γ̂ = Σ f_nk ψ_nk ψ*_nk`; derived = `ρ`, `E[ρ]`, `ε_nk`, `E_F`, bands; discretized size `N_G × N_k × N_b` complex | `11.2:89-107` (§E.2) | mine | `oracle/state/gamma-hat#definition` | The spectral decomposition of `γ̂` is its definition (B5). The `N_G × N_k × N_b` sizing is the input to `gamma-budget`; `gamma-hat`'s encoding vocabulary assumes it. |
| B11 | Electronic dynamics: SCF is a **fixed-point iteration, not a time dynamics** (Pulay/Anderson/Broyden mixing); TDKS integrators (Crank–Nicolson, Magnus, split-operator); imaginary-time gradient flow | `11.2:109-117` (§E.3) | mine | `oracle/registry/computational-methods#integrators` | "SCF is not time evolution" parallels A13 and matters for the same reason. `computational-methods:35-38` lists `SCF-mixing`/`Pulay-mixing` as `variational-minimization` sub-methods — consistent, and this is the statement of *why*. `generic-dynamics:114` says "SCF as gradient flow on `E_KS`" — see Contradictions C3. |
| B12 | Electronic constraints: orthonormality (exact under unitary TDKS, Gram–Schmidt otherwise); particle number; **`T=0` idempotency `γ̂² = γ̂`**; causality of response kernels; Hermiticity of `Ĥ_KS` and `γ̂`; residual gauge invariance | `11.2:119-130` (§E.4) | delete | — | Duplication: `residual-definitions` cat 10 (`3.2:120-129`) has γ̂ Hermiticity, `0 ⪯ γ̂ ⪯ 1`, and idempotency **with an applicability gate to claimed-zero-temperature states** — canon's form is strictly better. cat 9 has particle number. Orthonormality is implied by `0 ⪯ γ̂ ⪯ 1`. |
| B13 | Electronic observables table — bands by BZ-path diagonalization; `g(E)` delta sum; `E_g = min ε_c − max ε_v`; `ρ` as diagonal trace; `E_KS[ρ]`; Hellmann–Feynman forces; `E_F` from `∫^{E_F} g = N` | `11.2:132-142` (§E.5) | mine | `oracle/registry/typed-compositions#electronic` | Operations behind registry rows 1, 2, 4, 5, 103. `typed-compositions:62-68` gives the composition types only. Row 5 `fermi-level-charge-neutral` in particular has its defining integral only here. |
| B14 | Electronic couplings (HF forces drive ions; e-ph vertex; BTE reads `ε_n(k)` and `v_n(k) = ħ⁻¹∇_k ε`; `F_el = E − TS_el`; charge-transfer/Marcus; shared `γ̂` with optical/magnetic) | `11.2:144-160` (§E.6) | delete | — | Duplication: `coupling-structure` channel table; e-ph vertex is A52 (same fact, deferred to group A by the page itself at `11.2:150`); `generic-dynamics:81` has `S_electronic` = Fermi–Dirac entropy of the `γ̂` spectrum. |
| B15 | `v_n(k) = ħ⁻¹ ∇_k ε_n(k)` (band group velocity) | `11.2:153-154` (§E.6) | delete | — | Duplication: registry row 11 `phonon-group-velocity` is the phonon analogue; the carrier form is restated at `11.3:61` and consumed by rows 14/23/24. Mine once from 11.3 (row C7) — it is group C's object. |
| B16 | Electronic numerics + **sparsity structure**: plane waves dense in `G`; LCAO sparse in `r`; tight-binding sparse in `R` (Wannier-localized); GW/BSE Green's-function methods | `11.2:162-168` (§E.7) | mine | `oracle/compilation/representation-substrate#basis-sparsity` | The basis↔sparsity mapping is a **compile-time fact**: it determines which `CompressionPlan` is admissible per basis. `gamma-hat`'s encoding vocabulary and `representation-substrate` both assume a sparsity model and neither states this one. |
| B17 | `## Regime 2 — Magnetic` container | `11.2:172` | delete | — | Section scaffolding. |
| B18 | Spin-DFT: `Ĥ_KS = [−(ħ²/2m)∇² + v_eff]𝟙 + μ_B σ·B_eff`, `B_eff = B_ext + B_xc[m]`; this is **non-collinear spin-DFT (von Barth–Hedin / Kübler)**, *distinct from* Vignale–Rasolt (which is current-DFT) | `11.2:174-188` (§M.1) | mine | `oracle/state/gamma-hat#spin-sector` | `properties.md:24-26` admits Magnetic "has no dedicated bundle, because `γ̂` is mean-field". So the magnetic sector is the thinnest part of canon, and this is its **only** derivation. The von Barth–Hedin vs Vignale–Rasolt distinction is a correction the 2026-07-07 audit made (Log-worthy **L4**) and it exists nowhere else — deleting it re-opens the error. |
| B19 | Atomistic spin model `Ĥ_spin = −Σ J_ij S_i·S_j − Σ K_i(S_i·ê_i)² − μ_s Σ S_i·B_ext`, `|S_i| = 1` | `11.2:190-197` (§M.1) | mine | `oracle/laws/residual-definitions#positivity` | **`residual-definitions` cat 10 lists `|S_i| = 1` as a Positivity residual (`3.2:114`) and `S_i` is defined nowhere in canon** — grep confirms the only other hit is `coupling-structure:527`'s "exchange / Heisenberg" channel row. A residual on an undefined symbol. Second homeless fact of the `Environment` class in this page. |
| B20 | Magnetic state: microscopic Pauli-spinor orbitals / `γ̂_{αβ}`; derived `ρ`, `m`, atomic moments `μ_I = ∫_{Ω_I} m` over Wigner–Seitz spheres; atomistic `{S_i ∈ S²}` one per atom; derived `M`, susceptibility, magnon dispersion | `11.2:199-212` (§M.2) | mine | `oracle/state/gamma-hat#spin-sector` | The Wigner–Seitz sphere integral is the operation behind `typed-compositions:110`'s `atomic-sphere-spin-integral` extractor, which is named and never defined. `{S_i ∈ S²}` is the domain B19's constraint lives on. |
| B21 | Magnetic dynamics: TDKS-Pauli; `∂_t m = γ m × B_xc + (non-local xc torque)` with `B_xc` generally non-parallel to `m` | `11.2:214-229` (§M.3) | mine | `oracle/laws/generic-dynamics#magnetic-extraction` | `generic-dynamics:115` says the magnetic extraction is "spin EOM = `L` (precession) + `M` (orientation-preserving relaxation `S × (S × H_eff)`)" — it asserts the split and never gives the precession equation. This is it. |
| B22 | **Landau–Lifshitz–Gilbert** `dS_i/dt = −γ S_i × H_i^eff + α S_i × dS_i/dt`, `H_i^eff = −(1/μ_s)∂H_spin/∂S_i`; explicit Landau–Lifshitz form with `γ' = γ/(1+α²)`, `λ = γα/(1+α²)` | `11.2:231-244` (§M.3) | mine | `oracle/laws/generic-dynamics#magnetic-extraction` | **Zero hits for "Landau–Lifshitz"/"LLG"/"Gilbert" in `journal/pages/` outside ch. 11.** `generic-dynamics:115`'s `S × (S × H_eff)` is verbatim the LL damping term — canon uses the equation's tail without the equation. The `γ'`/`λ` reparametrization is what makes the two forms agree; it is the sort of thing that silently drifts. |
| B23 | Magnetic constraints: `|S_i| = 1` preserved exactly by LLG (both terms lie in the tangent plane); spinor orthonormality under unitary TDKS; **`M` conserved without SOC, only `J = L + S` with SOC**; time-reversal `m → −m`, broken in FM equilibrium | `11.2:246-255` (§M.4) | mine | `oracle/laws/residual-definitions#positivity` | The *reason* `|S_i| = 1` is exactly preserved (tangency) is the warrant for cat 10 scoring it as a hard constraint rather than a soft penalty. The SOC caveat on total-spin conservation is a correctness condition on cat 9's "spin" entry, absent from `3.2:107-112`. |
| B24 | Magnetic observables — moments by WS integral; spin density `tr_σ[σγ̂]`; **Liechtenstein exchange** `J_ij = (1/2π) Im ∫dE Tr[Δ_i G_ij Δ_j G_ji]`; magnon dispersion by spin-wave linearization; `T_C`/`T_N` by mean-field or MC on Heisenberg | `11.2:257-265` (§M.5) | mine | `oracle/registry/typed-compositions#magnetic` | `typed-compositions:110-113` gives `ExchangeInteractions = ResponseOfTo(…, kernel = exchange-coupling-formula, …)` — the kernel is **named and never defined**, and no registry row implements it. Liechtenstein is that kernel. Zero hits for "Liechtenstein" outside ch. 11. |
| B25 | Magnetic couplings (magneto-elastic `H_me = −B Σ S_i·ε_ij·S_j`; spin-lattice relaxation; magnetostriction; magnon heat capacity; magnon-phonon scattering; spin/anomalous Hall; magnetochemistry) | `11.2:267-274` (§M.6) | delete | — | Duplication: `coupling-structure:519-527` channel table carries magneto-elastic and exchange/Heisenberg; A54 covers the thermal side. |
| B26 | Magnetic numerics (spinor plane-wave DFT `nspin=4`; KKR Green's-function for `J_ij`; atomistic LLG codes; spin-wave linearization) | `11.2:276-280` (§M.7) | mine | `oracle/registry/computational-methods#reference-implementations` | Same class as A21/A37/A55; flagged in Notes as possibly droppable. |
| B27 | `## Regime 3 — Optical` container + "three distinct regimes of optical response, all reducible to operators on `γ̂`" | `11.2:284-289` | delete | — | Duplication of B4's thesis. |
| B28 | Linear response: `χ(r,r',ω) = δρ/δv_ext`; `ε⁻¹ = δ(r−r') + ∫v(r−r'')χ(r'',r',ω)`; TDDFT Dyson `χ = χ₀ + χ₀(v + f_xc)χ` with KS `χ₀` and `f_xc = δv_xc/δρ` | `11.2:291-304` (§O.1) | mine | `oracle/registry/typed-compositions#optical` | This is the **definitional home the B3 banner points at**. Registry rows 93 `high-frequency-response-tensor` and 94 `electronic-linear-response-tensor` consume `χ`; the Dyson equation that defines it appears nowhere else. Mining B28 without B3's collision note leaves the χ/χ^PP/χ^JJ ambiguity unmarked. |
| B29 | **Bethe–Salpeter** `L = L₀ + L₀KL`, kernel `K = −iv + iW`; required for excitonic bound states absent from RPA/TDDFT | `11.2:306-314` (§O.1) | mine | `oracle/registry/typed-compositions#optical` | **Zero hits for "Bethe" in `journal/pages/` outside ch. 11.** `born-oppenheimer-levels:111-113` defers "BSE iterative variants" to Layer 1.75 — canon schedules BSE and never says what it is. A deferred item still needs a definition to be deferred *coherently*. |
| B30 | Semiconductor Bloch equations for `p_k(t)`, `n_k^{e/h}(t)` with Rabi `Ω_k = d_k·E + Σ V_{kk'}p_{k'}`; Maxwell coupling `(∂_t² + c²∇×∇×)E = −4π ∂_t² P`, `P = Σ d_k p_k` (Gaussian) | `11.2:316-332` (§O.1) | mine | `oracle/registry/typed-compositions#optical` | The `4π` here is the one trap 11 (`10.4-traps.md:109`) warns about, and it is the **corrected** form (the 2026-07-07 audit restored a missing `4π` — Log-worthy **L5**). Trap 11 is "advisory" with no enforced page; this is the statement it should point at. |
| B31 | Optical state: linear response = equilibrium KS + perturbative `δρ` (derived); real-time TDDFT = TDKS orbitals **+ the EM field `A`** with `E = −∂_tA − ∇φ`, `B = ∇×A`; SBE = `{p_k}`, `{n_k^{e,h}}` + macroscopic `E` | `11.2:334-346` (§O.2) | mine | `oracle/state/unified-state#slot-A` | This is the **provenance of the `A` slot** in the 7-tuple. `unified-state:39-44` carries `A` with the Weyl-gauge/transversality note but never says which physics requires it to be state; `generic-dynamics:169-182` owns the gauge partition. The "why `A` is a slot" answer is only here. |
| B32 | Optical dynamics: TDKS, or SBE/Maxwell–Bloch, or linear-response Dyson update; **causal structure `χ(t<0) = 0` enforced at the dynamics level** | `11.2:348-352` (§O.3) | mine | `oracle/laws/residual-definitions#kramers-kronig` | cat 11 `Algebraic/Kramers-Kronig` says "causality dispersion identities on response functions" — the causality condition `χ(t<0)=0` that *generates* those identities is stated only here. Mining KK without it leaves cat 11 unfounded. |
| B33 | Kramers–Kronig `Re ε(ω) = 1 + (2/π)P∫ ω'Im ε(ω')/(ω'²−ω²) dω'` | `11.2:354-361` (§O.4) | delete | — | Duplication: `residual-definitions` cat 11; `6.2-typeclass-alphabet.md` and `10.3-audit-prompt.md` both reference KK. Canon names it; the closed form adds an integral that belongs on a registry row, and no row implements it — see OQ-B2. |
| B34 | **f-sum (Thomas–Reiche–Kuhn)** `(2/π)∫ω Im ε dω = ω_p² = 4πn_e e²/m` | `11.2:363-367` (§O.4) | delete | — | Duplication: `residual-definitions` cat 12 (`3.2:136-137`) states the f-sum in the same `(2/π)∫ω·Im ε dω = ω_p²` form. Canon's is complete. |
| B35 | Gauge invariance under `A → A + ∇χ`, `φ → φ − ∂_tχ`; charge conservation `∂_tρ + ∇·J = 0` | `11.2:369-371` (§O.4) | delete | — | Duplication: `generic-dynamics:169-182` owns the normative gauge paragraph and states gauge invariance of observables is architectural (registry row 104); `residual-definitions` cat 9 owns charge conservation. |
| B36 | Optical observables — `ε_αβ = δ_αβ + 4πχ^PP`; `α(ω) = (ω/c)·2Im√ε = (ω/c)Im ε/n`; `n(ω) = Re√ε`; anisotropic tensor diagonalization; **`σ(ω) = −iωχ^JJ/(4π)`**; Van Roosbroeck–Shockley PL; optical matrix elements `⟨ψ_c|p̂|ψ_v⟩` or Berry connection | `11.2:373-383` (§O.5) | mine | `oracle/registry/typed-compositions#optical` | Operations behind `typed-compositions:73-77` (`absorption-from-dielectric`, `refractive-index-from-dielectric`) and registry rows 93/94/134. **This table is where all three `χ` variants appear together** — mine it together with B3 and B28 or the collision is silently propagated. |
| B37 | Optical couplings (shared `γ̂`; IR/Raman via `Z*`; phonon-assisted indirect transitions; AHC gap `T`-dependence; **`σ_DC = lim_{ω→0} Re σ(ω)` is Kubo–Greenwood — "the optical regime IS the dynamical-response form of transport"**; photochemistry) | `11.2:385-397` (§O.6) | delete | — | Duplication: `Z*`/LO-TO is A53; AHC is registry row 120; the "optical IS transport" identity is restated at `11.3:661-663` (§4.4) and is the **declared MethodEquivalence pair** BTE-σ ≡ Kubo-σ already in `residual-definitions:149` and `typed-compositions:123-128`. Mine once from 11.3 (row C48). |
| B38 | Optical numerics (real-time TDDFT: Octopus/Yambo; linear-response TDDFT: Sternheimer/Casida; GW; BSE-on-GW; Wannier interpolation for fine k-meshes) | `11.2:399-404` (§O.7) | mine | `oracle/registry/computational-methods#reference-implementations` | Same class as A21/A37/A55/B26. G₀W₀ is wired for the MVP (`born-oppenheimer-levels:123-125`); Wannier–EPW is named at `6.1-canonical-vocabularies.md:75`. |
| B39 | `## Cross-regime synthesis` container | `11.2:408` | delete | — | Section scaffolding. |
| B40 | Shared substrate — the five `γ̂` modes: static/scalar/idempotent · time-evolved scalar (LvN) · 2×2 Pauli-spinor · minimally coupled to `A` (`p̂ → p̂ − (e/c)A`) with `χ = −i⟨[ρ̂(t),ρ̂(0)]⟩θ(t)` · BSE two-particle `L` from `γ̂` and `G` | `11.2:410-426` (§B.1) | mine | `oracle/state/gamma-hat#one-body-substrate` | The **minimal-coupling substitution** `p̂ → p̂ − (e/c)A` is the mechanism by which the `A` slot enters the matter Hamiltonian; `coupling-structure` has a "minimal coupling" channel (`generic-dynamics:76-77`) and never writes it. The retarded-`χ` definition closes B3/B28. |
| B41 | Variational backbone: HK/KS minimization; Runge–Gross + van Leeuwen + **Dirac–Frenkel** for TDKS orbitals; LLG from a Poisson-bracket structure on `(S²)^N` with Rayleigh dissipation, ≈ gradient descent on `H_Heisenberg` at small angles | `11.2:428-438` (§B.2) | mine | `oracle/laws/generic-dynamics#magnetic-extraction` | The LLG-as-Poisson-plus-Rayleigh statement is **exactly the GENERIC `L + M` decomposition of the magnetic regime** that `generic-dynamics:115` asserts without derivation. This is its warrant. Dirac–Frenkel: zero hits outside ch. 11. |
| B42 | Effective single-particle structure: every regime = (state-dependent one-body operator) + (closure functional); closure table Electronic `v_xc` · Magnetic `B_xc` + non-collinear xc · Optical `f_xc` + BSE kernel `K` | `11.2:440-456` (§B.3) | mine | `oracle/state/gamma-hat#closure-functionals` | The closure table is the **L1 model-form-error inventory**: each closure is where the approximation (and therefore the `σ` in the ledger) enters. `residual-definitions:302-305` requires a model-form-error term per residual; this names the L1 sources. Not in canon. |
| B43 | Deepest common canonical: relativistic `Ĥ = α·(p̂ − (e/c)A) + βmc² + V + V̂_ee` with **SOC contained in the Dirac kinetic term (must not be added by hand)**; reduction to four-current `j^μ = (ρ, J)` + `m` gives current-spin-density functional `E[ρ,J,m]` (Vignale–Rasolt + spin); all three regimes = TDCSDFT instances | `11.2:458-477` (§B.4) | mine | `oracle/state/gamma-hat#tdcsdft-root` | `born-oppenheimer-levels:42-43` names "TDCSDFT" for L1 and never unpacks it. The **SOC-double-count warning** is a 2026-07-07 correction (Log-worthy **L6**) and is a live trap: adding an explicit SOC term on top of a Dirac operator double-counts. Trap candidate. |
| B44 | Irreducibly different: **tensor rank and parity of the order parameter** — `ρ` rank-0; `m` rank-1 **axial**; `A` rank-1 **polar** + current `J` polar; transformation under inversion and time reversal (`ρ` even, `m` odd, `J` odd, `A` odd) | `11.2:479-485` (§B.5) | mine | `oracle/registry/canonical-vocabularies#tensor-parity` | Directly determines equivariance behaviour (registry row 104, `Algebraic/Symmetries` cat 14). A residual that treats `m` as polar is wrong under inversion. **Zero statement of slot parities anywhere in canon** — `unified-state` gives types, not transformation classes. Strong homeless-fact candidate. |
| B45 | Irreducibly different: time structure — electronic and magnetic ground states are fixed points; **optical absorption is intrinsically a frequency-domain object on a causal kernel, its existence depends on temporal non-locality**; pure TD dynamics of `γ̂` does not distinguish them — the distinction is in which observable is read | `11.2:487-491` (§B.5) | mine | `oracle/laws/residual-definitions#kramers-kronig` | Explains why cat 11 exists as a separate category rather than folding into `EOM/γ̂`: the causal kernel is not recoverable from a single trajectory sample. Directly relevant to how the PINO must be sampled. |
| B46 | Irreducibly different: **locality of the closure** — local KS works for electronic (LDA/GGA); magnetic needs non-collinear/transverse-gradient; optical needs long-range `f_xc ~ 1/|r−r'|` for excitonic binding, **LDA fails qualitatively** | `11.2:493-497` (§B.5) | mine | `oracle/certification/applicability-classifiers#closure-locality` | A **qualitative** failure mode, not a tolerance miss — exactly what an applicability classifier must refuse rather than score. `out-of-scope`/`applicability-classifiers` carry no closure-locality gate today. |
| B47 | Practical synthesis: group-B state unifies as `γ̂(r,r';t)` (or `{ψ_{nk,α}}` + `{f_nk}`) **plus external `A(r,t)` when driving**; every observable in `properties.md` is a derived functional of it; the sum rules are residuals computable **without additional DOFs** | `11.2:499-509` (§B.6) | delete | — | Duplication: this is slots 6 and 7 of `unified-state:29-40`, whose 7-tuple already includes `γ̂` and `A`. "No additional DOFs" is `unified-state:46-53`'s emergence axiom. **Note the citation `` `properties.md` `` is a raw filename** — a third citation syntax, unchecked (probe 2); see Notes. |
| B48 | Sources verified during research | `11.2:513-522` | delete | — | Same reason as A69. |
| B49 | `## Changelog` (2026-07-16 banner conversion; 2026-07-07 gap-audit B7: Vignale–Rasolt mislabel, SOC double-count, missing `4π`, e-ph mass convention) | `11.2:524-533` | delete | — | Scaffolding (D1/D2). Content preserved: Log-worthy **L4**, **L6**, **L5**, **L3**; the corrected statements → B18, B43, B30, A52. |

### 11.3 `deriv-transport` — Group C (transport, thermodynamic, chemical/surface)

| # | Fact or block | Current location | Disposition | Target | Evidence |
|---|---|---|---|---|---|
| C1 | Frontmatter | `11.3-deriv-transport.md:1-14` | delete | — | Same as A1. **But**: `referenced-by` lists `multiscale-state`, and unlike the other two this page **is cited in canon prose seven times** — see C50 and Notes. Valid YAML (verified). |
| C2 | κ appears in both Green–Kubo (total) and electronic (Kubo second-moment) forms; observable-level decomposition `κ_total = κ_el + κ_ph`; regimes referred to by name, the canonical nine-regime table is `generic-dynamics`'s | `11.3:17` (banner) | delete | — | Duplication: `6.2-typeclass-alphabet.md:27` owns `κ = κ_el + κ_ph`; `generic-dynamics:107-119` owns the nine-regime table. The banner is a pointer, not a fact. |
| C3 | Framing + "the synthesis argues for GENERIC with a free-energy/entropy Lyapunov functional, while identifying the irreducibly different kinematic geometries" | `11.3:19-30` | delete | — | Abstract of the page's own §4; the claims land at C43/C46. |
| C4 | Notation (`∂_t`, `∇_r`, `∇_p`/`∇_k`, `δ/δx`, `⟨·⟩`, `β = 1/k_BT`) | `11.3:32-35` | mine | `practice/glossary` | Same class as A7/B5. `β` in particular is used unglossed in `residual-definitions` and `multiscale-state`. |
| C5 | `## 1. Transport regime` container + the framing question | `11.3:39-46` | delete | — | Section scaffolding. |
| C6 | **Boltzmann transport equation** `∂_t f_n + v_n·∇_r f_n + (1/ħ)F·∇_k f_n = C[f]_n`, `F = −e(E + v×B)` | `11.3:48-63` (§1.1) | mine | `oracle/state/multiscale-state#macro-tier` | **Already cited from canon**: `multiscale-state:104`, `:294`, `:319`, `:336` all cite `[deriv-transport] §1.1`. The equation itself is not restated at the target — `multiscale-state:104` says "…lifted from one cell to a device mesh" and relies on §1.1 for the object. **This is a dangling promise in reverse**: canon leans on a page it is about to delete. Must be mined *into* `multiscale-state` before the citation is dropped. |
| C7 | Band group velocity `v_n(k) = ħ⁻¹∇_k ε_n(k)` | `11.3:61` (§1.1) | mine | `oracle/registry/typed-compositions#transport` | Consumed by registry rows 14, 19, 23, 24 and by `multiscale-state:310`'s acoustic branch. Defined only here and (redundantly) at `11.2:154`. Mine once from here. |
| C8 | Fermi-golden-rule collision integral with in/out scattering and Pauli blocking `(1−f)`; **detailed balance makes `f⁰` (Fermi–Dirac) stationary**; Maxwell–Boltzmann for classical ions, Bose–Einstein for phonons | `11.3:64-76` (§1.1) | mine | `oracle/state/multiscale-state#macro-tier` | The blocking factors are the difference between a correct and an incorrect collision residual. `residual-definitions` cat 13 names detailed balance; the collision-integral form it constrains is only here. |
| C9 | **RTA** `C[f]_n ≈ −(f_n − f_n⁰)/τ_n(k)`, "what `BoltzTraP`-style codes actually solve" | `11.3:78-86` (§1.1) | mine | `oracle/registry/typed-compositions#transport` | Registry row 25 `single-mode-rta-lattice-kappa` and rows 121/122 (the 4-phonon and iterative-LBTE **consistency pair**, `residual-definitions:155-156`) are all defined against RTA. The approximation they are a model-gap *from* is stated only here. |
| C10 | **Kubo–Greenwood** `σ_αβ(ω) = (1/V)∫₀^∞ dt e^{iωt}⟨ĵ_α(t)ĵ_β(0)⟩_eq`; **Green–Kubo** `κ_αβ = (1/Vk_BT²)∫₀^∞ dt⟨Ĵ^Q_α(t)Ĵ^Q_β(0)⟩_eq` | `11.3:87-101` (§1.1) | mine | `oracle/registry/typed-compositions#transport` | `typed-compositions:123-124` names `ConductivityViaKubo = ResponseOfTo(…, kernel = current-current-correlator, frequency = ω→0⁺)` — the correlator is named, never written. This is one half of the declared BTE≡Kubo equivalence pair (`residual-definitions:149`, cert obligation 6). |
| C11 | The **equivalence of linearized BTE-RTA and Kubo–Greenwood** for non-interacting carriers is a textbook result, derived via the Wigner function / Dyson gradient expansion | `11.3:103-106` (§1.1) | mine | `oracle/laws/residual-definitions#method-equivalence` | This is the **agreement theorem** that makes BTE-σ ≡ Kubo-σ an *equivalence pair* (`τ_equiv`) rather than a *consistency pair* (`τ_method`). `residual-definitions:147-150` turns entirely on which sub-kind applies and cites no theorem. Deleting this makes the pair's classification unfounded. |
| C12 | **Drift-diffusion / Poisson–Nernst–Planck** `∂_t n_i + ∇·j_i = G_i − R_i`, `j_i = μ_i n_i E − D_i∇n_i`, `∇·(ε∇φ) = −ρ`; **Einstein relation** `D_i = μ_i k_BT/q_i` enforcing consistency | `11.3:108-120` (§1.1) | delete | — | Duplication: `multiscale-state:336` states the three macro balance PDEs and cites this section; `multiscale-state:324` owns the Einstein relation **with its degenerate-statistics caveat as a declared model-form error** — strictly better than the bare statement here. `residual-definitions` cat 13 names it. |
| C13 | Transport state: `{f_n(r,k,t)}` on 6-D phase space, dimensionless in `[0,1]` (fermions) or `[0,∞)` (bosons); phonon `{n_λ(r,q,t)}` with BE equilibrium; ionic drift-diffusion `{n_i(r,t)}` + `φ` **constrained by Poisson, not independent** | `11.3:121-132` (§1.2) | mine | `oracle/state/multiscale-state#macro-tier` | Cited from canon at `multiscale-state:292` ("*scored*, not free; [deriv-transport] §1.2"). The "`φ` is constrained, not independent" fact is what that citation is *for* — it justifies `multiscale-state:414`'s "quasi-static (constraints `φ`,`j`)". Must land at the target. |
| C14 | Derived (not state): number/current/energy densities, stress — all **contractions of `f` against an operator and the band tabulation** | `11.3:134-136` (§1.2) | delete | — | Duplication: `unified-state:46-53` emergence axiom; `multiscale-state` tier table. |
| C15 | Discretized BTE on a tetrahedron/uniform k-mesh × real-space grid; **collision integral local in `r`, nonlocal in `k`**; explicit (BGK/RTA) vs implicit iterative; steady state ⇒ a linear matrix problem | `11.3:138-153` (§1.3) | mine | `oracle/compilation/representation-substrate#sparsity` | The locality structure is a **compile-time sparsity fact** — restated at `11.3:206-208` (§1.7). It determines the achievable `CompressionPlan` for a transport kernel. Same class as B16 and equally absent from canon. |
| C16 | Transport conservation: charge continuity from `∫C[f]d³k = 0`; momentum/energy moments of `C` vanish in clean systems | `11.3:157-161` (§1.4) | delete | — | Duplication: `residual-definitions` cat 9. |
| C17 | **Onsager reciprocity** `L_αβ(B) = L_βα(−B)` from microscopic time-reversal | `11.3:162-163` (§1.4) | delete | — | Duplication: `residual-definitions` cat 14 (`3.2:143-144`) names Onsager reciprocity; `coupling-structure` and `typeclass-alphabet` both reference it. Note the `B`-argument form is restated at `11.3:197-199` with transposed indices — see Contradictions C4. |
| C18 | **H-theorem**: `S[f] = −k_B Σ_n ∫{f ln f + (1−f)ln(1−f)}`, `dS/dt ≥ 0`, equality only at local equilibrium | `11.3:164-166` (§1.4) | mine | `oracle/laws/generic-dynamics#entropy-functional` | `generic-dynamics:81` decomposes `S[x] = S_vib + S_electronic + S_config` — **the Fermi-blocking entropy of a carrier distribution matches none of the three**. It is the `S` that the `EOM/Continuum` and transport residuals differentiate. Homeless. Also the direction-corrected form (2026-07-07 audit, Log-worthy **L7**). |
| C19 | PSD of transport coefficients `σ, κ, μ` from the bilinear Kubo structure; bounds `0 ≤ f_n ≤ 1`, `n_λ ≥ 0`; detailed balance `W_{i→j}f⁰_i(1−f⁰_j) = W_{j→i}f⁰_j(1−f⁰_i)` | `11.3:167-171` (§1.4) | delete | — | Duplication: `residual-definitions` cat 10 has `f ∈ [0,1]`, `σ ⪰ 0`, `M ⪰ 0`; cat 13 has detailed balance. |
| C20 | Transport observables — `n = ∫f d³k/(2π)³`; `j = −eΣ∫v_n f`; `σ_αβ = e²Σ∫τ v_α v_β(−∂f⁰/∂ε)`; `μ = σ/(en)`; Seebeck as a weighted `σ`-integral; `κ` as the second-moment Kubo integral; `D_i = μ_ik_BT/q_i` | `11.3:173-184` (§1.5) | mine | `oracle/registry/typed-compositions#transport` | Cited from canon at `multiscale-state:294` ("[deriv-transport] §1.1, §1.5"). Operations behind registry rows 14, 15, 19, 23, 24. `typed-compositions:119-129` gives composition types only. |
| C21 | Migration barrier `E_a^i` comes from NEB in the chemical regime | `11.3:184` (§1.5) | delete | — | Duplication: `typed-compositions:140-141` `MigrationBarrier = PathStationaryOf(…, method = climbing-image-NEB, n_images = 9, tol = 1e-3)` — canon's is more specific. |
| C22 | Transport couplings (bands/velocities from electronic; scattering from defects, strain, e-ph; `T` in `f⁰` and phonon distributions as transport state; **bidirectional e-ph energy exchange — Joule heating, Peltier**; chemical barriers set `D_i`; `B` generates Hall and switches Onsager to `L(B) = Lᵀ(−B)`) | `11.3:186-199` (§1.6) | delete | — | Duplication: `coupling-structure` channel table; registry row 19 `hall-mobility-from-σ`, row 70 `self-heating-T_op`, row 71 `coupled-em-thermal-pde-residual`. |
| C23 | Transport numerics (`BoltzTraP`/`BoltzTraP2`, `EPW`, `AMSET`, NEMD, Green–Kubo on MD) + **sparsity: collision matrix local in `r`, dense in `k`; sparsity hints from concentration near the Fermi surface** | `11.3:201-208` (§1.7) | mine | `oracle/compilation/representation-substrate#sparsity` | The Fermi-surface concentration hint is a real compile-time optimization fact, not tooling. Tool names same class as A21. |
| C24 | `## 2. Thermodynamic regime` container | `11.3:212-220` | delete | — | Section scaffolding. |
| C25 | Canonical partition function `Z = Tr e^{−βĤ}`; `F = −k_BT ln Z`; `dF = −SdT − PdV + μdN` ⇒ `S = −(∂F/∂T)`, `P = −(∂F/∂V)`, `μ = (∂F/∂N)`; Gibbs and grand potential as Legendre transforms | `11.3:221-243` (§2.1) | delete | — | Duplication: `born-oppenheimer-levels` L3 (`2.5:48-51`) — "partition functions, free energies, quasi-harmonic approximation, convex hull"; A61 covers the group-A instance. Registry rows 65–69 implement the derivatives. |
| C26 | Harmonic vibrational free energy `F_vib(T)` and the decomposition `F(T,V) ≈ E₀(V) + F_vib(T,V) + F_el(T,V)` | `11.3:245-253` (§2.1) | delete | — | **Intra-scope duplication** of A41 (`11.1:406-413`), which states the same `F_vib` inside the fuller QHA expression. Canon has it too: `typed-compositions:155-158` `helmholtz-free-energy-decomposition` with `F_vib` and `F_el` as spectral aggregates. Mine once via A41. |
| C27 | Convex hull: `g*(x,T,P)` as the lower convex envelope over phase mixtures; `φ` stable iff on the hull; `E_hull = g_φ − g*(x_φ) ≥ 0`; **common-tangent construction** for two-phase coexistence; "this is CALPHAD's mathematical core" | `11.3:255-267` (§2.1) | mine | `oracle/registry/typed-compositions#thermodynamic` | Registry rows 67 `phase-diagram-convex-hull`, 68 `phase-boundary-slope`, 124 `tp-aware-hull` and `residual-definitions` cat 17 all consume `E_hull`; `typed-compositions:154` gives `ConvexOptimization(points, objective = lower-convex-envelope)`. The **common-tangent** construction (which is what row 68 computes) is defined only here. |
| C28 | Thermodynamic state is **aggregate / zero-dimensional**: intensive scalars `T, P (or V), {μ_i}`; composition `{x_i}` on the simplex; phase amounts `{c_φ}`, `Σc_φ = 1`, `c_φ ≥ 0`; spatially-extended (phase-field) generalization `c_φ(r,t)` / `η_φ(r,t)`; **potentials are `model`, not state** | `11.3:269-282` (§2.2) | mine | `oracle/state/multiscale-state#slow-tier` | `multiscale-state`'s slow tier is `{[D]^q}` (defect populations); composition vectors are named at `unified-state:57-58` as first-class slow-tier state and **never given a schema**. This is the schema. The "potentials are model, not state" line parallels A28. |
| C29 | **Cahn–Hilliard** `∂_t c = ∇·(M(c)∇δℱ/δc)` with `ℱ = ∫[f(c) + (κ/2)|∇c|²]`; **Allen–Cahn** `∂_tη = −Lδℱ/δη`; both **gradient flows** with `dℱ/dt ≤ 0` — `ℱ` is a Lyapunov functional | `11.3:284-303` (§2.3) | mine | `oracle/state/multiscale-state#slow-tier` | Registry row 107 `platelet-nucleation-allen-cahn` implements one; `computational-methods` and `computational-overview` both mention Cahn. The **gradient-flow/Lyapunov structure** — the reason these are the admissible slow-tier dynamics — is stated only here. Zero hits for "Lyapunov" in `journal/pages/` outside ch. 11. |
| C30 | **Onsager / Mori–Zwanzig** near-equilibrium form `∂_t x_i = −Σ L_ij δℱ/δx_j + ξ_i(t)` with `L` symmetric PSD and **FDT** `⟨ξ_i(t)ξ_j(t')⟩ = 2k_BT L_ij δ(t−t')` | `11.3:305-312` (§2.3) | mine | `oracle/laws/generic-dynamics#dissipative-block` | This is the `M δS/δx` branch written with its fluctuation term. `generic-dynamics:96-102` gives `M` as "diagonal kernels + cross-kernels" and **never states the fluctuation-dissipation constraint that ties `M` to `T`**. That constraint is what makes `M ⪰ 0` (cat 10) physically necessary rather than stipulated. |
| C31 | Thermodynamic constraints: convexity of `F`; **Maxwell relations** from `d²F = 0`; **Gibbs–Duhem** `Σ N_i dμ_i = −SdT + VdP`; **third law** `S → 0` as `T → 0`; second law / free-energy Lyapunov; stoichiometric balances; composition simplex `x_i ∈ [0,1]`, `Σx_i = 1`; `c_φ ≥ 0` | `11.3:314-328` (§2.4) | mine | `oracle/laws/residual-definitions#symmetries` | Partially covered — cat 14 names "Maxwell relations" and registry row 69 is `thermodynamic-cross-derivative-residual`; `multiscale-state:172-174` has three slow-tier thermodynamic-consistency identities. **Not covered: Gibbs–Duhem, the third law, convexity of `F` as a checkable constraint, and the composition simplex.** The simplex is the domain C28's state lives on. |
| C32 | Thermodynamic observables — `U` from DFT; `ΔH_f = E₀^cmpd − Σn_iE₀^elem`; `F = −k_BT ln Z`; `S = −∂F/∂T`; `P = −∂F/∂V`; `μ_i = ∂F/∂N_i`; `C_V = −T∂²F/∂T²`; `E_hull` as distance above hull; phase boundaries by common tangent | `11.3:330-342` (§2.5) | mine | `oracle/registry/typed-compositions#thermodynamic` | Operations behind registry rows 30, 65, 66, 67, 68, 124 and `typed-compositions:150-158`. `C_V = −T∂²F/∂T²` in particular has no registry row despite `C_v` being a ledger observable — see OQ-A3. |
| C33 | Thermodynamic couplings (`E₀` + Hessian from structure; `F_vib` dominant; `F_el` from FD filling, `F_mag` from magnetic order; surface `γ` as the 2-D restriction; **Onsager `L_ij` computed from equilibrium fluctuations — Green–Kubo/FDT**) | `11.3:344-354` (§2.6) | delete | — | Duplication: C30 states the FDT link; `coupling-structure` owns the channel inventory; `typed-compositions:155-158` owns the free-energy decomposition. |
| C34 | Thermodynamic numerics (CALPHAD; finite-`T` DFT; QHA; **SCP**; thermodynamic integration; Wang–Landau; lattice MC / **cluster expansion for configurational entropy**; free-energy perturbation / umbrella sampling; Quickhull) | `11.3:356-362` (§2.7) | mine | `oracle/registry/computational-methods#reference-implementations` | Registry rows 13 `SCPH-self-consistent-phonons` and 84 `cluster-expansion-energy` exist; `born-oppenheimer-levels:110-113` defers full SCPH to Layer 1.75. Configurational entropy is `generic-dynamics:82`'s `S_config` — **which is otherwise undefined**; this names its computation. |
| C35 | `## 3. Chemical / surface regime` container | `11.3:366-374` | delete | — | Section scaffolding. |
| C36 | PES `V({R_I})` with minima connected by a **MEP** `γ(s)` along which `∇V ∥` tangent except at the saddle | `11.3:375-381` (§3.1) | mine | `oracle/registry/typed-compositions#chemical` | `typed-compositions:140-141`/`166-167` use `PathStationaryOf(F = E_BO, …)`; the MEP condition that defines "path stationary" is stated only here. |
| C37 | **NEB** `F_i^NEB = −∇V(R_i)|_⊥ + F_i^spring|_∥`; **climbing image** `F_{i*}^CI = −∇V + 2(∇V·τ̂)τ̂`, converging to the exact saddle (one negative Hessian eigenvalue along `τ̂`) | `11.3:382-396` (§3.1) | mine | `oracle/registry/typed-compositions#chemical` | `typed-compositions:141` pins `method = climbing-image-NEB, n_images = 9, tol = 1e-3` — canon fixes the *parameters* of a method it never defines. The one-negative-eigenvalue saddle test is a checkable postcondition with no residual. |
| C38 | **TST** `k = (k_BT/h)(Q^‡/Q^R)e^{−E_a/k_BT}`; **harmonic TST / Vineyard** `k = (Π ν^min / Π ν^‡) e^{−E_a/k_BT}` | `11.3:398-408` (§3.1) | mine | `oracle/registry/typed-compositions#chemical` | **Zero hits for "Vineyard" or "hTST" in `journal/pages/` outside ch. 11.** `typed-compositions:130-139` builds `IonicDiffusion` from `harmonic-transition-rate-normalization` over a **product of modes** — that product is the Vineyard prefactor, named nowhere. Registry rows 105, 109, 110 are Arrhenius/Eyring rates resting on it. |
| C39 | **Marcus theory** `k_ET = A exp[−(ΔG° + λ)²/(4λk_BT)]`, with reorganization energy `λ` and the normal/inverted regions | `11.3:410-417` (§3.1) | mine | `oracle/registry/typed-compositions#chemical` | Named in `born-oppenheimer-levels:54-55` (L4 math list) and in ledger; **no registry row implements it** and the equation is only here. Same shape as B29 (BSE): scheduled, undefined. |
| C40 | **BEP** `E_a = αΔE_rxn + β`, `0 < α < 1`; **Sabatier's principle** ⇒ volcano-plot activity vs adsorption-energy descriptor | `11.3:419-428` (§3.1) | mine | `oracle/registry/typed-compositions#chemical` | **Zero hits for "Sabatier" or "volcano" in `journal/pages/` outside ch. 11.** `residual-definitions` §1 cat 16/17 does not list BEP, but `11.3:486-488` proposes it as a *soft* screening constraint — see C42. |
| C41 | **Microkinetic rate equations** `dθ_i/dt = Σ_r ν_ir(k_r^+ Πθ^{ν⁻} − k_r^- Πθ^{ν⁺})` with `k_r^+/k_r^- = e^{−ΔG_r/k_BT}`; **lattice-gas master equation** `dP(C,t)/dt = Σ[W(C'→C)P(C') − W(C→C')P(C)]` with detailed balance | `11.3:429-444` (§3.1) | mine | `oracle/state/multiscale-state#slow-tier` | `generic-dynamics:119` says the chemical extraction is "Master equation on configurations (`M` = rate matrix)" — asserted, never written. `multiscale-state:413` gives the slow tier a "master-eq generator" with no equation. Registry row 42 `mass-action-defect-complex` and row 125 (Wegscheider) both consume this structure. |
| C42 | Chemical state: mean-field coverages `θ(t)` with **site balance `Σθ_i + θ_free = 1`** (+ optional reaction extents `ξ_r`); lattice-gas/kMC occupations `σ_a ∈ {0..S}`; defect concentrations `{c_d(r,t)}` | `11.3:446-461` (§3.2) | mine | `oracle/state/multiscale-state#slow-tier` | `multiscale-state`'s slow tier is `{[D]^q}` indexed `(species, site)` — the **coverage** representation and its simplex constraint are a different schema for the same tier and are not there. Cat 10 lists `f ∈ [0,1]` but not `θ ∈ Δ^S`. |
| C43 | Chemical EOM: rate equations in the mean-field limit; master equation in the discrete limit; kMC samples master-equation trajectories; **Langevin along the reaction coordinate** `m_ξ ξ̈ = −∂G/∂ξ − γ_ξ ξ̇ + ζ(t)` with FDT `⟨ζζ⟩ = 2γ_ξk_BTδ` | `11.3:463-473` (§3.3) | mine | `oracle/state/multiscale-state#slow-tier` | Second FDT statement (cf. C30) — the two together are the only statements of fluctuation–dissipation in the corpus. |
| C44 | Chemical constraints: **element balance via the left null space of the stoichiometry matrix `ν`**; site balance (hard simplex); detailed balance; **Wegscheider cycle conditions**; non-negativity; BEP linear-scaling as a **soft** screening constraint | `11.3:475-488` (§3.4) | mine | `oracle/laws/residual-definitions#balance-laws` | Partly covered: registry row 125 `detailed-balance-cycle-residual` (a.k.a. Wegscheider) and cat 13 name it. **Not covered**: the left-null-space characterization of conserved moieties — which is *how* you compute element balance, and is exactly the "element balance from stoichiometry" that `11.3:672-673` (§4.5) and `residual-definitions` assume. The hard-vs-soft distinction (BEP is soft) has no representation in the residual taxonomy. |
| C45 | Chemical observables — `E_ads = E_surf+ads − E_surf − E_ads,gas`; `γ = (E_slab − nE_bulk)/(2A)`; `E_a = V^‡ − V^min`; `k(T)` by hTST; reaction profile `V(ξ)`; TOF from steady state; volcano descriptor; **degree of rate control `X_RC,i = (k_i/r)(∂r/∂k_i)`**; selectivity `r_A/(r_A+r_B)` | `11.3:490-502` (§3.5) | mine | `oracle/registry/typed-compositions#chemical` | Operations behind `typed-compositions:164-167` and registry row 44. `γ_s` duplicates A18 — mine once from A18 (group A states it first). **Degree of rate control** has no registry row and no canon mention; it is the sensitivity measure that tells you which barrier matters, i.e. where to spend oracle accuracy. See OQ-C1. |
| C46 | Chemical couplings (PES/NEB/terminations/defect geometries are structural; adsorption energies, surface dipoles, work function, charge transfer, Marcus from electronic; `T` in Arrhenius/hTST + adsorbate vibrational entropy; surface diffusion feeds `D_i`; `γ` as the surface restriction of the thermodynamic potential; Langmuir from grand-canonical) | `11.3:504-515` (§3.6) | delete | — | Duplication: `coupling-structure` channel table; registry rows 44 `surface-grand-potential-γ`, 47 `barrier-from-workfunction-affinity`. |
| C47 | Chemical numerics (NEB/CI-NEB/dimer/string; hTST/variational TST/**ring-polymer instanton**; kMC; mean-field ODE integrators; Campbell degree-of-rate-control; grand-canonical MC; Wang–Landau; scaling-relation surrogates) | `11.3:517-524` (§3.7) | mine | `oracle/registry/computational-methods#reference-implementations` | Ring-polymer instanton is the quantum-tunnelling correction to hTST — relevant to light-atom (H) barriers, and registry row 106 is `hydrogen-redistribution-drift-diffusion`. Flagged in Notes with the rest of the tooling class. |
| C48 | `## 4. Cross-regime synthesis` container | `11.3:528` | delete | — | Section scaffolding. |
| C49 | All three group-C regimes are statistical: each carries a distribution (`f_n` on phase space · Boltzmann measure `e^{−βE_i}/Z` · master-equation measure `P(C,t)` / coverage `θ`), and each has a **scalar Lyapunov functional** — table: Transport `−S[f]` non-increasing (H-theorem `dS/dt ≥ 0`) · Thermodynamic `ℱ[c]` non-increasing · Chemical `ℱ_micro[P] = Σ P ln(P/P_eq)` non-increasing. Equilibrium = unique minimizer under the conserved quantities | `11.3:530-551` (§4.1) | mine | `oracle/laws/generic-dynamics#entropy-functional` | **Zero hits for "Lyapunov" in `journal/pages/` outside ch. 11.** This is the monotonicity structure that makes `M ⪰ 0` (cat 10) and the second-law direction checkable at all. The relative-entropy form `Σ P ln(P/P_eq)` for the chemical tier is `multiscale-state`'s slow-tier decay law, unstated there. Direction was corrected 2026-07-07 (Log-worthy **L7**). |
| C50 | **GENERIC** `dx/dt = L δE/δx + M δS/δx` (Grmela–Öttinger) with degeneracy `L δS/δx = 0`, `M δE/δx = 0`; `L` antisymmetric (Poisson, Jacobi), `M` symmetric PSD; observable form `dA/dt = {A,E} + [A,S]` | `11.3:553-574` (§4.2) | delete | — | Duplication: `generic-dynamics:38-51` states the equation, both degeneracy conditions, and the `L`/`M` properties — and adds the per-tier normalization, the Lie–Poisson `γ̂` block, and the Jacobi status table that this section lacks. Canon's is strictly better. |
| C51 | **How each regime sits in GENERIC**: Transport `x = f`, `E = ∫εf`, `S = −k_B∫[f ln f + (1−f)ln(1−f)]`, `L` = streaming+force, `M` = collisions, **Onsager symmetry is a direct consequence of `M = Mᵀ`**; Thermodynamic/phase-field `x = c(r)`, `E` constant, pure dissipative `∂_t c = ∇·M∇(δℱ/δc)` = `M δS/δx` with `S = −ℱ/T`; Chemical `x = P(C)` or `θ`, master equation with detailed balance = **gradient flow of relative entropy `D(P‖P_eq)`** | `11.3:575-588` (§4.2) | mine | `oracle/state/multiscale-state#tier-brackets` | Cited from canon twice — `multiscale-state:417` and `:419` ("exactly [deriv-transport] §4.2's 'pure dissipative `M δS/δx`' fluid limit"). The target restates the *conclusion* for the macro tier; the **per-regime `(x, E, S, L, M)` assignment table** is the content and is not there. "Onsager follows from `M = Mᵀ`" is the warrant for cat 14, absent from `residual-definitions`. |
| C52 | Degeneracy `M δE/δx = 0` **ensures energy conservation by dissipation** — heat/mass/charge moves, total energy constant; the same law enforced by BTE collisions, common-tangent, and rate-equation stoichiometry | `11.3:590-593` (§4.2) | delete | — | Duplication: `generic-dynamics:50-51` and `:125-159` own the degeneracy conditions and their per-tier status, including `Degeneracy` being cert-only. |
| C53 | **Positivity of `L`/`M` is the single most important shared constraint**: `σ`, `κ`, `D`, `W^sym`, Onsager `L_ij`, and `M` are all PSD as a direct consequence of the second law; **one PINO loss term enforcing `L ⪰ 0` enforces all three regimes' positivity at once** | `11.3:595-599` (§4.2) | mine | `oracle/laws/residual-definitions#positivity` | cat 10 lists the PSD requirements as an **unmotivated enumeration** (`M ⪰ 0`, `σ ⪰ 0`, …). That they are one consequence of one law — and therefore one residual, not six — is a loss-design fact stated only here. Compare A60, the same shape for group A. |
| C54 | **Irreducibly different — kinematic geometry**: Transport = 6-D phase space, *vector flow* · Thermodynamic = scalar aggregates/fields, *gradient flow* · Chemical = discrete simplex/lattice configurations, *combinatorial*, detailed balance is graph-theoretic (Wegscheider cycles). Summary table (state type / index / dynamics: hyperbolic+dissipative PDE / parabolic gradient-flow PDE / ODE–Markov–SDE) | `11.3:601-624` (§4.3) | delete | — | **Already resolved into canon**: `multiscale-state:406-414` cites this section by name ("Resolving [deriv-transport] §4.3 …") and lands it as the three-tier table with `x`, index, EOM category, and the `L`/`M` branches per tier. Canon's version is more specific. Delete the prose; **the citation at `multiscale-state:406` must be rewritten** — see Notes. |
| C55 | "These are *not* unifiable into a single tensor representation without loss. A library should expect three state schemas, three discretizations, and a **common residual contract**: (i) state-vector portion, (ii) dynamics residuals, (iii) sign-constrained conservation/positivity residuals, (iv) algebraic identities" | `11.3:624-630` (§4.3) | delete | — | Duplication: `multiscale-state:404-425` §11 "The unified three-tier residual contract" is the executed form of this recommendation, including "**not unifiable into one tensor** (three distinct discretizations) — hence *stratified*, not flattened". |
| C56 | Cross-couplings within group C (Green–Kubo from equilibrium fluctuations; local FD as BTE's local-equilibrium target; `k_r^±` constrained by `ΔG_r`; adsorption isotherms from grand-canonical; phase stability constrains terminations; `D_i` from hTST barrier crossings) | `11.3:632-643` (§4.4) | delete | — | Duplication: C30/C33 (FDT), C41/C44 (detailed balance), `typed-compositions:130-139` (`IonicDiffusion` via hTST), `coupling-structure`. |
| C57 | Out-of-group couplings (to electronic, structural, thermal, mechanical, magnetic, optical), including **`σ_αβ(ω)` is the same Kubo–Greenwood object — "transport IS the optical regime in dynamical-response form"** | `11.3:645-663` (§4.4) | mine | `oracle/laws/residual-definitions#method-equivalence` | The optical↔transport identity (restated at `11.2:394-396`) is the **third leg** of the BTE-σ ≡ Kubo-σ ≡ `lim_{ω→0} Re σ(ω)` equivalence. `typed-compositions:123-128` declares the BTE/Kubo pair; the `ω→0` optical limit as the *same* object is stated only in ch. 11. Mine once, from here. The rest of the section duplicates `coupling-structure`. |
| C58 | Residual contract shape — four clusters: (1) EOM residuals (discretized BTE, gradient flow, rate/master equation); (2) conservation (`∂_tn + ∇·j = 0`, `Σc_φ = 1`, `Σθ_i = 1`, element balance, Maxwell closure); (3) positivity/inequality (`f ∈ [0,1]`, `θ ∈ [0,1]`, `L ⪰ 0`, `σ ⪰ 0`, `κ ⪰ 0`, `dS/dt ≥ 0`); (4) algebraic identities (Einstein, detailed balance, Onsager, common tangent, BEP) | `11.3:665-679` (§4.5) | delete | — | **Superseded ancestor.** `residual-definitions` §1 is the executed taxonomy: 9 EOM + 3 GENERIC-structural + 5 algebraic + 2 constraint = 19 categories, with symbolic tags, applicability gates, and sub-kinds. Cited from canon at `multiscale-state:417`. Delete the prose; the *fact that this is where the taxonomy came from* → Log-worthy **L8**. |
| C59 | "The GENERIC two-bracket form gives all four cleanly: EOM is `ẋ = LδE/δx + MδS/δx`; conservation is antisymmetry of `L` plus `MδE/δx = 0`; positivity is `M ⪰ 0` plus the Jacobi identity for `L`; identities follow from explicit forms of `E,S,L,M`" | `11.3:680-684` (§4.5) | delete | — | Duplication: `generic-dynamics:53-61` states exactly this ("Every other residual category in `residual-definitions` is either a refinement of it … or an algebraic identity the dynamics must satisfy") plus the Jacobi-status table at `:148-154`. |
| C60 | Sources verified during research | `11.3:688-704` | delete | — | Same reason as A69. |
| C61 | `## Changelog` (2026-07-16 banner conversion + regime-number replacement; 2026-07-07 gap-audit B7 Lyapunov direction fix) | `11.3:706-714` | delete | — | Scaffolding (D1/D2). Content preserved: Log-worthy **L7**; the corrected statement → C49. |

---

---

### from `appendix-b`

### `11.4-deriv-defects` — frontmatter and header

| # | Fact or block | Current location | Disposition | Target | Evidence |
|---|---|---|---|---|---|
| 1 | `authority: supporting`, `content-hash: beb9b42f86fb` | `11.4-deriv-defects.md:4-5` | delete | — | D1/§4: `authority` tier and `content-hash` are both abolished. The two-tier system dies with ch. 11 by D3. |
| 2 | `canonical-for: [deriv-defects]` | `11.4-deriv-defects.md:6-7` | delete | — | Vacuous ownership (plan §2 defect 4): the sole owned topic is the page's own id, so the duplicate-topic invariant cannot fire. Page is deleted regardless. |
| 3 | `depends-on: [timeline]` | `11.4-deriv-defects.md:8-9` | delete | — | Sole edge is to `10.5-timeline`, itself dissolving into `log/timeline.md` (D2). Nothing to rewire outbound. |
| 4 | `referenced-by:` 7 entries | `11.4-deriv-defects.md:10-17` | delete | — | §4: `referenced-by` is generated, never hand-written. The *inbound* edges it mirrors are enumerated in Notes for Phase 2 §A and must be repointed. |
| 5 | Title + scope sentence ("Survey of defect, doping, surface, and interface physics… two-tier (cheap/faithful) parameterizations") | `11.4-deriv-defects.md:19-21` | delete | — | Container framing for a container being deleted. The two-tier cheap/faithful discipline it announces is canon already at `oracle/accuracy/accuracy-ledger` and `oracle/laws/residual-definitions#method-equivalence` (consistency pairs, `3.2-residual-definitions.md:155-164`). |
| 6 | **Conventions header — "χ is always termination-tagged"** | `11.4-deriv-defects.md:23-26` | delete | — | Already canon as a live hazard: `10.4-traps.md:156` trap 18, *"Diamond electron affinity is termination-dependent — every use must be termination-tagged."* Traps register survives. Naming the *page* — "Part E is the canonical per-termination table" — is what must be repointed (row 26). |
| 7 | **Conventions header — "F.4 barrier heights and carbide onsets are literature-survey grade; pin provenance before any value seeds a `ProvenanceLedger`"** | `11.4-deriv-defects.md:24-26` | delete | — | Already canon: `10.4-traps.md:349` trap 51, *"Pin provenance before any ledger entry… Open pins include contested Schottky barriers, carbide onsets."* Word-for-word the same guard, in the register that survives. |
| 8 | Conventions header — "History: the `## Changelog` at the end of this file" | `11.4-deriv-defects.md:25-26` | delete | — | Scaffolding by D1; the changelog it points at is deleted (row 59). |

### `11.4` Part A — native defects

| # | Fact or block | Current location | Disposition | Target | Evidence |
|---|---|---|---|---|---|
| 9 | A.1 per-host native-defect inventory (6 hosts × dominant defects, complexes, notes) | `11.4-deriv-defects.md#a1-inventory-by-host` | delete | — | **Already stated in canon, present-tense, as a closed vocabulary**: `2.4-multiscale-state.md:113-124` §3.1 reproduces all six hosts with charge states as the `DefectSpecies` `Universe[T]` enumerator, and cites this table as its source. The canonical copy is the multiscale one. |
| 10 | A.1 note: "β-Ga₂O₃ V_O deep, NOT the shallow donor (was historically misattributed)" | `11.4-deriv-defects.md:40` | delete | — | The parenthetical is history scaffolding (D1). The live half — `V_O` is deep — survives at `2.4-multiscale-state.md:118` (` `V_O` deep `). |
| 11 | A.2 typed signatures `defect_inventory`, `E_form`, `ε(q/q')`, `E_migr`, `S_form`, `[D]_eq` | `11.4-deriv-defects.md#a2-typed-signatures` | delete | — | Landed: registry rows 30 (`defect-formation-energy`), 34 (`charge-transition-level`), 35 (`defect-boltzmann-population`), and the `DefectSpecies` record type at `2.4-multiscale-state.md:126-128` which cites A.2 explicitly. Every signature has a surviving home. |
| 12 | A.3 cheap dilute-Boltzmann `[D]^q = N_site·g_q·exp(−E_form^q/kT)` | `11.4-deriv-defects.md#a3-equilibrium-concentration` | delete | — | Registry row 35 `defect-boltzmann-population` `(E_form, T, μ_i) → c_def`, source cell `S2+S3`. Consumed at `2.4-multiscale-state.md:139-140`. |
| 13 | A.3 **faithful path: self-consistent `E_F(T)` from the charge-balance closure** `∑_D∑_q q[D]^q + p − n + ∑_d q_d[Dopant] = 0` | `11.4-deriv-defects.md:66-71` | mine | `oracle/state/multiscale-state#charge-balance-closure` | The *closure* is load-bearing and has **no registry row of its own** — row 30 takes `E_F` as an input, and nothing states where `E_F` comes from. `2.4-multiscale-state.md:450-452` names `E_F(T)` (R-T2) as a thing that exists without defining its solve. Homeless-fact candidate; see Open questions `fermi-level-charge-balance-closure`. |
| 14 | A.4 worked example: `E_form(V_C⁰) ≈ 7.2 eV (HSE06)`, `[V_C]` vanishing at 773 K | `11.4-deriv-defects.md#a4-worked-example-v_c-in-diamond-at-500c` | delete | — | Restated verbatim in canon: `2.4-multiscale-state.md:172-174` — *"Diamond `V_C`: `E_form≈7.2 eV` (HSE06), so `G_thermal(773 K)` is negligible."* |
| 15 | A.4 conclusion: **"treat [V_C] as a state variable Z_I evolving by generation–annihilation kinetics, not an equilibrium quantity"** | `11.4-deriv-defects.md:77` | delete | — | Landed, and **corrected in the landing**: `2.4-multiscale-state.md:143-152` rules the slow fiber is a **new C3 fiber promoting `SiteDecoration.occupancy`, explicitly *not* a mutation of `Z_I`** — "`Z_I` stays immutable." The appendix's `Z_I` reading is superseded. Log as Contradiction C1; the surviving statement is the multiscale one. |
| 16 | A.5 migration-barrier values (7 rows: `V_C⁰` 2.3, `V_C⁻` 2.8, `C_i` 1.6–1.7, `V_N(GaN)` 2.6, `V_Ga(GaN)` 1.9, `V_O(Ga₂O₃)` 1.9–2.4, `V_Al(AlN)` 3.4 eV) | `11.4-deriv-defects.md#a5-migration-barriers-neb-class-faithful` | delete | — | All seven restated in canon at `2.4-multiscale-state.md:174-176` as F-G1's annihilation barriers `k_ann = ν₀exp(−E_migr/kT)`. |
| 17 | A.5 **onset-temperature column and its `ln(ν₀τ) ≈ 30.6` criterion** (`ν₀=1e13 s⁻¹`, `τ_hop ≈ 2 s`) | `11.4-deriv-defects.md:81-89` | mine | `oracle/state/multiscale-state#slow-tier-onsets` | Not carried by the multiscale landing, which took only the barriers. The onset temperatures are what make the barriers *actionable* ("`V_C` is on the verge at 500 °C"). Losing the criterion loses the reading. |
| 18 | A.5 correction parenthetical (τ=1 yr header reproduced none of its own values; header was wrong, table right) | `11.4-deriv-defects.md:91-98` | delete | — | History of a fix (D1). **Log-worthy L1** — the finding itself is a genuine advancement and is recorded nowhere else. |
| 19 | A.5 conclusion: "at 500 °C `V_C` in diamond is on the verge of mobility — the regime where defects cluster into platelets" | `11.4-deriv-defects.md:100` | mine | `oracle/state/multiscale-state#slow-tier-onsets` | Present-tense physical reading tying A.5 to G.3; canon carries G.3's kinetics (`2.4-multiscale-state.md:181-183`) but not the *reason* platelets are the 500 °C concern. |

### `11.4` Part B — dopant chemistry

| # | Fact or block | Current location | Disposition | Target | Evidence |
|---|---|---|---|---|---|
| 20 | B.1 per-host dopant table (6 hosts: donors/acceptors/compensators with levels) | `11.4-deriv-defects.md#b1-per-host-dopant-table` | mine | `oracle/accuracy/accuracy-ledger#dopant-levels` | **No surviving page carries any of it.** `2.4-multiscale-state.md` §3.1 took the *native-defect* inventory and left dopants behind. Diamond B 0.37 eV and P 0.57 eV are the MVP-relevant numbers and appear in no reference-data CSV (`grep -i dopant\|boron physics/library/cert/reference-data/*.csv` → nothing). Ledger is the value home. |
| 21 | B.1 "**no p-type Ga₂O₃** — fundamental limit"; "no shallow acceptor known"; self-trapped holes | `11.4-deriv-defects.md:114` | mine | `oracle/certification/out-of-scope#material-limits` | A *refusal boundary*, not a datum: it says a whole channel cannot be claimed for a material. `5.3-out-of-scope.md` is the surviving page for "we do not do this." The ledger already cert-refuses β-Ga₂O₃ holes on a different observable (`9.1-accuracy-ledger.md:191`, "holes never measured (STH — cert-refused)"), so the pattern exists. |
| 22 | B.1 "AlGaN doping bottleneck": Si DX onset at `x_Al ≳ 0.8`; Mg `E_a` rises linearly with `x_Al` | `11.4-deriv-defects.md:112,115` | mine | `oracle/accuracy/accuracy-ledger#dopant-levels` | Composition-gated behaviour with no surviving home; the ledger already carries `x`-dependent AlGaN entries (κ dip, alloy ΔU, `9.1-accuracy-ledger.md:266-267`) so the shape fits. |
| 23 | B.2 five compounding obstacles to diamond doping (small lattice → interstitial preference; high cohesive energy; no native shallow donor; self-compensation B–V / N–V; surface depletion) | `11.4-deriv-defects.md#b2-why-diamond-doping-is-hard` | mine | `program/purpose/rationale#why-diamond-is-hard` | Argumentative content — *why the MVP material is the hard case*. Nothing in canon states it. Obstacle 3's corollary ("usable only at high T — favorable for harsh-env") is the project's own thesis and appears nowhere else. |
| 24 | B.2 obstacle 5 "surface depletion: H-termination's NEA strips electrons; O-termination depletes holes; bulk doping efficacy depends on the surface boundary condition" | `11.4-deriv-defects.md:125` | mine | `oracle/state/multiscale-state#surface-coupling` | A bulk↔surface coupling statement. Canon carries the χ shift as a *degradation* path (F-47, `2.4-multiscale-state.md:195-197`) but not the doping consequence. |
| 25 | B.3 typed signatures + cheap single-donor `n(T)` closed form + faithful multi-level balance | `11.4-deriv-defects.md#b3-typed-signatures` | mine | `data/registry-manifest.csv` (Source cell, a new dopant-ionization row) | Better as registry provenance than prose, per the brief's fourth option: `dopant_activation`, `solubility_limit`, `self_compensation`, `DX_transition` are four unregistered signatures. `R_SolubilityLimit`/`R_ActivationEnergy` in 11.6 D.1 are the residual faces of the same four. See Open questions `dopant-ionization-registry-rows`. |
| 26 | B.4 worked example: P-doped diamond at 773 K, ~2% activation, `n ≈ 2e17 cm⁻³`; **"harsh-environment T turns marginal dopants into viable ones"** | `11.4-deriv-defects.md#b4-worked-example-p-doped-diamond-at-500c` | mine | `program/purpose/rationale#why-diamond-is-hard` | The inversion is a project-level claim (a marginal dopant becomes usable *because* the environment is hostile) stated nowhere else. Keep the conclusion; the arithmetic is illustrative. Note this example is the one trap 44 (`10.4-traps.md:303-307`) records as having been ~3× wrong. |

### `11.4` Part C — charged-defect machinery

| # | Fact or block | Current location | Disposition | Target | Evidence |
|---|---|---|---|---|---|
| 27 | C.1 Zhang–Northrup `E_form[D^q]` + typed signature + μ-triangle bound + `E_F` sweep | `11.4-deriv-defects.md#c1-zhangnorthrup-formation-energy` | delete | — | Registry row 30 `defect-formation-energy` `(E_def, E_host, μ_i, q, E_F) → E_form`, `S1+S2+S3 (a.k.a. Zhang–Northrup)`. Consumed by `2.4-multiscale-state.md:450` (R-T2). |
| 28 | C.2 Makov–Payne + **sign convention "ADDED to `E_tot[D^q]`"** + `α_M ≈ 1.418` for diamond fcc | `11.4-deriv-defects.md#c2-makovpayne-cheap-path` | mine | `data/registry-manifest.csv` row 31 (Source cell) | Row 31 `charged-supercell-extrapolation-isotropic-cubic` exists but its Source is a bare `S3 (a.k.a. Makov-Payne)`. **The sign convention and the per-Bravais `α_M` are not stated anywhere else** — a sign error here flips the correction. Precisely the "better as registry provenance than prose" case. |
| 29 | C.3 Freysoldt/FNV split + **the `E_corr^FNV` symbol-collision guard** ("symbol `E_corr`, not `E_F`: `E_F` is the Fermi level everywhere else") | `11.4-deriv-defects.md#c3-freysoldt-faithful-path` | mine | `practice/traps` (new trap) + `data/registry-manifest.csv` row 32 | Row 32 exists (`…-planar-aligned`, `a.k.a. Freysoldt / FNV`). The *notation collision guard* is a live hazard of exactly the traps-register kind and is stated only here. The alignment scalar being **returned as an observable** is also unstated elsewhere. |
| 30 | C.3 `ΔV_alignment` returned as a PINO-observable alongside the energy | `11.4-deriv-defects.md:192` | mine | `oracle/registry/observable-bundles#b4-defect` | An observable claim, not a formula claim; B4 is the defect bundle (registry rows 30–35 all carry `B4`). |
| 31 | C.4 Lany–Zunger `E_LZ = (1−c_sh)(q²α_M)/(2εL)`, `c_sh ≈ 0.36` cubic | `11.4-deriv-defects.md#c4-lanyzunger` | mine | `data/registry-manifest.csv` row 33 (Source cell) | Row 33 `charged-supercell-extrapolation-image-charge` exists with bare `S3 (a.k.a. Lany-Zunger)`. **`c_sh ≈ 0.36` is the row's only numeric parameter and lives only here.** |
| 32 | C.5 defect–defect screened Coulomb + self-consistent Debye `λ_D`; validity floor `[D] ≳ 1e19 cm⁻³` | `11.4-deriv-defects.md#c5-defectdefect-interaction--configurational-entropy` | mine | `oracle/certification/applicability-classifiers#dilute-limit` | A **validity boundary on row 35** (dilute Boltzmann breaks above 1e19). No registry row; `5.2-applicability-classifiers.md` is the surviving page for "when does this formula stop applying." |
| 33 | C.5 configurational entropy `S_conf` + dilute limit | `11.4-deriv-defects.md:209-213` | mine | `data/registry-manifest.csv` (Source cell, row 35) | The `S_form` input to row 35's Boltzmann population; stated nowhere else. |
| 34 | C.6 two-tier ladder table (cheap GGA+MP 1× / mid HSE+LZ ~30× / faithful HSE+GW+FNV ~300×) and "PINO surrogate trained on cheap, residual-corrected against mid, anchor-validated against faithful" | `11.4-deriv-defects.md#c6-two-tier-ladder-for-charged-defects` | mine | `operator/training/curriculum#tier-ladder` | This is a **three-tier training protocol**, and it is the same shape as the training-stage ordering the plan flags as homeless (§2). It states which tier supplies labels, which supplies residual corrections, and which validates. Relative costs (1×/30×/300×) appear nowhere else. |

### `11.4` Part D — recombination and trapping

| # | Fact or block | Current location | Disposition | Target | Evidence |
|---|---|---|---|---|---|
| 35 | D.1 SRH rate + `n_1`,`p_1`,`τ_n`,`τ_p`,`v_th(T)` + `Trap` record type | `11.4-deriv-defects.md#d1-shockleyreadhall` | mine | `oracle/state/multiscale-state#recombination` | `2.4-multiscale-state.md:256` cites `[deriv-defects] Part D.1/D.4` for `τ_n = 1/(σ_n v_th N_T)` but **states only that one fragment** — the `n_1/p_1` detailed-balance factors and the `Trap` record are not carried. Dangling-promise class: the citation lands on the page, not the claim. |
| 36 | D.2 Auger `R_Aug = (C_n n + C_p p)(np − n_i²)`; `C ~ 1e−30 cm⁶/s` for wide-gap | `11.4-deriv-defects.md#d2-auger` | mine | `oracle/state/multiscale-state#recombination` | No registry row, no canon statement. Registry row 134 `R_rad` (radiative) exists per `9.1-accuracy-ledger.md:287`; the non-radiative siblings do not. |
| 37 | D.3 multiphonon emission / Huang–Rhys `σ_capture(T) = σ_0 exp(−E_b/kT)`, `E_b = (S − ΔE/ℏω)²ℏω/(4S)` | `11.4-deriv-defects.md#d3-multiphonon-emission-deep-level-non-radiative` | mine | `oracle/state/multiscale-state#recombination` | Unregistered, uncanonized. The Marcus-crossing barrier form is the only deep-trap capture model in the corpus. |
| 38 | D.4 three capture-cross-section regimes (attractive `σ~1e−14`, `∝T⁻²`; neutral `~1e−15`; repulsive `~1e−18`, activated) | `11.4-deriv-defects.md#d4-capture-cross-sections` | mine | `oracle/state/multiscale-state#recombination` | Cited by `2.4-multiscale-state.md:256` (`Part D.1/D.4`) but not restated — same dangling-promise as row 35. The regime split is what makes `σ_n` selectable rather than fitted. |
| 39 | D.5 NV as dominant SRH centre above 600 K; `[N]` in CVD diamond 1e17–1e19 cm⁻³ sets `τ_n`; `τ_n ≈ 10–100 ns` at 773 K | `11.4-deriv-defects.md#d5-diamond-specific-recombination-centers-at-500c` | mine | `oracle/accuracy/accuracy-ledger#diamond-carrier-lifetime` | The MVP material's carrier lifetime at the operating point. The page labels it "rough estimate" — carry the hedge with it. No CSV anchor exists. |

### `11.4` Part E — surfaces

| # | Fact or block | Current location | Disposition | Target | Evidence |
|---|---|---|---|---|---|
| 40 | **E.1 the per-termination χ / γ table** (10 rows: facet × termination × reconstruction × γ × χ) | `11.4-deriv-defects.md#e1-diamond-surface-reconstructions` | mine | `oracle/accuracy/accuracy-ledger#diamond-terminations` | **The single highest-value block in my scope.** Trap 18 (`10.4-traps.md:156`) *mandates* termination-tagging but does not supply the tags; `11.6-deriv-csp.md:141` and `:143` both name this table as canonical; `2.4-multiscale-state.md` relies on the NEA→PEA shift. It is the referent of a corpus-wide enforced rule and it exists in exactly one place, inside a chapter being deleted. Not in any reference-data CSV. |
| 41 | E.2 `γ(Termination, μ_H, μ_O, μ_C)` slab form + faithful `μ_H(T,p) = ½g_H2` | `11.4-deriv-defects.md#e2-surface-energy-as-function-of-termination-dependent` | delete | — | Registry row 44 `surface-grand-potential-γ` `(E_slab, μ_env, area) → γ(term,μ)`, `B5`, `S2+S3`. Consumed at `2.4-multiscale-state.md:448` (R-T1 Gibbs adsorption ties row 44 to slow `[H]`/`x_ox`). |
| 42 | E.3 dangling-bond in-gap states; reconstructions push toward band edges but leave mid-gap residue; **Fermi-level pinning sites for unpassivated contacts** | `11.4-deriv-defects.md#e3-dangling-bond-states` | mine | `oracle/state/multiscale-state#surface-coupling` | This is the physical mechanism behind `D_it` in F.3's slope parameter and behind registry row 116 `interface-trap-density`. Row 116 exists (`registry-manifest.csv:117`, "catalog #33 dangling-bond + strain"); the *mechanism* is only here. |
| 43 | E.4 NEA/PEA dipole mechanism `χ_surface = χ_bulk + ΔV_surf_dipole`; H reduces χ ~1.8 eV, O raises ~1.5 eV | `11.4-deriv-defects.md#e4-neapea-mechanism-for-diamond` | mine | `oracle/accuracy/accuracy-ledger#diamond-terminations` | The generative rule behind the E.1 table — it is what lets a new termination be estimated rather than looked up. Nowhere else. |
| 44 | E.4 **"H desorbs at ~700–900 °C → NEA contacts degrade at harsh-env T; O-termination is thermally more stable; χ(T,t) is a measurable PINO observable"** | `11.4-deriv-defects.md:330` | delete | — | Landed in canon: `2.4-multiscale-state.md:195-197` F-47 `hydrogen-desorption-rate-eyring`, "`E_des≈3.8 eV`… Drives the irreversible χ shift (NEA→PEA); desorbs 700–900 °C", registry row 110. |
| 45 | E.5 companion-UWBG terminations (c-BN cleavage; AlN/GaN polarity; β-Ga₂O₃ (-201)/(010)/(100); AlGaN segregation) | `11.4-deriv-defects.md#e5-companion-uwbg-surface-terminations-one-liners` | mine | `oracle/accuracy/accuracy-ledger#diamond-terminations` | Five one-liners, no other home. The β-Ga₂O₃ line interacts with the monoclinic frame guard (`9.1-accuracy-ledger.md:174-181`) — flag at landing. |

### `11.4` Part F — metal/semiconductor interfaces

| # | Fact or block | Current location | Disposition | Target | Evidence |
|---|---|---|---|---|---|
| 46 | F.1 Schottky–Mott `φ_B^n = φ_M − χ_S`, `φ_B^p = E_g + χ_S − φ_M`, **sum rule `φ_B^n + φ_B^p = E_g`** | `11.4-deriv-defects.md#f1-schottkymott-now-barrier-from-workfunction-affinity-cheap-path` | delete | — | Registry row 47 `barrier-from-workfunction-affinity`. The sum rule is restated at `11.7-deriv-observable-catalog.md:145` and in trap 44's text (`10.4-traps.md:310`, "holes see `φ_B^p = E_g − φ_B^n`"). |
| 47 | F.1 applicability caveat: "applies when no MIGS, no interface dipole, no Fermi-pinning — almost never true; zeroth-order only" | `11.4-deriv-defects.md:355` | mine | `oracle/certification/applicability-classifiers#schottky-mott` | Row 47 is `D1/cheap` with no stated validity domain. This is its domain, and it says the domain is nearly empty. |
| 48 | F.2 Bardeen limit `φ_B → E_g − φ_CNL`, independent of `φ_M`; holds for `D_it > 1e14 cm⁻²eV⁻¹` | `11.4-deriv-defects.md#f2-bardeen-limit` | mine | `oracle/certification/applicability-classifiers#schottky-mott` | The `S=0` end of row 48's interpolation, with the numeric `D_it` threshold that selects it. `11.7-deriv-observable-catalog.md:145` names "S=0 → Bardeen/fully pinned" but not the threshold. |
| 49 | **F.3 MIGS / Cowley–Sze `φ_B = S(φ_M−χ_S) + (1−S)(E_g−φ_CNL)`, `S = 1/(1 + e²D_it δ/(ε_i ε_0))`** | `11.4-deriv-defects.md#f3-migs--cowleysze-faithful-path` | delete | — | **This block is already load-bearing registry provenance**: `registry-manifest.csv:49` (row 48) says *"the Cowley-Sze form deriv-defects §F.3 states normatively"* and derives row 48's signature change from it. It survives *as* the registry row. The prose is redundant — but the citation string `deriv-defects §F.3` inside the CSV must be repointed (Notes §A). |
| 50 | F.3 `S_param` typed signature; `δ` interface-layer thickness, `ε_i` interface permittivity | `11.4-deriv-defects.md:374-378` | mine | `data/registry-manifest.csv` row 48 (Source cell) | Row 48's signature is `(W_m, χ_s, S, φ_CNL, E_g)` — `S` arrives as a given. **What `S` is computed from (`D_it`, `δ`, `ε_i`) exists only here.** |
| 51 | **F.4 diamond/metal contact table** (10 metals × `φ_M` × `φ_B^p` on H- and O-diamond × carbide-forming × `T_max`) | `11.4-deriv-defects.md#f4-diamondmetal-barrier-values-from-literature` | mine | `oracle/accuracy/accuracy-ledger#diamond-metal-contacts` | `11.6-deriv-csp.md:143-146` declares this table **the owner** of diamond/metal stability ceilings ("F.4 wins"). It is survey-grade and provenance-pending per trap 51. Must land tagged as unpinned, not as ledger fact. See Contradiction C3 — the deference was declared but not applied. |
| 52 | F.4 `S ≈ 0.7–0.8` for diamond ("relatively un-pinned vs GaAs/Si; MIGS correction real but small") | `11.4-deriv-defects.md:395` | mine | `data/registry-manifest.csv` row 48 (Source cell) | The only numeric `S` for the MVP material. Note `11.8-deriv-generator-catalog.md:493` uses `S≈0.85` and is flagged UNPINNED in `registry-manifest.csv:49` — see Contradiction C4. |
| 53 | F.5 carbide parabolic growth `x = √(2k_carb t)`, `k_carb = k_0 exp(−E_carb/kT)` + 4-metal kinetics table | `11.4-deriv-defects.md#f5-carbide-formation-kinetics` | delete | — | Registry row 81 `carbide-growth-parabolic`, and restated with all four metals in canon at `2.4-multiscale-state.md:188-190` (F-F5: "Ti 1.4 eV (~600 nm/1000 h@500 °C, severe), W 2.4 eV (~3 nm), Mo 2.1 eV (~15 nm), Pt none"). |
| 54 | F.5 `k_0` prefactors (Ti ~1e−5, W ~1e−4, Mo ~3e−5 cm²/s) | `11.4-deriv-defects.md:404-409` | mine | `oracle/accuracy/accuracy-ledger#diamond-metal-contacts` | Canon took `E_carb` and the 1000-h outcomes but **not the prefactors** — so row 81 cannot be evaluated at any other time or temperature from canon alone. Dangling-promise class. |
| 55 | F.6 thermal-stability ranking (6 tiers, Pt → Ti/Al/Ni) | `11.4-deriv-defects.md#f6-thermal-stability-ranking-500c-harsh-env-contacts` | mine | `oracle/accuracy/accuracy-ledger#diamond-metal-contacts` | A design conclusion drawn from F.4+F.5; the engineered-stack entries (TiN barrier + Pt/Au cap; W with TiN) are recommendations that exist nowhere else. |

### `11.4` Part G — high-T degradation

| # | Fact or block | Current location | Disposition | Target | Evidence |
|---|---|---|---|---|---|
| 56 | G preamble: "all Arrhenius-form `rate = ν₀exp(−E_a/kT)`" + `rate_thermal_proc` signature | `11.4-deriv-defects.md:425-428` | delete | — | Verbatim in canon: `2.4-multiscale-state.md:162-163`, *"all Arrhenius rates use `rate = ν₀·exp(−E_a/kT)` ([deriv-defects] Part G)"*. |
| 57 | G.1 vacancy generation `d[V]/dt = G − [V]k_ann`; `G_total = G_thermal + G_carbide_front + G_ion_flux` | `11.4-deriv-defects.md#g1-vacancy-generation` | delete | — | Registry row 105 `vacancy-generation-arrhenius`, Source `S3 (defects G.1)`; fully restated at `2.4-multiscale-state.md:167-177` including the `G_interface`/`G_irradiation` split. |
| 58 | G.2 dopant diffusion PDE + 4-row diffusivity table (B 4.5 eV, N 5.0, H 1.7, Li 1.0 eV) | `11.4-deriv-defects.md#g2-dopant-diffusion` | delete (partial — see 59) | — | Registry row 106; `2.4-multiscale-state.md:178-180` carries the PDE, H's `E_diff=1.7 eV`, `D(500 °C)≈1e−13`, and the 6 µm figure. |
| 59 | G.2 **B / N / Li rows** (`E_diff` 4.5 / 5.0 / 1.0 eV, profile shifts <1 nm / negligible / runaway) | `11.4-deriv-defects.md:447-452` | mine | `oracle/accuracy/accuracy-ledger#dopant-levels` | Canon took **only the H row**. The B and N rows are what license the claim that the *doping profile* is stable at 500 °C while H is not — that contrast is the whole point of G.2 and canon states only half of it. |
| 60 | G.2 "**H diffusion is the silent killer**" — H redistribution exposes dangling bonds, changes χ, breaks H-activated p-GaN | `11.4-deriv-defects.md:454` | mine | `oracle/state/multiscale-state#surface-coupling` | The three-way coupling (surface χ ↔ bulk H ↔ dopant activation) that Cross-stream note 4 (row 71) flags as needing a single owner. Canon's F-G2 carries the transport, not the consequence. |
| 61 | G.3 platelet nucleation `k_nuc[N_s]² − k_dis[platelet]`, `E_nuc ≈ 3.5 eV`, half-life years@500 °C / hours@1000 °C | `11.4-deriv-defects.md#g3-defect-clustering-n-platelets-in-diamond` | delete | — | Registry row 107; restated at `2.4-multiscale-state.md:181-183` including both half-lives. |
| 62 | G.3 "platelets pin dislocations and act as charged scatterers" | `11.4-deriv-defects.md:462` | mine | `oracle/state/multiscale-state#slow-tier-onsets` | The observable consequence — why platelet population matters to transport. Not carried by the F-G3 landing. |
| 63 | G.4 "carbide growth — see F.5; treat `x_carbide` as an interface state variable" | `11.4-deriv-defects.md#g4-carbide-growth-see-f5` | delete | — | Pure pointer. The state-variable decision landed: `2.4-multiscale-state.md:143` lists `carbide_thickness ≔ x_carbide` as a slow-fiber field. |
| 64 | G.5 Black's equation `MTTF = (A/j^n)exp(E_EM/kT)`, `n≈2`, `E_EM` 0.7–1.0 (Al) / 1.2 (Cu) / 1.5 eV (refractory); MTTF ~10³–10⁴ h at 500 °C, 1e6 A/cm² | `11.4-deriv-defects.md#g5-electromigration` | mine | `data/registry-manifest.csv` (new row) + `oracle/accuracy/accuracy-ledger#diamond-metal-contacts` | **Not registered.** 11.4 I.2 item 12 proposed it; the "landed as" reconciliation (row 76) never confirmed it, and no row in `registry-manifest.csv` matches. A reliability FoM with no home. |
| 65 | G.6 CTE mismatch + Coffin–Manson `N_f = C(Δε_pl)^−β`, `β≈2` ductile / `β≈4` brittle; 5-metal α table; **diamond α ≈1.1 (RT) / ≈3 (500 °C) / ≈4.5 (1200 °C)** | `11.4-deriv-defects.md#g6-thermomechanical-fatigue-cte-mismatch` | mine | `oracle/accuracy/accuracy-ledger#diamond-metal-contacts` | The diamond CTE(T) anchor was a 2026-07-07 correction (changelog) and exists in no CSV. Coffin–Manson is unregistered (11.6 D.1 has no equivalent; 11.5 I.3 proposes `plastic-strain-fatigue-life` separately — see Contradiction C6). |
| 66 | G.7 vibration-induced generation `dN_dis/dt = κ_vib(σ/σ_y)^m f_vib`, `m≈4–6`; `G_V = ξ ρ_dis v_dis b` | `11.4-deriv-defects.md#g7-vibration-induced-defect-generation` | delete | — | Registry row 108 `vibration-induced-vacancy-generation`, Source `S3 (defects G.7)`; restated in full at `2.4-multiscale-state.md:184-186`. |
| 67 | G.7 turbine coupling rationale ("broadband 100 Hz–10 kHz couples to acoustic phonons; the relevant kinetics is dislocation multiplication and vacancy generation at moving dislocations") | `11.4-deriv-defects.md:498-500` | mine | `program/purpose/rationale#harsh-environment-envelope` | The *why* behind row 108 — what physically connects a turbine vibration spectrum to a defect rate. Nowhere else. |

### `11.4` Part H — heterostructure interfaces

| # | Fact or block | Current location | Disposition | Target | Evidence |
|---|---|---|---|---|---|
| 68 | H.1 misfit convention `ε_misfit = (a_film − a_sub)/a_sub`, coherent-strain and biaxial-stress relations | `11.4-deriv-defects.md#h1-lattice-mismatch--strain` | delete | — | Canon: `10.4-traps.md:76-78` trap 4, *"Misfit strain sign. Normalized corpus-wide to `ε_misfit = (a_film − a_sub)/a_sub`."* The traps register survives. |
| 69 | H.1 **substrate table** (SiC 4.36 → −18%; Si 5.43 → −34%; Ir 3.84 → −7%; Pt 3.92 → −9%; c-BN 3.615 → −1.3%) with lattice constants | `11.4-deriv-defects.md:515-521` | mine | `oracle/accuracy/accuracy-ledger#heteroepitaxy` | Duplicated at `11.6-deriv-csp.md#c4-diamond-on-substrate-epitaxy` (row 116) — both die together. **11.4 carries the `a_substrate` column, 11.6 does not**, so only 11.4's version is recomputable. Land 11.4's; c-BN at −1.3% is the Cap-1 hetero pair (`8.2-capability-slices.md:35`, row 54). |
| 70 | H.2 Matthews–Blakeslee `h_c` transcendental form + typed signature; `h_c ≈ 1 nm` for diamond/Ir | `11.4-deriv-defects.md#h2-critical-thickness-matthewsblakeslee` | delete | — | Registry row 54 `critical-thickness-force-balance` `(a_film, a_sub, ν, b) → h_c`, `D3`, whose Source cell already carries the transcendental-fixed-point and adjoint analysis. Trap 4 names Matthews–Blakeslee as what the sign convention breaks. |
| 71 | H.2 conclusion "diamond films on Ir relax through misfit dislocation networks immediately" | `11.4-deriv-defects.md:532` | mine | `oracle/accuracy/accuracy-ledger#heteroepitaxy` | The design consequence of `h_c ≈ 1 nm`; determines that coherent-growth residuals are inapplicable to the best heteroepitaxy route. |
| 72 | H.3 misfit dislocation density `ρ_misfit(h)`, `ε_relax(h) ≈ ε(1 − h_c/h)` | `11.4-deriv-defects.md#h3-misfit-dislocation-density` | mine | `data/registry-manifest.csv` row 54 (Source cell) | The post-`h_c` branch of row 54 — what the formula means *above* critical thickness. Unstated in the registry and nowhere in canon. |
| 73 | H.4 interface chemistry, 4 pairs (diamond/SiC amorphous-C 1–2 nm; diamond/Si SiC 5–20 nm at ~800 °C; diamond/Ir no carbide; diamond/Pt d-band hybridization) | `11.4-deriv-defects.md#h4-interface-chemistry` | mine | `oracle/accuracy/accuracy-ledger#heteroepitaxy` | Overlaps `11.6-deriv-csp.md#c4` but 11.6 gives defect densities and 11.4 gives interlayer chemistry/thicknesses — **disjoint content, both dying.** Merge at landing. |
| 74 | H.5 thermal-cycle stability + delamination criterion; diamond/SiC and diamond/Ir survive >1000 cycles to 500 °C, diamond/Si fails within ~100 | `11.4-deriv-defects.md#h5-thermal-cycle-stability` | mine | `oracle/accuracy/accuracy-ledger#heteroepitaxy` | The durability verdict per substrate. `11.6-deriv-csp.md#e1` proposes `R_ThermalCycleStability` as the residual; **this is the only place the residual has anything to be checked against.** |

### `11.4` Part I — bridge to /physics, and tail

| # | Fact or block | Current location | Disposition | Target | Evidence |
|---|---|---|---|---|---|
| 75 | I.1 existing-registry reuse map (5 rows) | `11.4-deriv-defects.md#i1-existing-registry-slots` | delete | — | A snapshot-era mapping exercise. Every claim it makes has landed: radiative → row 134; Boltzmann → row 35; drift–diffusion → `2.4-multiscale-state.md:329`; Arrhenius template → `2.4-multiscale-state.md:162`. The D.3 "radiative branch" mislabel it records fixing is history (D1). |
| 76 | I.2 seventeen proposed new formulas + the superseded-scoping parenthetical ("24 already in registry / 41 total" against a 2026-06 snapshot) | `11.4-deriv-defects.md#i2-new-formulas-to-add` | delete | — | Proposal record whose outcome is the registry itself (132 substantive rows). Explicitly self-marked superseded at `:590`. **But: items 12 (Black's MTTF) and 13 (Coffin–Manson) never landed** — carried forward as rows 64 and 65 above rather than lost here. |
| 77 | I.3 three proposed observable bundles (interface-resolved, defect-resolved, surface-resolved) with their index tuples | `11.4-deriv-defects.md#i3-observable-bundles` | delete | — | Landed as B4/B5/B6 (`6.7-observable-bundles.md`; registry rows 30–35 carry `B4`, row 44 `B5`, rows 47–51 `B6`). The proposal's index structure is what the bundles now encode. |
| 78 | I.4 five residual categories, incl. **`thermodynamic-consistency` (Gibbs adsorption, charge–Fermi Maxwell, Clausius–Clapeyron analog)** | `11.4-deriv-defects.md#i4-residual-categories` | delete | — | Landed verbatim: `2.4-multiscale-state.md:446-454` R-T1/R-T2/R-T3, cited to "from [deriv-defects] Part I.4", filed under `Static/Thermodynamic` (`3.2-residual-definitions.md:165`). |
| 79 | I.4 `reference-battery` residual proposal ("predicted vs known DFT/experimental anchors") | `11.4-deriv-defects.md:607` | delete | — | Landed as the reference battery itself: `9.2-reference-battery.md`, cert obligation 4 (`9.1-accuracy-ledger.md:131`). |
| 80 | I.5 template reuse map + **two new templates: `InterfaceEquilibriumOf`, `SelfConsistentChargeBalanceOf`** | `11.4-deriv-defects.md#i5-methods--templates` | mine | `oracle/state/multiscale-state#charge-balance-closure` | The template *names* are dead (the closed method alphabet is `canonical-vocabularies §1`). But `SelfConsistentChargeBalanceOf` is the fixed-point-on-`E_F` closure of row 13 — the same homeless fact, stated a second way. Land once; see Open questions `fermi-level-charge-balance-closure`. |
| 81 | I.6 two-tier classification of the six Part-G processes | `11.4-deriv-defects.md#i6-two-tier-classification-of-part-g-processes-concrete-table` | mine | `operator/training/curriculum#tier-ladder` | Per-process cheap/faithful split for the degradation channel. Canon's F-G1…F-G4 carry the cheap path only; the faithful column (Z–N at self-consistent `E_F`; reaction-diffusion carbide; atomistic `Z*`; charge-state-resolved `D^q`) is unstated elsewhere. |
| 82 | I.7 eight-step diamond-anchor implementation order | `11.4-deriv-defects.md#i7-diamond-anchor-priority-list-phase-2-implementation-order` | mine | `program/build/build-order#defect-channel` | A build-ordering claim with dependencies stated (Z–N + Freysoldt first "because everything else depends on `E_F`-resolved `E_form`"). `8.5-build-order.md` is the surviving owner of ordering. |
| 83 | Cross-stream notes 1–3 (charge-balance as shared implementation; interface bundle co-design; reference-battery as a universal residual category) | `11.4-deriv-defects.md#cross-stream-coordination-notes` | delete | — | All three resolved: charge balance → row 13/80 (still open, carried); interface bundle → B6; reference battery → `9.2-reference-battery.md`. Stream-coordination framing is scaffolding of a process that no longer exists. |
| 84 | Cross-stream note 4: **H diffusion is a three-way coupling (surface termination ↔ long-time bulk ↔ dopant activation) needing one owner** | `11.4-deriv-defects.md:652` | mine | `oracle/state/multiscale-state#surface-coupling` | An unresolved ownership claim, not a resolved one. Restated as Open question `hydrogen-three-way-coupling-owner`. |
| 85 | "End of report." | `11.4-deriv-defects.md:654` | delete | — | Container artifact. |
| 86 | `## Changelog` (4 entries: 2026-07-22, 07-21, 07-16, 07-07) | `11.4-deriv-defects.md#changelog` | delete | — | D1: changelogs are abolished outright. Advancements extracted to **Log-worthy L1, L2**. |

### `11.5-deriv-high-field` — frontmatter and header

| # | Fact or block | Current location | Disposition | Target | Evidence |
|---|---|---|---|---|---|
| 87 | `authority: supporting`, `content-hash: cae3f3a52cb5`, vacuous `canonical-for: [deriv-high-field]` | `11.5-deriv-high-field.md:4-7` | delete | — | Same as rows 1–2. Plan §2 defect 4 names all nine `deriv-*` pages. |
| 88 | `depends-on: [canonical-vocabularies, deriv-observable-catalog, deriv-transport, formula-registry, properties, timeline]`; `referenced-by: [multiscale-state, out-of-scope]` | `11.5-deriv-high-field.md:8-17` | delete | — | Four outbound edges leave ch. 11 (`canonical-vocabularies`, `formula-registry`, `properties`, `timeline`); all four targets survive, so no outbound rewiring is needed — only the six inbound edges (Notes §A). |
| 89 | **Provenance rule header: "Seed coefficients from the accuracy-ledger / reference-data CSVs, never from this file's tables — the CSVs are the audited canonical values"** | `11.5-deriv-high-field.md:21-24` | mine | `practice/agent-contract#value-provenance` | This is the plan's own trap rule, already written down inside the corpus. It generalizes past this page and belongs in the machine-readable contract (§8). Do not lose it when the page carrying it dies. |
| 90 | Scope paragraph (turbine envelope `T_L ≥ 500 °C`, `E` to several MV/cm; anchor material set; **"diamond is the non-polar anchor (no Fröhlich term); the other UWBG anchors are polar"**) | `11.5-deriv-high-field.md:26` | mine | `oracle/certification/applicability-classifiers#is-polar-material` | The polar/non-polar split is an **applicability classifier that already exists and is already trap-relevant**: trap 44 (`10.4-traps.md:308-311`) records a worked example firing Fröhlich on non-polar diamond "violating its own `is-polar-material` classifier". The classifier's material assignment lives here. |
| 91 | CS-discipline reminder ("every quantity as `name : (typed inputs) → typed output`, every residual as `‖LHS − RHS‖²`, every method a sub-method of the closed 12") | `11.5-deriv-high-field.md:28` | delete | — | Canon: `6.1-canonical-vocabularies.md` owns the closed method alphabet; `3.2-residual-definitions.md` owns residual form. A restatement of house style inside a page. |

### `11.5` Part A — high-field transport regimes

| # | Fact or block | Current location | Disposition | Target | Evidence |
|---|---|---|---|---|---|
| 92 | **A.1 six-regime operational table** (Ohmic / warm / hot / velocity-saturated / avalanche / breakdown, with field windows and governing object) | `11.5-deriv-high-field.md#a1-operational-regime-table` | delete | — | Landed in canon: `2.4-multiscale-state.md:312-314`, *"Validity: Ohmic `≲10⁴ V/cm` (`T_e≈T_L`); warm `10⁴–10⁵`; hot `10⁵–10⁶`… saturated `≳ few×10⁵` (`j≈qnv_sat`)"*, cited to Part E.4. The avalanche/breakdown rows land via registry rows 74 and 123. |
| 93 | A.1 **per-material anchor table** (7 materials × `μ_e` × `v_sat` × `E_BR`) | `11.5-deriv-high-field.md:47-55` | delete | — | Superseded by the audited canonical set: `9.1-accuracy-ledger.md:185-191` and `transport-coefficients.csv`. The page's own header (row 89) forbids seeding from it. **See Contradictions C7, C8, C9** — three of its cells disagree with the CSVs today. |
| 94 | A.1 c-BN and h-BN rows (c-BN `μ_e ~825`, `v_sat ~1.5e7`, `E_BR ~17.5`; h-BN anisotropic, `E_BR ~12`) | `11.5-deriv-high-field.md:50-51` | mine | `oracle/accuracy/accuracy-ledger#companion-materials` | c-BN and h-BN have **no rows in any reference-data CSV** (verified: `grep -i "c-BN\|h-BN" physics/library/cert/reference-data/*.csv` → nothing). Unlike the III-N rows these were never audited and never landed. Land tagged unprovenanced, or the material drops out of the corpus entirely. |
| 95 | A.2 Caughey–Thomas `μ(E) = μ₀[1 + (μ₀E/v_sat)^β]^(−1/β)` + typed signature | `11.5-deriv-high-field.md#a2-field-dependent-mobility--caughey-thomas` | delete | — | Verbatim in canon: `2.4-multiscale-state.md:328-330`, *"field-dependent mobility Caughey–Thomas `μ(E)=μ₀[1+(μ₀E/v_sat)^β]^(−1/β)` ([deriv-high-field] Part A.2)"*. Coefficients in `transport-coefficients.csv` (`caughey-thomas-*`). |
| 96 | A.2 cheap/faithful split + **the method-equivalence residual `CT(E) ≡ BTE-j(E)/(nqE)`** | `11.5-deriv-high-field.md:72-73` | delete | — | Landed: `2.4-multiscale-state.md:331-332` ("Faithful tier verifies vs BTE-`j(E)` as an `Algebraic/MethodEquivalence` residual") and `3.2-residual-definitions.md:150-164` category 15, which distinguishes equivalence from consistency pairs. |
| 97 | A.2 `mobility-impurity-phonon` proposal (Caughey-Thomas-Klaassen with `T^−n` phonon + Brooks–Herring ionized-impurity terms) | `11.5-deriv-high-field.md:72` | mine | `data/registry-manifest.csv` (Source cell, μ₀ row) | `μ₀(T, N_D)` is a declared micro-supplied input at `2.4-multiscale-state.md:330-331` with **no stated formula**. Brooks–Herring + `T^−n` is the only statement of what supplies it. Dangling-promise class. |
| 98 | A.3 `v_sat` POP ceiling — **the Shockley form `√(8ℏω_LO/(3π m*))`** and the record of the bare `√(2ℏω_LO/m*)` form disagreeing by 2× | `11.5-deriv-high-field.md#a3-saturation-velocity-v_sat` | mine | `data/registry-manifest.csv` (Source cell, a `v-sat` row) | **No `v-sat` row exists in the registry** (verified against `registry-manifest.csv`); `saturation-velocity` is a *seeded CSV coefficient*, not a formula. So the closed form for `v_sat` is homeless. The two-forms-for-one-row finding is **Log-worthy L3**; the surviving form is the Shockley one. |
| 99 | A.3 `v-sat-intervalley` `(ΔE_valley, m*, τ_iv)` for diamond | `11.5-deriv-high-field.md:80` | mine | `data/registry-manifest.csv` (Source cell, a `v-sat` row) | The non-polar branch — required because the POP form does not apply to the MVP material (row 90's classifier). |
| 100 | A.4 worked example (diamond 800 K, `E=5e5`): `μ₀≈175`, `μ(E)≈25.6`, `v_d≈1.28e7 cm/s` — already saturated | `11.5-deriv-high-field.md#a4-worked-example-diamond-800-k-e--5105-vcm` | mine | `oracle/accuracy/accuracy-ledger#diamond-high-field` | The conclusion — **turbine-envelope diamond devices operate saturated, so `μ` is not the design lever** — is a project-level claim stated nowhere else. Carry the conclusion; the `μ₀(800 K)` scaling exponent 2.5 conflicts with the CSV's declared `T^(−1.5..−2.8)` band only in that it picks a point in it. |
| 101 | A.4 correction parenthetical (evaluated the β=2 form one line after declaring β=1) | `11.5-deriv-high-field.md:91-94` | delete | — | History of a fix (D1). **Log-worthy L4.** |

### `11.5` Part B — Fröhlich / polar-optical scattering

| # | Fact or block | Current location | Disposition | Target | Evidence |
|---|---|---|---|---|---|
| 102 | B preamble: applies to AlN/GaN/β-Ga₂O₃/c-BN/AlGaN, **not diamond**; h-BN borderline in-plane | `11.5-deriv-high-field.md:100` | mine | `oracle/certification/applicability-classifiers#is-polar-material` | Same classifier as row 90; the h-BN "borderline" case is the edge the classifier must decide and is stated only here. |
| 103 | B.1 `polar-coupling-constant` α = `(q²/4πℏ)√(m*/(2ℏω_LO))(1/ε∞ − 1/ε₀)` + typed signature | `11.5-deriv-high-field.md#b1-fröhlich-coupling` | mine | `data/registry-manifest.csv` (Source cell) | The **defining formula for `α_F`**, which is a declared input to registry row 73 `tau-energy-POP-acoustic` `(α_F, v_s, ρ, Ξ, T) → τ_E`. Row 73 consumes `α_F` and nothing in the corpus says how it is computed. Dangling-promise class, high confidence. |
| 104 | B.1 five-material anchor table (`ω_LO`, `ε₀`, `ε∞`, `m*/m_e`, `α`) | `11.5-deriv-high-field.md:120-126` | delete | — | GaN/AlN rows are the audited canonical set (`9.1-accuracy-ledger.md:264`: `α_F` 0.40 / 0.58, `ω_LO` 92 / 110–114 meV) and the diamond row (`α = 0`, non-polar) restates row 90's classifier. β-Ga₂O₃ and c-BN rows are unprovenanced — carried by row 94's target. |
| 105 | B.2 POP scattering rate + Bose–Einstein occupancy + **emission threshold `ε ≥ ℏω_LO`** | `11.5-deriv-high-field.md#b2-pop-scattering-rate` | mine | `data/registry-manifest.csv` (Source cell) | Unregistered. The emission threshold is what makes the hot-carrier distribution non-Maxwellian and is the physical basis of Part E's long `τ_E`. |
| 106 | **B.3 `μ_PO ∝ [exp(ℏω_LO/k_BT) − 1]`, and the load-bearing consequence: β-Ga₂O₃'s `ℏω_LO ≈ 44 meV ≈ k_B·500 K`, so it sits on the high-T side of its Fröhlich curve in turbine conditions and collapses harder than GaN** | `11.5-deriv-high-field.md#b3-t-dependence-of-po-limited-mobility` | mine | `oracle/accuracy/accuracy-ledger#companion-materials` | A material-selection verdict for the harsh envelope, derived not tabulated. Nowhere else in the corpus. The page marks it "**the** load-bearing reason" and it is. |

### `11.5` Part C — breakdown

| # | Fact or block | Current location | Disposition | Target | Evidence |
|---|---|---|---|---|---|
| 107 | C.1 Chynoweth `α(E) = a·exp(−b/E)` + typed signature | `11.5-deriv-high-field.md#c1-chynoweths-law` | delete | — | Registry row 74 `impact-ionization-coefficient`, whose Source cell already carries the unit guard ("α is ionizing collisions per unit length — cm⁻¹, not a rate in s⁻¹"). |
| 108 | C.1 five-material coefficient table | `11.5-deriv-high-field.md:172-182` | delete | — | Reconciled to the audited set 2026-07-16 and the page says so at `:180-182`. Canonical copies: `transport-coefficients.csv` `impact-ionization-*` rows and `9.1-accuracy-ledger.md:185-191`. Verified cell-by-cell to agree today. |
| 109 | C.2 avalanche multiplication `M = 1/(1 − ∫α dx)`, **breakdown integral `∫α dx = 1`**, typed signature | `11.5-deriv-high-field.md#c2-avalanche-multiplication-and-breakdown-condition` | mine | `data/registry-manifest.csv` (new row) + `oracle/registry/observable-bundles#b9` | **Not registered.** Row 74 gives `α(E)`; nothing integrates it. `E_BR` is quoted as a material property throughout the corpus with no stated route from `α` to it. This is the missing link and it is MVP-adjacent. |
| 110 | C.2 shape claim: "the breakdown observable is a field-on-grid integrated to a scalar — same shape as a 1D Poisson solve" | `11.5-deriv-high-field.md:198` | mine | `oracle/registry/observable-bundles#b9` | An interface/bundle-shape claim; determines that breakdown needs the field-on-grid interface. Relevant to J.2's `field-resolved` proposal (row 129). |
| 111 | C.3 `E_BR(T) = E_BR(300)[1 + κ_BR(T−300)]`, `κ_BR ≈ +5e−4 /K` diamond, `+7e−4` 4H-SiC; **positive slope in wide-gap** | `11.5-deriv-high-field.md#c3-temperature-dependence-of-e_br` | delete | — | Registry row 123 `breakdown-field-temperature-slope`, Source `S4 (E_b(T)=E_b0[1+κ_BR(T−300)], κ_BR>0 ⇒ E_b rises with T; Hiraiwa-Kawarada…)`; values at `9.1-accuracy-ledger.md:187,190`. |
| 112 | C.3 **the design trade-off: "device hardens against breakdown as it heats, but mobility collapses, so the constraint trades"** | `11.5-deriv-high-field.md:202` | mine | `program/purpose/rationale#harsh-environment-envelope` | The counter-intuitive coupling that motivates joint E/T optimization. Row 123 carries the slope; nothing carries the trade. |
| 113 | C.4 Zener/`interband-tunneling-rate` with Kane prefactor; meaningful only above ~1e7 V/cm in UWBG; **flagged second-priority** | `11.5-deriv-high-field.md#c4-zener-tunneling` | mine | `oracle/certification/out-of-scope#deferred-mechanisms` | A deliberate deferral with a stated numeric threshold. `5.3-out-of-scope.md` is the surviving page for deferrals and already receives one deferral from this file (row 128). |
| 114 | C.5 cheap/faithful split + method-equivalence residual Chynoweth ≡ BTE | `11.5-deriv-high-field.md#c5-cheap-vs-faithful` | delete | — | Landed and *refined*: `3.2-residual-definitions.md:155-164` names "cheap-Chynoweth vs BTE/MC α" explicitly as a **consistency pair** (no agreement theorem, `τ_method` tolerance) — not the equivalence pair this section assumes. The canon version is strictly better. Log as Contradiction C10. |

### `11.5` Part D — self-heating

| # | Fact or block | Current location | Disposition | Target | Evidence |
|---|---|---|---|---|---|
| 115 | D preamble: "the central failure-mode coupling for jet-turbine UWBG — the device that survives breakdown still has to dissipate the heat" | `11.5-deriv-high-field.md:219` | mine | `program/purpose/rationale#harsh-environment-envelope` | Frames why the coupled PDE exists. One sentence, no other home. |
| 116 | D.1 heat equation `C_pρ ∂T/∂t = ∇·(κ∇T) + j·E` and its **GENERIC reading** (Joule term drives `∂T/∂t` via `δS/δx`; diffusion is the symmetric-PSD spatial coupling) | `11.5-deriv-high-field.md#d1-heat-flow-equation` | delete | — | Landed: `2.4-multiscale-state.md:339-341` states the (H) PDE verbatim, cited to Part D.4. The GENERIC reading is `3.1-generic-dynamics.md`'s subject. |
| 117 | D.2 `κ(T) = κ_0(T_0/T)^n`, `n ≈ 1.25` diamond / `1.0` GaN; and the note that it landed split across rows 25 + 121 rather than as one power law | `11.5-deriv-high-field.md#d2-thermal-conductivity-t` | delete | — | The page states its own supersession at `:231` — rows 25 (`single-mode-rta-lattice-kappa`) and 121 (`kappa-4phonon-high-t-correction`) own it, with row 122 (`iterative-lbte-kappa`) as the consistency partner (`3.2-residual-definitions.md:163-164`). The fit is self-labelled "survey-grade". |
| 118 | D.2 **six-material κ(T) table at 300/600/900/1200 K** | `11.5-deriv-high-field.md:235-242` | delete | — | Superseded by `9.1-accuracy-ledger.md:167-172` + `transport-coefficients.csv`, which are canonical and use a **different temperature grid** (300/773/1100 K). **Contradictions C11 (GaN), C12 (diamond monotonicity), C13 (β-Ga₂O₃ scalar-vs-tensor).** Do not seed from it. |
| 119 | D.3 AMM/DMM thermal boundary resistance, inputs, Robin BC role; anchors (diamond/W ≈1e−8, GaN/SiC ≈3e−9, β-Ga₂O₃/Cu ≈2e−8 m²K/W) | `11.5-deriv-high-field.md#d3-thermal-boundary-resistance-tbr` | mine | `data/registry-manifest.csv` (new row) + `oracle/accuracy/accuracy-ledger#diamond-metal-contacts` | **Unregistered and unanchored** — no `TBR` row in `registry-manifest.csv`, no CSV entry. TBR at the diamond/W contact is the thermal bottleneck of the whole device concept and exists in one place. High-value mine. |
| 120 | D.4 the coupled three-PDE system (Poisson + drift-diffusion + heat) and its residual | `11.5-deriv-high-field.md#d4-coupled-electrical-thermal-residual-the-key-new-residual-category` | delete | — | Landed verbatim as canon's homogenization target: `2.4-multiscale-state.md:336-344` states all three PDEs citing Part D.4. |
| 121 | D.4 **the category decision** — `coupled-pde/` as a 6th top-level vs `coupled-em-thermal-pde-residual` inside `eom-violation/`, with the reason (`eom-violation` is over unified-state evolution; this is over the emergent field tuple) | `11.5-deriv-high-field.md:265-281` | delete | — | Resolved by the taxonomy that now exists: `3.2-residual-definitions.md:59-68` — **nineteen** categories, of which two of the nine EOM categories are "the cross-tier slow and macro siblings added with `multiscale-state`". The macro sibling *is* this residual. The deliberation is scaffolding; the outcome is canon. Log-worthy L6 (the reasoning that produced it). |

### `11.5` Part E — hot carriers

| # | Fact or block | Current location | Disposition | Target | Evidence |
|---|---|---|---|---|---|
| 122 | E preamble: **large `ℏω_LO` in UWBG ⇒ long `τ_E` ⇒ `T_e` overshoots `T_L` at modest fields**; `τ_E ~ ℏω_LO/(2k_BT_L sinh(ℏω_LO/2k_BT_L)) × 10⁻¹³ s` | `11.5-deriv-high-field.md:287` | mine | `data/registry-manifest.csv` row 73 (Source cell) | **The closed form for `τ_E`.** Row 73 `tau-energy-POP-acoustic` `(α_F, v_s, ρ, Ξ, T) → τ_E` has Source `S4` and no formula. Canon at `2.4-multiscale-state.md:307-311` says row 73 "carries both channels" without stating either. Dangling-promise class, high confidence. |
| 123 | E.1 two-temperature balance + steady state `T_e − T_L = (2/3)(j·E)τ_E/(nk_B)` + typed signature | `11.5-deriv-high-field.md#e1-two-temperature-energy-balance` | delete | — | Verbatim in canon: `2.4-multiscale-state.md:307-308`, including the transient form which this page does not state. |
| 124 | E.2 the `tau-energy-pop` / `tau-energy-acoustic` split proposal and its landing as one row | `11.5-deriv-high-field.md#e2-τ_e-parameterization` | delete | — | Proposal record; `2.4-multiscale-state.md:308-311` states the outcome ("they were proposed as separate… formulas"). Note canon *also* carries the superseded proposal — flag for Phase 2 to drop that half. |
| 125 | E.2 four-material `τ_E` table (300 K / 800 K) | `11.5-deriv-high-field.md:313-318` | mine | `oracle/accuracy/accuracy-ledger#diamond-high-field` | `τ_E` is a **declared micro-supplied input** to canon's energy closure (`2.4-multiscale-state.md:308`) with no anchor anywhere. No CSV row. Unprovenanced but load-bearing — land tagged. |
| 126 | E.3 GaN HEMT worked example: `T_e − T_L ≈ 1.9e3 K`, `T_e ≈ 2.7e3 K` at `T_L = 800 K`; **avalanche threshold reached at sub-breakdown `E` because the carriers are hot** | `11.5-deriv-high-field.md#e3-worked-example-gan-hemt-channel` | mine | `oracle/accuracy/accuracy-ledger#diamond-high-field` | The conclusion is a *cert-boundary* claim: `E_BR` alone does not bound device safety at high `T_L`. It is why hot-carrier modelling is in scope at all. The sheet-vs-volumetric guard (`n_3D = n/t_ch`) is a live unit hazard — consider a trap. |
| 127 | E.3 correction parenthetical (100× Joule fix not propagated; sheet density used as volumetric; errors partly cancelled to a plausible 386 K) | `11.5-deriv-high-field.md:328-332` | delete | — | History of a fix (D1). **Log-worthy L5** — a two-error partial cancellation producing a plausible wrong answer is exactly what trap 44 is about. |
| 128 | E.4 `T_e` is **emergent** (2nd moment of the BTE distribution); `T_L` is already in state via `S_vib` | `11.5-deriv-high-field.md#e4-state-vector-status` | delete | — | Landed verbatim: `2.4-multiscale-state.md:296-298` ("Kept emergent (never promoted)… `T_e(r)` (2nd moment, closed form §8.1)") and `:311` ("`T_e` is **never state**"). |

### `11.5` Parts F–I

| # | Fact or block | Current location | Disposition | Target | Evidence |
|---|---|---|---|---|---|
| 129 | F.1 Fowler–Nordheim `J_FN` closed form + typed signature | `11.5-deriv-high-field.md#f1-fowler-nordheim` | mine | `data/registry-manifest.csv` (new row) | **Unregistered.** No `field-emission` row in `registry-manifest.csv`. Contact leakage at a Schottky barrier is an MVP-relevant observable (row 48's consumers) and the FN form is the only statement of it. |
| 130 | F.2 thermionic + TFE; **Padovani–Stratton `E_00 = (ℏ/2)√(N/(m*ε))` regime selector** | `11.5-deriv-high-field.md#f2-thermionic--thermionic-field-emission` | mine | `oracle/certification/applicability-classifiers#contact-transport-regime` | A three-way regime classifier with a computable discriminant. Unregistered, uncanonized. |
| 131 | F.2 **diamond at 800 K, `N_D=1e18`: `E_00 ≈ 8 meV` vs `k_BT ≈ 69 meV` ⇒ pure thermionic; at 300 K with 1e20 ⇒ TFE** | `11.5-deriv-high-field.md:363` | mine | `oracle/accuracy/accuracy-ledger#diamond-high-field` | Settles which contact model the MVP actually needs at the operating point — a scoping fact, not just a number. |
| 132 | F.3 image-force lowering `Δφ = √(qE/(4πε_s))`; diamond `Δφ ≈ 0.16 eV` at 1e6 V/cm, 0.50 eV at 1e7 | `11.5-deriv-high-field.md#f3-image-force-barrier-lowering` | delete | — | Registry row 49 `image-force-barrier-lowering` `(E, ε) → Δφ_IF`, `B6/B9`. Value is Wave-1-audited (`9.1-accuracy-ledger.md` residue / `11.5:619-627` records A15); `ε_s = 5.7` is in `material-constants.csv`. |
| 133 | F.4 method assignment to `response-of-to`; new `interface-tunneling` sub-method; NEGF as the faithful path routed out of scope | `11.5-deriv-high-field.md#f4-method-assignment` | delete | — | Landed: registry row 80 `NEGF-transmission` `T3/faithful`; the closed method alphabet is `6.1-canonical-vocabularies.md §1`. The sub-method proposal is a proposal against a now-closed vocabulary. |
| 134 | **Part G hydrodynamic phonon transport** — N/U collision split `C = C_N + C_U`; Poiseuille drift regime; second sound `c_2 = v_s/√3`; `phonon-poiseuille-length`; observed ~70 K in high-purity diamond, collapses at 800 K | `11.5-deriv-high-field.md#part-g--hydrodynamic-phonon-transport` | mine | `oracle/certification/out-of-scope#deferred-mechanisms` | Explicitly **"low-priority for jet-turbine envelope but high-priority if the project extends to cryogenic harsh environments (space, deep-cooled detectors)"** — a scoped deferral with a named re-entry condition. That is exactly `out-of-scope`'s job. Losing it loses the record that the regime was considered. |
| 135 | G closing: no new method primitive needed, just a typed `CollisionOperator` admitting the (C_N, C_U) split | `11.5-deriv-high-field.md:395` | mine | `oracle/certification/out-of-scope#deferred-mechanisms` | The cost estimate attached to the deferral — what re-entry would actually require. Carries with row 134. |
| 136 | H.1 NRT `N_d = 0.8·T_dam/(2E_d)` + `E_d` per host (diamond 37–50, GaN 20, Ga₂O₃ 25, AlN 35 eV) | `11.5-deriv-high-field.md#h1-nrt-displacement-damage` | delete | — | Registry row 111 `nrt-displacements`, Source `S4 (non-eq H.1)`; restated with all four `E_d` values at `2.4-multiscale-state.md:198-200`. **But see Contradiction C14** — `material-constants.csv:42` sources its β-Ga₂O₃ `E_d` back to *this page*, so the CSV's provenance is circular. |
| 137 | H.2 `frenkel-pair-yield` `(N_d, T_L, η_recomb) → DefectDensity` | `11.5-deriv-high-field.md#h2-defect-cascade-yield` | delete | — | Registry row 112, whose Source cell carries a *fuller* signature than this page (`[V]_irr = Φ·Σ_d·N_d·(1−η_recomb)`, `Σ_d = N_atom·σ_d` NIEL). Restated at `2.4-multiscale-state.md:201-208`. |
| 138 | H.3 `seu-cross-section` — "belongs to a digital-circuit layer above `/physics`; flag for downstream" | `11.5-deriv-high-field.md#h3-seu-rate-digital` | delete | — | Already migrated: `5.3-out-of-scope.md:101` — *"([deriv-high-field] H.3 disposition, now registered here)"*. The out-of-scope page states it owns this. |
| 139 | **H closing: UWBG radiation tolerance is not uniformly better than Si; diamond ~2× on `E_d` (37–50 vs Si 21 eV), GaN ~20 eV is at or below Si** | `11.5-deriv-high-field.md:417` | mine | `oracle/certification/out-of-scope#deferred-mechanisms` | A **refutation of a widely-assumed project premise**, derived from H.1's own numbers. The Si `E_d ≈ 21 eV` reference value appears nowhere else in the corpus. High value; do not lose. |
| 140 | H closing correction parenthetical ("§H.1's own numbers refute both halves — the ratio and the word 'uniformly'") | `11.5-deriv-high-field.md:417` | delete | — | History of a fix (D1). **Log-worthy L7.** |
| 141 | I.1 piezoresistance `Δρ/ρ = π_ijkl σ_kl`; diamond `π_11 ≈ +4e−11 Pa⁻¹` | `11.5-deriv-high-field.md#i1-piezoresistance` | mine | `data/registry-manifest.csv` (new row) + `oracle/accuracy/accuracy-ledger#diamond-high-field` | Unregistered; no CSV row. The vibration→transport coupling that makes the turbine envelope different from a static high-T envelope. |
| 142 | I.2 `ΔE_g = Ξ_c ε_hydro + Ξ_v ε_hydro`; diamond `Ξ ≈ −7 eV`; GaN `Ξ_c ≈ −9`, `Ξ_v ≈ +2 eV` | `11.5-deriv-high-field.md#i2-stress-induced-bandgap-shift` | mine | `oracle/accuracy/accuracy-ledger#diamond-high-field` | Deformation potentials for **diamond** are absent from the ledger, which carries only GaN/AlN `a_V` (`9.1-accuracy-ledger.md:208`). See Contradiction C15 on the GaN values. |
| 143 | **I.2 the sign convention: with `ΔE_g = Ξ·ε_hydro` and `ε_hydro` positive in dilation, negative `Ξ` means compression raises the gap; the magnitude for diamond's indirect gap is unpinned and the convention is the load-bearing part** | `11.5-deriv-high-field.md:437` | mine | `practice/traps` (new trap) | A sign-convention hazard with an explicit "treat the magnitude as unseeded" instruction. Same species as trap 4 (misfit sign) and the ledger's `b_P` / pyroelectric sign guards. Belongs in the register that survives. |
| 144 | I.3 cyclic-loading defect generation; `plastic-strain-fatigue-life` `(Δε_p, ε_f, c) → CyclesToFailure` | `11.5-deriv-high-field.md#i3-cyclic-loading-defect-generation` | mine | `data/registry-manifest.csv` (new row) | Unregistered. **Duplicates 11.4 G.6's Coffin–Manson under a different name and a different parameterization** — see Contradiction C6. Land once. |
| 145 | I.4 method assignment; **"the existing `applied_stress` slot in `Environment` already exposes the input cleanly"** | `11.5-deriv-high-field.md#i4-method-assignment` | mine | `oracle/state/crystal-inputs#environment` | Direct evidence for the plan's second confirmed homeless fact: the `Environment` record is used in signatures across canon and owned by no page. This page **names three of its slots**. See Open questions `environment-schema`. |
| 146 | I.5 `transport-under-stress` `(σ_ext, π_tensor, μ_0, T) → MobilityTensor` as the cheap combined path | `11.5-deriv-high-field.md#i5-recommendation` | mine | `data/registry-manifest.csv` (new row) | Unregistered; the composed cheap path for rows 141–143. |

### `11.5` Part J and tail

| # | Fact or block | Current location | Disposition | Target | Evidence |
|---|---|---|---|---|---|
| 147 | J.1 eighteen-formula proposal table + "**registry size grows from ~24 to ~42**" | `11.5-deriv-high-field.md#j1-formula-registry-additions` | delete | — | Proposal record superseded by the registry (132 substantive rows). **Unlike `11.4:590` and `11.6:283-284`, this page does not flag its own count as superseded** — the "~24 to ~42" reads as current. Contradiction C16. Rows F3/F4/F11/F14/F15/F17/F18 never landed and are carried forward individually above (rows 98, 99, 119, 129, 130, 134, 141, 144). |
| 148 | J.1 "keep them in a `formulas/non-equilibrium-uwbg/` sub-collection per house style" | `11.5-deriv-high-field.md:480` | delete | — | Contradicted by outcome: the registry is one flat manifest (`registry-manifest.csv`), no sub-collections. Dead organizational proposal. |
| 149 | J.2 two proposed bundles `field-resolved/` and `hot-carrier-resolved/`; **the `causal? : Boolean` interface question for non-causal nonlinear responses** | `11.5-deriv-high-field.md#j2-observable-bundles--additions` | mine | `oracle/registry/observable-bundles#b9` | Bundles landed as B9 (rows 49, 73, 74, 123 all carry `B9`). **The causality question did not land** — whether `response` must carry a `causal?` flag to host `α(E)`, `J_FN(E)` is unresolved and is restated as Open question `response-causality-slot`. |
| 150 | J.3 method-assignment table (10 rows) + **"two genuinely new sub-methods: `field-line-integral`, `interface-tunneling`, bounded — no new mathematical capability"** | `11.5-deriv-high-field.md#j3-method-assignment` | delete | — | The closed alphabet is `6.1-canonical-vocabularies.md §1` with three registered sub-methods (`2.4-multiscale-state.md:164-166`: "the closed alphabet's three registered sub-methods are unchanged"). These two are not among them; the proposal was declined by outcome. |
| 151 | J.4 residual additions: positivity (`kappa-non-negative`, `mobility-non-negative`, **`T-e-geq-T-L`**, `multiplication-finite`) | `11.5-deriv-high-field.md#j4-residual-categories` | delete | — | `T_e ≥ T_L` landed: `2.4-multiscale-state.md:313-314`, "*Positivity bound `T_e ≥ T_L` scored as a `Positivity` residual ([deriv-high-field] Part J.4)*". The other three are instances of a category that exists. |
| 152 | J.4 conservation additions: `energy-balance-joule-heat` (integral form), `current-continuity` | `11.5-deriv-high-field.md:533-535` | delete | — | Landed: `2.4-multiscale-state.md:294-295` carries `j(r)` as macro state expressly "so current-continuity `∇·j + ∂ρ/∂t = 0` is a scorable balance". |
| 153 | J.4 algebraic-identity additions incl. `einstein-relation-at-T` **extended to validate at `T_e ≠ T_L`** | `11.5-deriv-high-field.md:537-542` | mine | `oracle/laws/residual-definitions#balance-laws` | Category 13 `Algebraic/BalanceLaws` names the Einstein relation (`3.2-residual-definitions.md:140-142`). The **hot-carrier extension** (does Einstein hold when carriers and lattice are at different temperatures?) is a distinct claim and is not there. |
| 154 | J.5 state-vector status table (6 quantities) + **"no additions to the unified state vector required; all Part-A–I physics is emergent from `(h, R_I, P_I, Π_h, Z_I, γ̂, A)`"** | `11.5-deriv-high-field.md#j5-state-vector-status-of-new-quantities` | delete | — | Landed and **superseded in the landing**: `2.4-multiscale-state.md:290-298` promotes `(T_L, φ, n, p, j)` to macro state, explicitly reversing the "already in state / emergent" verdicts for `T_L`, `E_field` and `j`. Contradiction C17. Canon wins. |
| 155 | J.5 2026-07-21 correction (the "current-density-from-distribution row is already in registry" claim was false) | `11.5-deriv-high-field.md:556` | delete | — | History of a fix (D1). **Log-worthy L8.** |
| 156 | J.6 two-tier accuracy summary (8 rows) + "**each row has the method-equivalence residual that ties cheap to faithful — this is the contract that makes the two-tier scheme verifiable**" | `11.5-deriv-high-field.md#j6-two-tier-accuracy-summary` | mine | `operator/training/curriculum#tier-ladder` | The claim that cheap↔faithful pairing is *what makes the scheme verifiable* is the design principle behind `3.2-residual-definitions.md`'s equivalence/consistency-pair distinction, stated as a general contract. Merge with rows 34 and 81. |
| 157 | J.7 material-coverage check; h-BN as the sparse spot with tensor-anisotropic transport from the start | `11.5-deriv-high-field.md#j7-material-coverage-check` | mine | `oracle/accuracy/accuracy-ledger#companion-materials` | A coverage audit — which materials have at least one anchor per formula. Carries with row 94. |
| 158 | Summary of recommendations, items 1–6 | `11.5-deriv-high-field.md#summary-of-recommendations-to-stream-j-cross-stream-synthesis` | delete | — | Restates J.1–J.5, all dispositioned above. Stream-J framing is dead process. |
| 159 | Summary item 7: **`Environment` needs an optional `temperature_gradient` field**; `applied_electric_field`, `applied_stress`, `temperature` already present | `11.5-deriv-high-field.md:592` | mine | `oracle/state/crystal-inputs#environment` | Names a **fourth** `Environment` slot and an unmet requirement on the record no page owns. Strongest single piece of evidence for the homeless-`Environment` finding. Carries with row 145; see `environment-schema`. |
| 160 | Summary item 8: the `response` interface needs `causal? : Boolean` | `11.5-deriv-high-field.md:593` | mine | `oracle/registry/observable-bundles#b9` | Duplicate of row 149's second half; land once. |
| 161 | "Related documents" list | `11.5-deriv-high-field.md:595-599` | delete | — | Superseded by generated `referenced-by` + `index/corpus.json` (§6). Its own instruction — "cite ids, not chapter numbers" — is canon at `10.1-conventions.md:221`. |
| 162 | `## Changelog` (5 entries: 2026-07-21, 07-16 ×3, 07-07) incl. the "Old values, for the record" lists | `11.5-deriv-high-field.md#changelog` | delete | — | D1, and these are the plan's named hazard: **superseded values behind a changelog.** The "old values for the record" blocks at `:612-614` and `:621-627` are exactly what must not survive into a page. Advancements extracted as **Log-worthy L8, L9**. |

### `11.6-deriv-csp` — frontmatter, header, legend

| # | Fact or block | Current location | Disposition | Target | Evidence |
|---|---|---|---|---|---|
| 163 | `authority: supporting`, `content-hash: 5bf567f4d304`, vacuous `canonical-for: [deriv-csp]` | `11.6-deriv-csp.md:4-7` | delete | — | Rows 1–2. |
| 164 | `depends-on: [deriv-defects, formula-registry, named-formulas, timeline, traps]`; `referenced-by: [multiscale-state, traps]` | `11.6-deriv-csp.md:8-16` | delete | — | Four outbound edges survive their targets; five inbound edges need repointing (Notes §A). |
| 165 | Intro: methodology survey grounded in GENERIC + 4-level BO hierarchy + the closed registries | `11.6-deriv-csp.md:20` | delete | — | Container framing. GENERIC is `3.1-generic-dynamics.md`; the BO hierarchy is `born-oppenheimer-levels`. |
| 166 | Conventions header — misfit `(a_film − a_sub)/a_sub`, χ termination-tagged, contact values survey-grade | `11.6-deriv-csp.md:22-25` | delete | — | Three restatements of surviving canon: trap 4 (`10.4-traps.md:76-78`), trap 18 (`:156`), trap 51 (`:349`). |
| 167 | **Cost-tier legend `T0`–`T4` and the note that it runs one tier deeper than the registry's `T0`–`T3`** | `11.6-deriv-csp.md:27` | mine | `practice/traps` (new trap) or `practice/glossary#cost-tier` | A **live collision**: this page's `T2`/`T3`/`T4` do not mean the registry's `T2`/`T3`. Every tier value in rows 172, 179, 184, 189 is in the local scheme. Phase 2 must translate, and the translation rule exists only here. Same species as trap 59 (`GAP` overloaded three ways). |
| 168 | **Differentiability legend: this survey's three-way word split (`smooth` / `piecewise` / `combinatorial`), originally `D+`/`D0`/`D−`, which collided with the registry's `D0 \| DN \| D1 \| D2 \| D3 \| D4` and where `D0` meant the opposite** | `11.6-deriv-csp.md:27` | delete | — | Scaffolding — the history of a renaming (D1). **Structurally important and I checked it specifically:** this legend is *already* de-collided and uses words, and it names the **current** post-2026-07-21 vocabulary including `DN`. Unlike `11.8-deriv-generator-catalog`, **11.6 does not carry the retired D-legend.** The one live instruction it gives — "`[named-formulas]` is canonical for those" — is canon. |

### `11.6` Part A — CSP methodology

| # | Fact or block | Current location | Disposition | Target | Evidence |
|---|---|---|---|---|---|
| 169 | A preamble: CSP as discrete-continuous combinatorial search under a black-box energy oracle; **canonical configuration-space encoding `c = (lattice, species, frac_coords, occupancies, spin/charge)` modulo space group**; symmetry quotient collapses by 10²–10⁴ but introduces non-smooth equivalence classes | `11.6-deriv-csp.md:33-37` | mine | `oracle/registry/typed-compositions#csp-candidate` | The **only definition of a CSP candidate type in the corpus.** Registry row 85 `structure-uniqueness-CSP` takes `(structure, set)` and nothing says what `structure` is. The non-smooth-equivalence remark is the reason row 85 is `D4`. |
| 170 | A.1 six-method comparison table (USPEX, CALYPSO, AIRSS, prototype substitution, MLIP-driven, generative) × search space / strategy / scoring / convergence / cheap proxy / faithful residual | `11.6-deriv-csp.md#a1-method-by-method` | mine | `program/purpose/library-landscape#csp-backends` | A landscape survey of external tooling. `1.3-library-landscape` (per §3, `program/purpose/library-landscape`) is the surviving page for "what exists outside and how it relates." Nothing in canon surveys CSP backends. |
| 171 | A.2 strengths and failure modes per method (extrapolation cliff; symmetry violations; charge-imbalance hallucinations; mode collapse on Fd-3m / P6₃mc) | `11.6-deriv-csp.md#a2-strengths-and-failure-modes` | mine | `program/purpose/library-landscape#csp-backends` | The failure modes are what the validity residuals in Part B are *for* — the motivating threat model. Mode collapse onto Fd-3m is directly the diamond case. |
| 172 | **A.3 the leverage claim: the CSP loop is outside the PINO, but one residual library serves three consumers — PINO physics-loss, generative post-filter, MLIP basin-hopping acquisition. "Build the residual library once and reuse it in every CSP backend."** | `11.6-deriv-csp.md#a3-the-pino-connection` | mine | `oracle/seams/residual-machinery#reuse` | A cross-module architecture claim (oracle residuals ↔ operator loss ↔ external CSP). It is the justification for the residual library being a shared artifact rather than a training-time detail. `7.x` seams pages are the surviving owner. High value. |

### `11.6` Part B — validity residuals

| # | Fact or block | Current location | Disposition | Target | Evidence |
|---|---|---|---|---|---|
| 173 | B preamble: normalization convention **`R = 0` ⇔ satisfied, `R > 0` ⇔ violation** for every residual | `11.6-deriv-csp.md:71` | delete | — | Canon: `3.2-residual-definitions.md` owns residual form; the `max(0, …)²` hinge convention is visible in registry row 124's Source cell (`R_hull = max(0, ΔG_form − ΔG_hull − δ_meta)²`). |
| 174 | **B.1 twelve-residual catalog** (`R_HumeRothery`, `R_Pauling`, `R_ChargeBalance`, `R_Stoichiometry`, `R_HullDistance_cheap`, `R_FormationEnergy`, `R_DynamicStab`, `R_BornStab`, `R_SymmetryConsistency`, `R_BondLengthSanity`, `R_CoordinationConsistency`) with cheap forms, faithful forms, tiers | `11.6-deriv-csp.md#b1-catalog-table` | delete (cheap forms → row 175) | — | The residuals landed as `Static/Snapshot`, category 16 (`3.2-residual-definitions.md:159-164`: "Valence-bond-sum charge balance; Born stability (now `elastic-stability-criteria`); dynamical stability; space-group equivariance of the snapshot") and as registry rows 53, 55, 56, 57, 67, 124 per this page's own F.2 map. |
| 175 | B.1 the **explicit cheap closed forms** — `Σ max(0, \|r_A−r_B\|/r̄ − 0.15)² + λ_EN(ΔEN)²`; `Σ_g∈G ‖g·x − x‖²`; `Σ max(0, (r_i+r_j)·0.7 − d_ij)² + max(0, d_ij − (r_i+r_j)·1.3)²`; soft-cutoff CN `Σ_j σ((r_cut−d_ij)/δ)` | `11.6-deriv-csp.md:77-87` | mine | `data/registry-manifest.csv` (Source cells, rows 53/56/57) | These are the **relaxations**, and `named-formulas` requires a `D4` row's relaxation to be named in its `source` cell or the registry-build gate fails (`6.6-named-formulas.md:105-109`). The soft-cutoff form is already cited by row 50 ("the form already used in deriv-csp", `registry-manifest.csv:51`) — so one of these cheap forms is *already* load-bearing registry provenance pointing at a doomed page. |
| 176 | B.2 diamond validity worked example (`R_ChargeBalance=0`; `R_SymmetryConsistency ~1e−30` at (1/8,1/8,1/8); `R_BornStab` with C₁₁/C₁₂/C₄₄ = 1079/124/578 GPa → R=0) | `11.6-deriv-csp.md#b2-worked-example-diamond-validity` | mine | `oracle/accuracy/reference-battery#diamond-validity` | **The only end-to-end validity check on the MVP material**, and I verified its `C_ij` against `elastic-tensors.csv` — 1079/124/578 exactly, McSkimin & Andreatch 1972. This is a reference-battery case in all but name. `9.2-reference-battery.md` survives. |
| 177 | **B.2 the diamond hull gotcha**: diamond is metastable vs graphite by ~25 meV/atom at T=0, so `R_HullDistance` must use a T,P-aware hull; cheap fix `ΔG = ΔE − TΔS_config − PΔV` | `11.6-deriv-csp.md:98-102` | delete | — | Landed completely: registry row 124 `tp-aware-hull` with `δ_meta`; `3.2-residual-definitions.md:168-172` states it *with* the +25 meV/atom anchor and σ; `material-constants.csv` `formation-energy-vs-graphite +25 meV/atom` (Berman–Simon); `8.2-capability-slices.md:35` lists row 124 in Cap 1. Four independent landings. |
| 178 | B.2 **generalization: "for harsh-environment chips the hull must be evaluated at operating conditions, not standard"** + `R_HullDistance_TP` row | `11.6-deriv-csp.md:100-102` | delete | — | The generalization is what row 124 *is*. Category 17 `Static/Thermodynamic` is defined as "depends on snapshot + environment (temperature, chemical potentials, partial pressures)". |
| 179 | **B.3 the cheap dynamical-stability proxy** — harmonic bond-bend Hessian `H_harm`, sparse 3N×3N diagonalization, `smooth` because the Hessian is analytic in the structure; **"gets the sign of stability right ~85% of the time on benchmark sets, which is enough for label generation"** | `11.6-deriv-csp.md#b3-why-cheap-dynamical-stability-matters` | mine | `operator/training/curriculum#label-generation` | A **cheap-label-quality budget with a number attached** — the accuracy at which a cheap proxy is good enough to generate training labels rather than validate. This is squarely the training-staging question the plan flags as homeless (§2), and it is the only quantified statement of a cheap-label acceptance threshold in the corpus. |

### `11.6` Part C — heterostructures

| # | Fact or block | Current location | Disposition | Target | Evidence |
|---|---|---|---|---|---|
| 180 | C.1 five-step heterostructure pipeline (phase choice → surface enumeration → lattice-matching `(m,n)` supercell search with rotations, 5% threshold → stacking enumeration → `γ_int` scoring) | `11.6-deriv-csp.md#c1-standard-pipeline` | mine | `data/registry-manifest.csv` row 51 (Source cell) | Row 51 `interface-stacking-energy` `(slab_A, slab_B, registry) → E_stack` has Source `S2` and no procedure. **The `(m,n)` supercell search and the 5% strain threshold are the procedure**, and 11.6 F.2 records that the proposed `interface-stacking-method` "landed as the `interface-stacking-energy` formula, row 51, not as a method" — so the method-level content had nowhere to go and stayed here. |
| 181 | C.2 eight heterostructure residuals (`R_LatticeMatch`, `R_StrainEnergy`, `R_WorkFunctionAlign`, `R_InterfaceEnergy`, `R_ChargeTransferSC`, `R_TerminationChemPot`, `R_ThermalMismatch`, `R_CarbideFormation`) with cheap/faithful forms | `11.6-deriv-csp.md#c2-heterostructure-residual-catalog` | delete (cheap forms → 182) | — | Landed as registry rows 47, 50, 51, 52, 54, 86 and as `Static/Thermodynamic` (carbide-formation is named explicitly at `3.2-residual-definitions.md:172`). |
| 182 | C.2 `R_ChargeTransferSC` **capacitor model `ΔQ = C(Φ_A − Φ_B)`** and `R_ThermalMismatch` `((α_A−α_B)ΔT)² × stiffness` | `11.6-deriv-csp.md:134,136` | mine | `data/registry-manifest.csv` (Source cells) | Two cheap forms with no registry row and no canon statement. The capacitor model is the only cheap route to interface charge transfer in the corpus. |
| 183 | **C.3 diamond-metal cheat-sheet table** (10 metals × Φ × contact behaviour × carbide × `T_stable` × use case) | `11.6-deriv-csp.md#c3-diamond-metal-interface-knowledge-cheat-sheet` | mine (merge into row 51's target) | `oracle/accuracy/accuracy-ledger#diamond-metal-contacts` | **Do not land both this and 11.4 F.4 independently.** The page declares F.4 the owner (`:143-146`) but the table **still disagrees with F.4 in 7 of 10 rows** — see Contradiction C3. Land 11.4 F.4's values; harvest from here only the columns F.4 lacks: *contact behaviour* (Schottky vs Ohmic vs reactive) and *use case* (gate / Ohmic / barrier / avoid). |
| 184 | C.3 χ recap (−1.3 H-term to +2.6 O-ketone) and diamond `E_g ≈ 5.47 eV` | `11.6-deriv-csp.md:141` | delete | — | Restatement of 11.4 E.1 (row 40) which the page itself calls canonical; `E_g = 5.47 eV` is in `material-constants.csv` (curated MVP anchor). |
| 185 | C.3 `R_InterfaceReactivity : (metal, semi, T) -> R⁺ × {stable, reactive, eutectic}` — lookup-table embedding + `max(0, T − T_reactive)²` cheap; grand-potential M-C-X phase diagram faithful | `11.6-deriv-csp.md:161-163` | mine | `data/registry-manifest.csv` (new row) | Unregistered. A **learned-feature table supervised by a residual** — the summary (row 195) calls this the most domain-specific content in the survey. No other statement of it. |
| 186 | C.4 diamond-on-substrate table (5 substrates × misfit × typical defect density × notes) | `11.6-deriv-csp.md#c4-diamond-on-substrate-epitaxy` | mine (merge with row 69) | `oracle/accuracy/accuracy-ledger#heteroepitaxy` | Misfit column duplicates 11.4 H.1 (verified numerically identical: −7 / −9 / −18 / −34%). **The defect-density column (Ir 10⁶–10⁷, Pt 10⁸ cm⁻²) is unique to this page** and is the actual heteroepitaxy quality metric. Sapphire row is unique too. |
| 187 | C.4 `R_HeteroEpiNucleation` cheap form `R_LatticeMatch · exp(−E_nuc/kT)` | `11.6-deriv-csp.md:175` | mine | `data/registry-manifest.csv` (new row) | Unregistered; ties lattice match to a nucleation barrier — the only route from static geometry to a growth outcome. |

### `11.6` Parts D–E — doping and defect-pattern residuals

| # | Fact or block | Current location | Disposition | Target | Evidence |
|---|---|---|---|---|---|
| 188 | D preamble: dopant placement is **the L4 kinetics problem in the BO hierarchy** — which configurations are accessible at growth conditions and stable at operating conditions | `11.6-deriv-csp.md:181` | mine | `oracle/state/born-oppenheimer-levels#l4-kinetics` | Assigns a whole problem class to an L-level. `born-oppenheimer-levels` survives and is the owner of what each level covers. Concise and load-bearing. |
| 189 | D.1 six doping residuals (`R_DopantSitePref`, `R_SolubilityLimit`, `R_SelfCompensation`, `R_Clustering`, `R_DopantChargeBalance`, `R_ActivationEnergy`) with cheap/faithful | `11.6-deriv-csp.md#d1-doping-residual-catalog` | mine | `oracle/laws/residual-definitions#static-thermodynamic` | Category 17 names "solubility, mass-action" generically (`3.2-residual-definitions.md:171-172`) but **none of these six residuals is individually stated in canon**, and only `mass-action-defect-complex` (row 42) and `cluster-expansion-energy` (row 84) have registry rows. `R_DopantSitePref`, `R_SelfCompensation`, `R_ActivationEnergy` are unregistered. |
| 190 | D.2 boron-doped diamond worked example: substitutional wins by ~3 eV on covalent-radius match; solubility ~1e21 cm⁻³ HPHT; **`R_ActivationEnergy` target 0.37 eV within ~30 meV**; metallic/superconducting above ~5e20 cm⁻³ | `11.6-deriv-csp.md#d2-worked-example-boron-doped-diamond-bc` | mine | `oracle/accuracy/reference-battery#diamond-validity` | **A tolerance on an MVP observable** (±30 meV on the B acceptor level) that appears in neither `9.1-accuracy-ledger` nor `9.2-reference-battery`. The degenerate-doping threshold 5e20 is also the regime `2.4-multiscale-state.md:346-352` flags for the Einstein-relation model-form error — same number, different page, no link. |
| 191 | D.2 **"n-type doping of diamond is unsolved at production scale… the residual library needs to *express the failure*, not hide it"** + `R_SolubilityLimit` for P in diamond unfavourable by ~10 eV/atom | `11.6-deriv-csp.md:204` | mine | `oracle/certification/out-of-scope#material-limits` | A design principle about refusal — residuals should make a known-impossible thing read as violated rather than silently succeed. Pairs with row 21 (no p-type Ga₂O₃) and with the cert-refusal machinery (`5.1-cert-obligations.md`). |
| 192 | E preamble: turbine operating window 500–1000 °C; native defect populations and ordering at these T are first-order property determinants | `11.6-deriv-csp.md:210` | delete | — | Canon carries the envelope (`2.4-multiscale-state.md` §4 slow tier; trap 43 at `10.4-traps.md:300-302` fixes the ceiling against graphitization). |
| 193 | E.1 eight defect-pattern residuals (`R_NativeDefectPop(T)`, `R_DefectClustering`, `R_VacancyOrdering`, `R_AntisiteOrder`, `R_StackingFault`, `R_TwinBoundary`, `R_ThermalCycleStability`, `R_NV-center-conc`) | `11.6-deriv-csp.md#e1-defect-residual-catalog` | mine | `oracle/laws/residual-definitions#static-thermodynamic` | `R_ThermalCycleStability` **is** cited from canon — `2.4-multiscale-state.md:234` calls it "[deriv-csp] Part E.1 `R_ThermalCycleStability`'s population-drift residual" — so canon depends on a definition that lives only here. Dangling-promise, confirmed. Rows 58, 59, 84, 42 cover four of the eight; `R_VacancyOrdering`, `R_AntisiteOrder`, `R_ThermalCycleStability`, `R_NV-center-conc` are unregistered. |
| 194 | E.2 diamond defect anchors: `V` `E_f ≈ 7 eV` ⇒ 1e−25 at 1000 °C; divacancy binding ~4 eV; **NV binding ~3 eV**; Σ3 twin `γ_TB` tens of mJ/m²; **`γ_ISF ≈ 280 mJ/m²`** | `11.6-deriv-csp.md#e2-diamond-specific-defect-notes` | mine | `oracle/accuracy/accuracy-ledger#diamond-defect-anchors` | None of these five is in any reference-data CSV. The stacking-fault and twin energies explain observed CVD microstructure (dense twinning, common SFs in heteroepitaxy) and feed rows 58/59, which have no anchors. See Contradiction C18 on `E_f ≈ 7` vs 11.4's 7.2 eV. |
| 195 | E.2 **the freeze-in distinction: defect populations equilibrate to `T_op` but freeze during cooldown, so the durability residual must distinguish equilibrium from frozen-in populations** | `11.6-deriv-csp.md:233` | delete | — | Landed: `2.4-multiscale-state.md:76-79` cites "[deriv-csp] Part E.1/E.2 requires distinguishing equilibrium…" and `:143-152` makes `SiteDecoration.occupancy` the frozen-in initial condition `s(t=0)`. |

### `11.6` Part F, cross-cutting, tail

| # | Fact or block | Current location | Disposition | Target | Evidence |
|---|---|---|---|---|---|
| 196 | F.1 reuse map: which state component each new residual class touches (`R_I`, `γ̂`, `Z_I`, `Π_h`, `A`) and which BO level it uses | `11.6-deriv-csp.md#f1-reuse-map-existing--new` | mine | `oracle/state/born-oppenheimer-levels#l4-kinetics` | A **state-component ↔ residual-class map** — answers "which slot does this residual read." `2.4-multiscale-state.md` does this for the slow tier only; the CSP/heterostructure/doping classes are unmapped in canon. |
| 197 | F.2 three proposed methods (`structure-validity-method`, `interface-stacking-method`, `defect-population-method`) with their landings | `11.6-deriv-csp.md#f2-new-methods--templates--formulas-needed` | delete | — | Proposal record; the page states each landing inline (row 51, etc.). The closed method alphabet declined all three as *methods* (`6.1-canonical-vocabularies.md §1`). |
| 198 | F.2 four proposed templates + landings (`harmonic-stiffness-hessian-template`; `bi-slab-grand-potential` row 86; `mass-action-equilibrium`; `cluster-expansion` row 84) | `11.6-deriv-csp.md:257-261` | delete (except → row 179) | — | Three landed as registry rows. `harmonic-stiffness-hessian-template` did **not** land — its content is row 179, carried there. |
| 199 | **F.2 the "Proposed as → Landed as" table** (13 rows) | `11.6-deriv-csp.md:263-284` | delete | — | Pure rename history (D1) and structurally redundant: `retired-names.csv` carries the same mapping and is itself deleted by §9. Verified all 13 landings against `registry-manifest.csv` — every one resolves. |
| 200 | F.2 `lattice-strain-energy` — **"not registered; the strain path runs through `elastic-constants-Cij` (row 60) and `critical-thickness-force-balance` (row 54)"** | `11.6-deriv-csp.md:273` | delete | — | The routing statement duplicates `retired-names.csv:38` (`lattice-strain-energy,(never registered — see deriv-csp F.2),2026-07-21`) — which **points back at this page**, so the CSV's explanation dies with it. Both are deleted by §9; the surviving fact is simply that rows 54 and 60 exist. |
| 201 | F.2 "projected 24 → ~39 named formulas. The registry stands at 132 substantive rows today" | `11.6-deriv-csp.md:283-284` | delete | — | Self-marked superseded snapshot. Correctly flagged here (unlike `11.5` row 147). |
| 202 | F.3 four proposed observable bundles (`structural-validity`, `band-alignment`, `defect-population-spectrum`, `interface-stack`) | `11.6-deriv-csp.md#f3-new-observable-bundles` | delete | — | Landed as B10 (`6.7-observable-bundles.md:39`, "B10 static-validity — bond-valence sum, `radius-ratio-coordination-class`"), B6, B4, B11. Row 85 carries `B10`. |
| 203 | F.4 two proposed residual categories (`structural-validity`, `thermodynamic-consistency`); "brings the category count from 5 → 7" | `11.6-deriv-csp.md#f4-new-residual-category` | delete | — | Landed as categories 16 `Static/Snapshot` and 17 `Static/Thermodynamic`. **The "5 → 7" count is superseded and not flagged**: the taxonomy is nineteen (`3.2-residual-definitions.md:59-62`). Contradiction C19. The *distinction* it draws (static constraints on structure vs constraints on trajectories) is exactly how canon defines the split, so the reasoning landed even though the count did not. |
| 204 | F.5 two-tier path summary: cheap = T0/T1, **~1–10 ms per candidate, ~50 scalar residuals, used every PINO training step**; faithful = T3/T4 selectively on uncertainty-weighted samples, feeding back a learned multiplier | `11.6-deriv-csp.md#f5-two-tier-path-summary` | mine | `operator/training/curriculum#tier-ladder` | **A training-loop budget**: what runs every step, what runs selectively, and how the faithful path corrects the cheap one. Merge with rows 34, 81, 156, 179 — the five together are a coherent training-staging statement that no page owns. |
| 205 | F.6 end-to-end diamond/W integration sketch (6 steps, ~50 ms cheap-path total) | `11.6-deriv-csp.md#f6-concrete-integration-sketch-diamond-w-contact-end-to-end` | mine | `oracle/accuracy/reference-battery#diamond-validity` | The **only worked end-to-end oracle invocation in the corpus** — from structure through validity, interface, band alignment, thermodynamics, defects, durability, with a wall-clock budget. Directly serves the fresh-agent acceptance gate (§11.6 of the plan). |
| 206 | F.6 step 3 χ correction (the bare 0.7 was a (110)-clean value on a (100) interface; H-term (100) gives 5.85 eV) | `11.6-deriv-csp.md:318` | delete | — | History of a fix (D1). The live rule is trap 18. **Log-worthy L10.** |
| 207 | F.6 "the PINO can then **invert** the problem: given target (Φ_B, durability), generate candidate (composition, orientation, termination) minimizing residual sum + matching targets" | `11.6-deriv-csp.md:323` | mine | `oracle/seams/residual-machinery#reuse` | An inverse-design claim about what the residual library enables. Pairs with row 172. |
| 208 | Cross-cutting 1: symmetry as first-class — every residual touching `R_I` must respect the space-group action or it leaks gauge dependence into the loss | `11.6-deriv-csp.md:329` | mine | `oracle/laws/residual-definitions#symmetries` | Category 14 `Algebraic/Symmetries` names "space-group equivariance of response tensors" (`3.2-residual-definitions.md:144-145`); this states it as a **constraint on residual construction**, which is a different and stronger claim. |
| 209 | Cross-cutting 2: discrete-continuous boundary — Gumbel-softmax over space-group probabilities with the residual as expectation | `11.6-deriv-csp.md:331` | mine | `data/registry-manifest.csv` (Source cells, `D4` rows) | A named relaxation technique. Row 84's Source already names "Gumbel-Softmax" for occupations; the **space-group** application is unstated. `D4` rows must name their relaxation (`6.6-named-formulas.md:105-109`). |
| 210 | Cross-cutting 3: reference-phase database as a dependency; **~50 phases needed for diamond-centric scope** (C, W, Mo, Pt, Au, Ti, Ni, Al, Ta + carbides + oxides/nitrides) | `11.6-deriv-csp.md:333` | mine | `oracle/accuracy/reference-battery#reference-phases` | Registry row 87 `reference-phase-energy-cache` `(phase-id) → E_ref`, `T3/faithful/DFT battery` — exists, but **the phase list it must contain exists only here**. The row is unusable without it. Dangling-promise, high confidence. |
| 211 | Cross-cutting 4: terminations explode combinatorially (10²–10³ for diamond(111)/W(110)); the PINO amortizes by ranking cheaply, faithful DFT on top-K | `11.6-deriv-csp.md:335` | mine | `operator/training/curriculum#label-generation` | The combinatorial argument for why an amortized surrogate is needed at all in the interface channel. Quantified. |
| 212 | Cross-cutting 5: T2 (MLIP) as the active-learning bridge between cheap and faithful; MACE-MP-0-class foundation potentials cover the chemistry in scope | `11.6-deriv-csp.md:337` | mine | `operator/training/curriculum#tier-ladder` | Names a **third tier** and an external tool class. Interacts with the plan's confirmed homeless fact (training staging) and with `n-op-training-staging` — the memory records VASP epochs narrowing first and oracle residuals guiding only the last epoch; the MLIP bridge is a distinct middle step and is unrecorded. Merge with row 204. |
| 213 | Cross-cutting 6: **four named non-coverage areas** (quantum-tunnelling residuals at M-S barriers; RF/high-frequency response; radiation-damage cascade; CVD surface chemistry with μ_H/μ_CH₄) | `11.6-deriv-csp.md:339-343` | mine | `oracle/certification/out-of-scope#deferred-mechanisms` | A deliberate coverage-gap statement. Two of the four have partial homes elsewhere (tunnelling → 11.5 F, rows 129–131; radiation → 11.5 H, rows 136–139) but **RF response and CVD surface chemistry are named nowhere else in the corpus.** |
| 214 | Summary recommendation (3 methods / 4 templates / 15 formulas / 4 bundles / 2 categories + `reference-phase-energy-cache`); "**all net additions preserve the GENERIC `dx/dt` structure — validity/thermo residuals are static constraints on `x`, not modifications to `L` or `M`**" | `11.6-deriv-csp.md:347` | mine | `oracle/laws/residual-definitions#static-thermodynamic` | The counts are superseded, but the **structural claim** is not: static-validity residuals do not touch the GENERIC operators. That is the justification for categories 16/17 being "constraint violations by input-domain type" (`3.2-residual-definitions.md:154-158`) rather than EOM categories, and canon states the taxonomy without stating this reason. |
| 215 | `## Changelog` (3 entries: 2026-07-16 ×2, 2026-07-07 B8) | `11.6-deriv-csp.md#changelog` | delete | — | D1. Advancements extracted as **Log-worthy L11**. |

---

---

### from `appendix-c`

### `11.7-deriv-observable-catalog` — 7,537 words

| # | Fact or block | Current location | Disposition | Target | Evidence |
|---|---|---|---|---|---|
| 1 | Frontmatter: `authority: supporting`, `content-hash: ac38543a00a9`, `canonical-for: [deriv-observable-catalog]` | `11.7:1-18` | delete | — | D1/D6 kill `authority` + `content-hash`; `canonical-for` names only the page's own id — one of the 18 vacuous claims (plan §2 defect 4). Owns nothing. |
| 2 | Conventions banner: Part C's 8 groups are the "pre-canon **data-shape** view"; canonical grouping is the 11 physics-domain bundles | `11.7:21-24` | delete | — | Duplicate. `canonical-vocabularies §4` + `observable-bundles` own the 11 bundles; the data-shape caveat is already stated at `6.7-observable-bundles.md:49` and `6.1-canonical-vocabularies.md:170`. |
| 3 | Part A — 24 materials × "physically distinctive (what makes the surrogate hard)" | `11.7#part-a` | mine | `oracle/certification/applicability-classifiers#material-scope` | Sole statement of *why each material is hard for a surrogate*. Not in `crystal-inputs`, not in `out-of-scope`. This is the material-coverage claim the applicability predicates are written against. |
| 4 | Part A — per-material high-T failure modes (≥500 °C): diamond air-oxidation 600–700 °C as the lifetime limiter vs graphitization >1500 °C; H-desorption 700–900 °C; Al melts 660 °C; Al₂O₃ crystallizes >700 °C; Au needs a barrier >400 °C | `11.7#part-a` (col. 5) | mine | `oracle/certification/out-of-scope#operating-ceilings` | Split from row 3: these are operating-envelope bounds, not scope. The diamond one is already `[traps] §43`; the other ~20 are stated nowhere else. |
| 5 | Part A — per-material role in the diamond-anchored chip (channel / spreader / buffer / ohmic / barrier / substrate) | `11.7#part-a` (col. 4) | mine | `program/purpose/purpose-and-scope#material-scope` | Split from row 3. States what the corpus is *for*, materially. Currently homeless. |
| 6 | **Part B — the 16 figures of merit: definition formula + required-observable list for each** | `11.7#part-b` | **mine** | `oracle/accuracy/accuracy-ledger#figures-of-merit` | **Highest-value block in this page.** `accuracy-ledger:110` asserts "the **16 FoMs** … are algebraic compositions of the above" and names only three; it gives no formula and no observable list. `computational-overview:605` and `audit-prompt:120` cite "(+16 FoM)" as a known formalization gap. **Part B is the only enumeration in the corpus** — and it has 15 rows, not 16 (Contradiction C-1). Grep for `BFOM\|JFOM\|KFOM\|Baliga\|Keyes` outside ch. 11 returns only prose mentions, no definitions. |
| 7 | Part C preamble — the 8 data-shape codes (`BZ EN RS AT TN TR RC SC`) | `11.7:85` | delete | — | Self-labelled "pre-canon"; superseded by the 11 bundles. The *existence* of a data-shape axis is already noted at `observable-bundles:49`. |
| 8 | Part C preamble — the closed method/template/named-formula vocabulary as of first draft (12 methods × 12 templates × 25 formulas) | `11.7:87` | delete | — | Superseded counts. `canonical-vocabularies §3` is canonical and current (12 methods + 3 sub-methods, 20 templates, 132 formulas). Note the page's own body admits "the braced list below has 25 members; '24' was the figure this preamble carried". |
| 9 | **Part C — the 52 catalog observables: per-observable `governing equation`** | `11.7#part-c` groups 1–8 | **mine** | **`oracle/registry/observable-catalog#governing-equations` (NEW PAGE — see Notes)** | Sole home. `physics/research/` holds only the two diamond VASP datasets — **no catalogs** — so the spec brief's claim that "the math today lives in `physics/research/`" is false. The ledger carries tolerance only. 52 equations die with this page. |
| 10 | **Part C — per-observable `faithful residual` expression** (`r_gap = …`, `r_ph = ‖D(q)e − ω²e‖²`, …) | `11.7#part-c` | **mine** | **`oracle/registry/observable-catalog#faithful-residuals`** | Split from row 9 — different target likely. These are the explicit residual forms the generator factory instantiates. `residual-definitions` owns the 19 *categories*; it does not carry 52 expressions. Sole statement. |
| 11 | **Part C — per-observable `cheap-path` (a)/(b) alternatives** (~104 named approximations: TB-3NN-sp³d⁵, Wannier interpolation, Slack, Cahill, Chynoweth, Okuto-Crowell, Anderson, Tersoff midgap, Padovani-Stratton, Griffith, Debye-Grüneisen …) | `11.7#part-c` | **mine** | **`oracle/registry/observable-catalog#cheap-paths`** | Sole statement of the *two-option* cheap path per observable. The ledger's "Cheap vs faithful" column compresses this to one clause and drops option (b) entirely. |
| 12 | Part C — per-observable `native composition` (method ∘ template, e.g. "hybrid-DFT ∘ eigenproblem on KS-Hamiltonian; or GW ∘ Dyson") | `11.7#part-c` | mine | `oracle/registry/typed-compositions#per-observable-compositions` | 52 worked instances of the composition grammar `typed-compositions` defines abstractly. Not duplicated there. |
| 13 | Part C — per-observable `accuracy regime` | `11.7#part-c` | delete | — | **Superseded by `9.1-accuracy-ledger` rows 1–52**, which is canonical and explicitly says it *restored* this column from here (`9.1:36`). Two rows already diverge (see C-2). Do not carry. |
| 14 | Part C — per-observable `high-T notes` | `11.7#part-c` | mine | `oracle/accuracy/accuracy-ledger` (merge into existing rows 1–52) | Partly present in the ledger, partly not. Same observable numbering (#1–52) makes the merge mechanical. Unsure — flagged as merge-not-copy. |
| 15 | Part C — per-observable `couples to` lists | `11.7#part-c` | delete | — | Duplicate of Part D's coupling matrix at finer granularity; Part D is the machine-readable form. Keep one. |
| 16 | Part C — the two `(material, T)`-style **signatures** on each observable | `11.7#part-c` col. 2 | mine | `oracle/registry/observable-catalog#signatures` | The observable's typed arity. The registry's `Signature` column is keyed to the 132 *formulas*, not the 52 observables — different axis (see Notes). |
| 17 | **Part D — the 22 × 21 coupling matrix (row observable needed to compute column observable)** | `11.7#part-d` | **mine** | `oracle/laws/coupling-structure#observable-coupling-matrix` | Sole machine-readable statement of the observable dependency graph. `coupling-structure` owns coupling but carries no such matrix. |
| 18 | Part D — centrality ranking: the 7 most-depended-upon observables (gap, bands, ω_λ(q), E_f, E_F, **T_op as meta-observable**, φ_B) | `11.7#part-d` | mine | `oracle/laws/coupling-structure#centrality` | Present-tense design-priority fact ("these seven get the most careful faithful-residual formulations"). T_op's meta-observable status is load-bearing for the layer ordering (`11.8` §2.2). |
| 19 | Part E — 12 named accuracy-failure modes of standard DFT/BTE, with magnitude + mitigation | `11.7#part-e` | mine | `oracle/accuracy/accuracy-ledger#failure-modes` | Present-tense method limits (KS gap underestimation 30–100%; charged-defect alignment 0.3–1 eV; BTE-RTA hot-tail; MIGS-vs-Schottky-Mott 0.3–1 eV; 3-ph vs 4-ph 10–20%; SOC 10–30 meV). Not duplicated in the ledger, which is per-observable, not per-method. |
| 20 | Part E — the QHA-breakdown row's per-material framing (diamond 0.49 Θ_D holds, GaN 1.29 Θ_D fails, AlN 0.77 Θ_D fails — "no single fraction fits") | `11.7#part-e` | delete | — | Already canonical at `[traps] §40` and `accuracy-ledger` #14, both cited by this row. The row's own "the Θ_D/2 justification this row used to give is the one that register explicitly retired" is scaffolding. |
| 21 | Part F — 12 honest limits of scope (strong correlation, flexoelectricity, magneto-thermal, non-Markovian deep defects, polaron localization, excitonic transport, radiation cascades, plasma damage, GB statistics, creep, tunneling-corrected rates, plasmon-phonon) | `11.7#part-f` | mine | `oracle/certification/out-of-scope#declared-limits` | Present-tense scope declaration. `out-of-scope` carries strong correlation (`5.3:26`); the other 11 need checking against it — several look absent. |
| 22 | "Summary of registry gaps flagged for architecture amendment" — 10 bullets, each with its landing disposition | `11.7#summary-of-registry-gaps` | delete | — | Scaffolding: self-described as "Kept as the **scoping record**". Every bullet has landed (verified: rows 40–41, 105–110, 113–119, 121, 13, 44/46, 70–71, 76–80, 115, 116, 128). |
| 23 | …except the three items still **genuinely outside** the registry: `#18`/`#49` full-band MC and the hot-carrier EDF tail; `#41` device-scale resonant modes; full radiation-cascade dynamics | `11.7:279` | mine | `oracle/certification/out-of-scope#registry-gaps` | Exception 1 of the brief: a closed item's *resolution* is present-tense. These three are **not** closed — they are live gaps. `accuracy-ledger` #49 carries the EDF-tail refusal; `#41` and cascade dynamics need a home. |
| 24 | Closing line: "52 catalog observables across the 8 data-shape groups" | `11.7:283` | delete | — | Count preserved in `glossary.md:76` ("Catalog observables") and `accuracy-ledger:49-53`, both of which survive. |
| 25 | `## Changelog` — 5 dated entries (2026-07-21 ×2, 2026-07-16 ×2, 2026-07-07) incl. superseded κ values ("±10%/±20%", "2000 → ~600 W/mK at 800 K") and the row-20 breakdown sign error | `11.7:287-326` | delete | → log | D1/D2. Two entries are log-worthy advancements (L-3, L-4 below); the superseded values are exactly the trap the brief warns about and must not travel. |

### `11.8-deriv-generator-catalog` — 6,824 words

| # | Fact or block | Current location | Disposition | Target | Evidence |
|---|---|---|---|---|---|
| 26 | Frontmatter: `authority: supporting`, `content-hash: d633cb54697d`, vacuous `canonical-for: [deriv-generator-catalog]` | `11.8:1-17` | delete | — | As row 1. Owns nothing. |
| 27 | Scope banner — "Historical S1–S5 reconciliation **snapshot** (87 entries)", the retired-legend warning, the `retired-names.csv` pointer | `11.8:20-30` | delete | — | Pure scaffolding, and **partly false** (R-1: rows 1–87 do align). Its live content — that `D3`/`D0` meant the opposite — is already `[traps] §53`, which survives. |
| 28 | §Preface — "Diamond is the anchor substrate. Where streams disagree, this document binds the resolution." | `11.8:34-36` | delete | — | Stream-arbitration framing; the streams (S1–S5) do not survive D3. Diamond-as-anchor is canonical in `mvp-scope`. |
| 29 | §1.1 Bundles B1–B11 with contents | `11.8:44-55` | delete | — | Duplicate. `canonical-vocabularies §4` (`6.1:151-165`) + `observable-bundles` own them. **Note B10 is named `structural-validity` here, `static-validity` in canon** — see C-3. |
| 30 | §1.1 Cost tiers T0–T3 **as scheduling cadence** (per-step / per-batch / per-epoch / on-demand) | `11.8:58-61` | mine | `operator/training#residual-cadence` | Not a duplicate: `named-formulas:59-61` defines T0–T3 by **wall-clock cost** (≤10 µs / ≤10 ms / ≤10 s / ≤10 min). The cadence reading is a distinct, operator-side fact and is stated nowhere else. See C-2. |
| 31 | §1.1 the retired S5 five-tier differentiability legend + its translation table | `11.8:63-74` | delete | — | Retired vocabulary, **and wrong as a decoder** (R-2: mispredicts 9 of the 18 changed rows). `named-formulas` §"Differentiability tags" is canonical; `[traps] §53` carries the hazard. Carrying this forward is strictly worse than dropping it. |
| 32 | §1.1 `Path`: `cheap` = per-step loss, `faithful` = compared against battery | `11.8:76` | delete | — | Superseded and **contradicted**: `formula-registry:94` states `Path` is an **anchor class, not a runtime path selector** ("every formula is on the single residual surface under the always-cheap pipeline"). The appendix's reading is the pre-reframe one. |
| 33 | §1.2 the 89-row catalog table — `#`, `Name`, `Bundle`, `Tier`, `Diff`, `Path`, `Source`, `Depends on` | `11.8:80-170` | delete | — | **Superseded in full by `physics/library/formulas/registry-manifest.csv`**, which carries the identical column schema plus 45 more rows and current tags. Verified mechanically: 87/87 name match, 19 cells of stale `Diff`/`Tier`, and the `#88`/`#89` collision (R-1, R-2). The `Signature` column is the one thing the registry also has. Nothing unique survives. |
| 34 | §1.2 the `Source` column's S1–S5 stream provenance | `11.8:80-170` | delete | — | Stream bookkeeping; D3 dissolves the two-tier/stream system. The registry's own `Source` column carries current provenance (literature DOIs / research pointers). |
| 35 | §1.2 footer — "87 + 2 = 89 rows; ~13% dedup compression from ~100 raw proposals"; the 5 named unifications | `11.8:172` | delete | → log | Superseded count (132 substantive now). The dedup *decision* is log-worthy (L-6); the arithmetic is not. |
| 36 | §2.1 Centrality hubs — the seven hubs and the T_op ASCII fan-out | `11.8:178-194` | delete | — | Duplicate of `11.7` Part D centrality ranking (row 18), which is the better-sourced statement (derived from the full coupling matrix). Keep one. |
| 37 | **§2.2 The layered compute DAG — Layers 0–6, with per-layer membership** | `11.8:196-246` | **mine** | `oracle/seams/residual-machinery#layer-dag` | **The single most load-bearing block in my scope.** `7.2-residual-machinery.md:67-68` declares the canon field `layer : 0..6` and defines it *only* by citing "the 7-layer compute DAG of **residual-generator-catalog §2**" — **an id that does not exist** (the page is `deriv-generator-catalog`). Dangling promise + dead id in one. If ch. 11 dies unmined, a canon field loses its definition entirely. |
| 38 | §2.2 the ordering constraint — **"T_op must be predicted/converged before Layer-3 residuals fire"**; RAD sampling and GradNorm must respect it | `11.8:216-218` | mine | `operator/training#layer-ordering` | Split from row 37: this is a *training-loop* obligation, not a graph fact. `11.8` §7.4 restates it as a hard requirement and §6.3 shows the failure it prevents (loss computes c_def at RT while predicting 500 °C performance). Stated nowhere in canon. |
| 39 | §2.3 Cycle resolution — Layer 3 ↔ Layer 5 (T_op → σ,κ → T_op) closed by fixed-point iteration **in the same forward pass, no nested optimization**; E_F ↔ c_def is a genuine nested fixed point needing an implicit-layer/DEQ node | `11.8:248-252` | mine | `oracle/laws/coupling-structure#self-consistency-cycles` | Present-tense structural fact. The registry confirms the consequence independently: rows 5, 36 and 13 are all `D3` (implicit-function adjoint) — which is *why* the legend mistranslation at R-2 matters. |
| 40 | §3.1 `ResidualGenerator` record definition | `11.8:281-299` | delete | — | **Superseded by `residual-machinery §1`** (`7.2:51-96`), which is richer and current: adds `applicability`, `category`, `axes`, `dressing-tag`, `characteristic-scale`, `registration-hash`; fixes `bundle` to a set; uses the canonical `D0\|DN\|D1\|D2\|D3\|D4`. The appendix's version predates the `applicability` slot (its own banner says so). |
| 41 | §3.1 the three legend-conditioned slots: `backward?` iff `D2`, `relaxation?` iff `D4`, **`fd-step?` iff `D3`** | `11.8:293-295` | delete | — | **Actively wrong under canon and must not be carried.** Canon `D3` = converged fixed point needing an implicit-function adjoint (`named-formulas`); an `fd-step` slot on a `D3` row is the exact error `[traps] §53` names. `residual-machinery` correctly has no such slot. |
| 42 | §3.1 `InputContract` / `OutputContract` records — `state-slots`, **`env-slots : subset of Environment record`**, `cache-handles`, `coverage-mask`; shape/units/expected-range/symmetry | `11.8:267-279` | mine | `oracle/registry/named-formulas#contracts` | `residual-machinery` has `input-contract : {TypedSlot}` / `output-contract : TypedSlot` with no expansion. This is the only place the contracts are unpacked — and the only place in the corpus that gestures at `Environment`'s contents. See `environment-schema`. |
| 43 | §3.2 `make-residual-generator` factory signature | `11.8:304-311` | delete | — | Superseded by `residual-machinery §3` (`7.2:117-126`), whose signature is current (`observable, formula, axes, sampling-policy, applicability`). |
| 44 | §3.2 factory *behaviour* — the 8-step instantiation procedure, incl. **"error at registration, not at training time"** for a missing `D2` adjoint | `11.8:313-322` | mine | `oracle/seams/residual-machinery#factory-behaviour` | The 8 steps are stated nowhere in canon; `residual-machinery §3` gives only the entry point. Step 4's registration-time-gate rule is restated as a decision at §4.5 and is live (`adjoint-cert` field exists). Steps keyed to the retired legend (`D3`→fd-step) must be dropped on the way — see row 41. |
| 45 | §3.3 `PinoTrainStep` record + the `step()` procedure (layer-grouped ordering, coverage-mask skip, σ-scaling, `maybe-update-state` for self-consistent nodes) | `11.8:326-354` | mine | `operator/training#train-step` | Operator-side; `/physics` canon deliberately stops at `weight-policy : ConsumedBy(/informed-operator)` (`7.2:89-90`). This is the only written form of the consuming loop. |
| 46 | §3.3 `curriculum-phase : Warmup \| Refine \| Calibrate \| Polish` | `11.8:332` | mine | `operator/training#curriculum` | The four-phase curriculum vocabulary. Referenced by §5 dispositions (Gumbel-Softmax τ annealed "to 0.1 by Polish") and §6.4. Homeless in canon. |
| 47 | §3.4 tier → sampling-policy map (T0→UniformBatch, T1→RAD-Adaptive, T2→PerEpoch, T3→OnDemand) and when each is evaluated | `11.8:358-363` | mine | `operator/training#sampling-policy` | `residual-machinery` declares the *enum* (`UniformBatch \| RAD(τ) \| Importance \| ValidationOnly`) but not which tier gets which. §7.12 restates it as a rule. Note the enums differ — see Notes. |
| 48 | §3.4 diff-tag → backward-path table | `11.8:365-371` | delete | — | Retired legend (the table's own header says so). `named-formulas` owns the current per-tag adjoint story. |
| 49 | §3.5 Multi-source loss — 4 source families (Model-internal/cheap, DFT-Battery, MLIP, Experiment), GradNorm outer rebalance every K steps, NTK-init inner weights frozen after warmup, Huber for experimental with per-observable σ | `11.8:373-383` | mine | `operator/loss#multi-source-balancing` | The loss-design fact. `accuracy-ledger:291` and `residual-definitions §7` own `characteristic-scale : σ` as an error-model input and explicitly say it is "**never a fitted weight**" — so the weighting scheme itself has no `/physics` owner and belongs operator-side. |
| 50 | §3.6 the three `/physics` exports realized via the factory — `Generate`, `Validate`, `Import` | `11.8:385-390` | mine | `oracle/seams/pino-bridge#exports` | `pino-bridge §2` owns `Import` (ground-truth-bridge). `Generate`/`Validate` as named exports need confirming against `pino-bridge`; the *triple* is stated only here. Unsure — flagged. |
| 51 | §4.1 Cost-tier reconciliation: keep T0–T3, reject S2's T0–T4; MLIP-ness belongs in the `source` tag, not the cost tier | `11.8:395-399` | delete | → log | Decision landed — canon is T0–T3 everywhere (`named-formulas:48`). Resolution is present-tense and already canonical; the S2-vs-rest story is scaffolding. Log as L-6. |
| 52 | §4.2 `coupled-em-thermal-pde-residual` is a sub-residual under `eom-violation`, not a new top-level category; PDE-ness is captured by `T1` + `D2` | `11.8:401-405` | mine | `oracle/laws/residual-definitions#category-assignment` | Present-tense taxonomy rule with a live rationale ("PDE-vs-algebraic crosscuts every bundle and breaks the bundle-as-cohesive-domain abstraction"). `residual-definitions` owns the 19 categories; this assignment rule is not stated there. |
| 53 | §4.3 `ReferenceCache` unification — one type, one lifecycle, one invalidation policy, two namespaced sub-caches (`ref-cache.phases`, `ref-cache.defects`) | `11.8:407-413` | mine | `oracle/accuracy/reference-battery#reference-cache` | Present-tense design decision. Registry rows 66 and 87 both exist and both reference it. No canon page defines the cache type. |
| 54 | §4.4 Bundle count = 11, with the S1–S4 dedup map (band-alignment→B6, defect-population-spectrum→B4, field-resolved+hot-carrier→B9, new B11) | `11.8:415-429` | delete | → log | Resolution is canonical (`canonical-vocabularies §4`). The dedup *map* is stream history. The rationale sentence — "bundles are physical-domain cohesion units for the unified state vector, not residual-category bins" — is worth checking against `observable-bundles`; if absent there, promote it (see Notes). |
| 55 | §4.5 The `AdjointFn` slot **must** be filled at registration for `D2`; the factory errors at registration, not training | `11.8:431-433` | mine | `oracle/seams/residual-machinery#registration-gate` | Live guard. `residual-machinery` has `adjoint-cert : Passed\|Failed(witness)\|NotApplicable\|Relaxed(rationale)` but does not state the fail-at-registration rule. Overlaps row 44 step 4 — merge on landing. |
| 56 | §5 Honest-gaps table — 10 hard residuals with a recommended disposition each (surrogate-net for #40/#41; faithful-only on-demand for #80 with #77/#79 as cheap proxies; periodic-refresh for #13; Gumbel-Softmax for #84; log-sum-exp soft-min for #67; SOAP-descriptor surrogate for #85) | `11.8:437-452` | mine | `oracle/certification/out-of-scope#hard-residual-dispositions` | Present-tense engineering dispositions with rationale. **Re-key required**: the "Current tag" column is in the retired legend (e.g. `#45` "T1/D3" is now `D4`; `#67` "T1/D3" is now `D4`; `#85` "T1/D3" is now `D4`). Carry the disposition prose, drop the tag column. |
| 57 | §5 the 4-phonon row's "(superseded)" entry recording that rows 121–122 landed | `11.8:445` | delete | → log | Closed-item story. The resolution (`kappa-4phonon-high-t-correction` row 121, `iterative-lbte-kappa` row 122, valid `T ≳ 0.4·Θ_D`) is already canonical in `accuracy-ledger` #12/#13 and the registry. |
| 58 | §5 Strategic gap — surrogate-net-bridge must be a **first-class architecture component**, complementing (not replacing) the MLIP bridge; required for #6/#13/#40/#41 | `11.8:454` | mine | `program/build/mvp-system#surrogate-bridge` | Live architectural requirement. Note the row's own parenthetical retracts the "~5% of the catalog" figure as computed off the wrong set — carry the requirement, not the number. |
| 59 | §6 Worked example — Diamond–W Schottky contact at 500 °C: scenario setup, 37-step firing sequence across Layers 0–6, cost accounting (24×T0, 9×T1, 2×T2, 0×T3) | `11.8:458-538` | mine | `program/build/build-verification#integration-test` | §7.13 proposes exactly this as "the canonical integration test for S7's amendment". `build-verification:45` already checks "all 132 formulas instantiate as `ResidualGenerator`" — this is the end-to-end complement. **Carry with the §6.2 corrections applied** (they are in-place already per the 2026-07-07 gap audit). |
| 60 | §6.2 step 9 — the MIGS-corrected barrier "**does not reproduce**": Cowley-Sze gives ~3.5–3.7 eV for every termination-tagged χ, not the ~4.5 eV stated. "Flagged unpinned; do not quote the 4.5 until the inputs are pinned" | `11.8:493` | mine | `oracle/registry/formula-registry` (row 48 `Source`) — **already there** | Not a new fact: `registry-manifest.csv` row 48's `Source` cell carries the identical UNPINNED flag naming `deriv-generator-catalog §6`. **The CSV cell will dangle at cutover** — see Notes, ordering hazard 3. Open question `migs-worked-example-inputs`. |
| 61 | §6.3 "Key physics demonstration": at 773 K the B-acceptor activation rises ~10³× vs RT; the catalog catches it because #35 fires *at* the T_op predicted by #70+#71 | `11.8:538` | mine | `program/build/build-verification#integration-test` | The assertion the integration test exists to make. Merge with row 59. |
| 62 | §6.4 Four failure modes the catalog catches (MIGS disagreement >0.3 eV; E_F non-convergence → drop sample; avalanche M>10 → out-of-envelope mask; carbide growth >100 nm → mission fail; T_op mismatch → Warmup-only) | `11.8:540-546` | mine | `oracle/certification/cert-obligations-detail#runtime-refusals` | Present-tense refusal/masking rules. `applicability-classifiers` owns coverage masks; these five concrete trips are stated only here. |
| 63 | §7 items 1–13 — summary recommendations to "S7" | `11.8:552-578` | delete | — | Restatements of §1–§6 addressed to a stream that no longer exists, plus superseded item 1 ("adopt the 87-entry catalog as the canonical registry" — it is 132). Every live element is carried by rows 37–62 above. |
| 64 | **§7 item 14 — "The `Environment` record needs a `temperature_gradient` field" — adopt directly** | `11.8:580` | mine | `oracle/state/unified-state#environment` | **Never adopted.** `grep -rn "temperature_gradient"` outside ch. 11 returns nothing. And `Environment` itself is the brief's confirmed homeless fact — used in signatures at 12+ canon sites (`named-formulas:54`, `residual-machinery:83`, `residual-definitions:235`, `applicability-classifiers:31`, `property-templates:51,79`, `product:102`, `computational-overview:52,334,473`, `cert-obligations:63`) and **defined by no page**. Open question `environment-schema`. |
| 65 | §7 item 15 — the response-interface needs a `causal? : Boolean` slot for non-causal nonlinear responses | `11.8:582` | mine | `oracle/registry/property-templates#response-interface` | Also never adopted; `grep` finds only `Algebraic/Kramers-Kronig` (`residual-definitions:134`) and causality witnesses (`typeclass-alphabet:39`), not the slot. Open question `response-causality-slot`. |
| 66 | §7 item 16 — v1's documented frontier = S1's 12 limits + §5's 4 architecture-level gaps | `11.8:584` | delete | — | Duplicate: the 12 limits are `11.7` Part F (row 21); the 4 gaps are §5 (row 56). Both dispositioned. |
| 67 | Closing flourish — "The catalog is now executable. The factory is now typed…" | `11.8:586` | delete | — | Rhetoric addressed to a retired stream. |
| 68 | `## Changelog` — 2026-07-16 strata rewrite, 2026-07-07 gap audit | `11.8:590-601` | delete | → log | D1/D2. The 2026-07-07 entry is log-worthy (L-3). |

### `11.9-deriv-language-study` — 2,314 words

| # | Fact or block | Current location | Disposition | Target | Evidence |
|---|---|---|---|---|---|
| 69 | Frontmatter + title "(`open-decisions`, **open item 5**)" | `11.9:1-19` | delete | — | As row 1 — **and the ordinal is wrong**: the language item is **6**; item 5 is the semiconductor-interface applicability predicate (`10.2:73`). See C-5. D6 kills bare `§`-ordinal citations anyway. |
| 70 | Status banner — "partially superseded"; what closed in 2026-06 was the *shape*; the picks did not close | `11.9:21-28` | delete | → log | Scaffolding. The live content is fully carried by `open-decisions` item 6 (`10.2:83-91`) and the Closed-decisions entry (`10.2:169-212`), both of which survive. Log as L-2. |
| 71 | Study provenance — 4 web-verified Round-1 axes, 1 adversarial audit, Round-2 pass; "survey current to 2024–2026" | `11.9:30-34` | delete | → log | Method provenance, not a fact. Belongs in the log entry (L-1) as attribution — this is a compliance-relevant "who/what produced it". |
| 72 | **The five durable requirements: (1) build in-house, minimize large-framework dependence — a framework that *owns* differentiation is a liability; (2) AD + implicit-diff already in hand, so built-in AD is no advantage — driving Stage-4 adjoint synthesis from our own IR is the requirement; (3) polyglot acceptable iff boundaries are clean; (4) well-known but domain-specific languages; (5) not Rust, and team familiarity is not a factor** | `11.9:36-53` | **mine** | `program/purpose/architectural-principles#implementation-requirements` | **The durable part, per `open-decisions` item 6 itself**: "the requirement each candidate was chosen to satisfy is the durable part." Stated in full nowhere else — `open-decisions` paraphrases requirements per-candidate but never lists the five constraints. Language-neutral. Reads as requirements, not as a mandate. |
| 73 | The structural insight: `/physics` is *a compiler over a content-addressed typed substrate that emits a fast runtime kernel* — two genuinely different workloads — and the Stage-4→Stage-5 boundary is a narrow, natural language seam | `11.9:55-59` | mine | `oracle/compilation/compose-time-pipeline#codegen-seam` | The argument the whole polyglot shape rests on. `open-decisions` states the *conclusion* (four roles) but not this derivation. `computational-overview:59,582` and `forced-decisions:23-28` state the seam; none states why it is a *language* seam. |
| 74 | The four-role polyglot table (Stages 1–4 compiler / Stage 2–2.5 group theory / Stage 5 runtime / proofs) with **"only two *live* languages; GAP and Lean are offline leaves on no hot path, so they add zero interop risk"** | `11.9:61-72` | delete | — | Duplicate. `open-decisions` Closed-decisions (`10.2:169-179`) carries all four roles in language-neutral form, and `forced-decisions:23-28` restates them. The offline-leaf argument is the one element not obviously duplicated — promote that clause only if absent (see Notes). |
| 75 | §Compiler-host = Haskell (ratified) — GADTs/`DataKinds` for the operad, `hegg` for Stage 3, `GHC.Generics` for the §20.4 serializer, a-la-carte typeclasses, ROBDD | `11.9:74-93` | delete | — | **Candidate advocacy, and the header word "ratified" is now false.** `open-decisions` item 6 requires every mention of a language to read as a candidate, "never a mandate". The per-candidate requirement summaries already live at `10.2:184-205`. Javier has reopened the picks. |
| 76 | §Compiler-host — the OCaml runner-up analysis and the fallback-host designation | `11.9:95-101` | delete | — | Same. `10.2:189` already records "(OCaml is the documented fallback host.)" |
| 77 | §Runtime = Julia — emitted source, JIT once per composition, zero FFI/ABI crossing per sample; rejected C-ABI/FFI and shared LLVM/MLIR | `11.9:103-113` | delete | — | Same — and duplicated at `10.2:190-194`, which correctly abstracts it to "a host with fast JIT of generated numeric source and a workable GPU story — not Julia specifically." |
| 78 | **What crosses the seam is narrow: the generated kernel (once) + a flat `Float64` state array in / residual + gradient + observable + cert arrays out. No substrate object ever serializes across** | `11.9:115-118` | **mine** | `oracle/compilation/compose-time-pipeline#codegen-seam` | Language-neutral and load-bearing — it is *the safety argument for the split*, and it pins the seam's data contract. `computational-overview:582` says the compiler "emits runtime-host source"; it does not state what crosses. |
| 79 | **§The condition that makes polyglot a net win — build a Stage-4→Stage-5 differential golden test up front (emitted kernel vs tree-walking interpreter of the same IR, random states, agreement to tolerance); emit a typed `Expr`, not raw strings; compile in a setup phase, run the hot loop in a later world age. Without it, collapse to a single host.** | `11.9:120-129` | **mine** | `oracle/certification/cert-obligations#tau-interp` | **Load-bearing for a live canon tolerance.** `cert-obligations:152` defines `τ_interp` = "Stage-4→Stage-5 differential golden test: two evaluators of the same IR must agree (`[deriv-language-study]`)" at `1e-10` relative — and cites *this page* as its source. The obligation exists in canon; its rationale and construction do not. |
| 80 | **§Per-stage hardware split — CPU-dominant compiler; GPU is a Stage-5-only optional accelerator chosen *per-composition* by Stage-4 codegen, not a global setting** (6-row table, Stages 1–5) | `11.9:131-143` | **mine** | `oracle/compilation/computational-overview#hardware-split` | Language-neutral. `10.2:206-208` compresses this to two sentences inside a *language* decision, which is the wrong owner — the split is a property of the pipeline. The per-stage table is unique. |
| 81 | §Why GPU is not central — Stage 2 yields small irrep blocks (`O_h` `d_λ ∈ {1,2,3}`; double cover under SOC adds spinor irreps to `d_λ = 4`) plus ragged sparsity = the classic GPU anti-pattern; GPU wins only at large k-mesh / `(Real/Wannier, Sparse)` defect supercells **and** when the PINO keeps batches device-resident | `11.9:145-156` | mine | `oracle/compilation/computational-overview#hardware-split` | The quantitative justification. `computational-overview:342` independently notes small blocks are "a poor fit for wide-SIMD/GPU batching unless many blocks are batched" — consistent, but far thinner. Ties to the `O(log n)` / no-solver-on-the-hot-path commitment (`representation-substrate §5`). |
| 82 | Round-1 scored matrix (6 candidates × 5 axes) and Round-2 compiler-host bake-off (3 hosts × 4 axes) | `11.9:158-180` | delete | — | Candidate advocacy with numeric scores — the most mandate-like content on the page. Javier has reopened the picks; `live/specs/2026-07-21-...brief.md` R0.1 explicitly requires re-deriving a comparison matrix and "**re-examine, rather than inherit, any prior exclusion**". Carrying scores would prejudge it. |
| 83 | §Excluded — **Python + JAX disqualified: JAX's tracing/`jit` model *owns* differentiation (`custom_vjp` can't pair forward-mode, can't take array `nondiff_argnums`, won't serialize a staged program), so with AD in hand its one advantage is moot and obstructive** | `11.9:186-190` | mine | `program/purpose/architectural-principles#implementation-requirements` | Keep as a **worked application of requirement (1)/(2)**, not as a verdict. `10.2:211-212` already carries the one-line version. This is the sharpest evidence for the AD-ownership requirement and the brief's R0.1 names it as a constraint to test against. Merge into row 72. |
| 84 | §Excluded — Rust (single-language winner, excluded by preference); Julia-as-compiler-host (2.5/5, ModelingToolkit.jl anti-pattern); custom MLIR (≈34–50+ pm, eventual optional Stage-4 GPU backend); single-host collapse fallback | `11.9:182-200` | delete | — | Candidate advocacy. The two durable clauses are already canonical: Rust-excluded-by-preference and the Julia-only/Haskell-only fallback at `10.2:209-211`. The MLIR-as-later-optional-GPU-backend note is thin and language-specific. |
| 85 | **§Honest liabilities — the codegen seam is the riskiest point: it is exactly where the compiler host's type system stops, so a bad template = silently wrong physics (adjoint signs, index conventions)** | `11.9:202-206` | **mine** | `practice/traps` (new entry) | **A live hazard, not a remnant** — brief exception 2. Language-neutral: it holds for any emit-source split. `[traps]` has no entry for it. Its mitigation is row 79's golden test, giving the trap a named enforcement. |
| 86 | §Liabilities — two-language build tax; Haskell iteration velocity at the `DataKinds` frontier; **the operad's compile-time soundness does not survive a host swap (drops to smart-constructor checks)** | `11.9:208-217` | delete | — | Candidate-specific. The third clause is a real structural observation but is stated only about Haskell→OCaml/Julia and would read as advocacy for a reopened pick. Unsure — flagged in Notes. |
| 87 | §Build-cost — live system ≈26–34 person-months; most likely blow-up is Stage-3 sparsity inference + the `SymbolicTensorOps` operad typechecker, not the commodity wiring | `11.9:219-225` | mine | `program/build/build-order#cost-risk` | The *risk localisation* is durable and language-neutral: the open-ended pieces are Stage-3 sparsity inference and the operad typechecker. The person-month figure is conditioned on the retired picks — carry the risk, drop the number. |
| 88 | §Sources — "Per-axis dossiers under `/tmp/impl-lang-research/` (`a1`–`a4`, `b1`–`b2`)" plus ~20 external references | `11.9:227-237` | delete | — | **The internal dossiers are gone** — `/tmp` paths from a 2026-06 session; unrecoverable and uncitable. The external list is candidate-specific and superseded by R0.1's mandate to re-verify. Note the dead-provenance fact in the log entry (L-1). |
| 89 | `## Changelog` — 2026-07-16, 2026-07-21 (partial reopen), 2026-06 (decision closed, "commit ec52314-era") | `11.9:241-251` | delete | → log | D1/D2. Both the 2026-06 closure and the 2026-07-21 partial reopen are log-worthy (L-1, L-2). |

---

---

### from `strata`

| # | Fact or block | Current location | Disposition | Target | Evidence |
|---|---|---|---|---|---|
| 1 | Provenance blockquote — "Not part of the lint-enforced atomic tree; the atomic tree (`the canon chapters*`, `the canon chapters*`) is canonical…" | `informed-operator/design/learnable-structure-requirements.md:3-8` | delete | — | **Damage reconstructed.** At `b88da0a` it read ``the atomic tree (`docs/architecture/*`, `docs/implementation/*`)``. Commit `0804f2b` ("Repair the governance layer the collapse invalidated") find/replaced **both distinct globs with one literal**. The two names were the pre-book `docs/architecture/*` and `docs/implementation/*` trees — the source of the `arch-NN`/`impl-NN` serials now in `retired-ids.csv`. Delete: it is a governance claim about a tree that no longer exists |
| 2 | Scope sentence — "the **minimum observable behavior `/physics` requires of `Learnable_Structure` at their shared boundary** — and nothing about its interior" | same, `:6-8` | mine | `operator/seam#scope` | The page's own charter; survives the blockquote it sits in |
| 3 | What `Learnable_Structure` and `/physics` each are; the pino-bridge is the only surface | `…learnable-structure-requirements.md:10-18` | delete | — | Restates `library-landscape`, `pino-bridge`, `purpose-and-scope` verbatim in substance. Duplication, not content |
| 4 | **Two-loop symmetry** — training pushes cotangents into weights, design into the candidate; same seam, only the gradient sink differs | same, `:20-23` | mine | `operator/seam#loop-agnostic` | Sole statement. Makes the contract loop-agnostic, which is what lets `/interface` own the loop (R8) |
| 5 | **R1 — state emission in the oracle's type**; structural well-formedness mandatory, physical admissibility scored not presupposed | same, `:26-38` | mine | `operator/seam#r1` | Carries the **dangling promise**: cites `unified-state` for "per-slot array shapes and layouts, units, and gauge conventions" — that table was never written. Confirmed independently by salvage README finding 2 and by request unit I1 |
| 6 | **R2 — evaluate-at-points**; caller-side interpolation does not satisfy it; "the single most load-bearing requirement" | same, `:40-48` | mine | `operator/seam#r2` | Sole statement. Cited by `prep.md` slide 3 and request unit I2 |
| 7 | **R3 — VJP at the state boundary**; oracle returns per-key cotangents, external loop combines to one state-shaped cotangent; per-key VJP not required | same, `:50-56` | mine | `operator/seam#r3` | Sole statement of the combination rule |
| 8 | **R4 — conditioning inputs**; accept `Environment` + query time as named typed channels | same, `:58-61` | mine | `operator/seam#r4` | **Homeless-fact dependency**: obliges a typed `Environment` no page owns. Also the origin of deferred unit I4 (time semantics) |
| 9 | **R5 — seam purity + determinism**; flat arrays only, bitwise determinism, float normalization | same, `:63-68` | mine | `operator/seam#r5` | Sole statement; load-bearing for content-addressed caching |
| 10 | **R6 — batch axis**, elements independent | same, `:70-72` | mine | `operator/seam#r6` | Sole statement |
| 11 | **R7 — content-addressable identity**; every instance exposes one stable content hash; retraining ⇒ new hash | same, `:74-78` | mine | `operator/seam#r7` | Sole statement. **Grep hazard — see Notes**: this is an *operator instance* hash, unrelated to the deleted page `content-hash` stamp |
| 12 | **R8 — loop-drivability**; `Learnable_Structure` never owns the loop; loop logic lives in `/interface` | same, `:80-84` | mine | `operator/seam#r8` | Sole statement; the module-boundary rule for `/interface` |
| 13 | **O1 — structure-plan intake** (Stage-2 symmetry sidecar as compile-time descriptor; equivariance residuals → 0 by construction). Offer, not ask | same, `:88-93` | mine | `operator/seam#optional-offers` | Sole statement |
| 14 | **O2 — tiered cadence**; per-slot update cadences for a future multi-rate driver | same, `:95-98` | mine | `operator/seam#optional-offers` | Sole statement; explicitly "nothing currently depends on it" |
| 15 | §3 — the six things `/physics` does not care about, + "a future need enters by amending this contract, never by reaching through the seam" | same, `:100-113` | mine | `operator/seam#out-of-scope` | Negative space is the useful half of a seam contract; no other page states it |
| 16 | Header + provenance: gating fractions owned by `arch-11-residuals §11.4.1`, settled as `0.10/0.60/0.90` in `arch-18-open-decisions` | `informed-operator/design/training-stages.md:1-8` | move | `operator/training#curriculum-pointer` | **Both ids retired 2026-07-22** (`retired-ids.csv:13,20`): → `residual-definitions` and `open-decisions`. **The section coordinate is also stale**: the fractions live at `residual-definitions §4.1` "Curriculum gating defaults" (`3.2-residual-definitions.md:245-260`), not §11.4.1. Confirmed second home at `10.2-open-decisions.md:262` |
| 17 | **The stage ordering** — 1 supervised VASP epochs · 2 informed epoch (oracle scores, returns keyed residuals + gradients) · 3 inference, operator alone | `…training-stages.md:10-20` | mine | `operator/training#stage-ordering` | **The homeless fact.** No page in `journal/pages/` states it. Only other home is `prep.md` slide 8 (row 68). Both untracked until now |
| 18 | **Why this order** — the oracle refines, it does not search; a residual is a local signal, useless over a large space; DFT searches, oracle refines | same, `:22-32` | mine | `operator/training#why-this-order` | Sole statement of the rationale. Paraphrased in `prep.md` and `talk-script.md`; this is the fullest form |
| 19 | Consequences — oracle is training-time machinery, not deployed; nothing in the inference path calls `Validate`; any diagram drawing the oracle as a permanent loop is **wrong** | same, `:34-38` | mine | `operator/training#consequences` | Sole statement. The "permanent loop is wrong" line is a live correction target, echoed in `prep.md` "Don't say" |
| 20 | The four supervisory sources are weighted across training; this file fixes where physics residuals sit in that order | same, `:39-41` | mine | `operator/training#consequences` | The only cross-link between the stage ordering and `operator/loss` Part E |
| 21 | Title + framing: literature-grounded methodology survey for the four-source regime | `informed-operator/design/residual-loss-methodology.md:1-3` | mine | `operator/loss#scope` | Establishes the file as *survey*, which is what justifies keeping Parts A–F at all |
| 22 | Part A.1–A.3 — PINN, FNO/PI-FNO, DeepONet/PI-DeepONet loss forms and documented failure modes | same, `#part-a` `:7-36` | mine | `operator/loss#survey` | Literature survey with citations; nothing in canon covers it. Design rationale |
| 23 | **Part A.4 — the MLIP lesson**: enforce architecturally where you can; the soft residual `‖F + ∇E‖²` is unused because the autodiff link makes it identically zero | same, `:38-54` | mine | `operator/loss#architectural-over-soft` | The strongest single design claim in the file; restated as recommendation 8 in the Summary and in H.7. Sole statement |
| 24 | Part A.5 — generative crystal models constrain via geometry, avoid residual loss entirely; cannot be the template because we want to *use* residuals | same, `:56-65` | mine | `operator/loss#survey` | Explains a rejected alternative — keep |
| 25 | Part A.6 — multi-fidelity: share physics residual across fidelities, specialize data loss per fidelity | same, `:67-76` | mine | `operator/loss#survey` | Directly informs the four-source setup |
| 26 | Part B — eight loss-balancing methods, empirical consensus, and the **recommendation**: GradNorm across the four source families, NTK-initialized fixed weights within the residual family | same, `#part-b` `:80-101` | mine | `operator/loss#balancing` | Sole statement; restated in H.3 |
| 27 | Part C tag table — `D0` closed-form / `D1` autodiff-native / `D2` adjoint / `D3` finite-difference fallback / `D4` non-differentiable | same, `:109-117` | delete | — | **This is the file's own superseded proposal, not the shipped vocabulary.** Canon is `D0 \| DN \| D1 \| D2 \| D3 \| D4` owned by `named-formulas`, where `D0` = pure read, `D3` = converged fixed point (no fallback tier exists), `D4` = relaxed. Keeping it is exactly the `11.8` trap one directory over |
| 28 | The "Superseded legend (updated 2026-07-21)" blockquote, incl. "An earlier version of this note was itself stale" | same, `:119-128` | delete | — | Scaffolding by definition (D1), and a stale-note-about-a-stale-note. Its *content* — the here-vs-canon mapping — is only needed while row 27 exists; delete both together |
| 29 | **Handling non-differentiable outputs** — Gumbel-Softmax / concrete distribution, straight-through estimator, and the surrogate-continuous trick (replace "is direct-gap" with `gap_indirect − gap_direct`, so the discrete fact is the sign of a differentiable scalar) | same, `:130-133` | mine | `operator/loss#relaxations` | Method knowledge, not tag semantics — survives row 27's deletion. **Must be re-keyed**: it is written against this file's `D4`, which is canon's `DN` |
| 30 | **Handling adjoint-required rows** — discrete adjoint (Giles & Pierce; Plessix) costs one adjoint solve regardless of parameter count; Pulay/DFPT for SCF; variational or RTA for BTE | same, `:135` | mine | `operator/loss#adjoints` | Method knowledge. Re-key to canon `D2`. Overlaps oracle-side R5.2 — Phase 2 should site it once |
| 31 | Architectural recommendation — every registry entry carries a differentiability tag plus an `:adjoint-implementation` slot for D2 | same, `:137` | delete | — | **Landed.** The registry has a Diff column checked by `check_data_agreement.py` (n). A shipped recommendation is not a recommendation |
| 32 | Part D — cost-tier table T0 per-step / T1 per-batch / T2 per-epoch / T3 on-demand, with cadences and examples | same, `#part-d` `:141-152` | mine | `operator/loss#sampling` | **Possible duplicate of canon's registry Tier column** — Phase 2 must diff before siting. See Contradictions |
| 33 | Part D sampling strategies (uniform, RAR, RAD, causal, curriculum) + the recommendation: T0 always-on, T1 RAD-sampled, T2 curriculum-gated off for first 30%, T3 as validation trigger only | same, `:154-168` | mine | `operator/loss#sampling` | Sole statement; restated in H.4 |
| 34 | **Part E — the four sources characterized** (cheap-compute / VASP / experiment / physics residuals) by trust, coverage, cost, noise, and what each constrains | same, `#part-e` `:174-181` | mine | `operator/loss#four-sources` | Sole statement. The table `training-stages.md` points at (row 20) |
| 35 | Part E proposed loss structure with coverage mask `M_o`, Huber on the experimental term, curriculum schedules `w_*(t)`, adaptive `λ_i(t)` | same, `:183-195` | mine | `operator/loss#loss-structure` | Sole statement of the assembled loss |
| 36 | Coverage-matrix discipline — per-sample coverage vector, loss masked per term; mandatory for the four-source regime | same, `:197-199` | mine | `operator/loss#coverage` | Sole statement. Note `open-decisions` records "coverage-mask format" as a *closed* decision — Phase 2 should check whether this duplicates it |
| 37 | Part E curriculum phase table — warm-up 0–10%, refine 10–60%, calibrate 60–90%, polish 90–100% | same, `:201-210` | mine | `operator/loss#curriculum` | **Same numbers as canon's gating fractions, different semantics** — this gates *source weights*, canon gates *residual categories*. See Contradictions |
| 38 | Residual-vs-label tension and the three fixes; recommendation = Huber with per-observable σ | same, `:212-220` | mine | `operator/loss#tension` | Sole statement |
| 39 | Part F — six documented failure modes with fixes; the theory limits (convergence proven only for linear elliptic + NTK regime; no general guarantee); "PINO convergence is empirical" | same, `#part-f` `:224-243` | mine | `operator/loss#convergence` | Sole statement. The honesty paragraph is load-bearing for any accuracy claim |
| 40 | Part G — residual generation as a first-class concept; the typed `residual-generator` interface, generator factory, sampling-policy record, and the training-step sketch | same, `#part-g` `:247-318` | delete | — | **Landed as `residual-machinery`** (`retired-ids.csv:29`: `impl-07-residual-factory` → `residual-machinery`). The "missing machinery" is no longer missing. Phase 2 should diff the pseudocode against `residual-machinery §1` and mine only what is genuinely absent there — flagged in Notes |
| 41 | H.1 — registry adds `:differentiability-tag ∈ {D0,D1,D2,D3,D4}`, `:cost-tier`, `:adjoint-implementation`, `:cheap-path`/`:faithful-path`, `:relaxation` | same, `#h-1` `:324-332` | delete | — | Recommends the **superseded five-tag set** (omits `DN`) for a registry that already shipped six tags. Stale recommendation, not survey |
| 42 | H.2 — the three exports Generate / Validate / Import, with Import named as the new one | same, `:334-341` | delete | — | Landed: `Validate` / `Import` are canon seams (`pino-bridge`, spec brief G9). "Import is the **new** thing this stream recommends" is a past-tense claim |
| 43 | H.3–H.6 — default balancing policy, sampling policy, curriculum knobs `(0.10, 0.60, 0.90)`, coverage-matrix record type | same, `:343-370` | mine | `operator/loss#defaults` | Restates B/D/E as settable defaults — that framing is what an implementer needs. Merge with rows 26/33/37 rather than duplicating |
| 44 | **H.7 — what we explicitly recommend AGAINST**: Lagrangian dual-ascent for four-source; soft residuals for architecturally-enforceable relations; single-λ uniform weighting; treating residuals as zero-noise labels | same, `:372-377` | mine | `operator/loss#anti-recommendations` | Sole statement, and the highest-value block in Part H — four named failure modes with reasons |
| 45 | Key References — 33 citations | same, `:381-414` | mine | `operator/loss#references` | The evidence base for everything kept above; a survey without its citations is an assertion |
| 46 | Summary — the eight-item recommendation set | same, `:417-430` | mine | `operator/loss#summary` | Keep, but item 1 names `D0–D4` (stale, see row 41) and item 2 describes Part G as missing (landed, see row 40). Rewrite both on the way in |
| 47 | §1 Goal — the terminal deliverable is one document that fully *specifies* but does not implement the oracle's code; "implementation is a mechanical act" | `journal/live/specs/2026-07-21-oracle-code-spec-research-brief.md#1-goal` | mine | `program/build#code-spec-goal` | The definition-of-done bar for the whole build programme. Sole statement |
| 48 | §2 Scope — in/out lists; **language and toolchain treated as OPEN**, every downstream unit written language-agnostically | same `#2-scope` | mine | `program/build#language-open` | Live and load-bearing. **Contradicts canon**, which marks the language decision closed — see Contradictions |
| 49 | §3.1–3.3 — runtime model, behavioral invariants, tractability spine | same `#3` | delete | — | Explicitly labelled "fixed inputs… not research targets"; each restates `product`, `architectural-principles`, `canonical-vocabularies`. Pure duplication |
| 50 | **§3.4 — the six oracle-contract requirements** (keyed never-aggregated residuals; axis-keyed slots; every targetable observable differentiable + σ + pinnable; primitive axes exposed; scorer/evolver split kept clean; one shared namespace) | same `#3-4` | mine | `oracle/seams/pino-bridge#contract-requirements` | A synthesis, not a restatement — it is the forward-compatibility checklist R9.1 validates against. No page carries the six as a set |
| 51 | **§3.5 — the diamond MVP acceptance test**: null (ground-truth diamond ≈ 0 within σ), sensitivity (perturbation fires the *specific* key naming the violated law), data-backed sensitivity against the strain hypersurface | same `#3-5` | move | `program/build#mvp-acceptance` | Also recorded in `capability-slices` Cap 1 — Phase 2 must site it once, not twice |
| 52 | **"~877 is the byte-salvage count from a truncated download, not the dataset size"** | same `#3-5` | mine | `practice/traps` | A live anti-trap protecting against a wrong N in a fit. Corroborated by `read-me-first.md` recovery history (row 88) |
| 53 | §3.6 — the settled-decisions list | same `#3-6` | delete | — | A pointer list into `open-decisions`; carries no fact of its own |
| 54 | **R0.1 — host-toolchain comparison**, with the four hard constraints (bring-your-own reverse-mode AD; e-graph library; typed IR; codegen path) and "re-examine rather than inherit any prior exclusion" | same `#g0` | mine | open question on `program/build` | The unit that gates every other. Constraints are the reusable part |
| 55 | R1.0 / R1.1 / R1.2…k / R1.x — registry realignment, diamond MVP formula set, remaining formulas by bundle, reference-solve integration | same `#g1` | mine | open questions on `oracle/registry/formula-registry` | R1.1 names the exact MVP formula list — the most actionable unit in the brief |
| 56 | R2.1–R2.5 — the five pipeline-stage algorithm units | same `#g2` | mine | open questions on `oracle/compilation/compose-time-pipeline` | R2.4 records that the ε-equality problem **closed 2026-07-21**; keep the *resolution* (exact identity + ε estimated beside it; rewrite admitted iff exact over ℝ with float side conditions discharged), drop the "was open" framing |
| 57 | R3.1–R3.2 — substrate primitives/serialization; serialization-injectivity verification | same `#g3` | mine | open questions on `oracle/compilation/representation-substrate` | Sole statement of the injectivity check as a deliverable |
| 58 | R4.1–R4.2 — cert machinery; checker bodies. Incl. **the obligation-9 scope defect** (obligation 9 says "surrogate-net validity (for `D4` surrogate formulas)" but `D4` now means *relaxed*; the one real learned surrogate is `D2` and falls outside) | same `#g4` | mine | open questions on `oracle/certification/cert-obligations` | A live, named, unresolved defect in canon — highest-value item in G4 |
| 59 | R5.1–R5.3 — reverse-mode AD engine; implicit-differentiation adjoint synthesis with the registration gate (VJP vs JVP on ~64 points, τ_adj = 1e-4); PDE-mesh adjoint | same `#g5` | mine | open questions on `oracle/seams/residual-machinery` | The τ_adj gate is a live guard — state it as fact, not as an open item |
| 60 | R6.1–R6.2 — encode the interface layer; analytic-structure witnesses | same `#g6` | mine | open questions on `oracle/registry/typeclass-alphabet` | Sole statement |
| 61 | **R7.1 — surrogate-net scope must be re-established first**; names today's six `D4` rows and notes the one actual learned surrogate is `D2` | same `#g7` | mine | open question on `oracle/registry/named-formulas` | This is §2 defect 6 stated from the other side. Its content is the *fix* for the drift |
| 62 | R7.3 — the γ̂ open CS problems, **struck through, closed 2026-07-21**, with the four dispositions | same `#g7` | delete | — | Strikethrough + closed framing = scaffolding (D1). **But**: the four dispositions are present-tense facts — see row 63 |
| 63 | The four γ̂ resolutions: ε-equality → rewrite-admission rule; materialization → Stage-4 adjoint-tape schedule; drift → exported to the integrating consumer; rank applicability → Stage-4 compile-time predicate | same `#g7` | mine | `oracle/state/gamma-hat#resolutions` | Brief §"exception 1": a closed item's *resolution* is a present-tense fact. Also records that the entry mis-quoted canon as calling these "the only open CS problems" when `computational-overview §13` said the opposite |
| 64 | R7.4 — Layer-1.75 minimum spec (V1 ships type/cert scaffolding + loud not-implemented stubs) | same `#g7` | mine | open question on `oracle/seams/residual-machinery` | Sole statement. `Layer-1.75` is a §4 nomenclature-defect name — flag on rename |
| 65 | R8.1–R8.3 (ABI, wire formats, cache policy) and R9.1 (the three consumer seams) and R10.1 (machine-readable catalogs) | same `#g8` `#g9` `#g10` | mine | open questions on `oracle/seams/pino-bridge` and `oracle/registry/formula-registry` | R9.1 is the unit that must honor §3.4's six (row 50) |
| 66 | §5 terminal-deliverable structure (10 sections) and §6 execution model / sequencing / **staging principle** (specify the diamond MVP subset first; it is the correctness anchor) | same `#5` `#6` | mine | `program/build#code-spec-structure` | The staging principle is a live ordering rule for Phase 2 and beyond |
| 67 | §7 source index | same `#7` | delete | — | A bibliography of internal page ids; `corpus.json` supersedes it mechanically (§6 of the plan) |
| 68 | §8 Changelog — the `DX → DN` rename and the eight retagged rows | same `#changelog` | delete | — | Changelog block (D1). The rename itself is log-worthy — see Log-worthy row 3 |
| 69 | **§1 verdict — "survives with restrictions"**; the *strong* reading of "one IR, two interpreters" **dies**; what survives is a shared-kernel factorization | `journal/live/specs/2026-07-16-evolver-duality-research.md#1` | mine | `oracle/laws/generic-dynamics#evolver-duality` | A genuine negative result. Cited by `cs-framing-outline` Beat 29 as the credibility beat. Sole statement |
| 70 | §2.1 three layers (term / system / temporal) and **§2.2 the impossibility argument** — causality assignment is a global matching property, so no per-op denotation exists | same `#2-1` `#2-2` | mine | `oracle/laws/generic-dynamics#evolver-duality` | The proof, not just the verdict. Load-bearing for "why a stepper is a Stage-4 sibling, not a backend" |
| 71 | §2.3 the causal cut and the surviving factorization (scorer = compare∘fold; evolver = fold re-exported as a tangent map) | same `#2-3` | mine | `oracle/laws/generic-dynamics#evolver-duality` | Sole statement of the shared-by-address mechanism |
| 72 | **§2.4 conditions S1–S6** — explicit comparison form, squareness/matching, solvable interior, generator-tag totality, cut stability under rewriting, single extraction | same `#2-4` | mine | `oracle/compilation/compose-time-pipeline#steppability-conditions` | Compile-time checkable conditions; S6 ("one extraction, two lowerings") is a hard rule the Stage-3 design must honor whether or not the evolver ships |
| 73 | §2.5 per-tier index analysis; §2.6 the violators table (which vocabulary entries violate which condition) | same `#2-5` `#2-6` | mine | `oracle/state/multiscale-state#tier-steppability` | §2.6 records that **the diff-tag axis is orthogonal to steppability** — an explicit correction of the brief's own conjecture |
| 74 | §2.7 e-graphs remain the right Stage-3 substrate, with the two disciplines; §2.8 **the scorer is the evolver's auditor** — what the stepper must preserve is what the scorer checks, under the same keys | same `#2-7` `#2-8` | mine | `oracle/laws/residual-definitions#preservation-obligations` | §2.8 is the strongest architectural claim in the memo and exists nowhere else |
| 75 | §3.1–3.5 the shape of a steppable lowering — Stage-4 sibling emission, the evolver-file's four-part anatomy, the steppable-form manifest, contracts, per-block integrator dispatch, and the V-ordering (slow tier first) | same `#3` | mine | `oracle/compilation/compose-time-pipeline#evolver-lowering` | Sole statement. §3.3's "the evolver never introduces a second truth" is the new cert obligation |
| 76 | §4 refusal cases — the principled rule, the seven-class closed enumeration, and §4.3 the structural–numerical gap (structural analysis can pass while the Jacobian is singular ⇒ pair witnesses with sampled numerical spot-checks) | same `#4` | mine | `oracle/certification/out-of-scope#evolver-refusals` | §4.3 is a live hazard applying to the *existing* adjoint gate, not only to a hypothetical evolver |
| 77 | §5 — the integrator-interface proposal: `dynamics` is the evolver-file hand-off (tangent + generators + constraints + structure + obligations + cadence + cost + identity), **not** an integrator; three rejected alternatives | same `#5` | move | `oracle/seams/pino-bridge#dynamics-handoff` | **This closed.** `README.md:115-116`: "The integrator interface closed 2026-07-16: a per-tier tangent kernel plus a steppable-form manifest, with the integrator staying consumer-side." State the resolution present-tense; drop "proposal for joint review" |
| 78 | §6 sources — 25 primary references, grouped A–E | same `#6` | mine | `oracle/laws/generic-dynamics#references` | Evidence base. **But** the header says "Repo documents cited throughout by id (`arch-xx`, `impl-xx` …)" — retired id formats named as the citation convention; drop that sentence |
| 79 | Status header — audit verdicts, what cleared, what is gated, "corrections applied in place" | `journal/live/specs/2026-07-08-wave2-beta-ga2o3-seeding.md:3-13` | delete | — | Process history of a superseded stratum (D13). The one live element — B3 still gated — is row 87 |
| 80 | **§1.1 the predicate split** — `is-polar-material` (Born charges/LO–TO; gates Fröhlich) vs `is-noncentrosymmetric` (piezo classes; gates rows 113–119/128); β-Ga₂O₃ is centrosymmetric yet strongly polar | same `#1` | delete | — | **Landed as normative in `applicability-classifiers`** and `canonical-vocabularies §3` (the spec says so itself at §5 item 3). Duplication of shipped canon |
| 81 | §1.2 direction tags mandatory on every anisotropic row; the RUS frame (x∥a, y∥b, z∥c*); **c\* is 13.83° from [001]**; Klimm's crystal-physical frame and the "never relabel λ₍₁₀₀₎/λ₍₀₀₁₎ as [100]/[001]" rule | same `#1` | mine | `practice/traps` | A live frame trap of exactly the class the traps register exists for. Not in canon |
| 82 | §1.3 gap bookkeeping — quasi-direct; the measured onset is polarization-dependent; **seed the effective gap and the polarized onsets as separate rows, never average them** | same `#1` | mine | `practice/traps` | Live convention. The never-average rule is a trap, not a value |
| 83 | §1.4 ZPR `slope-kind` — isochoric applies to the ZPR amplitude only; the −0.45 eV 700 K shift is the *total*, else row 63 double-counts | same `#1` | mine | `oracle/accuracy/accuracy-ledger#zpr` | A live double-counting guard tied to a specific ledger row |
| 84 | §1.5 hole physics = polarons by design refusal; do not seed hole mobility / hole α_ii / p-type C–T; cert-refuse instead | same `#1` | mine | `oracle/certification/out-of-scope#beta-ga2o3-holes` | A live refusal with a physical reason. Echoed in `prep.md` "What can't it do?" |
| 85 | §1.6 Fröhlich is multi-mode; a single-α_F shorthand fits β-Ga₂O₃ poorly; mode table pinned to Mengle & Kioupakis Table I | same `#1` | mine | `oracle/registry/computational-methods#frohlich` | Live: the KernelExt assembly is still research-side work (open question) |
| 86 | §2 the β-Ga₂O₃ seed tables (structure, electronic, all 13 elastic constants, phonon/thermal, transport/high-field) and §3's III-N pyroelectric rider values | same `#2` `#3` | delete | — | **Already seeded into `physics/library/cert/reference-data/*.csv`** (§5 item 2 records the landing). The CSVs survive to `data/` and are the checked copy; the tables are an unchecked second copy of the same numbers |
| 87 | **B3 polarization-bowing rider is still gated** on the FBA APL 80 1204 / Ambacher JPCM 14 3399 pin-read; the gate row rides in `polarization-piezoelectric.csv` | same `#3` `#4` `#5` | mine | open question on `oracle/accuracy/accuracy-ledger` + user-action item | The one genuinely unfinished thing in the wave. Matches the memory record (B3 gated on paywalled texts, Javier acquiring) |
| 88 | **The FB01 bowing sign trap** — FB01 Eq. 4–5 define `−b·x(1−x)` with **b negative** ("bowing always upward"); the `+b, b ≈ +0.019…0.021` form is the same physics via double-negative; a naive lift of FB01's Eq-5 `b` into a `+b` formula flips the bowing and corrupts interface charge | same `#3`, and `…-audit.md#1` item 1 | mine | `practice/traps` | Stated in both files. A textbook sign trap — exactly the `D12` figure the talk was built around |
| 89 | §4 GAP register — C–T quartet (no consensus fit), Θ_D spread, Fröhlich KernelExt assembly, GaN pyro thin data, AlGaN-analog alloying deferred | same `#4` | mine | open questions on `oracle/accuracy/accuracy-ledger` | Genuine carried gaps, per D7 they attach to the owning page |
| 90 | §5 landing plan, executed | same `#5` | delete | — | Process record of a completed landing. Migrate the dated fact to the log |
| 91 | §6 sources (verified this pass) | same `#6` | delete | — | **Carries two values the audit refuted and that were never propagated here** — see Contradictions rows 1–2. Delete rather than migrate; the corrected values are in §2 and in the CSVs |
| 92 | Audit §1 convention verdicts 1–7 (FB erratum sign+frame; pyroelectric sign; ZPR retag; predicate split; anisotropy/frame; hole/STH refusal; multi-mode Fröhlich attribution) | `journal/live/audits/2026-07-16-wave2-beta-ga2o3-audit.md#1` | mine | `practice/traps` + `oracle/accuracy/accuracy-ledger` | Stratum deleted by decision (D13); the trap content is live. Overlaps rows 81–85, 88 — Phase 2 sites each once |
| 93 | **The pyroelectric sign trap** — seeded III-N P_sp are negative in the ZB-ref frame, so `p = dP_sp/dT` is **positive**; raw literature quotes p negative under the positive-P_s convention | same `#1` item 2 | mine | `practice/traps` | Live sign guard riding the CSV rows. Sole full derivation |
| 94 | Audit §2 number verdicts — the E_c mis-attribution (no "≈8 MV/cm" in Ghosh–Singisetti; the triple is 10.2/4.8/7.6), direction labels, Chynoweth extraction, polarized onsets, Handwerg parenthetical, κ[001] ≈ 14, m*_e source, Klimm year/tensor, per-axis ε₀ | same `#2` | delete | — | All corrections were applied to the spec §2 and seeded to the CSVs. The corrected values survive in `data/`; the record of correcting them is history |
| 95 | **Klimm frame finding** — principal axes ≠ crystallographic directions; only λ₂₂ ≡ [010] and λ₃₃ ≡ [001] are frame-shared; λ₍₁₀₀₎/λ₍₀₀₁₎ are along a\*/c\* | same `#2`, `#4` G2 | mine | `practice/traps` | Live relabelling hazard; the audit and the spec state it twice because it is easy to get wrong |
| 96 | §3 GAP register (post-audit) | same `#3` | delete | — | Superseded by the spec's own §4 register (row 89), which is the later copy |
| 97 | **§4 G1 disposition — the erratum is benign for B3** (corrects only CuPt-ordered In-alloy *piezoelectric* polarization; the P_sp bowing of FB01 Eq. 4–5 / Fig. 7 is untouched; CuPt-ordered AlGaN is Vegard-within-error) | same `#4` | mine | `oracle/accuracy/accuracy-ledger#iii-n-bowing` | A resolved gate with a verbatim-quoted primary source. Present-tense fact |
| 98 | **§4 G1 — "Ebert" = Lan et al. PRB 113 155302 (2026), CC-BY, InGaN-only**; ZB-frame theory bows with the *opposite* curvature sign to LH-frame theory and experiment; **curvature is NOT reference-invariant**; upgrades ledger row-35 high-In InGaN/GaN cert-refusal from theoretical to experimental | same `#4` | mine | `oracle/accuracy/accuracy-ledger#row-35` | **This overturns the audit's own §1 cross-flag** (which asserted curvature is "~invariant under reference choice"). The §4 reading is the later, text-in-hand adjudication. See Contradictions |
| 99 | §4 G2 — closed same day; Klimm is open access; derived anchors κ[010](773 K) ≈ 9 ± 2, κ[010](1100 K) ≈ 6 ± 1.5 W/mK seeded | same `#4` | mine | `oracle/accuracy/accuracy-ledger` | Values seeded to CSVs; the *derivation method* (300 K direct tensor × the paper's own T^−m law) is provenance worth keeping |
| 100 | **"The auditor's 403s were bot-blocks, not a paywall"** (twice: Klimm, and the Lan/Ebert paper) | same `#4` | mine | `practice/traps` | A live research-process trap that cost a false GAP entry and a false gate. Generalizable and cheap to state |
| 101 | §5 disposition — spec corrected in place, rows seeded, README population status updated | same `#5` | delete | — | Process record; the outcome is the CSV contents |
| 102 | `check_book_structure.py` — 12 numbered invariants (frontmatter parse; unique `id`; **unique `canonical-for` topics**; `depends-on` real + `referenced-by` not stale; `[id]` resolves; a cited `[id]` has its depends-on edge; section coordinates + dated anchors resolve, ambiguous dated anchor rejected; no line-number citations; nine registry-count classes; trap numbering contiguous + `[traps] §N` resolves; `authority` in vocabulary; `content-hash` matches body). Also **emits** `contents.md` / `index.md` and restamps `content-hash`, `referenced-by`, probe count | `journal/tools/check_book_structure.py:1-40` | delete | rebuilt as `tools/` per plan §8 | Do not rewrite (instructed). Surface is `journal/pages/` only (`:50,135`) — probe B. Checks 11 and 12 die with `authority` and `content-hash`; the rest carry over |
| 103 | `check_data_agreement.py` — 15 check classes (a)–(o), and an explicit **"what this tool deliberately does not check"** section recording three checks that were built, measured, and removed (tolerance-ledger exhaustiveness — fired 48× on correct prose; bare `§` self-references — indistinguishable from literature citations; legend scoping — `D0` as charge state vs diff tag) | `journal/tools/check_data_agreement.py:1-64` | delete | rebuilt as `tools/` per plan §8 | **The removal rationale must survive the rebuild** or someone rebuilds the noise. Surface at `:69-82` = README + instructions + `journal/pages/**` + reference CSVs + `informed-operator/design/*.md`; **`journal/live/` exempt** — probe A |
| 104 | **`corpus_root.py`** — `refuse_if_scratch_copy()` exits 2 inside `.claude/worktrees`; `describe()` stamps every success line with `commit · path`. Written because the repo carried two complete copies of the book, both printing `apparatus OK`, and an agent believed the stale one | `journal/tools/corpus_root.py` | mine | `tools/` — **carry forward, do not drop** | The `1e04770` failure made executable. Plan §"Phase 2" requires it be *structurally impossible* for two trees to both claim to be the corpus; this file is the existing half of that guarantee |
| 105 | `check_the_checkers.py` — plants one defect per check into a temp copy, asserts the message appears; 58 probes + 2 guard probes; reports missed / stale / **uncovered** | `journal/tools/check_the_checkers.py:1-40` | delete | rebuilt as `tools/` per plan §8 | Design is sound and plan §8 says keep it. The `GUARD_PROBES` constant is read by the structure checker so the published probe count stays the calibrated one — a nice interlock to preserve |
| 106 | `registry-manifest.csv` — 134 rows = 132 substantive + 2 architectural markers | `physics/library/formulas/registry-manifest.csv` | move | `data/registry-manifest.csv` | Canonical and machine-checked (nine count classes in the structure checker, plus (a),(g),(j),(n) in the data checker). Survives unchanged |
| 107 | `retired-ids.csv` — 38 rows mapping `arch-NN`/`impl-NN` → landed ids, all retired 2026-07-22 | `physics/library/formulas/retired-ids.csv` | delete | — | Per D1/§9. **Ordering hazard**: it is the only resolver for rows 16 and 40 and for the `arch-xx`/`impl-xx` references in row 78. **Must be consumed before deletion** — see Notes |
| 108 | `retired-names.csv` — 62 rows of retired formula names | `physics/library/formulas/retired-names.csv` | delete | — | Per D1/§9. Consumed by data-checker class (e), which dies with it. Same ordering hazard, lower risk (no live retired name found in my scope) |
| 109 | `cert/reference-data/*.csv` — 5 files: elastic-tensors, material-constants, phonon-frequencies, polarization-piezoelectric, transport-coefficients | `physics/library/cert/reference-data/` | move | `data/reference-data/*.csv` | Canonical seed values, machine-checked by classes (k),(l),(m). Where the Wave-2 values actually landed (rows 86, 94, 99). **Seed values from here, never from an appendix page** |
| 110 | 15 `.gitkeep` scaffold dirs under `physics/library/` (abstract-properties, applicability, canonicals, cert, dynamics, formulas, generic, inputs, interfaces, lattice-math, methods, observables, pino-bridge, residuals, state) + `informed-operator/design/.gitkeep` + `interface/.gitkeep` | `physics/library/**`, `informed-operator/design/`, `interface/` | delete | — | Empty directories reserving a **code** layout for code that does not exist and is out of scope (plan §0: "Write implementation code for `/physics`. There is none and none is wanted yet"). The spec brief §"Design status" calls `physics/library/` "an empty scaffold". Only `formulas/` and `cert/` hold content, and both move to `data/` |
| 111 | `read-me-first.md` — the suite map, what the data is in three sentences, archive contents table (six distortion families, point counts) | `physics/research/diamond-stretch-and-skew-sweep/read-me-first.md` | move | `data/diamond-strain-sweep/read-me-first.md` | Live MVP feedstock, not scaffolding: §3.5's acceptance test and R1.1 both depend on it. **Cites `CHANGELOG.md` twice as part of "The suite"; that file is not in this folder** — dangling |
| 112 | **The dataset itself is not in the repo and must not be** — it lives at `/Pool/Diamond_Stretch_And_Skew_Sweep/`; every run directory holds a VASP-licensed POTCAR and this remote is public; `/Pool` wins on any disagreement | same `:3-11`, `#non-negotiable-warnings` item 1 | move | `data/diamond-strain-sweep/read-me-first.md#licensing` | A licensing constraint on a public remote. The single most consequential line in the suite |
| 113 | Provenance and recovery history — generated 2026-04-10 by `enk43` (UFV), VASP 6.2.0, `PAW_PBE C 08Apr2002`; the two truncated downloads; **family 4 was never lost, it was simply last** (archive order 1,3,2,6,5,4); "OneDrive's download-folder-as-zip truncates on payloads this size" | same `#provenance-and-recovery-history` | move | `data/diamond-strain-sweep/read-me-first.md#provenance` | Provenance is required by the log schema (§7: evidence + attribution). The truncation lesson is a live operational trap and the origin of the bogus "~877" (row 52) |
| 114 | **AEXX = 0.27 is not standard HSE06** — never compare these hybrid energies/gaps to literature HSE06(0.25) without saying so; always carry α in provenance | same `#non-negotiable-warnings` item 2 | mine | `practice/traps` | Live comparability trap; matches the memory record (PBE→HSE α=0.27) |
| 115 | **The strain reference is the experimental lattice constant**, not either functional's equilibrium — every strained run carries a constant reference stress offset (**+19.7 kB PBE, −86.3 kB HSE06**). Difference it out or carry a linear term; **omitting it biases C₄₄ by 15%** | same `#non-negotiable-warnings` item 3 | mine | `practice/traps` | A live, quantified numerical trap on the exact quantity the MVP acceptance test fits. Highest-value single line in `physics/research/` |
| 116 | **1,179 rows are only 1,131 distinct shapes** — 24 single-skew shapes computed three times, all bit-identical; de-duplicate before any fit or training set or they carry 3× weight; `duplicate_group` marks them | same item 4, and `04-…md#duplicates` | mine | `practice/traps` + `data/diamond-strain-sweep/` | Stated in three places (here, `04-…`, and spec §3.5). Sole quantitative statement of the mechanism |
| 117 | Never rename the files back by hand; `CHANGELOG.md` is the exact inverse map; VASP requires canonical filenames for any restart | same item 5 | move | `data/diamond-strain-sweep/read-me-first.md#warnings` | Operational, live. **Same dangling `CHANGELOG.md`** as row 111 |
| 118 | `01-what-this-data-is.md` — the physical object; the two-step PBE→HSE ladder; the six deformation families and exact conventions; the nested shear design; the per-file dictionary; **§5 units and sign conventions**; §6 what "derivable" means | `physics/research/diamond-stretch-and-skew-sweep/01-what-this-data-is.md` | move | `data/diamond-strain-sweep/01-what-this-data-is.md` | Describes an external dataset's file layout and conventions — canon carries none of this, so it is not duplication. §5 (units/signs) is trap-adjacent and should also be linked from `practice/traps` |
| 119 | `02-how-to-read-and-derive.md` — Part A the needs-map (bundle/bridge → recipe), Part B reading mechanics, Part C worked verified examples with real numbers | same, `02-…` | move | `data/diamond-strain-sweep/02-how-to-read-and-derive.md` | The bridge from raw VASP output to the state slots. Part C's re-runnable self-tests are the only worked numerical examples anywhere in the corpus |
| 120 | `03-what-is-derivable.md` — the T0–T6 tiered inventory, the explicit **NOT-derivable** list, and the extrapolation discipline | same, `03-…` | move | `data/diamond-strain-sweep/03-what-is-derivable.md` | The NOT-derivable list is a refusal register for the dataset — same discipline as `out-of-scope`. **`T0–T6` here means model-dependence; canon's `T0–T3` means cost tier.** Collision — see Contradictions |
| 121 | `04-data-health-gaps-and-caveats.md` — the completeness ledger (family 4: 12 absent points; family 5: 24 absent combinations), design gaps never sampled, systematic caveats, file-level quirks, fit-for-purpose vs the program, trust-level summary | same, `04-…` | move | `data/diamond-strain-sweep/04-…md` | Honest gap accounting for live feedstock. §5 "fit-for-purpose vs the program" is the direct input to whether the MVP acceptance test can be run at all |
| 122 | `index-of-all-runs.tsv` — machine-readable manifest, 1,179 rows, parsed deformation parameters, duplicate flags, completeness status | same, `index-of-all-runs.tsv` | move | `data/diamond-strain-sweep/index-of-all-runs.tsv` | The machine face of the dataset; `duplicate_group` is what row 116 instructs you to use |
| 123 | `diamond-defect-corpus/index-of-all-runs.tsv` — a second run manifest with no companion prose | `physics/research/diamond-defect-corpus/index-of-all-runs.tsv` | move | `data/diamond-defect-corpus/index-of-all-runs.tsv` | **Undocumented**: no `read-me-first`, no provenance, no licensing note, and nothing in my scope cites it. Moved rather than deleted per the tie-break rule — see Notes |
| 124 | Title + purpose — n-Op trains a PINO that "predicts how the multiphysics state of a crystalline material **evolves in time**", for durable UWBG chips in harsh environments | `README.md:1-7` | mine | `program/purpose/purpose-and-scope` | **The completion-vs-time-evolution tension** (plan §12), stated in the entry point. Log as contradiction, do not adjudicate |
| 125 | The MVP is diamond, three capabilities (crystal-structure prediction, electron-cloud diffusion, heat diffusion); "the spec is comprehensive; the build is diamond-first" | `README.md:9-11` | mine | `program/build/mvp-scope` | Live scope statement; matches `capability-slices` Cap 1 |
| 126 | **"No code has been written yet. This repository is specification and research."** | `README.md:13` | mine | `program/purpose/purpose-and-scope` | The single most important disclaimer in the corpus. Repeated in the spec brief, the outline's honesty constraints, and the talk |
| 127 | "Start here" table — instructions / contents / index / glossary | `README.md:15-25` | mine | corpus entry point, rewritten | All four referents change: `contents.md`+`index.md` are emitted (§6), `instructions.md` → `practice/agent-contract`, `glossary.md` → `practice/glossary` |
| 128 | "Chapters are **by concern**, and chapters **2–6 are the contiguous `/physics` oracle block**" | `README.md:27-28` | delete | — | Chapters cease to exist (D5: journals → sections → pages). The navigational job passes to `corpus.json` |
| 129 | "Pages are addressed by their `id`, never by filename or by the display tag. That is why the corpus could be restructured wholesale without breaking a cross-reference" | `README.md:30-32` | mine | `practice/agent-contract#addressing` | The *principle* survives and is vindicated; the *statement* inverts — under D6 the filename **is** the id and the display tag is deleted |
| 130 | The three libraries — `physics/` (reference oracle; does not hold state values, train, integrate, or wrap DFT at runtime), `informed-operator/` (the PINO), `interface/` (driving loops, not yet designed) | `README.md:34-46` | mine | `program/purpose/library-landscape` | The clearest three-module statement in the corpus and the direct justification for D4's three-journal shape |
| 131 | Architectural framing — the `PhysicsGraph` (three node kinds, typed dataflow edges, per-stage sidecars) and the **4+1 stage** pipeline, Stages 1–4 once per identity, Stage 5 millions of times | `README.md:48-59` | mine | `program/purpose/architectural-principles` | Keep. **"4+1 stage" is a §4 nomenclature defect** (a 4+1 pipeline containing a "Stage 2.5") — propose, do not rename |
| 132 | The `Layout` tree — directory map with 58 pages / 49 canon / chapter folders | `README.md:61-78` | delete | — | Describes the tree being demolished. Regenerate from `corpus.json` if an entry point still wants one |
| 133 | Checker operating instructions — run `check_book_structure.py` after editing, `--check` is the gate alongside `check_data_agreement.py`, and the ten named failure classes | `README.md:80-87` | mine | `practice/agent-contract#gate` | The *list of what is enforced* is reusable as the acceptance spec for the rebuilt checker; the invocations are not |
| 134 | **"Green is not evidence a check ran."** Both checkers have shipped holes that skipped whole classes of citation silently; plant a defect of the class you claim is absent before citing a clean run (`[traps] §58`); `check_the_checkers.py` does exactly that | `README.md:89-94` | mine | `practice/traps` + `practice/agent-contract` | The single most load-bearing methodological rule in the corpus, and the basis of this fragment's own two probes. **Uncuttable** |
| 135 | Open decisions summary — language open; surrogate-net / PDE-mesh adjoint / Layer-1.75; the four γ̂ questions closed 2026-07-21; **one** open verifier-soundness gap (no post-registration adjoint-drift monitoring), three others named and closed | `README.md:96-113` | move | per-page `open-questions:` frontmatter (D7) | The adjoint-drift gap is the plan's own §5 worked example. The rationale for listing closed ones — "an absent check reads exactly like a passing one and a silently closed one reads like it was never there" — is a real argument that D2 overrides; note it for Javier |
| 136 | "The integrator interface closed 2026-07-16: a per-tier tangent kernel plus a steppable-form manifest, with the integrator staying consumer-side" | `README.md:115-116` | move | `oracle/seams/pino-bridge#dynamics-handoff` | The resolution of row 77. Present-tense fact; drop the date framing |
| 137 | "Before changing anything physical — read `[traps]`. Sign, unit, and reference-frame errors in this corpus have historically survived ordinary review and inverted real physics" | `README.md:118-123` | mine | corpus entry point + `practice/traps` | Live hazard warning, empirically justified by rows 88, 93, 95, 115 |
| 138 | **The σ collision — sigma means four things**: standard deviation (bridge record) · stress (dashboard, kbar) · SIGMA smearing width (VASP) · σ_g orbital label. Capital Σ is summation and none of the above | `journal/live/presentations/2026-07-22-prep.md#sigma-means-four-things` | mine | `practice/glossary#sigma` + `practice/traps` | **Exists nowhere else.** A four-way overload of the most-used symbol in the corpus, and the same defect class as the §4 nomenclature list |
| 139 | Slide 2 — channel completion: in = topology + known properties, out = the channels not supplied; topology mandatory because partial properties are one-to-many; **"channel" = function, not number** | same `#slide-2` | mine | `program/purpose/purpose-and-scope#completion` | Javier's own framing of what the operator does — the completion side of the plan §12 tension. Say "one-to-many", not "ill-posed" |
| 140 | Slide 3 — five properties motivating an operator (function-valued outputs, discretisation invariance, evaluate-at-arbitrary-points, nonlocal kernels, amortised solution map), with evaluate-at-points named the load-bearing one; and **keep architecture and training regime separate** | same `#slide-3` | mine | `operator/seam#why-an-operator` | Ties directly to R2 (row 6). The architecture-vs-regime separation is a live anti-conflation rule |
| 141 | Slide 4 — the seven slots with shapes and read-aloud names; GL⁺(3,ℝ) not SL(3) because volume is physical; Parrinello–Rahman; **`Z_I` immutable ⇒ no gradient through composition ⇒ there is no "proposer" component**; γ̂ never densified (~18 MB vs ~460 MB) | same `#slide-4` | mine | `oracle/state/unified-state#slots` (pronunciations → `practice/glossary`) | The `Z_I` ⇒ no-proposer chain is a complete argument stated in one place. The 18/460 MB figure corroborates `gamma-budget` |
| 142 | **The VASP coverage analysis** — one real H₂ run measured against the state: 3 present, 1 partial, 3 absent, with a named cause per absence (`Π_h`/`P_I` absent by *run settings*, `A` absent by *theory*); conjugates plotted instead; why γ̂ is partial (CHGCAR is the spatial diagonal only; WAVECAR was never written; occupations are the eigenvalues of γ̂) | same `#slide-4` | mine | `oracle/state/unified-state#data-source-coverage` | **Exists nowhere else.** The only place the corpus tests its state definition against a real data source. Directly answers "which slots does the supervised stage constrain" (deferred unit I5) |
| 143 | Slide 5 — GENERIC expanded (General Equation for the Non-Equilibrium Reversible–Irreversible Coupling), metriplectic, the two brackets and their properties, the degeneracy conditions, **why L acting on conjugate pairs fixes the slot set**, the four regimes as restrictions; "say restrictions of one law, avoid unification" | same `#slide-5` | mine | `oracle/laws/generic-dynamics` (naming → `practice/glossary`) | The "no `P_I` ⇒ no L on the ions" argument is the cleanest statement of why the state has the members it has |
| 144 | Slide 6 — the **fingerprint**: hash functional, AEXX, HFSCREEN, ENCUT, POTCAR TITELs, k-mesh, ISPIN, SIGMA, ISYM; compare absolute energies only within one hash, because basis-set incompleteness error does not cancel across ENCUT | same `#slide-6` | mine | `oracle/registry/computational-methods#admission-fingerprint` | The admission gate for external data. Corroborates row 114's α-in-provenance rule |
| 145 | **The invariant battery** — Σ_I f_I = 0 (translational invariance) · σ_ij = σ_ji (angular momentum) · Σ_nk w_k f_nk = NELECT (electron conservation) · det(A) = V (arithmetic) · spin parity. Closed form, one pass per frame; catches parser bugs and run failures together | same `#slide-6` | mine | `oracle/certification/cert-obligations#ingest-battery` | **Exists nowhere else.** Five checkable invariants with their reasons — directly implementable, and the cheapest possible data gate |
| 146 | **The strain transpose trap** — `M = A_ref⁻¹A_def`, `F = Mᵀ`, `E_GL = ½(FᵀF − I)`; lattice matrices are stored **by row**, so every transpose is a chance to be wrong and stay plausible; the transpose-free form `E_GL = ½(A_ref⁻¹ G_def A_ref⁻ᵀ − I)` with `G = AAᵀ` | same `#slide-6` | mine | `practice/traps` | **Exists nowhere else**, and it is the trap that governs the MVP dataset (rows 115–116 are strain fits). A row-major convention silently corrupting a strain tensor |
| 147 | Getting an uncertainty — the bridge record `(value, uncertainty, provenance, coverage)`; redundancy and symmetry-nulls as the two routes; **"precision, not accuracy — say it explicitly"**: basis-set, pseudopotential and functional error are identical across equivalent configurations and cancel exactly, contributing nothing to the spread | same `#slide-6` | mine | `oracle/accuracy/accuracy-ledger#precision-vs-accuracy` | A live methodological guard against quoting a precision floor as accuracy. Echoed in "Don't say" |
| 148 | Slide 7 — the three limits: **extrapolation** (in-range values with unchanged confidence, no error bar, nothing marks which side of the support boundary), **spectral bias** (band edges, defect levels and sharp DOS features come out rounded), **data appetite**; none announces itself; the oracle does **not** fix extrapolation and cannot flag OOD at prediction time | same `#slide-7` | mine | `operator/training#limits` | The honest half. Nothing in canon states the operator's failure modes; this is the sole home |
| 149 | Slide 8 — why a checker: Barahona 1982 (3D Ising ground state NP-hard); Kitaev (k-local Hamiltonian QMA-complete); Baker–Gill–Solovay (relativization as the limit of the technique); **decision vs valuation oracle** — one bit gives a learner no direction; Grover does not apply; CEGIS with a gradient is the honest analogue | same `#slide-8` | mine | `program/purpose/rationale#complexity-frame` | Flagged in the outline as **brought in, not found in canon** (row 158) — so this is the sole home for all of it |
| 150 | Slide 8 "Where it attaches" — the three stages, why not residuals first, and **keyed residuals never summed** (individual weighting, scheduling, auditing which law fired; makes the acceptance test meaningful) | same `#slide-8` | mine | `operator/training#stage-ordering` | **The second of the two homes of the stage ordering** (row 17). The keyed-never-summed rationale is the fullest statement anywhere |
| 151 | Slide 9 — the residual categories with read-aloud translations (structural, positivity ×2, conservation, algebraic ×2, thermodynamic, symmetry); **why the complete state is required**: the trace condition and phonon positivity are not computable from atom positions | same `#slide-9` | mine | `oracle/laws/residual-definitions` (translations → `practice/glossary`) | The "why the complete state" argument is the physics twin of the outline's Beat 9 |
| 152 | Backup — the compiler: stages, closed vocabulary counts, **guards must be first-order decidable**, Futamura's first projection, **refusal is absence**, runtime `O(log n)` and the implicit-function theorem | same `#backup` | delete | — | Compressed restatement of `compose-time-pipeline`, `named-formulas`, `computational-overview`, `representation-substrate`. Duplication — the canon statements are the checked ones |
| 153 | Questions to expect — isn't this a PINN; results (none); how do you know the residuals are right; why not more DFT; time evolution; which language; does it design materials; **topological materials** (two Kitaev results conflated; the atlas classifies and generates nothing; indicators need irrep labels + SOC, and ISYM=0/no-SOC makes them uncomputable from this corpus); **what can't it do** (strongly correlated — γ̂ is mean-field; hole transport in β-Ga₂O₃ — self-trapped polarons; breakdown above ~500 °C — no hot-carrier anchors) | same `#questions-to-expect` | mine | `oracle/certification/out-of-scope` + `practice/traps` | The three refusals each carry a physical reason and match canon's refusal discipline. The Kitaev conflation is a live trap |
| 154 | **"Don't say"** — no "proposer"; never the oracle as a permanent loop or any oracle call at inference; never the precision floor as accuracy; the language is not decided; GENERIC is not a "unification" | same `#dont-say` | mine | `practice/traps#five-things-not-to-say` | Five live misstatement hazards, each corresponding to a real error someone could make. Sole statement |
| 155 | Reference — symbols table (17 rows) and codes table (11 rows) | same `#reference` | mine | `practice/glossary` | Read-aloud names and expansions for every symbol and acronym in the corpus. The glossary has 77 entries; this adds the *pronunciation* axis, which nothing else carries |
| 156 | Talk script slides 1–10 — the spoken version of rows 139–151 | `journal/live/presentations/2026-07-22-talk-script.md#1`–`#10` | delete | — | Deliberately derivative: its own header says the source material is the outline and prep, and "content cut from the spoken version is retained here and in that file's Q&A backup". Nothing in it is unique except rows 157 and Contradiction 5 |
| 157 | Talk script — "the data structure and the physical law are the same object" | same `#1` | mine | `program/purpose/rationale#thesis` | The one-line thesis of the whole programme, stated nowhere else this compactly |
| 158 | Backup-slide list and "Answers to have ready" | same `#backup-slides` `#answers` | delete | — | The backup list indexes `figures/unused/`, deleted by decision (D12/§9). The answers are a shorter copy of row 153 |
| 159 | The situation, audience and rhetorical goal; **the relationship stated correctly** — operator emits, oracle scores, residuals are loss terms, there is no proposer; "properties in → structures out" is a *direction*, and inverse design is out of scope | `journal/live/presentations/2026-07-22-cs-framing-outline.md#the-situation` | mine | `program/purpose/purpose-and-scope#the-relationship` | The cleanest one-paragraph statement of the module relationship in the corpus. Per memory, the *CS-framing* packaging is not to be announced — the content survives, the framing does not |
| 160 | **The pain → concept → resolution table** — eight rows mapping a practitioner problem to the CS concept that dissolves it | same `#the-pain-concept-resolution-table` | mine | `program/purpose/rationale#why-these-choices` | Eight design decisions each with a *motivating problem*. Canon states the decisions; only this states what each is for |
| 161 | Acts 0–1, Beats 1–8 — the incumbents, the asymmetry, the complexity frame, oracle machines and the PH ladder, decision-vs-valuation, CEGIS | same `#act-0` `#act-1` | delete | — | Same content as row 149, in beat form. Keep prep's version (fuller); this one is stage directions |
| 162 | **Beat 9 — certificate completeness ⇒ score-not-solve.** Verification is only cheap if you are handed the complete witness; hand the checker a partial witness and it must *solve* for the rest, putting you back in the hard class. Therefore score-not-solve is a **complexity-theoretic requirement, not a stylistic preference** — and it is why the operator must carry the electronic state | same `#act-1` Beat 9 | mine | `program/purpose/rationale#score-not-solve` | **Explicitly flagged by its own author as a candidate addition to canon**: "Beat 9 is a genuine synthesis and may deserve to land in canon — treat it as a candidate addition, not as something pre-existing." Canon asserts score-not-solve; only this *derives* it. Highest-value single block in the presentations stratum |
| 163 | Acts 2–5, Beats 10–28 — the loop, granular keyed residuals, honest scope, the front end, the closed grammar, the state, the compiler, decidable static semantics, Futamura, the complexity ledger, symmetry as an optimization pass, hash-consing/CSE, differentiation as a design constraint, offline equality saturation, the four exits, evidence-never-verdicts, refusal-as-absence, proof-carrying results, content addressing | same `#act-2`–`#act-5` | delete | — | Each beat is a citation-carrying restatement of a canon page it names. Duplication — the canon statements are the checked ones |
| 164 | **Beat 22 uses `D0 \| DX \| D1 \| D2 \| D3 \| D4`** | same `#act-4` Beat 22 | delete | — | `DX` was **renamed `DN` on 2026-07-21** (`retired-names.csv`, spec brief changelog) — a day *before* this 2026-07-22 document. Retired vocabulary token, in the unswept stratum. Recorded here so the deletion is a decision, not an oversight |
| 165 | **Beat 29 — a real negative result offered deliberately**; "a crisp negative result buys more credibility than any positive claim" | same `#act-6` | mine | `oracle/laws/generic-dynamics#evolver-duality` | The presentational half of row 69. Keep the *epistemic* point — publish the negative result — as practice guidance |
| 166 | Beat 30 — how correctness was engineered: machine-checked corpus invariants, calibration-gated adversarial audit, the standing traps register defined as **"durable findings where the wrong version looks exactly as plausible as the right one"** | same `#act-6` | mine | `practice/traps#what-a-trap-is` | The best definition of a trap in the corpus, and the criterion Phase 2 needs to decide what belongs in the register |
| 167 | **Beat 31 — the parser bug, told against myself.** `check_data_agreement.py` originally scanned only backtick-quoted formula names, so `formula = <name>` arguments were invisible; the checker reported **clean** while pages invoked formulas absent from the registry. Widening the scan surfaced **17 findings immediately** | same `#act-6` | mine | `practice/traps` + log | A concrete, quantified instance of the green-checker failure (row 134). The strongest available evidence for why plan §8's holes must close |
| 168 | Beat 32 — close; relativization as the limit of the technique | same `#act-6` | delete | — | Rhetorical bookend; the substance is in row 149 |
| 169 | Diagram specs D1–D19, the figure-mapping note, and the slide-count reality check | same `#diagram-specs` | delete | — | Specs for assets deleted by decision (D12/§9). **Exception**: D12's content is row 146 and D17's is row 162, both already mined |
| 170 | **"Found vs. brought — keep this distinction honest"**: found in canon = every parsing hook, every complexity figure, the decidability gate; brought from outside = NP-hardness/QMA framing, all oracle-class material, CEGIS, and Beat 9 | same `#found-vs-brought` | mine | log + `program/purpose/rationale` | An explicit provenance ledger separating corpus claims from imported framing. Directly serves the audit obligation (§7 attribution) |
| 171 | **Honesty constraints** — zero implementation code, so every complexity figure is a design commitment never a benchmark, said in the same breath as the number; time-evolution deliberately unclaimed; inverse design out of scope; **"Language/toolchain is open (canon marks it closed; that decision has been reopened)"** | same `#honesty-constraints` | mine | `program/purpose/purpose-and-scope#claim-discipline` | Four live claim-discipline rules. The language parenthetical is a direct statement that canon is stale — see Contradictions |
| 172 | Sources — canon page ids used, plus the external "brought" citations | same `#sources` | delete | — | Bibliography; superseded by `corpus.json` and by row 170's provenance ledger |
| 173 | `STYLE.md` — the greyscale figure house style: exactly three stroke weights, three dash patterns with fixed meanings (incl. `1 2` = absent/refused), **"boxes are the failure mode of technical figures"** with the six-form enclosure vocabulary, and arrow geometry (curved = morphism, straight **mandatory** for BDD edges, Hasse diagrams, Merkle DAGs) | `journal/live/presentations/figures/STYLE.md` | mine | `practice/conventions#figure-style` | **Low confidence — flagged.** Every artifact it governs is deleted (D12/§9), so by the letter it is dead. But it is a self-contained, reusable design vocabulary with real content, and per the brief's tie-break I chose `mine` over `delete`. If Javier will never redraw a figure, this is a clean delete |

---

