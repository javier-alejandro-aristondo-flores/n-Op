# Merged — contradictions

Fragments: 11/11

### from `oracle-state`

— COLLECTED, NOT RESOLVED

| claim | source A | source B | nature of the conflict |
|---|---|---|---|
| What `unified-state` says about emergence | `multiscale-state:59-60`: "`unified-state` lists *defect populations* and *distributions* among quantities that are '**emergent — coarse-grainings of `x(t)`**' and therefore forbidden from the state" | `unified-state:46-62`: emergence is qualified "on the micro timescale and scale", and defect populations are explicitly named "**first-class state in their own tier, not emergent**" | A **dangling quotation**: the quoted string appears nowhere in `unified-state` — its only occurrence corpus-wide is inside this quotation of it. `multiscale-state §1` argues against a version of `unified-state` that no longer exists. Same defect *class* as R1 (a citation that resolves to a page which does not contain what the citation claims), in a new form — a quoted string rather than a promised table |
| Whether the Layer-1.25 validity radius is declared | `born-oppenheimer-levels:104-109`: "What is **NOT yet declared** is the validity radius … Until it is, the staleness term **has no bound**, and a composition cannot be refused for leaving the radius because no radius is stated. Tracked in `[open-decisions]`" | `open-decisions:113-121`: "~~No declared validity radius for frozen Layer-1.25 dressings.~~ **Closed 2026-07-21** — … the estimator *is* the radius … `OneShotCert` gains the coefficient field, and the product enters `combineTol` as the dressing-staleness term that already exists but **had no number in it**" | **Second realized instance of the register/page drift class (plan §2 defect 6).** The page states a gap the register says is closed, and cites that register as tracking it. Exactly what D7 makes unrepresentable. Note both sources agree the term exists in `combineTol`; they disagree only on whether it has a bound |
| Whether the four γ̂ questions were ever "the only open CS problems" | `journal/live/specs/2026-07-21-oracle-code-spec-research-brief.md:125`: the entry "quoted canon as calling these 'the only open CS problems in the design'" | `computational-overview §13` "explicitly disclaimed it" (per the same line) — the spec file records canon saying the opposite *at the time* | A self-reported mis-citation of canon inside an unswept stratum (`journal/live/specs/` has never been swept — salvage README). Registered because it is evidence that `gamma-hat §4`'s status was reported inconsistently across strata, not because the resolution is in doubt |
| Where the gauge conventions are recorded | `learnable-structure-requirements.md:27-31` (R1): emitted candidates must match "the gauge conventions **recorded there**" — pointing at `unified-state` | `unified-state:42-44` carries a parenthetical and explicitly defers: "normative gauge/partition paragraph in `generic-dynamics`"; the normative text is at `generic-dynamics:169-182` | Not a physics conflict — a **pointer conflict**. The seam requirement names the wrong owner. Registered so the builder retargets R1 rather than duplicating the paragraph onto `unified-state` |

---

### from `oracle-laws-seams`

— COLLECTED, NOT RESOLVED

| claim | source A | source B | nature of the conflict |
|---|---|---|---|
| What the curriculum fractions are fractions **of** | `3.2-residual-definitions.md:253` — "fraction ∈ [0, 1] of **total training budget**" | `informed-operator/design/training-stages.md:8,17` — the oracle attaches for one stage only; "curriculum gating **within this stage** follows [§4.1]" | If the oracle is absent from stages 1 and 3, a schedule indexed on *total* training would place Warmup and Cooldown outside the window in which any residual is evaluated. Either the denominator is the informed stage, or the stage ordering is wrong. Load-bearing: it determines when each category actually turns on. |
| The curriculum **phase names and count** | `3.2-residual-definitions.md:245-259` — four phases: Warmup / Refine / Polish / **Cooldown** | (i) `7.2-residual-machinery.md:218` — three: "Warmup → Refine → Polish"; (ii) `11.8-deriv-generator-catalog.md:332` — `curriculum-phase : Warmup \| Refine \| **Calibrate** \| Polish`; (iii) `informed-operator/design/residual-loss-methodology.md:199-205` — Warm-up / Refine / **Calibrate** / Polish | Three distinct vocabularies across four sources, two of them inside canon. The `11.8` enum is a *typed* disagreement, not prose, and `11.8` is being mined — mining it verbatim would import the wrong enum. |
| The `γ̂`-block of `L` | `3.1-generic-dynamics.md:90` — "Liouville–von Neumann on γ̂ `(1/iℏ)[Ĥ_KS, ·]`" | `3.1-generic-dynamics.md:134-139` — "written `[·, γ̂]` **not** the bare `[Ĥ_KS, ·]`" | Self-contradiction within one page: the operator table states the form the later section explicitly corrects. The correcting section gives the reason (Jacobi and degeneracy hold by construction only in Lie–Poisson form), so the table appears simply stale — but I am not ruling on it. |
| The `Validate` output types | `7.1-pino-bridge.md:39-48` — `residuals : Map<ResidualKey, Scalar>`, `values : Map<ObservableRef, Value>`, `cograds : Map<ResidualKey, Cotangent>`, `cert : CertEvidence` | `1.2-product.md:140-146` — `residuals : Map<SlotKey, Float>`, `values : Map<Name, Value>`, `cograds : optional gradient map`, **`kernel : content hash`** | Same signature, two type vocabularies, and a **different fourth output** (`cert : CertEvidence` vs `kernel : content hash`). `pino-bridge` is `canonical-for` "Validate signature", so `product` should be the derived copy — but the fourth field is not a renaming, it is a different value. |
| T2 residual cadence | `7.2-residual-machinery.md:298` — T2 is "**per-epoch cached** (offline reference cache per composition + `(T,P,q)` query)" | `informed-operator/design/residual-loss-methodology.md:167,353` — T2 is "**curriculum-scheduled**: first 30% of training without T2; introduce gradually" and "evaluated on a fraction of the batch chosen by largest-residual-first" | Two different policies for the same tier, and the second introduces a *third* schedule axis (a 30% cost-tier gate) alongside the category gate (0.10/0.60/0.90) and the source-weight schedule. Whether these compose or conflict is not stated anywhere. |
| Where the seam contract's requirements are canonical | `informed-operator/design/learnable-structure-requirements.md:3-6` — "Not part of the lint-enforced atomic tree; the atomic tree … is canonical for every shape, convention, and count cited here" | `7.1-pino-bridge.md:33-34` + `2.2-unified-state.md` — the canon it defers to does not contain the shapes, layouts, or units R1 defers to it for | The deference is circular in one direction: R1 says canon is canonical for the layouts; canon does not state them. Neither document is wrong on its own terms; together they leave the fact unowned. (Registered also as open question `state-array-layout`.) |

---

---

### from `oracle-compilation`

— COLLECTED, NOT RESOLVED

| claim | source A | source B | nature of the conflict |
|---|---|---|---|
| **C1** — How many compose-time stages are there? | `4.2:6` `canonical-for: 4+1 stage compose-time pipeline`; `architectural-principles:21`; `4.4:316` | `4.2:56` "runs in **five** stages plus the Stage-2.5 sub-stage"; `4.2:268-275` table enumerates **six** rows (1, 2, 2.5, 3, 4, 5); `10.3:27` also enumerates six under the "4+1" label | The canonical topic name states a count the page's own table contradicts. `4.2:114-116` gives the substantive defense — 2.5 is the constructive dual of stage 2, hence a sub-stage — but the name never absorbed it. **Nomenclature; propose, do not rename** (§4) |
| **C2** — Which section of `representation-substrate` resolves ε-equality? | `4.2:177` and `gamma-hat:136` cite `§20.4.1` for the exact-identity separation, which maps to today's `§4.1` | `gamma-hat:127-128`, `4.4:231`, `10.2:62`, `10.2:114`, `live/specs:97,125` all cite the pair `` `§4.1`/`§20.4.2` `` | `§20.x` is the **retired serial-derived coordinate** for `arch-20-representations` (`10.1:153`, `instructions.md:51`). Under the old numbering `§20.4.1 ↔ §4.1` and `§20.4.2 ↔ §4.2`, so the pair `§4.1`/`§20.4.2` names **two different sections as though they were one section under two numberings**. Half-migrated: someone renumbered `.1` and left `.2`. Full census in Notes |
| **C3** — Is `computational-overview` held to the same rules as canon? | `4.4:38` "the checkers hold it to the same citation rules as any other page" | Calibration: 95 of its 99 citations are backticked and therefore unchecked; its frontmatter does not parse as YAML; its `canonical-for` is vacuous | The page asserts a level of enforcement that does not exist for it |
| **C4** — Does `4.4` cite `arch-xx`/`impl-xx` sources? | `4.4:39` "Every claim cites its canonical `arch-xx` / `impl-xx` source" | Zero occurrences of either format in the page | Stale self-description; same retired-id class as `10.3:96` |
| **C5** — Is the implicit-diff adjoint `O(1)`? | `4.2:207-210` "one extra linear *system*, independent of forward iteration count — **not `O(1)` work**, since that system is itself solved iteratively" | `4.4:370-372` "**one linear solve** … independent of the forward iteration count" | The companion drops the qualification the canonical page was careful to add. Precision loss, arguably a false claim → auditor 2 |
| **C6** — Does `D4` mean *surrogate* or *relaxed*? | `4.4:436` "`D4` relaxed, e.g. log-sum-exp soft-hull / Gumbel-Softmax"; `cert-obligations:147` "`D4` relaxation validity" | `4.4:519` "surrogate-net validity (**D4 only**)"; `4.4:624` "surrogate-net build-vs-adopt"; `cert-obligations:65` "for D4 **surrogate** formulas"; `cert-obligations:178` "surrogate validity" | **New instances of plan §2 defect 6**, which was documented only at `10.2` item 1. The 2026-07-21 retag reached the tag table but not the cert obligations or the deferred-decisions list. `cert-obligations` contradicts itself internally (`:65` vs `:147`). Flagged to the certification surveyor |
| **C7** — Is the `InvariantTerm`/`FormulaApply` symbolic-form fiber part of C4? | `4.3:139-140` lists it as the row immediately after C4, unlabeled | `4.4:143` "a separate fiber, **not C4**" | Only the companion disambiguates. If 4.4 is deleted wholesale the ambiguity returns (row 53) |
| **C8** — Are the four `γ̂` questions closed? | `gamma-hat:123-130`, `4.4:229`, `live/specs:125` — closed | `10.3-audit-prompt:114` still instructs the auditor to examine "the `γ̂` §15.4 ε-equality **open problem**" | The audit prompt was not updated at closure, and cites a retired `§15.4` coordinate besides. `10.3` is another surveyor's scope; flagged |

---

---

### from `oracle-cert-accuracy`

— COLLECTED, NOT RESOLVED

| claim | source A | source B | nature of the conflict |
|---|---|---|---|
| What obligation 9 covers | `5.1-cert-obligations.md:65` and `5.4:30`, `4.4:519` — "for `D4` surrogate formulas" / "for D4: … surrogate uncertainty … refresh up to date" | `named-formulas.md:105-108` — "`D4` — relaxed. Genuinely non-smooth (argmin, hull, sort, discrete)"; registry: all six D4 rows are relaxations, the one learned surrogate (row 6) is `D2` | The obligation names a set that does not exist. **Also internal to 5.1**: line 65 says "D4 surrogate", line 147 says "`D4` relaxation validity" — one page, two meanings of the same tag |
| What lives in `physics/library/cert/reference-data/` | `5.1-cert-obligations.md:193-199` — obligations 4 and 8 read a `SqliteReferenceCache` at `physics/library/cert/reference-data/cache.sqlite` | `9.2-reference-battery.md:28-41, 115-117` — "Format is one CSV (or JSON) per sub-area; the cert layer reads them at obligation-check time"; the canonical directory listing contains no `cache.sqlite`; "the canonical archive stays CSV" | Two incompatible accounts of what obligation 8 reads and of that directory's contents. Neither page mentions an ingest step. On disk: 5 CSVs, no sqlite, no README |
| Which obligation carries the 3σ trip | `5.1:61-64` and `5.4:29` — the >3σ trip belongs to **obligation 8** (reference-battery-versioned) | `4.4-computational-overview.md:514` — "4 \| reference battery \| `SqliteReferenceCache` lookup, trip at `σ > 3`", i.e. **obligation 4** | The threshold is stated at five sites (`5.1:63`, `5.1:148` as `τ_battery`, `5.4:29`, `9.2:112`, `4.4:514`) and attributed to two different obligations |
| Which diff tags obligation 10 gates | `5.1:144` (`τ_adj` — "`D2` and `D3`") and `residual-machinery.md:176-186` ("`D2` **and** `D3` entries run a vJp-vs-JvP check") | `4.4-computational-overview.md:520` — "10 \| adjoint existence (**D2**, build-time)" | 4.4 omits `D3` from the gate. `residual-machinery §5` is emphatic that `D3` is not an exemption |
| Whether the reference-data directory contains a README | `9.2-reference-battery.md:32-33` — the tree lists `README.md  (this file)` | `ls physics/library/cert/reference-data/` — five CSVs, no README.md; and 9.2 is a book page at `journal/pages/09-…`, not that file | The page asserts its own location incorrectly. Unmigrated remnant of its pre-book life as a directory README |
| Which mechanism check (c) uses | `5.1:156-158` — "compares a **hard-coded list** of tolerance symbols" | `check_data_agreement.py:173-183` — "The symbol list is HARVESTED from the page that owns it, not restated here. It used to be nine symbols hard-coded in this file…" | Canon describes the pre-fix tool. The conclusion drawn from it (a tolerance with no row here is invisible) survives the correction; the mechanism description does not |
| How many CSVs the battery has | `9.2:31-41` — eight files listed as the directory contents | disk: `elastic-tensors`, `material-constants`, `phonon-frequencies`, `polarization-piezoelectric`, `transport-coefficients` (5) | 9.2:81-83 does say three are "pending", so this is reconcilable — recorded because the tree block reads as a description of what exists, and an agent reading it would look for files that are not there |

---

### from `oracle-registry`

— COLLECTED, NOT RESOLVED

| claim | source A | source B | nature of the conflict |
|---|---|---|---|
| Whether other documents restate the canonical counts | `6.1-canonical-vocabularies.md:44` — "Every other document references these numbers rather than restating them." | `1.5-rationale.md:75-77` (132 substantive, 12 methods +3 sub-methods, 20 templates, 11 bundles, 19 categories, 10 obligations) · `8.7-build-verification.md:35-36` · `4.4-computational-overview.md:432-435` · `4.1-physics-graph.md:174-183` · `journal/live/presentations/2026-07-22-prep.md:324-325` | The page's own governing rule is false of the corpus in both directions: at least five documents restate the numbers, and `6.1` itself restates eleven counts other pages own. Nothing enforces the rule |
| Whether a legend may be restated on `formula-registry` | `6.9-formula-registry.md:104-106` — "Restating it here is what let five incompatible legends coexist; this page now links rather than repeats." | `6.9-formula-registry.md:100-102` — the full `T0`/`T1`/`T2`/`T3` legend with all four timing bounds, verbatim from `6.6:59-61`, two lines earlier | The page states the anti-restatement rule for one tag column while violating it for another, in the same section. Probe B confirms the duplicate is unguarded |
| Whether coded-column vocabularies are harvested or restated in the checker | `check_data_agreement.py:436-437` — "The vocabularies are HARVESTED, never restated here — a second copy is the thing this tool exists to catch." | `check_data_agreement.py:460-462` — `('Tier', {'T0','T1','T2','T3'})`, `('Diff', {'D0','D1','D2','D3','D4','DN'})`, `('Path', {'cheap','faithful'})` hardcoded | Only `Bundle` is harvested. Three of four coded columns are exactly the second copy the comment forbids. If `6.6`'s legend changes, the checker keeps validating against the old vocabulary silently |
| Where cert obligation-7 / the cert obligations live | `6.5-property-templates.md:107` — "a cert obligation (§8 obligation-7)" | `6.2-typeclass-alphabet.md:51` — "Cert obligations (§12)" · `6.1-canonical-vocabularies.md:121-122` — "obligation-7 … §14" · `6.3-topology-atlas.md:40` — "`DiscreteStructure` instances (§10)" | Four bare ordinals in one chapter for two targets, all mutually inconsistent and none resolving to a page. Pre-book coordinates that rotted silently; `check_data_agreement.py:39-41` documents that bare-`§` resolution was deliberately removed from the checker |
| The name of the ν₀ harmonic prefactor | `6.8-typed-compositions.md:133-135` — `ν₀ = AlgebraicOf({…}, formula = harmonic-transition-rate-normalization)` | `6.8-typed-compositions.md:168` — `ν₀ = harmonic-rate-prefactor` | The same quantity carries two names on one page. Only the first is in `unregistered-formulas`; the second is invisible to the seam sweep because it is not in a `formula =` slot |
| Whether the named-formula vocabulary is closed | `6.1-canonical-vocabularies.md:54` — "Named formulas | 132 substantive (+2 architectural markers) | **yes**" (Closed? column) · `:128` — "Closed registry of typed, fully-parameterized algebraic formulas" | `6.8-typed-compositions.md:182-188` — eighteen invoked names are not registry rows, "so this page's closed-vocabulary claim does not yet hold in full" | `6.1` asserts closure without qualification; `6.8` declares the gap. Both are canon. Verified: all 18 absent from the CSV |
| How many live places use the `path` anchor class | `6.6-named-formulas.md:169-173` — "the anchor-class meaning is what the corpus actually uses the column for, **in four live places**… Retiring a field that **four pages** depend on…" | Sweep of `journal/pages/` for `cheap`/`faithful`: outside `6.6` and ch. 11 the pages are `6.9`, `9.1-accuracy-ledger`, `2.4-multiscale-state` — three. `7.3-cross-cutting-rules.md:51-54` defines the consistency pair but never uses `cheap`/`faithful`, and `5.1-cert-obligations.md:141-142` names the pair tolerances without the axis | An uncited count in prose that no page owns and no checker can verify. Which four were meant is not recoverable from the text |
| Which artifact wins a scope-vs-inventory disagreement | `6.10-properties.md:29` — "where they disagree, **the registry wins**" | `6.1-canonical-vocabularies.md:44` — the counts page is where numbers live and others reference it | Two different precedence rules for the same class of conflict, stated on two pages, neither citing the other. Mild, but the restructure has to pick one |

---

---

### from `program`

— COLLECTED, NOT RESOLVED

| claim | source A | source B | nature of the conflict |
|---|---|---|---|
| **C1 — What the operator does.** Completion vs time-evolution. | `1.1-purpose-and-scope.md:19-20`: the PINO "predicts the **time evolution** of the state of a crystalline material". Also `1.3-library-landscape.md:25-26`: `/informed-operator` "learns the **time-evolution operator**" | Javier: the operator returns the channels **not supplied**, given a topology plus known properties — completion. Consistent with `1.2-product.md:124-128`: "the missing pieces are precisely what the neural operator **learns to supply**" and `1.5-rationale.md:104-108` | Two incompatible readings of the operator's job, both in canon, one of them twice. Auditor 2 owns it. **Pages in my scope that state or depend on the framing:** `1.1` (rows 2, 7), `1.3` (row 55), `1.2` (rows 31, 44, 47), `1.4` (row 68), `1.5` (rows 81, 82, 94), `8.7` (row 142, curriculum gate). The oracle side is not in tension — `1.2:67-68` score-not-solve holds under either reading |
| **C2 — Is the evolver research commissioned or resolved?** | `1.2-product.md:242-251`: the scorer↔stepper duality research "**resolved** *survives with restrictions*"; the evolver is a flag-gated Stage-4 sibling artifact | `1.5-rationale.md:211-216`: "a live research question we are **commissioning as an independent deep-dive**. Until it resolves, time-evolution product verbs stay unclaimed" | Same research question, two mutually exclusive states, both present-tense in canon. `10.2-open-decisions.md:213-216` sides with `1.2` ("verdict: survives with restrictions"), making `1.5` the stale source — but `1.5` carries no marker saying so beyond its blanket "as of 2026-07-16" header |
| **C3 — Canonical topic count.** | `1.5-rationale.md:176`: "**99** canonical topics each owned exactly once" | `check_book_structure.py --check` at 2af93d2 prints "58 pages, **98** canonical topics"; my independent parse of all 58 pages' `canonical-for` blocks also yields 98 | Off by one in a page that presents the number as an audited fact. Trivial to fix, non-trivial that a self-description in canon disagrees with the tool that certifies it and no check compares them |
| **C4 — Is the `core ← shared` module chain retired or is it the build plan?** | `1.4-architectural-principles.md:24-26`: "The earlier `core ← shared ← …` import chain is **retired**: it described a module layout that no longer exists" | `8.5-build-order.md:19-20` steps 1–2 are `core` and `shared`; `8.6-build-sequence.md:35-36` phases 1–2 are "(`core`)" and "(`shared`)" | One page declares a naming scheme dead; two others build against it. Either the retirement is wrong or three pages name modules that do not exist. Note the retirement sentence is itself scaffolding (row 61), so resolving C4 cannot be done by deleting it |
| **C5 — The material anchor set.** | `1.1-purpose-and-scope.md:44-47`: c-BN, AlN, GaN, β-Ga₂O₃, AlGaN; contact metals W, Mo, Pt, Ti, Ni, Ta, TiN, WSi₂; substrates SiC, Si, sapphire; dielectrics Al₂O₃, HfO₂, AlN-as-dielectric | `11.5-deriv-high-field.md:26`: diamond, c-BN, **h-BN**, AlN, GaN, β-Ga₂O₃, AlGaN; refractory metals W, Mo, **Re, Ir**, TiN, **TaN**; dielectrics **SiO₂**, Al₂O₃, HfO₂ | Neither set is a subset of the other: A adds Pt/Ti/Ni/Ta/WSi₂ and the substrate class; B adds h-BN/Re/Ir/TaN/SiO₂ and omits substrates entirely. `1.1` owns "purpose" and should own scope; the appendix is being mined, so the merged set must be decided rather than inherited |
| **C6 — The MVP build order omits phases the MVP requires.** | `8.5-build-order.md:17`: "A focused subset of the phases in `build-sequence`", listing 9 steps; closing at `8.5:31-34` claims the result is "the concrete substrate `/informed-operator` then trains against" | `8.6-build-sequence.md:32-47` has 14 phases. `8.5` omits phase 6 (templates), 8 (GENERIC operators), 10 (observables), 12 (dynamics), and 13 (API seal + `pino-bridge`) — yet `8.3-mvp-scope.md:21` puts "10 of the 20" templates in the MVP, `8.3:24-26` puts five bundles in as primaries, and the exit criterion needs `Validate`, which phase 13 delivers | A subset relation asserted but never checked, and false as written. `8.7:80-86` gate 5 exercises `Validate` and `Import`, so the verification gates assume a phase the build order never builds. The merge in row 124 makes this class of error unrepresentable |
| **C7 — How many curriculum phases are there?** | `8.7-build-verification.md:70`: "a **three-phase** training run"; `7.2-residual-machinery.md:218`: "the current curriculum phase (Warmup → Refine → Polish)" — three | `11.8-deriv-generator-catalog.md:332`: `curriculum-phase : Warmup \| Refine \| Calibrate \| Polish` — four; `11.8:447` schedules SCPH refresh at "S5's Refine→Calibrate" boundary, a boundary that does not exist in the three-phase reading | A gate in `8.7` asserts a phase count that one page confirms and another contradicts. `11.8` is an appendix under the retired-legend warning, so the four-phase reading may be the stale one — but `11.8:447` and `11.8:571` schedule real work at the extra boundary, so it cannot simply be dropped when 11.8 is mined |

---

---

### from `practice`

— COLLECTED, NOT RESOLVED

| claim | source A | source B | nature of the conflict |
|---|---|---|---|
| How many probes `check_the_checkers.py` runs | `10.1-conventions.md:307` — "58 probes" (restamped by the checker; `probe_count()` returns 58) | `10.1-conventions.md:309` — "Two of the **52** plant a *location* rather than a corpus defect" | Two numbers for one quantity, eleven lines apart, in the page that owns the convention. `PROBE_COUNT_RE` at `check_book_structure.py:583` matches only a digit immediately followed by "probes", so the second number is never restamped and the corpus reports clean |
| Which open item the implementation-language decision is | `10.2-open-decisions.md:83-91` — the language item is numbered **6**; item 5 is the semiconductor-interface predicate, opened 2026-07-22 | `10.2-open-decisions.md:169`, `11.9-deriv-language-study.md:3,19,245`, `contents.md:154`, `index.md:37` — all say "open item **5**" | Off-by-one created when item 5 was inserted on 2026-07-22 and the ordinal citations were not swept. **This is defect 6's class realized a second time**, and one of the two sites is `10.2` citing itself. Nothing checks bare ordinals into a numbered list |
| Whether `journal/live/specs/` is checked | `10.1-conventions.md:56-62` — "**Frozen material is `journal/live/audits/` only.** … **`journal/live/specs/` is not frozen.** … Specs track current truth and carry a `## Changelog` like any other stratum" | `check_data_agreement.py:69-70` — "`journal/live/` is frozen work product: internal staleness there is by-design and exempt (**conventions, authority order**)" | The tool exempts the whole of `journal/live/` and cites `conventions` as its warrant; `conventions` says the opposite for half of it. `EDITABLE` at line 73 confirms neither `live/specs/` nor `live/audits/` is swept. Both containers are demolished by D13, but this is the exact "checker inherits the soundness of the invariant it is built on" failure of trap 69, in force right now |
| Whether `uwbg-observable-catalog` exists | `10.3-audit-prompt.md:96` — cites ` `uwbg-observable-catalog` ` as the target-observables source, in canon | `index.md` (98 topics) and `contents.md` (58 pages) — no such id | A nonexistent id sitting in canon. `check_book_structure.py`'s `REF_RE` matches only bracketed `[id]`, so the backticked form is unchecked (defect 1). The nearest real page is `deriv-observable-catalog`, which chapter 11 deletes |
| How many finding classes `check_data_agreement.py` has | `instructions.md:165` — "fails on **sixteen** classes", then enumerates ~14 and omits (n) coded registry columns and (o) table pipe counts entirely | `check_data_agreement.py:13-27` — **fifteen** lettered classes (a)–(o); `10.4-traps.md:503` — "**fourteen** finding classes" | Three numbers for one quantity, none of them 15 in the file an agent reads first. The count-check machinery covers six *vocabulary* phrasings and the probe count — not this |
| Where the Callaway-vs-BTE consistency-pair rule is enforced | `10.4-traps.md:216-218` — "enforced, `[cert-obligations]` §1.2" | `5.1-cert-obligations.md` §1.2 contains no "Callaway", "BTE", "consistency pair" or "excess"; it carries only `τ_equiv`/`τ_method` (lines 141–142). The rule is stated at `7.3-cross-cutting-rules.md:51-53`, `8.2-capability-slices.md:84`, `6.6-named-formulas.md:164` | A **dangling promise**: the citation resolves to a real page *and* a real heading, so the section-coordinate checker passes, but the claim is not there. Three other pages state the rule and none is cited. This is the same shape as seam requirement R1 |
| Where the mesh-σ floor gap is registered | `9.1-accuracy-ledger.md:58` — "It is named as an open item in `[open-decisions]` rather than filled in" | `10.2-open-decisions.md` — no mesh-σ item exists among the 6 numbered decisions or the 4 verifier-soundness gaps | A dangling promise **created by the repair of the pointer trap 61 is about**. The 2026-07-21 repair replaced an invented reconstruction with a forward reference to a register that does not carry it |
| What `physics/research/*` holds | `10.3-audit-prompt.md:87,126` — "Compare the `physics/research/*` ground truth against what landed in the committed spec" | `physics/research/` holds `diamond-defect-corpus/` and `diamond-stretch-and-skew-sweep/` — the VASP strain-sweep dataset and its documentation, 7 files | Worse than a dangling path: it **resolves to a real directory holding entirely different content**. An auditor following the instruction diffs canon against a strain-sweep dataset |
| Whether "52 observables" or "59" is the ledger count | `glossary.md:76` — "Catalog observables … the 52 observables of `[deriv-observable-catalog]` Part C — a frozen historical stratum count" | `glossary.md:77` — "Ledger-tracked observables … the 52 catalog observables (#1–52) plus later additions (#53–59)" | Two adjacent glossary rows define two counts for one word, with the disambiguation carried only by which two-word phrase precedes it. Flagged as a *readability* conflict for auditor 2: the corpus's answer is that both are correct and the term is overloaded |
| Where the log's full narrative lives | `10.5-timeline.md:41-43` — "Full narrative for every entry is in git history; this page keeps only what a future reader needs" | Plan §7 / D2 — `log/timeline.md` is the **sole** place research advancement is recorded, required by contract and subject to government audit | If git history is the narrative of record, the log is an index into it, not the record. If the log is the record, the sentence is false after cutover. **Javier must resolve this before the log schema is fixed** — it is the one decision a later checker run cannot repair |

---

### from `appendix-a`

— COLLECTED, NOT RESOLVED

| claim | source A | source B | nature of the conflict |
|---|---|---|---|
| The DFT stress sign convention and the flow `dh/dτ = −Ωσh^{−⊤}` | `11.1:144` (§1.5 stress row) | itself, `11.1:110-111` (§1.3) | **The line is textually corrupted and cannot be read.** Raw bytes: `the sign §1.3s $dh/d<TAB>au = -\Omega\sigma h^{-<TAB>op}$ requires`. Two `\t` escapes were expanded to literal tabs, destroying `\tau` and `\top`; `§1.3s` is unreadable (probably `§1.3's`). The *sign convention* is load-bearing and stated nowhere else. A **new corruption class** (LaTeX backslash-escape expansion), distinct from plan defect 8's find/replace collapse. Corpus-wide sweep: **this is the only occurrence** (`grep -rnP '\S\t'` over `journal/`, `physics/`, `informed-operator/` → 1 hit). Invisible to the checker (probe 4). |
| How sound velocity is computed, and whether it is direction-dependent | `11.1:317` (§2.5): `ρv² =` eigenvalue of the Christoffel matrix `Γ_ik(q̂) = C_ijkl q̂_j q̂_l` — **anisotropic, direction-argument required** | registry row 62 `sound-velocity-isotropic`, signature `(C_ij, ρ) → v_L, v_T` — **no direction argument**; and `9.1-accuracy-ledger.md:94` row 38 `sound_velocity v_s ±5%`, method "**Christoffel**" | Three sources, three positions: the appendix gives the anisotropic construction, the registry row is named and typed isotropic, and the ledger names the anisotropic method as the source of the isotropic row's tolerance. Either the row is misnamed, the ledger's method is loose, or an anisotropic row is missing. |
| Which averaging scheme backs the bulk/shear moduli | `11.1:314-315` (§2.5): `K` = Voigt **or Reuss / VRH**; `G` = Voigt / Reuss / **Hill** | `9.1-accuracy-ledger.md:95` row 37 `bulk/shear modulus B,G ±5%`, method "**Voigt average**" (single scheme) | The ledger pins one scheme; the appendix offers three. `typed-compositions:87-88` sides with the appendix and calls it unresolved: "averaging scheme (Voigt / Reuss / Hill) is an **open pick**". So the ledger's `±5%` is quoted against a scheme the registry has not chosen. |
| What SCF *is*, dynamically | `11.2:111-113` (§E.3): SCF is "**not a time dynamics** — a *fixed-point iteration*" (Pulay/Anderson/Broyden mixing) | `3.1-generic-dynamics.md:114`: the Electronic extraction is "**SCF as gradient flow** on `E_KS`" | A fixed-point iteration under density mixing is not a gradient flow of `E_KS` — the mixing schemes are quasi-Newton on the residual, not descent on the energy. Matters because `generic-dynamics` uses the gradient-flow reading to place Electronic inside the GENERIC `L`/`M` structure. `computational-methods:35-38` sides with the appendix (`SCF-mixing` and `Pulay-mixing` are `variational-minimization` sub-methods, not integrators). |
| Onsager reciprocity index placement under a magnetic field | `11.3:162` (§1.4): `L_αβ(B) = L_βα(−B)` | `11.3:198-199` (§1.6): `L(B) = Lᵀ(−B)` — and `11.3:677` (§4.5): `L_ij(B) = L_ji(−B)` | The same file states it three times with two index conventions (`αβ`/`ij` Cartesian-vs-flux indices). These are the same statement only if the index sets coincide, which the file never says. `residual-definitions:143` names "Onsager reciprocity" with no form at all, so canon inherits the ambiguity without a way to detect it. |
| Whether phonon/carrier distributions are emergent or first-class state | `11.1:686-690` (§S.4): continuum fields and reciprocal-space distributions are "**emergent representations** … not part of the irreducible state" | `unified-state:54-67` + `multiscale-state`: quantities **not recoverable on the micro timescale or scale** are "**first-class state in their own tier**, not emergent" | Canon has explicitly reconciled this (`unified-state:63-67` names it as reconciling "the earlier 'distributions are emergent' wording"), so this is a *superseded* appendix claim rather than a live disagreement — but it reads as flat contradiction to anyone opening 11.1 without `unified-state` in hand. Recorded so the mining pass does not carry the pre-refinement wording forward. Disposition A65 deletes it. |

---

---

### from `appendix-b`

— COLLECTED, NOT RESOLVED

| claim | source A | source B | nature of the conflict |
|---|---|---|---|
| **C1** Where the vacancy population lives in the state | `11.4-deriv-defects.md:77` — "the PINO should treat `[V_C]` as a state variable **`Z_I`** evolving by generation–annihilation kinetics" | `2.4-multiscale-state.md:145-152` — the slow fiber is a new C3 fiber promoting `SiteDecoration.occupancy`, "**`Z_I` stays immutable**; atomic-number identity does not change as a vacancy forms" | Direct opposition on which slot carries defect population. Canon is later and reasoned; the appendix reading is presumably superseded, but it is stated as present tense with no marker. |
| **C2** Diamond phosphorus donor level | `11.4-deriv-defects.md:110` — P at **0.57 eV** ("marginal n-type"), and `:144` builds the whole B.4 worked example on `E_d = 0.57` | `11.6-deriv-csp.md:204` — "P (`E_a` = **0.6 eV**, hard to incorporate substitutionally)" | Two pages in the same chapter, ~5% apart, on the MVP material's only candidate n-type dopant. Neither is in a reference-data CSV, so nothing arbitrates. |
| **C3** Diamond/metal thermal stability ceilings | `11.4-deriv-defects.md:382-393` §F.4 `T_max`: Mo ~500 °C · Au ~400 °C · Ti ~400 °C · Ni ~500 °C · Ta ~700 °C · Pt >800 °C · WSi₂ >700 °C | `11.6-deriv-csp.md:148-159` §C.3 `T_stable`: Mo "Good to 900 °C" · Au "up to ~600 °C" · Ti "TiC stable to 1500 °C" · Ni "Poor for chips" · Ta "Excellent to 1200 °C" · Pt "Excellent to 1100 °C" · WSi₂ "Good to 800 °C" | **7 of 10 rows disagree, some by 2–3×.** `11.6:143-146` explicitly declares "*Where a `T_stable` cell here disagreed with it, F.4 wins*" — but only the W row was converted to a citation; the other nine still carry independent values. A stated deference that was not applied. |
| **C4** Diamond MIGS slope parameter `S` | `11.4-deriv-defects.md:395` — `S ≈ 0.7–0.8` for diamond | `11.8-deriv-generator-catalog.md:493` — worked example uses `S ≈ 0.85`; flagged UNPINNED in `registry-manifest.csv:49` because the example evaluates to ~3.5–3.7 eV under every termination-tagged χ, not the ~4.5 eV stated | Row 48's only numeric `S` for the MVP material is stated two ways in one chapter, and the registry already records that the downstream example does not reproduce. (11.8 is outside my scope; flagged for whoever holds it.) |
| **C5** Diamond electron mobility | `11.5-deriv-high-field.md:49` — `μ_e ~2000 cm²/Vs (intrinsic)`, and `:86` scales the A.4 worked example from it | `transport-coefficients.csv` — `mobility-electron-tof, diamond, 300 K, **4500 cm2/Vs**, ×0.15, Isberg Science 297 1670 (2002)`; `9.1-accuracy-ledger.md:129` sets the μ design target at **factor-2** | 2.25× apart — outside the stated factor-2 target. The page's own header (`:21-24`) forbids seeding from its tables, but its worked example seeds from them. |
| **C6** Diamond hole vs electron saturation velocity | `11.5-deriv-high-field.md:49` — diamond row gives `μ_e` and `v_sat ~1.5×10⁷ cm/s` in the same row, and A.4 combines them | `transport-coefficients.csv` — `saturation-velocity, **diamond (holes)**, high field, 1.5e7 cm/s` | The seeded 1.5×10⁷ is explicitly a **hole** value; the appendix applies it to an electron mobility. Carrier-species mismatch in the corpus's only high-field worked example. |
| **C7** Thermomechanical fatigue, named twice | `11.4-deriv-defects.md:483` — Coffin–Manson `N_f = C(Δε_pl)^−β`, `β ≈ 2` ductile / `β ≈ 4` brittle | `11.5-deriv-high-field.md:441` — `plastic-strain-fatigue-life` `(Δε_p, ε_f, c) → CyclesToFailure` | The same physics proposed as two differently-parameterized formulas in two appendix pages; neither is registered, so nothing forced reconciliation. |
| **C8** GaN thermal conductivity at 300 K | `11.5-deriv-high-field.md:240` — GaN κ(300) = **130 W/m·K** | `transport-coefficients.csv` — `thermal-conductivity, GaN (wz a-axis), 300 K, **240 W/mK**` (3-ph; exp ~200, Zheng 2019); `9.1-accuracy-ledger.md:170` concurs | ~2× below both the first-principles and the measured canonical value. This table sits in the region the 2026-06-10 re-audit cleared "as sound" (`10.3-audit-prompt.md:130`). |
| **C9** Diamond κ(T) — monotonicity across sources | `11.5-deriv-high-field.md:237` — diamond κ(**900 K**) ≈ **400 W/m·K**, κ(1200 K) ≈ 280 | `transport-coefficients.csv` / `9.1-accuracy-ledger.md:169` — diamond κ(**1100 K**) = **450 W/m·K** | κ must fall with T, yet source A's 900 K value is **below** source B's 1100 K value. The two tables also use disjoint grids (600/900/1200 vs 300/773/1100), so neither can check the other. Separately, the page's own stated fit `n = 1.25` predicts 925 at 600 K against its own tabulated ~700. |
| **C10** β-Ga₂O₃ thermal conductivity — scalar vs tensor | `11.5-deriv-high-field.md:241` — a single "**β-Ga₂O₃ (⊥b)**" row: 11 / ~7 / ~5.5 / ~4.5 W/m·K | `9.1-accuracy-ledger.md:172,174-181` — `[010] 27.0, [100] 10.9, [001] ≈14`, ~2.5× anisotropy, plus the **monoclinic frame guard: "never relabel"**, four distinct axis systems in play | A ~2.5×-anisotropic tensor presented as one scalar under an axis label ("⊥b") that the guard does not admit. The corpus already enforces exactly this for the same material's critical field (trap 39: "an anisotropic triple, not a scalar"). |
| **C11** Chynoweth ↔ BTE residual kind | `11.5-deriv-high-field.md:213` — "**Algebraic-identities residual: method-equivalence** between Chynoweth and BTE in the high-field asymptotic regime" | `3.2-residual-definitions.md:155-164` — cheap-Chynoweth vs BTE/MC α is a **consistency pair**, not an equivalence pair: no agreement theorem exists, so it trips only on excess beyond a declared model-gap tolerance `τ_method` | Scored as an identity, a legitimate model gap would read as a bug. Canon's version is the refinement; the appendix states the pre-refinement reading in present tense. |
| **C12** β-Ga₂O₃ displacement threshold — circular provenance | `11.5-deriv-high-field.md:407` — `E_d`: Ga₂O₃ ~25 eV (no citation) | `material-constants.csv:42` — `displacement-threshold-Ed, β-Ga₂O₃, 25 eV, ±5, source: "**literature (carried from non-equilibrium stratum H.1**; multiscale-state §4)"` | The CSV — the corpus's canonical value store — cites the appendix page as its source, and the appendix cites nothing. When the page dies the value's only provenance is a pointer to a deleted file. This is the "provenance is a prior clearance rather than a source" case from my brief. Note diamond/GaN/AlN `E_d` have **no CSV row at all**. |
| **C13** GaN deformation potentials | `11.5-deriv-high-field.md:437` — GaN `Ξ_c ≈ −9 eV`, `Ξ_v ≈ +2 eV` (implying a gap deformation potential ≈ −11 eV) | `9.1-accuracy-ledger.md:208` — GaN gap deformation potential `a_V = **−7.6 eV**` (Rinke PRB 77, 2008) | ~45% apart if the conventions coincide. **They may not** — `a_V` and a `Ξ_c − Ξ_v` difference are not always the same object. Flagging rather than adjudicating; auditor 2 should establish the convention first. |
| **C14** State-vector verdicts for `T_L`, `E(r)`, `j(r)` | `11.5-deriv-high-field.md:552-561` — "**No additions to the unified state vector required.** All Part-A–I physics is emergent from the existing seven-tuple"; `T_L` and `E_field` "already in state", `j` "emergent" | `2.4-multiscale-state.md:284-298` — `(T_L, φ, n, p, j)` are **promoted to macro state** on the device mesh, "a new **scale**, not a new distribution" | Canon promoted three of the six quantities this section rules need no promotion. The appendix's headline conclusion is reversed by the page that cites it. |
| **C15** Registry-size and taxonomy counts stated as current | `11.5-deriv-high-field.md:480` — "Registry size grows from ~24 to ~42"; `:525` — "the existing **5** categories"; `11.6-deriv-csp.md:297,305` — "the current **5** categories… brings the count from 5 → 7" | `registry-manifest.csv` — **132 substantive rows**; `3.2-residual-definitions.md:59-62` — "**nineteen** categories" | Both pages carry superseded counts in present tense. `11.4:590` and `11.6:283-284` *do* flag their superseded registry counts; the residual-category counts are flagged nowhere, and `11.5`'s registry count is flagged nowhere. |
| **C16** Diamond vacancy formation energy | `11.4-deriv-defects.md:75` — `E_form(V_C⁰, intrinsic) ≈ **7.2 eV** (HSE06)`, restated in canon at `2.4-multiscale-state.md:172` | `11.6-deriv-csp.md:227` — "Vacancy V: `E_f ≈ **7 eV**` in pristine diamond" | Minor (~3%) and probably rounding, but it is the input to an exponential — `exp(−0.2/kT)` at 773 K is a factor of 20 in the population. Logged because C9 and C16 are the same class: the same quantity written twice, never cross-checked. |
| **C17** Chapter-11 differentiability tag for row 85 | `registry-manifest.csv:86` (row 85) — `structure-uniqueness-CSP`, Diff = **`D4`**, "Was `DX` — the tag now spelled `DN`… it is an MVP Cap-1 row and must carry a gradient" | `11.8-deriv-generator-catalog.md:166` — row 85 listed as Diff **`D3`** | The generator catalog carries the pre-retag tag under the retired legend. **Outside my scope** (11.8), flagged because row 85 is the subject of my Notes §C and whoever holds 11.8 needs it. |

---

---

### from `appendix-c`

— COLLECTED, NOT RESOLVED

| claim | source A | source B | nature of the conflict |
|---|---|---|---|
| C-1 · How many figures of merit are there? | "the **16 FoMs**" — `9.1-accuracy-ledger.md:110`; "the 52-observable (**+16 FoM**) catalog" — `4.4-computational-overview.md:605`; "the 52 (**+16 FoM**) observable catalog" — `10.3-audit-prompt.md:120` | `11.7#part-b` enumerates **15** rows | The count is asserted in three canon pages; the only enumeration anywhere has 15 entries. *(Possible reconciliation, not adjudicated: row 5 is "HMFOM / HCAFOM", which may name two distinct published FoMs in one row.)* |
| C-2 · What do the cost tiers T0–T3 mean? | **Wall-clock cost**: "T0 closed-form (≤10 µs) · T1 small linear algebra / 1D quadrature (≤10 ms) · T2 Brillouin-zone / mesh integral (≤10 s) · T3 self-consistent loop or PDE solve (≤10 min)" — `6.6-named-formulas.md:59-61` | **Scheduling cadence**: "T0 = per-step … T1 = per-batch … T2 = per-epoch (MLIP-bridge or surrogate-mediated) … T3 = on-demand (faithful DFT/MD/NEGF call, rare)" — `11.8:58-61` | Two different definitions of the same four tokens. They mostly co-vary, but they are not the same claim, and `SCPH-self-consistent-phonons` is where they part: `T2` (per-epoch surrogate refresh) in the appendix, **`T3`** (self-consistent loop) in the registry. |
| C-3 · What is bundle B10 called? | `static-validity` — `6.1-canonical-vocabularies.md:164`; `observable-bundles` ("B10 static-validity") | `structural-validity` — `11.8:54` | Bundle name divergence. Canon is self-consistent; the appendix is the outlier. Low stakes, but the appendix's bundle list is otherwise presented as authoritative. |
| C-4 · Is `Path` a runtime selector or an anchor class? | "**Anchor class**, `cheap` or `faithful` — what the row's value is trusted against, ***not* a runtime path selector** (every formula is on the single residual surface under the always-cheap pipeline)" — `6.9-formula-registry.md:94` | "`cheap` = goes into per-step loss; `faithful` = compared against DFT/experiment battery" — `11.8:76`; and §3.3's `step()` branches on it | The appendix reads `Path` as a live dispatch axis and builds a training loop on that reading. Canon says it is a provenance/anchor label. This affects how §3.3 (row 45) may be carried. |
| C-5 · Which `open-decisions` item is the language decision? | Item **6** — `10.2-open-decisions.md:83`. Item 5 is "A semiconductor-interface applicability predicate" (`10.2:73`) | "open item **5**" — `11.9` title/`id` line (`11.9:3`, `11.9:19`) **and** `10.2-open-decisions.md:169` ("the *picks* are open item 5 above") | A stale ordinal in two places, one of them **inside the owning canon page itself**. It resolves to a real item that is a different decision entirely — a dangling promise that no checker can see. |
| C-6 · Do the appendix row numbers match the registry? | "Row numbers `#N` are snapshot-local, **NOT** current registry numbers" — `11.8:20-21` | Mechanical diff: rows **1–87 match one-to-one by name**; only `#88`/`#89` diverge (→ registry 103/104) | The warning is over-broad in a way that is itself hazardous: a reader who verifies it and finds it false may then trust the whole table, including 88/89 — where the collision is real and silent. |

---

---

### from `strata`

— COLLECTED, NOT RESOLVED

| claim | source A | source B | nature of the conflict |
|---|---|---|---|
| What Handwerg 2015 measured | `…-seeding.md §6`: "Handwerg et al. (2015) — κ[010]=29±2, κ[100]=11±1 (concurring)" | `…-audit.md §2`: "Handwerg 2015 measured **[100] only**, κ = 13±1 — the spec's 'concurs (29±2 / 11±1)' matched **no** Handwerg value"; `…-seeding.md §2` carries the corrected form | **An audit fix applied in one section and not the other, inside the never-swept stratum.** §2's table was corrected; §6's source list still carries the refuted numbers. Same file |
| The year and volume of the Klimm thermal-conductivity tensor study | `…-seeding.md §6`: "Cryst. Res. Technol. (**2022**)" | `…-audit.md §2` and `…-seeding.md §2`: "Cryst. Res. Technol. **58** 2200204 (**2023**, not 2022), DOI 10.1002/crat.202200204" | Same failure mode as the row above, same file, same pass. Two of two spot-checks of §6 against §2 found a stale value — §6 was evidently never re-read after adjudication |
| Whether polarization bowing is invariant under reference-frame choice | `…-audit.md §1` holography cross-flag: "bowing is a curvature (second difference), **~invariant under reference choice**… converts cleanly — no tension" | `…-audit.md §4` G1 item 2: "its '~invariant under reference choice' premise is **unsound**" — the paper's own Figs. 5–6 show ZB-frame theory bowing with the *opposite* curvature sign to LH-frame theory and experiment | **The file contradicts itself.** §4 is the later, text-in-hand adjudication and §1 is the pre-read expectation, but both are stated in the present tense in a document being deleted. The §4 reading must be the one that survives |
| Whether the implementation language is decided | `README.md:100`: "**Implementation language — open.**"; `…-brief.md §2`: "treated as **OPEN**"; `…cs-framing-outline.md`: "canon marks it closed; that decision has been **reopened**"; `prep.md`: "Which language? Open" | `open-decisions` (per three independent citations above), which records the language decision as **closed** | Four documents state canon is stale on this point and none of them fixed canon. Matches the memory record (impl language REOPENED 2026-07-21) |
| The number of documented traps | `prep.md#questions-to-expect`: "a standing register of **51** documented traps"; `talk-script.md`: "**51** documented cases where care wasn't enough" | Plan §2 and `check_book_structure.py` check 10: **70** traps, numbering contiguous and machine-checked | A stale count in the unchecked stratum. The registry-count checks do not reach `journal/live/` (probe A), so nothing could have caught it |
| What the curriculum fractions `0.10 / 0.60 / 0.90` gate | `residual-definitions §4.1`: they gate which **residual categories** participate at each training fraction (Warmup: Conservation+Positivity only; Refine: add EOM/* and Algebraic/*; …) | `residual-loss-methodology.md` Part E / H.5: they are the boundaries of a **source-weight** curriculum (warm-up cheap-heavy → refine VASP+residual → calibrate experiment → polish GradNorm) | **Same three numbers, two different schedules.** `training-stages.md:5` asserts "canon owns the curriculum gating fractions", conflating them. Either they are one schedule described twice or two schedules that coincidentally share endpoints — unresolved |
| What `T0–T3` denotes | `residual-loss-methodology.md` Part D and canon's registry `Tier` column: **cost tier** (per-step / per-batch / per-epoch / on-demand) | `physics/research/…/03-what-is-derivable.md`: `T0–T6` = **model-dependence tier** (direct reads → meta-derivables) | A prefix collision of the same family as the §4 nomenclature defects. `T0`–`T3` are valid in both vocabularies with different meanings, and both are in scope for the new corpus |
| What the operator does | `README.md:3-5`: "predicts how the multiphysics state of a crystalline material **evolves in time**"; `purpose-and-scope` (per plan §12) | `prep.md` slide 2 and `talk-script.md` slide 2: "The operator does **channel completion**… it returns the ones you didn't [supply]"; `cs-framing-outline.md`: "the operator emits a state, the oracle scores it" | Plan §12 defers this to auditor 2. Recorded here because it is stated in the corpus **entry point**, which this restructure rewrites first |
| Whether the `D4` differentiability tag means "surrogate" | `residual-loss-methodology.md` Part C: `D4 — Non-differentiable`, and Part H.1's `{D0…D4}`; `cert-obligations` obligation 9: "surrogate-net validity (for `D4` surrogate formulas)"; `open-decisions` item 1: "the D4 surrogate formulas" | `named-formulas` (canonical since 2026-07-21): `D4` means **relaxed** — genuinely non-smooth, shipping a *named* relaxation; the six `D4` rows are relaxations, and the one actual learned surrogate is `D2` | Three documents still read `D4` as surrogate. This is plan §2 defect 6, and it reaches further than `open-decisions`: obligation 9's **checker body** describes a learned net |
| Which tree is canonical for shapes, conventions and counts | `learnable-structure-requirements.md:3-5`: "the atomic tree (`docs/architecture/*`, `docs/implementation/*`) is canonical" (reconstructed from `b88da0a`) | `journal/pages/` — a single tree; the `arch-`/`impl-` split was retired 2026-07-22 (`retired-ids.csv`) | A dead two-tree authority model, preserved because a find/replace corrupted the sentence instead of updating it |

---

